═══════════════════════════════════════════════════════════════════════════════
TASK
═══════════════════════════════════════════════════════════════════════════════

In the pursuit of high-quality code (low line count (e.g. low undue complexity)
and high feature/correctness) - refactor and fix ada83.c to pass a single
C-test group completely (e.g. cXX) with much better Haskell-like C99 and smart
code-golfing with, ironically, textbook "Literate Programming" style.

Do NOT skip around - when you encounter a complex problem look at it as an
opportunity to finally attack a big issue. Remember to follow the flow upstream
to avoid hacking up the expander.

Create a file to track everything called "checklist.txt" and include this
prompt in it.

═══════════════════════════════════════════════════════════════════════════════
GROUND RULES (from CLAUDE.md)
═══════════════════════════════════════════════════════════════════════════════
- Do The Right Thing: structural fix over hack, every time.
- Follow the flow UPSTREAM. A wrong value at codegen usually means a wrong
  decision at resolution/semantics. Fix it where it is first wrong.
- Never hack up the expander. Predicates over duplicated logic. No hardcoded
  "i8"/"i32" in emit code.
- Real parser for code search, not grep-as-final-answer.
- Landing constraint: chosen group 100% pass; no regressions in full Class C.

═══════════════════════════════════════════════════════════════════════════════
TARGET GROUP: c42  (13 tests: 11 pass, 2 fail, 0 skip)
═══════════════════════════════════════════════════════════════════════════════
Chosen because both failures share ONE upstream root cause (string-literal
bound determination, RM 4.2 -> 4.3.2), and the fix is a semantic determination,
not an expander hack. "Attack the big issue": how array-valued literals get
their bounds from context.

═══════════════════════════════════════════════════════════════════════════════
FAILURES IN TARGET GROUP
═══════════════════════════════════════════════════════════════════════════════
[ ] c42007e  string literal as left/right operand of "&" (catenation).
             CONCAT1 RETURN STR, STR index subtype STR_RANGE = 2..10.
             ("ABC" & (7..8=>'D')) must have 'FIRST = 2, 'LAST = 6.
             We currently produce 'FIRST = 1 -> all four bound checks fail.
[ ] c42007f  parenthesized string literal ("WHO") as an aggregate/record
             component expression. Index subtype SF1 = 2..6, so ("WHO")
             has bounds 2..4; component subtype is BASE(3..5); value 2..4
             does NOT belong to 3..5 -> CONSTRAINT_ERROR must be raised.
             We currently raise nothing (treat "WHO" as 3..5).

═══════════════════════════════════════════════════════════════════════════════
ROOT-CAUSE NOTES (upstream analysis)  -- RM 4.2, RM 4.3.2, RM 3.6.3
═══════════════════════════════════════════════════════════════════════════════
RM 4.2: a string literal's bounds are those of a positional array aggregate
        (4.3.2). Type is determined solely from context.
RM 4.3.2 (positional, no others): the lower bound is
   * the APPLICABLE INDEX CONSTRAINT's lower bound, if the aggregate/literal
     appears in one of contexts (a),(b),(c);
   * OTHERWISE  S'FIRST, where S is the array type's index SUBTYPE.
   The upper bound follows from the component count.
Contexts (a)-(c) (which supply an applicable index constraint):
   (a) actual param / generic actual / function result / RHS of ":=", AND the
       corresponding subtype is a CONSTRAINED array subtype;
   (b) operand of a qualified expression whose type mark is a constrained
       array subtype;
   (c) the expression of a component association of an enclosing (array or
       record) aggregate (multidim subaggregates inherit the context).
Parentheses BREAK contexts (a)-(c): a parenthesized primary is a general
   expression, so the string literal inside gets the "otherwise" rule
   (lower = S'FIRST). Its value is then checked against the surrounding
   subtype -> may raise CONSTRAINT_ERROR (this is c42007f).
RM 3.6.3 (catenation): result low bound = low bound of the left operand when
   the left operand is non-null; hence a "&"-operand string literal is NOT in
   a context (a)-(c), so it takes S'FIRST (this is c42007e).

Why siblings a-d,g-k already pass: they exercise contexts (a)-(c) where we
already take bounds from the target constraint. The hole is precisely the
"otherwise -> S'FIRST" branch, where we hardcode lower bound = 1.

CONFIRMED MECHANISM (traced through the compiler):
  Generate_String_Literal (ada83.c ~25315) ALREADY takes the index subtype's
  'FIRST when node->type is the applicable array type. The bug is upstream in
  RESOLUTION: the string-literal operand of "&" is committed to STRING before
  the STR context is known, so it never reaches that codegen branch.
    - Analyze_Expr eagerly resolves a bare NK_STRING to STRING (13712), unlike
      NK_AGGREGATE which is deferred as a flexible operand (13493).
    - The catenation codegen (27089+) already computes result bounds correctly
      (RM 4.5.3: result low = left operand's own low bound, null-left -> right).
      So fixing the operand's type is sufficient; codegen needs no change.

  c42007e FIX (done-ish): seed flexible concat operands (NK_STRING / aggregate)
    with Type_Base(ctx) in Resolve_Binary_Op, so they carry the applicable
    array type (index subtype -> 'FIRST). Concat operands are never in an
    applicable-index-constraint context (a)-(c), so the UNCONSTRAINED base is
    always right -> S'FIRST.

  c42007f FIX (harder): the parser DISCARDS parentheses around a non-aggregate
    primary (Parse: line ~6153 returns the inner expr; only NK_AGGREGATE gets
    is_parenthesized). So ("WHO") becomes bare NK_STRING and we lose the fact
    that it must use S'FIRST (parentheses remove it from context (c)). Fix:
    preserve parenthesization for string literals, and when propagating a
    context array type into a parenthesized literal, propagate the
    UNCONSTRAINED base so it takes S'FIRST; the existing belongs-to-subtype
    check then raises CONSTRAINT_ERROR (value 2..4 vs component subtype 3..5).

UNIFYING RULE: propagate the unconstrained base (-> S'FIRST) to a string
  literal / positional aggregate UNLESS it sits DIRECTLY (no parentheses, no
  "&") in an applicable-index-constraint context (a)-(c).

═══════════════════════════════════════════════════════════════════════════════
PROGRESS LOG
═══════════════════════════════════════════════════════════════════════════════
[DONE] c42007e — seed flexible catenation operands with Type_Base(ctx) in
       Resolve_Binary_Op so string literals / aggregates take the applicable
       array type -> index subtype 'FIRST. Also fixed an internal error on
       aggregate-only catenation. Committed 5557fe7.
[DONE] c42007f — three coordinated upstream fixes:
       1. Parser preserves parenthesization on string literals
          (string_val.is_parenthesized), mirroring aggregates.
       2. Generate_String_Literal: a parenthesized literal takes the index
          subtype's 'FIRST (named index subtype's first, else the constraint's
          own low bound for a directly-constrained array type).
       3. Array-aggregate component type propagation was MISSING NK_STRING, so
          string components defaulted to STRING (1..N). Added it — a real
          upstream bug that made siblings pass only by accident.
       4. New Emit_Array_Component_Belongs_Check (RM 4.3.2 / 3.6.1): an
          array-typed aggregate/record component must have bounds equal to the
          component subtype (no sliding). Added at the array path
          (Agg_Resolve_Elem) and the record path (Agg_Rec_Store).
       c42 now 13/13 (100%).
[GATE] Full Class C: PASSED. 1684 -> 1688 pass, 138 -> 135 fail, ZERO
       regressions. Per-test diff vs reference shows only improvements:
       c42007e, c42007f (targets), plus bonus c43205e and ce3116a.
       The belongs check was tuned to be correct AND safe:
         - static-bound component subtypes only (a dynamic bound like
           ARR(1..F) is re-evaluated and can differ from the value's own
           bounds -> would raise spuriously; fixed c36204a, c37003a);
         - null array subtypes exempt (a null value belongs to any null
           subtype, RM 3.6.1; fixed c36174a — ARRAY(..) OF STRING(1..0)).

RESULT: c42 group is 13/13 (100%). Two long-standing string-literal-bound
        bugs fixed at their source (resolution + a missing RM 4.3.2 runtime
        check), no expander hacks, net +4 suite passes.

═══════════════════════════════════════════════════════════════════════════════
SECOND GROUP (continue): c47  (12 tests: 11 pass, 0 fail, 1 N/A) -> COMPLETE
═══════════════════════════════════════════════════════════════════════════════
Target failure: c47009a — a qualified expression whose type mark is a
CONSTRAINED ACCESS subtype must raise CONSTRAINT_ERROR when a non-null
operand's designated object has index bounds / discriminant values that differ
from the access constraint (RM 3.7.2 / 4.7). The check was missing entirely.

Root cause & fix (upstream, reusing existing machinery):
  1. Generate_Qualified had scalar / record / constrained-array checks but none
     for a constrained ACCESS subtype. Added Emit_Access_Subtype_Constraint_
     Check — the value-carrying twin of the allocator's slot-based Emit_Deref_*
     checks: for access-to-unconstrained-array the operand is a fat pointer
     whose bounds are the designated object's (compare per dimension); for
     access-to-record the operand is a pointer to the designated record
     (compare discriminants). NULL satisfies any access constraint (RM 4.8(4)),
     so each check skips a null designator.
  2. ACC6 sub-case: the designated type was still the PRIVATE view (a subtype
     ACC6S IS ACC6(6) is legal in the visible part before the full type). The
     access-subtype-constraint builder required Type_Is_Record(designated) and
     skipped the private view, so no constraint was recorded. A discriminated
     private view already carries its known discriminant part (RM 7.4.1), so
     the builder and the new check now accept Type_Is_Private too.

Gate: full Class C — c47009a FAIL->PASS, zero real regressions (ce3116a is a
pre-existing environmental flake: it exhausts file descriptors probing the
implementation limit and fails on clean HEAD in isolation too).

The same missing check also affects TYPE CONVERSIONS to a constrained access
subtype (c46054a) — a different codegen path (not done here; c46 also carries
the independent hard failure c46022a, so the group is not completable now).

═══════════════════════════════════════════════════════════════════════════════
THIRD GROUP (continue): c67  (14 tests: 13 pass, 0 fail, 1 N/A) -> COMPLETE
═══════════════════════════════════════════════════════════════════════════════
Target failure: c67005b — "if equality is redefined for a scalar type, case
statements still use predefined equality". All three sub-failures were one root
cause: a comparison of a value of MY (TYPE MY IS NEW INTEGER) wrongly resolved
to a "=" redefined for INTEGER. MY is a DISTINCT type (RM 3.4) that keeps the
predefined equality inherited at the point of derivation; a "=" declared for
INTEGER after the derivation is not MY's.

Root cause: Type_Covers lets a type and a type derived from it cover each other
(so shared integer/enumeration literals resolve), and Type_Base collapses a
derived type onto its parent's representation. So the user INTEGER "=" matched
MY operands in overload resolution.

Fix: Named_Type walks the subtype (base_type) chain but stops at a derivation
boundary (parent_type set) — a subtype of INTEGER resolves to INTEGER's base,
while MY resolves to MY. Formal_Accepts_Actual matches a user operator's formal
to an actual by exact named type (universals still convert via Type_Covers).

Scope: applied to the EQUALITY operators ("=", "/=") only. parent_type is
overloaded — CALENDAR.TIME is represented THROUGH DURATION (TIME->parent_type =
DURATION), so treating them as distinct broke the CALENDAR "+"/"-" resolution
(c96005e) that relies on the loose coverage. Equality is exactly the RM 6.7
operator whose redefinition must not cross a derivation boundary, so scoping
there is correct and leaves arithmetic on the existing permissive path.

Gate: full Class C — c67005b FAIL->PASS, zero regressions (c97307a is the
known flaky tasking test).

═══════════════════════════════════════════════════════════════════════════════
FOURTH TARGET (continue): cd representation clauses — fixed-point 'SMALL cluster
═══════════════════════════════════════════════════════════════════════════════
Attacked the hardest cd sub-cluster: fixed-point types with user 'SMALL/'SIZE
clauses producing wrong arithmetic / spurious CONSTRAINT_ERROR. Three upstream
fixes, +7 tests (cd1009l, cd1c03f, cd2a53a, cd2a53b, cd2a54a, cd2d11a, cd2d13a),
zero regressions.

1. `for T'SMALL use <expr>` only honored a REAL literal — a named number
   (BASIC_SMALL : constant := 0.01) was ignored, so the type silently kept the
   delta-derived default. Use the folded value; record it on the base type
   (where Fixed_Repr_Small reads it).

2. The constant folder folded SIZE/FIRST/LAST/LENGTH/POS but NOT the fixed-point
   model attributes, so `for T'SMALL use S'SMALL` (and named numbers built from
   'SMALL) folded to NaN and set nothing. Fold 'DELTA/'SMALL/'LARGE/'SAFE_SMALL/
   'SAFE_LARGE via the existing evaluator — restricted to FIXED-point prefixes
   (folding FLOAT 'LARGE to a plain double loses the base-type accuracy that
   membership/equality rely on — c35712a).

3. Fixed-point constraint-check bounds were materialized at the object rep
   (i8 for 'SIZE 7), so a scaled bound like 8.0/(2**-4) = 128 wrapped to -128
   and raised CONSTRAINT_ERROR on an in-range value. Materialize in a width
   that holds the scaled mantissa (>= i64); the comparison already widens the
   value to match.

Remaining in this cluster (deeper, deferred): cd2a52e/cd2a52f/cd2a54e still fail
on 'LAST — they need the fixed-point ARITHMETIC width decoupled from the
SIZE-capped STORAGE width (the scaled 'LAST 128 overflows i8 in the comparison
operator too). Pervasive change across bounds/attributes/comparisons; separate
effort.

RECORD REPRESENTATION CLAUSES — investigated, reverted (no clean win yet):
The parser at Parse (record rep) PARSES the `RANGE first..last` of a component
clause and DISCARDS it (comment "bit_range is now part of the expression" is
false), and resolution only set byte_offset for an NK_INTEGER position, not a
folded expression. A fix that captures the range and folds the position via
Eval_Const_Numeric made cd4051b/cd4051d pass, but two blockers stopped a clean
landing:
  1. cd1009n/x/y/z position is `n * UNITS_PER_INTEGER`, a named number whose
     initializer is (INTEGER'SIZE + SYSTEM.STORAGE_UNIT - 1)/SYSTEM.STORAGE_UNIT.
     The folder can't evaluate it: named numbers store no precomputed value
     (frame_offset is 0), they re-evaluate their init, and SYSTEM.STORAGE_UNIT
     (ALI-loaded from rts/system.ads) does not fold. Needs a reliable
     named-number static value or foldable SYSTEM constants.
  2. Deriving bit_size from a sub-natural range denotes a PACKED bit field
     whose load/store codegen does not implement — forcing it corrupted values
     (cd1c03h). And even setting only byte_offset/bit_offset perturbed derived-
     record layout inheritance (cd1c03h regressed), unresolved.
Reverted to keep the tree regression-free. Proper record-rep support needs
named-number/SYSTEM folding AND bit-field load/store — a substantial feature.

RECORD REPRESENTATION CLAUSES — REVISITED AND LANDED (+6, zero regressions):
The earlier "named number won't fold" diagnosis was wrong. UPI folds fine; the
bug was that `(INTEGER'SIZE + SYSTEM.STORAGE_UNIT - 1) / SYSTEM.STORAGE_UNIT`
folded to 4.875 because the constant folder did FLOAT division — it only used
Ada integer division (RM 4.5.5) when Is_Integer_Expr held for both operands,
and INTEGER'SIZE (an attribute) has no structural integer case. So 2*UPI folded
to 9, corrupting every position. Fixes:
  1. The division fold now treats an operand as integer when Is_Integer_Expr
     OR its resolved type is integer/universal_integer (INTEGER'SIZE included).
     Scoped to the division fold, NOT the global predicate, so the subtype-
     bound folder at line ~15199 is unchanged and c42005a (null string at
     INTEGER'FIRST) keeps its runtime-raise path.
  2. Parser captures the component clause `RANGE f..l` (previously parsed and
     discarded) as choices[1].
  3. Resolution folds the AT position and first bit (RM 13.4) and applies
     byte_offset/bit_offset; bit_size is left natural (sub-natural = packed bit
     field, unimplemented, would corrupt values).
  4. The record's total size is RECOMPUTED from the explicit layout (furthest
     component end) — without it a moved component (E AT 3*UNITS) wrote out of
     bounds and derived types inherited a too-small size (cd1c03h).
Landed: cd1009n/x/y/z, cd4051b/c (+6). Still open in this cluster: cd4051d
(LAST_BIT needs bit-field bit_size), cd1c04e/cd1c03a (derived position/size
inheritance), cd4024b (fixed-point component clause), cd4031a. Packed bit-field
load/store remains the big missing feature.

DERIVED-TYPE SIZE-CLAUSE INHERITANCE — LANDED (+1 cd1c03a):
`for PARENT'SIZE use 5; type D is new PARENT;` made D'SIZE report 8 (byte
size) not 5. Root cause: the NK_DERIVED_TYPE resolution correctly copied
parent->specified_bit_size onto the anonymous `derived` Type_Info, but the
NK_TYPE_DECL handler that builds the NAMED first subtype from the definition
copied kind/size/alignment/bounds/base/parent and NOT specified_bit_size, so
the name D dropped the inherited clause and T'SIZE fell back to size*8. Fix:
copy specified_bit_size from def_type in NK_TYPE_DECL (RM 13.1). No-op unless a
SIZE clause is in play. Full Class C: +1, zero real regressions (the c97307a
flip was pre-existing tasking flakiness — fails 5/5 on the pre-change binary in
isolation too, PASS in one suite run was the fluke).

Other cd clusters still open: address clauses (cd5011g etc.), collection
STORAGE_SIZE (cd2b11d/e, cd7205c), derived SMALL inheritance (cd1c03e),
derived record POSITION/FIRST_BIT/LAST_BIT inheritance (cd1c04e — separate
record-layout mechanism, not the scalar 'SIZE path).

CATENATION ("&") RESOLUTION REWRITE — LANDED (+9, zero regressions):
The (component & component) form of RM 4.5.3 — `C1 & C2` yielding a one-
dimensional array whose type comes only from context — was unimplemented, and
two goblins masked it:
  1. Analyze_Binary's empty-set fallback fabricated a scalar interpretation from
     the LEFT operand (`is_cmp ? boolean : L->type`) whenever no operator interp
     was built. For (C,C) this handed back the component type, so the node was
     never seen as needing context and codegen aborted ("unhandled binary
     operator 33"). Fixed: skip that fallback for concatenation (`not is_concat`)
     so the interp set stays empty and the context pass owns it.
  2. The NK_APPLY `"&"(A,B)` path blindly defaulted the result to STRING,
     ignoring an array context already seeded on the node. Fixed: keep the array
     context when present; STRING only as the CHARACTER & CHARACTER default.
New: Resolve_Context_Catenation + Catenation_Operand_Target (static helpers by
Resolve_Operand). When Resolve_In_Context finds no interpretation for `&`, it
derives the result array type from the applicable context (Type_Base of the
context array, or STRING for a character operand with no array context), drives
each operand against the array or its component, and sets the node type to the
context subtype. The (array & *) forms are untouched — they always build an
interpretation and flow through the ordinary operand-propagation path.
Cleared the `&`-on-derived-array compile-skip cluster: c34005a/d/j, c45347a/c/d,
c64005c, cc3220a pass; c34005g now compiles and fails on a SEPARATE, pre-
existing slice bug (INCORRECT SLICE (VALUE)) — honest skip->fail, not a
regression. Class C 1704->1713, skips 153->144, FAIL unchanged.

OBJECT RENAMING (RM 8.5) — COMPOSITE-RENAME VALUE FIX LANDED (+1 c85005a):
An object rename captures its target's address once at elaboration into a hidden
pointer slot; reads go through Emit_Rename_Address. The value-read path
(Generate_Identifier) unconditionally did `load Type_To_Rep(type), addr` — right
for a scalar, but for a by-reference COMPOSITE (array/record) the rep is `ptr`
and the value of such an object IS its address, so the load read the array's
first word as a spurious pointer. Passing an array rename by reference as an
entry (task rendezvous) parameter then marshalled that garbage word as the
parameter address and the acceptor's copy-back memcpy'd through it -> SIGSEGV
(c85005a; native gdb backtrace pinned it in the task entry body). Fix: mirror
the ordinary SYMBOL_VARIABLE path — a constrained-array/record rename returns
the captured address without a load; only scalars/access load. Full Class C
1713->1714, zero regressions.
Still open in the cluster: c85006f/g — SLICE renames (`ADJECTIVES : STRING
renames S(10..24)`). The rename keeps the slice's underlying array type
(unconstrained), so 'FIRST/'LAST/'LENGTH read the base bounds (1..) instead of
the slice's (10..24) and indexing is off. Needs a constrained subtype built from
the slice's range for the rename symbol (RM 8.5: the renamed object's constraint
is used, not any type mark).

OTHER REMAINING LEVERS (from full-suite gap analysis):
  * Object renaming (RM 8.5) must yield a true alias (pointer to the renamed
    object), currently copies → segv/CE/wrong. Coherent set c85005a-e,
    c85006f/g (~7 tests).
  * Unconstrained-array positional aggregate bounds default to 1..INT_MAX
    ("runaway aggregate count=2147483647", ~ada83.c:35402) instead of the
    applicable index constraint — same RM 4.3.2 family already solved for
    string literals. c85006a-e (5 tests).
  * Large multi-fix areas (no single lever): generics (~29), TEXT_IO/*_IO RTS
    (~22), representation clauses (~18, packed bit-fields still the hard part),
    tasking (~19, some real, some delay-scaling flakiness).
