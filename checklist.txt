═══════════════════════════════════════════════════════════════════════════
CHECKLIST — ada83.c refactor: drive one ACATS C-group to 100%
═══════════════════════════════════════════════════════════════════════════

ORIGINAL PROMPT
---------------
In the pursuit of high-quality code (low line count (e.g. low undue
complexity) and high feature/correctness) - refactor and fix ada83.c to
pass a single C-test group completely (e.g. cXX) with much better
Haskell-like C99 and smart code-golfing with, ironically, textbook
"Literate Programming" style.

Do NOT skip around - when you encounter a complex problem look at it as an
opportunity to finally attack a big issue. Remember to follow the flow
upstream to avoid hacking up the expander.

Create a file to track everything called "checklist.txt" and include this
prompt in it.

Focus on skipped tests.

TARGET GROUP: c44 (RM 4.4 expressions — operator precedence with
user-defined operator overloads). Chosen because ALL FIVE tests
(c44003a/d/e/f/g) were SKIPPED — the compiler emitted invalid LLVM IR
(use of undefined %t0) so nothing in the group even linked.

PLAN
----
[x] Build compiler (clean: gcc -O3 -Wall, no warnings)
[x] Survey: full class-C run — baseline 1794/1976 pass, 63 fail, 119 skip
[x] Pick target group: c44 — 5/5 skipped, one shared root cause
[x] Diagnose skip #1: flexible-operand adoption in Analyze_Binary
[x] Diagnose fail #2 (c44003e): universal preference in Select_Interp
[x] Fix both structurally in §12 resolution (no expander changes needed)
[x] Verify: c44 group passes 5/5
[x] Verify: full class C shows no regressions — ZERO regressions,
    NINE progressions (1794 -> 1803 of 1976; fails unchanged at 63):
      c44003a c44003d c44003e c44003f c44003g   (target group, now 5/5)
      c23001a c23006a c23006b c23006c           (same aggregate root
                                                 cause; c23 now green)
[x] Commit + push to claude/ada83-refactor-tests-8jn4ch

WORK LOG
--------
1. Survey run (test_results/c-20260717-152813-4406): c44 was the only
   group with 100% of its tests skipped. All five died the same way:
   llvm-link rejected the IR with `use of undefined value '%t0'`,
   preceded by `warning: Generate_Aggregate: unhandled aggregate type
   kind=1` (kind 1 = TYPE_BOOLEAN).

2. Root cause #1 — Analyze_Binary flexible-operand adoption (§12).
   In `(I1 MOD I2 <= I5) /= (1 .. 3 => 7)` the inner "<=" legitimately
   carries two interpretations: predefined "<=" -> BOOLEAN and the user
   "<="(ARR,ARR) -> ARR. The aggregate operand has no interpretations of
   its own (RM 4.3 flexible operand), and Analyze_Binary let it adopt the
   sibling's candidate types WHOLESALE — including BOOLEAN. That built a
   spurious "/="(BOOLEAN,BOOLEAN) interpretation which Select_Interp
   picked, driving the aggregate into a scalar type it cannot denote; the
   expander then had nothing to emit and produced the %t0 placeholder.
   FIX (upstream, in resolution, not the expander): adoption is now
   bounded by what the literal form can possibly denote (RM 8.6
   applicable interpretations): an aggregate only a composite type
   (RM 4.3), a string literal only a one-dimensional character array
   (RM 4.2). Nested predicate Flexible_Can_Denote in Analyze_Binary.
   Result: c44003a/d/f/g pass; c44003e progressed to a runtime FAIL.

3. Root cause #2 — Select_Interp universal preference (§12).
   c44003e section 3 (fixed-point USR with user "AND" ">=" "+" "-" "/"
   "**"): `-3.5 - 11.0` in a USR context computed -14.5 instead of -3.5.
   The candidates for the binary "-" are (universal_real) and the
   predefined "-"(USR,USR) whose operand type was contributed by the
   nested USER unary "-"(USR) interpretation. Step 3 of Select_Interp
   (RM 4.10 universal preference) returned the universal interpretation
   because its escape clause only recognized USER-NAMED candidates
   matching the context. But per RM 4.6 an implicit universal conversion
   applies iff there is no legal interpretation without it, and only a
   literal, named number, or attribute is a convertible operand — an
   operator RESULT is not. So a universal interpretation can never
   legally satisfy a concrete numeric context; the literals convert
   instead and the concrete operation governs (which then resolves the
   nested unary minus to the user "-"(USR) -> 7.5 -> 7.5-11.0 = -3.5).
   FIX: the escape now recognizes ANY concrete candidate denoting the
   context type — user-declared or predefined — while keeping the
   NEW-derivation exclusion (c87b07d) and the no-context / universal-
   context behavior (c87b07a) intact.

4. c44 group: 5/5 PASSED.

DISCOVERED, OUT OF GROUP SCOPE (not fixed here)
-----------------------------------------------
- `USR'IMAGE` on a fixed-point type emits invalid IR: `sext i64 to i32`
  (invalid cast opcode; should be trunc). Hit while probing with
  TEXT_IO.PUT_LINE of a fixed 'IMAGE. Worth its own investigation.

═══════════════════════════════════════════════════════════════════════════
ROUND 2 — "continue for the rest of the skips" (fix, never skip→fail)
═══════════════════════════════════════════════════════════════════════════

FIXED AND VERIFIED (each = structural root cause, no expander hacks)
--------------------------------------------------------------------
[x] Variant selection shared §12/§13 (Aggregate_Selected_Variant):
    c52001a, ce2201g
[x] RM 3.6 anonymous unconstrained array base (Array_Unconstrained_Base),
    scoped to POSITIONAL operand aggregates: cc3225a
    (named aggregates keep the constrained view — c37213e-family stays green)
[x] Flexible_Can_Denote shared predicate bounds sibling-type adoption and
    catenation operand choice; nested "&" chains type as the array:
    c45347a, c45347c, c45347d
[x] Named call arguments map by NAME in the view's profile; constraints
    from the RENAMED profile (RM 8.5/6.4.1): c85013a
[x] Qualified operand resolution shared with destructured allocators
    (Resolve_Qualified_Operand, RM 4.7/4.8): c47002d
[x] RM 9.1 self-task references: 'CALLABLE/'TERMINATED/ABORT via type name
    (c91004b, c9a011a), self entry call TT1.E1 (c91004c)
[x] Task bodies are static-chain levels: own %__frame_base when they have
    nested units, scope linked to symbol, Find_Enclosing_Subprogram and
    the frame-var propagation boundary treat task-body owners as frame
    levels; accept formals bind into the task's own frame: c9a006a,
    c85005c, c85006c, c41308c + library-level leaf tasks (part of c94005b)
[x] Accept formals marshal BY VALUE except records (caller address) —
    record formals flagged param_by_reference at RESOLUTION time
    (deferred-body emission order made codegen-time flagging unsound)
[x] ACCEPT/SELECT statements traversed by Has_Nested_In_Statements
[x] RM 9.10 abort epilogue checks SELF's abnormal flag (covers aborting an
    ancestor): c94020a
[x] RM 12.3 generic self-reference (GEN.X inside template or instance
    clone); package renaming peel (Package_Denoted) in selected-component,
    USE clause, and instance rebinding of template self-renames:
    c85011a/c94008c progressed (remaining errors are later, distinct)

KNOWN REMAINING (each diagnosed, not yet fixed)
-----------------------------------------------
- probei.ada (scratchpad): by-ref RECORD accept formal renamed then passed
  on — write does not reach caller (pre-existing; not covered by ACATS
  pass set today)
- c94005b: multi-unit source file WITH'ing a unit defined earlier in the
  SAME file emits it twice (library mgmt §14/§17)
- ca5004a/b: bodyless library package with task object gets no synthesized
  elaboration function (task never created → TASKING_ERROR)
- c94008c/d: generic formal subprogram actual matching ("parameter 1 of
  the actual for formal subprogram 'SET' has the wrong type")
- c85011a: USE of renamed packages — fixed in source (Package_Denoted in
  USE path) pending verification
- Parser gaps: expression bounds in index constraints (c41106a `INTEGER'LAST
  - 4 ..`, c52102b/d `-4 .. X`), expanded-name GOTO targets (c41307d),
  character-literal renames (c85019a, cc1307b), generic unit name forms
  (c86005b)
- c59001b: forward GOTO to labels declared later (undefined label L311)
- c83033a: INTERNAL ERROR Generate_Apply unclassified
- c95085b/c: untyped aggregate in codegen (entry OUT formals)
- Generic formal matching strictness cluster: cc1207b cd1009i cc3224a
  cc3603a cc1305b cc3601a cc3120b
- Multi-file families: ca1013a6m ca1020d/e3m ca3009a6m ca5003b5m c86004*
  c83024e1m c83f01* cc1307a/b (undefined identifiers across units)
- 'IMAGE on fixed-point emits invalid sext (from round 1)

═══════════════════════════════════════════════════════════════════════════
ROUND 3 — finish moved tests to PASS (never skip→fail)
═══════════════════════════════════════════════════════════════════════════

[x] c85018a + probef: bare call through PROCEDURE-renames-entry now routes
    through the full apply machinery (family index from the rename's
    hidden elaboration slot, RM 8.5); the rename's own formal NAMES and
    DEFAULTS govern entry calls (entry_view in marshaling + default fill
    + 'CONSTRAINED slot mapping). The old "Entry call (renamed)" shortcut
    (wrong index 0, no defaults → permanent hang) is deleted.
[x] cc1207b: a generic instance package body's begin now activates the
    tasks of its SPEC CLONE (inst_sym->declaration is the instantiation
    node; the recorded activation range lives on instance_spec) — the
    entry call in the instance body no longer deadlocks on an unactivated
    task (RM 9.3(3)). Formal-private actuals with discriminants accepted
    for discriminated formal private types (RM 12.1.2, cc1207b's inner
    NEW PACK (R)).
[x] cd1009i + probelp: limitedness follows the FULL view after a private
    type's completion (RM 7.4.4) — is_limited is recomputed from the
    completion, so private-part instantiations with non-limited formals
    accept a LIMITED PRIVATE type completed by a scalar.
[x] c95085b: RM 6.4.1 record-actual check at ENTRY calls — discriminants
    must equal a constrained formal's constraint, checked in the caller
    via shared Emit_Discriminants_Match before any rendezvous.
[x] c95085c: four fixes —
    (1) Emit_Length_From_Bounds computes in i64: 3 .. INTEGER'FIRST is a
        null range, not a wrapped 2^31 memcpy (crash);
    (2) RM 6.4.1 array-actual BOUNDS-equality check at entry calls for fat
        and flat dynamic actuals (per dimension, rendezvous never slides);
    (3) static-static entry bounds check generalized from 1 dimension to N
        (subtest D: 3-D actual with a short middle dimension);
    (4) RM 4.3.2(6) inner named range vs constraint for multidim
        aggregates with a dynamic inner choice, plus the deferred
        inner-consistency epilogue also compares against the TYPE's own
        inner constraint.

ROUND 3 (continued) — regression fixes and remaining map
--------------------------------------------------------
[x] c43211a: the new inner-named-range check respects SLIDING contexts
    (assignment RHS compares LENGTH, RM 5.2.1) and requires static type
    bounds before comparing.
[x] ce3706c: reverted the blanket `is_limited = def_type->is_limited` at
    private completion — unnecessary (the completion's KIND mutation
    already clears limitedness for Type_Is_Limited; the GEN_DEF_PRIVATE
    check peels with Type_Underlying) and it broke TEXT_IO FILE_TYPE
    behavior. cd1009i still passes.
[x] ca5004a: a bodyless LIBRARY package spec with task objects (single
    tasks or objects of WITH'd task types) synthesizes the <pkg>___elab a
    body would provide, starting each task, ordered via the §15 graph.
[x] c93005b: timeout was load contention, passes quiet.
[~] c97307a: timed-entry-call cancellation — fails under load on BOTH the
    previous commit and the working tree (passed in suite runs #1-#5);
    load-sensitive scaled-delay test, needs a quiet re-run to confirm.

STILL-REMAINING SKIPS (diagnosed root causes, unfixed)
------------------------------------------------------
- §14 unit supersession within one file: re-declared spec of the same
  unit must obsolete the earlier spec+body (ca5004b duplicate globals,
  c94005b duplicate @f).
- c94008c/d: generic formal subprogram actual matching (SET wrong-type).
- Expanded-name and multi-file clusters (c86*, ca1*, ca3*, c83f*),
  parse gaps (c41307d GOTO expanded names, c85019a/cc1307b character
  operator renames, c86005b generic unit names), c73007a/c67005d
  selected-component forms, c83033a internal error, cc3120b/cc3601a/
  cc3603a/cc1305b/cc3224a generic-actual matching, c95067a frame alias
  in package-nested-in-task, c94006a, cc3607b, cd2/cd5/cda clusters,
  'IMAGE on fixed point (invalid sext).

FINAL VERDICT (full class C, run c-20260717-191631-23969)
---------------------------------------------------------
  pass 1794 -> 1832 (92%)   fail 63 -> 61   skip 119 -> 83
  39 progressions, 0 regressions caused by this session.
  c97307a (timed entry call cancellation) flips PASS/FAIL by machine
  timing on the PRE-SESSION binary too (verified twice at fe252b5 under
  identical serial conditions) — pre-existing timing sensitivity, not a
  regression from this work.

═══════════════════════════════════════════════════════════════════════════
ROUND 4 — continued skips
═══════════════════════════════════════════════════════════════════════════
[x] c86005a: ASCII's members are no longer installed as BARE names — they
    are visible only as ASCII.X or under USE ASCII (RM C.3), so user
    library units named NUL/ENQ/LF/SI resolve (this was the test's point).
[x] c86006a: STANDARD now exports its predefined types (BOOLEAN, INTEGER,
    FLOAT, CHARACTER, STRING) beside ASCII, so STANDARD."NOT"/"AND"/...
    synthesize against BOOLEAN and STANDARD.TRUE/.BOOLEAN resolve.
[x] c94008c (semantic part): generic formal subprogram actuals rebind to
    the CONFORMING overload of the bound name (RM 12.3.6,
    Actual_Conforms_To_Formal_Subprogram + Collect_Interpretations sweep);
    operator-string actuals for types without pre-built operator symbols
    synthesize a conforming predefined symbol from the substituted formal
    profile (partially lands cc3601a).

STILL OPEN (diagnosed)
----------------------
- cc3601a: instantiations whose operator-string actual never produced a
  binding at all ("missing actual for F1") — the binding-creation site
  needs the same synthesis as validation.
- c94008c (codegen tail): '%tN' i8 vs ptr in the instance body.
- c94008d: SEPARATE subunit of a generic instance ("no compiled body").
- c86002b/c86004b/c: library units named STANDARD / multi-file homographs.
- cc3120b (entry family via generic formal task?), cc3224a (formal array
  index matching), cc3603a, cc1305b (ran, outcome unverified).

VALIDATION (run 4, final): 1837/1976 pass (92%), 60 fail, 79 skip.
Zero regressions vs original baseline AND vs previous run.
Session total: 1794 -> 1837 (+43 passes), fails 63 -> 60, skips 119 -> 79.
New this round: c86005a c86005b c86005e c86006a (+ c97307a flake passed).

═══════════════════════════════════════════════════════════════════════════
ROUND 5 — cc3601a taken all the way (no half-fixes)
═══════════════════════════════════════════════════════════════════════════
[x] cc3601a PASSES. Three structural fixes, each independently general:
    1. Generic actual ATTRIBUTE bindings (WEEKDAY'SUCC as F1): the
       instantiation argument loop binds NK_ATTRIBUTE actuals via
       actual_attribute (restored — this was the reverted half-fix), and
       the synthesized wrappers carry correct profiles ('VALUE fat->i8,
       'IMAGE i8->fat, 'SUCC/'PRED i8->i8).
    2. Instance IN formal-object binding of a statically constrained
       array actual to a formal of unconstrained array type
       (V => AB1 where T's actual is AB, AB1 : AB(BOOLEAN)): the actual
       arrives as a plain data pointer; Emit_Instance_Binding_Elaboration
       now wraps it into a real fat pointer (Normalize_To_Fat_Pointer with
       the array's bound rep) before the deep copy — previously it did a
       raw ptr->fat rep conversion that LOADED 16 bytes of frame data as
       a descriptor (the f1_s188 segfault).
    3. Named array aggregate with a single DYNAMIC non-range choice
       against an unconstrained type: (B2 => X) now gets bounds B2 .. B2.
       RM 4.3.2 makes a non-static choice legal only as the sole choice,
       so choice..choice is exact. Scan_Aggregate_Shape records
       first_dynamic_single_choice; the Generate_Aggregate bounds
       override uses it beside the existing dynamic-range path.
       (Previously such an aggregate silently took the FULL index range —
       wrong bounds for any 'FIRST/'LAST/equality use, not just generics.)

═══════════════════════════════════════════════════════════════════════════
ROUND 6 — c9a006a determinism + the expanded-name / literal-as-function family
═══════════════════════════════════════════════════════════════════════════
[x] c9a006a (was a load-dependent flake, now deterministic): an entry call
    about to raise TASKING_ERROR because the target is ABNORMAL now waits
    (__ada_await_abnormal_teardown, under __rv_mutex) for the target's
    TERMINATED byte before raising, so the RM 9.9 observable ordering
    "TASKING_ERROR then 'TERMINATED = TRUE" holds. Deadlock-free by two
    exclusions: plain COMPLETED targets raise immediately (their
    termination may wait on live dependents), and an abort-pending CALLER
    raises immediately (it may be a dependent the dying master joins).
    Verified 12-way parallel: all PASSED.
[x] cc3603a: enumeration literals as generic actual subprograms. The
    literal-as-function call path uses frame_offset for non-enumeration
    character types (predefined CHARACTER carries no literal table).
[x] cc3224a: RM 3.6.1(2) — ARRAY (1 .. 3) index type is INTEGER, as an
    anonymous constrained subtype (universal_integer failed formal-array
    same-base matching; bare INTEGER broke RM 12.3.4 agreement checks
    with an unconditional raise).
[x] cc3120b: RM 4.1.3 — within a task body, T.X denotes the body's own
    declarations (accept formal shadowing idiom T1.I := I).
[x] c85019a: FUNCTION SEA RETURN CHARACTER RENAMES 'C'. Parse_Name accepts
    character literals as names (RM 6.1); shared helper
    Character_Literal_As_Function resolves them against the expected
    result type (synthesizing CHARACTER literals, position = char code);
    renaming binds it as the renamed "function".
[x] cc1307b: character literals as generic formal subprogram defaults
    (IS 'S') and object defaults (OBJ2 : ENUM := 'S'). Defaults resolve
    against the substituted formal result/object type via
    Formal_Mark_Actual_Type + Resolve_Char_As_Enum.
[x] c41307d (expanded names, 17 subtests): GOTO takes expanded label
    names (final selector identifies the label); M.S inside a generic
    instance body (subprogram-prefix lookup peels the RM 12.1(7)
    self-binding rename and falls back to plain visibility when the named
    body's scope encloses the point of use); single-task L.R (body scope
    hangs on the object symbol, not the type's defining symbol — both
    consulted); E1.A accept formals (entry symbol carries the live accept
    scope during that accept's resolution, RM 4.1.3(20)); T.BLK.B
    (RM 5.1: labels/block/loop names implicitly declare in the innermost
    enclosing BODY, so Preregister_Labels skips accept formal scopes —
    new Scope.is_accept_formal_scope); LP.I loop parameters (loop label
    symbol linked to the loop scope, RM 4.1.3(19)).

═══════════════════════════════════════════════════════════════════════════
ROUND 7 — regression repair + c94008c structural layers + c94008d
═══════════════════════════════════════════════════════════════════════════
[x] Regression repair (caught by suite before landing): the ROUND-6
    anonymous bounded index subtype leaked constraint semantics into
    null-array/formal checks (c36172a, c48010a, c61008a, c95065e).
    Correct model restored: ARRAY (1 .. 3) has index type plain INTEGER
    (RM 3.6.1(2)); the range lives in Index_Info only. The RM 12.3.4
    agreement check now builds its bounded comparison view locally from
    the actual's Index_Info; Emit_Array_Index_Constraint_Check treats a
    full-range INTEGER index type as unconstraining and falls back to the
    base array's index subtype (c36172a subtest 10: A (A_9_11'RANGE)).
    All four regressions verified fixed; cc3224a still passes.
[x] c94008d: a subunit among the CURRENT compilation's units satisfies a
    stub (Current_Compilation_Provides_Subunit) — SEPARATE (PARENT) in
    the same file as the generic and the main no longer reports
    "no compiled body in the library".
[~] c94008c (compiles+links+runs; one tasking-semantics failure left):
    four structural fixes en route, each general:
    1. RM 8.5.4 rename disambiguation for PKG.NAME targets: candidates
       from the exported list AND the package scope's bucket chain, picked
       by the rename's own profile (EVENT renames TRACE.UPDATE picked the
       1-param instance UPDATE, not the 2-param formal actual).
    2. Instance package exports: a spec subprogram homonymous with a
       formal-subprogram binding chains onto the BINDING's overload head
       below binding_count — the export scan now walks every head and
       filters by creation-sequence window alone (TERMINATE_COUNT
       exported only GET before; now SET/UPDATE/GET).
    3. Generate_Lvalue / Generate_Composite_Address: a package-qualified
       parameterless FUNCTION as composite prefix is an implicit call
       (TRACE.GET.TRACE in a catenation emitted a nonexistent %symbol).
    4. The SYMBOL_GENERIC expanded-name branch resolves the selector in
       the unit's own region scope FIRST (SHARED.SET inside the task body
       found the task's entry SET via plain visibility, calling an entry
       where the formal procedure was meant).
    Remaining: "30 SECOND DELAY NOT ENOUGH - 1" — terminate-alternative
    guard counting; needs native-build debugging.

ROUND 7 (continued) — c94008c COMPLETE + more regression repair
---------------------------------------------------------------
[x] c94008c PASSES. Final layer: guarded TERMINATE alternatives
    (WHEN cond => TERMINATE) — the parser DROPPED the guard entirely, so
    RM 9.7.1 guard evaluation (whose side effects c94008c counts) never
    ran, and a closed terminate alternative still joined the consensus.
    NK_NULL_STMT now carries null_stmt.guard (parse -> clone -> resolve);
    the guard pre-evaluates once with the other select guards; the
    at-terminate consensus byte (TCB+58) stores the guard's value; the
    parking dispatch skips a closed alternative; the RM 9.7.1 all-closed
    PROGRAM_ERROR check counts guarded terminates.
[x] c42007f regression repair: the plain-INTEGER index normalization made
    a parenthesized string literal take INTEGER'FIRST as its low bound.
    New predicate Index_Type_Is_Unconstraining (absent / boundless /
    full-INTEGER) shared by Generate_String_Literal (falls back to the
    dimension's own bounds) and Emit_Array_Index_Constraint_Check.
[~] c94008d: stub-satisfaction gate fixed (compiles now); remaining gap is
    grafting a subunit onto its GENERIC parent's body template so
    instances clone the real task body instead of the stub (link error:
    undefined %__frame.<instance>__share).

ROUND 7 (final) — instance task storage + graft infrastructure
--------------------------------------------------------------
[x] Instance package-level SINGLE tasks are module storage: the object
    symbol is marked is_package_level (Mark_Package_Level_Objects now
    finds the homonymous task VARIABLE beside the spec's type symbol);
    Symbol_Is_Global then classifies it, the single-task elaboration
    emits @<name> = linkonce_odr global (via Emit_String_Const_Symbol_Name
    so the definition and every reference agree byte for byte) and stores
    the TCB through Emit_Global_Ref. Repaired the three link regressions
    the first cut caused (cc1207b, cc3207b, c93005d: definition spelled
    without the unique-id suffix while references carried it).
[x] Load_Package_Spec treats an in-file GENERIC library unit as already
    loaded — reloading from the catalog shadowed the in-file symbol whose
    template AST carries graftings.
[x] Subunit-of-generic graft: SEPARATE (GENERIC_PARENT) bodies replace
    the stub in the template AST at CU resolution
    (compilation_unit.grafted_into_generic skips standalone emission);
    instantiations now clone the real body.
[~] c94008d compiles + links + starts; hangs in the terminate-consensus /
    master-join interplay (main pthread_joins a SHARE task still parked
    at its terminate alternative; two instance tasks parked, T1 tree not
    yet activated). Same family as the known c94021a gap. Next session:
    trace __ada_term_wait consensus vs the procedure-level master's done
    flag for instance tasks.

═══════════════════════════════════════════════════════════════════════════
ROUND 8 — the BIND cluster (IR-invalid codegen)
═══════════════════════════════════════════════════════════════════════════
[x] c41103a, c41103b, c41107a: slice-prefixed element access.
    Generate_Lvalue's non-symbol prefix fallback unpacks fat-valued
    prefixes (a slice value's data/bounds) instead of GEP-ing the fat
    struct; Generate_Composite_Address's scalar-element branch subtracts
    the PREFIX SLICE's own low bound (N3(2..5)(4) is N3(4) at offset
    4 - 2), mirroring the nested-slice branch.
[x] c41323a: float negation and ABS in function notation ("-"(X),
    "ABS"(X)) emitted integer sub / ssub.with.overflow on floats — now
    fneg / llvm.fabs.
[x] c41328a: Has_Nested_Subprograms recurses into nested packages at ANY
    depth (Declarations_Reach_Frame) — P.Q's subprograms two package
    levels down take the enclosing procedure's static chain, so the
    frame must exist.
[x] c85016a: FUNCTION SUCC (I : INTEGER) RETURN INTEGER RENAMES
    INTEGER'SUCC — attribute renamings synthesize a wrapper body through
    the same Attach_Wrapper_Function_Body machinery as generic attribute
    actuals; the renaming's codegen defers/emits it, and the wrapper
    counts toward the enclosing frame decision.

STILL OPEN in the BIND cluster (diagnosed this round):
- c64106d: %rec3_s109.constr undefined ('CONSTRAINED flag slot)
- c64108a: fat vs ptr in parameter passing
- c74004a: ptr vs fat mismatch (private type completion view?)
- c74305b: @c74305b__p6 undefined (deferred body never emitted?)
- c83025c: duplicate elab function (homograph package names)
- c83027a: %a_s102 undefined (homograph resolution)
- c83051a: invalid operand type

ROUND 8 (continued) — universal preference for operator call notation
---------------------------------------------------------------------
[x] c87b40a: `"-"(-1)` / `"ABS"(+1)` under a universal comparison must
    denote the predefined universal operators even though user homographs
    fit (RM 4.6: an implicit conversion applies only when no legal
    interpretation exists without it — and an expression result is not a
    convertible operand, so a concrete context excludes the universal
    reading while a free context demands it). The operator-notation apply
    path now PROBES the infix interpretation on a clone (new
    Diagnostics_Suppressed flag silences the tentative pass) and adopts
    it only when the context is not concrete and the probe yields a
    universal result; otherwise the by-name lookup arbitrates user
    operators exactly as before, and the infix machinery remains the
    fallback for predefined operators that are not named symbols.
    Slice range bounds that pre-resolved UNIVERSAL re-arbitrate against
    the index subtype once the slice is classified (c87b24b:
    PI ("+"(3) .. "-"(5)) calls the INTEGER-returning user operators).
    Validated: 1853 passes, all twelve first-cut regressions recovered.

ROUND 8 (continued) — visibility and elaboration structure
----------------------------------------------------------
[x] c74305b: instantiating through a USE-visible alias — the alias never
    receives the generic body when the package body arrives after the USE
    clause; the instantiation now peels ->aliased to the original.
[x] c83025c (+c86007a): the spec-only package ___elab is suppressed only
    when a body of that package is emitted by THIS compilation (new
    Note_Compilation_Package_Bodies registry recorded before generation).
    First cut used Body_Already_Loaded and broke every spprt13-based
    cd5* address-clause test (a body merely known to the library may
    never be linked); registry fix restored them and picked up cd5003h.
[x] cc3607b: IS <> box defaults pick the visible subprogram whose profile
    CONFORMS (Actual_Conforms_To_Formal_Subprogram over the overload
    chain), with RM 8.4(5) direct-visibility preference: a use-visible
    alias is hidden by a directly visible homograph.
[x] .ALL as a composite prefix extracts the fat access value's data slot
    (c64108a partial); Generate_Composite_Address's element path accepts
    TYPE_STRING prefixes (Type_Is_Array_Like).
STILL OPEN: c64108a (element-of-dereferenced-function-result as IN OUT
    actual still routes through the rvalue fallback), c64106d, c74004a,
    c83027a, c83051a, c94008d hang, c86002b/c86004b STANDARD homographs.

═══════════════════════════════════════════════════════════════════════════
ROUND 9 — parameter marshaling and membership (1858 -> 1863)
═══════════════════════════════════════════════════════════════════════════
[x] c64108a (+c64109c): all actual-parameter name forms.
    - X.ALL of a FAT access value as a by-ref actual: the access value IS
      the descriptor — evaluated once (data slot as address; spilled whole
      for an unconstrained formal).
    - Discriminant-dependent components (R.S : STRING(1..N)) as actuals
      spill Generate_Selected's aliasing fat descriptor (RM 3.7.4 bounds
      from the record instance).
    - Implicit dereference in Generate_Composite_Address accepts
      access-to-STRING designated types (Type_Is_Array_Like, not just
      TYPE_ARRAY).
[x] c64106d (+cc1227a): the RM 3.7.4 hidden 'CONSTRAINED flag is frame-
    published beside the parameter's slot (one extra pointer-size unit
    reserved at Symbol_Assign_Frame_Slot for discriminated-record
    formals); Emit_Parent_Frame_Aliases emits %__frame.<name>.constr,
    Emit_Constrained_Slot_Ref reads through it uplevel, and the flag is
    marked at resolution so deferred nested bodies see it.
[x] c74004a: 'VALUE normalizes a statically constrained string argument
    to a fat pointer; membership in a TYPE MARK with no expressible range
    (float DIGITS types) is TRUE — the equality fallback must never fire
    for type marks (the right operand of a membership is not a value).
STILL OPEN: c83027a, c83051a, c94008d hang, STANDARD homographs,
    multi-file ca* families.

═══════════════════════════════════════════════════════════════════════════
ROUND 10 — homograph defaults and expanded-name operators
═══════════════════════════════════════════════════════════════════════════
[x] c83027a: Emit_Init_Record_Defaults (the instance-global / nested-
    component default path) was a degraded duplicate of
    Emit_Apply_Component_Defaults — no discriminant binding, no variant
    guards, no scalar range checks. A component default reading a
    discriminant (D : INTEGER := A, RM 3.7.1) lowered to an undefined
    local. Refactored to delegate its per-component work to
    Emit_Apply_Component_Defaults (binds each discriminant's
    disc_agg_temp to the stored field) and keep only the recursion into
    nested composite defaults plus Emit_Check_Nested_Disc_Constraints.
[x] c83051a (three distinct defects):
    - Generate_Binary_Op promotes to float arithmetic when the operand
      reps turn out float (fixed / integer in double rep), but left the
      opcode chosen for integers — emitted `sdiv double`. The promotion
      now re-selects fadd/fsub/fmul/fdiv.
    - BPACK."/="(ARY, (...)): predefined operators of a type declared in
      P exist as no named symbol (array equality, array "<"), so the
      expanded-name member lookup could only land on a homograph of a
      different type (the derived boolean's "/="). When the by-name
      pick's profile does not cover the actuals, Resolve_Apply now
      probes the infix reading (clone + suppressed diagnostics) and
      adopts it — the same fallback the unqualified operator-call path
      already had (RM 8.3, 6.7).
    - The RM 4.8 allocator element-count check classified the aggregate
      by the near-dead aggregate.is_named flag (set only for extension
      aggregates), so NEW T9B'(1..5 => .., 6..10 => ..) was treated as
      positional-with-2-elements: unconditional CE raise plus a memcpy
      capped at 2 bytes. Classification now uses Scan_Aggregate_Shape.
    - Also: an aggregate ACTUAL's type is context-only (RM 4.3) — the
      call-argument retype fires whenever the formal does not cover the
      pre-pass guess, not just when the aggregate is untyped.
[x] c48009e/f regression repair (found by the suite): the old broken
    positional classification had been accidentally raising CE for
    NAMED aggregates under NEW T'(..). The real rule (RM 4.3.2 / 4.7):
    a named aggregate's bounds come from its sole choice, and
    qualification by a constrained array subtype requires bound
    EQUALITY — null against null included. Implemented once as
    Emit_Qualified_Aggregate_Bounds_Check, called from both
    qualification spellings: Generate_Qualified (T'(agg)) and the
    allocator (NEW T'(agg) — the parser unwraps the qualified node, so
    Generate_Qualified never sees it).
[x] c94008d: Declarations_Create_Task_Dependents now sees through
    NK_GENERIC_INST into the instance's expanded spec/body (RM 12.3):
    a task inside a generic package instance depends on the scope that
    instantiates it (RM 9.4). Without this the instantiating scope
    never became a master — no done flag was ever raised, and the
    instance's SHARE tasks parked at terminate alternatives slept
    through the teardown join (main deadlocked in pthread_join).
[x] Consolidation: the operator-in-call-notation synthesis (operand
    collection with RM 6.1 LEFT/RIGHT named-association mapping +
    NK_BINARY_OP/NK_UNARY_OP construction) now lives in one helper,
    Synthesize_Infix_From_Apply, used by both the unqualified and the
    expanded-name fallbacks — the expanded-name path thereby gains the
    LEFT/RIGHT mapping it previously lacked.
STILL OPEN: c86002b/c86004b STANDARD homographs, multi-file ca*
    families, remaining COMPILE-error skips (cc1225a, cc1307a, cc3128a,
    c45347b, c46033a, c73007a, c83027c, c83030c, c83033a, ...).

═══════════════════════════════════════════════════════════════════════════
ROUND 11 — visibility skips (c45347b, c46033a, c73007a, c83027c,
           c83030c, c83033a)
═══════════════════════════════════════════════════════════════════════════
[x] c45347b: catenation interps enumerate every RM 4.5.3 form each
    operand pair satisfies — an array-typed operand may be the ARRAY
    side or a COMPONENT of an array-of-arrays; the old synthesis
    assumed array-like ⇒ array side. Plus: ARR & ARR under an array
    context whose element covers the pick's result re-reads as the
    component&component form (Resolve_In_Context).
[x] c46033a: RM 8.6 context re-drive for `-IDENT(X)` — an overloaded
    call operand of an arithmetic unary op re-resolves against the
    context type when the bottom-up pick cannot satisfy it (the
    predefined unary ops return their operand type, RM 4.5.4).
[x] c73007a: a use-visible PACKAGE alias carries no scope/exported list
    — the expanded name PACK1.FUNC1 peels to the original (RM 8.4);
    task-entry prefixes likewise peel via Single_Task_Object_Denoted.
[x] c83027c: RM 12.1 — the discriminants of a generic formal private
    type are known inside the generic by the FORMAL's names; the
    instance binding's actual-view now grafts those names onto the
    actual's discriminant components (layout unchanged).
[x] c83030c (three parts):
    - a body STUB completing a generic subprogram resolves its profile
      inside the generic's formal part (X : T); the subunit pairs
      itself as the generic body through the existing branch.
    - RM 12.3: within a generic subprogram's own region (or an
      instance's body clone re-resolving in the reconstructed template
      environment) the unit's name denotes the CURRENT INSTANCE — an
      overloadable subprogram that hides only same-profile homographs.
      Generic_Current_Instance_Homograph implements the profile
      comparison inside Cancel_Use_Visible_Homographs' non-overloadable
      rule; Filter_By_Arguments drops the generic when the call gives
      more arguments than the unit's profile takes.
[x] c83033a: an expression-position selector through a block-label
    prefix denotes the block's ENTITY, not a statement label sharing
    its name (two-pass member search); Resolve_Apply redoes the pick
    across the block's homonyms with the actuals in view, as the
    package-prefix path already did.
[x] Round-11 regression repairs (found by the suite):
    - cc3203a/cc3240a: the formal-name graft leaked outside the
      instance. Reworked as a VIEW-dependent mapping: the actual's
      names stay on the view; generic_formal_disc_view carries the
      formal's; Find_Record_Component prefers the formal names only
      inside the instance/template region (Inside_View_Instance_Region)
      and the actual's outside. Under a formal/actual name COLLISION
      (cc3240a's TX.A) the resolver's pick is recorded on the node
      (selected.component_index) and the emitter honors it via
      Selected_Component_Slot — a by-name re-lookup at emission cannot
      re-arbitrate. An IN OUT formal object wearing the actual's
      subtype gets a copy carrying the disc view.
    - c45345a: a STRING-typed string literal stays context-flexible for
      catenation (RM 4.2) — it satisfies the array side of ANY
      character-array base, so SMALL_STRING'("ABCD") & "EF" builds the
      SMALL_STRING interp whose RM 4.5.3 bound check raises.
[x] ca2009d: the main-program candidate check excluded only STUBS
    (is_separate); a subunit's own CU (separate_parent) and a
    SYMBOL_GENERIC pairing are no main program — the driver was
    emitting @main around the generic subunit PROC1.
