═══════════════════════════════════════════════════════════════════════════════
  ada83.c — make ONE C-test group pass completely, with literate, root-cause fixes
═══════════════════════════════════════════════════════════════════════════════

ORIGINAL PROMPT
---------------
In the pursuit of high-quality code (low line count (e.g. low undue complexity)
and high feature/correctness) - refactor and fix ada83.c to pass a single
C-test group completely (e.g. cXX) with much better Haskell-like C99 and smart
code-golfing with, ironically, textbook "Literate Programming" style.

Do NOT skip around - when you encounter a complex problem look at it as an
opportunity to finally attack a big issue. Remember to follow the flow upstream
to avoid hacking up the expander.

Create a file to track everything called "checklist.txt" and include this
prompt in it.

TARGET GROUP
------------
c34005 — predefined operations of DERIVED types (RM 3.4). 14 subtests
(a,c,d,f,g,i,j,l,m,o,p,r,s,u), each component-type variant of derived
one-dimensional arrays / records. Baseline: only c34005i passed.

ROOT CAUSES FOUND & FIXED
-------------------------
[x] Concatenation counted ELEMENTS as BYTES (String-centric, element_size=1
    assumption). Fixed Generate (`&`) to scale alloc/memcpy/offset by the
    result component's byte width.  -> §13 binary `&`.
[x] `Array_Bound_LLVM_Rep(left_type)` in `&` crashed ("non-array kind") for
    component&component / component&array, where left is a scalar. Now derive
    the bound rep from the result array type.  -> §13 binary `&`.
[x] Emit_Fat_Pointer_Copy_To_Name memcpy'd element count as byte count (shared
    by fat->constrained assignment and var init). Now scales by the dest
    component byte width.
[x] Concatenation RESULT type was the operand's CONSTRAINED subtype; per
    RM 4.5.3 it must be the unconstrained base. Fixed Resolve_Binary_Op to use
    Type_Base. (Stopped constrained index leaking onto the compared aggregate.)
[x] Derived array type `type T is new PARENT(C)` never built an unconstrained
    base T'BASE, so Type_Base(T)=T (constrained). Fixed NK_DERIVED_TYPE to
    construct the unconstrained base and set derived->base_type.

MORE ROOT CAUSES FIXED
----------------------
[x] Aggregate in `AGG IN T` got the constrained subtype -> CE while building.
    Now propagates Type_Base (RM 4.4). + membership codegen for constrained
    arrays now does a real per-dimension LENGTH comparison (runtime-capable),
    not the old "composite IN is always true".
[x] Slice assignment to a *dynamically*-bounded constrained array used the
    flat-data path (treated the fat-ptr struct as data, didn't subtract the
    runtime low bound). Now routes dynamic-bounds targets through the
    fat-pointer slice path (target_is_fat).
[x] 'SIZE on arrays: unconstrained base now reports the index-subtype max
    extent, and a dynamically-bounded subtype/object measures length*component
    at runtime (was 0 / a stale type->size). Handles BOUND_EXPR index bounds.

EVEN MORE ROOT CAUSES FIXED
---------------------------
[x] Returning a constrained array as an unconstrained result loaded a fat
    pointer out of the flat data instead of building one from data + bounds.
    Generate_Return_Statement now builds it via Normalize_To_Fat_Pointer.
[x] Element address of a dynamic-bounds constrained array used the fat-pointer
    struct as flat data (mirror of the slice fix). Generate_Composite_Address
    now loads the data pointer and runtime low bound.
[x] Overload resolution: Possible_Alt_Type treated an indexed component A(I) as
    a call to A and offered A's array type as a spurious alternative, so a
    component comparison resolved to the array overload (RM 8.3). Now skipped
    when the name denotes an array object.
[x] Passing a fat-pointer-stored array (dynamic bounds) to an unconstrained
    formal built a fat pointer from the symbol's struct address, so a callee
    write clobbered the bounds. Now keyed on Type_Needs_Fat_Pointer.
[x] Consolidated the hand-rolled "stored as a fat pointer" test
    (Type_Is_Unconstrained_Array or Type_Has_Dynamic_Bounds) onto the existing
    Type_Needs_Fat_Pointer predicate at the array-typed sites.

c34005 STATUS: 9 / 14 pass (was 1). No regressions in the wider suite
(c52103 improved 6->7; c35502/c36205/c34001/c34003 still 100%).
  PASS: a c d f g i j l p
  FAIL: m  (multi-dimensional derived array, segfault)
        o  (multi-dimensional derived array, 4 aggregate-bound assertions)
        r  (array OUT-param assignment length checks: CONSTRAINT_ERROR not
            raised on wrong-length ASSIGN; no longer crashes)
        s  (2-D limited-private derived array, segfault)
        u  (limited-private derived array, segfault)

STATUS: c34005 COMPLETE — 14 / 14 (100%). No suite regressions
(c43205 90->100%, c52103 6->8, c64109 restored). All root-cause fixes upstream
of the expander, per the brief.
--------------------------------
[x] Multidimensional fat-pointer plumbing: passing a fat actual to a constrained
    N-D formal, flat->fat conversion, the aggregate-assignment memcpy, and
    Normalize_To_Fat_Pointer all carried only dimension 0; now carry every dim
    (Emit_Fat_Pointer_For_Lvalue / Emit_Array_Byte_Size).
[x] 'FIRST(N)/'LAST(N)/'LENGTH(N)/'RANGE(N): fold a static named-constant
    dimension argument (was defaulting to dimension 1).
[x] A positional inner sub-aggregate kept the index subtype's full range when
    that subtype's FIRST was a static named number; fold it to the element count.
[x] Named multidimensional aggregate vs constraint: check every dimension
    (static and dynamic paths), not just the outermost / 1-D.
[x] RM 6.4.1: an array actual passed to a constrained array formal is
    length-checked per dimension (bounds slide); raises CONSTRAINT_ERROR.
[x] Overload resolution: an indexed component A(I) no longer offers A's array
    type as a spurious alternative (RM 8.3).
[x] Regression guard: distinguish a fat-stored OBJECT (simple var/param ref)
    from INLINE element data (record/array component) when wrapping an actual
    for an unconstrained formal.

ALSO FIXED
----------
[x] Assigning a fat array (aggregate / unconstrained) into a dynamically-bounded
    multidimensional target copied only dimension 0's length * component size
    (the first row). Now uses Emit_Array_Byte_Size (product of all dims). This
    made c34005s run instead of crash and fixed positional/named 2-D aggregate
    assignment (verified by reduced cases).

REMAINING WORK TO REACH 100% ON c34005   (current: 9/14)
-------------------------------------------------------
Each remaining subtest needs a distinct piece of multi-dimensional or
limited-private support; reduced repros isolate the next root causes:
- m (2-D, ~23 checks): positional and named 2-D aggregates now work, but a
  FUNCTION returning a constrained dynamic 2-D array (e.g. IDENT) yields wrong
  data; 2-D qualification/conversion/slice/membership still to do.
- s (2-D limited private, ~18 checks): same 2-D operation set over LP.
- o (2-D, 4 checks): 2-D aggregate-vs-constraint CONSTRAINT_ERROR checks.
- r (5 checks): CONSTRAINT_ERROR on a wrong-length assignment through an
  unconstrained array OUT formal (no longer crashes).
- u: still segfaults (another fat-stored limited-private path).

WIDER SUITE: no regressions; c52103 improved 6 -> 8 from these fixes.

WORKING NOTES
-------------
- Build: `make`  (gcc -O3 -Wall -march=native -> ./ada83)
- Run one test:
    ./ada83 -I acats acats/c34005a.ada -o /tmp/x.ll
    llvm-link -o /tmp/x.bc /tmp/x.ll acats/report.ll && lli /tmp/x.bc
- Group run:  bash run_acats.sh q c34005
- Debug substrate: gcc -g -O0 -o /tmp/ada83g ada83.c -lm -lpthread ; gdb.
- Principle: fix upstream (semantics/type construction), not the expander.

═══════════════════════════════════════════════════════════════════════════════
DERIVED-TYPE GROUPS COMPLETE (100%): c34005 (arrays), c34006 (records),
c34009 (non-limited private). Discriminated-record support added this round:
  - by-value record results copied to the secondary stack (RM 6.5)
  - record equality compares only the active variant (RM 4.5.2)
  - 'SIZE of a discriminant-dependent component read from the object fat pointer
  - derived discriminated subtype gets an unconstrained base
  - membership in a constrained discriminated subtype checks discriminants
  - max-size reservation for a discriminant-dependent component (fixed offsets)
  - a boolean/enum discriminant PARAMETER is not a literal
  - a derived operation's discriminant check uses the parent's base formal
  - BIP state kept across multiple RETURNs
  - OUT record parameter re-checks the actual's discriminant constraint
No suite regressions throughout. Next derived-type targets: c34007 (access),
c34014 (derived subprogram visibility).

═══════════════════════════════════════════════════════════════════════════════
c34004 COMPLETE (2/2, 100%): derived FIXED POINT types. Root causes fixed:
  - Fixed-point membership (X IN T, R IN T) scaled the value but not the type
    bounds; extracted the bound-to-mantissa logic out of a macro into a real
    function (Emit_Fixed_Bound_Mantissa) and used it in both the constraint
    check and the scalar/range membership paths.
  - A derived fixed-point type's explicit RANGE with non-static bounds (built
    from IDENT_INT) was dropped; now stored via Assign_Eval_Bound like float.
  - Fixed-point types now build an anonymous base type (Build_Fixed_Base) that
    spans the symmetric model range +/-(2**MANTISSA-1)*SMALL, so T'BASE'FIRST,
    'LAST, 'MANTISSA, 'LARGE, 'SAFE_LARGE are mutually consistent. A derived
    fixed type's base is the parent's base (its finer DELTA, full range).
  - T'MANTISSA / T'FORE / T'LARGE for a non-static range are evaluated at
    runtime (Emit_Fixed_Mantissa_Runtime via ctlz, Emit_Fixed_Fore_Runtime via
    a divide-by-ten digit loop, Emit_Fixed_Large_Runtime).
  - Representation SMALL vs accuracy SMALL: a derived type given a coarser
    DELTA has a larger 'SMALL than its base, but its objects are stored and its
    arithmetic performed in the base's units so base values absent from the
    subtype stay representable. Introduced Fixed_Repr_Small (= base SMALL) and
    routed every representation site (literals, init, arithmetic, comparison,
    membership, conversion, aggregates, returns, 'FIRST/'LAST) through it.
    Attribute values ('SMALL, 'DELTA, 'MANTISSA, 'FORE) keep the subtype SMALL.
    Identity for ordinary types (base SMALL == own SMALL), so no suite impact.
  - Qualified real to fixed (T'(1.0)) used a plain float-to-int convert,
    dropping the SMALL scaling; now uses Convert_Real_To_Fixed.
  - Fixed a malformed llvm.round.double intrinsic name (now f32/f64).
Suite: c34004 0->2/2; c46 8->9; c34 37->39; c35/c45/c87/c96 unchanged.
Next derived-type targets: c34007 (access), c34014 (derived subprogram vis).

═══════════════════════════════════════════════════════════════════════════════
c34007 (derived ACCESS types) IN PROGRESS (2->3/11). Root causes fixed:
  - Dereferencing a fat access (access-to-unconstrained) whose designated
    subtype is statically constrained mis-tagged the loaded { ptr, ptr } as a
    thin pointer, so array comparison/operations read it wrong. The .ALL value
    (NK_UNARY_OP/TK_ALL and the NK_SELECTED path) and Expression_Produces_Fat_
    Pointer now tag the deref by the access's representation, not the designated
    subtype. A derived access type from a constrained access subtype now builds
    an unconstrained access base so the fat-pointer decision sees through it.
  - RM 4.5.2: equality slides array bounds. A constrained array operand of a
    comparison no longer imposes its index constraint on the sibling aggregate
    (was raising a spurious element-count CONSTRAINT_ERROR). Strip to the base
    in both aggregate-typing sites of Resolve_Binary_Op.
  - RM 3.3.2: membership in a constrained access-to-array subtype now checks the
    designated bounds (null belongs; non-null belongs iff bounds match) instead
    of always returning TRUE.
  Suite: c34007 2->3 (a,f,j); c64 26->27; no regressions (c34/c43/c45/c46/c52).
  Remaining c34007 need distinct features: slice of access-to-array codegen (d),
  2-D access plumbing (g,i,m,p), limited-private (s,u), OUT-param length (r).

═══════════════════════════════════════════════════════════════════════════════
c34014 (derived subprogram visibility) IN PROGRESS (1->2/14). Root cause fixed:
  - A call to an inherited (derived) subprogram must bind to the parent's body
    (RM 3.4): the derived subprogram has no code of its own. The codegen climbed
    at most one parent_operation level (the binary-operator path climbed none),
    so a multi-level derivation (S is new T is new P) and the parameterless and
    operator call paths emitted references to intermediate derived operations
    that were never defined, failing at link. Added Ultimate_Operation (GNAT's
    Ultimate_Alias) and routed all four call-emission sites through it:
    Generate_Apply, the parameterless path in Generate_Identifier, and the
    unary/binary user-operator paths. Fixed every compile/link failure in the
    group; c34014g now passes.
  Suite: c34 39->40; c45/c87 stable; no regressions.
  Remaining c34014 are a separate semantic feature: visibility/overload binding
  of a derived subprogram vs an explicit homograph (NEW/OLD ... NOT VISIBLE).
