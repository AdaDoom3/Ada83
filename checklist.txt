════════════════════════════════════════════════════════════════════════════
 CHECKLIST — Class-C skipped-test refactor campaign
════════════════════════════════════════════════════════════════════════════

ORIGINAL PROMPT
---------------
In the pursuit of high-quality code (low line count (e.g. low undue
complexity) and high feature/correctness) - refactor and fix ada83.c all
C-tests completely (e.g. cXX) with much better Haskell-like C99 and smart
code-golfing with, ironically, textbook "Literate Programming" style.

Do NOT skip around - when you encounter a complex problem look at it as an
opportunity to finally attack a big issue. Remember to follow the flow
upstream to avoid hacking up the expander.

Create a file to track everything called "checklist.txt" and include this
prompt in it.

Focus on skipped tests.

METHOD
------
1. Run full Class C suite; enumerate every `skip` result and its cause
   (COMPILE[…] = front-end rejects legal code; BIND = unresolved symbols
   at llvm-link; N/A = test reports NOT APPLICABLE at runtime).
2. Group skips by root cause, not by test. Fix the deepest shared cause
   first (upstream: parser → semantics → expander → emit).
3. Re-run affected groups after each fix; record deltas here.
4. Land only structurally correct fixes — no expander hacks, no
   special-cases keyed to test names.

STATUS LOG
----------
[x] Baseline Class C run: 1935 pass / 21 fail / 20 skip (of 1976)
    results: test_results/c-20260723-115818-3689
[x] G1 CLOSED — all 46 ca* tests pass; +4 former fails fixed too.
    Root causes fixed (all upstream, no expander hacks):
      - WITH of library-level generic instantiations never loaded
        (loader, ALI writer, Unit_Simple_Name)
      - RM 10.1 replacement rule missing in Lookup_Path
      - hand-rolled loaded-body resolution drifted from
        Resolve_Declaration (never set pkg scope -> subunit parents
        unresolvable); deleted in favor of Resolve_Declaration
      - subunit modules emitted strong defs of parent-owned globals
      - separate package-body stubs lost their elab call at library
        level (deferred into enclosing elab function now)
      - whole-loaded units emitted strong function defs -> multiply
        defined at llvm-link; now linkonce_odr (Loaded_Define_Linkage)
      - cross-file subprogram bodies (spec and body in different
        compilations) never loaded
      - RM 10.2 subunit context clauses not elaborated before ancestor
        body (Load_Subunit_Context_Closure)
      - RM 3.9 elaboration flags were per-compilation state; now
        linkonce_odr globals, set unconditionally at body elaboration,
        with the dynamic cross-package check gated by pragma ELABORATE
        edges (Elab_Body_Forced_Before) — fixes ca1002a3m ca1014a0m
        ca5006a ca1020e3m and the whole flaky ca-elab family
      - harness: fragments link per RM 10.1 replacement (latest
        provider per unit, ancestor-aware for subunits); rejected
        fragments no longer abort a family compile
[x] Full Class C: 1950 pass / 17 fail / 9 skip — zero regressions.

[x] ALL FIXABLE SKIPS CLOSED — Class C now 1955 pass / 17 fail / 4 skip,
    zero regressions vs the original baseline.
    - c39006f3m: block-scoped packages/objects were module-globals in
      Symbol_Is_Global but locals at the definition site; symbols now
      carry declared_in_statements and the two sites agree. Instance
      packages keep global data; their dynamic-bounds arrays now get
      heap data + bounds stored into the linkonce fat-pointer global.
    - c86004c2m (+kept c86004b2m green): a loaded body now inherits its
      spec's context clause (Fold_Spec_Context_Into_Body in the loader);
      an already-loaded library subprogram is not re-resolved by a
      second WITH.
    - c86002a2m/c86002b2m: a WITH'd library unit named STANDARD shadows
      the predefined package for selected/callable uses (RM 8.6).
    - cd1009i: limitedness of a generic actual judged by the view at
      the instantiation point (Type_Limited_View_Active), so a private
      part can instantiate with its own completed full view.
[x] G4 VERIFIED GENUINE — c64103a c64103c c64103d c95084a report
    NOT APPLICABLE because this implementation has exactly one integer
    base type ('BASE'LAST equal across all range types); that is a
    conformant ACATS outcome, not a missing feature.

FAILS — RESOLVED
--------------------------------------
[x] c42007i  constrained-dynamic-bounds function results deep-copy to
             the secondary stack (bounds were dangling frame allocas)
[x] c43103b  named aggregate bounds equal the constraint even when
             null — except in assignment (sliding) contexts, where
             lengths govern (c52103s kept green)
[x] c45252a  real->fixed conversion guards the representation range
             before fptosi (poison became CONSTRAINT_ERROR)
[x] c45282b  access membership sees discriminant constraints through
             private designated views
[x] c64104c  string-literal actuals length-checked against constrained
             formals; OUT/IN OUT array actuals need exact bounds
             (no sliding without value conversion), enforced statically
             and dynamically
[x] c87b10a  bounds of real type definitions resolve expecting a real
             type (RM 8.7), so user integer-returning "+" loses
[x] ce3111c  shared external files (see text_io notes above)
[x] ce3208a  default-file identity across CLOSE + MODE_ERROR rule
[x] ce3411b  COUNT'LAST/LAYOUT_ERROR model

FAILS — REMAINING 6, all accounted for
--------------------------------------
Fixed since the last blessing (d367df8): c55b06a (membership range
char-literal bounds resolve against the tested enumeration), cb7001a
(RM 4.5.7 NUMERIC_ERROR on infinite float results), cd1c04e (derived
records own a copy of the parent component layout; rep-clause extents
computed in bits).

Officially WITHDRAWN ACVC 1.11 tests (expectations ruled erroneous by
the AVO; list reproduced in DTIC validation summary reports, e.g.
ADA243300/ADA262889): c98003b cc1226b ce3902b.  ce3902b regressed
pass->fail because its expectation (reopening the current default
output with IN mode succeeding) directly contradicts the valid
ce3208a, which we now honor — the AVO withdrew ce3902b, not ce3208a.

Representation campaign (REPRESENTATION.md), staged and landing:
  [x] Stage 1  extent unified behind Array_Element_Bits (one owner,
               bits); six ad-hoc sites and four fallback conventions
               deleted; sweep bit-identical
  [x] Stage 2  Storage_Locus layer; component clauses obeyed at every
               record access site; cd4031a + cd4024b now PASS
  [x] Stage 3  bit-packed boolean arrays for static-bounds families;
               cda201a now PASSES.  Packing degree is a per-family
               choice: static-bounds families pack to 1 bit, dynamic
               and unconstrained families keep byte stride until
               Stage 4.  Per-type equality functions routed through
               Generate_Array_Equality (deleted a duplicated memcmp).
  [x] Stage 4  the family gate is gone: packed arrays work with
               dynamic bounds and general element widths (a discrete
               element with a smaller 'SIZE packs to that width,
               straddling bytes); pragma PACK lays records out at bit
               granularity; unchecked conversion uses runtime-sized
               buffers, canonicalizes packed padding, and extends
               scalars from the packed width by destination
               signedness; allocations pad for worst-case windows;
               the runtime type-size descriptors, aggregate rows,
               fat-element equality (elementwise, was memcmp-of-
               pointers / memcmp-of-zero-bytes), and the five
               remaining ad-hoc extent sites are all storage-byte
               correct.  cda201a, cd2a31a-c, cd2a32a, cd4022a,
               cd1c03a and the whole packed family pass.
  Remaining follow-ups (no Class C coverage; need hand tests first):
    - packed slices (elementwise copy/materialization)
    - OUT-mode packed element actuals (copyback through a temp)
    - dynamic packed equality tail masking (dynamic-bounds compare
      with differing padding)

FINAL STATE (current)
---------------------
Class C: 1969 pass / 3 fail / 4 skip  (baseline was 1935/21/20)
  - the 3 fails are exactly the officially withdrawn ACVC tests
    (c98003b, cc1226b, ce3902b); every valid test passes
  - all 4 skips are genuine RM-conformant NOT APPLICABLE
  - zero unexplained regressions
acats.baseline blessed at this state; run_acats.sh check gates on it.
