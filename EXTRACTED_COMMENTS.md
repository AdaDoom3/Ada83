# Extracted Comments

Every comment removed from `ada83.c` and `Makefile`, in source order.
Line numbers refer to the files as of the commit that performed the
extraction (the last commit in which the comments were still in place
is that commit's parent).

## ada83.c

### ada83.c:2

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3

```
//
```

### ada83.c:4

```
//                                           A D A 8 3 . C                                
```

### ada83.c:5

```
//                                                                                                
```

### ada83.c:6

```
//                  An Ada 1983 Compiler Targeting LLVM Intermediate Representation                   
```

### ada83.c:7

```
//                                                                                                
```

### ada83.c:8

```
//  §1.  Foundations        Includes, typedefs, target constants, ctype wrappers                
```

### ada83.c:9

```
//  §2.  Measurement        Bit/byte morphisms, LLVM type selection, range checks               
```

### ada83.c:10

```
//  §3.  Memory             Arena allocator for the compilation session                         
```

### ada83.c:11

```
//  §4.  Text               String slices, hashing, edit distance                               
```

### ada83.c:12

```
//  §5.  Provenance         Source locations and diagnostic reporting                           
```

### ada83.c:13

```
//  §6.  Arithmetic         Big integers, big reals, exact rationals                            
```

### ada83.c:14

```
//  §7.  Lexical Analysis   Token kinds, lexer state, scanning functions                        
```

### ada83.c:15

```
//  §8.  Syntax             Node kinds, the syntax tree, node lists                             
```

### ada83.c:16

```
//  §9.  Parsing            Recursive descent for the full Ada 83 grammar                       
```

### ada83.c:17

```
//  §10. Types              The Ada type lattice, Type_Info, classification                     
```

### ada83.c:18

```
//  §11. Names              Symbol table, scopes, overload resolution                           
```

### ada83.c:19

```
//  §12. Semantics          Name resolution, type checking, constant folding                    
```

### ada83.c:20

```
//  §13. Code Generation    LLVM IR emission for every Ada construct                            
```

### ada83.c:21

```
//  §14. Library Management ALI files, checksums, dependency tracking                           
```

### ada83.c:22

```
//  §15. Elaboration        Dependency ordering for multi-unit programs                         
```

### ada83.c:23

```
//  §16. Generics           Tree-copy instantiation of generic units                          
```

### ada83.c:24

```
//  §17. File Loading       Include-path search, source file I/O                                
```

### ada83.c:25

```
//  §18. Vector Paths       SIMD-accelerated scanning on x86-64 and ARM64                       
```

### ada83.c:26

```
//  §19. Driver             Command-line parsing and top-level orchestration                    
```

### ada83.c:27

```
//  §20. Native Backend     In-process IR -> executable via runtime-loaded LLVM-C                
```

### ada83.c:28

```
//
```

### ada83.c:29

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:30

```
//
```

### ada83.c:31

```
//                                    S P E C I F I C A T I O N
```

### ada83.c:32

```
//                 
```

### ada83.c:33

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:35

```
// Extensions
```

### ada83.c:38

```
// Standard types/IO
```

### ada83.c:51

```
// POSIX
```

### ada83.c:59

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:60

```
//
```

### ada83.c:61

```
// §1. Foundation - Includes, limit settings, typedefs, target constants, ctype wrappers
```

### ada83.c:62

```
//
```

### ada83.c:63

```
// Ada's numeric model demands integers wider than 64 bits; GCC/Clang __int128 gives us native
```

### ada83.c:64

```
// 128-bit registers on 64-bit targets. All widths, bounds, and capacities for the compiler's
```

### ada83.c:65

```
// subsystems are defined here so that every translation unit sees consistent constants. Safe ctype
```

### ada83.c:66

```
// wrappers cast to unsigned char before calling the C library.
```

### ada83.c:67

```
//
```

### ada83.c:68

```
// Size morphisms convert between the byte world of Type_Info and the bit world of LLVM IR.
```

### ada83.c:69

```
// Ali_Llvm_Int_Type maps a bit width to the smallest LLVM int type that holds it (ALI writer
```

### ada83.c:70

```
// boundary helper). Range predicates determine representation width for integer and modular types.
```

### ada83.c:71

```
//
```

### ada83.c:72

```
// To_Bits:    Bytes > Bits  (Multiplicative, total - never truncates)
```

### ada83.c:73

```
// To_Bytes:   Bits  > Bytes (Ceiling division - rounds up to next whole byte)
```

### ada83.c:74

```
//
```

### ada83.c:75

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:77

```
// Versioning
```

### ada83.c:80

```
// Arena memory allocator chunk size limits
```

### ada83.c:81

```
// 16 MiB
```

### ada83.c:83

```
// Resolution limits
```

### ada83.c:84

```
// Hash buckets (§11)
```

### ada83.c:85

```
// Overload ceiling
```

### ada83.c:87

```
// Elaboration limits
```

### ada83.c:88

```
// Elaboration graph (§15)
```

### ada83.c:92

```
// Code generator limits
```

### ada83.c:93

```
// Exception references per unit
```

### ada83.c:94

```
// Array aggregate dimensions
```

### ada83.c:95

```
// Discriminant value cache depth
```

### ada83.c:97

```
// Generics limits (§16)
```

### ada83.c:98

```
// RM 12.3(81) subtype-agreement pairs per instance
```

### ada83.c:99

```
// Formal-to-binding symbol remaps per instance
```

### ada83.c:100

```
// Nested instantiation chain (cycle detector)
```

### ada83.c:102

```
// File loading limits (§17)
```

### ada83.c:104

```
// Library management limits (§14)
```

### ada83.c:105

```
// Parsed .ali files held for spec reuse
```

### ada83.c:107

```
// Math constants
```

### ada83.c:110

```
// Fat-Pointer layout uses unconstrained array parameters carry {data, bounds} - 16 bytes on 64-bit
```

### ada83.c:113

```
// A single machine pointer (thin access value, task TCB handle, etc.) - 8 bytes on 64-bit
```

### ada83.c:116

```
// RM 11.1: an automatic (stack) object whose storage exceeds this ceiling is
```

### ada83.c:117

```
// treated as unallocatable and raises STORAGE_ERROR at its elaboration rather
```

### ada83.c:118

```
// than emitting an alloca the machine stack cannot satisfy (which would fault
```

### ada83.c:119

```
// with no Ada exception). The bound is deliberately generous — far above any
```

### ada83.c:120

```
// realistic local object — so it fires only for pathological declarations such
```

### ada83.c:121

```
// as ARRAY (1 .. INTEGER'LAST) OF BOOLEAN. A precise per-thread limit would
```

### ada83.c:122

```
// need runtime stack-extent probing; this compile-time ceiling is the honest
```

### ada83.c:123

```
// approximation for objects whose size is or approaches the whole address
```

### ada83.c:124

```
// space.
```

### ada83.c:127

```
// Automatic objects at or above this size get a runtime stack-extent probe
```

### ada83.c:128

```
// (__ada_stack_check) before their alloca; smaller ones are covered by the
```

### ada83.c:129

```
// probe's safety margin and skip the call.
```

### ada83.c:132

```
// ???
```

### ada83.c:138

```
// Named LLVM types for widths fixed by something OTHER than an Ada type, so the
```

### ada83.c:139

```
// literal stops standing in for (and colliding with) "Ada INTEGER". Each expands
```

### ada83.c:140

```
// to the same string it would otherwise be written as; the name records WHY the
```

### ada83.c:141

```
// width is what it is.
```

### ada83.c:142

```
// C-ABI int: memcmp/strcmp/setjmp return values
```

### ada83.c:143

```
// pointer-sized integer on the LP64 target
```

### ada83.c:144

```
// runtime type-descriptor global element (@__rt_*)
```

### ada83.c:145

```
// CHARACTER storage (RM 3.5.2)
```

### ada83.c:146

```
// BOOLEAN storage (RM 3.5.3) - i8, not i1
```

### ada83.c:148

```
// LLVM representation lattice. Every value at the IR level has one of these
```

### ada83.c:149

```
// kinds; widths/signedness refine it. Generic-style dispatch lives at the rep
```

### ada83.c:150

```
// level, not on strings: producers tag a value with a LLVM_Rep, consumers
```

### ada83.c:151

```
// dispatch on .rep.kind + .rep.bits, and IR text comes from LLVM_Rep_To_String
```

### ada83.c:152

```
// only at the emit boundary. The Ada type lattice (Type_Info) still owns
```

### ada83.c:153

```
// subtype/range semantics; LLVM_Rep is purely the lowered representation.
```

### ada83.c:155

```
// no value (matches NO_VALUE / void return)
```

### ada83.c:156

```
// iN: bits = 1, 8, 16, 32, 64, 128
```

### ada83.c:157

```
// float / double: bits = 32, 64
```

### ada83.c:158

```
// opaque ptr
```

### ada83.c:159

```
// { ptr, ptr }
```

### ada83.c:164

```
// INT: width in bits; FLOAT: 32/64; ignored otherwise
```

### ada83.c:165

```
// INT only: true for Ada modular types (RM 3.5.4)
```

### ada83.c:168

```
// A typed SSA value: an LLVM register paired with its structured rep. IR text
```

### ada83.c:169

```
// spelling derives from .rep at emit boundaries via LLVM_Rep_To_String.
```

### ada83.c:175

```
// An i1 predicate. Only legal flow: br / select operand, or widened to a
```

### ada83.c:176

```
// BOOLEAN LLVM_Value via Emit_Bool_Value. The type system prevents accidental
```

### ada83.c:177

```
// use as an arbitrary value, because i1 is transient at the LLVM IR level
```

### ada83.c:178

```
// (RM 3.5.3 - BOOLEAN's value rep is i8).
```

### ada83.c:183

```
// The absence of a value: register 0, void rep.
```

### ada83.c:186

```
// Runtime Check Flags: each bit controls a check category suppressible via pragma Suppress
```

### ada83.c:198

```
// Tasking restrictions (implementation-defined pragma RESTRICTIONS, and
```

### ada83.c:199

```
// pragma PROFILE (RAVENSCAR) as the deterministic-tasking bundle of all of
```

### ada83.c:200

```
// them — the Ada 83 analogue of the Ravenscar profile's static subset).
```

### ada83.c:201

```
// Each bit makes the named construct a compile-time error at resolution.
```

### ada83.c:202

```
// selective waits, conditional/timed calls
```

### ada83.c:203

```
// ABORT
```

### ada83.c:204

```
// NEW of a task-bearing type
```

### ada83.c:205

```
// ENTRY E (RANGE)
```

### ada83.c:207

```
// IEEE Floating-Point Model
```

### ada83.c:220

```
// Fast identifier-character lookup table
```

### ada83.c:221

```
//
```

### ada83.c:222

```
// A character is an identifier constituent when it is an ASCII letter, digit, or underscore, or
```

### ada83.c:223

```
// a Latin-1 letter in 0xC0..0xD6, 0xD8..0xF6, 0xF8..0xFF (excluding the multiplication sign 0xD7
```

### ada83.c:224

```
// and division sign 0xF7).
```

### ada83.c:225

```
//
```

### ada83.c:227

```
// Convience aggregate macro
```

### ada83.c:230

```
// ???
```

### ada83.c:233

```
// ASCII upperca_e letters 'A' through 'Z'
```

### ada83.c:237

```
// ASCII lowercase letters: 'a' through 'z'
```

### ada83.c:241

```
// Digits and underscore
```

### ada83.c:244

```
// Latin-1 uppercase letters: À through Ö (0xC0 – 0xD6) and Ø through ß (0xD8–0xDF)
```

### ada83.c:249

```
// Latin-1 lowercase letters: à through ö (0xE0 – 0xF6) and ø through ÿ (0xF8 – 0xFF)
```

### ada83.c:255

```
// Clear the aggregate macro
```

### ada83.c:258

```
// Character Classification
```

### ada83.c:259

```
//
```

### ada83.c:260

```
// Wrappers cast to unsigned char before calling ctype. The C library functions take int and require
```

### ada83.c:261

```
// unsigned char to avoid undefined behaviour on platforms where plain char is signed
```

### ada83.c:262

```
//
```

### ada83.c:270

```
// GCC and Clang give us native 128-bit registers on 64-bit targets
```

### ada83.c:274

```
// Target Data Model: 64-bit LP64 host throughout; sizes in Type_Info are bytes, bit widths IR only 
```

### ada83.c:278

```
// Standard sizes
```

### ada83.c:294

```
// Given a bit width, return the smallest LLVM integer or float type that can hold that width.
```

### ada83.c:298

```
// Round size up to the nearest alignment boundary
```

### ada83.c:303

```
// Return the smallest LLVM integer type (i1 through i128) for `bits' bits.
```

### ada83.c:304

```
// ALI writer boundary helper: serialise an integer width as an LLVM type string
```

### ada83.c:305

```
// for the .ali file's export-type field. Not used in IR emission; codegen goes
```

### ada83.c:306

```
// through LLVM_Rep_To_String. Rounds non-standard widths up to the next legal
```

### ada83.c:307

```
// LLVM int width.
```

### ada83.c:313

```
// ─── LLVM_Rep helpers ─────────────────────────────────────────────────────────
```

### ada83.c:314

```
// Structured representation is canonical; strings exist only at Emit() calls.
```

### ada83.c:319

```
// matches C_INT_TYPE
```

### ada83.c:321

```
// Round an integer width up to the nearest legal LLVM integer width. Enforces
```

### ada83.c:322

```
// the LLVM_Rep invariant that INT bits is one of {1, 8, 16, 32, 64, 128}, so
```

### ada83.c:323

```
// that .bits always equals the width LLVM_Rep_To_String renders. A type whose
```

### ada83.c:324

```
// byte size lowers to a non-standard bit count (e.g. a 3-byte derived type ->
```

### ada83.c:325

```
// 24 bits) is stored in the next legal register width, exactly as the IR does.
```

### ada83.c:345

```
// The float representation two operands should be compared/combined at when no
```

### ada83.c:346

```
// result type dictates the precision (comparisons, membership): the wider of
```

### ada83.c:347

```
// two float operands, the float side of a mixed float/int pair, or double as a
```

### ada83.c:348

```
// last resort. Centralizes the rule so the many float op sites agree.
```

### ada83.c:368

```
// Render an LLVM IR type string for the rep. Use only at IR emit boundaries.
```

### ada83.c:383

```
// unreachable: LLVM_Rep_Int canonicalises bits at construction
```

### ada83.c:397

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:398

```
//
```

### ada83.c:399

```
// §2.3  Range Predicates - Determining representation width
```

### ada83.c:400

```
//
```

### ada83.c:401

```
// Compute the minimum number of bits needed to represent the integer range [lo .. hi]. All bounds
```

### ada83.c:402

```
// are int128_t / uint128_t so the full Ada integer range is representable.
```

### ada83.c:403

```
//
```

### ada83.c:404

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:406

```
// Return true when [lo..hi] fits within the signed range of `bits' bits
```

### ada83.c:415

```
// Return true when [lo..hi] fits within the unsigned range of `bits' bits
```

### ada83.c:416

```
// Return the smallest standard LLVM integer width (8, 16, 32, 64, or 128) that can represent every
```

### ada83.c:417

```
// value in the range [lo .. hi].
```

### ada83.c:432

```
// Return the smallest standard LLVM integer width for a modular type whose range is 0 .. modulus-1
```

### ada83.c:442

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:443

```
//
```

### ada83.c:444

```
// §3. Memory - Arena allocator for the compilation session
```

### ada83.c:445

```
//
```

### ada83.c:446

```
// A simple bump allocator used for AST nodes, interned strings, and other objects whose lifetime
```

### ada83.c:447

```
// spans the entire compilation. All memory is freed in one shot at the end via Arena_Free_All.
```

### ada83.c:448

```
// Chunks are 16 MiB by default; oversized requests get their own chunk. All pointers are 16-byte
```

### ada83.c:449

```
// aligned.
```

### ada83.c:450

```
//
```

### ada83.c:451

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:455

```
// Singly-linked list of chunks
```

### ada83.c:456

```
// First usable byte
```

### ada83.c:457

```
// Bump pointer
```

### ada83.c:458

```
// One past last usable byte
```

### ada83.c:462

```
// Most recently allocated chunk in the singly-linked list
```

### ada83.c:463

```
// Default byte size for new chunk allocations (16 MiB)
```

### ada83.c:468

```
// Zero-filled, 16-byte aligned
```

### ada83.c:469

```
// Release every chunk at end of main
```

### ada83.c:471

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:472

```
//
```

### ada83.c:473

```
// §4. Text - Non-owning string views
```

### ada83.c:474

```
//
```

### ada83.c:475

```
// A String_Slice is a (pointer, length) pair borrowed from the source buffer or the arena. It
```

### ada83.c:476

```
// avoids strlen() calls and allows substring views without allocation. Ada identifiers are
```

### ada83.c:477

```
// case-insensitive (RM 2.3), so comparison and hashing fold to lower case.
```

### ada83.c:478

```
//
```

### ada83.c:479

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:481

```
// No null terminator required. Comparison and hashing fold to lower case (Ada RM 2.3).
```

### ada83.c:483

```
// Pointer into the source buffer or arena (not null-terminated)
```

### ada83.c:484

```
// Number of bytes in the slice
```

### ada83.c:491

```
// Arena-allocate a copy of `slice' so it outlives the source buffer.
```

### ada83.c:493

```
// Byte-exact equality test (case-sensitive).
```

### ada83.c:495

```
// Case-insensitive equality following Ada RM 2.3 identifier equivalence.
```

### ada83.c:497

```
// FNV-1a hash folded to lower case for use in hash tables.
```

### ada83.c:499

```
// Levenshtein edit distance for spelling-correction suggestions.
```

### ada83.c:502

```
// Closest-name accumulator for "did you mean" diagnostics over an
```

### ada83.c:503

```
// arbitrary candidate list (record components, task entries, package
```

### ada83.c:504

```
// exports). Begin with the misspelled target, Consider each candidate,
```

### ada83.c:505

```
// then read `best` if Found.
```

### ada83.c:507

```
// The name that failed to resolve
```

### ada83.c:508

```
// Closest candidate seen so far
```

### ada83.c:509

```
// Its edit distance (distance_limit+1 = none)
```

### ada83.c:510

```
// Maximum distance worth suggesting
```

### ada83.c:518

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:519

```
//
```

### ada83.c:520

```
// §5. Provenance - Anchoring diagnostics to source text
```

### ada83.c:521

```
//
```

### ada83.c:522

```
// Every AST node, token, and symbol carries a Source_Location so that error messages can point the
```

### ada83.c:523

```
// programmer at the exact file, line, and column where the problem was detected. Errors are
```

### ada83.c:524

```
// accumulated rather than triggering an immediate abort, so the compiler can report multiple issues
```

### ada83.c:525

```
// in a single invocation.
```

### ada83.c:526

```
//
```

### ada83.c:527

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:529

```
// Accumulated; Error_Count is checked after each phase. Fatal_Error calls exit(1).
```

### ada83.c:531

```
// Path of the source file (interned, never freed)
```

### ada83.c:532

```
// One-based line number within the file
```

### ada83.c:533

```
// One-based column number within the line
```

### ada83.c:536

```
// ???
```

### ada83.c:539

```
// Global error counter
```

### ada83.c:542

```
// Shared core of the diagnostic reporters: `file:line:col: severity: message`.
```

### ada83.c:546

```
// Emit a diagnostic message and increment Error_Count without stopping.
```

### ada83.c:550

```
// Emit a diagnostic message and terminate the compiler immediately.
```

### ada83.c:554

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:555

```
//
```

### ada83.c:556

```
// §6. Arithmetic - Big integers, big reals, exact rationals
```

### ada83.c:557

```
//
```

### ada83.c:558

```
// Three levels of exact arithmetic for Ada numeric literals and constant folding:
```

### ada83.c:559

```
//
```

### ada83.c:560

```
//   - Big_Integer - Arbitrary-precision integers as little-endian 64-bit limb arrays.
```

### ada83.c:561

```
//   - Big_Real    - Significand (Big_Integer) paired with a power-of-ten exponent.
```

### ada83.c:562

```
//   - Rational    - Exact quotient of two Big_Integers, always reduced by GCD.
```

### ada83.c:563

```
//
```

### ada83.c:564

```
// All storage is arena-allocated; no explicit deallocation. Ada numeric literals can exceed the
```

### ada83.c:565

```
// 64-bit range (e.g. mod 2**128), so arbitrary precision is not optional.
```

### ada83.c:566

```
//
```

### ada83.c:567

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:569

```
// Big_Integer
```

### ada83.c:571

```
// Little-endian 64-bit digits
```

### ada83.c:572

```
// Active limbs
```

### ada83.c:573

```
// Allocated slots
```

### ada83.c:574

```
// Sign flag; magnitude always positive
```

### ada83.c:577

```
// Arena-allocate a zero-initialised Big_Integer with room for `capacity' limbs.
```

### ada83.c:579

```
// Return an independent deep copy of `source'.
```

### ada83.c:581

```
// Return a fresh Big_Integer whose value is 1.
```

### ada83.c:584

```
// Grow the limb array if fewer than `needed' slots are allocated.
```

### ada83.c:587

```
// Strip leading zero limbs so that `count' reflects the true magnitude.
```

### ada83.c:590

```
// In-place: integer = integer * factor + addend  (schoolbook single-limb step).
```

### ada83.c:595

```
// Three-way comparison: returns -1, 0, or +1.
```

### ada83.c:598

```
// Return a new Big_Integer equal to left + right (signs handled internally).
```

### ada83.c:601

```
// Schoolbook O(n*m) multiplication; result is arena-allocated.
```

### ada83.c:604

```
// Euclid's algorithm; both operands are treated as non-negative.
```

### ada83.c:607

```
// Simultaneous division and remainder (Knuth Algorithm D).
```

### ada83.c:613

```
// Parse a decimal digit string using SIMD-accelerated 8-digit groups.
```

### ada83.c:616

```
// If the magnitude fits in a signed 64-bit word, write it to *out and return true.
```

### ada83.c:621

```
// Big_Real
```

### ada83.c:622

```
// A real number represented as significand * 10^exponent. The significand is an
```

### ada83.c:623

```
// arbitrary-precision integer, so no precision is lost during literal parsing.
```

### ada83.c:625

```
// The integer part of the scientific notation
```

### ada83.c:626

```
// Power-of-ten scaling factor (may be negative)
```

### ada83.c:629

```
// Arena-allocate a zero-valued Big_Real
```

### ada83.c:631

```
// Best-effort - may lose precision
```

### ada83.c:633

```
// Rational
```

### ada83.c:634

```
//
```

### ada83.c:635

```
// An exact quotient p/q with the invariant that gcd(|p|,|q|) = 1 and q > 0.
```

### ada83.c:636

```
// Used for fixed-point delta arithmetic and static expression folding.
```

### ada83.c:637

```
//
```

### ada83.c:639

```
// Signed numerator (the sign lives here)
```

### ada83.c:640

```
// Positive denominator, always >= 1
```

### ada83.c:645

```
// Construct the rational value/1
```

### ada83.c:646

```
// a/b + c/d = (ad + bc) / bd, then reduce
```

### ada83.c:647

```
// Exact rational subtraction.
```

### ada83.c:648

```
// (a*c) / (b*d), then reduce.
```

### ada83.c:649

```
// (a*d) / (b*c), then reduce.
```

### ada83.c:650

```
// Exponent should be unsigned ???
```

### ada83.c:651

```
// returns -1, 0, or +1.
```

### ada83.c:652

```
// may lose precision.
```

### ada83.c:654

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:655

```
//
```

### ada83.c:656

```
// §7. Lexical Analysis - Token kinds, lexer state, scanning functions
```

### ada83.c:657

```
//
```

### ada83.c:658

```
// The scanner converts a flat character buffer into a stream of typed tokens. Token_Kind enumerates
```

### ada83.c:659

```
// every lexeme in the Ada 83 grammar: identifiers, numeric and string literals, delimiters,
```

### ada83.c:660

```
// operator symbols, and the sixty-three reserved words of RM 2.9. The lexer maintains a sliding
```

### ada83.c:661

```
// cursor with one character of look-ahead; SIMD fast-paths accelerate whitespace skipping and
```

### ada83.c:662

```
// identifier scanning on x86-64 and ARM64.
```

### ada83.c:663

```
//                                                                          
```

### ada83.c:664

```
// The lexer works as an iterator: each call to Lexer_Next_Token advances the source stream by      
```

### ada83.c:665

```
// one token and returns it. The function dispatches on the first character to select the          
```

### ada83.c:666

```
// appropriate scanner, with a final switch statement handling operators and delimiters.  
```

### ada83.c:667

```
//
```

### ada83.c:668

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:670

```
// Convience macros for token kind names for diagnostics and error messages
```

### ada83.c:675

```
// Token_Kind enumerates every lexeme in the Ada 83 grammar: identifiers, numeric and string
```

### ada83.c:676

```
// literals, delimiters, operator symbols, and reserved words.
```

### ada83.c:679

```
// Sentinels
```

### ada83.c:682

```
// Literals
```

### ada83.c:685

```
// Delimiters
```

### ada83.c:688

```
// Compound delimiters
```

### ada83.c:691

```
// Operators
```

### ada83.c:695

```
// Keywords
```

### ada83.c:710

```
// Token to string mapping table for parsing
```

### ada83.c:713

```
// Sentinels
```

### ada83.c:716

```
// Literals
```

### ada83.c:720

```
// Symbols
```

### ada83.c:728

```
// Keywords
```

### ada83.c:742

```
// Clean up the convience macros
```

### ada83.c:747

```
// Token Record
```

### ada83.c:748

```
//
```

### ada83.c:749

```
// A single lexeme produced by the scanner. The two anonymous unions overlay machine-width and
```

### ada83.c:750

```
// arbitrary-precision values so that each token is compact.
```

### ada83.c:751

```
//
```

### ada83.c:753

```
// Discriminant identifying the token class
```

### ada83.c:754

```
// Where in the source file this token began
```

### ada83.c:755

```
// Raw source text of the lexeme
```

### ada83.c:756

```
// Native number value
```

### ada83.c:757

```
// Machine-width value for integer literals
```

### ada83.c:758

```
// Machine-width value for real literals
```

### ada83.c:760

```
// Big representation
```

### ada83.c:761

```
// Arbitrary-precision integer (overflow path)
```

### ada83.c:762

```
// Arbitrary-precision real (overflow path)
```

### ada83.c:764

```
// Exact value of a based real literal, or NULL
```

### ada83.c:767

```
// Construct a zero-valued token with the given kind, location, and text.
```

### ada83.c:778

```
// Lexer State
```

### ada83.c:779

```
//
```

### ada83.c:780

```
// The lexer maintains a sliding cursor over the source buffer. The prev_token_kind field is needed
```

### ada83.c:781

```
// to distinguish the character literal tick from the attribute tick in contexts like X'First vs 'A'.
```

### ada83.c:782

```
//
```

### ada83.c:784

```
// First byte of the source buffer
```

### ada83.c:785

```
// Current scan position (bump pointer)
```

### ada83.c:786

```
// One past the last byte of the source buffer
```

### ada83.c:787

```
// Source file name for error reporting
```

### ada83.c:788

```
// Current one-based line number
```

### ada83.c:789

```
// Current one-based column number
```

### ada83.c:790

```
// Kind of the most recently returned token
```

### ada83.c:793

```
// Construct a fresh lexer over the given source buffer, positioned at line 1, column 1.
```

### ada83.c:806

```
// Lexer_Peek
```

### ada83.c:807

```
//
```

### ada83.c:808

```
// Return the character `offset' bytes ahead of the cursor without consuming it. We return '\0' when
```

### ada83.c:809

```
// `offset' would read past the end of the source buffer.
```

### ada83.c:810

```
//
```

### ada83.c:815

```
// Consume and return the current character, advancing line/column tracking. Returns '\0' when the
```

### ada83.c:816

```
// source buffer has been exhausted.
```

### ada83.c:825

```
// Skip whitespace and Ada comments (-- to end of line) using SIMD fast paths.
```

### ada83.c:827

```
// Produce the next token from the input stream.
```

### ada83.c:830

```
// Scan an Ada identifier or reserved word.
```

### ada83.c:832

```
// Scan a numeric literal (integer or real, with optional base and exponent).
```

### ada83.c:834

```
// Scan a single character literal delimited by tick marks.
```

### ada83.c:836

```
// Scan a string literal delimited by double quotes (handles "" escapes).
```

### ada83.c:838

```
// Return the integer value of a hex digit character, or -1 if not a digit.
```

### ada83.c:841

```
// Linear scan over Token_Name is adequate for the sixty-three reserved words of Ada 83.
```

### ada83.c:842

```
// AND_THEN and OR_ELSE contain spaces and are never matched as bare identifiers, so they're
```

### ada83.c:843

```
// harmlessly skipped.
```

### ada83.c:857

```
// One row per Ada operator designator. Single source of truth for the
```

### ada83.c:858

```
// token <-> source-text name mapping, the operator's class (RM 4.5), and its
```

### ada83.c:859

```
// LLVM comparison predicates (NULL for non-comparison operators).
```

### ada83.c:860

```
//
```

### ada83.c:861

```
// The names are bare (unquoted): the lexer strips quotes from operator symbol
```

### ada83.c:862

```
// declarations (RM 6.1), so symbol table entries store bare names like
```

### ada83.c:863

```
// +, mod, **. Match that convention here.
```

### ada83.c:867

```
// = /= < <= > >=
```

### ada83.c:868

```
// and or xor not
```

### ada83.c:869

```
// + - * / mod rem ** abs
```

### ada83.c:870

```
// &
```

### ada83.c:871

```
// abs not
```

### ada83.c:872

```
// icmp predicate
```

### ada83.c:874

```
// fcmp predicate (ordered; unordered
```

### ada83.c:875

```
// for /= so NaN compares unequal)
```

### ada83.c:908

```
// Row for an operator token, or NULL when the token is not an operator.
```

### ada83.c:916

```
// Row for an operator's bare source-text name (e.g. "+", "mod"), or NULL
```

### ada83.c:917

```
// when the name is not an operator designator.
```

### ada83.c:925

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:926

```
//
```

### ada83.c:927

```
// §8. Syntax - Node kinds, the syntax tree, node lists
```

### ada83.c:928

```
//
```

### ada83.c:929

```
// The AST uses a tagged-union design: each Syntax_Node carries a Node_Kind tag and a union payload
```

### ada83.c:930

```
// specific to that kind. The tree is a forest - one root per compilation unit, with shared subtrees
```

### ada83.c:931

```
// within a unit where the grammar allows (e.g. subtype marks referenced from multiple
```

### ada83.c:932

```
// declarations). All nodes are arena-allocated and never individually freed.
```

### ada83.c:933

```
//
```

### ada83.c:934

```
// We also map to a Node_Kind via the Syntax_Node record carries kind, location, type annotation,
```

### ada83.c:935

```
// symbol link, and a payload union discriminated by the kind tag.
```

### ada83.c:936

```
//
```

### ada83.c:937

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:939

```
// Forward Declarations
```

### ada83.c:945

```
// Node List
```

### ada83.c:946

```
//
```

### ada83.c:947

```
// A growable array of Syntax_Node pointers. Used everywhere a grammar rule produces a sequence
```

### ada83.c:948

```
// (statement lists, declaration lists, parameter lists, etc.).
```

### ada83.c:949

```
//
```

### ada83.c:951

```
// Arena-allocated pointer array
```

### ada83.c:952

```
// Number of nodes currently stored
```

### ada83.c:953

```
// Allocated slots before a resize is needed
```

### ada83.c:956

```
// Append `node' to the list, growing the backing array if necessary.
```

### ada83.c:959

```
// Node Kinds
```

### ada83.c:962

```
// Literals and primaries
```

### ada83.c:966

```
// Expressions
```

### ada83.c:969

```
// Type definitions
```

### ada83.c:976

```
// Statements
```

### ada83.c:980

```
// Declarations
```

### ada83.c:989

```
// Generic formals
```

### ada83.c:995

```
// Attribute Designator Kind  (extracted from NK_ATTRIBUTE payload)
```

### ada83.c:996

```
//
```

### ada83.c:997

```
// One enumerator per attribute designator the compiler handles (Ada 83
```

### ada83.c:998

```
// Appendix A plus the extensions this compiler accepts). ATTRIBUTE_UNKNOWN
```

### ada83.c:999

```
// is deliberately zero so a zero-initialized node reads as "no known
```

### ada83.c:1000

```
// designator" and lands on the same fall-through path an unrecognized
```

### ada83.c:1001

```
// attribute name takes.
```

### ada83.c:1002

```
//
```

### ada83.c:1003

```
// Each X-macro row is (enumerator, designator, type_view) where the flag
```

### ada83.c:1004

```
// says whether the attribute applies to the prefix's TYPE (or, for the
```

### ada83.c:1005

```
// RM 13.7.2/13.7.3 storage-layout attributes, to a component's storage)
```

### ada83.c:1006

```
// rather than to a designated object, suppressing the implicit access
```

### ada83.c:1007

```
// dereference of RM 4.1(3) in both name resolution and code generation.
```

### ada83.c:1009

```
/* Array and scalar bounds (RM 3.5, 3.6.2) */
```

### ada83.c:1014

```
/* Representation (RM 13.7.2, 13.7.3) */
```

### ada83.c:1023

```
/* Scalar and enumeration operations (RM 3.5.5) */
```

### ada83.c:1036

```
/* Access views */
```

### ada83.c:1039

```
/* Floating-point model (RM 3.5.8) */
```

### ada83.c:1051

```
/* Fixed-point model (RM 3.5.10) */
```

### ada83.c:1055

```
/* Machine model (RM 3.5.8) */
```

### ada83.c:1062

```
/* Objects and tasks (RM 3.7.4, 9.9) */
```

### ada83.c:1067

```
/* Type views (RM 3.3.3) */
```

### ada83.c:1079

```
// Per-attribute properties, indexed by Attribute_Kind. The designator slice
```

### ada83.c:1080

```
// backs the name-to-kind lookup; the view flag gates the implicit access
```

### ada83.c:1081

```
// dereference of RM 4.1(3) (see ATTRIBUTE_KIND_LIST).
```

### ada83.c:1088

```
// The single designator-name-to-kind lookup: linear scan over the table,
```

### ada83.c:1089

```
// case-insensitive (same shape as Lookup_Keyword). Unrecognized names map
```

### ada83.c:1090

```
// to ATTRIBUTE_UNKNOWN.
```

### ada83.c:1093

```
// Parameter Mode Kind  (extracted from NK_PARAM_SPEC payload)
```

### ada83.c:1096

```
// Generic Formal Type Definition Kind  (extracted from NK_GENERIC_TYPE_PARAM payload)
```

### ada83.c:1102

```
// Generic Object Mode Kind  (extracted from NK_GENERIC_OBJECT_PARAM payload)
```

### ada83.c:1105

```
// One resolved formal-to-actual association of a generic instantiation
```

### ada83.c:1106

```
// (RM 12.3): filled by the actual-resolution passes, consumed by the formal
```

### ada83.c:1107

```
// binder and by the matching validators.
```

### ada83.c:1108

```
// One instantiation-elaboration agreement check (RM 12.3(81)): two scalar
```

### ada83.c:1109

```
// subtypes that must have equal bounds, captured at resolution time (where
```

### ada83.c:1110

```
// the formal part's marks are resolvable) and emitted at the instantiation
```

### ada83.c:1111

```
// point.
```

### ada83.c:1113

```
// What the formal part requires
```

### ada83.c:1114

```
// What the actual supplies
```

### ada83.c:1115

```
// Diagnostic tag (index/component/discriminant)
```

### ada83.c:1119

```
// Name of the generic formal parameter
```

### ada83.c:1120

```
// Actual type supplied at instantiation
```

### ada83.c:1121

```
// Actual subprogram supplied at instantiation
```

### ada83.c:1122

```
// Actual expression (for object formals)
```

### ada83.c:1123

```
// Attribute actual (a default like IS
```

### ada83.c:1124

```
// T'SUCC); becomes a wrapper body
```

### ada83.c:1125

```
// Intrinsic operator kind, or TK_EOF
```

### ada83.c:1126

```
// "/=" actual: the bound "=", negated
```

### ada83.c:1127

```
// through a wrapper body (RM 6.7)
```

### ada83.c:1130

```
// What an NK_APPLY denotes. The parser spells call, indexing, slicing,
```

### ada83.c:1131

```
// conversion, and operator-as-function all as the one syntactic shape `X (…)`
```

### ada83.c:1132

```
// (see Parse_Postfix); Resolve_Apply is the single oracle that decides which of
```

### ada83.c:1133

```
// these an application is, and records its verdict here. Code generation then
```

### ada83.c:1134

```
// obeys the verdict instead of re-deriving it from symbol kinds and argument
```

### ada83.c:1135

```
// shapes. APPLY_UNRESOLVED is the zero value every fresh or cloned node wears
```

### ada83.c:1136

```
// until the oracle has spoken — a node still wearing it at emit time is a
```

### ada83.c:1137

```
// resolver bug, never an emit-time question to re-answer.
```

### ada83.c:1139

```
// not yet classified (parser / clone default)
```

### ada83.c:1140

```
// function or procedure call (incl. generic recursion)
```

### ada83.c:1141

```
// predefined operator named as a function: "+" (X, Y)
```

### ada83.c:1142

```
// value conversion: Integer (X)
```

### ada83.c:1143

```
// constrained subtype indication: String (1 .. N)
```

### ada83.c:1144

```
// indexed component: A (I)
```

### ada83.c:1145

```
// slice: A (L .. H)
```

### ada83.c:1146

```
// task entry call: T.E (X)
```

### ada83.c:1149

```
// Syntax Node Record - a union discriminated by Node_Kind
```

### ada83.c:1151

```
// Discriminant tag
```

### ada83.c:1152

```
// Where this construct appeared in source
```

### ada83.c:1153

```
// Set by semantic analysis, NULL before
```

### ada83.c:1154

```
// Set by name resolution, NULL before
```

### ada83.c:1155

```
// Two-pass overload resolution (RM 8.6): Analyze_Expr fills `interps` with the
```

### ada83.c:1156

```
// full set of candidate interpretations bottom-up without committing; then
```

### ada83.c:1157

```
// Resolve_Expr picks the unique one compatible with the context type and
```

### ada83.c:1158

```
// writes `type`/`symbol`. NULL until analysis; a single-interp set for
```

### ada83.c:1159

```
// unambiguous nodes.
```

### ada83.c:1161

```
// Folded static-value cache: Eval_Const_Numeric memoizes a node's compile-time
```

### ada83.c:1162

```
// numeric value here once, so the constant-folding deep walk is not repeated
```

### ada83.c:1163

```
// every time resolution and codegen ask for the same subtree's value.
```

### ada83.c:1167

```
// case kind is
```

### ada83.c:1169

```
// when NK_INTEGER =>
```

### ada83.c:1171

```
// Machine-width literal value
```

### ada83.c:1172

```
// Arbitrary-precision overflow
```

### ada83.c:1175

```
// when NK_REAL =>
```

### ada83.c:1177

```
// Machine-width float value
```

### ada83.c:1178

```
// Arbitrary-precision overflow
```

### ada83.c:1179

```
// Exact value of a based real literal, or NULL
```

### ada83.c:1182

```
// when NK_STRING | NK_CHARACTER | NK_IDENTIFIER =>
```

### ada83.c:1184

```
// Raw source text
```

### ada83.c:1185

```
// A string literal written as ("...")
```

### ada83.c:1188

```
// when NK_SELECTED =>
```

### ada83.c:1190

```
// The dotted prefix expression
```

### ada83.c:1191

```
// The selected component name
```

### ada83.c:1192

```
// Record component slot the resolver picked,
```

### ada83.c:1193

```
// -1 until then. The emitter must honor it:
```

### ada83.c:1194

```
// a formal private type's discriminant name
```

### ada83.c:1195

```
// can collide with a component of the actual
```

### ada83.c:1196

```
// (RM 12.1), which a by-name re-lookup at
```

### ada83.c:1197

```
// emission cannot re-arbitrate (cc3240a)
```

### ada83.c:1200

```
// when NK_ATTRIBUTE =>
```

### ada83.c:1202

```
// The prefix before the tick
```

### ada83.c:1203

```
// Attribute designator (kept for diagnostics)
```

### ada83.c:1204

```
// Designator kind; set wherever name is set
```

### ada83.c:1205

```
// Optional attribute arguments
```

### ada83.c:1208

```
// when NK_QUALIFIED =>
```

### ada83.c:1210

```
// Qualifying subtype
```

### ada83.c:1211

```
// Qualified expression
```

### ada83.c:1214

```
// when NK_BINARY_OP =>
```

### ada83.c:1216

```
// Operator token kind
```

### ada83.c:1217

```
// Left operand
```

### ada83.c:1218

```
// Right operand
```

### ada83.c:1221

```
// when NK_UNARY_OP =>
```

### ada83.c:1223

```
// Operator token kind
```

### ada83.c:1224

```
// Sole operand
```

### ada83.c:1227

```
// when NK_AGGREGATE =>
```

### ada83.c:1229

```
// Component associations
```

### ada83.c:1230

```
// True if using named notation
```

### ada83.c:1231

```
// True if parenthesized
```

### ada83.c:1234

```
// when NK_ALLOCATOR =>
```

### ada83.c:1236

```
// Allocated subtype
```

### ada83.c:1237

```
// Initializer or NULL
```

### ada83.c:1238

```
// Context access type from assignment/return (RM 4.8)
```

### ada83.c:1241

```
// when NK_APPLY =>
```

### ada83.c:1243

```
// Called / indexed / sliced / converted name
```

### ada83.c:1244

```
// Actual parameter list
```

### ada83.c:1245

```
// What this application is (set by Resolve_Apply)
```

### ada83.c:1248

```
//  when NK_RANGE =>
```

### ada83.c:1250

```
// Low bound expression
```

### ada83.c:1251

```
// High bound expression
```

### ada83.c:1252

```
// RM 3.5: when this range was written `S RANGE L..H` (a range constraint
```

### ada83.c:1253

```
// on a named subtype), the subtype mark S the range must be compatible
```

### ada83.c:1254

```
// with. NULL for a bare `L..H`. Set when a slice/discrete_range normalises
```

### ada83.c:1255

```
// a subtype_indication to its inner range.
```

### ada83.c:1259

```
// when NK_ASSOCIATION =>
```

### ada83.c:1261

```
// Discrete choices or names
```

### ada83.c:1262

```
// Associated value
```

### ada83.c:1265

```
// when NK_SUBTYPE_INDICATION =>
```

### ada83.c:1267

```
// Named subtype
```

### ada83.c:1268

```
// Optional constraint or NULL
```

### ada83.c:1269

```
// True for `subtype_mark RANGE <>` (unconstrained index)
```

### ada83.c:1272

```
// when NK_INDEX_CONSTRAINT =>
```

### ada83.c:1274

```
// Discrete range per dimension
```

### ada83.c:1277

```
// when NK_RANGE_CONSTRAINT =>
```

### ada83.c:1279

```
// The constraining range
```

### ada83.c:1282

```
// when NK_DISCRIMINANT_CONSTRAINT =>
```

### ada83.c:1284

```
// Discriminant value associations
```

### ada83.c:1287

```
// when NK_DIGITS_CONSTRAINT =>
```

### ada83.c:1289

```
// Digits expression
```

### ada83.c:1290

```
// Optional range or NULL
```

### ada83.c:1293

```
// when NK_DELTA_CONSTRAINT =>
```

### ada83.c:1295

```
// Delta expression
```

### ada83.c:1296

```
// Optional range or NULL
```

### ada83.c:1299

```
// when NK_ARRAY_TYPE =>
```

### ada83.c:1301

```
// Index subtype definitions
```

### ada83.c:1302

```
// Element subtype indication
```

### ada83.c:1303

```
// True for constrained arrays
```

### ada83.c:1306

```
// when NK_RECORD_TYPE =>
```

### ada83.c:1308

```
// Discriminant specifications
```

### ada83.c:1309

```
// Component declarations
```

### ada83.c:1310

```
// Variant part or NULL
```

### ada83.c:1311

```
// True for null record
```

### ada83.c:1314

```
// when NK_ACCESS_TYPE =>
```

### ada83.c:1316

```
// Designated subtype
```

### ada83.c:1317

```
// Access-to-constant
```

### ada83.c:1320

```
// when NK_DERIVED_TYPE =>
```

### ada83.c:1322

```
// Parent subtype indication
```

### ada83.c:1323

```
// Optional constraint or NULL
```

### ada83.c:1326

```
// when NK_ENUMERATION_TYPE =>
```

### ada83.c:1328

```
// Enumeration literal names
```

### ada83.c:1331

```
// when NK_INTEGER_TYPE =>
```

### ada83.c:1333

```
// Range constraint
```

### ada83.c:1334

```
// Modular type modulus
```

### ada83.c:1335

```
// True for mod types
```

### ada83.c:1338

```
// when NK_REAL_TYPE =>
```

### ada83.c:1340

```
// Digits or delta expression
```

### ada83.c:1341

```
// Optional range or NULL
```

### ada83.c:1342

```
// Delta for fixed-point
```

### ada83.c:1345

```
// when NK_COMPONENT_DECL =>
```

### ada83.c:1347

```
// Defining identifiers
```

### ada83.c:1348

```
// Component subtype indication
```

### ada83.c:1349

```
// Default expression or NULL
```

### ada83.c:1352

```
// when NK_VARIANT_PART =>
```

### ada83.c:1354

```
// Discriminant name
```

### ada83.c:1355

```
// Variant alternatives
```

### ada83.c:1358

```
// when NK_VARIANT =>
```

### ada83.c:1360

```
// Discrete choice list
```

### ada83.c:1361

```
// Component declarations
```

### ada83.c:1362

```
// Nested variant part or NULL
```

### ada83.c:1365

```
// when NK_DISCRIMINANT_SPEC =>
```

### ada83.c:1367

```
// Discriminant names
```

### ada83.c:1368

```
// Discriminant subtype
```

### ada83.c:1369

```
// Default value or NULL
```

### ada83.c:1372

```
// when NK_ASSIGNMENT =>
```

### ada83.c:1374

```
// Left-hand side name
```

### ada83.c:1375

```
// Right-hand side expression
```

### ada83.c:1378

```
// when NK_RETURN =>
```

### ada83.c:1380

```
// Return value or NULL
```

### ada83.c:1383

```
// when NK_IF =>
```

### ada83.c:1385

```
// Boolean condition
```

### ada83.c:1386

```
// Then-part statements
```

### ada83.c:1387

```
// Elsif clauses
```

### ada83.c:1388

```
// Else-part statements
```

### ada83.c:1391

```
// when NK_CASE =>
```

### ada83.c:1393

```
// Selecting expression
```

### ada83.c:1394

```
// Case alternatives
```

### ada83.c:1397

```
// when NK_LOOP =>
```

### ada83.c:1399

```
// Optional loop label
```

### ada83.c:1400

```
// Resolved label symbol
```

### ada83.c:1401

```
// For/while or NULL
```

### ada83.c:1402

```
// Loop body
```

### ada83.c:1403

```
// True for reverse iteration
```

### ada83.c:1406

```
// when NK_BLOCK =>
```

### ada83.c:1408

```
// Optional block label
```

### ada83.c:1409

```
// Resolved label symbol
```

### ada83.c:1410

```
// Declarative part
```

### ada83.c:1411

```
// Statement sequence
```

### ada83.c:1412

```
// Exception handlers
```

### ada83.c:1413

```
// RM 9.4: declarations create task
```

### ada83.c:1414

```
// dependents (set at resolution)
```

### ada83.c:1415

```
// Program-unique id of this master
```

### ada83.c:1416

```
// (resolution; 0 = not a master)
```

### ada83.c:1419

```
// when NK_EXIT =>
```

### ada83.c:1421

```
// Target loop name or empty
```

### ada83.c:1422

```
// When condition or NULL
```

### ada83.c:1423

```
// Resolved loop symbol
```

### ada83.c:1426

```
// when NK_GOTO =>
```

### ada83.c:1428

```
// Target label name
```

### ada83.c:1429

```
// Resolved label symbol
```

### ada83.c:1432

```
// when NK_LABEL =>
```

### ada83.c:1434

```
// Label identifier
```

### ada83.c:1435

```
// Labelled statement
```

### ada83.c:1436

```
// Label symbol
```

### ada83.c:1439

```
// when NK_RAISE =>
```

### ada83.c:1441

```
// Exception name or NULL for reraise
```

### ada83.c:1444

```
// when NK_ACCEPT =>
```

### ada83.c:1446

```
// Accepted entry name
```

### ada83.c:1447

```
// Entry family index or NULL
```

### ada83.c:1448

```
// Formal parameters
```

### ada83.c:1449

```
// Accept body, then select-alternative trailing
```

### ada83.c:1450

```
// Count of true rendezvous-body statements; the
```

### ada83.c:1451

```
// rest of `statements` runs AFTER the rendezvous
```

### ada83.c:1452

```
// completes (select-alternative trailing stmts)
```

### ada83.c:1453

```
// Resolved entry symbol
```

### ada83.c:1454

```
// Select-alternative guard (WHEN cond), or NULL
```

### ada83.c:1455

```
// Codegen: a selective wait pre-evaluated the
```

### ada83.c:1456

```
// family index into this alloca (RM 9.7.1);
```

### ada83.c:1457

```
// accept generation reloads it instead of
```

### ada83.c:1458

```
// re-evaluating. 0 when not in a select.
```

### ada83.c:1461

```
// when NK_SELECT =>
```

### ada83.c:1463

```
// Select alternatives
```

### ada83.c:1464

```
// Else part or NULL
```

### ada83.c:1467

```
// when NK_DELAY =>
```

### ada83.c:1469

```
// Duration expression
```

### ada83.c:1470

```
// WHEN guard for a select delay alternative
```

### ada83.c:1473

```
// when NK_NULL_STMT (as a select TERMINATE alternative) =>
```

### ada83.c:1475

```
// WHEN guard for a select terminate alternative
```

### ada83.c:1478

```
// when NK_ABORT =>
```

### ada83.c:1480

```
// Task objects to abort
```

### ada83.c:1483

```
// when NK_OBJECT_DECL =>
```

### ada83.c:1485

```
// Defining identifiers
```

### ada83.c:1486

```
// Object subtype indication
```

### ada83.c:1487

```
// Initial value or NULL
```

### ada83.c:1488

```
// True for constant declarations
```

### ada83.c:1489

```
// True for aliased objects
```

### ada83.c:1490

```
// True for renaming declarations
```

### ada83.c:1493

```
// when NK_TYPE_DECL =>
```

### ada83.c:1495

```
// Type name
```

### ada83.c:1496

```
// Known discriminant part
```

### ada83.c:1497

```
// Type definition
```

### ada83.c:1498

```
// Limited type
```

### ada83.c:1499

```
// Private type
```

### ada83.c:1502

```
// when NK_EXCEPTION_DECL =>
```

### ada83.c:1504

```
// Exception identifiers
```

### ada83.c:1505

```
// Renamed exception or NULL
```

### ada83.c:1508

```
// when NK_PROCEDURE_SPEC | NK_FUNCTION_SPEC =>
```

### ada83.c:1510

```
// Subprogram name
```

### ada83.c:1511

```
// Formal parameter list
```

### ada83.c:1512

```
// Return type or NULL
```

### ada83.c:1513

```
// Renamed entity or NULL
```

### ada83.c:1516

```
// when NK_PROCEDURE_BODY | NK_FUNCTION_BODY =>
```

### ada83.c:1518

```
// The subprogram spec
```

### ada83.c:1519

```
// Declarative part
```

### ada83.c:1520

```
// Body statements
```

### ada83.c:1521

```
// Exception handlers
```

### ada83.c:1522

```
// Is a subunit stub
```

### ada83.c:1523

```
// Already emitted
```

### ada83.c:1524

```
// RM 9.4: declarations create task
```

### ada83.c:1525

```
// dependents (set at resolution; on a
```

### ada83.c:1526

```
// generic template the formal-private
```

### ada83.c:1527

```
// clause makes it conservative)
```

### ada83.c:1528

```
// Program-unique id of this master
```

### ada83.c:1529

```
// (resolution; 0 = not a master)
```

### ada83.c:1532

```
// when NK_PACKAGE_SPEC =>
```

### ada83.c:1534

```
// Package name
```

### ada83.c:1535

```
// Visible part declarations
```

### ada83.c:1536

```
// Private part declarations
```

### ada83.c:1538

```
// Pending-activation entries created while elaborating this nested
```

### ada83.c:1539

```
// spec (RM 9.3(3)): a package WITH a body activates them at the
```

### ada83.c:1540

```
// body's begin; a bodyless package leaves them to the enclosing
```

### ada83.c:1541

```
// master's begin (the implicit body at the end of the region).
```

### ada83.c:1546

```
// when NK_PACKAGE_BODY =>
```

### ada83.c:1548

```
// Package name
```

### ada83.c:1549

```
// Body declarations
```

### ada83.c:1550

```
// Body statements
```

### ada83.c:1551

```
// Exception handlers
```

### ada83.c:1552

```
// Is a subunit stub
```

### ada83.c:1553

```
// Elaboration function already emitted
```

### ada83.c:1554

```
// For a generic package body: the
```

### ada83.c:1555

```
// enclosing package-body scope, so an
```

### ada83.c:1556

```
// instance can see it (RM 8.1, 12.3)
```

### ada83.c:1559

```
// when NK_PACKAGE_RENAMING =>
```

### ada83.c:1561

```
// New package name
```

### ada83.c:1562

```
// Renamed package name
```

### ada83.c:1565

```
// when NK_TASK_SPEC =>
```

### ada83.c:1567

```
// Task name
```

### ada83.c:1568

```
// Entry declarations
```

### ada83.c:1569

```
// True for task type
```

### ada83.c:1572

```
// when NK_TASK_BODY =>
```

### ada83.c:1574

```
// Task name
```

### ada83.c:1575

```
// Body declarations
```

### ada83.c:1576

```
// Body statements
```

### ada83.c:1577

```
// Exception handlers
```

### ada83.c:1578

```
// Is a subunit stub
```

### ada83.c:1579

```
// RM 9.4: declarations create task
```

### ada83.c:1580

```
// dependents (set at resolution)
```

### ada83.c:1581

```
// Program-unique id of this master
```

### ada83.c:1582

```
// (resolution; 0 = not a master)
```

### ada83.c:1585

```
// when NK_ENTRY_DECL =>
```

### ada83.c:1587

```
// Entry name
```

### ada83.c:1588

```
// Formal parameters
```

### ada83.c:1589

```
// Family index constraints
```

### ada83.c:1592

```
// when NK_PARAM_SPEC =>
```

### ada83.c:1594

```
// Parameter identifiers
```

### ada83.c:1595

```
// Parameter subtype indication
```

### ada83.c:1596

```
// Default value or NULL
```

### ada83.c:1600

```
// when NK_GENERIC_DECL =>
```

### ada83.c:1602

```
// Generic formal parameters
```

### ada83.c:1603

```
// The generic unit declaration
```

### ada83.c:1606

```
// when NK_GENERIC_INST =>
```

### ada83.c:1608

```
// Name of generic template
```

### ada83.c:1609

```
// Actual parameter list
```

### ada83.c:1610

```
// Instance defining name
```

### ada83.c:1611

```
// TK_PACKAGE / TK_PROCEDURE / TK_FUNCTION
```

### ada83.c:1614

```
// when NK_GENERIC_TYPE_PARAM =>
```

### ada83.c:1617

```
// Form of the generic formal type
```

### ada83.c:1618

```
// Type definition detail
```

### ada83.c:1619

```
// Discriminant part
```

### ada83.c:1622

```
// when NK_GENERIC_OBJECT_PARAM =>
```

### ada83.c:1624

```
// Object parameter names
```

### ada83.c:1625

```
// Object subtype indication
```

### ada83.c:1626

```
// Default value or NULL
```

### ada83.c:1630

```
// when NK_GENERIC_SUBPROGRAM_PARAM =>
```

### ada83.c:1632

```
// Formal subprogram name
```

### ada83.c:1633

```
// Formal parameters
```

### ada83.c:1634

```
// Return type or NULL
```

### ada83.c:1635

```
// Default subprogram or NULL
```

### ada83.c:1636

```
// True for generic function
```

### ada83.c:1637

```
// True for <>
```

### ada83.c:1640

```
// when NK_USE_CLAUSE =>
```

### ada83.c:1642

```
// Used package names
```

### ada83.c:1645

```
// when NK_PRAGMA =>
```

### ada83.c:1647

```
// Pragma identifier
```

### ada83.c:1648

```
// Pragma arguments
```

### ada83.c:1651

```
// when NK_EXCEPTION_HANDLER =>
```

### ada83.c:1653

```
// Handled exception choices
```

### ada83.c:1654

```
// Handler statements
```

### ada83.c:1657

```
// when NK_REPRESENTATION_CLAUSE =>
```

### ada83.c:1659

```
// Entity being represented
```

### ada83.c:1660

```
// Representation attribute (diagnostics)
```

### ada83.c:1661

```
// Designator kind of that attribute
```

### ada83.c:1662

```
// Representation expression
```

### ada83.c:1663

```
// Record rep component clauses
```

### ada83.c:1664

```
// True for record rep clause
```

### ada83.c:1665

```
// True for enum rep clause
```

### ada83.c:1666

```
// True for FOR X USE AT addr
```

### ada83.c:1669

```
// when NK_CONTEXT_CLAUSE =>
```

### ada83.c:1671

```
// With clauses
```

### ada83.c:1672

```
// Use clauses
```

### ada83.c:1673

```
// Context pragmas (Elaborate, Elaborate_All, ...)
```

### ada83.c:1676

```
// when NK_COMPILATION_UNIT =>
```

### ada83.c:1678

```
// Context clause
```

### ada83.c:1679

```
// The library unit
```

### ada83.c:1680

```
// Separate parent or NULL
```

### ada83.c:1681

```
// Subunit grafted into its generic parent's
```

### ada83.c:1682

```
// template; no standalone resolution/emission
```

### ada83.c:1683

```
// Spec's applicable context already folded in
```

### ada83.c:1688

```
// Node constructor
```

### ada83.c:1691

```
// An explicit dereference, PREFIX.ALL (RM 4.1.3): a unary operator node
```

### ada83.c:1692

```
// whose operator token is ALL.
```

### ada83.c:1695

```
// The explicit range of a constrained subtype indication - the NK_RANGE node
```

### ada83.c:1696

```
// of `S RANGE L .. H` - or NULL when the node is not a subtype indication,
```

### ada83.c:1697

```
// carries no range constraint, or the constraint's range is a range
```

### ada83.c:1698

```
// attribute rather than an explicit pair of bounds (RM 3.5).
```

### ada83.c:1701

```
// A child edge of a syntax node: a single node pointer (is_list 0) or a node
```

### ada83.c:1702

```
// list (is_list 1), at a byte offset into the node. The tree's shape is the
```

### ada83.c:1703

```
// data: SYNTAX_TREE_SHAPE_LIST names every node kind exactly once together
```

### ada83.c:1704

```
// with its child edges, and the static assertion beside the table definition
```

### ada83.c:1705

```
// in the §8 body keeps it total -- a new node kind does not compile until
```

### ada83.c:1706

```
// this list declares its children (or declares, by listing none, that it has
```

### ada83.c:1707

```
// none). Generic instantiation cloning (§16) walks this table. A row
```

### ada83.c:1708

```
// terminates at offset 0 (which is `kind`, never a child); rows hold at most
```

### ada83.c:1709

```
// five edges plus the terminator -- widen the table's row width if a kind
```

### ada83.c:1710

```
// ever grows a sixth child.
```

### ada83.c:1715

```
/* Literals and primaries */
```

### ada83.c:1726

```
/* Expressions */
```

### ada83.c:1734

```
/* Type definitions and constraints */
```

### ada83.c:1752

```
/* Statements */
```

### ada83.c:1769

```
/* Declarations */
```

### ada83.c:1796

```
/* Generic formals */
```

### ada83.c:1801

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:1802

```
//
```

### ada83.c:1803

```
// §9. Recursive descent parser
```

### ada83.c:1804

```
//
```

### ada83.c:1805

```
// Recursive descent mirrors the grammar, making the grammar itself the invariant
```

### ada83.c:1806

```
//
```

### ada83.c:1807

```
// Key design decisions:
```

### ada83.c:1808

```
//
```

### ada83.c:1809

```
//   1. UNIFIED APPLY NODE - All X(...) forms parse as NK_APPLY. Semantic analysis later
```

### ada83.c:1810

```
//      distinguishes calls, indexing, slicing, and type conversions.
```

### ada83.c:1811

```
//   2. UNIFIED ASSOCIATION PARSING - One helper handles positional, named, and choice
```

### ada83.c:1812

```
//      associations used in aggregates, calls, and generic actuals.
```

### ada83.c:1813

```
//   3. UNIFIED POSTFIX CHAIN - One loop handles .selector, 'attribute, and (args).                                                
```

### ada83.c:1814

```
//                                                                                                  
```

### ada83.c:1815

```
// The grammar encodes precedence; recursion direction determines associativity.                   
```

### ada83.c:1816

```
//                                                                                                  
```

### ada83.c:1817

```
// Ada precedence (highest to lowest):           
```

### ada83.c:1818

```
//                                                   
```

### ada83.c:1819

```
//   **                               (Right-associative exponentiation)                            
```

### ada83.c:1820

```
//   ABS  NOT                         (Unary prefix)                                                
```

### ada83.c:1821

```
//   *  /  MOD  REM                   (Multiplying operators)                                       
```

### ada83.c:1822

```
//   +  -  &  (binary)  +  - (unary)  (Adding operators and concatenation)                          
```

### ada83.c:1823

```
//   =  /=  <  <=  >  >=  IN  NOT IN  (Relational)                                                   
```

### ada83.c:1824

```
//   AND  OR  XOR  AND THEN  OR ELSE  (Logical, short-circuit)        
```

### ada83.c:1825

```
//                               
```

### ada83.c:1826

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:1828

```
// ???
```

### ada83.c:1834

```
// ???
```

### ada83.c:1836

```
// The underlying lexer driving token production
```

### ada83.c:1837

```
// One-token look-ahead for LL(1) decisions
```

### ada83.c:1838

```
// The most recently consumed token
```

### ada83.c:1839

```
// True once any syntax error has been reported
```

### ada83.c:1840

```
// True while skipping tokens to resynchronise
```

### ada83.c:1841

```
// Line of the previous token (for progress check)
```

### ada83.c:1842

```
// Column of the previous token (for progress check)
```

### ada83.c:1843

```
// Kind of the previous token (for progress check)
```

### ada83.c:1846

```
// Construct a parser over the given source buffer, priming the one-token look-ahead.
```

### ada83.c:1848

```
// Return true when the current token matches `kind' without consuming it.
```

### ada83.c:1850

```
// Return true when the current token matches either k1 or k2.
```

### ada83.c:1852

```
// Peek one token ahead and return true if it matches `kind'.
```

### ada83.c:1854

```
// Consume the current token and return it; refill the look-ahead.
```

### ada83.c:1856

```
// If the current token matches `kind', consume it and return true.
```

### ada83.c:1858

```
// Consume the current token if it matches `kind'; otherwise report an error.
```

### ada83.c:1860

```
// Return the source location of the current token.
```

### ada83.c:1862

```
// Expect and consume an identifier, returning its text.
```

### ada83.c:1864

```
// Report a syntax error at the previous token's location.
```

### ada83.c:1866

```
// Report a syntax error at the current token's location.
```

### ada83.c:1868

```
// Skip tokens until a statement or declaration boundary is found.
```

### ada83.c:1870

```
// Guard against infinite loops: return false if the parser has not advanced.
```

### ada83.c:1938

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:1939

```
//
```

### ada83.c:1940

```
// §10. TYPES - The Ada type lattice, Type_Info, classification
```

### ada83.c:1941

```
//
```

### ada83.c:1942

```
// Ada's types form a lattice rooted at the universal types. Every type in the program is
```

### ada83.c:1943

```
// represented by a Type_Info descriptor carrying kind, scalar bounds, composite structure,
```

### ada83.c:1944

```
// representation size, and derivation chains. A type combines name, range, and representation as
```

### ada83.c:1945

```
// three orthogonal concerns.
```

### ada83.c:1946

```
//
```

### ada83.c:1947

```
// NOTE: All sizes in Type_Info are stored in BYTES, not bits.
```

### ada83.c:1948

```
//
```

### ada83.c:1949

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:1954

```
// Global list of exception symbols for code generation
```

### ada83.c:1958

```
// ???
```

### ada83.c:1968

```
// ???
```

### ada83.c:1971

```
// A scalar bound that may be a compile-time integer, a compile-time float, or a deferred expression
```

### ada83.c:1972

```
// to be evaluated at elaboration time.
```

### ada83.c:1975

```
// case kind is
```

### ada83.c:1977

```
// when BOUND_INTEGER =>
```

### ada83.c:1978

```
// Compile-time integer bound
```

### ada83.c:1980

```
// when BOUND_FLOAT =>
```

### ada83.c:1981

```
// Compile-time floating-point bound
```

### ada83.c:1983

```
// when BOUND_EXPR =>
```

### ada83.c:1984

```
// Deferred expression (elaboration-time)
```

### ada83.c:1986

```
// when BOUND_NONE => null;
```

### ada83.c:1988

```
// LLVM temp register holding the evaluated bound, or zero
```

### ada83.c:1989

```
// LLVM rep of cached_temp (set together with it)
```

### ada83.c:1990

```
// RM 3.7.1: id of @__rt_cbnd_<n> holding a non-disc
```

### ada83.c:1991

```
// bound frozen at the record type's elaboration, or 0
```

### ada83.c:1994

```
// Describes one alternative of a record variant part
```

### ada83.c:1996

```
// Low bound of the discriminant range for this variant
```

### ada83.c:1997

```
// High bound of the discriminant range for this variant
```

### ada83.c:1998

```
// True when this variant covers the `others' choice
```

### ada83.c:1999

```
// Index of the first component belonging to this variant
```

### ada83.c:2000

```
// Number of components in this variant
```

### ada83.c:2001

```
// Byte size of this variant's component area
```

### ada83.c:2004

```
// Describes one field of a record type, whether it is a discriminant, a fixed component, or a
```

### ada83.c:2005

```
// component inside a variant alternative.
```

### ada83.c:2007

```
// Field name as it appears in source
```

### ada83.c:2008

```
// Type of this field
```

### ada83.c:2009

```
// Offset from record base in bytes
```

### ada83.c:2010

```
// Additional bit offset (for packed records)
```

### ada83.c:2011

```
// Bit size when packed; 0 means use component_type->size
```

### ada83.c:2012

```
// Default initial value expression, or NULL
```

### ada83.c:2013

```
// True if this field is a discriminant
```

### ada83.c:2014

```
// Index into Type_Info.record.variants, or negative one
```

### ada83.c:2017

```
// Describes one dimension of an array type
```

### ada83.c:2019

```
// The discrete type governing this dimension
```

### ada83.c:2020

```
// Lower bound of the index range
```

### ada83.c:2021

```
// Upper bound of the index range
```

### ada83.c:2022

```
// RM 3.5: when the index constraint's discrete range has the form
```

### ada83.c:2023

```
// `S RANGE L..H`, the range must also lie within S (in addition to the
```

### ada83.c:2024

```
// array's index subtype). This holds that explicit S, or NULL.
```

### ada83.c:2028

```
// ???
```

### ada83.c:2029

```
// Memoization state for Type_Has_Task_Component (RM 9.2/9.4): whether a type
```

### ada83.c:2030

```
// contains a task — itself, or any component, recursively. UNKNOWN until first
```

### ada83.c:2031

```
// queried; reset to UNKNOWN wherever a Type_Info is mutated after construction
```

### ada83.c:2032

```
// (private/incomplete type completion).
```

### ada83.c:2040

```
// Discriminant of the type union below
```

### ada83.c:2041

```
// User-visible type name
```

### ada83.c:2042

```
// Back-link to the symbol that declared this type
```

### ada83.c:2043

```
// Object size in bytes (never bits)
```

### ada83.c:2044

```
// Required alignment in bytes
```

### ada83.c:2045

```
// Explicit 'Size clause value, or 0 when unset
```

### ada83.c:2046

```
// Scalar lower bound (unused for composites)
```

### ada83.c:2047

```
// Scalar upper bound (unused for composites)
```

### ada83.c:2048

```
// Modulus for TYPE_MODULAR; 0 otherwise
```

### ada83.c:2049

```
// Nearest anonymous base type, or self
```

### ada83.c:2050

```
// Parent in a derived-type chain, or NULL
```

### ada83.c:2052

```
// case kind is
```

### ada83.c:2054

```
// when TYPE_ARRAY | TYPE_STRING =>
```

### ada83.c:2056

```
// Per-dimension index type and bounds
```

### ada83.c:2057

```
// Number of dimensions
```

### ada83.c:2058

```
// Component type of the array
```

### ada83.c:2059

```
// RM 3.6 anonymous unconstrained base of a
```

### ada83.c:2060

```
// constrained array TYPE, materialized
```

### ada83.c:2061

```
// lazily by Array_Unconstrained_Base
```

### ada83.c:2062

```
// True for constrained (bounds known statically)
```

### ada83.c:2063

```
// Implicit row type of a multi-dimensional
```

### ada83.c:2064

```
// aggregate's inner dimensions: such a row
```

### ada83.c:2065

```
// slides with the whole array in an
```

### ada83.c:2066

```
// assignment (RM 5.2.1), unlike a true
```

### ada83.c:2067

```
// component aggregate, which never slides.
```

### ada83.c:2070

```
// when TYPE_RECORD =>
```

### ada83.c:2072

```
// Flat list of all record fields
```

### ada83.c:2073

```
// Length of the components array
```

### ada83.c:2074

```
// How many are discriminants
```

### ada83.c:2075

```
// True when a discriminant part exists
```

### ada83.c:2076

```
// True if every discriminant has a default
```

### ada83.c:2077

```
// True for a constrained record subtype
```

### ada83.c:2078

```
// Variant alternatives, or NULL
```

### ada83.c:2079

```
// Number of variant alternatives
```

### ada83.c:2080

```
// Byte offset where the variant area begins
```

### ada83.c:2081

```
// Largest variant alternative in bytes
```

### ada83.c:2082

```
// AST node of the variant part, or NULL
```

### ada83.c:2083

```
// Static discriminant constraint values
```

### ada83.c:2084

```
// Dynamic discriminant constraint exprs
```

### ada83.c:2085

```
// Per-disc module-global ids (@__rt_cbnd_*)
```

### ada83.c:2086

```
// holding the once-evaluated constraint value
```

### ada83.c:2087

```
// True when a discriminant constraint exists
```

### ada83.c:2088

```
// RM 3.8.1: constrained subtypes (ACCESS CONS (V)) built against this type
```

### ada83.c:2089

```
// while it was still incomplete. Their discriminant-dependent component
```

### ada83.c:2090

```
// constraints cannot be checked until the full declaration completes the
```

### ada83.c:2091

```
// layout, so they are flushed at that point (c37216a).
```

### ada83.c:2094

```
// RM 3.7.2: set on a constrained record subtype whose base record was not
```

### ada83.c:2095

```
// completely declared (it had a private/incomplete component) when this
```

### ada83.c:2096

```
// constraint was written. Its non-discriminant constraint parts are then
```

### ada83.c:2097

```
// checked at the enclosing type's full declaration (c37214a) rather than
```

### ada83.c:2098

```
// deferred to an explicit constraint, as a complete record's are
```

### ada83.c:2099

```
// (c37213a).
```

### ada83.c:2103

```
// when TYPE_ACCESS =>
```

### ada83.c:2105

```
// The type that the pointer designates
```

### ada83.c:2106

```
// True for access-to-constant
```

### ada83.c:2107

```
// RM 9.4: scope id of the master that
```

### ada83.c:2108

```
// elaborates this access-to-task type —
```

### ada83.c:2109

```
// allocator tasks depend on THAT master
```

### ada83.c:2110

```
// (__ada_master_find at the allocator).
```

### ada83.c:2111

```
// 0 = library level (program master).
```

### ada83.c:2114

```
// when TYPE_ENUMERATION | TYPE_BOOLEAN | TYPE_CHARACTER =>
```

### ada83.c:2116

```
// Names of the enumeration literals in order
```

### ada83.c:2117

```
// Number of literals
```

### ada83.c:2118

```
// Representation values from an enum rep clause
```

### ada83.c:2121

```
// when TYPE_FIXED =>
```

### ada83.c:2124

```
// when TYPE_FLOAT =>
```

### ada83.c:2128

```
// Bitmask of CHK_* flags suppressed by pragma
```

### ada83.c:2129

```
// True when pragma Pack has been applied
```

### ada83.c:2130

```
// True for limited types (no assignment or equality)
```

### ada83.c:2131

```
// Declared PRIVATE (or LIMITED PRIVATE): outside the
```

### ada83.c:2132

```
// defining package only =, /= and declared operators
```

### ada83.c:2133

```
// exist (RM 7.4.2); survives the full-view completion
```

### ada83.c:2134

```
// True once the representation has been finalised
```

### ada83.c:2135

```
// Type_Has_Task_Component memo
```

### ada83.c:2136

```
// Governing_Discriminant_Index memo: 0 = not yet
```

### ada83.c:2137

```
// resolved; else 1 + component index, or -1 when
```

### ada83.c:2138

```
// the record has no governing discriminant.
```

### ada83.c:2139

```
// Top-level (outside the kind union) like the
```

### ada83.c:2140

```
// task memo, so it never aliases another variant.
```

### ada83.c:2141

```
// 'Storage_Size attribute value, or -1 if unset
```

### ada83.c:2142

```
// Non-static 'STORAGE_SIZE clause expression
```

### ada83.c:2143

```
// (RM 13.2 allows one); evaluated once at the
```

### ada83.c:2144

```
// clause's elaboration into the global below
```

### ada83.c:2145

```
// @__rt_cbnd_<n> id of the frozen value, or 0
```

### ada83.c:2146

```
// Access types: @__rt_sscap_<n> id of the
```

### ada83.c:2147

```
// collection's REMAINING storage units,
```

### ada83.c:2148

```
// debited by each allocator (RM 13.2); 0 =
```

### ada83.c:2149

```
// no specified collection size (unbounded)
```

### ada83.c:2150

```
// Mangled name of the compiler-generated "=" function
```

### ada83.c:2151

```
// Runtime type identifier for exception dispatch
```

### ada83.c:2152

```
// True when this type stands for a generic
```

### ada83.c:2153

```
// formal type inside its template (RM 12.1.2)
```

### ada83.c:2154

```
// Instance-side view of a formal
```

### ada83.c:2155

```
// type: names the actual (base_type)
```

### ada83.c:2156

```
// but its predefined operators bind
```

### ada83.c:2157

```
// to the actual's PREDEFINED ones
```

### ada83.c:2158

```
// even when redefined (RM 12.1.2
```

### ada83.c:2159

```
// (41-42))
```

### ada83.c:2160

```
// The actual type this view denotes;
```

### ada83.c:2161

```
// Peel_Generic_Actual_View follows it
```

### ada83.c:2162

```
// Actual-view bound to a formal PRIVATE
```

### ada83.c:2163

```
// type: the generic body saw only the
```

### ada83.c:2164

```
// constraint-free private view, so an
```

### ada83.c:2165

```
// OUT/IN OUT copy-back through it must
```

### ada83.c:2166

```
// not gain the actual's scalar range
```

### ada83.c:2167

```
// check (RM 12.3; ce2205b, ce2405a)
```

### ada83.c:2168

```
// The formal private type's own
```

### ada83.c:2169

```
// record view: its discriminant NAMES
```

### ada83.c:2170

```
// map positionally onto the actual's
```

### ada83.c:2171

```
// (RM 12.1) for component lookup
```

### ada83.c:2172

```
// inside the generic (c83027c)
```

### ada83.c:2173

```
// Which formal class (private, discrete,
```

### ada83.c:2174

```
// range <>, digits <>, ...) when
```

### ada83.c:2175

```
// is_generic_formal; drives the class's
```

### ada83.c:2176

```
// available operations and the RM 12.3.3+
```

### ada83.c:2177

```
// matching rules at instantiation
```

### ada83.c:2231

```
// Within a generic instance a formal type denotes its actual (RM 12.1.2). The
```

### ada83.c:2232

```
// instance's view is a distinct Type_Info flagged is_generic_actual_view; this
```

### ada83.c:2233

```
// follows it back to the underlying actual so type comparisons treat them as
```

### ada83.c:2234

```
// one type.
```

### ada83.c:2245

```
// Compile-time storage size of an object type (int128, -1 if not statically
```

### ada83.c:2246

```
// known), for the RM 11.1 stack-object limit check.
```

### ada83.c:2265

```
// The compile-time value of a real model attribute (EPSILON, SMALL, LARGE,
```

### ada83.c:2266

```
// SAFE_SMALL, SAFE_LARGE) of a floating-point, fixed-point, or universal
```

### ada83.c:2267

```
// prefix (RM 3.5.8, 3.5.10). `prefix_type` carries the declared view;
```

### ada83.c:2268

```
// `classify_type` is the generic-actual-resolved view used to classify
```

### ada83.c:2269

```
// fixed-point prefixes.
```

### ada83.c:2296

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:2297

```
// §11. NAMES - Symbol table, scopes, overload resolution
```

### ada83.c:2298

```
//
```

### ada83.c:2299

```
// Every named entity - variable, constant, type, subprogram, package, exception, loop, label - is
```

### ada83.c:2300

```
// represented by a Symbol. Symbols live in Scopes chained outward from inner to enclosing. The
```

### ada83.c:2301

```
// symbol table implements Ada's visibility and overloading rules: hierarchical scopes, use-clause
```

### ada83.c:2302

```
// visibility, and multi-meaning overloads. We use a hash table with chaining and a scope stack for
```

### ada83.c:2303

```
// nested contexts.
```

### ada83.c:2304

```
//
```

### ada83.c:2305

```
// Overload resolution is a two-pass process:
```

### ada83.c:2306

```
//
```

### ada83.c:2307

```
//   - Bottom-up: collect all possible interpretations of each identifier.
```

### ada83.c:2308

```
//   - Top-down: given context type expectations, select the unique valid one.
```

### ada83.c:2309

```
//
```

### ada83.c:2310

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:2312

```
// Label - is represented by a Symbol. Symbols live in Scopes chained outward from
```

### ada83.c:2326

```
// Describes one formal parameter of a subprogram (RM 6.1).
```

### ada83.c:2328

```
// Parameter name as it appears in the spec
```

### ada83.c:2329

```
// Subtype of the formal
```

### ada83.c:2330

```
// IN, OUT, or IN OUT
```

### ada83.c:2331

```
// Default expression, or NULL
```

### ada83.c:2332

```
// Back-link to the Symbol for this parameter
```

### ada83.c:2335

```
// Return true when a parameter of this mode is passed by reference.
```

### ada83.c:2338

```
// Mode of a SYMBOL_PARAMETER, recovered from its subprogram's parameter list;
```

### ada83.c:2339

```
// PARAM_IN when the symbol is not a parameter or the mode cannot be found.
```

### ada83.c:2342

```
// Stamp is_package_level on every object symbol declared directly in the
```

### ada83.c:2343

```
// given declaration list (a package spec's visible/private part or a
```

### ada83.c:2344

```
// package body's declarative part).
```

### ada83.c:2347

```
// Resolve the IS-name defaults of a generic's formal subprograms
```

### ada83.c:2348

```
// (RM 12.1.3). Called where the generic's formals are installed in
```

### ada83.c:2349

```
// scope (body completion paths), so defaults naming earlier formals or
```

### ada83.c:2350

```
// formal-type attributes resolve without spurious diagnostics. The
```

### ada83.c:2351

```
// resolved symbol rides on the default_name node; instantiation binds
```

### ada83.c:2352

```
// it when no explicit actual is given.
```

### ada83.c:2355

```
// Install the generic's formal parameters as symbols in the current scope
```

### ada83.c:2356

```
// (RM 12.1): formal types as distinct Type_Infos tagged with their formal
```

### ada83.c:2357

```
// class (RM 12.1.2), formal objects as a constant per name for mode IN and a
```

### ada83.c:2358

```
// variable per name for mode IN OUT (RM 12.1.1), formal subprograms with
```

### ada83.c:2359

```
// full parameter profiles (RM 12.1.3). Symbols are created on first call and
```

### ada83.c:2360

```
// cached on the formal nodes; later calls (a body completing the generic, a
```

### ada83.c:2361

```
// disk-loaded body) re-install the same symbols into the current scope.
```

### ada83.c:2364

```
// Formal-to-binding remap pairs produced while binding an instance's
```

### ada83.c:2365

```
// formals; applied to decorated default-expression clones (RM 12.3 rules
```

### ada83.c:2366

```
// (b)/(c)/(d): in the instance, formal names denote the bindings).
```

### ada83.c:2373

```
// Shared kernels of the wrapper synthesizers below. Materialize a
```

### ada83.c:2374

```
// wrapper's parameter profile from a formal subprogram's parameter list:
```

### ada83.c:2375

```
// count and allocate the binding's Parameter_Info array, resolve each
```

### ada83.c:2376

```
// type mark against the instance's bindings (on a clone, so the template
```

### ada83.c:2377

```
// spec stays undecorated), install a SYMBOL_PARAMETER for every name in
```

### ada83.c:2378

```
// the wrapper's scope, resolve the return type, and append one decorated
```

### ada83.c:2379

```
// reference node per parameter to `parameter_references` for use as the
```

### ada83.c:2380

```
// wrapper body's actuals.
```

### ada83.c:2384

```
// Wrap `result_expression` as the single RETURN statement of a synthesized
```

### ada83.c:2385

```
// function body named after the binding, and record that body as the
```

### ada83.c:2386

```
// binding's declaration so the instance emits it like any subprogram.
```

### ada83.c:2391

```
// Synthesize a resolved wrapper body on `binding`:
```

### ada83.c:2392

```
//   function <formal> (L, R) return BOOLEAN is
```

### ada83.c:2393

```
//   begin return not "="(L, R); end;
```

### ada83.c:2394

```
// for a "/=" generic actual (RM 6.7). The body is ordinary resolved AST;
```

### ada83.c:2395

```
// it emits with the instance and calls through the formal are plain calls.
```

### ada83.c:2399

```
// Synthesize a resolved wrapper body on `binding` for an attribute actual
```

### ada83.c:2400

```
// (a default like IS T'SUCC, RM 12.3.6): the body applies the attribute —
```

### ada83.c:2401

```
// its formal references remapped to the instance's bindings — to the
```

### ada83.c:2402

```
// wrapper's parameters. Calls through the formal are then ordinary calls.
```

### ada83.c:2408

```
// Synthesize a resolved wrapper body on `binding` for a predefined-operator
```

### ada83.c:2409

```
// actual (e.g. F2 => "&"): the body applies the operator to the wrapper's
```

### ada83.c:2410

```
// parameters. Calls through the formal are then ordinary calls, and the
```

### ada83.c:2411

```
// operator lowers through the one shared binary-op emitter.
```

### ada83.c:2415

```
// Bind each generic formal to its actual as a symbol of the current
```

### ada83.c:2416

```
// (instance) scope: formal types alias the actual subtype, formal objects
```

### ada83.c:2417

```
// become per-instance binding globals (IN value / IN OUT pointer cell),
```

### ada83.c:2418

```
// formal subprograms rename their actuals. Fills `remap`.
```

### ada83.c:2423

```
// Legality of the instantiation's actuals against the formal part
```

### ada83.c:2424

```
// (RM 12.3.1 .. 12.3.6): class membership for formal scalar types,
```

### ada83.c:2425

```
// limitedness and discriminant agreement for formal private types,
```

### ada83.c:2426

```
// dimensionality / index types / component type / constrainedness for
```

### ada83.c:2427

```
// formal array types, designated-type agreement for formal access types,
```

### ada83.c:2428

```
// variable-ness for IN OUT formal objects, full profile-and-mode
```

### ada83.c:2429

```
// conformance for formal subprograms, and the missing-actual rule.
```

### ada83.c:2430

```
// Diagnoses; never transforms.
```

### ada83.c:2435

```
// The actual subtype a formal-part type mark denotes at an instantiation.
```

### ada83.c:2438

```
// Templates currently being instantiated, mirroring §17's Loading_Set:
```

### ada83.c:2439

```
// re-entering one is the recursive instantiation RM 12.3 (and 12.1 Notes)
```

### ada83.c:2440

```
// prohibits — direct, or through a cycle — and is rejected at compile time
```

### ada83.c:2441

```
// with the cycle named.
```

### ada83.c:2448

```
// Tree-copy instantiation of a generic package (RM 12.3): clone the
```

### ada83.c:2449

```
// resolved template spec and body, bind formals, resolve the clones in the
```

### ada83.c:2450

```
// generic declaration's environment, and export the resolved per-instance
```

### ada83.c:2451

```
// symbols. Emission then walks the clones through the ordinary package
```

### ada83.c:2452

```
// paths. Returns false when the template spec is unavailable.
```

### ada83.c:2461

```
// Tree-copy instantiation of a generic subprogram (RM 12.3): clone the
```

### ada83.c:2462

```
// resolved template body structurally, bind each formal to its actual as an
```

### ada83.c:2463

```
// ordinary symbol or declaration of the instance's own scope, and resolve
```

### ada83.c:2464

```
// the clone in the environment of the generic's declaration. On success the
```

### ada83.c:2465

```
// instance symbol carries the resolved clone as its declaration and emits
```

### ada83.c:2466

```
// through the ordinary subprogram paths. Returns false when no template
```

### ada83.c:2467

```
// body is available yet (the caller's deferred path then applies).
```

### ada83.c:2470

```
// Single source of truth for the parameter-passing convention: true when a
```

### ada83.c:2471

```
// formal of this mode and type is passed as a pointer to the actual's
```

### ada83.c:2472

```
// storage. OUT and IN OUT formals always are; IN formals are when the type
```

### ada83.c:2473

```
// is a record, a task (RM 6.2: the formal and the actual denote the same
```

### ada83.c:2474

```
// task object), or a statically-bounded constrained array/string stored as
```

### ada83.c:2475

```
// raw data. Caller marshaling and both callee parameter materializers must
```

### ada83.c:2476

```
// all classify through this predicate.
```

### ada83.c:2482

```
// Symbol Visibility Level  (extracted from Symbol record)
```

### ada83.c:2490

```
// Calling Convention  (extracted from Symbol record)
```

### ada83.c:2496

```
// ???
```

### ada83.c:2499

```
// Identity
```

### ada83.c:2500

```
// What kind of entity this symbol represents
```

### ada83.c:2501

```
// The Ada identifier for this entity
```

### ada83.c:2502

```
// Where this entity was declared
```

### ada83.c:2503

```
// Type of the entity (variable, constant, param, etc.)
```

### ada83.c:2504

```
// The scope in which this symbol was introduced
```

### ada83.c:2505

```
// Enclosing package or subprogram symbol, or NULL
```

### ada83.c:2506

```
// Next homonym in the overload chain
```

### ada83.c:2507

```
// Next symbol in the hash-bucket collision chain
```

### ada83.c:2508

```
// Monotonic creation stamp: instance
```

### ada83.c:2509

```
// resolution hides symbols stamped
```

### ada83.c:2510

```
// after the template's visibility
```

### ada83.c:2511

```
// cutoff (declaration-point
```

### ada83.c:2512

```
// visibility, RM 12.3 rule (i))
```

### ada83.c:2513

```
// Current visibility (hidden, use, direct)
```

### ada83.c:2515

```
// Declaration link
```

### ada83.c:2516

```
// AST node that declared this entity
```

### ada83.c:2518

```
// Subprogram profile
```

### ada83.c:2519

```
// Array of formal parameter descriptors
```

### ada83.c:2520

```
// Length of the parameters array
```

### ada83.c:2521

```
// Return type for functions; NULL for procedures
```

### ada83.c:2523

```
// Package exports
```

### ada83.c:2524

```
// Symbols made visible by a package specification
```

### ada83.c:2525

```
// Number of exported symbols
```

### ada83.c:2527

```
// Addressing
```

### ada83.c:2528

```
// Compiler-unique serial number for mangling
```

### ada83.c:2529

```
// Mangled name frozen at first emission:
```

### ada83.c:2530

```
// codegen-synthesized homographs must not
```

### ada83.c:2531

```
// flip an already-emitted symbol's
```

### ada83.c:2532

```
// spelling (is_overloaded, scope moves)
```

### ada83.c:2533

```
// Static nesting depth for uplevel access
```

### ada83.c:2534

```
// Byte offset within the enclosing stack frame
```

### ada83.c:2535

```
// Body scope owned by this symbol (pkg/subp)
```

### ada83.c:2537

```
// Import / export / convention
```

### ada83.c:2538

```
// Pragma Inline applied
```

### ada83.c:2539

```
// Pragma Import applied
```

### ada83.c:2540

```
// Pragma Export applied
```

### ada83.c:2541

```
// External linker name from pragma Import/Export
```

### ada83.c:2542

```
// Link-time name override
```

### ada83.c:2543

```
// Calling convention (Ada, C, Stdcall, etc.)
```

### ada83.c:2544

```
// Bitmask of CHK_* suppressed by pragma Suppress
```

### ada83.c:2545

```
// Pragma Unreferenced applied
```

### ada83.c:2547

```
// Code generation bookkeeping
```

### ada83.c:2548

```
// LLVM declare already written
```

### ada83.c:2549

```
// Registered for a deferred module-level declare
```

### ada83.c:2550

```
// RTS-provided unit; runtime supplies its body
```

### ada83.c:2551

```
// ___elab recorded for the extern flush
```

### ada83.c:2552

```
// ___elab DEFINED in this module
```

### ada83.c:2553

```
// LLVM define already written
```

### ada83.c:2554

```
// This is a number declaration (RM 3.2)
```

### ada83.c:2555

```
// FOR-loop parameter (RM 5.5) — always
```

### ada83.c:2556

```
// stack-local to the function emitting
```

### ada83.c:2557

```
// the loop, never package-level storage
```

### ada83.c:2558

```
// Object declared directly in a package
```

### ada83.c:2559

```
// spec or body declarative part (not in
```

### ada83.c:2560

```
// a block or subprogram nested inside);
```

### ada83.c:2561

```
// inside a generic package this selects
```

### ada83.c:2562

```
// per-instance global storage
```

### ada83.c:2563

```
// Generic formal object of mode IN,
```

### ada83.c:2564

```
// bound for a tree-copy instance: a
```

### ada83.c:2565

```
// per-instance global constant whose
```

### ada83.c:2566

```
// value stores at the instantiation
```

### ada83.c:2567

```
// point (RM 12.1.1, 12.3(80))
```

### ada83.c:2568

```
// Generic formal object of mode IN
```

### ada83.c:2569

```
// OUT, bound for a tree-copy
```

### ada83.c:2570

```
// instance: a per-instance global
```

### ada83.c:2571

```
// POINTER cell renaming the actual
```

### ada83.c:2572

```
// variable; reads and writes
```

### ada83.c:2573

```
// indirect through it (RM 12.1.1)
```

### ada83.c:2574

```
// The resolved actual expression the
```

### ada83.c:2575

```
// binding elaborates from
```

### ada83.c:2576

```
// The binding elaborates from the
```

### ada83.c:2577

```
// formal's DEFAULT, not an explicit
```

### ada83.c:2578

```
// actual: RM 12.3(80) evaluates every
```

### ada83.c:2579

```
// explicit actual before any default
```

### ada83.c:2580

```
// Synthesized instance-level wrapper
```

### ada83.c:2581

```
// body for a non-plain formal-
```

### ada83.c:2582

```
// subprogram actual ("/=" negation,
```

### ada83.c:2583

```
// attribute, enumeration literal):
```

### ada83.c:2584

```
// declaration holds the resolved
```

### ada83.c:2585

```
// body; emitted with the instance
```

### ada83.c:2586

```
// More than one meaning in scope
```

### ada83.c:2587

```
// USE-visible alias -> the original it denotes
```

### ada83.c:2588

```
// Completion of RM 7.4 deferred constant:
```

### ada83.c:2589

```
// points to the visible (existing) symbol
```

### ada83.c:2590

```
// that owns the storage. Consumed by
```

### ada83.c:2591

```
// Resolve_Declaration to bind the AST name
```

### ada83.c:2592

```
// node back to that storage.
```

### ada83.c:2593

```
// Body already associated with a spec
```

### ada83.c:2594

```
// Pragma Pure names this unit (RM 10.5)
```

### ada83.c:2595

```
// Pragma Preelaborate names this unit
```

### ada83.c:2596

```
// A define exists in this module
```

### ada83.c:2597

```
// Queued for the SEPARATE-boundary extern flush
```

### ada83.c:2598

```
// Package body is a SEPARATE stub in this compilation
```

### ada83.c:2599

```
// Frame slot already assigned; re-adds keep it
```

### ada83.c:2600

```
// By-ref parameter whose frame slot holds a
```

### ada83.c:2601

```
// pointer to caller storage (for uplevel access)
```

### ada83.c:2602

```
// OUT/IN OUT formal of an unconstrained
```

### ada83.c:2603

```
// mutable discriminated type: its 'CONSTRAINED
```

### ada83.c:2604

```
// is the actual's, passed as a hidden i8 formal
```

### ada83.c:2605

```
// and spilled to %<name>.constr (RM 3.7.4).
```

### ada83.c:2606

```
// Module-level storage of a subunit's own module
```

### ada83.c:2607

```
// Standard-library predefined entity
```

### ada83.c:2608

```
// Needs a @__address_marker global
```

### ada83.c:2609

```
// Derived identity "=" operator
```

### ada83.c:2610

```
// Explicitly declared immediately within a
```

### ada83.c:2611

```
// package's visible part. RM 3.4 calls such a
```

### ada83.c:2612

```
// subprogram "derivable of the first kind".
```

### ada83.c:2613

```
// Temp register for discriminant aggregate
```

### ada83.c:2614

```
// Constrained by a discriminant constraint
```

### ada83.c:2616

```
// Derived operations
```

### ada83.c:2617

```
// Original primitive inherited by derivation
```

### ada83.c:2618

```
// Parent type from which this op was derived
```

### ada83.c:2620

```
// RM 3.9 access-before-elaboration: a subprogram given as a forward spec
```

### ada83.c:2621

```
// carries a boolean flag, set false where the spec elaborates and true where
```

### ada83.c:2622

```
// the body elaborates. A call checks it and raises PROGRAM_ERROR when the body
```

### ada83.c:2623

```
// has not yet run. The global is reachable from any function, so a call from
```

### ada83.c:2624

```
// an instance or default expression that runs in a different frame still sees
```

### ada83.c:2625

```
// it.
```

### ada83.c:2626

```
// The flag global is named @<mangled>.elab from the subprogram's stable
```

### ada83.c:2627

```
// mangled name, so a SEPARATE subunit checks the very flag its parent unit
```

### ada83.c:2628

```
// defines. elab_flag_id is nonzero once the flag is DEFINED in this module;
```

### ada83.c:2629

```
// needs_elab_flag records (from resolution, before any codegen) that the
```

### ada83.c:2630

```
// subprogram HAS a flag, so a subunit that never codegens the parent still
```

### ada83.c:2631

```
// emits the check; elab_flag_extern_noted guards the one extern declaration a
```

### ada83.c:2632

```
// referencing module emits for a flag defined elsewhere.
```

### ada83.c:2633

```
// Nonzero once the flag is defined in this module
```

### ada83.c:2634

```
// A standalone forward spec in a subprogram
```

### ada83.c:2635

```
// extern decl of the flag emitted here
```

### ada83.c:2637

```
// Labels, loops, entries
```

### ada83.c:2638

```
// LLVM label for goto targets
```

### ada83.c:2639

```
// LLVM label for exit-loop targets
```

### ada83.c:2640

```
// Index in the task entry family
```

### ada83.c:2641

```
// Renamed entity expression, or NULL
```

### ada83.c:2642

```
// Peeled subprogram-rename target, memoized
```

### ada83.c:2643

```
// by Resolve_Subprogram_Rename (NULL = uncomputed)
```

### ada83.c:2644

```
// Name after RENAMES when the target is an
```

### ada83.c:2645

```
// entry: NK_SELECTED, or NK_APPLY for an
```

### ada83.c:2646

```
// entry-family member (index retained)
```

### ada83.c:2647

```
// Hidden local holding the task object
```

### ada83.c:2648

```
// pointer the renaming bound when it was
```

### ada83.c:2649

```
// elaborated (RM 8.5)
```

### ada83.c:2650

```
// Hidden local holding the family index
```

### ada83.c:2651

```
// evaluated at the same elaboration
```

### ada83.c:2652

```
// Hidden local holding the address of the
```

### ada83.c:2653

```
// renamed entity, captured at the rename's
```

### ada83.c:2654

```
// elaboration (RM 8.5). Later access reads
```

### ada83.c:2655

```
// through this slot so the rename stays
```

### ada83.c:2656

```
// bound to the object first designated,
```

### ada83.c:2657

```
// even if the renamed expression's
```

### ada83.c:2658

```
// sub-parts change afterwards.
```

### ada83.c:2659

```
// FOR X USE AT expr (RM 13.5): the object
```

### ada83.c:2660

```
// is an overlay on that address. Accesses
```

### ada83.c:2661

```
// redirect through renamed_object, which
```

### ada83.c:2662

```
// dereferences the hidden address_cell.
```

### ada83.c:2663

```
// Hidden variable holding the evaluated
```

### ada83.c:2664

```
// clause address as an access value
```

### ada83.c:2666

```
// Generic instantiation state
```

### ada83.c:2667

```
// AST of the generic formal part
```

### ada83.c:2668

```
// AST of the generic unit declaration
```

### ada83.c:2669

```
// AST of the generic body (found later)
```

### ada83.c:2670

```
// Disk-parsed generic body AST, parsed
```

### ada83.c:2671

```
// once per template and cloned per instance
```

### ada83.c:2672

```
// Back-link to the generic template symbol
```

### ada83.c:2673

```
// creation_sequence horizon at the
```

### ada83.c:2674

```
// end of template spec analysis
```

### ada83.c:2675

```
// ... at the start of template
```

### ada83.c:2676

```
// body analysis (0 = unseen)
```

### ada83.c:2677

```
// The package-body scope that
```

### ada83.c:2678

```
// textually encloses the generic
```

### ada83.c:2679

```
// body, so an instance body can
```

### ada83.c:2680

```
// see the enclosing body's
```

### ada83.c:2681

```
// declarations (RM 8.1, 12.3)
```

### ada83.c:2682

```
// The instantiation's resolved
```

### ada83.c:2683

```
// formal-to-actual record (one per
```

### ada83.c:2684

```
// slot; object formals one per
```

### ada83.c:2685

```
// name): input to the formal
```

### ada83.c:2686

```
// binder and the RM 12.3
```

### ada83.c:2687

```
// validators
```

### ada83.c:2688

```
// Length of actual_bindings
```

### ada83.c:2689

```
// RM 12.3(81) subtype-agreement
```

### ada83.c:2690

```
// pairs, captured at resolution
```

### ada83.c:2691

```
// Length of agreement_checks
```

### ada83.c:2692

```
// The instance's resolved spec
```

### ada83.c:2693

```
// clone (packages)
```

### ada83.c:2694

```
// The instance's resolved body
```

### ada83.c:2695

```
// clone
```

### ada83.c:2698

```
// A scope is a hash table of symbols chained outward from inner to enclosing.
```

### ada83.c:2700

```
// Hash-bucket heads for O(1) name lookup
```

### ada83.c:2701

```
// Enclosing scope (NULL for the global scope)
```

### ada83.c:2702

```
// Package or subprogram that owns this scope
```

### ada83.c:2703

```
// Static nesting depth (0 = global)
```

### ada83.c:2704

```
// Flat array of all symbols in declaration order
```

### ada83.c:2705

```
// Number of symbols currently in the scope
```

### ada83.c:2706

```
// Allocated slots in the symbols array
```

### ada83.c:2707

```
// Total byte size of the local stack frame
```

### ada83.c:2708

```
// Subset of symbols that occupy frame slots
```

### ada83.c:2709

```
// Number of frame variables
```

### ada83.c:2710

```
// Allocated slots in the frame_vars array
```

### ada83.c:2713

```
// The Symbol_Manager owns the scope stack and caches handles to the predefined
```

### ada83.c:2714

```
// types so that every part of the compiler can reach Standard.Boolean, etc.
```

### ada83.c:2716

```
// Top of the scope stack
```

### ada83.c:2717

```
// The outermost (Standard) scope
```

### ada83.c:2718

```
// Standard.Boolean
```

### ada83.c:2719

```
// Standard.Integer
```

### ada83.c:2720

```
// Standard.Float
```

### ada83.c:2721

```
// Standard.Character
```

### ada83.c:2722

```
// Standard.String
```

### ada83.c:2723

```
// Duration (for delay statements)
```

### ada83.c:2724

```
// Universal_Integer (numeric class)
```

### ada83.c:2725

```
// Universal_Real (numeric class)
```

### ada83.c:2726

```
// System.Address
```

### ada83.c:2727

```
// Monotonic counter for mangled names
```

### ada83.c:2728

```
// Monotonic Symbol creation stamp
```

### ada83.c:2729

```
// When nonzero: lookups hide symbols
```

### ada83.c:2730

```
// stamped after this (instance
```

### ada83.c:2731

```
// resolution under the template's
```

### ada83.c:2732

```
// declaration-point visibility)
```

### ada83.c:2733

```
// The instance scope (and its
```

### ada83.c:2734

```
// descendants): bindings and the
```

### ada83.c:2735

```
// clone's own entities stay visible
```

### ada83.c:2736

```
// True only while resolving the visible
```

### ada83.c:2737

```
// declarations of a package specification,
```

### ada83.c:2738

```
// so newly added subprograms can be stamped
```

### ada83.c:2739

```
// as first-kind derivable (RM 3.4).
```

### ada83.c:2772

```
// Captures the types and names of actual arguments at a call site for
```

### ada83.c:2773

```
// overload resolution. `alt_types` carries alternative interpretations
```

### ada83.c:2774

```
// for ambiguous argument expressions (e.g. a binary op like `5<5`
```

### ada83.c:2775

```
// resolving as either predefined `<`→BOOLEAN or user `<`→INTEGER); the
```

### ada83.c:2776

```
// resolver tries `types[i]` first, then `alt_types[i]` if non-NULL,
```

### ada83.c:2777

```
// implementing RM 6.6 bidirectional overload resolution.
```

### ada83.c:2779

```
// Array of resolved argument types (primary)
```

### ada83.c:2780

```
// Optional alternates, parallel to `types` (or NULL)
```

### ada83.c:2781

```
// Number of actual arguments
```

### ada83.c:2782

```
// Named-association formal names, or NULL for positional
```

### ada83.c:2786

```
// Candidate symbol; NULL = predefined operation
```

### ada83.c:2787

```
// Result type
```

### ada83.c:2788

```
// Per-operand target types (left/sole in [0], right
```

### ada83.c:2789

```
// in [1]); NULL when the operand is absent. This is
```

### ada83.c:2790

```
// the type each operand is resolved/converted to, so
```

### ada83.c:2791

```
// heterogeneous operators (CHARACTER & STRING, fixed
```

### ada83.c:2792

```
// * INTEGER, T ** INTEGER) are expressed directly.
```

### ada83.c:2793

```
// Distance from use to declaration
```

### ada83.c:2802

```
// As Type_Covers, but `allow_derivation` gates the RM 3.4 shared-root rule that
```

### ada83.c:2803

```
// treats a derived type and an ancestor as mutually compatible. Assignment and
```

### ada83.c:2804

```
// explicit conversion want it on; overload resolution (RM 8.7, which matches an
```

### ada83.c:2805

```
// interpretation's type exactly) wants it off, so a parent operation cannot
```

### ada83.c:2806

```
// masquerade as the derived type and vice versa.
```

### ada83.c:2809

```
// Overload-resolution coverage: Type_Covers with the shared-root rule disabled.
```

### ada83.c:2833

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:2834

```
//
```

### ada83.c:2835

```
// §12. Semantics Analysis - Name resolution, type checking, constant folding
```

### ada83.c:2836

```
//
```

### ada83.c:2837

```
// The semantic pass walks the syntax tree to resolve identifiers, check type compatibility, fold
```

### ada83.c:2838

```
// static expressions, and freeze type representations. A permissive parser gives the type checker
```

### ada83.c:2839

```
// material to work with.
```

### ada83.c:2840

```
//
```

### ada83.c:2841

```
// Semantic analysis performs:
```

### ada83.c:2842

```
//
```

### ada83.c:2843

```
//   - Name resolution: bind identifiers to symbols
```

### ada83.c:2844

```
//   - Type checking: verify type compatibility of operations
```

### ada83.c:2845

```
//   - Overload resolution: select correct subprogram
```

### ada83.c:2846

```
//   - Constraint checking: verify bounds, indices, etc.
```

### ada83.c:2847

```
//
```

### ada83.c:2848

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:2865

```
// Two-pass overload resolution (RM 8.6): Analyze_Expr collects the candidate
```

### ada83.c:2866

```
// interpretation set on a node bottom-up; Resolve_In_Context filters that set by
```

### ada83.c:2867

```
// the required type and commits a unique interpretation top-down.
```

### ada83.c:2871

```
// !!!
```

### ada83.c:2872

```
// Position of the character literal 'character' among an enumeration's
```

### ada83.c:2873

```
// declared literals, or -1 when the type declares no such literal.
```

### ada83.c:2902

```
// ???
```

### ada83.c:2907

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:2908

```
//
```

### ada83.c:2909

```
// §13. Code Generation - LLVM IR emission for every Ada construct
```

### ada83.c:2910

```
//
```

### ada83.c:2911

```
// The code generator walks the resolved syntax tree and emits LLVM IR as plain text to a FILE*. The
```

### ada83.c:2912

```
// AST is semantic and the IR is operational, with translation bridging the gap.
```

### ada83.c:2913

```
//
```

### ada83.c:2914

```
// Key principles:
```

### ada83.c:2915

```
//
```

### ada83.c:2916

```
//   - Operate at native type width; convert only at explicit Ada type conversions and LLVM
```

### ada83.c:2917

```
//     intrinsic boundaries (memcpy length, alloc size must be i64).
```

### ada83.c:2918

```
//   - All pointer types use opaque 'ptr' (LLVM 15+).
```

### ada83.c:2919

```
//   - Static links for nested subprogram access.
```

### ada83.c:2920

```
//   - Fat pointers {ptr, ptr} for unconstrained arrays (data + bounds).
```

### ada83.c:2921

```
//
```

### ada83.c:2922

```
// The Code_Generator record holds all mutable state for one compilation unit: temporaries, labels,
```

### ada83.c:2923

```
// globals, string constants, deferred bodies, and exception state.
```

### ada83.c:2924

```
//
```

### ada83.c:2925

```
// LLVM IR is an SSA (Static Single Assignment) form where every %N temporary is defined exactly
```

### ada83.c:2926

```
// once. Emit_Temp() allocates a fresh temporary number; the Emit() function writes IR text. Labels
```

### ada83.c:2927

```
// partition code into basic blocks - straight-line sequences ending in a terminator (br, ret,
```

### ada83.c:2928

```
// switch, unreachable). Emit_Label_Here() begins a new block; Emit_Branch_If_Needed() closes the
```

### ada83.c:2929

```
// current one if it isn't already terminated.
```

### ada83.c:2930

```
//
```

### ada83.c:2931

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:2933

```
// ???
```

### ada83.c:2936

```
// One active master on the per-function stack (RM 9.4): the IR temps holding
```

### ada83.c:2937

```
// its heap done-flag and runtime record, and the pending-activation count at
```

### ada83.c:2938

```
// scope entry (so its dependents can be activated, joined, and cleared as a
```

### ada83.c:2939

```
// range).
```

### ada83.c:2941

```
// done-flag temp from Emit_Master_Flag_Enter
```

### ada83.c:2942

```
// master record temp from __ada_master_enter
```

### ada83.c:2943

```
// pending_activation_count at scope entry
```

### ada83.c:2946

```
// A library-scope elaboration action deferred into the owning unit's ___elab
```

### ada83.c:2947

```
// function (RM 7.2): either a dynamic object initializer, or a whole
```

### ada83.c:2948

```
// declaration whose elaboration emits runtime code.
```

### ada83.c:2954

```
// What Emit_Master_Enter_If hands back to a scope: everything its matching
```

### ada83.c:2955

```
// Emit_Master_Exit needs. `entered` is false for a taskless scope (no flag,
```

### ada83.c:2956

```
// no record, nothing pushed) — the exit then only restores the pending count.
```

### ada83.c:2958

```
// 0 when not entered
```

### ada83.c:2959

```
// 0 when not entered
```

### ada83.c:2960

```
// pending_activation_count at scope entry
```

### ada83.c:2961

```
// true when __ada_master_enter was emitted (and pushed)
```

### ada83.c:2964

```
// ???
```

### ada83.c:2966

```
// Output stream and counters
```

### ada83.c:2967

```
// Destination file for LLVM IR text
```

### ada83.c:2968

```
// Next %N temporary register number
```

### ada83.c:2969

```
// Next label.N basic-block label number
```

### ada83.c:2970

```
// Next @global.N global variable number
```

### ada83.c:2971

```
// Next @.str.N string constant number
```

### ada83.c:2973

```
// Current context
```

### ada83.c:2974

```
// The subprogram currently being emitted
```

### ada83.c:2975

```
// Static nesting depth of current_function
```

### ada83.c:2977

```
// Control flow
```

### ada83.c:2978

```
// Target label for the nearest enclosing loop exit
```

### ada83.c:2979

```
// Target label for the nearest enclosing loop continue
```

### ada83.c:2980

```
// True if current path ends with a return
```

### ada83.c:2981

```
// True if the current basic block is already terminated
```

### ada83.c:2982

```
// True once the LLVM module header has been written
```

### ada83.c:2984

```
// Deferred work
```

### ada83.c:2985

```
// The "main" procedure symbol, or NULL
```

### ada83.c:2986

```
// Subprogram bodies to emit after the current unit (arena-grown)
```

### ada83.c:2987

```
// Number of deferred bodies queued
```

### ada83.c:2988

```
// Allocated slots in deferred_bodies
```

### ada83.c:2990

```
// Tasks declared in the current declarative part(s), awaiting activation at
```

### ada83.c:2991

```
// the enclosing master's `begin` (RM 9.3). A scope saves the count on entry
```

### ada83.c:2992

```
// and activates everything appended since, on the declaration->statement
```

### ada83.c:2993

```
// boundary. Stacked implicitly via the saved-count discipline. Arena-grown
```

### ada83.c:2994

```
// via Pending_Activation_Append; appends never drop.
```

### ada83.c:2999

```
// Conditional/timed entry call (RM 9.7.2/9.7.3). When entry_call_try_mode is
```

### ada83.c:3000

```
// set, the entry-call codegen emits __ada_entry_call_try (non-blocking /
```

### ada83.c:3001

```
// deadline) instead of the blocking call: entry_call_timeout_temp is the i64
```

### ada83.c:3002

```
// microsecond budget (0 = literal 0 = poll once, conditional), and the i8
```

### ada83.c:3003

```
// "rendezvoused" result is left in entry_call_result_temp.
```

### ada83.c:3007

```
// RM 9.7.3: the delay of a timed entry call is evaluated AFTER the entry
```

### ada83.c:3008

```
// name, its family index, and its parameter associations. Carry the delay
```

### ada83.c:3009

```
// expression here so the entry-call codegen can evaluate it at that point,
```

### ada83.c:3010

```
// once the arguments are in hand, rather than up front (c97302a).
```

### ada83.c:3013

```
// Active masters in the current function (RM 9.4). RETURN statements must
```

### ada83.c:3014

```
// complete every enclosing master — raise its done flag, join its dependents
```

### ada83.c:3015

```
// and leave its runtime record — before transferring control out. The single
```

### ada83.c:3016

```
// source of truth for the innermost master (Current_Master_Record reads the
```

### ada83.c:3017

```
// top). Arena-grown via Emit_Master_Enter_If; pushes never drop.
```

### ada83.c:3022

```
// Library-scope elaboration deferred into the owning unit's ___elab
```

### ada83.c:3023

```
// function — module scope admits no instructions (RM 7.2). Two shapes, kept
```

### ada83.c:3024

```
// in one list so they drain in declaration order: an object whose
```

### ada83.c:3025

```
// initializer is not a static literal (its global is emitted
```

### ada83.c:3026

```
// zero-initialized; the drain evaluates and stores it), and a declaration
```

### ada83.c:3027

```
// whose elaboration emits runtime code (subtype compatibility checks,
```

### ada83.c:3028

```
// RM 13.5 address overlays) — the drain re-enters Generate_Declaration for
```

### ada83.c:3029

```
// it inside the elaboration function. Filled at library scope, drained by
```

### ada83.c:3030

```
// the package-body / spec-only elaboration emitters.
```

### ada83.c:3033

```
// OBJECT_INITIALIZER: the object; DECLARATION: unused
```

### ada83.c:3034

```
// OBJECT_INITIALIZER: the initializer expression;
```

### ada83.c:3035

```
// DECLARATION: the declaration node itself
```

### ada83.c:3039

```
// Nested subprograms (static chain)
```

### ada83.c:3040

```
// Immediately enclosing function for uplevel access
```

### ada83.c:3041

```
// True while emitting a nested subprogram
```

### ada83.c:3043

```
// Exception handling state
```

### ada83.c:3044

```
// Label of the current exception handler entry
```

### ada83.c:3045

```
// Temp holding the setjmp buffer
```

### ada83.c:3046

```
// True inside a begin..exception..end
```

### ada83.c:3048

```
// RM 5.8/9.5: a return statement directly within an accept body completes the
```

### ada83.c:3049

```
// ACCEPT (writes OUT params back, wakes the caller, pops the accept handler)
```

### ada83.c:3050

```
// and then continues the task body after the accept — it does NOT return from
```

### ada83.c:3051

```
// the task body. These carry the innermost enclosing accept's completion
```

### ada83.c:3052

```
// context; saved/restored around each accept body so nested accepts nest.
```

### ada83.c:3053

```
// True while emitting an accept body
```

### ada83.c:3054

```
// Rendezvous record of the current caller
```

### ada83.c:3055

```
// Caller's parameter block
```

### ada83.c:3056

```
// The accept's formal parameter list
```

### ada83.c:3057

```
// Label just past the accept
```

### ada83.c:3059

```
// RM 9.10: cooperative abort-during-rendezvous. While emitting a task body,
```

### ada83.c:3060

```
// task_body_done_label is its termination epilogue (body_done). A blocking
```

### ada83.c:3061

```
// entry call is an abort-completion point: on return the body checks
```

### ada83.c:3062

```
// abort_pending (TCB+61) and, if this task was aborted during the rendezvous,
```

### ada83.c:3063

```
// branches here to terminate without running the rest of the body or its
```

### ada83.c:3064

```
// handlers.
```

### ada83.c:3065

```
// 0 when not in a task body's statements
```

### ada83.c:3067

```
// String constant accumulator
```

### ada83.c:3068

```
// Growing buffer for LLVM string constant data
```

### ada83.c:3069

```
// Bytes currently in the buffer
```

### ada83.c:3070

```
// Allocated capacity of the buffer
```

### ada83.c:3072

```
// Imported-subprogram extern declarations must land at module scope. A
```

### ada83.c:3073

```
// generic instantiation expands its declarative part at the instantiation
```

### ada83.c:3074

```
// point (inside a function body), so the `declare` for an imported operation
```

### ada83.c:3075

```
// it renames cannot be emitted in place — it is registered here and flushed
```

### ada83.c:3076

```
// once, at module level, after the unit's declarations are generated.
```

### ada83.c:3077

```
// Imported subprograms awaiting a top-level declare
```

### ada83.c:3078

```
// Number of entries in pending_extern_symbols
```

### ada83.c:3080

```
// A module may hold only one declaration per external name, yet each generic
```

### ada83.c:3081

```
// instantiation owns a distinct copy of the symbols it renames — so N
```

### ada83.c:3082

```
// instantiations importing the same C function yield N symbols all named,
```

### ada83.c:3083

```
// e.g., "fread". Track the external names already declared and emit each once.
```

### ada83.c:3084

```
// External names with a live module declare
```

### ada83.c:3085

```
// Number of entries in emitted_extern_names
```

### ada83.c:3086

```
// Mangled names with a module `declare`
```

### ada83.c:3089

```
// Library-level bodies whose RM 3.9
```

### ada83.c:3090

```
// elaboration-flag store must run in
```

### ada83.c:3091

```
// main's elaboration sequence (a store
```

### ada83.c:3092

```
// at module scope is not legal IR)
```

### ada83.c:3095

```
// Address markers
```

### ada83.c:3096

```
// Symbols needing @__address_marker globals
```

### ada83.c:3097

```
// Number of address markers registered
```

### ada83.c:3099

```
// Monotonic id for RM 3.9 elaboration flags
```

### ada83.c:3101

```
// Duplicate emission guard
```

### ada83.c:3102

```
// Unique IDs of already-emitted functions
```

### ada83.c:3103

```
// Number of entries in emitted_func_ids
```

### ada83.c:3105

```
// Task support
```

### ada83.c:3106

```
// True while emitting a task body
```

### ada83.c:3107

```
// Elaboration functions to call from main (arena-grown)
```

### ada83.c:3109

```
// Immediate parent when emitting a subunit CU
```

### ada83.c:3110

```
// Emitting a WITH-loaded unit, not the file's own
```

### ada83.c:3111

```
// Called across a SEPARATE boundary (arena-grown)
```

### ada83.c:3114

```
// Package stubs whose ___elab may be external (arena-grown)
```

### ada83.c:3117

```
// Number of registered elaboration functions
```

### ada83.c:3119

```
// Exception reference table
```

### ada83.c:3120

```
// Mangled names of referenced exceptions
```

### ada83.c:3121

```
// Number of exception references
```

### ada83.c:3123

```
// Miscellaneous
```

### ada83.c:3124

```
// True when trim-string helpers are needed
```

### ada83.c:3125

```
// Counter for runtime type descriptors
```

### ada83.c:3126

```
// Nesting depth inside aggregate generation
```

### ada83.c:3127

```
// True while generating an assignment-statement RHS (RM 5.2.1 sliding context)
```

### ada83.c:3128

```
// Enclosing aggregate is in slide context, so its
```

### ada83.c:3129

```
// inner dimension rows slide too (RM 5.2.1); a row
```

### ada83.c:3130

```
// inside a non-sliding component aggregate does not.
```

### ada83.c:3131

```
// True while generating the operand of a qualified expression (RM 4.7 — no sliding, exact bounds match)
```

### ada83.c:3132

```
// Low-bound temps for inner dimensions
```

### ada83.c:3133

```
// High-bound temps for inner dimensions
```

### ada83.c:3134

```
// Rep each lo/hi temp was produced at
```

### ada83.c:3135

```
// Number of inner aggregate bound pairs
```

### ada83.c:3136

```
// RM 4.3.2 applicable index constraint: an OTHERS aggregate takes its
```

### ada83.c:3137

```
// first-dimension bounds from context, not its own choices. When the context
```

### ada83.c:3138

```
// is an unconstrained array target those bounds are runtime (a fat pointer's
```

### ada83.c:3139

```
// 'FIRST/'LAST), so the assignment publishes them here for the immediately
```

### ada83.c:3140

```
// generated aggregate to consume; consumed-and-cleared so nested aggregates
```

### ada83.c:3141

```
// fall back to their own type's bounds.
```

### ada83.c:3142

```
// A constraint is published for the next aggregate
```

### ada83.c:3143

```
// Runtime first-dimension low bound
```

### ada83.c:3144

```
// Runtime first-dimension high bound
```

### ada83.c:3145

```
// Rep the lo/hi temps were produced at
```

### ada83.c:3146

```
// Cached discriminant values (typed)
```

### ada83.c:3147

```
// Number of entries in the disc cache
```

### ada83.c:3148

```
// Record type the disc cache belongs to
```

### ada83.c:3150

```
// Emit debug-location instrumentation. When `emit_debug_locations`
```

### ada83.c:3151

```
// is true, each emitted IR line gets a trailing `; @ FUNC:LINE` comment
```

### ada83.c:3152

```
// identifying the C source site that started the line. Toggled by `-g`.
```

### ada83.c:3154

```
// Debug-only producer-honesty checker: shadow of each temp's actual emitted
```

### ada83.c:3155

```
// LLVM result type, so Val_Rep / Val_Of_Type can assert the label matches reality.
```

### ada83.c:3156

```
// Indexed by temp id; epoch-stamped to invalidate across function boundaries
```

### ada83.c:3157

```
// (temp ids reset per function). Active only under -g.
```

### ada83.c:3159

```
// "func:line" of the defining Emit
```

### ada83.c:3162

```
// bumped at each function start
```

### ada83.c:3164

```
// true if next Emit begins a fresh IR line
```

### ada83.c:3165

```
// any non-whitespace written since last '\n'
```

### ada83.c:3166

```
// captured __func__ when line started
```

### ada83.c:3167

```
// captured __LINE__ when line started
```

### ada83.c:3170

```
// Global code generator state
```

### ada83.c:3173

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3174

```
//
```

### ada83.c:3175

```
// §13.1 Core IR Emission Primitives
```

### ada83.c:3176

```
//
```

### ada83.c:3177

```
// LLVM IR is an SSA (Static Single Assignment) intermediate representation. Every value lives in a
```

### ada83.c:3178

```
// numbered "temporary" written %1, %2, ... and is defined exactly once. Emit_Temp() hands out the
```

### ada83.c:3179

```
// next number; the Emit() variadic function writes a line of IR text to the output stream.
```

### ada83.c:3180

```
//
```

### ada83.c:3181

```
// Control flow is expressed as "basic blocks" - straight-line instruction sequences ending in
```

### ada83.c:3182

```
// exactly one terminator (br, ret, switch, unreachable). Emit_Label_Here() opens a new block;
```

### ada83.c:3183

```
// Emit_Branch_If_Needed() closes the current one with a branch if it is not already terminated.
```

### ada83.c:3184

```
// This two-function protocol prevents the common bug of emitting instructions after a terminator
```

### ada83.c:3185

```
// (which LLVM rejects).
```

### ada83.c:3186

```
//
```

### ada83.c:3187

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3189

```
// Allocate a fresh SSA temporary number (%N). Numbers are never reused within a function.
```

### ada83.c:3191

```
// Allocate a fresh basic-block label number (label.N).
```

### ada83.c:3193

```
// Construct a typed SSA value: pairs a register id with its LLVM type.
```

### ada83.c:3194

```
// Constructors are macros for __func__/__LINE__ capture. See Val_Rep_Impl
```

### ada83.c:3195

```
// for the actual definition.
```

### ada83.c:3199

```
// Write a formatted line of LLVM IR text to the output stream (like fprintf to cg->output).
```

### ada83.c:3200

```
// The public `Emit (...)` macro forwards `__func__`/`__LINE__` from the call site
```

### ada83.c:3201

```
// so that, with `-g`, each IR line carries a trailing `; @ FUNC:LINE` comment.
```

### ada83.c:3204

```
// Mint a fresh SSA temporary and emit one instruction line defining it:
```

### ada83.c:3205

```
// `  %tN = <formatted text>`. Returns N. The caller's format string carries
```

### ada83.c:3206

```
// everything after the `=` (including the trailing newline, when the line is
```

### ada83.c:3207

```
// complete). NOTE: argument expressions are evaluated before the temporary is
```

### ada83.c:3208

```
// minted, so they must not emit IR or mint temporaries themselves.
```

### ada83.c:3211

```
// Emit an LLVM debug-location comment anchoring the next instruction to source.
```

### ada83.c:3213

```
// Emit a basic-block label, closing the previous block if it has no terminator.
```

### ada83.c:3215

```
// If the current block lacks a terminator, emit an unconditional "br label %N".
```

### ada83.c:3217

```
// Close an emitted runtime-support function that returns void: optionally a
```

### ada83.c:3218

```
// fallthrough branch to its shared named `done` block, then the block itself,
```

### ada83.c:3219

```
// `ret void`, and the closing brace.
```

### ada83.c:3221

```
// Append formatted text to the string-constant accumulator (for @.str.N globals).
```

### ada83.c:3223

```
// Append a single character to the string-constant accumulator.
```

### ada83.c:3225

```
// Append a symbol's mangled name to the string-constant accumulator (for
```

### ada83.c:3226

```
// module-level global definitions created while inside a function body).
```

### ada83.c:3228

```
// As above, but with the full Emit_Symbol_Name naming rules applied.
```

### ada83.c:3230

```
// Module-level zero-initialized definition for a per-instance global.
```

### ada83.c:3232

```
// Emit a floating-point constant using LLVM's hexadecimal literal syntax.
```

### ada83.c:3238

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3239

```
//
```

### ada83.c:3240

```
// §13.1.1 LLVM Type Helpers
```

### ada83.c:3241

```
//
```

### ada83.c:3242

```
// LLVM's type system is simpler than Ada's: integers are iN, floats are "float"/"double", and all
```

### ada83.c:3243

```
// pointers are the opaque type "ptr". These helpers translate between Ada's rich type model and
```

### ada83.c:3244

```
// LLVM's flat one, choosing the correct comparison predicate, arithmetic width, or bounds struct
```

### ada83.c:3245

```
// layout.
```

### ada83.c:3246

```
//
```

### ada83.c:3247

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3249

```
// Canonical Ada → LLVM representation lowering: single source of truth for
```

### ada83.c:3250

```
// fat-vs-thin policy and integer/float widths. Spell IR text at the Emit()
```

### ada83.c:3251

```
// site via LLVM_Rep_To_String (Type_To_Rep (t)); everywhere else dispatch
```

### ada83.c:3252

```
// on the rep fields.
```

### ada83.c:3261

```
// Return the LLVM integer type used for string bounds (e.g. "i32").
```

### ada83.c:3263

```
// Return the LLVM integer type used for general-purpose integer arithmetic.
```

### ada83.c:3267

```
// Return the wider of two LLVM integer types (e.g. wider of "i16" and "i32" is "i32").
```

### ada83.c:3269

```
// Return the LLVM fcmp predicate string ("oeq", "olt", ...) for a floating-point comparison.
```

### ada83.c:3271

```
// Return the LLVM icmp predicate ("eq", "slt", "ult", ...) for an integer comparison.
```

### ada83.c:3274

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3275

```
//
```

### ada83.c:3276

```
// §13.1.2 Name Mangling and Symbol References
```

### ada83.c:3277

```
//
```

### ada83.c:3278

```
// LLVM IR uses textual identifiers for globals (@name) and locals (%name). Ada's dotted names
```

### ada83.c:3279

```
// (Package.Subprogram) and overloaded identifiers must be encoded into unique, linker-safe strings.
```

### ada83.c:3280

```
// The mangling scheme prefixes each component with its byte length (like Itanium C++ mangling) so,
```

### ada83.c:3281

```
// for example, "Ada.Text_IO.Put" becomes "_ada__3ada8text_io3put".
```

### ada83.c:3282

```
//
```

### ada83.c:3283

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3285

```
// Return true if the symbol has global (module-level) linkage rather than local scope.
```

### ada83.c:3288

```
// Mangle one name component into buf[pos..pos+max]; return the new write position.
```

### ada83.c:3294

```
// Mangle a full symbol (with parent chain) into buf; return the new write position.
```

### ada83.c:3300

```
// Return the mangled linker name for a symbol as an arena-allocated slice.
```

### ada83.c:3302

```
// Return a qualified mangled name by joining parent and child components.
```

### ada83.c:3304

```
// Emit "@mangled_name" (the define/declare name) for a symbol.
```

### ada83.c:3307

```
// Emit "@mangled_name" as a reference (call target or global address).
```

### ada83.c:3309

```
// Emit the alloca or global declaration that provides storage for a symbol.
```

### ada83.c:3311

```
// Emit a reference to an exception's runtime type descriptor global.
```

### ada83.c:3315

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3316

```
//
```

### ada83.c:3317

```
// §13.1.3 Static Chain - Nested Subprogram Access
```

### ada83.c:3318

```
//
```

### ada83.c:3319

```
// Ada allows nested subprograms to reference variables declared in enclosing scopes. LLVM has no
```

### ada83.c:3320

```
// built-in closure mechanism, so the compiler passes a hidden "static chain" pointer as an extra
```

### ada83.c:3321

```
// argument. Each nesting level adds one pointer indirection; the depth is computed at compile time
```

### ada83.c:3322

```
// from the symbol table's nesting_level fields.
```

### ada83.c:3323

```
//
```

### ada83.c:3324

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3326

```
// Walk the scope chain to find the nearest enclosing subprogram symbol.
```

### ada83.c:3328

```
// Return true if this subprogram needs a static-chain parameter (has uplevel references).
```

### ada83.c:3330

```
// Return true if accessing this symbol requires traversing a static chain.
```

### ada83.c:3332

```
// Return the number of static-chain hops needed to reach a nested procedure's frame.
```

### ada83.c:3334

```
// Precompute the static-chain argument for a nested call (returns a temp holding the pointer).
```

### ada83.c:3336

```
// Emit the static-chain argument in a call instruction; return true if one was emitted.
```

### ada83.c:3338

```
// Follow the rename chain for subprogram renamings (RM 8.5). Returns the
```

### ada83.c:3339

```
// underlying body. Call this on a callable symbol before emitting `@name`
```

### ada83.c:3340

```
// or checking static-chain nesting; renames do not generate their own body.
```

### ada83.c:3342

```
// Map an operator's source-text name (e.g. "+", "<=", "mod") to its Token_Kind.
```

### ada83.c:3343

```
// Returns TK_EOF if the name isn't an operator.
```

### ada83.c:3346

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3347

```
//
```

### ada83.c:3348

```
// §13.2 Runtime Checks and Type Conversions
```

### ada83.c:3349

```
//
```

### ada83.c:3350

```
// Ada requires runtime checks for range violations, overflow, division by zero, null access, index
```

### ada83.c:3351

```
// bounds, array length mismatches, and discriminant mismatches. Each check category can be
```

### ada83.c:3352

```
// suppressed via pragma Suppress. These functions emit LLVM IR that compares a value against bounds
```

### ada83.c:3353

```
// and branches to an exception-raising block on failure.
```

### ada83.c:3354

```
//
```

### ada83.c:3355

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3357

```
// ???
```

### ada83.c:3361

```
// ???
```

### ada83.c:3364

```
// ???
```

### ada83.c:3376

```
// RM 11.1: raise STORAGE_ERROR at an object's elaboration when its storage
```

### ada83.c:3377

```
// exceeds what the stack provides. The Oversized variant is the structural
```

### ada83.c:3378

```
// raise for placeholder-slot objects beyond MAX_STACK_OBJECT_BYTES; the probe
```

### ada83.c:3379

```
// variants emit an __ada_stack_check call testing the actual remaining stack.
```

### ada83.c:3384

```
// RM 4.6 array conversion: raise CONSTRAINT_ERROR when the operand and target
```

### ada83.c:3385

```
// array types have component subtypes whose constraints differ.
```

### ada83.c:3389

```
// RM 3.9: raise PROGRAM_ERROR at a call whose target body has not yet been
```

### ada83.c:3390

```
// elaborated (the callee's elaboration flag is still false).
```

### ada83.c:3393

```
// ???
```

### ada83.c:3400

```
// ???
```

### ada83.c:3407

```
// ???
```

### ada83.c:3416

```
// ???
```

### ada83.c:3435

```
// ???
```

### ada83.c:3442

```
// ???
```

### ada83.c:3454

```
// ???
```

### ada83.c:3457

```
// ???
```

### ada83.c:3463

```
// Constraint check (RM 5.2.1, 4.6, 6.4.1). Value carries its real rep; the
```

### ada83.c:3464

```
// check widens to the comparison rep internally. There is no bare-reg form -
```

### ada83.c:3465

```
// the rep MUST come from the producer.
```

### ada83.c:3480

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3481

```
//
```

### ada83.c:3482

```
// §13.3 Fat-Pointer Operations
```

### ada83.c:3483

```
//
```

### ada83.c:3484

```
// Ada unconstrained arrays (e.g. String) carry their bounds at runtime. In LLVM IR this is modelled
```

### ada83.c:3485

```
// as a "fat pointer" - a {ptr, ptr} pair where the first element points to the data and the second
```

### ada83.c:3486

```
// to a {i32, i32} bounds struct holding the low and high index values. This design lets slices,
```

### ada83.c:3487

```
// function parameters, and allocator results share a uniform representation without knowing the
```

### ada83.c:3488

```
// array size at compile time.
```

### ada83.c:3489

```
//
```

### ada83.c:3490

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3553

```
// Total byte size (i64) of the data behind a fat pointer: product of dimension
```

### ada83.c:3554

```
// lengths * element size, clamped >= 0. Lengths derived at the index width.
```

### ada83.c:3594

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3595

```
//
```

### ada83.c:3596

```
// §13.4 Bound and Length Emission
```

### ada83.c:3597

```
//
```

### ada83.c:3598

```
// Array dimensions carry compile-time or runtime bounds. These helpers emit IR to extract bounds
```

### ada83.c:3599

```
// from Type_Info (when statically known) or from fat-pointer structs (when dynamic), compute
```

### ada83.c:3600

```
// lengths as high - low + 1, and clamp to zero for null ranges.
```

### ada83.c:3601

```
//
```

### ada83.c:3602

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3604

```
// Pair of LLVM temporaries holding the low and high bounds of an array
```

### ada83.c:3605

```
// dimension, together with the LLVM integer type they are expressed in.
```

### ada83.c:3607

```
// LLVM temp register for the low bound
```

### ada83.c:3608

```
// LLVM temp register for the high bound
```

### ada83.c:3609

```
// LLVM rep of the bounds (the integer index width)
```

### ada83.c:3654

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3655

```
//
```

### ada83.c:3656

```
// §13.5 Exception Handling
```

### ada83.c:3657

```
//
```

### ada83.c:3658

```
// Ada exceptions use a setjmp/longjmp model: entering a begin..exception..end block calls setjmp to
```

### ada83.c:3659

```
// save the machine state, then dispatches to the matching handler on longjmp. The Exception_Setup
```

### ada83.c:3660

```
// struct captures the four LLVM temporaries/labels emitted at block entry.
```

### ada83.c:3661

```
//
```

### ada83.c:3662

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3664

```
// The four LLVM labels or temps emitted when entering a begin..exception..end block
```

### ada83.c:3666

```
// Stack alloca for the exception handler frame
```

### ada83.c:3667

```
// setjmp buffer temp
```

### ada83.c:3668

```
// Label for the non-exceptional continuation
```

### ada83.c:3669

```
// Label for the exception dispatch entry
```

### ada83.c:3679

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3680

```
//
```

### ada83.c:3681

```
// §13.6 Expression Generation
```

### ada83.c:3682

```
//
```

### ada83.c:3683

```
// Each Generate_* function lowers one AST node kind to LLVM IR. The return value is a uint32_t
```

### ada83.c:3684

```
// temporary number (%N) holding the result. Expressions are compiled bottom-up: leaves (literals,
```

### ada83.c:3685

```
// identifiers) produce a single load or constant; compound nodes (binary ops, calls, aggregates)
```

### ada83.c:3686

```
// recursively generate their children and then emit the combining instruction.
```

### ada83.c:3687

```
//
```

### ada83.c:3688

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3706

```
// Registers of a 'VALUE string argument after blank stripping (RM 3.5.5):
```

### ada83.c:3707

```
// the address of the first non-blank character and the trimmed length at
```

### ada83.c:3708

```
// the integer working width.
```

### ada83.c:3715

```
// The shared tail of a constant-valued integer attribute arm: load `value`
```

### ada83.c:3716

```
// into the caller's pre-allocated result register `t`, tagged with the
```

### ada83.c:3717

```
// attribute's IR comment, and return it at the integer working width.
```

### ada83.c:3728

```
// Raise CONSTRAINT_ERROR unless a non-null access value's designated object
```

### ada83.c:3729

```
// satisfies a constrained access subtype's index/discriminant constraint.
```

### ada83.c:3736

```
// The full aggregate generator; call only through Generate_Aggregate, which
```

### ada83.c:3737

```
// establishes the RM 5.2.1 dimension-row slide inheritance around it.
```

### ada83.c:3790

```
// Convert an already-generated scalar VALUE to DEST's representation for a
```

### ada83.c:3791

```
// store, applying fixed-point /SMALL scaling (RM 3.5.9) when a real value
```

### ada83.c:3792

```
// targets a fixed-point type. DEST_REP is DEST's store representation, passed
```

### ada83.c:3793

```
// in so the caller's own fallback applies when DEST is unknown.
```

### ada83.c:3833

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3834

```
//
```

### ada83.c:3835

```
// §13.7 Statement Generation
```

### ada83.c:3836

```
//
```

### ada83.c:3837

```
// Statements produce side effects rather than values, so they return void. Control flow is lowered
```

### ada83.c:3838

```
// to LLVM basic blocks connected by br/switch terminators.
```

### ada83.c:3839

```
//
```

### ada83.c:3840

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3844

```
// RM 11.4: run a package-body elaboration statement sequence under its own
```

### ada83.c:3845

```
// exception handlers (bare when there are none).
```

### ada83.c:3859

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3860

```
//
```

### ada83.c:3861

```
// §13.8 Declaration Generation
```

### ada83.c:3862

```
//
```

### ada83.c:3863

```
// Declarations allocate storage (alloca for locals, @global for globals), emit initialisation code,
```

### ada83.c:3864

```
// and register subprogram definitions. Deferred bodies are queued for later emission to handle
```

### ada83.c:3865

```
// forward references.
```

### ada83.c:3866

```
//
```

### ada83.c:3867

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3869

```
// ???
```

### ada83.c:3871

```
// ???
```

### ada83.c:3879

```
// Operations over every task embedded in a composite object (records and
```

### ada83.c:3880

```
// arrays containing task components, recursively).
```

### ada83.c:3882

```
// create each TCB and store it in its slot
```

### ada83.c:3883

```
// spawn each task's thread
```

### ada83.c:3884

```
// wait for each activation; record failures
```

### ada83.c:3885

```
// join each task
```

### ada83.c:3886

```
// mark each created-but-never-activated task
```

### ada83.c:3887

```
// completed (failed batch activation, RM 9.3)
```

### ada83.c:3960

```
// Elaborate a tree-copy instance's formal-object bindings at the
```

### ada83.c:3961

```
// instantiation point (RM 12.3(80)).
```

### ada83.c:3963

```
// Emit the synthesized wrapper bodies of an instance's bindings.
```

### ada83.c:3967

```
// Emit module-scope zero-initialized storage for a package instance's
```

### ada83.c:3968

```
// exported variables and constants (per-instance globals named
```

### ada83.c:3969

```
// <instance>__<var>). Initializers run at the instantiation point.
```

### ada83.c:3971

```
// ???
```

### ada83.c:3976

```
// One source of truth for a subprogram's LLVM signature spelling. The
```

### ada83.c:3977

```
// define (Emit_Function_Header) and every declare of the same symbol
```

### ada83.c:3978

```
// (Emit_Extern_Subprogram, the SEPARATE-boundary flush) must spell
```

### ada83.c:3979

```
// identical return and parameter lists or llvm-link rejects the pair.
```

### ada83.c:3990

```
// ???
```

### ada83.c:3995

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:3996

```
//
```

### ada83.c:3997

```
// §13.9 Aggregate Helpers
```

### ada83.c:3998

```
//
```

### ada83.c:3999

```
// Ada aggregates (both array and record) are compiled by classifying the associations
```

### ada83.c:4000

```
// (positional vs. named vs. others), allocating storage, and emitting per-element stores.
```

### ada83.c:4001

```
// Multi-dimensional array aggregates and discriminated record aggregates require careful
```

### ada83.c:4002

```
// index arithmetic and variant-part selection.
```

### ada83.c:4003

```
//
```

### ada83.c:4004

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4006

```
// One scan of an array or record aggregate's items: the item classification
```

### ada83.c:4007

```
// (positional / named / OTHERS) together with the span of its static choices.
```

### ada83.c:4008

```
// Every consumer of "what do the choices cover" reads one of these instead of
```

### ada83.c:4009

```
// rescanning the item list.
```

### ada83.c:4011

```
// Items that are not associations
```

### ada83.c:4012

```
// Some item is a named association
```

### ada83.c:4013

```
// An OTHERS association is present
```

### ada83.c:4014

```
// The OTHERS expression, or NULL
```

### ada83.c:4015

```
// Some static choice contributed a low bound
```

### ada83.c:4016

```
// Some static choice contributed a high bound
```

### ada83.c:4017

```
// Least static single-index / range low
```

### ada83.c:4018

```
// Greatest static single-index / range high
```

### ada83.c:4019

```
// Some range choice bound is non-static
```

### ada83.c:4020

```
// Some single (non-range) choice is non-static
```

### ada83.c:4021

```
// First range choice with a non-static bound
```

### ada83.c:4022

```
// First non-static single (non-range) choice
```

### ada83.c:4023

```
// First item's value (positional item or association expression)
```

### ada83.c:4024

```
// First named association's expression
```

### ada83.c:4027

```
// A single discriminant allocation entry: symbol plus its LLVM temp.
```

### ada83.c:4033

```
// Array of discriminant allocation entries for aggregate generation.
```

### ada83.c:4039

```
// ???
```

### ada83.c:4047

```
// Raise CONSTRAINT_ERROR unless a fat-pointer array value's bounds equal a
```

### ada83.c:4048

```
// constrained array component subtype's bounds (RM 4.3.2 belongs check).
```

### ada83.c:4053

```
// ???
```

### ada83.c:4061

```
// A discrete operand of the array-aggregate machinery that is either a
```

### ada83.c:4062

```
// compile-time constant or an emitted SSA value. "Static" is a property of
```

### ada83.c:4063

```
// the operand, not of a separate lowering path: every helper below accepts
```

### ada83.c:4064

```
// either form and folds the constant case into the same emission.
```

### ada83.c:4066

```
// value is compile-time known
```

### ada83.c:4067

```
// the constant, when is_static
```

### ada83.c:4068

```
// SSA temp at the owning context's index_rep
```

### ada83.c:4069

```
// (for a static operand, an optional pre-materialization)
```

### ada83.c:4074

```
// Threaded state of one array aggregate's per-choice lowering: the target
```

### ada83.c:4075

```
// storage, the element layout facts every store needs, the aggregate bounds
```

### ada83.c:4076

```
// as operands, and the bookkeeping the choice handlers and the OTHERS pass
```

### ada83.c:4077

```
// share (RM 4.3.2, 4.3.3).
```

### ada83.c:4079

```
// Target storage and element layout
```

### ada83.c:4080

```
// aggregate storage (alloca temp)
```

### ada83.c:4081

```
// outer level of a multi-dimensional aggregate?
```

### ada83.c:4082

```
// element stored by memcpy rather than store
```

### ada83.c:4083

```
// the aggregate's array type
```

### ada83.c:4084

```
// LLVM rep for scalar element stores
```

### ada83.c:4085

```
// element subtype (drives the scalar check)
```

### ada83.c:4086

```
// static element (or row) byte size
```

### ada83.c:4087

```
// runtime element/row byte stride temp (0 = static)
```

### ada83.c:4088

```
// rep of rt_element_size
```

### ada83.c:4090

```
// Aggregate bounds and index arithmetic
```

### ada83.c:4091

```
// rep of every runtime index value below
```

### ada83.c:4092

```
// first-dimension low bound
```

### ada83.c:4093

```
// first-dimension high bound
```

### ada83.c:4094

```
// expr whose evaluation produced low.reg, or NULL;
```

### ada83.c:4095

```
// a choice bound that IS that expr reuses the
```

### ada83.c:4096

```
// SSA value instead of re-evaluating (RM 3.2.1)
```

### ada83.c:4098

```
// Compile-time coverage (static-bounds aggregates only)
```

### ada83.c:4099

```
// per-flat-index "was stored" map, or NULL
```

### ada83.c:4100

```
// map length
```

### ada83.c:4102

```
// RM 4.3.2(3): index-subtype membership checks on choices
```

### ada83.c:4104

```
// subtype bounds compile-time known
```

### ada83.c:4106

```
// for runtime bound loads when non-static
```

### ada83.c:4108

```
// RM 4.3.2(6): named-choice span tracking (min-low / max-high allocas)
```

### ada83.c:4112

```
// RM 4.3.2(6): inner sub-aggregate bounds consistency tracking
```

### ada83.c:4118

```
// i1 SSA: aggregate byte size is non-zero
```

### ada83.c:4119

```
// (0 = no guard); a null aggregate has no
```

### ada83.c:4120

```
// components, so bound consistency is vacuous
```

### ada83.c:4122

```
// Fat-pointer bound seeds of an unconstrained aggregate: first runtime
```

### ada83.c:4123

```
// choice bound per side, for dimensions 0 and 1
```

### ada83.c:4127

```
// Non-OTHERS choice spans, for the runtime OTHERS fill to skip
```

### ada83.c:4128

```
// (allocated only when a runtime-bounds aggregate has OTHERS)
```

### ada83.c:4174

```
// Enumeration representation clauses (RM 13.3): an object is stored by position
```

### ada83.c:4175

```
// and its internal CODE surfaces only where the representation is observed — an
```

### ada83.c:4176

```
// unchecked conversion in or out of the type. Enum_Rep_Base returns the
```

### ada83.c:4177

```
// enclosing enumeration type that carries a representation clause, or NULL for
```

### ada83.c:4178

```
// the identity mapping (code == position); the Emit helpers fold between a code
```

### ada83.c:4179

```
// and its position at that boundary.
```

### ada83.c:4197

```
// ???
```

### ada83.c:4204

```
// Shared emitters for the exponent handling of the __ada_parse_integer
```

### ada83.c:4205

```
// runtime (RM 2.4): the decimal and the based literal arm each emit an
```

### ada83.c:4206

```
// exponent digit-scan loop, a power loop, and signed-return tails that
```

### ada83.c:4207

```
// differ only in names and in the power loop's multiplier (10 for a
```

### ada83.c:4208

```
// decimal literal, the runtime %base for a based one).
```

### ada83.c:4224

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4225

```
//
```

### ada83.c:4226

```
// §13.10 Build-in-Place Protocol
```

### ada83.c:4227

```
//
```

### ada83.c:4228

```
// Functions returning limited types (records with tasks, controlled components, or explicit limited
```

### ada83.c:4229

```
// declarations) cannot be copied. Instead the caller passes extra hidden formals (BIPalloc,
```

### ada83.c:4230

```
// BIPaccess, BIPmaster, BIPchain, BIPfinal) that tell the callee where to construct the result
```

### ada83.c:4231

```
// directly. This avoids illegal copies of limited objects.
```

### ada83.c:4232

```
//
```

### ada83.c:4233

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4235

```
// Build-in-Place Formal Names
```

### ada83.c:4243

```
// ???
```

### ada83.c:4244

```
// ???
```

### ada83.c:4245

```
// ???
```

### ada83.c:4246

```
// ???
```

### ada83.c:4247

```
// ???
```

### ada83.c:4248

```
// ???
```

### ada83.c:4255

```
// Context for a BIP function call: describes where and how the result object is allocated
```

### ada83.c:4257

```
// The function returning a limited type
```

### ada83.c:4258

```
// The limited result type
```

### ada83.c:4259

```
// Who allocates the result storage
```

### ada83.c:4260

```
// LLVM temp holding the destination pointer
```

### ada83.c:4261

```
// True if the result needs finalisation
```

### ada83.c:4262

```
// True if the result contains task components
```

### ada83.c:4265

```
// Per-function state tracking during BIP function code generation.
```

### ada83.c:4267

```
// True while emitting a BIP function body
```

### ada83.c:4268

```
// Temp for the BIPalloc extra formal
```

### ada83.c:4269

```
// Temp for the BIPaccess extra formal
```

### ada83.c:4270

```
// Temp for the BIPmaster extra formal
```

### ada83.c:4271

```
// Temp for the BIPchain extra formal
```

### ada83.c:4272

```
// True if the result type contains tasks
```

### ada83.c:4275

```
// Global BIP state for current function being generated
```

### ada83.c:4278

```
// ???
```

### ada83.c:4282

```
// ???
```

### ada83.c:4288

```
// ???
```

### ada83.c:4294

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4295

```
//
```

### ada83.c:4296

```
// §14. Library Management - ALI files, checksums, dependency tracking
```

### ada83.c:4297

```
//
```

### ada83.c:4298

```
// ALI (Ada Library Information) files record dependencies, checksums, and exported symbols per
```

### ada83.c:4299

```
// compilation unit. When a package is WITH'd, the compiler first checks the ALI cache to avoid
```

### ada83.c:4300

```
// re-parsing unchanged sources. CRC32 checksums detect stale dependencies; exported symbols are
```

### ada83.c:4301

```
// reinstalled into the symbol table from ALI data.
```

### ada83.c:4302

```
//
```

### ada83.c:4303

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4305

```
// One WITH dependency recorded in an ALI file.
```

### ada83.c:4307

```
// Withed unit name
```

### ada83.c:4308

```
// Source filename of the withed unit
```

### ada83.c:4309

```
// ALI filename of the withed unit
```

### ada83.c:4310

```
// True for a limited-with clause
```

### ada83.c:4311

```
// Pragma Elaborate applies
```

### ada83.c:4312

```
// Pragma Elaborate_All applies
```

### ada83.c:4315

```
// One compilation unit described in an ALI file.
```

### ada83.c:4317

```
// Fully qualified Ada unit name
```

### ada83.c:4318

```
// Filename of the source
```

### ada83.c:4319

```
// CRC32 of the source text
```

### ada83.c:4320

```
// The unit's scope, for frame-layout F lines
```

### ada83.c:4321

```
// True for a body unit, false for a spec
```

### ada83.c:4322

```
// Package unit (spec or body); false = subprogram/task
```

### ada83.c:4323

```
// True for a SEPARATE subunit (name is PARENT.CHILD)
```

### ada83.c:4324

```
// True for a generic unit
```

### ada83.c:4325

```
// Pragma Preelaborate applies
```

### ada83.c:4326

```
// Pragma Pure applies
```

### ada83.c:4327

```
// Unit has elaboration-time code
```

### ada83.c:4328

```
// Spec requires a compiled body (RM 7.1)
```

### ada83.c:4329

```
// Body-stub names (SEPARATE) in this body
```

### ada83.c:4331

```
// THIS unit's WITH clauses (not the file's)
```

### ada83.c:4335

```
// One exported symbol in an ALI file.
```

### ada83.c:4337

```
// Ada name of the exported entity
```

### ada83.c:4338

```
// Linker-level mangled name
```

### ada83.c:4339

```
// 'V' variable, 'F' function, 'T' type, etc.
```

### ada83.c:4340

```
// Source line where the entity is declared
```

### ada83.c:4341

```
// Ada name of the entity's type
```

### ada83.c:4342

```
// LLVM IR type string
```

### ada83.c:4343

```
// Number of parameters (for subprograms)
```

### ada83.c:4347

```
// Arena-grown, no cap
```

### ada83.c:4350

```
// Arena-grown, no cap
```

### ada83.c:4355

```
// An exported symbol as read back from an ALI file (heap-allocated strings).
```

### ada83.c:4357

```
// Entity kind character ('V', 'F', 'T', etc.)
```

### ada83.c:4358

```
// Ada name (heap-allocated)
```

### ada83.c:4359

```
// Linker-level name (heap-allocated)
```

### ada83.c:4360

```
// LLVM IR type string (heap-allocated)
```

### ada83.c:4361

```
// Source line number
```

### ada83.c:4362

```
// Ada type name (heap-allocated)
```

### ada83.c:4363

```
// Number of parameters (for subprograms)
```

### ada83.c:4366

```
// Cached summary of one previously read ALI file, used to avoid re-parsing.
```

### ada83.c:4368

```
// Fully qualified Ada unit name
```

### ada83.c:4369

```
// Source filename
```

### ada83.c:4370

```
// ALI filename on disk
```

### ada83.c:4371

```
// CRC32 of the source at compile time
```

### ada83.c:4372

```
// True for a specification, false for a body
```

### ada83.c:4373

```
// True for a generic unit
```

### ada83.c:4374

```
// Pragma Preelaborate applies
```

### ada83.c:4375

```
// Pragma Pure applies
```

### ada83.c:4376

```
// True once symbols have been installed
```

### ada83.c:4377

```
// Names of units this unit depends on
```

### ada83.c:4378

```
// Number of with dependencies
```

### ada83.c:4379

```
// Exported symbols (arena-grown, no cap)
```

### ada83.c:4380

```
// Number of exported symbols
```

### ada83.c:4387

```
// The program library catalog (RM 10.4): unit name -> the source file that
```

### ada83.c:4388

```
// provides it. Populated in-process as units load or compile, and from the
```

### ada83.c:4389

```
// ALI files of earlier compilations in the output directory — that is how
```

### ada83.c:4390

```
// a unit living in a file whose NAME does not match (ACATS fragments,
```

### ada83.c:4391

```
// subunit files) becomes findable. Arena-grown, no cap.
```

### ada83.c:4398

```
// One published frame slot: a frame-resident variable's emitted name and
```

### ada83.c:4399

```
// its byte offset in the owning library unit's frame.
```

### ada83.c:4401

```
// Mangled name, as emitted
```

### ada83.c:4402

```
// Byte offset within the unit's frame
```

### ada83.c:4406

```
// Library unit name; PARENT.CHILD for subunits
```

### ada83.c:4407

```
// Path of the file that provides the unit
```

### ada83.c:4409

```
// Source checksum recorded at registration
```

### ada83.c:4410

```
// ALI mtime: newest registration REPLACES (RM 10.3)
```

### ada83.c:4411

```
// Already loaded into THIS compilation
```

### ada83.c:4413

```
// The unit's PUBLISHED frame layout (ALI F/FS lines): the module that
```

### ada83.c:4414

```
// compiled the unit allocated the frame, so its offsets are the ground
```

### ada83.c:4415

```
// truth every subunit compilation must adopt — recomputing them from a
```

### ada83.c:4416

```
// fresh resolution diverges on any side-effect difference.
```

### ada83.c:4419

```
// The frame alloca size (chain-slot base)
```

### ada83.c:4421

```
// The unit's WITH dependencies (ALI W lines) — what the binder walks
```

### ada83.c:4422

```
// to verify the closure of a main program is consistent (RM 10.3).
```

### ada83.c:4426

```
// RM 7.1/10.2 completeness facts: whether a spec requires a body
```

### ada83.c:4427

```
// (ALI RB flag), whether the unit is a subprogram (ALI SU flag — a
```

### ada83.c:4428

```
// subprogram's body may answer a spec lookup, a package's may not),
```

### ada83.c:4429

```
// and which subunits a body's stubs demand (ST lines).
```

### ada83.c:4458

```
// Map an Ada type name to its LLVM IR type string via the symbol table.
```

### ada83.c:4459

```
// Falls back to the default integer type if the name is not found.
```

### ada83.c:4485

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4486

```
//
```

### ada83.c:4487

```
// §15. Elaboration - Dependency ordering for multi-unit programs
```

### ada83.c:4488

```
//
```

### ada83.c:4489

```
// Library-level packages must be elaborated in dependency order before the main procedure runs.
```

### ada83.c:4490

```
// This section builds the dependency graph, detects strongly-connected components via Tarjan's
```

### ada83.c:4491

```
// algorithm, and produces a topological ordering. Cycles caused by Elaborate_All pragmas are
```

### ada83.c:4492

```
// detected and reported.
```

### ada83.c:4493

```
//
```

### ada83.c:4494

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4496

```
// What a graph vertex stands for: a unit's spec, its body, or a unit
```

### ada83.c:4497

```
// that only ever has one of the two.
```

### ada83.c:4500

```
// Three-way ordering used when choosing the next vertex to elaborate.
```

### ada83.c:4503

```
// Why one unit must (or should) elaborate before another.
```

### ada83.c:4505

```
// WITH clause: the named unit's spec first (RM 10.5)
```

### ada83.c:4506

```
// pragma Elaborate: the named unit's body first
```

### ada83.c:4507

```
// pragma Elaborate_All: transitively binding
```

### ada83.c:4508

```
// A unit's spec always precedes its own body
```

### ada83.c:4509

```
// Elaboration code calls into the unit — WEAK: a
```

### ada83.c:4510

```
// preference, never a constraint that can deadlock
```

### ada83.c:4511

```
// Synthetic ordering injected by the algorithm
```

### ada83.c:4514

```
// Outcome of the order computation. Anything but OK means no consistent
```

### ada83.c:4515

```
// order exists and the program is illegal (RM 10.5).
```

### ada83.c:4517

```
// A consistent order was produced
```

### ada83.c:4518

```
// Constraint cycle (WITH/Elaborate)
```

### ada83.c:4519

```
// Cycle through an Elaborate_All edge
```

### ada83.c:4526

```
// Graph-unique vertex index
```

### ada83.c:4527

```
// Unit name
```

### ada83.c:4528

```
// Spec, body, or standalone
```

### ada83.c:4529

```
// Corresponding package symbol
```

### ada83.c:4530

```
// SCC component from Tarjan
```

### ada83.c:4531

```
// Unelaborated strong predecessors
```

### ada83.c:4532

```
// Unelaborated weak predecessors
```

### ada83.c:4533

```
// Already placed in final order
```

### ada83.c:4534

```
// Pragma Preelaborate
```

### ada83.c:4535

```
// Pragma Pure
```

### ada83.c:4536

```
// Has elaboration code
```

### ada83.c:4537

```
// Standard library unit
```

### ada83.c:4538

```
// Compiler-generated unit
```

### ada83.c:4539

```
// Needs an elab call in main
```

### ada83.c:4540

```
// Paired body (from a spec)
```

### ada83.c:4541

```
// Paired spec (from a body)
```

### ada83.c:4542

```
// Head of predecessor edge list
```

### ada83.c:4543

```
// Head of successor edge list
```

### ada83.c:4544

```
// Tarjan discovery index
```

### ada83.c:4545

```
// Tarjan lowlink value
```

### ada83.c:4546

```
// Currently on Tarjan stack
```

### ada83.c:4550

```
// Graph-unique edge index
```

### ada83.c:4551

```
// Dependency kind
```

### ada83.c:4552

```
// Strong edges block elaboration
```

### ada83.c:4553

```
// Source vertex
```

### ada83.c:4554

```
// Target vertex
```

### ada83.c:4555

```
// Next edge in pred list
```

### ada83.c:4556

```
// Next edge in succ list
```

### ada83.c:4559

```
// ???
```

### ada83.c:4562

```
// The full elaboration dependency graph. Vertices are compilation units,
```

### ada83.c:4563

```
// edges are WITH / ELABORATE / ELABORATE_ALL / invocation dependencies.
```

### ada83.c:4565

```
// All compilation units
```

### ada83.c:4566

```
// Number of vertices
```

### ada83.c:4567

```
// All dependency edges
```

### ada83.c:4568

```
// Number of edges
```

### ada83.c:4569

```
// Strong predecessors per SCC
```

### ada83.c:4570

```
// Weak predecessors per SCC
```

### ada83.c:4571

```
// Number of SCCs found by Tarjan
```

### ada83.c:4572

```
// Final topological order
```

### ada83.c:4573

```
// Length of the order array
```

### ada83.c:4574

```
// True if ELABORATE_ALL cycle exists
```

### ada83.c:4577

```
// Mutable state for Tarjan's strongly-connected-components algorithm.
```

### ada83.c:4579

```
// DFS stack of vertex indices
```

### ada83.c:4580

```
// Current stack depth
```

### ada83.c:4581

```
// Next DFS discovery index
```

### ada83.c:4632

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4633

```
//
```

### ada83.c:4634

```
// §16. Generics - Tree-copy instantiation of generic units
```

### ada83.c:4635

```
//
```

### ada83.c:4636

```
// An instance is a copy of the generic unit (RM 12.3): the template AST is cloned structurally,
```

### ada83.c:4637

```
// each formal is bound to its actual as an ordinary symbol or declaration of the instance's own
```

### ada83.c:4638

```
// scope, and the clone is resolved under the declaration-point visibility of the template
```

### ada83.c:4639

```
// (creation-sequence cutoffs) and emitted through the ordinary subprogram/package paths. Every
```

### ada83.c:4640

```
// instantiation gets its own symbols, its own types, its own storage, and its own emitted IR.
```

### ada83.c:4641

```
//
```

### ada83.c:4642

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4644

```
// Pure structural deep copy over the §8 shape table: no substitution, and
```

### ada83.c:4645

```
// every semantic decoration (symbols, types, fold cache, apply verdicts,
```

### ada83.c:4646

```
// master state) reset so re-resolution owns the clone outright. This is the
```

### ada83.c:4647

```
// instantiation cloner: an instance is a copy of the generic unit (RM 12.3)
```

### ada83.c:4648

```
// whose meaning re-derives in its own scope.
```

### ada83.c:4651

```
// Structural deep copy that KEEPS the template's semantic decorations.
```

### ada83.c:4652

```
// Used for generic formal-part default expressions, whose names were bound
```

### ada83.c:4653

```
// at the generic declaration with that point's visibility (RM 12.1(13)) --
```

### ada83.c:4654

```
// re-resolving them at instantiation time would see declarations added to
```

### ada83.c:4655

```
// the same scopes afterwards. Formal references inside the copy are then
```

### ada83.c:4656

```
// remapped to the instance's bindings via Reassign_Symbol_References.
```

### ada83.c:4659

```
// Replace every reference to `from` with `to` throughout a decorated clone
```

### ada83.c:4660

```
// (the formal-to-binding remap of RM 12.3 rules (b)/(c)/(d)).
```

### ada83.c:4663

```
// ???
```

### ada83.c:4665

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4666

```
//
```

### ada83.c:4667

```
// §17. File Loading - Include-path search, source file I/O
```

### ada83.c:4668

```
//
```

### ada83.c:4669

```
// WITH clauses name packages that must be found on disk, loaded, parsed, analysed, and
```

### ada83.c:4670

```
// code-generated before the withing unit can proceed. Include_Paths lists the directories to
```

### ada83.c:4671

```
// search; Lookup_Path maps a unit name to a file path. Loading_Set detects circular WITH
```

### ada83.c:4672

```
// dependencies by tracking which units are currently being loaded.
```

### ada83.c:4673

```
//
```

### ada83.c:4674

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4676

```
// Tracks which packages are currently being loaded to detect circular WITH chains.
```

### ada83.c:4678

```
// Unit names currently being loaded (arena-grown)
```

### ada83.c:4679

```
// Number of names in the set
```

### ada83.c:4680

```
// Allocated slots
```

### ada83.c:4686

```
// The implementation's predefined-library directory (<exe_dir>/rts) and
```

### ada83.c:4687

```
// whether the most recent Lookup_Path resolution came from it. Predefined
```

### ada83.c:4688

```
// units' bodies are provided by the runtime itself (RM Appendix C) — the
```

### ada83.c:4689

```
// library-completeness checks must not demand compiled bodies for them.
```

### ada83.c:4696

```
// arena-grown, no cap
```

### ada83.c:4698

```
// Package bodies present in the CURRENT compilation's unit list — recorded
```

### ada83.c:4699

```
// before code generation so the spec-only elaboration decision can ask
```

### ada83.c:4700

```
// "does a body I will emit host these initializers?" (a body merely KNOWN
```

### ada83.c:4701

```
// to the library is not enough: it may never be linked in — spprt13).
```

### ada83.c:4707

```
// RM 10.3/10.5 library completeness, consulted when compiling a main
```

### ada83.c:4708

```
// program (this compiler's bind point). A WITH'd package whose spec
```

### ada83.c:4709

```
// requires a body the library has not compiled gets its elaboration
```

### ada83.c:4710

```
// call emitted against an external definition; a WITH'd library
```

### ada83.c:4711

```
// subprogram in the same position gets a presence anchor in @main.
```

### ada83.c:4712

```
// Either way a later compilation may supply the body — and execution
```

### ada83.c:4713

```
// is rejected when none does.
```

### ada83.c:4733

```
// Recursively load the spec of every package named in a compilation unit
```

### ada83.c:4734

```
// context's WITH clauses (RM 10.1.1). NULL context is a no-op.
```

### ada83.c:4736

```
// Resolve a compilation unit context's USE clauses (RM 8.4) so the WITH'd
```

### ada83.c:4737

```
// packages' contents are directly visible. NULL context is a no-op.
```

### ada83.c:4742

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4743

```
//
```

### ada83.c:4744

```
// §18. Vector Optimizations - SIMD-accelerated scanning on x86-64 and ARM64
```

### ada83.c:4745

```
//
```

### ada83.c:4746

```
// Vectorised scanning primitives for whitespace skipping, identifier recognition, digit scanning,
```

### ada83.c:4747

```
// and single-character search. Three implementations are selected at compile time by the platform
```

### ada83.c:4748

```
// detection macros:
```

### ada83.c:4749

```
//
```

### ada83.c:4750

```
//   - x86-64  AVX-512BW (64-byte), AVX2 (32-byte), with scalar tail
```

### ada83.c:4751

```
//   - ARM64   NEON/ASIMD (16-byte), with scalar tail
```

### ada83.c:4752

```
//   - Generic Scalar fallback with unrolled loops
```

### ada83.c:4753

```
//
```

### ada83.c:4754

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4756

```
// Platform Detection
```

### ada83.c:4788

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4789

```
//
```

### ada83.c:4790

```
// §19. Driver - Command-line parsing and top-level orchestration
```

### ada83.c:4791

```
//
```

### ada83.c:4792

```
// The main driver parses command-line arguments, compiles each source file to LLVM IR - optionally
```

### ada83.c:4793

```
// forking a subprocess per file for parallel compilation - and returns an exit status.
```

### ada83.c:4794

```
// Derive_Output_Path maps an input .adb or .ads to the .ll output.
```

### ada83.c:4795

```
//
```

### ada83.c:4796

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4799

```
// Source file to compile
```

### ada83.c:4800

```
// NULL means derive from input
```

### ada83.c:4801

```
// Zero for success, one for failure
```

### ada83.c:4809

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4810

```
// §20. Native Backend - In-process IR -> executable via runtime-loaded LLVM-C
```

### ada83.c:4811

```
//
```

### ada83.c:4812

```
// `--native -o prog` finishes the pipeline inside the compiler: the emitted IR is parsed,
```

### ada83.c:4813

```
// optimized (default<O2>), and lowered to a host object file through LLVM's C API — with ZERO
```

### ada83.c:4814

```
// build-time LLVM dependency. LLVM-C is a stable C ABI, so the entry points are declared here
```

### ada83.c:4815

```
// as function-pointer types and resolved with dlopen/dlsym at RUN time from whatever libLLVM
```

### ada83.c:4816

```
// the host has: the distro package on Linux, Homebrew or Xcode's on macOS, or an LLVM-C.dll
```

### ada83.c:4817

```
// dropped next to ada83.exe on Windows (LoadLibrary searches the executable's directory
```

### ada83.c:4818

```
// first — vendoring the DLL in the distribution is the whole install story).
```

### ada83.c:4819

```
// ADA83_LLVM_LIB overrides the probe. The final step — linking one object file — is delegated
```

### ada83.c:4820

```
// to the first system C compiler found ($CC, cc, clang, gcc), because that is the one tool
```

### ada83.c:4821

```
// every development machine already has and the only part of LLVM with no stable C API (lld).
```

### ada83.c:4822

```
//
```

### ada83.c:4823

```
// The compiler still emits textual .ll by default; --native is additive.
```

### ada83.c:4824

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4832

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4833

```
//
```

### ada83.c:4834

```
//
```

### ada83.c:4835

```
//                                           B O D Y                
```

### ada83.c:4836

```
//                 
```

### ada83.c:4837

```
//
```

### ada83.c:4838

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4840

```
// Note: Sections §1 through §2 exist only in the specification section
```

### ada83.c:4842

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4843

```
// §3. MEMORY - Bump allocation for the compilation session                                         
```

### ada83.c:4844

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4874

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4875

```
// §4. TEXT - Non-owning string views                                                               
```

### ada83.c:4876

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4895

```
// FNV-1a hash with case folding, used for case-insensitive symbol-table lookup. The offset
```

### ada83.c:4896

```
// basis and prime are the standard 64-bit FNV parameters from Fowler/Noll/Vo.
```

### ada83.c:4904

```
// Levenshtein edit distance for "did you mean …?" diagnostic suggestions. The O(n*m) dynamic-
```

### ada83.c:4905

```
// programming table is stack-allocated; identifiers longer than 20 characters bail out early.
```

### ada83.c:4925

```
// Suggest at distance 1–2, and never more than one edit for a short name
```

### ada83.c:4926

```
// (a 3-character name may differ by one; 4+ characters by two).
```

### ada83.c:4951

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4952

```
// §5.2 Error Handling - Accumulating diagnostic reports
```

### ada83.c:4953

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4955

```
// A tentative resolution pass — probing an alternative interpretation for
```

### ada83.c:4956

```
// legality without committing to it — sets this so its expected failures
```

### ada83.c:4957

```
// never reach the user or the error count.
```

### ada83.c:4960

```
// Active RESTRICT_* bits (pragma RESTRICTIONS / PROFILE (RAVENSCAR)):
```

### ada83.c:4961

```
// resolution rejects the restricted constructs while set.
```

### ada83.c:4980

```
// Located, non-fatal warning. Does not affect Error_Count or compilation — used
```

### ada83.c:4981

```
// e.g. when a runtime check is statically known to fail (RM permits compiling
```

### ada83.c:4982

```
// the program and raising at run time, but a quality compiler flags it).
```

### ada83.c:4998

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:4999

```
// §6. Big Integer - Arbitrary-precision integers and reals for Ada literal values                  
```

### ada83.c:5000

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:5018

```
// Remove leading zero limbs and canonicalise the sign (zero is always non-negative).
```

### ada83.c:5025

```
// Multiply the integer in place by a single-word factor and add a single-word addend. This is
```

### ada83.c:5026

```
// the core loop for building a big integer from a digit string: integer = integer * base + digit.
```

### ada83.c:5040

```
// If the big integer fits in a signed 64-bit value, store it in *out and return true.
```

### ada83.c:5055

```
// Extract a Big_Integer as an unsigned 128-bit value. Returns true when the magnitude fits in
```

### ada83.c:5056

```
// uint128_t (0 .. 2**128 - 1), i.e. the integer has at most two limbs and is non-negative.
```

### ada83.c:5068

```
// Extract a Big_Integer as a signed 128-bit value. Returns true when the value fits in the
```

### ada83.c:5069

```
// int128_t range (-2**127 .. 2**127 - 1).
```

### ada83.c:5079

```
// The most negative 128-bit signed value is -(2**127).
```

### ada83.c:5090

```
// Sign-aware comparison of two big integers. Returns -1, 0, or +1 following the convention
```

### ada83.c:5091

```
// of strcmp: negative means left < right.
```

### ada83.c:5102

```
// Both have the same sign - compare magnitudes limb by limb from the most significant.
```

### ada83.c:5118

```
// Sign-aware addition of two big integers. Returns a freshly allocated result.
```

### ada83.c:5123

```
// When both operands have the same sign, add the magnitudes and keep the sign.
```

### ada83.c:5137

```
// Different signs: subtract the smaller magnitude from the larger.
```

### ada83.c:5142

```
// Compare magnitudes (ignoring sign) to decide which is larger.
```

### ada83.c:5170

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:5171

```
// §6.1 BIG_REAL - Arbitrary-precision real numbers for Ada literals                                
```

### ada83.c:5172

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:5181

```
// Parse a real literal string into an arbitrary-precision Big_Real. Handles forms like 3.14,
```

### ada83.c:5182

```
// 3.14E-10, and 3.14159_26535_89793_23846_26433_83279 (underscore-separated digit groups).
```

### ada83.c:5188

```
// Collect all significant digits, stripping the decimal point and underscores.
```

### ada83.c:5205

```
// Parse the optional exponent part (E+nnn or E-nnn).
```

### ada83.c:5219

```
// Compute the combined exponent. If the decimal appeared at position 3 within six digits
```

### ada83.c:5220

```
// (e.g. "3.14159"), the implicit exponent is 3 - 6 = -3; add the explicit exponent.
```

### ada83.c:5226

```
// Build the significand from the cleaned digit string.
```

### ada83.c:5231

```
// Approximate a Big_Real as an IEEE 754 double. Precision loss is expected for significands
```

### ada83.c:5232

```
// that exceed 53 bits; the result is the nearest representable double value.
```

### ada83.c:5236

```
// Reconstruct the significand as a double from limbs (most-significant first).
```

### ada83.c:5242

```
// Scale by 10**exponent.
```

### ada83.c:5251

```
// Return a deep copy of a Big_Integer, allocated from the arena.
```

### ada83.c:5261

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:5262

```
// §6.2 Exact Rational Arithmetic for Universal Reals (RM §4.10)                                    
```

### ada83.c:5263

```
//                                                                                                  
```

### ada83.c:5264

```
// Ada requires that static universal_real expressions be evaluated exactly during compilation.    
```

### ada83.c:5265

```
// IEEE double cannot faithfully represent fractions like 1/3, so we carry each value as a          
```

### ada83.c:5266

```
// numerator/denominator pair of Big_Integers, always reduced by their GCD.                        
```

### ada83.c:5267

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:5269

```
// Full-precision multiplication of two Big_Integers using textbook O(n*m) arithmetic.
```

### ada83.c:5295

```
// Unsigned division with remainder: quotient = |dividend| / |divisor|, remainder = |dividend|      
```

### ada83.c:5296

```
// mod |divisor|. Signs are ignored. Implements Knuth's Algorithm D for multi-limb divisors       
```

### ada83.c:5297

```
// and a fast path for single-limb divisors.                                                       
```

### ada83.c:5298

```
//                                                                                                  
```

### ada83.c:5312

```
// Single-limb divisor: simple linear pass.
```

### ada83.c:5331

```
// Multi-limb: Knuth Algorithm D. First normalise so that the top bit of the divisor's most
```

### ada83.c:5332

```
// significant limb is set (this guarantees the trial-quotient estimate is within 2).
```

### ada83.c:5337

```
// Create shifted copies of dividend (into u) and divisor (into v).
```

### ada83.c:5363

```
// Main loop: estimate each quotient digit and subtract.
```

### ada83.c:5370

```
// Refine the trial quotient using the next limb of the divisor.
```

### ada83.c:5380

```
// Multiply and subtract: u[j .. j+n] -= q_hat * v[0 .. n-1].
```

### ada83.c:5392

```
// If the subtraction went negative, add back one copy of the divisor.
```

### ada83.c:5406

```
// The remainder is in u, but still shifted - undo the normalisation.
```

### ada83.c:5421

```
// Greatest common divisor via the Euclidean algorithm (magnitudes only, signs ignored).
```

### ada83.c:5440

```
// Reduce a rational by dividing both numerator and denominator by their GCD. The denominator
```

### ada83.c:5441

```
// is always made positive (the sign lives on the numerator).
```

### ada83.c:5442

```
// Push a rational's sign onto the numerator, leaving the denominator positive.
```

### ada83.c:5485

```
// left + right  =  (left.n * right.d + right.n * left.d) / (left.d * right.d)
```

### ada83.c:5505

```
// left / right  =  left.n * right.d  /  (left.d * right.n)
```

### ada83.c:5523

```
// For a negative exponent, invert the result by swapping numerator and denominator.
```

### ada83.c:5538

```
// Approximate a Rational as an IEEE 754 double by converting numerator and denominator
```

### ada83.c:5539

```
// independently and dividing. Precision loss is inherent for large values.
```

### ada83.c:5554

```
// §7–§7.2: Token_Name, Keywords, Lookup_Keyword, Lexer_New, Lexer_Peek,
```

### ada83.c:5555

```
// Lexer_Advance, and Make_Token are defined earlier in §7 (/ static const).
```

### ada83.c:5556

```
// Lexer_Skip_Whitespace_And_Comments remains here because it depends on SIMD functions.
```

### ada83.c:5559

```
// Use SIMD to find first non-whitespace, then check for Ada comments (-- to end of line)
```

### ada83.c:5563

```
// Update line/column by scanning for newlines in the skipped region
```

### ada83.c:5570

```
// Ada comment: -- to end of line
```

### ada83.c:5574

```
// Use SIMD to find newline
```

### ada83.c:5582

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:5583

```
// §7.3 Scanning Functions - Each scanner consumes one token and returns it
```

### ada83.c:5584

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:5586

```
// Current source position of the lexer as a Source_Location.
```

### ada83.c:5602

```
// Parse digit value in any base up to 16; returns -1 for non-hex characters
```

### ada83.c:5609

```
// Scan an exponent: the 'E', optional sign, then digit/underscore run. The 'e'
```

### ada83.c:5610

```
// itself is already detected by the caller.
```

### ada83.c:5627

```
// Scan integer part (possibly base specifier)
```

### ada83.c:5631

```
// Based literal: 16#FFFF# or 2#1010#
```

### ada83.c:5637

```
// Parse base from what we've scanned so far
```

### ada83.c:5643

```
// consume # or :
```

### ada83.c:5645

```
// Scan mantissa
```

### ada83.c:5660

```
// Decimal literal with optional fraction and exponent
```

### ada83.c:5671

```
// exponent alone doesn't make it real: 12E1 is 120
```

### ada83.c:5679

```
// Convert to value: strip underscores and base delimiters, then parse
```

### ada83.c:5687

```
// Parse into Big_Real for arbitrary precision
```

### ada83.c:5692

```
// Based real — exact value as a Rational: the whole mantissa (integer
```

### ada83.c:5693

```
// and fraction digits together) accumulates into one big integer M
```

### ada83.c:5694

```
// with F fraction digits, giving M / base^F, then base^±E applies the
```

### ada83.c:5695

```
// exponent. No floating-point arithmetic touches the value; the
```

### ada83.c:5696

```
// machine double is a final, single rounding of the exact rational.
```

### ada83.c:5722

```
// value = mantissa / base^(F - E); a negative power scales the
```

### ada83.c:5723

```
// numerator instead.
```

### ada83.c:5744

```
// Decimal integer with exponent (e.g., 12E1 = 120). Exact through
```

### ada83.c:5745

```
// Big_Integer — int64 accumulation silently wrapped for large
```

### ada83.c:5746

```
// mantissas or exponents. RM 2.4.1: an integer literal's exponent
```

### ada83.c:5747

```
// must not have a minus sign.
```

### ada83.c:5777

```
// Based integer: parse from original string (e.g., 16#E#E1 = 14*16 =
```

### ada83.c:5778

```
// 224). Same exact Big_Integer accumulation as the decimal path.
```

### ada83.c:5782

```
// 0 = base, 1 = mantissa, 2 = exponent
```

### ada83.c:5788

```
// Skip base part - already parsed
```

### ada83.c:5791

```
// Mantissa in given base
```

### ada83.c:5798

```
// Exponent (always decimal, after second delimiter)
```

### ada83.c:5822

```
// consume opening '
```

### ada83.c:5828

```
// consume closing '
```

### ada83.c:5834

```
// Double an arena string buffer when it is about to fill (room for one more
```

### ada83.c:5835

```
// char plus the terminator).
```

### ada83.c:5847

```
// consume opening " or %
```

### ada83.c:5854

```
// Doubled delimiter => literal delimiter character (Ada RM §2.6)
```

### ada83.c:5861

```
// consume closing delimiter
```

### ada83.c:5874

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:5875

```
// §7.4 Main Lexer Entry Point                                                                         
```

### ada83.c:5876

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:5887

```
// Identifiers and keywords
```

### ada83.c:5890

```
// Numeric literals
```

### ada83.c:5893

```
// Character literal: 'X' where X is any graphic character.                                      
```

### ada83.c:5894

```
// Special case: ''' is a character literal containing single quote.                             
```

### ada83.c:5895

```
// Context: after identifier or RPAREN, '( is tick+lparen (qualified expression), not char lit.  
```

### ada83.c:5896

```
//                                                                                                
```

### ada83.c:5905

```
// Not a character literal - fall through to tick handling
```

### ada83.c:5912

```
// String literal - both " and % delimiters (RM §2.6, Ada 83)
```

### ada83.c:5915

```
// Operators and delimiters
```

### ada83.c:5961

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:5962

```
// §8. ABSTRACT SYNTAX TREE - Parse Tree Representation                                             
```

### ada83.c:5963

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:5977

```
// Node constructor - zero-initialises the union so that all pointers start as NULL
```

### ada83.c:5986

```
// An `expr => ...` association unwraps to its expression; anything else is
```

### ada83.c:5987

```
// itself. NULL passes through.
```

### ada83.c:5992

```
// An explicit dereference, PREFIX.ALL (RM 4.1.3): a unary operator node
```

### ada83.c:5993

```
// whose operator token is ALL.
```

### ada83.c:5998

```
// The explicit range of a constrained subtype indication - the NK_RANGE node
```

### ada83.c:5999

```
// of `S RANGE L .. H` - or NULL when the node is not a subtype indication,
```

### ada83.c:6000

```
// carries no range constraint, or the constraint's range is a range
```

### ada83.c:6001

```
// attribute rather than an explicit pair of bounds (RM 3.5).
```

### ada83.c:6010

```
// The syntax tree's child-edge table, generated from SYNTAX_TREE_SHAPE_LIST
```

### ada83.c:6011

```
// (§8 spec). KID names a single child node pointer, KIDS a child node list.
```

### ada83.c:6027

```
// The per-attribute property table, generated from ATTRIBUTE_KIND_LIST
```

### ada83.c:6028

```
// (§8 spec). The ATTRIBUTE_UNKNOWN row stays zeroed: an empty designator
```

### ada83.c:6029

```
// that matches nothing, with the type-view flag off.
```

### ada83.c:6037

```
// Linear scan over the fifty-one designators, matching Lookup_Keyword's
```

### ada83.c:6038

```
// pattern. Attribute references are rare enough in source text that a scan
```

### ada83.c:6039

```
// beats maintaining a second hash structure.
```

### ada83.c:6048

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6049

```
// §9. PARSER - Recursive Descent with Unified Postfix Handling                                     
```

### ada83.c:6050

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6060

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6061

```
// §9.2 Token Movement                                                                              
```

### ada83.c:6062

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6071

```
// Lookahead: check if the NEXT token (after current) is of the given kind
```

### ada83.c:6076

```
// Update prev_token_kind for context-sensitive lexing during lookahead
```

### ada83.c:6087

```
// Update lexer's prev_token_kind before getting next token (for context-sensitive lexing)
```

### ada83.c:6091

```
// Handle compound keywords: AND THEN, OR ELSE
```

### ada83.c:6112

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6113

```
// §9.3 Error Recovery                                                                              
```

### ada83.c:6114

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6130

```
// Synchronize to a recovery point (statement or declaration boundary)
```

### ada83.c:6148

```
// Check for parser progress - detect stuck parsing loops
```

### ada83.c:6162

```
// Expect a specific token; report error and return false if not found
```

### ada83.c:6172

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6173

```
// §9.4 Identifier Parsing                                                                          
```

### ada83.c:6174

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6186

```
// Check that the END identifier matches the expected name (also handles operator strings)
```

### ada83.c:6200

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6201

```
// §9.13 Subprogram Declarations and Bodies                                                         
```

### ada83.c:6202

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6204

```
// IN copies in, OUT copies out, IN OUT does both. Mode defaults to IN when omitted.  
```

### ada83.c:6205

```
// Parse a comma-separated identifier_list, pushing one NK_IDENTIFIER node per
```

### ada83.c:6206

```
// name. Shared kernel of every declaration form that names several entities
```

### ada83.c:6207

```
// at once (parameters, objects, discriminants, components, generic formals).
```

### ada83.c:6216

```
// Parse a comma-separated list of names. Shared kernel of the USE clause,
```

### ada83.c:6217

```
// the WITH clause, and the ABORT statement.
```

### ada83.c:6231

```
// Mode
```

### ada83.c:6242

```
// Default expression
```

### ada83.c:6251

```
// Build a named/choice-list aggregate association from an already-parsed first
```

### ada83.c:6252

```
// choice: collect `| choice` alternatives, then the `=> expression` if present.
```

### ada83.c:6264

```
// Parse primary: literals, names, aggregates, allocators, parenthesized expressions
```

### ada83.c:6268

```
// Integer literal
```

### ada83.c:6277

```
// Real literal - store both double and Big_Real for arbitrary precision
```

### ada83.c:6287

```
// Character literal - store only the text (e.g. "'X'"), extract char value when needed.         
```

### ada83.c:6288

```
// Do not set integer_lit.value here: it overlaps with string_val.text.data in the union and      
```

### ada83.c:6289

```
// would corrupt the text pointer.                                                               
```

### ada83.c:6290

```
//                                                                                                
```

### ada83.c:6298

```
// String literal - but check for operator symbol used as function name. In Ada, "+"(X, Y)       
```

### ada83.c:6299

```
// is a valid function call where "+" is the operator. If this looks like a short operator       
```

### ada83.c:6300

```
// string followed by (, fall through to Parse_Name which handles operator names.                
```

### ada83.c:6301

```
//                                                                                                
```

### ada83.c:6313

```
// NULL
```

### ada83.c:6316

```
// OTHERS (in aggregates)
```

### ada83.c:6319

```
// NEW allocator
```

### ada83.c:6324

```
// If Parse_Subtype_Indication returned a qualified expression, extract the parts
```

### ada83.c:6334

```
// Unary operators: NOT, ABS, +, -
```

### ada83.c:6345

```
// Parenthesized expression or aggregate
```

### ada83.c:6349

```
// First aggregate choice given as `subtype_mark RANGE low .. high`
```

### ada83.c:6350

```
// (e.g. (DT RANGE L .. R => M)). Parse_Expression stops at RANGE, so
```

### ada83.c:6351

```
// complete the discrete_subtype_indication here, mirroring Parse_Choice.
```

### ada83.c:6368

```
// Check for aggregate indicators
```

### ada83.c:6375

```
// Extension aggregate: (ancestor with components)
```

### ada83.c:6381

```
// First element is a range: expr .. high
```

### ada83.c:6385

```
// consume ..
```

### ada83.c:6388

```
// Check for choice list or named association
```

### ada83.c:6399

```
// First element is part of a choice list
```

### ada83.c:6407

```
// First element is positional, followed by more
```

### ada83.c:6410

```
// consume the comma we know is there
```

### ada83.c:6418

```
// Parentheses make a general expression, removing it from the
```

### ada83.c:6419

```
// applicable-index-constraint contexts of RM 4.3.2(a)-(c): a
```

### ada83.c:6420

```
// parenthesized aggregate ((a,b,c)) or string literal ("...") therefore
```

### ada83.c:6421

```
// takes INDEX_SUBTYPE'FIRST for its lower bound, unlike a direct
```

### ada83.c:6422

```
// sub-aggregate (a,b,c) or a bare literal, which take the applicable
```

### ada83.c:6423

```
// index constraint.
```

### ada83.c:6431

```
// Name (identifier, selected, indexed, etc.)
```

### ada83.c:6435

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6436

```
// §9.6 Unified Postfix Parsing                                                                     
```

### ada83.c:6437

```
//                                                                                                  
```

### ada83.c:6438

```
// Handles: .selector, 'attribute, (arguments) - in one loop.                                      
```

### ada83.c:6439

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6445

```
// Base: identifier or operator symbol
```

### ada83.c:6450

```
// Operator symbol as name: "+" etc
```

### ada83.c:6456

```
// Character literal as name: an enumeration literal is a parameterless
```

### ada83.c:6457

```
// function (RM 6.1), so it stands wherever a function name is required —
```

### ada83.c:6458

```
// a renaming target (RM 8.5(3)) or a generic formal subprogram default
```

### ada83.c:6459

```
// (RM 12.1.3).
```

### ada83.c:6470

```
// Postfix chain
```

### ada83.c:6474

```
// .selector or .ALL
```

### ada83.c:6477

```
// Dereference: prefix.ALL
```

### ada83.c:6484

```
// P.'C' - character literal as enum member (RM 4.1.3)
```

### ada83.c:6493

```
// P."+" - operator symbol (RM 6.1)
```

### ada83.c:6502

```
// Selection: prefix.component
```

### ada83.c:6513

```
// 'attribute or '(qualified)
```

### ada83.c:6516

```
// Qualified expression: Type'(Expr or Aggregate)
```

### ada83.c:6521

```
// Parse expression or aggregate
```

### ada83.c:6526

```
// Aggregate
```

### ada83.c:6529

```
// Range: expr .. high
```

### ada83.c:6552

```
// consume comma
```

### ada83.c:6562

```
// Attribute: prefix'Name or prefix'Name (arg)
```

### ada83.c:6567

```
// Attribute name can be reserved word or identifier
```

### ada83.c:6580

```
// Optional attribute arguments (one or more)
```

### ada83.c:6590

```
// (arguments) - call, index, slice, or type conversion
```

### ada83.c:6604

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6605

```
// §9.6.1 Simple Name Parsing (no parentheses or attributes)                                        
```

### ada83.c:6606

```
//                                                                                                  
```

### ada83.c:6607

```
// Used for generic unit names in instantiations where we don't want                                
```

### ada83.c:6608

```
// parentheses interpreted as function calls.                                                      
```

### ada83.c:6609

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6615

```
// Base: identifier
```

### ada83.c:6624

```
// Only follow dotted selections, not parentheses or ticks
```

### ada83.c:6632

```
// Accept character literal ('C') or operator string ("+") after dot
```

### ada83.c:6647

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6648

```
// §9.7 Unified Association Parsing                                                                 
```

### ada83.c:6649

```
//                                                                                                  
```

### ada83.c:6650

```
// The same syntax serves calls, aggregates, and instantiations. The parser                         
```

### ada83.c:6651

```
// cannot tell them apart; semantic analysis can.                                                  
```

### ada83.c:6652

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6654

```
// Helper to parse a choice (expression, range, or discrete_subtype_indication)
```

### ada83.c:6658

```
// OTHERS choice
```

### ada83.c:6665

```
// Empty list
```

### ada83.c:6670

```
// Check for choice list with | or named association with =>
```

### ada83.c:6674

```
// Positional association
```

### ada83.c:6681

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6682

```
// §9.8 Binary Expression Parsing - Precedence Climbing                                             
```

### ada83.c:6683

```
//                                                                                                  
```

### ada83.c:6684

```
// Climbing starts at low precedence and consumes equal-or-higher before returning.                
```

### ada83.c:6685

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6711

```
// RM 4.5 precedence: a unary adding operator (+ or -) sits at the
```

### ada83.c:6712

```
// simple_expression level — it applies to a whole TERM, binding LOOSER than
```

### ada83.c:6713

```
// the multiplying operators and **. So `- A * B` is `-(A * B)` and
```

### ada83.c:6714

```
// `- A ** B` is `-(A ** B)`, not `(-A) * B` / `(-A) ** B`. For predefined
```

### ada83.c:6715

```
// arithmetic the two groupings give the same number, but with user-defined
```

### ada83.c:6716

```
// operators (or overflow edges) the difference is observable, so parse the
```

### ada83.c:6717

```
// operand as a term rather than a bare primary.
```

### ada83.c:6727

```
// ABS and NOT are at the factor level — they bind tightly to a single primary
```

### ada83.c:6728

```
// (a nested unary), higher than the multiplying operators.
```

### ada83.c:6748

```
// After advance, check for compound keywords (AND THEN, OR ELSE)
```

### ada83.c:6749

```
// that were detected in Parser_Advance
```

### ada83.c:6754

```
// Handle NOT IN specially - mirrors IN range handling below (RM 4.4)
```

### ada83.c:6772

```
// Handle IN with possible range
```

### ada83.c:6776

```
// Check for range: X in A .. B
```

### ada83.c:6791

```
// Standard binary operation
```

### ada83.c:6806

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6807

```
// §9.9 Range Parsing                                                                               
```

### ada83.c:6808

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6810

```
// Shared tail of Parse_Choice and Parse_Range: given an already-parsed first
```

### ada83.c:6811

```
// expression, complete a range (`first .. high`), a discrete subtype
```

### ada83.c:6812

```
// indication (`mark RANGE constraint`, e.g. "INTEGER RANGE 1..10"), or return
```

### ada83.c:6813

```
// the expression itself (a subtype name used as a range).
```

### ada83.c:6824

```
// Check for subtype_mark RANGE low..high (discrete subtype definition)
```

### ada83.c:6829

```
// Now parse the actual range constraint
```

### ada83.c:6838

```
// Just a range attribute or name
```

### ada83.c:6850

```
// BOX: <> for unconstrained
```

### ada83.c:6852

```
// Empty range = unconstrained
```

### ada83.c:6857

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6858

```
// §9.10 Subtype Indication Parsing                                                                 
```

### ada83.c:6859

```
//                                                                                                  
```

### ada83.c:6860

```
// A subtype is a type with a constraint that narrows the range of valid values.                   
```

### ada83.c:6861

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6866

```
// Parse_Name may consume (args) as NK_APPLY - we need to unwrap it for constraints
```

### ada83.c:6869

```
// If Parse_Name returned NK_APPLY, the parenthesized part might be a constraint
```

### ada83.c:6874

```
// Classify: if any item is a named association, it's a discriminant constraint
```

### ada83.c:6884

```
// Create NK_SUBTYPE_INDICATION with appropriate constraint
```

### ada83.c:6900

```
// Check for RANGE constraint
```

### ada83.c:6910

```
// Check for DIGITS constraint (floating-point types)
```

### ada83.c:6917

```
// Optional RANGE constraint after DIGITS
```

### ada83.c:6925

```
// Check for DELTA constraint (fixed-point types)
```

### ada83.c:6932

```
// Optional RANGE constraint after DELTA
```

### ada83.c:6942

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6943

```
// §9.11 Statement Parsing                                                                          
```

### ada83.c:6944

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:6949

```
// Enumeration type
```

### ada83.c:6954

```
// Array type
```

### ada83.c:6959

```
// Record type: RECORD ... END RECORD or NULL RECORD
```

### ada83.c:6964

```
// Null record type: NULL RECORD
```

### ada83.c:6972

```
// Access type
```

### ada83.c:6977

```
// Derived type
```

### ada83.c:6982

```
// Integer types: range, mod
```

### ada83.c:6992

```
// Evaluated during semantic analysis
```

### ada83.c:6997

```
// Real types: digits, delta
```

### ada83.c:7018

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7019

```
// §9.17 Pragmas                                                                                    
```

### ada83.c:7020

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7035

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7036

```
// §9.11.1 Simple Statements                                                                        
```

### ada83.c:7037

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7049

```
// Procedure call (target is already an NK_APPLY or NK_IDENTIFIER)
```

### ada83.c:7051

```
// Reuse field for simplicity
```

### ada83.c:7081

```
// RM 4.1.3: the target may be an expanded name (BLOCK.LABEL). A label is
```

### ada83.c:7082

```
// unique within the enclosing body, so the final selector identifies the
```

### ada83.c:7083

```
// target; the prefixes only name enclosing constructs.
```

### ada83.c:7112

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7113

```
// §9.11.2 If Statement                                                                             
```

### ada83.c:7114

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7124

```
// ELSIF parts
```

### ada83.c:7135

```
// ELSE part
```

### ada83.c:7144

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7145

```
// §9.11.3 Case Statement                                                                           
```

### ada83.c:7146

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7155

```
// Parse alternatives
```

### ada83.c:7161

```
// Parse choices - use Parse_Choice to handle ranges and OTHERS
```

### ada83.c:7167

```
// Statements for this alternative stored as expression temporarily
```

### ada83.c:7178

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7179

```
// §9.11.4 Loop Statement                                                                           
```

### ada83.c:7180

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7187

```
// WHILE loop
```

### ada83.c:7192

```
// FOR loop
```

### ada83.c:7198

```
// Iterator identifier
```

### ada83.c:7205

```
// Discrete range
```

### ada83.c:7219

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7220

```
// §9.11.5 Block Statement                                                                          
```

### ada83.c:7221

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7223

```
// Parse a sequence of `when ... =>` exception handlers (after TK_EXCEPTION) into
```

### ada83.c:7224

```
// `handlers`. Shared by block, subprogram-body, package-body, and task-body.
```

### ada83.c:7260

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7261

```
// §9.11.6 Accept Statement                                                                         
```

### ada83.c:7262

```
//                                                                                                  
```

### ada83.c:7263

```
// ACCEPT is the server side of rendezvous where the caller blocks until accepted.                 
```

### ada83.c:7264

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7272

```
// Optional index and/or parameters                                                               
```

### ada83.c:7273

```
// Need to distinguish:                                                                           
```

### ada83.c:7274

```
// - Entry index: (expression) like (5) or (I)                                                    
```

### ada83.c:7275

```
// - Parameters: (id : type) like (X : INTEGER)                                                   
```

### ada83.c:7276

```
// Lookahead to distinguish index vs parameters                                                   
```

### ada83.c:7277

```
//                                                                                                
```

### ada83.c:7281

```
// consume (
```

### ada83.c:7284

```
// past identifier
```

### ada83.c:7286

```
// If followed by : or ,, it's a parameter list
```

### ada83.c:7290

```
// Restore and parse correctly
```

### ada83.c:7294

```
// This is a parameter list, not an index
```

### ada83.c:7298

```
// Parse entry index (expression)
```

### ada83.c:7300

```
// consume (
```

### ada83.c:7304

```
// Now check for optional parameters after index
```

### ada83.c:7311

```
// Optional body
```

### ada83.c:7319

```
// Everything parsed so far is the rendezvous body. A selective-wait parent
```

### ada83.c:7320

```
// may append trailing statements after this count (run post-rendezvous).
```

### ada83.c:7325

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7326

```
// §9.11.7 Select Statement                                                                         
```

### ada83.c:7327

```
//                                                                                                  
```

### ada83.c:7328

```
// SELECT makes a nondeterministic choice among open alternatives at runtime.                      
```

### ada83.c:7329

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7331

```
// Parse the optional statement sequence trailing a select alternative
```

### ada83.c:7332

```
// (delay/accept/entry-call), appending into `target` with `;` separators and
```

### ada83.c:7333

```
// stopping at OR / ELSE / END / EOF.
```

### ada83.c:7348

```
// Parse alternatives. Each alternative is optionally guarded:                                   
```

### ada83.c:7349

```
//   [WHEN condition =>] accept_stmt ; [stmts]                                                    
```

### ada83.c:7350

```
//   [WHEN condition =>] delay_stmt  ; [stmts]                                                    
```

### ada83.c:7351

```
//   [WHEN condition =>] TERMINATE ;                                                              
```

### ada83.c:7352

```
//   entry_call_stmt ; [stmts]           (timed/conditional)                                      
```

### ada83.c:7353

```
//                                                                                                
```

### ada83.c:7357

```
// Optional guard (WHEN condition =>). Retained and attached to ACCEPT
```

### ada83.c:7358

```
// and DELAY alternatives so codegen can treat a false guard as a closed
```

### ada83.c:7359

```
// alternative (RM 9.7.1). Guards on terminate/entry-call alternatives
```

### ada83.c:7360

```
// are not yet enforced.
```

### ada83.c:7378

```
// Optional statement sequence after delay
```

### ada83.c:7386

```
// Optional sequence of statements after accept in select. These run
```

### ada83.c:7387

```
// AFTER the rendezvous completes (RM 9.7.1) — record the boundary so
```

### ada83.c:7388

```
// codegen signals the caller before executing them, not after (a caller
```

### ada83.c:7389

```
// that re-calls this task inside the trailing statements would deadlock).
```

### ada83.c:7393

```
// Entry call alternative - conditional or timed entry call
```

### ada83.c:7399

```
// Optional sequence of statements after entry call
```

### ada83.c:7414

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7415

```
// §9.11.8 Statement Dispatch                                                                       
```

### ada83.c:7416

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7421

```
// Check for label(s) (RM 5.1): <<L1>> <<L2>> stmt — EVERY label names this
```

### ada83.c:7422

```
// statement position and is a distinct GOTO target (c59001b), so all are
```

### ada83.c:7423

```
// collected, not just the last. An `identifier :` immediately before a
```

### ada83.c:7424

```
// loop or block is that construct's NAME and is consumed by it instead.
```

### ada83.c:7429

```
// Handle multiple consecutive labels
```

### ada83.c:7438

```
// Lookahead for "identifier :" (label) vs assignment/call
```

### ada83.c:7444

```
// This is a label
```

### ada83.c:7448

```
// Not a label - restore and let assignment/call handle it
```

### ada83.c:7459

```
// Update location to after labels
```

### ada83.c:7463

```
// Null statement
```

### ada83.c:7464

```
// Semicolon is handled by Parse_Statement_Sequence
```

### ada83.c:7469

```
// Compound statements — the nearest preceding label is the loop's/block's
```

### ada83.c:7470

```
// NAME; any earlier labels wrap it as ordinary statement labels below.
```

### ada83.c:7484

```
// Simple statements
```

### ada83.c:7492

```
// Pragma in statement sequence (Ada 83 RM 2.8)
```

### ada83.c:7495

```
// Assignment or procedure call
```

### ada83.c:7498

```
// Wrap the statement in the collected labels, innermost = last written.
```

### ada83.c:7504

```
// Set during resolution
```

### ada83.c:7528

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7529

```
// §9.12 Declaration Parsing                                                                        
```

### ada83.c:7530

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7532

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7533

```
// §9.12.1 Object Declaration (variables, constants)                                                
```

### ada83.c:7534

```
//                                                                                                  
```

### ada83.c:7535

```
// Multiple names can share one type declaration but each gets its own symbol.                     
```

### ada83.c:7536

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7544

```
// Check for exception declaration: identifier_list : EXCEPTION [RENAMES name]
```

### ada83.c:7545

```
// Check for renaming
```

### ada83.c:7557

```
// ALIASED uses ACCESS token?
```

### ada83.c:7560

```
// Named number (number declaration): identifier : CONSTANT := static_expression;                 
```

### ada83.c:7561

```
// No type specified, goes directly to :=                                                         
```

### ada83.c:7562

```
// Check for anonymous array type: ARRAY (...) OF ...                                            
```

### ada83.c:7563

```
//                                                                                                
```

### ada83.c:7572

```
// Renames: X : T RENAMES Y
```

### ada83.c:7579

```
// Initialization
```

### ada83.c:7586

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7587

```
// §9.12.2 Type Declaration                                                                         
```

### ada83.c:7588

```
//                                                                                                  
```

### ada83.c:7589

```
// Discriminants parameterize the type with values fixed when the object is created.               
```

### ada83.c:7590

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7595

```
// Container for discriminants
```

### ada83.c:7616

```
// Discriminant part
```

### ada83.c:7624

```
// Incomplete type declaration
```

### ada83.c:7646

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7647

```
// §9.12.3 Type Definitions                                                                         
```

### ada83.c:7648

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7677

```
// Index types: can be discrete_subtype_indication or discrete_range
```

### ada83.c:7683

```
// Determine if constrained based on what we parsed.                                             
```

### ada83.c:7684

```
// An index is unconstrained if it's just a type mark (identifier/selected)                       
```

### ada83.c:7685

```
// without a range constraint. A range or subtype_indication with constraint                      
```

### ada83.c:7686

```
// means constrained.                                                                            
```

### ada83.c:7687

```
//                                                                                                
```

### ada83.c:7688

```
// Unconstrained iff an index is `subtype_mark RANGE <>` (a box). A bare type
```

### ada83.c:7689

```
// mark (e.g. ARRAY (BOOLEAN)) is CONSTRAINED to that subtype's range (RM 3.6).
```

### ada83.c:7704

```
// Parse discrete_range: can be subtype_indication or range
```

### ada83.c:7708

```
// RM 3.6.1 / 3.5: discrete_range ::= discrete_subtype_indication | range,
```

### ada83.c:7709

```
// and a range's bounds are SIMPLE EXPRESSIONS — `INTEGER'LAST - 4 ..
```

### ada83.c:7710

```
// INTEGER'LAST` and `-4 .. IDENT_INT(4)` are as legal as bare names
```

### ada83.c:7711

```
// (c41106a, c52102b). Parse one expression first; the token that follows
```

### ada83.c:7712

```
// decides which alternative this is (RANGE -> subtype indication,
```

### ada83.c:7713

```
// `..` -> this was the low bound, else a bare type mark).
```

### ada83.c:7716

```
// Type RANGE low..high or Type RANGE <>
```

### ada83.c:7719

```
// Unconstrained - `subtype_mark RANGE <>`. Mark the box so the array-type
```

### ada83.c:7720

```
// parser can tell it apart from a bare type mark (which is CONSTRAINED to
```

### ada83.c:7721

```
// the subtype's range, RM 3.6).
```

### ada83.c:7733

```
// Create subtype indication with range constraint
```

### ada83.c:7742

```
// Name is actually the low bound of a range
```

### ada83.c:7750

```
// Just a type name
```

### ada83.c:7754

```
// Parse the component declarations of a record type or of one variant
```

### ada83.c:7755

```
// alternative: `identifier_list : subtype_indication [:= default];` repeated,
```

### ada83.c:7756

```
// with `null;` accepted as an empty component list. Stops at END, at CASE
```

### ada83.c:7757

```
// (a variant part follows), at EOF, and — inside a variant alternative
```

### ada83.c:7758

```
// (stop_at_when) — at WHEN.
```

### ada83.c:7765

```
// NULL; as empty component list
```

### ada83.c:7789

```
// Variant part
```

### ada83.c:7804

```
// Variants
```

### ada83.c:7810

```
// Choices - can be expressions, ranges, or OTHERS
```

### ada83.c:7817

```
// Nested variant part
```

### ada83.c:7842

```
// Parse_Subtype_Indication may have incorrectly consumed DIGITS/DELTA                            
```

### ada83.c:7843

```
// constraints as part of the parent type. For derived types, these                               
```

### ada83.c:7844

```
// constraints belong to the derived type definition, not the parent.                            
```

### ada83.c:7845

```
// Extract them if present.                                                                      
```

### ada83.c:7846

```
//                                                                                                
```

### ada83.c:7852

```
// Move constraint from parent to derived type
```

### ada83.c:7864

```
// Remove constraint from parent - use just the subtype mark
```

### ada83.c:7869

```
// Handle any remaining accuracy constraint (DIGITS or DELTA).                                   
```

### ada83.c:7870

```
// Ada RM 3.5.7: derived_type_definition ::=                                                      
```

### ada83.c:7871

```
//   new subtype_indication [accuracy_constraint] [range_constraint]                              
```

### ada83.c:7872

```
// accuracy_constraint ::= DIGITS expression | DELTA expression                                   
```

### ada83.c:7873

```
// Parse as NK_REAL_TYPE to reuse real type constraint handling                                   
```

### ada83.c:7874

```
//                                                                                                
```

### ada83.c:7880

```
// Skip DELTA
```

### ada83.c:7893

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7894

```
// §9.13.2 Procedure/Function Specification                                                         
```

### ada83.c:7895

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7897

```
// A subprogram designator is either an operator string ("+") or an identifier.
```

### ada83.c:7923

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7924

```
// §9.13.3 Subprogram Body                                                                          
```

### ada83.c:7925

```
//                                                                                                  
```

### ada83.c:7926

```
// Declarations, then BEGIN, then statements. The structure is invariant.                          
```

### ada83.c:7927

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7936

```
// Check for SEPARATE
```

### ada83.c:7949

```
// Check end name - handle both identifier and operator string
```

### ada83.c:7956

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7957

```
// §9.14 Package Declarations and Bodies                                                            
```

### ada83.c:7958

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:7962

```
// Note: caller must consume TK_PACKAGE before calling
```

### ada83.c:7968

```
// Visible declarations
```

### ada83.c:7971

```
// Private part
```

### ada83.c:7983

```
// Note: caller must consume TK_PACKAGE and TK_BODY before calling
```

### ada83.c:7989

```
// Check for SEPARATE
```

### ada83.c:8008

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:8009

```
// §9.15 Generic Units                                                                              
```

### ada83.c:8010

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:8018

```
// Generic type formal: type T[(discriminants)] is private | type T is (<>) | etc
```

### ada83.c:8022

```
// Parse type name
```

### ada83.c:8025

```
// Parse optional discriminant part: (discriminant_spec {; discriminant_spec})
```

### ada83.c:8026

```
// RM 12.1.2: known discriminants for formal private types.
```

### ada83.c:8035

```
// Parse type definition form
```

### ada83.c:8042

```
// (<>) for discrete types
```

### ada83.c:8048

```
// range <> for integer types
```

### ada83.c:8053

```
// digits <> for float types
```

### ada83.c:8058

```
// delta <> for fixed types
```

### ada83.c:8063

```
// array (index {, index}) of element_type for array types. The
```

### ada83.c:8064

```
// definition is kept as a real NK_ARRAY_TYPE node: the RM 12.3.4
```

### ada83.c:8065

```
// matching and agreement checks need the index marks, the
```

### ada83.c:8066

```
// constrained/unconstrained form, and the component subtype.
```

### ada83.c:8073

```
// Index subtypes: subtype_mark [RANGE <>] (RM 12.1.2: a discrete
```

### ada83.c:8074

```
// range in a formal array type must be a type mark).
```

### ada83.c:8081

```
// <>: unconstrained in this dimension
```

### ada83.c:8085

```
// subtype_mark RANGE low..high (constrained)
```

### ada83.c:8105

```
// access type_name for access types
```

### ada83.c:8110

```
// new parent_type for derived types - skip NEW, parse parent
```

### ada83.c:8116

```
// Unknown form - error recovery: skip to semicolon
```

### ada83.c:8129

```
// Generic object formal: identifier_list : [mode] type [:= default]
```

### ada83.c:8135

```
// Parse mode: IN (default), OUT, or IN OUT
```

### ada83.c:8147

```
// Parse subtype mark
```

### ada83.c:8150

```
// Parse optional default expression
```

### ada83.c:8159

```
// Generic subprogram formal: WITH PROCEDURE/FUNCTION spec [IS name | IS <>]
```

### ada83.c:8161

```
// consume WITH
```

### ada83.c:8167

```
// Optional parameters
```

### ada83.c:8174

```
// Function name - can be identifier or operator string
```

### ada83.c:8182

```
// Optional parameters
```

### ada83.c:8187

```
// Return type
```

### ada83.c:8192

```
// Optional default: IS name | IS <>
```

### ada83.c:8195

```
// IS <> means any matching subprogram
```

### ada83.c:8199

```
// IS name means default to that subprogram
```

### ada83.c:8217

```
// The actual unit
```

### ada83.c:8223

```
// consume PACKAGE
```

### ada83.c:8231

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:8232

```
// §9.16 Use and With Clauses                                                                       
```

### ada83.c:8233

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:8250

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:8251

```
// §9.19 Representation Clauses                                                                     
```

### ada83.c:8252

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:8259

```
// Parse: FOR entity_name'attribute USE expression;                                               
```

### ada83.c:8260

```
//    or: FOR type_name USE RECORD ... END RECORD;                                                
```

### ada83.c:8261

```
//    or: FOR type_name USE (enum_rep_list);                                                      
```

### ada83.c:8262

```
//    or: FOR object_name USE AT address;                                                         
```

### ada83.c:8263

```
//                                                                                                
```

### ada83.c:8265

```
// Parse entity name (possibly qualified: T or T'ATTRIBUTE).
```

### ada83.c:8266

```
// Parse_Name may consume the tick+attribute, producing NK_ATTRIBUTE.
```

### ada83.c:8269

```
// If Parse_Name already consumed T'SIZE, decompose the NK_ATTRIBUTE
```

### ada83.c:8270

```
// so rep_clause.entity_name = T (the prefix) and .attribute = SIZE
```

### ada83.c:8278

```
// Fallback: tick not consumed by Parse_Name
```

### ada83.c:8290

```
// Check for different representation clause forms
```

### ada83.c:8291

```
// Record representation clause: FOR T USE RECORD ... END RECORD;
```

### ada83.c:8295

```
// Parse optional alignment: AT MOD alignment;
```

### ada83.c:8302

```
// Parse component clauses: component_name AT position RANGE first_bit..last_bit;
```

### ada83.c:8304

```
// component_name AT byte_position RANGE first_bit .. last_bit. The name
```

### ada83.c:8305

```
// is choices[0] and the position is the association expression; the
```

### ada83.c:8306

```
// optional bit range (RM 13.4) is kept as choices[1] so resolution can
```

### ada83.c:8307

```
// set the component's first bit alongside its byte offset.
```

### ada83.c:8320

```
// Enumeration representation: FOR T USE (A => 0, B => 1, ...);
```

### ada83.c:8323

```
// consume (
```

### ada83.c:8327

```
// Address clause: FOR X USE AT address;
```

### ada83.c:8332

```
// Attribute value: FOR T'SIZE USE 32;
```

### ada83.c:8339

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:8340

```
// §9.20 Declaration Dispatch                                                                       
```

### ada83.c:8341

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:8346

```
// Generic
```

### ada83.c:8353

```
// Procedure/Function - could be spec, body, or generic instantiation
```

### ada83.c:8356

```
// consume PROCEDURE/FUNCTION
```

### ada83.c:8358

```
// Get the name - can be identifier or operator string for functions
```

### ada83.c:8367

```
// Check for generic instantiation: NAME IS NEW
```

### ada83.c:8368

```
// Peek ahead to see if it's IS NEW
```

### ada83.c:8372

```
// consume IS
```

### ada83.c:8374

```
// consume NEW
```

### ada83.c:8376

```
// Create generic instantiation node
```

### ada83.c:8381

```
// Parse the generic unit name
```

### ada83.c:8384

```
// Generic actuals
```

### ada83.c:8393

```
// Not IS NEW - restore and parse as spec/body
```

### ada83.c:8398

```
// Parse parameters (if any) - Parse_Parameter_List handles the parens
```

### ada83.c:8404

```
// Create the spec node
```

### ada83.c:8409

```
// For functions, parse return type
```

### ada83.c:8415

```
// Check for subprogram renaming: PROCEDURE P RENAMES Q; or FUNCTION F RENAMES G;
```

### ada83.c:8423

```
// Check for body or just spec
```

### ada83.c:8430

```
// Just a specification
```

### ada83.c:8435

```
// Package
```

### ada83.c:8437

```
// consume PACKAGE
```

### ada83.c:8439

```
// consume BODY
```

### ada83.c:8445

```
// Check for package renaming: PACKAGE name RENAMES old_name;
```

### ada83.c:8456

```
// Check for generic instantiation: PACKAGE name IS NEW generic_name
```

### ada83.c:8462

```
// Parse the generic unit name
```

### ada83.c:8465

```
// Generic actuals
```

### ada83.c:8474

```
// Not a generic instantiation - parse as specification
```

### ada83.c:8478

```
// Parse visible declarations (each declaration consumes its own semicolon)
```

### ada83.c:8485

```
// Parse private declarations
```

### ada83.c:8501

```
// Task declaration
```

### ada83.c:8503

```
// consume TASK
```

### ada83.c:8505

```
// TASK BODY name IS ...
```

### ada83.c:8507

```
// consume BODY
```

### ada83.c:8531

```
// TASK [TYPE] name [IS ... END name];
```

### ada83.c:8538

```
// Task spec with entries
```

### ada83.c:8547

```
// Entry may have family index: ENTRY name(index)                                         
```

### ada83.c:8548

```
// and/or parameters: ENTRY name(...) or ENTRY name(index)(...)                           
```

### ada83.c:8549

```
// Family index is a discrete_subtype_definition (like 1..10)                             
```

### ada83.c:8550

```
// Parameters start with identifier : mode type                                           
```

### ada83.c:8551

```
// Check if this is an entry family index or parameter list                               
```

### ada83.c:8552

```
// Entry family: (discrete_range) like (1..10) or (T'RANGE)                               
```

### ada83.c:8553

```
// Parameters: (id : mode type) - starts with identifier followed by :                    
```

### ada83.c:8554

```
//                                                                                        
```

### ada83.c:8558

```
// consume ( for lookahead
```

### ada83.c:8561

```
// Not starting with identifier - must be family index
```

### ada83.c:8565

```
// Look ahead to see if it's id : (parameter) or just id (family)
```

### ada83.c:8569

```
// past identifier
```

### ada83.c:8575

```
// Parse discrete subtype definition - already past (
```

### ada83.c:8581

```
// Optionally parse parameters after family index
```

### ada83.c:8586

```
// Restore and use Parse_Parameter_List which handles ( )
```

### ada83.c:8599

```
// Representation clause in task spec
```

### ada83.c:8617

```
// Type declaration
```

### ada83.c:8621

```
// Incomplete type declaration (no definition, not private/limited) already consumed semicolon
```

### ada83.c:8629

```
// Subtype declaration
```

### ada83.c:8636

```
// Use clause
```

### ada83.c:8643

```
// Pragma
```

### ada83.c:8650

```
// FOR representation clause
```

### ada83.c:8657

```
// Object or exception declaration
```

### ada83.c:8674

```
// Each declaration now consumes its own trailing semicolon
```

### ada83.c:8678

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:8679

```
// §9.21 Compilation Unit                                                                           
```

### ada83.c:8680

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:8694

```
// Context pragmas (Elaborate, Elaborate_All, ...) are kept: the
```

### ada83.c:8695

```
// elaboration graph (§15) consumes them as ordering edges.
```

### ada83.c:8707

```
// Handle trailing pragmas at end of file (no more library units)
```

### ada83.c:8713

```
// Separate unit
```

### ada83.c:8719

```
// Parse the actual subunit below
```

### ada83.c:8722

```
// Main unit - Parse_Declaration now consumes its trailing semicolon
```

### ada83.c:8727

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:8728

```
// §10. TYPE SYSTEM - Ada Type Semantics                                                            
```

### ada83.c:8729

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:8731

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:8732

```
// §10.2.1 Frozen Composite Types List                                                              
```

### ada83.c:8733

```
//                                                                                                  
```

### ada83.c:8734

```
// Track composite types that need implicit equality operators.                                    
```

### ada83.c:8735

```
// These are added during Freeze_Type and processed during code generation.                        
```

### ada83.c:8736

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:8738

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:8739

```
// §10.3 Type Construction                                                                          
```

### ada83.c:8740

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:8749

```
// A task object is its TCB pointer (rep LL_REP_PTR): pointer-sized, so
```

### ada83.c:8750

```
// arrays of tasks and records with task components lay out correctly. A task
```

### ada83.c:8751

```
// type is limited (RM 9.2) — record that in the flag so every reader sees the
```

### ada83.c:8752

```
// truth, rather than special-casing the kind at each site.
```

### ada83.c:8761

```
// RM 3.2.1: a component declared with several names (A1, A2 : SUBTYPE) is a
```

### ada83.c:8762

```
// series of single declarations; if SUBTYPE carries a NON-STATIC constraint it
```

### ada83.c:8763

```
// is elaborated independently for each name. Replicate such a subtype so each
```

### ada83.c:8764

```
// name owns its constraint — a distinct runtime descriptor (dynamic array or
```

### ada83.c:8765

```
// scalar) or an independently frozen discriminant constraint (a discriminated
```

### ada83.c:8766

```
// record component, or an access-to-discriminated-record component). The marked
```

### ada83.c:8767

```
// copies are anonymous and local, so their per-component freezing may use
```

### ada83.c:8768

```
// function-local temporaries safely. Returns the original type unchanged when
```

### ada83.c:8769

```
// no per-name replication is required.
```

### ada83.c:8789

```
// Each copy freezes its discriminant constraint independently; clearing the
```

### ada83.c:8790

```
// shared pre-eval slot makes the ordered per-component freezing pass build a
```

### ada83.c:8791

```
// fresh one for this name.
```

### ada83.c:8803

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:8804

```
// §10.4 Type Predicates                                                                            
```

### ada83.c:8805

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:8825

```
// Type uses floating-point LLVM representation (for codegen, not semantic analysis).
```

### ada83.c:8826

```
// Fixed-point types are NOT included as they use integer representation.
```

### ada83.c:8841

```
// The designated type of an access type (RM 3.8), or NULL when the type is
```

### ada83.c:8842

```
// not an access type.
```

### ada83.c:8847

```
// Per GNAT sem_util.ads - systematic predicates for every type class
```

### ada83.c:8851

```
// Does the type contain a task — itself, or any component, recursively
```

### ada83.c:8852

```
// (RM 9.2/9.4)? Objects of such a type create task dependents at elaboration,
```

### ada83.c:8853

```
// so the declaring scope must be a master and allocators must register the
```

### ada83.c:8854

```
// component tasks. Memoized on the Type_Info; the cache is reset wherever a
```

### ada83.c:8855

```
// type is mutated after construction (private/incomplete completion). Access
```

### ada83.c:8856

```
// components are not traversed: the designated object is a separate object
```

### ada83.c:8857

```
// with its own dependence (RM 9.4).
```

### ada83.c:8879

```
// RM 3.5.9: the effective SMALL of a fixed-point type. Uses the declared SMALL,
```

### ada83.c:8880

```
// falling back to DELTA (then 1.0) when SMALL is unset. Single source for the
```

### ada83.c:8881

```
// `small <= 0 ? delta : 1.0` guard that was duplicated at every scaling site.
```

### ada83.c:8889

```
// RM 3.5.9: the SMALL that governs the in-memory representation of a
```

### ada83.c:8890

```
// fixed-point value. All values of a type (every subtype and the type itself)
```

### ada83.c:8891

```
// share the base type's SMALL, which is the finest in the family. A derived
```

### ada83.c:8892

```
// type given a coarser DELTA has a larger 'SMALL than its base, but its objects
```

### ada83.c:8893

```
// are still stored, and its arithmetic still performed, in the base's units so
```

### ada83.c:8894

```
// that base values absent from the subtype remain representable. For an
```

### ada83.c:8895

```
// ordinary type the base SMALL equals its own, so this is identity.
```

### ada83.c:8900

```
// RM 3.5.9: the default SMALL of an ordinary fixed-point type is the largest
```

### ada83.c:8901

```
// power of two not greater than DELTA; its scale is that power's base-2
```

### ada83.c:8902

```
// exponent. Shared by fixed-point type creation and derived-type accuracy
```

### ada83.c:8903

```
// constraints.
```

### ada83.c:8917

```
// RM 3.5.9: the number of binary digits a fixed-point type needs to span a
```

### ada83.c:8918

```
// given magnitude in units of SMALL: ceiling(log2(bound / small)), at least 1.
```

### ada83.c:8927

```
// RM 3.5.9: construct the anonymous base type of a fixed-point first-named
```

### ada83.c:8928

```
// subtype. The base spans the symmetric model range +/-(2**MANTISSA - 1)*SMALL,
```

### ada83.c:8929

```
// where MANTISSA derives from the subtype's declared range. Giving the base
```

### ada83.c:8930

```
// explicit bounds keeps 'BASE'FIRST, 'BASE'LAST, 'BASE'MANTISSA, 'BASE'LARGE
```

### ada83.c:8931

```
// and 'BASE'SAFE_LARGE mutually consistent (each is read back from the range).
```

### ada83.c:8947

```
// Express a fixed-point SMALL as an exact integer ratio numerator/denominator.
```

### ada83.c:8948

```
// Every SMALL in this compiler is either a power of two (the RM 3.5.9 default,
```

### ada83.c:8949

```
// computed from DELTA) or a negative power of ten (DURATION's 1e-5), both of
```

### ada83.c:8950

```
// which one of the two integers captures exactly; the rounding only repairs
```

### ada83.c:8951

```
// the double's representation error (1/1e-5 = 99999.999... -> 100000).
```

### ada83.c:8963

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:8964

```
// IEEE 754 Named Constants - replaces magic numbers throughout codegen.                           
```

### ada83.c:8965

```
// Single source of truth for float/double structural parameters.                                  
```

### ada83.c:8966

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:8968

```
// Resolve effective DIGITS for a float type: uses declared digits if > 0,
```

### ada83.c:8969

```
// else defaults from IEEE precision.
```

### ada83.c:8973

```
// Default from the machine width (RM 3.5.7): a 4-byte float carries IEEE
```

### ada83.c:8974

```
// single precision, anything else double. The width lives on the BASE
```

### ada83.c:8975

```
// type — a DIGITS-constrained subtype narrows accuracy, not storage.
```

### ada83.c:8982

```
// Compute model parameters for a floating-point type (RM 3.5.8).                                  
```

### ada83.c:8983

```
// mantissa = ceil (DIGITS * log2 (10)) + 1                                                         
```

### ada83.c:8984

```
// emax     = 4 * mantissa                                                                          
```

### ada83.c:8985

```
// Used by MANTISSA, EMAX, EPSILON, SMALL, LARGE attributes.                                       
```

### ada83.c:8986

```
//                                                                                                  
```

### ada83.c:9002

```
// T'EPSILON = 2^(1 - MANTISSA) (RM 3.5.8(9))
```

### ada83.c:9010

```
// T'SMALL - fixed-point: power of 2 <= delta; float: 2^(-EMAX - 1)
```

### ada83.c:9019

```
// 2^-1022
```

### ada83.c:9022

```
// T'LARGE - fixed-point: (2^MANTISSA - 1) * SMALL (RM 3.5.10)
```

### ada83.c:9023

```
// float: 2^EMAX * (1 - 2^(-MANTISSA)) (RM 3.5.8(10))
```

### ada83.c:9040

```
// T'SAFE_SMALL - smallest positive safe value
```

### ada83.c:9041

```
// Fixed-point (RM 3.5.10): SAFE_SMALL = BASE'SMALL
```

### ada83.c:9042

```
// Float (RM 3.5.8): 2^(-(SAFE_EMAX+1))
```

### ada83.c:9051

```
// T'SAFE_LARGE - largest safe value
```

### ada83.c:9052

```
// Fixed-point (RM 3.5.10): SAFE_LARGE = BASE'LARGE = (2^B_MANT - 1) * BASE'SMALL
```

### ada83.c:9053

```
// Float (RM 3.5.8): 2^(SAFE_EMAX) * (1 - 2^(-MANTISSA))
```

### ada83.c:9074

```
// not a real model attribute
```

### ada83.c:9088

```
// RM 7.4.4 / 9.2: the is_limited flag is the single source of truth — set for
```

### ada83.c:9089

```
// task types (at creation) and explicit `limited` record/private declarations;
```

### ada83.c:9090

```
// limited private types carry it in their kind.
```

### ada83.c:9098

```
// Modular types are unsigned in Ada (RM 3.5.4). This predicate drives
```

### ada83.c:9099

```
// codegen choices: zext vs sext, udiv vs sdiv, unsigned comparisons, etc.
```

### ada83.c:9108

```
// RM 3.5.2: a character type is an enumeration type at least one of whose
```

### ada83.c:9109

```
// literals is a character literal. The predefined CHARACTER is the common
```

### ada83.c:9110

```
// case, but a user-defined enumeration such as TYPE T IS ('A', B, 'C') is
```

### ada83.c:9111

```
// equally a character type. String literals (RM 4.2) require a character
```

### ada83.c:9112

```
// component type; this predicate — not the strict kind==TYPE_CHARACTER
```

### ada83.c:9113

```
// Type_Is_Character — gates whether a context array type may fix a string
```

### ada83.c:9114

```
// literal's bounds. Chase to the root so a subtype or derived type is judged
```

### ada83.c:9115

```
// by the base enumeration's literals.
```

### ada83.c:9129

```
// Needs fat pointer { ptr, { bound, bound } } for unconstrained array or access thereto
```

### ada83.c:9130

```
// The representation view of a type: a private or incomplete type is
```

### ada83.c:9131

```
// represented as its completion (RM 7.4.1), so every representation and
```

### ada83.c:9132

```
// constraint decision must resolve through it — exactly as Type_To_Rep does.
```

### ada83.c:9142

```
// A private view reps as its completion; the fat-vs-thin decision follows it.
```

### ada83.c:9146

```
// RM 3.10: Access to unconstrained array always needs fat pointer; access to
```

### ada83.c:9147

```
// a constrained array with DYNAMIC bounds likewise stores as fat because the
```

### ada83.c:9148

```
// bounds must travel with the value (RM 3.6.1). Chase base_type so a subtype
```

### ada83.c:9149

```
// narrowing the bounds (ACCESS ARR (1..N)) still resolves to the underlying
```

### ada83.c:9150

```
// representation decision.
```

### ada83.c:9151

```
//
```

### ada83.c:9153

```
// Follow the ROOT access type (matches Type_To_Rep): an access subtype
```

### ada83.c:9154

```
// constraint can't change the pointer width — only an access type that
```

### ada83.c:9155

```
// directly designates a static-bounds-constrained array is thin.
```

### ada83.c:9167

```
// Check if array type is unconstrained (needs fat pointer representation)
```

### ada83.c:9173

```
// Constrained array: TYPE_ARRAY or TYPE_STRING with static bounds
```

### ada83.c:9179

```
// Universal numeric types: compile-time only, no storage
```

### ada83.c:9191

```
// Check if array type has dynamic bounds (BOUND_EXPR) that need runtime access.
```

### ada83.c:9192

```
// This includes constrained arrays like ARRAY (1..G) where G is a variable.
```

### ada83.c:9199

```
// Check if any bound is a runtime expression, either on the array                                
```

### ada83.c:9200

```
// index entry itself or on the index type (e.g., ARRAY (SNI,..)                                  
```

### ada83.c:9201

```
// where SNI has dynamic range -N..N).                                                           
```

### ada83.c:9202

```
//                                                                                                
```

### ada83.c:9209

```
// Also check if the index type itself has dynamic bounds
```

### ada83.c:9219

```
// A type is DYNAMICALLY SIZED when its byte size is not known at compile time
```

### ada83.c:9220

```
// — either because it is an unconstrained array (bounds arrive with the value)
```

### ada83.c:9221

```
// or a record with a component whose non-discriminant constraint is a runtime
```

### ada83.c:9222

```
// expression. Type_Info encodes the property by reporting size 0; the runtime
```

### ada83.c:9223

```
// byte size lives in the type's rt descriptor. One predicate, one lie-free
```

### ada83.c:9224

```
// rule everywhere — parameter passing mechanism, frame layout, allocation
```

### ada83.c:9225

```
// strategy, activation-record indirection all flow from it. Mirrors GNAT-LLVM
```

### ada83.c:9226

```
// Is_Dynamic_Size (reference/gnat/gnatllvm-types.adb:238).
```

### ada83.c:9231

```
// A component that gives its enclosing record a runtime byte size: an array
```

### ada83.c:9232

```
// component whose constraint is a non-static, non-discriminant expression.
```

### ada83.c:9233

```
// Discriminant-dependent components are covered by the discriminated-record
```

### ada83.c:9234

```
// machinery instead, so they are excluded. This is the shared source of truth
```

### ada83.c:9235

```
// for the record layout (which sets record.size = 0) and the rt-descriptor
```

### ada83.c:9236

```
// emission (which allocates @__rt_rec_N_size). Both must agree.
```

### ada83.c:9242

```
// Emit an i64 SSA value giving the byte size of T. Returns a constant when T
```

### ada83.c:9243

```
// is statically sized (t->size > 0), otherwise loads from the record's runtime
```

### ada83.c:9244

```
// size descriptor (@__rt_rec_N_size). The single interface for "how big is
```

### ada83.c:9245

```
// this?" — every caller that allocates or copies a value of a type uses this
```

### ada83.c:9246

```
// instead of a bespoke `size > 0 ? size : fallback`. Undefined for
```

### ada83.c:9247

```
// unconstrained arrays, which carry their size in the fat pointer rather than
```

### ada83.c:9248

```
// a per-type descriptor; use Emit_Array_Byte_Size for those.
```

### ada83.c:9261

```
// Check if an expression is a slice (NK_APPLY with NK_RANGE argument).
```

### ada83.c:9262

```
// Slices produce fat pointers at runtime even when their declared type
```

### ada83.c:9263

```
// is constrained, so they need special handling in comparisons.
```

### ada83.c:9264

```
//                                                                                                  
```

### ada83.c:9265

```
// A slice index argument is either an explicit range (low..high) or a 'RANGE
```

### ada83.c:9266

```
// attribute (RM 4.1.2); both denote a discrete_range, not a scalar index.
```

### ada83.c:9272

```
// RM 4.1.2: a slice's discrete range may be written as a subtype mark
```

### ada83.c:9273

```
// (A (DT)) or a constrained subtype indication (A (DT RANGE L .. R)).
```

### ada83.c:9289

```
// Check if an expression will produce a fat pointer value at runtime.                             
```

### ada83.c:9290

```
// This centralizes the "src_is_fat_ptr" detection pattern used in assignments                      
```

### ada83.c:9291

```
// and comparisons: STRING, unconstrained arrays, slices, and concatenations                        
```

### ada83.c:9292

```
// all produce fat pointer values { ptr, { bound, bound } }.                                       
```

### ada83.c:9293

```
//                                                                                                  
```

### ada83.c:9297

```
// Operation-specific checks FIRST: these operations always produce fat
```

### ada83.c:9298

```
// pointers at runtime regardless of the expression's declared type.
```

### ada83.c:9299

```
// E.g., concatenation of two constrained STRINGs still builds { ptr, ptr }.
```

### ada83.c:9300

```
// Array aggregates of unconstrained or dynamic-bounds types now return SSA
```

### ada83.c:9301

```
// { ptr, ptr } directly; record aggregates and static-bounds array
```

### ada83.c:9302

```
// aggregates still return a plain ptr to flat storage.
```

### ada83.c:9303

```
//
```

### ada83.c:9305

```
// A qualified expression T'(X) produces whatever X produces — unwrap so a
```

### ada83.c:9306

```
// qualified aggregate of an unconstrained/dynamic type is still seen as fat.
```

### ada83.c:9316

```
// String literals always produce fat pointers
```

### ada83.c:9320

```
// Concatenation always returns fat pointer
```

### ada83.c:9324

```
// Slices always produce fat pointers even with constrained declared type
```

### ada83.c:9328

```
// A .ALL dereference of a fat access yields the loaded { ptr, ptr }
```

### ada83.c:9329

```
// (data + bounds), even when the designated subtype is statically
```

### ada83.c:9330

```
// constrained — the bounds still travel with an access-to-unconstrained.
```

### ada83.c:9337

```
// Identifier-based check: if the node's own type has dynamic bounds or is
```

### ada83.c:9338

```
// unconstrained, Generate_Identifier will load it as a fat pointer value.
```

### ada83.c:9345

```
// Type-based checks: constrained arrays with STATIC bounds are flat allocas.                    
```

### ada83.c:9346

```
// Constrained arrays with DYNAMIC bounds (e.g., STRING (1..F(X))) are stored                     
```

### ada83.c:9347

```
// as fat pointers because their bounds are runtime-determined (RM 3.6.1).                       
```

### ada83.c:9348

```
//                                                                                                
```

### ada83.c:9358

```
// Check if a record field type requires loading as a fat pointer.                                 
```

### ada83.c:9359

```
// Unconstrained arrays, dynamic-bound arrays, and unconstrained STRING fields                      
```

### ada83.c:9360

```
// are stored as fat pointers { ptr, { bound, bound } } in records.                                
```

### ada83.c:9361

```
// Constrained STRING subtypes (e.g., STRING (1..6)) are flat arrays.                              
```

### ada83.c:9362

```
//                                                                                                  
```

### ada83.c:9366

```
// Constrained arrays with static bounds are flat - no fat pointer.
```

### ada83.c:9370

```
// A constrained array with non-zero size has compile-time-known layout                           
```

### ada83.c:9371

```
// even when bounds are stored as BOUND_EXPR (constant expressions like                           
```

### ada83.c:9372

```
// OA = OTHER_ARRAY (2..4)). Treat as static.                                                   
```

### ada83.c:9373

```
//                                                                                                
```

### ada83.c:9383

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:9384

```
// §10.5 Base Type Traversal                                                                        
```

### ada83.c:9385

```
//                                                                                                  
```

### ada83.c:9386

```
// Per RM 3.3.1: The base type of a type is the ultimate ancestor.                                 
```

### ada83.c:9387

```
// For subtypes, follow base_type links; for derived types, follow parent_type.                    
```

### ada83.c:9388

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:9395

```
// RM 3.6: a constrained array type definition (`type A is array (1..3) of T`)
```

### ada83.c:9396

```
// implicitly declares an anonymous UNCONSTRAINED base sharing A's index and
```

### ada83.c:9397

```
// component types, with A a constrained subtype of it. Slices, and flexible
```

### ada83.c:9398

```
// operands in a non-applicable-index-constraint context (RM 4.3.2: a string
```

### ada83.c:9399

```
// literal or aggregate operand of an operator fixes its own bounds from its
```

### ada83.c:9400

```
// length), are values of that base. The predefined STRING already provides
```

### ada83.c:9401

```
// its own unconstrained base; for user array types the base is materialized
```

### ada83.c:9402

```
// here on first demand and memoized so every use sees one identity.
```

### ada83.c:9414

```
// Fresh per-dimension Index_Info so bound caching on the constrained
```

### ada83.c:9415

```
// type is never shared. The bound VALUES carry over as placeholders —
```

### ada83.c:9416

```
// exactly the predefined STRING pattern (POSITIVE'FIRST..'LAST): a
```

### ada83.c:9417

```
// positional aggregate overrides high to low + N - 1, a named one takes
```

### ada83.c:9418

```
// both bounds from its choices, and array equality reads lengths, so
```

### ada83.c:9419

```
// the placeholder values themselves are never load-bearing.
```

### ada83.c:9429

```
// Type_Root: Follow both base_type and parent_type chains to find the root                         
```

### ada83.c:9430

```
// ancestor type. This is used for derived type compatibility checking where                        
```

### ada83.c:9431

```
// we need to find the ultimate parent enumeration/integer type.                                   
```

### ada83.c:9432

```
//                                                                                                  
```

### ada83.c:9446

```
// Recover a discriminant's Type_Info from its enclosing record by name.
```

### ada83.c:9447

```
// Used when a discriminant Symbol's `->type` was never populated (some
```

### ada83.c:9448

```
// resolver paths attach the symbol without setting its type). Records carry
```

### ada83.c:9449

```
// each discriminant as a `Component_Info` with `is_discriminant = true`.
```

### ada83.c:9461

```
// NOTE: Type compatibility checking is consolidated in Type_Covers ()                              
```

### ada83.c:9462

```
// defined in §11.6.2 (Overload Resolution section). That function provides                         
```

### ada83.c:9463

```
// coverage checking for:                                                                           
```

### ada83.c:9464

```
// - Same type identity                                                                             
```

### ada83.c:9465

```
// - Universal type compatibility                                                                   
```

### ada83.c:9466

```
// - Base type matching                                                                             
```

### ada83.c:9467

```
// - Array/string structural compatibility                                                          
```

### ada83.c:9468

```
// - Access type designated type compatibility                                                      
```

### ada83.c:9469

```
//                                                                                                  
```

### ada83.c:9471

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:9472

```
// §10.6 Type Freezing                                                                              
```

### ada83.c:9473

```
//                                                                                                  
```

### ada83.c:9474

```
// Freezing determines the point at which a type's representation is fixed.                        
```

### ada83.c:9475

```
// The compiler must track what the RM permits but the programmer cannot see.                      
```

### ada83.c:9476

```
// Per RM 13.14:                                                                                    
```

### ada83.c:9477

```
// - Types are frozen by object declarations, bodies, end of declarative part                       
```

### ada83.c:9478

```
// - Subtypes freeze their base type                                                                
```

### ada83.c:9479

```
// - Composite types freeze their component types                                                   
```

### ada83.c:9480

```
// - Once frozen, size/alignment/layout cannot change                                               
```

### ada83.c:9481

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:9483

```
// Forward declaration for Symbol
```

### ada83.c:9486

```
// Freeze a type and all its dependencies
```

### ada83.c:9487

```
// Per RM 13.14: When a type is frozen, its representation is fixed
```

### ada83.c:9491

```
// Mark as frozen first to prevent infinite recursion
```

### ada83.c:9494

```
// Freeze base type if present
```

### ada83.c:9499

```
// Freeze parent type for derived types
```

### ada83.c:9504

```
// Freeze component types for composites
```

### ada83.c:9509

```
// Freeze element type
```

### ada83.c:9514

```
// Freeze index types
```

### ada83.c:9523

```
// Freeze all component types
```

### ada83.c:9532

```
// Access type freezing does NOT freeze designated type                                       
```

### ada83.c:9533

```
// Per RM 13.14: "Freezing an access type does not freeze                                     
```

### ada83.c:9534

```
// its designated subtype"                                                                    
```

### ada83.c:9535

```
//                                                                                            
```

### ada83.c:9541

```
// Register composite types for implicit equality function generation
```

### ada83.c:9542

```
// Per RM 4.5.2: Equality is predefined for all non-limited types
```

### ada83.c:9546

```
// Generate a unique function name for this type's equality
```

### ada83.c:9556

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:9557

```
// §11. SYMBOL TABLE - Scoped Name Resolution                                                       
```

### ada83.c:9558

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:9581

```
// already resolved
```

### ada83.c:9582

```
// An operator designator (IS "&") may denote a PREDEFINED operator that
```

### ada83.c:9583

```
// exists as no named symbol — failure to resolve is not an error; the
```

### ada83.c:9584

```
// instantiation binds it as a builtin (RM 12.1.3, cc1307a).
```

### ada83.c:9604

```
// A SINGLE task declared at package level is a package-level object
```

### ada83.c:9605

```
// too — its TCB slot must be module-level storage, not a slot in
```

### ada83.c:9606

```
// whichever frame elaborates the package (an instance of a library
```

### ada83.c:9607

```
// generic elaborated inside a procedure otherwise names a frame alias
```

### ada83.c:9608

```
// no other function has, c94008d).
```

### ada83.c:9612

```
// The spec node's symbol is the task TYPE; the object is the
```

### ada83.c:9613

```
// homonymous VARIABLE beside it (mirrors the elaboration emitter).
```

### ada83.c:9645

```
// RM 3.7.4: for an IN OUT or OUT formal whose subtype is an unconstrained but
```

### ada83.c:9646

```
// mutable discriminated record (all discriminants defaulted, no explicit
```

### ada83.c:9647

```
// constraint), the formal's 'CONSTRAINED — and whether a whole-record
```

### ada83.c:9648

```
// assignment may change its discriminants — is inherited from the ACTUAL at
```

### ada83.c:9649

```
// the call, a runtime property. Such a formal carries a hidden Boolean flag
```

### ada83.c:9650

```
// alongside its by-reference address. An IN formal is a constant (statically
```

### ada83.c:9651

```
// constrained) and a constrained formal subtype is statically TRUE, so neither
```

### ada83.c:9652

```
// needs the flag.
```

### ada83.c:9659

```
// Emit "%<name>.constr" — the local i8 slot holding a by-reference formal's
```

### ada83.c:9660

```
// runtime 'CONSTRAINED flag. Both the prologue (store) and the attribute /
```

### ada83.c:9661

```
// assignment sites (load) name it through here so they agree.
```

### ada83.c:9665

```
// An uplevel reference reads the frame-published copy through the
```

### ada83.c:9666

```
// %__frame. alias the function header emitted (RM 3.7.4 flag of an
```

### ada83.c:9667

```
// enclosing subprogram's formal, c64106d).
```

### ada83.c:9673

```
// Materialize the 'CONSTRAINED state of an actual parameter as an i8 value, to
```

### ada83.c:9674

```
// pass as the hidden flag of a mutable OUT/IN OUT formal (RM 3.7.4). An object
```

### ada83.c:9675

```
// is constrained when its nominal subtype constrains the discriminants, when it
```

### ada83.c:9676

```
// is a constant or IN formal (a constant view), or when it is reached through
```

### ada83.c:9677

```
// an access value (a heap object's discriminants are fixed at allocation). A
```

### ada83.c:9678

```
// formal that itself carries a runtime flag propagates it unchanged.
```

### ada83.c:9708

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:9709

```
// Check_Is_Suppressed - GNAT-style check suppression query (RM 11.5)                               
```

### ada83.c:9710

```
//                                                                                                  
```

### ada83.c:9711

```
// Consults the suppressed_checks bitmask on:                                                       
```

### ada83.c:9712

```
//   1. The target type (if provided)                                                               
```

### ada83.c:9713

```
//   2. The target type's base_type (if different)                                                  
```

### ada83.c:9714

```
//   3. The symbol (if provided)                                                                    
```

### ada83.c:9715

```
// Returns true if the specified check_bit is suppressed at any level.                             
```

### ada83.c:9716

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:9725

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:9726

```
// §11.3 Scope Structure                                                                            
```

### ada83.c:9727

```
//                                                                                                  
```

### ada83.c:9728

```
// Each scope has its own hash table with 1024 buckets, which covers most programs.                
```

### ada83.c:9729

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:9733

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:9734

```
// §11.4 Scope Operations                                                                           
```

### ada83.c:9735

```
//                                                                                                  
```

### ada83.c:9736

```
// Lexical scoping is a tree; visibility rules turn it into a forest.                              
```

### ada83.c:9737

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:9744

```
// Inherit frame_size from parent so nested scope variables get unique offsets.
```

### ada83.c:9745

```
// This ensures variables in DECLARE blocks don't overlap with outer variables.
```

### ada83.c:9757

```
// Propagate frame variables from child scope to parent scope.
```

### ada83.c:9758

```
// Variables in DECLARE blocks share the enclosing function's frame,                            
```

### ada83.c:9759

```
// so nested functions need frame aliases for ALL variables, not just                           
```

### ada83.c:9760

```
// those in the immediate parent scope. Only skip if this scope IS the                          
```

### ada83.c:9761

```
// function's own body scope (i.e., child->owner->scope == child).                             
```

### ada83.c:9762

```
// We use the separate frame_vars list to avoid polluting symbol lookup.                       
```

### ada83.c:9763

```
//                                                                                              
```

### ada83.c:9766

```
// A frame-owning body scope: a subprogram's, or a task body's (a task
```

### ada83.c:9767

```
// body owns a per-instance frame and is a static-chain level, so its
```

### ada83.c:9768

```
// storage must NOT leak into the enclosing function's alias set —
```

### ada83.c:9769

```
// nested units reach it through the chain instead).
```

### ada83.c:9774

```
// This is a block scope (DECLARE/loop/etc), not a subprogram's own body scope.
```

### ada83.c:9775

```
// Propagate its storage-bearing symbols to parent's frame_vars for alias
```

### ada83.c:9776

```
// generation, and its frame_size — block variables share the enclosing
```

### ada83.c:9777

```
// function's frame, so the function must allocate room for them. A
```

### ada83.c:9778

```
// FUNCTION body scope propagates NEITHER: a nested function's storage
```

### ada83.c:9779

```
// lives in its own frame (it inherits the parent's frame_size at push
```

### ada83.c:9780

```
// only to keep chain-wide offsets unique), and inflating the parent's
```

### ada83.c:9781

```
// frame_size here would move the parent's static-chain slot after the
```

### ada83.c:9782

```
// parent function was already emitted with the smaller size — a subunit
```

### ada83.c:9783

```
// compilation then chases the chain at an offset the separately compiled
```

### ada83.c:9784

```
// parent never wrote (c64005d: three levels of subunits, each compilation
```

### ada83.c:9785

```
// inflating its parent differently and reading a garbage grandparent
```

### ada83.c:9786

```
// frame pointer).
```

### ada83.c:9805

```
// Also propagate any frame_vars that were already collected in the child
```

### ada83.c:9806

```
// (from even deeper nested scopes).
```

### ada83.c:9820

```
// Push an existing scope, REPARENTING it under the current scope. Used by
```

### ada83.c:9821

```
// generic instantiation, whose declaration-point visibility machinery
```

### ada83.c:9822

```
// depends on the instance scope hanging off the instantiation context.
```

### ada83.c:9830

```
// Enter an existing scope WITHOUT reparenting: the scope's own lexical
```

### ada83.c:9831

```
// chain is the point — a subunit resolves in its stub's environment, whose
```

### ada83.c:9832

```
// enclosing scopes (RM 10.2) must stay reachable. Returns the scope to
```

### ada83.c:9833

```
// hand back to Symbol_Manager_Leave_Scope.
```

### ada83.c:9844

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:9845

```
// §11.5 Symbol Table Operations                                                                    
```

### ada83.c:9846

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:9861

```
// Declaration-point visibility for instance resolution (RM 12.3 rule (i)):
```

### ada83.c:9862

```
// when a cutoff is active, a symbol stamped after it is invisible unless it
```

### ada83.c:9863

```
// belongs to the instance's own scope subtree (formal bindings and the
```

### ada83.c:9864

```
// clone's freshly created entities).
```

### ada83.c:9873

```
// RM 6.6 / 8.3: two subprograms are homographs when they share a designator
```

### ada83.c:9874

```
// and have the same parameter-and-result type profile. The result type is part
```

### ada83.c:9875

```
// of a function's profile in Ada 83, so two functions differing only in result
```

### ada83.c:9876

```
// type are not homographs.
```

### ada83.c:9877

```
// A subprogram symbol: a function or a procedure (RM 6.1).
```

### ada83.c:9894

```
// A subprogram is "implicitly declared" when it was introduced by derivation
```

### ada83.c:9895

```
// (RM 3.4) rather than by an explicit declaration, renaming, or instantiation.
```

### ada83.c:9896

```
// RM 8.3: such an implicit declaration is hidden by an explicit homograph in
```

### ada83.c:9897

```
// the same declarative region.
```

### ada83.c:9907

```
// RM 3.4 / 8.4: remember whether a subprogram is declared immediately within a
```

### ada83.c:9908

```
// package's visible part. An explicit one there becomes derivable of the first
```

### ada83.c:9909

```
// kind (and hides a derived homograph for further derivation); an implicit
```

### ada83.c:9910

```
// (inherited) one there is what a USE clause makes directly visible. Consumers
```

### ada83.c:9911

```
// that care only about explicit operations re-check implicitness themselves.
```

### ada83.c:9916

```
// Check if symbol is already in this bucket (avoid self-cycle)
```

### ada83.c:9918

```
// Already added
```

### ada83.c:9922

```
// Overloading: add to chain if subprograms, enumeration literals, or
```

### ada83.c:9923

```
// task entries. Per RM 8.6 enumeration literals are overloadable like
```

### ada83.c:9924

```
// functions, and per RM 9.5(2) entries of a task may be overloaded.
```

### ada83.c:9930

```
// Check if sym is already in the overload chain (prevents cycles)
```

### ada83.c:9933

```
// Already in chain
```

### ada83.c:9937

```
// Only assign unique_id if not already set (symbol may have been
```

### ada83.c:9938

```
// added to another scope already)
```

### ada83.c:9942

```
// Mark first decl as overloaded too
```

### ada83.c:9949

```
// The linear symbols[] enumeration must carry EVERY symbol of the
```

### ada83.c:9950

```
// scope, chained overloads included — Emit_Instance_Wrapper_Bodies,
```

### ada83.c:9951

```
// Emit_Instance_Binding_Elaboration and the package-level markers
```

### ada83.c:9952

```
// walk it, and a second same-named formal-subprogram binding that
```

### ada83.c:9953

```
// never entered it was a wrapper whose body never emitted
```

### ada83.c:9954

```
// (cc1301a's four post-type formals).
```

### ada83.c:9967

```
// RM 8.3: when an explicitly declared subprogram and an implicitly
```

### ada83.c:9968

```
// declared (derived) homograph coexist in one declarative region, the
```

### ada83.c:9969

```
// implicit one is hidden. This applies in either declaration order: the
```

### ada83.c:9970

```
// explicit declaration may precede or follow the derived type that
```

### ada83.c:9971

```
// introduces the implicit homograph. Predefined operators have their
```

### ada83.c:9972

```
// own hiding rule (a derived operator hides them) and are excluded.
```

### ada83.c:9984

```
// Allow variable to shadow type with same name (single task declaration).                   
```

### ada83.c:9985

```
// Per RM 9.1, a single task declaration creates both a task type and                         
```

### ada83.c:9986

```
// an anonymous object of that type with the same name. The object                            
```

### ada83.c:9987

```
// shadows the type for normal name lookups.                                                 
```

### ada83.c:9988

```
//                                                                                            
```

### ada83.c:9990

```
// Proceed to add the variable - it will shadow the type
```

### ada83.c:9993

```
// Deferred constant completion (RM 7.4): update existing symbol's
```

### ada83.c:9994

```
// declaration to the full declaration which has the initializer.
```

### ada83.c:9995

```
// The AST name-node binding is fixed up by the caller after Symbol_Add
```

### ada83.c:9996

```
// returns, because Resolve_Declaration is the only place with the
```

### ada83.c:9997

```
// per-name index — Symbol_Add sees the completion sym alone and can't
```

### ada83.c:9998

```
// tell which name of a multi-name decl it belongs to.
```

### ada83.c:9999

```
//
```

### ada83.c:10005

```
// The visible deferred declaration skipped frame allocation because
```

### ada83.c:10006

```
// the private-type view sizes only the discriminant part. Now that
```

### ada83.c:10007

```
// the completion supplies the full type, carve the slot — unless the
```

### ada83.c:10008

```
// constant is library-package state, which lives in a global.
```

### ada83.c:10018

```
// A USE-visible alias (RM 8.4) is not a same-region conflict: a new
```

### ada83.c:10019

```
// immediately-visible declaration of the same name hides the alias rather
```

### ada83.c:10020

```
// than being rejected. The overloadable-vs-overloadable case was handled
```

### ada83.c:10021

```
// above (the chain keeps both, preference scoring picks); this reaches the
```

### ada83.c:10022

```
// kinds that otherwise fall through to the skip-return — e.g. an object
```

### ada83.c:10023

```
// declaration hiding a use-visible enumeration literal (`E13 : INTEGER`
```

### ada83.c:10024

```
// over the literal E13, c83031a). Skip the alias and keep scanning so
```

### ada83.c:10025

```
// `sym` is added; Collect_Interpretations drops the cancelled alias.
```

### ada83.c:10031

```
// Same symbol already exists at this scope - skip
```

### ada83.c:10037

```
// Only assign unique_id if not already set
```

### ada83.c:10045

```
// Set parent to enclosing package/subprogram for nested symbol support
```

### ada83.c:10048

```
// Add to linear symbol list for enumeration (static link support)
```

### ada83.c:10058

```
// Track frame offset for variables/parameters/constants/discriminants.
```

### ada83.c:10059

```
// Named numbers (is_named_number) have no storage - skip frame allocation
```

### ada83.c:10060

```
// so their pre-set frame_offset (e.g. ASCII.DEL=127) is preserved.
```

### ada83.c:10061

```
// USE-visible aliases (RM 8.4) name someone else's storage; they must
```

### ada83.c:10062

```
// not carve their own slot, or Emit_Parent_Frame_Aliases would find the
```

### ada83.c:10063

```
// alias first and hand nested subprograms a phantom offset that the
```

### ada83.c:10064

```
// enclosing frame never allocated (RM 3.7.4 layout is set by the
```

### ada83.c:10065

```
// original declaration, not the USE-clause).
```

### ada83.c:10066

```
//
```

### ada83.c:10067

```
// Global-storage symbols (library-package state) never consume frame
```

### ada83.c:10068

```
// offsets: they are emitted as module-level @globals, and a phantom slot
```

### ada83.c:10069

```
// here shifts every LATER local's offset by however many library units
```

### ada83.c:10070

```
// happened to load first — a subunit compilation WITHing more units than
```

### ada83.c:10071

```
// its parent then disagrees with the parent's frame layout for the same
```

### ada83.c:10072

```
// source (ca1107a: the parent laid its local package's I at 4, the
```

### ada83.c:10073

```
// subunit — which also loaded the library homograph — read it at 8).
```

### ada83.c:10085

```
// A symbol whose parent chain reaches the library level without crossing a
```

### ada83.c:10086

```
// frame-owning construct — subprogram, task, or generic template — lives in
```

### ada83.c:10087

```
// module-level storage. This is deliberately NARROWER than Symbol_Is_Global:
```

### ada83.c:10088

```
// generic formals and instance package state keep their frame slots exactly
```

### ada83.c:10089

```
// as before; only true library-package state (a loaded REPORT's variables)
```

### ada83.c:10090

```
// is excluded from the frame counter.
```

### ada83.c:10102

```
// A deferred constant (RM 7.4) has its full type declared later in the
```

### ada83.c:10103

```
// same package's private part. Sizing the slot at the visible declaration
```

### ada83.c:10104

```
// would use the private-type view (discriminants only) and under-reserve
```

### ada83.c:10105

```
// storage; wait for the completion.
```

### ada83.c:10118

```
// Fat pointers for dynamic/unconstrained arrays (or access thereto) need
```

### ada83.c:10119

```
// { ptr, ptr } = 16 bytes, not just ptr = 8 bytes.
```

### ada83.c:10123

```
// A dynamically-sized record cannot sit inline at a compile-time offset;
```

### ada83.c:10124

```
// its frame slot holds a pointer to storage allocated separately at
```

### ada83.c:10125

```
// elaboration (RM 3.3). Arrays are handled above via Type_Needs_Fat_Pointer.
```

### ada83.c:10129

```
// A task object is represented as a pointer to its TCB (LL_REP_PTR), so it
```

### ada83.c:10130

```
// needs a full pointer-sized slot regardless of the task type's nominal
```

### ada83.c:10131

```
// size — otherwise adjacent task objects overlap and corrupt each other.
```

### ada83.c:10133

```
// pointer to TCB
```

### ada83.c:10135

```
// A by-reference parameter is reached from a nested subprogram through a
```

### ada83.c:10136

```
// pointer to the caller's storage held in the enclosing frame, so its
```

### ada83.c:10137

```
// slot must be at least pointer-sized. The parameter mode is not yet known
```

### ada83.c:10138

```
// here, so reserve pointer space for every parameter's slot.
```

### ada83.c:10143

```
// A discriminated-record formal may carry the RM 3.7.4 hidden
```

### ada83.c:10144

```
// 'CONSTRAINED flag, which nested subprograms reach through the frame
```

### ada83.c:10145

```
// exactly like the parameter itself — reserve its byte (padded to keep
```

### ada83.c:10146

```
// slot alignment) right after the parameter's slot.
```

### ada83.c:10156

```
// RM 11.1: an object too large for the stack reserves only a placeholder slot
```

### ada83.c:10157

```
// so the whole frame stays allocatable; its elaboration raises STORAGE_ERROR
```

### ada83.c:10158

```
// (Generate_Object_Declaration), after which references to it are unreachable.
```

### ada83.c:10162

```
// Natural alignment: a slot's offset rounds up to its size class (1, 2, 4,
```

### ada83.c:10163

```
// then 8 for anything pointer-sized or larger). Frame layout must not
```

### ada83.c:10164

```
// depend on its neighbors for alignment — an odd-sized local would
```

### ada83.c:10165

```
// otherwise misalign every pointer and fat-pointer slot after it.
```

### ada83.c:10176

```
// RM 9.1: a single task declaration declares an OBJECT; its task type is
```

### ada83.c:10177

```
// anonymous. Name lookup that lands on the type symbol of a single task
```

### ada83.c:10178

```
// (the declaration's carrier for entries and the body completion) denotes
```

### ada83.c:10179

```
// the implicit object variable; find it in the type's defining scope.
```

### ada83.c:10181

```
// A use-visible alias denotes the original (RM 8.4); the object scan below
```

### ada83.c:10182

```
// needs the original's defining scope, and codegen needs the symbol whose
```

### ada83.c:10183

```
// name the object was emitted under.
```

### ada83.c:10201

```
// Find symbol by name, searching enclosing scopes
```

### ada83.c:10208

```
// Skip past a hidden head (e.g. a derived subprogram hidden by an
```

### ada83.c:10209

```
// explicit homograph, RM 8.3) to the first visible overload.
```

### ada83.c:10219

```
// "Did you mean": the nearest visible name within a small edit distance
```

### ada83.c:10220

```
// of `name`, for undefined-name diagnostics. Returns NULL when nothing is
```

### ada83.c:10221

```
// close enough to suggest.
```

### ada83.c:10238

```
// RM 8.5: a package renaming denotes the SAME package — peel to the
```

### ada83.c:10239

```
// original so consumers see the complete, live export list. A renaming
```

### ada83.c:10240

```
// declared INSIDE the renamed package (`PACKAGE PACKA RENAMES PACK1` in
```

### ada83.c:10241

```
// PACK1's own visible part, c85011a) snapshots a still-growing export
```

### ada83.c:10242

```
// array; the original always carries the finished one.
```

### ada83.c:10251

```
// Find symbol by name and type (for enumeration literal disambiguation)
```

### ada83.c:10255

```
// Get base type for matching (handles derived types and constrained subtypes).
```

### ada83.c:10256

```
// Follow both parent_type (derived types) and base_type (constrained subtypes).
```

### ada83.c:10260

```
// Character literals are case-sensitive in Ada (RM 2.6), unlike identifiers.                    
```

### ada83.c:10261

```
// Hash is case-insensitive so char lits with different case share a bucket.                     
```

### ada83.c:10262

```
// Use case-insensitive at bucket level, case-sensitive in overload chain.                       
```

### ada83.c:10263

```
//                                                                                                
```

### ada83.c:10266

```
// Search all scopes for a matching symbol - don't stop at first name match,
```

### ada83.c:10267

```
// keep searching if the type doesn't match (for enumeration literal
```

### ada83.c:10268

```
// overloading). Within a scope, a same-named-type match is preferred over a
```

### ada83.c:10269

```
// loose root match: Type_Root collapses an entire derivation class to one
```

### ada83.c:10270

```
// root, so a function inherited into a derived type and a homograph returning
```

### ada83.c:10271

```
// the parent type would otherwise be indistinguishable here (RM 3.4).
```

### ada83.c:10272

```
//
```

### ada83.c:10273

```
// RM 8.3(15): a user subprogram declaration hides a homographic implicit
```

### ada83.c:10274

```
// declaration (including a derived enum literal). When both a FUNCTION and
```

### ada83.c:10275

```
// a LITERAL match the expected type, prefer the FUNCTION — e.g. c34015b's
```

### ada83.c:10276

```
// `TYPE T IS NEW ENUM` derives LITERAL RED of T, but the user's
```

### ada83.c:10277

```
// `FUNCTION RED RETURN T` in the visible part hides it, and a bare `RED`
```

### ada83.c:10278

```
// means the user function.
```

### ada83.c:10285

```
// Visibility is checked per overload so a hidden homograph (RM 8.3) at
```

### ada83.c:10286

```
// the head does not mask a visible one. Char literals require exact
```

### ada83.c:10287

```
// case (RM 2.6).
```

### ada83.c:10293

```
// For functions, match by return type, not by symbol type.
```

### ada83.c:10308

```
// No exact match in this scope; accept the root match
```

### ada83.c:10310

```
// No matching symbol found
```

### ada83.c:10313

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:10314

```
// §11.6 OVERLOAD RESOLUTION                                                                        
```

### ada83.c:10315

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:10317

```
//    interpretation using disambiguation rules.                                                   
```

### ada83.c:10318

```
//                                                                                                  
```

### ada83.c:10319

```
// Key concepts:                                                                                    
```

### ada83.c:10320

```
// - Interp: Record of (Nam, Typ, Opnd_Typ) representing one interpretation                         
```

### ada83.c:10321

```
// - Covers: Type compatibility test (T1 covers T2 if T2's values are legal for T1)                 
```

### ada83.c:10322

```
// - Disambiguate: Select best interpretation when multiple are valid                               
```

### ada83.c:10323

```
//                                                                                                  
```

### ada83.c:10324

```
// Per RM 8.6: Overload resolution identifies the unique declaration for each                       
```

### ada83.c:10325

```
// identifier. It fails if no interpretation is valid or if multiple are valid.                    
```

### ada83.c:10326

```
//                                                                                                  
```

### ada83.c:10328

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:10329

```
// §11.6.1 Interpretation Structure                                                                 
```

### ada83.c:10330

```
//                                                                                                  
```

### ada83.c:10331

```
// "type Interp is record Nam, Typ, Opnd_Typ..."                                                    
```

### ada83.c:10332

```
// We store interpretations in a contiguous array during resolution.                               
```

### ada83.c:10333

```
// Sixty-four interpretations suffices since deeper ambiguity signals a pathological program.      
```

### ada83.c:10334

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:10336

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:10337

```
// §11.6.2 Type Covering (Compatibility)                                                            
```

### ada83.c:10338

```
//                                                                                                  
```

### ada83.c:10339

```
// For example: T1 covers T2 if values of T2 are legal where T1 is expected.                       
```

### ada83.c:10340

```
//                                                                                                  
```

### ada83.c:10341

```
// Key rules from RM 8.6:                                                                           
```

### ada83.c:10342

```
// - Same type: always covers                                                                       
```

### ada83.c:10343

```
// - Subtypes of same base type: cover each other                                                   
```

### ada83.c:10344

```
// - Universal types: Universal_Integer covers any integer type, etc.                              
```

### ada83.c:10345

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:10347

```
// Two scalar/real types are compatible by name when they share a kind class and
```

### ada83.c:10348

```
// a name — this is how a generic instantiation's freshly-created types match the
```

### ada83.c:10349

```
// original spec's types (RM 12.3).
```

### ada83.c:10360

```
// Is the current scope chain outside the package that declared `t`? The
```

### ada83.c:10361

```
// partial view of a private type governs exactly there; inside the package
```

### ada83.c:10362

```
// (its spec's private part or its body) the full view applies (RM 7.4).
```

### ada83.c:10372

```
// Is the LIMITED view of this type the governing one here? (RM 7.4.4)
```

### ada83.c:10377

```
// Is the PRIVATE (partial) view of this type the governing one here? Where it
```

### ada83.c:10378

```
// is, the type has no predefined arithmetic, ordering, logical or catenation
```

### ada83.c:10379

```
// operators — only =, /= and its explicitly declared operators (RM 7.4.2).
```

### ada83.c:10384

```
// The type a view denotes at the CURRENT point. A generic-actual view is the
```

### ada83.c:10385

```
// instance's window onto its actual: inside the instance the formal type's own
```

### ada83.c:10386

```
// operations govern and the view stays opaque (RM 12.1.2 — a user operator
```

### ada83.c:10387

```
// visible at the instantiation site must not capture the formal's operands,
```

### ada83.c:10388

```
// c74409b); outside the instance, its profile exposes the actual itself, so
```

### ada83.c:10389

```
// the view peels to it (an instance "="(INTEGER, INTEGER) is an INTEGER
```

### ada83.c:10390

```
// equality to its clients, c67002d). Every other type denotes itself.
```

### ada83.c:10408

```
// Null types are permissive (incomplete analysis)
```

### ada83.c:10411

```
// Same type always covers
```

### ada83.c:10414

```
// Universal_Integer is covered by any Integer type — except a LIMITED
```

### ada83.c:10415

```
// private one seen from OUTSIDE its defining package: there the partial
```

### ada83.c:10416

```
// view governs and a limited type has no literals (RM 7.4.4). Inside the
```

### ada83.c:10417

```
// package (its spec's private part or its body), the full view restores
```

### ada83.c:10418

```
// them.
```

### ada83.c:10426

```
// Universal_Real covers any real type
```

### ada83.c:10430

```
// Same base type covers
```

### ada83.c:10436

```
// For derived types, check if they share the same root type (RM 3.4). This
```

### ada83.c:10437

```
// lets a parent type's value flow into a derived context (assignment and
```

### ada83.c:10438

```
// explicit conversion). It is DISABLED for overload resolution, where a
```

### ada83.c:10439

```
// parent operation must not masquerade as the derived type: otherwise
```

### ada83.c:10440

```
// `Derived'(X) = Parent_Literal` synthesises a spurious parent-typed
```

### ada83.c:10441

```
// comparison and the reference is ambiguous (c34015b, c83031a).
```

### ada83.c:10448

```
// SYSTEM.ADDRESS compatibility (RM 13.7): all ADDRESS types are interoperable                    
```

### ada83.c:10449

```
// This handles the case where 'ADDRESS attribute returns a built-in ADDRESS                      
```

### ada83.c:10450

```
// type but the target is declared as SYSTEM.ADDRESS from the package                             
```

### ada83.c:10451

```
//                                                                                                
```

### ada83.c:10457

```
// Array/string compatibility: same structure
```

### ada83.c:10458

```
// STRING is compatible with CHARACTER arrays
```

### ada83.c:10464

```
// Arrays with same element type
```

### ada83.c:10472

```
// Access types: check designated type compatibility
```

### ada83.c:10481

```
// NULL literal covers any access type
```

### ada83.c:10486

```
// Boolean types: both boolean types are always compatible
```

### ada83.c:10491

```
// Same-kind, same-name scalar/real types from generic instantiation (RM 12.3).
```

### ada83.c:10496

```
// Character literals as enumeration literals (RM 3.5.1):                                         
```

### ada83.c:10497

```
// An enumeration type can define character literals (e.g., TYPE T IS ('A', 'B');).              
```

### ada83.c:10498

```
// When comparing, CHARACTER type should be compatible with such enumerations.                   
```

### ada83.c:10499

```
// Check by looking for literals that start with single quote.                                   
```

### ada83.c:10500

```
//                                                                                                
```

### ada83.c:10512

```
// Also check derived enumeration types via root
```

### ada83.c:10528

```
// Check if enum has any character literals
```

### ada83.c:10533

```
// Enum has character literals
```

### ada83.c:10541

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:10542

```
// §11.6.3 Parameter Conformance
```

### ada83.c:10543

```
//
```

### ada83.c:10544

```
// Check if an argument list matches a subprogram's parameter profile.
```

### ada83.c:10545

```
// Per RM 6.4.1: actual parameters must be type conformant with formals.
```

### ada83.c:10546

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:10548

```
// True when `param` and `arg` are DISTINCT scalar types related only through
```

### ada83.c:10549

```
// derivation (RM 3.4): one is an ancestor of the other, or the two share an
```

### ada83.c:10550

```
// ancestor. Such a pair is not a valid overload match — a value of one type
```

### ada83.c:10551

```
// does not implicitly satisfy a parameter of the other without an explicit
```

### ada83.c:10552

```
// conversion (RM 8.6). Without this, e.g. CALENDAR."<"(TIME) wrongly matches
```

### ada83.c:10553

```
// DURATION operands inside its own body (DURATION(L) < DURATION(R)) and
```

### ada83.c:10554

```
// recurses instead of binding the predefined DURATION "<".
```

### ada83.c:10555

```
//
```

### ada83.c:10556

```
// The test rests on the type-graph invariant that base_type never crosses a
```

### ada83.c:10557

```
// derivation boundary (a first subtype reaches its own anonymous base, whose
```

### ada83.c:10558

```
// parent_type reaches the parent's base): same Type_Base = same type (subtypes
```

### ada83.c:10559

```
// conform), different Type_Base with the same Type_Root = distinct types
```

### ada83.c:10560

```
// related only by derivation. Universals still convert; composite and access
```

### ada83.c:10561

```
// types are left entirely to Type_Covers.
```

### ada83.c:10563

```
// RM 12.1.2: inside a generic instance a formal type IS its actual, never a
```

### ada83.c:10564

```
// sibling of it. A formal view built from a derived actual (TYPE NI IS NEW
```

### ada83.c:10565

```
// INTEGER) copies the actual's parent_type, so without peeling the view and
```

### ada83.c:10566

```
// the actual would look like two sibling derivations of INTEGER and this
```

### ada83.c:10567

```
// predicate would spuriously reject the actual as an argument for the formal.
```

### ada83.c:10576

```
// same type: subtype-related
```

### ada83.c:10577

```
// distinct types, common ancestor
```

### ada83.c:10580

```
// The overload-resolution applicability test shared by ordinary calls and
```

### ada83.c:10581

```
// operators (RM 8.7): a formal accepts an actual when its type covers the
```

### ada83.c:10582

```
// actual's AND the two are not merely related through a derivation boundary (a
```

### ada83.c:10583

```
// value of a derived type does not implicitly satisfy a parameter of the parent
```

### ada83.c:10584

```
// type, nor the reverse — RM 3.4). Universal literals still convert.
```

### ada83.c:10590

```
// A predefined-operator stand-in (Symbol_Manager_Init_Predefined registers one
```

### ada83.c:10591

```
// symbol per numeric class so `RENAMES "+"` can denote a predefined operator —
```

### ada83.c:10592

```
// RM 4.5 / 8.5) stands for the operator of EVERY type in its class: its
```

### ada83.c:10593

```
// INTEGER profile matches any integer type, its FLOAT profile any floating
```

### ada83.c:10594

```
// type. Class membership, not type identity, decides its applicability. Fixed
```

### ada83.c:10595

```
// point stays outside both classes — a fixed operand's predefined operation
```

### ada83.c:10596

```
// works on the scaled mantissa, which the stand-in's float profile would
```

### ada83.c:10597

```
// misrepresent, and such calls take the direct operator path instead
```

### ada83.c:10598

```
// (c45331a: "+"(LEFT => F_VAR, RIGHT => F_VAR)).
```

### ada83.c:10611

```
// Track which formal parameters are covered by arguments
```

### ada83.c:10617

```
// Check type compatibility for each argument and mark params covered
```

### ada83.c:10623

```
// Handle named association
```

### ada83.c:10636

```
// Positional: use i-th parameter
```

### ada83.c:10642

```
// Mark this parameter as covered
```

### ada83.c:10649

```
// RM 6.6: try alternative interpretation if available (e.g. user
```

### ada83.c:10650

```
// `<` returns INT but predefined `<` returns BOOLEAN for the same
```

### ada83.c:10651

```
// operand types — the alt may match where the primary didn't).
```

### ada83.c:10659

```
// Verify all required parameters (no default value) are covered
```

### ada83.c:10662

```
// Required parameter not provided
```

### ada83.c:10668

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:10669

```
// §11.6.4 Interpretation Collection                                                                
```

### ada83.c:10670

```
//                                                                                                  
```

### ada83.c:10671

```
// Gather candidates first, filter later. Visibility determines the set.                           
```

### ada83.c:10672

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:10674

```
// ── RM 8.4 use-clause visibility ────────────────────────────────────────────
```

### ada83.c:10675

```
// A declaration a use clause would make directly visible is NOT visible where a
```

### ada83.c:10676

```
// homograph is immediately visible (RM 8.4(11)). One predicate + one filter
```

### ada83.c:10677

```
// serve every interpretation set: names gathered across scopes and the operator
```

### ada83.c:10678

```
// sets built during expression analysis. The rule lives here rather than being
```

### ada83.c:10679

```
// re-derived at each use site.
```

### ada83.c:10681

```
// RM 8.3: overloadable declarations are subprograms, enumeration literals and
```

### ada83.c:10682

```
// entries; a predefined operation (an interpretation with no symbol) is one too.
```

### ada83.c:10689

```
// Formal type of operand `i`: the operator builders record it in opnd[], an
```

### ada83.c:10690

```
// ordinary subprogram or literal carries it on the symbol's parameter list.
```

### ada83.c:10704

```
// RM 12.3: inside a generic subprogram's own declarative region, its name
```

### ada83.c:10705

```
// denotes the CURRENT INSTANCE — an ordinary, overloadable subprogram with
```

### ada83.c:10706

```
// the unit's profile. Returns that profile compared against `other` when the
```

### ada83.c:10707

```
// interpretation is such a generic (0 = differ, 1 = homograph), or -1 when
```

### ada83.c:10708

```
// the current-instance reading does not apply and the caller must use the
```

### ada83.c:10709

```
// non-overloadable rule.
```

### ada83.c:10720

```
// The region of the generic itself, or of one of its instances —
```

### ada83.c:10721

```
// whose body clone re-resolves in the reconstructed template
```

### ada83.c:10722

```
// environment and must see the same names the template saw.
```

### ada83.c:10755

```
// RM 8.3: two same-named declarations are homographs when either is not
```

### ada83.c:10756

```
// overloadable, or both are overloadable with matching parameter and result
```

### ada83.c:10757

```
// profiles.
```

### ada83.c:10761

```
// A generic subprogram seen from within its own region hides only the
```

### ada83.c:10762

```
// homographs of its current-instance profile (RM 12.3; c83030c: bare F3
```

### ada83.c:10763

```
// recurses while F3(A) still reaches the use-visible F3(INTEGER)).
```

### ada83.c:10769

```
// Two ordinary subprograms: defer to the established homograph test, which
```

### ada83.c:10770

```
// compares profiles with Types_Same_Named (distinguishing sibling types such
```

### ada83.c:10771

```
// as INT and INT2 that share a representation base).
```

### ada83.c:10775

```
// At least one is a predefined operation or an enumeration literal: compare
```

### ada83.c:10776

```
// operand and result profiles the same way.
```

### ada83.c:10786

```
// The predefined operations of a type are implicitly declared at the type's
```

### ada83.c:10787

```
// declaration and are directly visible exactly where that declaration is
```

### ada83.c:10788

```
// (RM 8.4). True when `t`'s declaring scope is on the current scope chain; a
```

### ada83.c:10789

```
// type reachable only through a USE clause or an expanded name is not. An
```

### ada83.c:10790

```
// anonymous or builtin view (no defining symbol) counts as directly visible.
```

### ada83.c:10801

```
// Does this interpretation denote a directly-visible declaration? A symbol
```

### ada83.c:10802

```
// carries its own visibility; a predefined operation (no symbol) shares the
```

### ada83.c:10803

```
// visibility of its operand type's declaration.
```

### ada83.c:10810

```
// RM 8.4: drop each use-visible interpretation cancelled by a directly-visible
```

### ada83.c:10811

```
// homograph in the same set — an immediately visible declaration, or a
```

### ada83.c:10812

```
// predefined operation of a directly-visible type (a USE clause must not let a
```

### ada83.c:10813

```
// package's "-"(INT, INT) shadow INT's own predefined "-", c84009a).
```

### ada83.c:10831

```
// Collect all visible interpretations of a name
```

### ada83.c:10837

```
// Search all enclosing scopes. Visibility is checked per overload, not just
```

### ada83.c:10838

```
// on the bucket head: an implicit derived subprogram hidden by an explicit
```

### ada83.c:10839

```
// homograph (RM 8.3) may sit anywhere in the chain, including at its head.
```

### ada83.c:10865

```
// Gather every overload of SELECTOR that PACKAGE makes visible, for the
```

### ada83.c:10866

```
// expanded-name call P.F (...). Qualified selection sees all members of P
```

### ada83.c:10867

```
// regardless of USE clauses, so visibility is not re-checked here — the
```

### ada83.c:10868

```
// exported list is the authoritative set (RM 4.1.3). Overloads of one name
```

### ada83.c:10869

```
// link through next_overload; distinct exported entries are deduplicated.
```

### ada83.c:10891

```
// Filter interpretations by argument compatibility
```

### ada83.c:10897

```
// Enumeration literals (RM 3.5.1) are nullary parameterless "callables";
```

### ada83.c:10898

```
// they can appear as a call-notation interpretation only when the call
```

### ada83.c:10899

```
// supplies no arguments. `TRUE (20)` supplies one argument, so the
```

### ada83.c:10900

```
// Standard.TRUE literal must be filtered out — otherwise it survives
```

### ada83.c:10901

```
// alongside the user-defined `FUNCTION TRUE (I : INTEGER) …` and
```

### ada83.c:10902

```
// Disambiguate has no basis to prefer the callable.
```

### ada83.c:10909

```
// RM 12.3: within its own region a generic subprogram's name denotes the
```

### ada83.c:10910

```
// current instance, whose profile is the unit's own — a call giving more
```

### ada83.c:10911

```
// arguments than that profile takes cannot denote it, and keeping it
```

### ada83.c:10912

```
// would outscore a use-visible overload that does fit (c83030c: F3(A)
```

### ada83.c:10913

```
// under a parameterless generic F3 must leave only F3(INTEGER)).
```

### ada83.c:10927

```
// Non-callable symbols don't filter by arguments
```

### ada83.c:10933

```
// Keep if arguments match
```

### ada83.c:10941

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:10942

```
// §11.6.5 Disambiguation                                                                           
```

### ada83.c:10943

```
//                                                                                                  
```

### ada83.c:10944

```
// Nearer scope, exact type match, and user definitions all take priority.                         
```

### ada83.c:10945

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:10947

```
// Check if sym1 hides sym2 (user-defined hiding predefined, or inner scope)
```

### ada83.c:10951

```
// RM 8.3(15): a user-declared overloadable symbol hides an implicit
```

### ada83.c:10952

```
// (predefined or derived-predefined) homograph at any scope, not just
```

### ada83.c:10953

```
// level 0. `TYPE T IS NEW INTEGER; FUNCTION "+" (X, Y : T) RETURN T`
```

### ada83.c:10954

```
// in c74210a — both live at P's scope but the user "+" hides the
```

### ada83.c:10955

```
// implicit predefined "+" for T; the earlier level-0-only guard let
```

### ada83.c:10956

```
// the predefined variant win at same-scope resolution.
```

### ada83.c:10962

```
// Inner scope hides outer scope
```

### ada83.c:10969

```
// Score an interpretation for preference ranking (higher = better)
```

### ada83.c:10976

```
// Prefer non-universal interpretations
```

### ada83.c:10981

```
// Prefer exact context type match
```

### ada83.c:10986

```
// Prefer immediately visible over USE-visible (RM 8.4).
```

### ada83.c:10987

```
// Derived type operations are immediately visible while parent
```

### ada83.c:10988

```
// operations via USE clause are use-visible.
```

### ada83.c:10989

```
//
```

### ada83.c:10994

```
// Prefer inner scopes (user-defined over predefined)
```

### ada83.c:10997

```
// For functions: prefer exact argument type matches
```

### ada83.c:11003

```
// Exact match is better than just coverage
```

### ada83.c:11014

```
// Select the best interpretation from a list
```

### ada83.c:11020

```
// Score all interpretations
```

### ada83.c:11031

```
// Check hiding rules
```

### ada83.c:11041

```
// If still tied, check for universal vs specific preference
```

### ada83.c:11042

```
// Prefer interpretation matching context exactly
```

### ada83.c:11050

```
// Prefer non-universal
```

### ada83.c:11060

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:11061

```
// §11.6.6 Unified Overload Resolution Entry Point                                                  
```

### ada83.c:11062

```
//                                                                                                  
```

### ada83.c:11063

```
// Collect, filter, disambiguate, fail if not unique.                                              
```

### ada83.c:11064

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:11071

```
// Phase 1: Collect all visible interpretations
```

### ada83.c:11074

```
// No visible interpretation
```

### ada83.c:11077

```
// Phase 2: Filter by argument compatibility
```

### ada83.c:11081

```
// No matching profile
```

### ada83.c:11085

```
// Phase 3: Apply context type filtering if provided.
```

### ada83.c:11086

```
// Two-stage: prefer interpretations whose return type is the SAME base
```

### ada83.c:11087

```
// (subtypes of one named type) over loose Type_Covers matches (which
```

### ada83.c:11088

```
// unify derived types with their parents via Type_Root). For
```

### ada83.c:11089

```
// `W : WHOLE := F1(0,0)` where WHOLE is `NEW INTEGER`, F1 overloads
```

### ada83.c:11090

```
// returning INTEGER and WHOLE both Type_Cover-match WHOLE, but only
```

### ada83.c:11091

```
// the WHOLE-returning one is implicitly assignable (RM 6.6).
```

### ada83.c:11114

```
// If no matches, keep all for better error reporting
```

### ada83.c:11117

```
// Phase 4: Disambiguate if multiple interpretations remain
```

### ada83.c:11121

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:11122

```
// §11.7 Symbol Manager Initialization                                                              
```

### ada83.c:11123

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:11127

```
// Create predefined types
```

### ada83.c:11134

```
// 32-bit - matches GNAT and standard Ada convention
```

### ada83.c:11139

```
// double precision
```

### ada83.c:11140

```
// IEEE double: ~15 decimal digits
```

### ada83.c:11147

```
// Fat pointer: ptr + length
```

### ada83.c:11150

```
// STRING's index type is POSITIVE (RM 3.6.3). Wire it into the type
```

### ada83.c:11151

```
// system so Array_Bound_LLVM_Rep can derive the bound type from the
```

### ada83.c:11152

```
// index, exactly as Standard's Bound_Sub_GT is derived from the index
```

### ada83.c:11153

```
// subtype's base type (see gnatllvm-arrays-create.adb).
```

### ada83.c:11154

```
// NOTE: type_positive is allocated below; we back-patch after it exists.                        
```

### ada83.c:11155

```
//                                                                                                
```

### ada83.c:11158

```
// 64-bit for high precision
```

### ada83.c:11159

```
// 10 microsecond resolution
```

### ada83.c:11161

```
// DURATION's range is implementation-defined (RM 9.6) but must exist: with
```

### ada83.c:11162

```
// no bounds, 'MANTISSA has no range to size, 'FIRST/'LAST/'LARGE read
```

### ada83.c:11163

```
// nothing, and every run-time range check tests against an empty interval.
```

### ada83.c:11164

```
// Use the model range of a 52-bit mantissa at this SMALL — ±(2**52 - 1) *
```

### ada83.c:11165

```
// 1.0E-5, about ±4.5E13 seconds — which comfortably contains CALENDAR's
```

### ada83.c:11166

```
// TIME span (±2E11 seconds around the epoch) and keeps 'MANTISSA * 'SMALL
```

### ada83.c:11167

```
// arithmetic inside 64-bit integers (cc1223a computes 2 ** 'MANTISSA - 1
```

### ada83.c:11168

```
// at run time).
```

### ada83.c:11178

```
// 32 bits - same as INTEGER (RM 3.5.4)
```

### ada83.c:11181

```
// 64 bits / double precision
```

### ada83.c:11183

```
// Add predefined type symbols to global scope
```

### ada83.c:11192

```
// NATURAL is subtype INTEGER range 0..INTEGER'LAST
```

### ada83.c:11203

```
// POSITIVE is subtype INTEGER range 1..INTEGER'LAST
```

### ada83.c:11214

```
// Back-patch STRING's index type to POSITIVE (deferred from above).
```

### ada83.c:11215

```
// This makes Array_Bound_LLVM_Rep derive STRING's bound type from
```

### ada83.c:11216

```
// POSITIVE's base type (INTEGER), matching Standard's Bound_Sub_GT.
```

### ada83.c:11217

```
//                                                                                                
```

### ada83.c:11242

```
// Boolean literals
```

### ada83.c:11249

```
// TRUE has position 1 in BOOLEAN (RM 3.5.3)
```

### ada83.c:11252

```
// Predefined exceptions (RM 11.1)
```

### ada83.c:11264

```
// SYSTEM.ADDRESS (RM 13.7) - implementation-defined private type.                               
```

### ada83.c:11265

```
// In our implementation, ADDRESS is a 64-bit integer type.                                      
```

### ada83.c:11266

```
// Bounds cover full i64 range so ptrtoint values always pass checks.                            
```

### ada83.c:11267

```
//                                                                                                
```

### ada83.c:11269

```
// 64-bit addresses
```

### ada83.c:11274

```
// STANDARD package (RM 8.6) - the implicit library containing predefined types
```

### ada83.c:11275

```
// All visible entities are implicitly declared here
```

### ada83.c:11279

```
// RM 8.6: STANDARD owns the global scope
```

### ada83.c:11282

```
// ASCII package (RM C.3) - predefined character constants
```

### ada83.c:11283

```
// In Ada 83, ASCII is a package in STANDARD, always visible
```

### ada83.c:11287

```
// Child of STANDARD
```

### ada83.c:11290

```
// STANDARD exports its predefined declarations (RM C): ASCII and the
```

### ada83.c:11291

```
// predefined types — the exported list feeds selected-component
```

### ada83.c:11292

```
// resolution and its predefined-operator synthesis, so STANDARD."NOT"
```

### ada83.c:11293

```
// finds BOOLEAN and STANDARD."&" finds STRING (c86006a).
```

### ada83.c:11303

```
// ASCII control characters and named constants
```

### ada83.c:11311

```
// Named punctuation
```

### ada83.c:11318

```
// Lowercase letters
```

### ada83.c:11333

```
// Store char value
```

### ada83.c:11334

```
// Treat as compile-time constant
```

### ada83.c:11335

```
// Deliberately NOT Symbol_Add'ed: ASCII's members are visible only as
```

### ada83.c:11336

```
// ASCII.X or under USE ASCII (RM C.3) — both resolve through the
```

### ada83.c:11337

```
// exported list. Installing them as bare names shadowed user library
```

### ada83.c:11338

```
// units that legally reuse the names (c86005a: PACKAGE NUL, ENQ, LF).
```

### ada83.c:11342

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:11343

```
// Predefined Operators (RM 4.5) - Needed for operator renaming                                   
```

### ada83.c:11344

```
//                                                                                                
```

### ada83.c:11345

```
// Per LRM 4.5.3-4.5.6, predefined operators exist for all numeric types.                        
```

### ada83.c:11346

```
// We add symbols for these so RENAMES "+" etc. can resolve them.                                
```

### ada83.c:11347

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:11353

```
// unary +/- (RM 4.5.4)
```

### ada83.c:11395

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:11396

```
// §12. SEMANTIC ANALYSIS - Type Checking and Resolution                                            
```

### ada83.c:11397

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:11400

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:11401

```
// §12.1 Expression Resolution                                                                      
```

### ada83.c:11402

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:11404

```
// True when a call with no arguments matches this subprogram's profile: it
```

### ada83.c:11405

```
// has no parameters, or every parameter has a default (RM 6.4).
```

### ada83.c:11414

```
// RM 3.4 / 9.1: a derived task type shares its parent's entries and body — its
```

### ada83.c:11415

```
// entry symbols are not redeclared, they live in the root task type's defining
```

### ada83.c:11416

```
// symbol. Walk the derivation chain to the task type that actually declares
```

### ada83.c:11417

```
// them so `X.E` and `X.E(...)` resolve for an object of a derived task type.
```

### ada83.c:11425

```
// RM 8.5.1: renaming an entry whose task prefix is an overloaded parameterless
```

### ada83.c:11426

```
// function — `ENTRY2 (J1, J2) renames F.ENTER` where F returns T1 or T2 — must
```

### ada83.c:11427

```
// use the rename's OWN parameter profile to choose which F, hence which task
```

### ada83.c:11428

```
// type, hence which ENTER. Ordinary resolution of F.ENTER has no such context
```

### ada83.c:11429

```
// and takes the first F. Select the interpretation of F whose result task type
```

### ada83.c:11430

```
// has an entry named by the selector with a matching profile; set the prefix's
```

### ada83.c:11431

```
// symbol and type to that function and return the chosen entry. Returns NULL
```

### ada83.c:11432

```
// when the pattern does not apply, leaving ordinary resolution to run.
```

### ada83.c:11441

```
// not overloaded; ordinary path suffices
```

### ada83.c:11443

```
// RM 8.5.1 requires type conformance: same parameter count and, per
```

### ada83.c:11444

```
// parameter, the same BASE type — so an ENTER (I1 : INT) rename picks T2's
```

### ada83.c:11445

```
// entry over T1's ENTER (I1 : INTEGER), which a looser numeric-compatibility
```

### ada83.c:11446

```
// test would wrongly accept as well.
```

### ada83.c:11468

```
// RM 8.5.1 type conformance: same base type. Subtypes conform; a
```

### ada83.c:11469

```
// derived type and its parent have distinct bases (the base_type link
```

### ada83.c:11470

```
// never crosses a derivation boundary) and so do not.
```

### ada83.c:11484

```
// RM 9.1: only the ancestor task type that declares the task body owns the
```

### ada83.c:11485

```
// emitted body function; a derived task type reuses it. Walk to that ancestor
```

### ada83.c:11486

```
// so both the body definition and every create/activate call site name the
```

### ada83.c:11487

```
// same function.
```

### ada83.c:11497

```
// RM 4.1.3 / 8.3: F.X where F names an ENCLOSING subprogram (we are inside F's
```

### ada83.c:11498

```
// body) is an expanded name selecting the entity X declared immediately within
```

### ada83.c:11499

```
// F — F is NOT called. This must be decided before resolving the prefix as a
```

### ada83.c:11500

```
// value: a self-referential default such as `Z : INTEGER := F.X;` inside F
```

### ada83.c:11501

```
// would otherwise be read as (call F).X and recurse forever.
```

### ada83.c:11502

```
//
```

### ada83.c:11503

```
// The exception (RM 8.3 hiding): if an inner declaration of F, declared within
```

### ada83.c:11504

```
// the enclosing F, is a homograph that hides it AND returns a record with the
```

### ada83.c:11505

```
// selected component, then F denotes that inner function and F.X IS a call —
```

### ada83.c:11506

```
// leave it to normal resolution. An inner F with a different result profile
```

### ada83.c:11507

```
// merely overloads (does not hide), so the enclosing F still denotes the
```

### ada83.c:11508

```
// current instance and F.X is the expanded name.
```

### ada83.c:11517

```
// Only intervene where normal resolution would misfire: the enclosing F
```

### ada83.c:11518

```
// returns a record whose component matches the selector, so `F.X` would
```

### ada83.c:11519

```
// be read as (call F).X — a recursive self-call. Expanded names with any
```

### ada83.c:11520

```
// other prefix (a procedure, or a function whose result lacks X — e.g.
```

### ada83.c:11521

```
// Proc.Enum_Literal) are already handled correctly downstream; leave them.
```

### ada83.c:11553

```
// Enclosing unit named F either declares no matching entity, or is hidden
```

### ada83.c:11554

```
// by an inner homograph — fall through to normal resolution.
```

### ada83.c:11559

```
// Resolve prefix first
```

### ada83.c:11562

```
// Strip quotes from operator selectors: P."/=" > P./= (RM 6.1)                                   
```

### ada83.c:11563

```
// The parser stores string-form operators with quotes, but                                       
```

### ada83.c:11564

```
// the symbol table stores them without quotes.                                                  
```

### ada83.c:11565

```
//                                                                                                
```

### ada83.c:11573

```
// Handle .ALL for explicit dereference (RM 4.1)
```

### ada83.c:11580

```
// Get effective type for record component lookup (handle implicit dereference)
```

### ada83.c:11585

```
// Unwrap private/derived types to reach the underlying record (RM 7.4.1).                       
```

### ada83.c:11586

```
// Generic formal private types with known discriminants like                                     
```

### ada83.c:11587

```
// TYPE PRIV (D : T) IS PRIVATE have parent_type pointing to the actual                           
```

### ada83.c:11588

```
// record type after instantiation.                                                              
```

### ada83.c:11589

```
//                                                                                                
```

### ada83.c:11594

```
// A constrained subtype of an incomplete type keeps its INCOMPLETE kind
```

### ada83.c:11595

```
// but records the completed base — via parent_type when derived, or
```

### ada83.c:11596

```
// base_type when a constrained subtype (ACCESS CONS(1), RM 3.8.1).
```

### ada83.c:11600

```
// A record with no components of its own — e.g. a constrained subtype
```

### ada83.c:11601

```
// (ACCESS CONS(1)) created while CONS was an incomplete type and only
```

### ada83.c:11602

```
// completed afterwards — delegates component lookup to the base/parent
```

### ada83.c:11603

```
// record that actually carries them (RM 3.8.1).
```

### ada83.c:11611

```
// Also check TYPE_PRIVATE types with known discriminants (RM 12.1.2).
```

### ada83.c:11612

```
// These have discriminant components stored in the record sub-struct.
```

### ada83.c:11617

```
// Look up component
```

### ada83.c:11619

```
// Find_Record_Component also resolves a formal private type's
```

### ada83.c:11620

```
// discriminant names inside a generic (RM 12.1, c83027c). Record the
```

### ada83.c:11621

```
// slot on the node — under a formal/actual name collision the emitter
```

### ada83.c:11622

```
// cannot re-derive it from the selector (cc3240a).
```

### ada83.c:11632

```
// RM 8.6: the selector participates in resolving an overloaded CALL
```

### ada83.c:11633

```
// prefix — F(args).IB denotes the F whose result record carries IB, so
```

### ada83.c:11634

```
// a bottom-up commit to a different overload re-resolves against the
```

### ada83.c:11635

```
// one that fits (c87b37d: F(5<5).IB picks F(INTEGER) RETURN REC2 and
```

### ada83.c:11636

```
// re-drives 5<5 as the user "<" returning INTEGER).
```

### ada83.c:11682

```
// Task entry selection: T.E1 where T is a task object (RM 9.5).
```

### ada83.c:11683

```
// Also handles P.E1 where P is access-to-task (implicit dereference).
```

### ada83.c:11684

```
// record_type was unwrapped from private/incomplete by the prior loop,
```

### ada83.c:11685

```
// so this catches LIMITED PRIVATE types whose full view is a task type.
```

### ada83.c:11689

```
// The prefix may be a use-visible alias of the task (RM 8.4) — codegen
```

### ada83.c:11690

```
// reads the TCB through the prefix symbol's emitted name, which only the
```

### ada83.c:11691

```
// denoted object symbol carries (c73007a: TASK1.FUNC1 under USE PACK).
```

### ada83.c:11706

```
// RM 4.1.3: within the task body, an expanded name T.X denotes an
```

### ada83.c:11707

```
// entity declared in the body itself — the idiom for reaching a
```

### ada83.c:11708

```
// task-local variable shadowed by an accept formal (cc3120b's
```

### ada83.c:11709

```
// `T1.I := I` inside ACCEPT PUT (I : ...)). Applies only when the
```

### ada83.c:11710

```
// task's body scope encloses the point of use. A task TYPE's body
```

### ada83.c:11711

```
// scope hangs on the type symbol; a SINGLE task's on the object
```

### ada83.c:11712

```
// symbol the body resolved under — consult both.
```

### ada83.c:11746

```
// Could be qualified name - look up in prefix's exported/visible symbols.                       
```

### ada83.c:11747

```
// Per RM 4.1.3, qualified names can use package, procedure, or function                          
```

### ada83.c:11748

```
// as prefix to access items declared within that scope.                                         
```

### ada83.c:11749

```
//                                                                                                
```

### ada83.c:11753

```
// RM 8.5: a package renaming denotes the SAME package — peel to the
```

### ada83.c:11754

```
// original so lookups see the complete, live export list (c85011a).
```

### ada83.c:11757

```
// RM 12.3: within a generic unit, the unit's name denotes the current
```

### ada83.c:11758

```
// instance — GEN.X inside GEN's own template resolves X exactly as a
```

### ada83.c:11759

```
// direct name in the current scope (c94008c: SHARED.SET called from
```

### ada83.c:11760

```
// within generic package SHARED's own body). The same applies while an
```

### ada83.c:11761

```
// INSTANCE body clone is being re-resolved: the enclosing scope is then
```

### ada83.c:11762

```
// owned by the instance symbol, whose template back-link names the
```

### ada83.c:11763

```
// prefix.
```

### ada83.c:11772

```
// The expanded name denotes an entity of the UNIT's own region —
```

### ada83.c:11773

```
// its scope's bucket chain first (c94008c: SHARED.SET inside the
```

### ada83.c:11774

```
// task body must find the package's SET, not the task's entry SET
```

### ada83.c:11775

```
// that plain visibility would surface) — with full-visibility
```

### ada83.c:11776

```
// lookup as the fallback for enclosing-scope entities.
```

### ada83.c:11794

```
// Search package's exported symbols
```

### ada83.c:11796

```
// A use-visible package alias carries neither scope nor exported list
```

### ada83.c:11797

```
// of its own — the expanded name denotes the ORIGINAL package
```

### ada83.c:11798

```
// (RM 8.4; c73007a: PACK1.FUNC1 with PACK1 visible via USE PACK).
```

### ada83.c:11805

```
// For function symbols, the expression type is the return
```

### ada83.c:11806

```
// type (RM 4.1.3). sym->type may be NULL for locally
```

### ada83.c:11807

```
// declared functions where only return_type is set.
```

### ada83.c:11808

```
//
```

### ada83.c:11818

```
// Also search the package's scope for predefined operators and                               
```

### ada83.c:11819

```
// other symbols not explicitly in the exported list (RM 4.1.3).                             
```

### ada83.c:11820

```
// This handles P."/=", P."=", P."<" etc. for predefined operators                            
```

### ada83.c:11821

```
// of types declared in the package.                                                         
```

### ada83.c:11822

```
//                                                                                            
```

### ada83.c:11837

```
// Inherited enum literals for derived types (RM 3.4(12)).                                   
```

### ada83.c:11838

```
// When a package has TYPE T IS NEW BOOLEAN, P.FALSE and P.TRUE                               
```

### ada83.c:11839

```
// must resolve to literals of T. Find a global literal matching                              
```

### ada83.c:11840

```
// the selector name whose type is a parent of some exported type.                           
```

### ada83.c:11841

```
//                                                                                            
```

### ada83.c:11857

```
// Check if exported type == or derives from literal's type.                         
```

### ada83.c:11858

```
// Walk both parent_type and base_type chains at each level                           
```

### ada83.c:11859

```
// to handle anonymous intermediate types.                                           
```

### ada83.c:11860

```
//                                                                                    
```

### ada83.c:11878

```
// Try parent_type first, then base_type
```

### ada83.c:11889

```
// Synthesize predefined operators for types declared in the package                          
```

### ada83.c:11890

```
// (RM 4.5). Every type implicitly declares =, /=, and for ordered                            
```

### ada83.c:11891

```
// types also <, <=, >, >=. Numeric types add +, -, *, /, etc.                               
```

### ada83.c:11892

```
// We lazily create these symbols when first referenced via P."op".                          
```

### ada83.c:11893

```
//                                                                                            
```

### ada83.c:11903

```
// Find first type in the package's exports
```

### ada83.c:11910

```
// All types have = /= < etc
```

### ada83.c:11922

```
// Create a synthetic predefined operator symbol
```

### ada83.c:11932

```
// Install in package scope for future lookups
```

### ada83.c:11947

```
// For procedure/function prefix, search the subprogram's scope.
```

### ada83.c:11948

```
// This handles cases like MAIN.A_B_C where MAIN is a procedure
```

### ada83.c:11949

```
// and A_B_C is an enum literal or type declared within it.
```

### ada83.c:11950

```
//
```

### ada83.c:11953

```
// A renaming prefix — most importantly the RM 12.1(7) self-binding a
```

### ada83.c:11954

```
// subprogram instance installs under its template's name — denotes
```

### ada83.c:11955

```
// the renamed subprogram; the scope test below must see that one.
```

### ada83.c:11970

```
// RM 4.1.3(18): inside the named subprogram's own body, the expanded
```

### ada83.c:11971

```
// name denotes what the simple name denotes — including entities in a
```

### ada83.c:11972

```
// scope wrapping the body, such as a generic instance's formal
```

### ada83.c:11973

```
// bindings (M.S inside an instance of generic procedure M, c41307d).
```

### ada83.c:11990

```
// RM 4.1.3(20): within an accept statement, the entry name prefixes its
```

### ada83.c:11991

```
// own formals — E.A denotes accept formal A (c41307d). The entry symbol
```

### ada83.c:11992

```
// carries the live accept scope only while that accept's body resolves.
```

### ada83.c:12006

```
// RM 8.3: a block label denotes its block as a declarative region.
```

### ada83.c:12007

```
// LABEL.X resolves X in the block's scope (only symbols declared
```

### ada83.c:12008

```
// directly in that block, not inherited from enclosing scopes).
```

### ada83.c:12009

```
//
```

### ada83.c:12013

```
// Two passes: an expression-position selector denotes the block's
```

### ada83.c:12014

```
// ENTITY, not a statement label that happens to share its name
```

### ada83.c:12015

```
// (c83033a: B1.RED with both a derived RED and a <<RED>> label) — a
```

### ada83.c:12016

```
// label matters only as a goto target, so it is the fallback.
```

### ada83.c:12031

```
// "Did you mean": when the prefix is a package, suggest the closest
```

### ada83.c:12032

```
// exported name; otherwise nothing useful to scan.
```

### ada83.c:12047

```
// Error recovery
```

### ada83.c:12050

```
// Get the operator name string for a token kind: the bare (unquoted)
```

### ada83.c:12051

```
// designator from the operator table, or "" for non-operator tokens.
```

### ada83.c:12070

```
// Resolve a character literal as an enumeration literal given a context type.
```

### ada83.c:12071

```
// Used for comparisons and assignments where a character literal should be
```

### ada83.c:12072

```
// interpreted as an enumeration value. Returns true if resolved.
```

### ada83.c:12073

```
//
```

### ada83.c:12078

```
// Find base enumeration type by following both parent_type and base_type chains.
```

### ada83.c:12079

```
// parent_type is used for derived types (TYPE T IS NEW X)
```

### ada83.c:12080

```
// base_type is used for constrained subtypes (SUBTYPE S IS X RANGE ...)
```

### ada83.c:12081

```
//
```

### ada83.c:12094

```
// Get the character from the literal (format: 'X')
```

### ada83.c:12098

```
// Look for matching character literal in enum
```

### ada83.c:12102

```
// Found matching enum literal - find symbol with matching type
```

### ada83.c:12113

```
// RM 6.6 helper: for a binary-op or function-call argument expression
```

### ada83.c:12114

```
// whose name is overloaded, collect an alternative return type that
```

### ada83.c:12115

```
// differs from the one picked by single-pass resolution. The outer
```

### ada83.c:12116

```
// overload-resolution can then test the call against both the primary
```

### ada83.c:12117

```
// and the alternative, yielding a unique combination when one exists.
```

### ada83.c:12118

```
//
```

### ada83.c:12119

```
// Returns NULL if there's no alternative (single interpretation, or
```

### ada83.c:12120

```
// caller isn't overloaded, or sub-resolution failed).
```

### ada83.c:12124

```
// Binary operator: try each interpretation of `op` and look for a
```

### ada83.c:12125

```
// return type that differs from arg->type. Operand types are already
```

### ada83.c:12126

```
// set by the single-pass resolve; also carry each operand's own alt type
```

### ada83.c:12127

```
// so a two-deep overload chain (P (A op B) with A or B overloaded) can
```

### ada83.c:12128

```
// still see the alternate return type of the operator.
```

### ada83.c:12151

```
// Function call: try each visible overload of the prefix name and
```

### ada83.c:12152

```
// return the first return type that differs from arg->type. Only when the
```

### ada83.c:12153

```
// name actually denotes a callable: if its innermost binding is an array
```

### ada83.c:12154

```
// object then A (I) is an indexed component, and treating it as a call to A
```

### ada83.c:12155

```
// would offer A's array type as a spurious alternative (RM 8.3).
```

### ada83.c:12185

```
// Recurse: a sub-arg that is itself an overloadable expression
```

### ada83.c:12186

```
// (F(...), an operator, a bare identifier) may satisfy the outer
```

### ada83.c:12187

```
// filter through an alternate return type, not only its greedy pick.
```

### ada83.c:12201

```
// Bare identifier: an enum literal that is homograph across sibling enum
```

### ada83.c:12202

```
// types (or a zero-argument overloaded function) exposes its non-greedy
```

### ada83.c:12203

```
// return type here so the outer overload filter can pick it.
```

### ada83.c:12219

```
// How the result of a nested call F(args) is consumed by the construct that
```

### ada83.c:12220

```
// wraps it, so overload resolution can flow the outer context inward (RM 8.6).
```

### ada83.c:12223

```
// Same nominal type modulo subtype constraint: exact identity, or a subtype in
```

### ada83.c:12224

```
// either direction (base_type one hop) that is NOT a NEW-derivation or generic
```

### ada83.c:12225

```
// actual view. Type_Covers is too permissive here — it treats NEW_CHARACTER and
```

### ada83.c:12226

```
// CHARACTER as interchangeable, which would let F returning an array of
```

### ada83.c:12227

```
// CHARACTER satisfy a NEW_CHARACTER target (c87b37c #2).
```

### ada83.c:12230

```
// The nominal base strips subtype constraints (follow base_type) but stops at
```

### ada83.c:12231

```
// a NEW-derived type, which carries a parent_type and is distinct from its
```

### ada83.c:12232

```
// parent. Two subtypes of one type share a nominal base (NEW_STR(1..2) and
```

### ada83.c:12233

```
// NEW_STR), while NEW_STR stays distinct from STRING and NEW_CHAR from
```

### ada83.c:12234

```
// CHARACTER. A generic actual view keeps its own identity (its base_type
```

### ada83.c:12235

```
// names the instantiation actual, not a subtype base).
```

### ada83.c:12243

```
// Does F's result, consumed as `use`, denote a value of ctx's type? For INDEX
```

### ada83.c:12244

```
// the (implicitly dereferenced) array's element; for SLICE the array itself;
```

### ada83.c:12245

```
// for DEREF the access's designated type — each must be ctx's nominal type.
```

### ada83.c:12260

```
// Re-select the overload of a nested call F(args) whose result — consumed as an
```

### ada83.c:12261

```
// index/slice/dereference — fits the outer context ctx, and re-resolve its
```

### ada83.c:12262

```
// arguments against the chosen formals. RM 8.6: the whole expression's context
```

### ada83.c:12263

```
// disambiguates the nested call (c87b37c/e: F(5<5)(1) and F(5<5).ALL pick the F
```

### ada83.c:12264

```
// overload whose array/access result matches the target, which in turn fixes
```

### ada83.c:12265

```
// `5<5` to predefined `<` (BOOLEAN) or the user `<` (INTEGER)). Returns true if
```

### ada83.c:12266

```
// the pick changed. A no-op unless F is an overloaded function whose current
```

### ada83.c:12267

```
// pick does not already satisfy ctx.
```

### ada83.c:12304

```
// Prefer an overload matched by the arguments' primary types (no implicit
```

### ada83.c:12305

```
// conversion) over one matched only through an alternate type (RM 8.7).
```

### ada83.c:12322

```
// RM 6.7 / 4.5: rebuild an operator applied in function-call notation —
```

### ada83.c:12323

```
// `"+"(A,B)`, `"<"(LEFT=>A, RIGHT=>B)`, `"abs"(RIGHT => X)` — as the
```

### ada83.c:12324

```
// equivalent infix/prefix operator node. A unary operator's sole parameter
```

### ada83.c:12325

```
// is named RIGHT (RM 6.1), so RIGHT maps to the only operand of a
```

### ada83.c:12326

```
// one-argument call and to the second of a two-argument call. Returns NULL
```

### ada83.c:12327

```
// when the operands do not form a complete operator application. The
```

### ada83.c:12328

```
// synthesized node inherits the apply's context type.
```

### ada83.c:12360

```
// applicable context supplied by the caller
```

### ada83.c:12362

```
// First, resolve all arguments to get their types
```

### ada83.c:12374

```
// Handle named associations: Name => Value
```

### ada83.c:12381

```
// Resolve the value expression
```

### ada83.c:12387

```
// Positional
```

### ada83.c:12389

```
// Defer aggregate resolution: aggregates need parameter type
```

### ada83.c:12390

```
// context for record component names (RM 4.3, 6.4). They'll
```

### ada83.c:12391

```
// be resolved after the callable is identified.
```

### ada83.c:12392

```
//
```

### ada83.c:12409

```
// Resolve the prefix
```

### ada83.c:12412

```
// call-and-index selected the index reading
```

### ada83.c:12414

```
// For identifier prefix, use overload resolution.
```

### ada83.c:12415

```
// Pass node->type as context so call returns may be filtered by the
```

### ada83.c:12416

```
// expected result type (RM 6.6: function-return overload disambiguation
```

### ada83.c:12417

```
// by context — e.g. `W : WHOLE := F1(0,0)` picks the F1 overload
```

### ada83.c:12418

```
// whose return type is WHOLE when several F1's have matching arg
```

### ada83.c:12419

```
// profiles but differing returns).
```

### ada83.c:12423

```
// RM 8.3: an inner object declaration hides an outer homograph subprogram.
```

### ada83.c:12424

```
// If the innermost binding of the name is an (access-to-)array object,
```

### ada83.c:12425

```
// `A (I)` is an indexed component or slice — possibly through an implicit
```

### ada83.c:12426

```
// dereference (RM 4.1(3)) — not a call, so overload resolution must not
```

### ada83.c:12427

```
// reach the hidden outer subprogram.
```

### ada83.c:12440

```
// Call-and-index: F(I) where F is a parameterless function whose result is
```

### ada83.c:12441

```
// an array (RM 4.1.3, 4.1). When F is overloaded across zero-arg variants
```

### ada83.c:12442

```
// returning different array types, RM 4.1.1 says the element type, the
```

### ada83.c:12443

```
// index type, and the number of indices are ALL disambiguators. Rank
```

### ada83.c:12444

```
// candidates: exact element match + all indices type-cover the arg types
```

### ada83.c:12445

```
// beats element-only match beats fallback.
```

### ada83.c:12452

```
// F may take arguments that all default (RM 6.4.2): F(I) is then an
```

### ada83.c:12453

```
// index of the defaulted call's array result, F()(I), when no direct
```

### ada83.c:12454

```
// call fits (c87b37f). A parameterless F is the common case.
```

### ada83.c:12463

```
// A parameterless F(I) is unambiguously an index, so the element need
```

### ada83.c:12464

```
// only be Type_Covers-compatible with the context. A defaulted F is
```

### ada83.c:12465

```
// ambiguous with a direct call, so require the element to be the
```

### ada83.c:12466

```
// context's nominal type — Type_Covers treats an array as covering its
```

### ada83.c:12467

```
// own element and would misread F(5>5) as an index for a BOOL_BOOL
```

### ada83.c:12468

```
// target where the direct call F(5>5)->BOOL_BOOL is meant (c87b37f).
```

### ada83.c:12471

```
// element type incompatible with ctx
```

### ada83.c:12474

```
// exact element/context match
```

### ada83.c:12481

```
// A literal subscript is context-typed (RM 4.2, 8.6): the
```

### ada83.c:12482

```
// bottom-up pass committed it to SOME homonymous literal, but it
```

### ada83.c:12483

```
// fits this candidate whenever the index type declares (or
```

### ada83.c:12484

```
// inherits) a literal of that name (c87b23a: A (3, C, TRUE)
```

### ada83.c:12485

```
// across four instances whose index types are sibling
```

### ada83.c:12486

```
// enumerations and derived booleans).
```

### ada83.c:12512

```
// RM 6.7 / 4.5.2: operator in function-call notation — `"+"(A,B)` /
```

### ada83.c:12513

```
// `"<"(A,B)` / `"+"(LEFT=>A, RIGHT=>B)`. Re-resolve as the infix
```

### ada83.c:12514

```
// operator FIRST: the infix machinery weighs user-defined operators,
```

### ada83.c:12515

```
// predefined operators, and the universal interpretations together
```

### ada83.c:12516

```
// through one overload resolution — a by-name lookup commits to a
```

### ada83.c:12517

```
// user operator on profile match alone, which both misreads
```

### ada83.c:12518

```
// `"<"(ARR1, ARR2)` (predefined array "<" is not a named symbol)
```

### ada83.c:12519

```
// and violates the RM 4.6 conversion-free preference (c87b40a:
```

### ada83.c:12520

```
// `"-"(-1)` under a universal "<" must denote the predefined
```

### ada83.c:12521

```
// universal minus even though a user "-"(INTEGER) also fits).
```

### ada83.c:12526

```
// Probe the infix interpretation on a CLONE with diagnostics off:
```

### ada83.c:12527

```
// when the context does NOT force a concrete numeric type and the
```

### ada83.c:12528

```
// probe yields a UNIVERSAL result, the RM 4.6 conversion-free
```

### ada83.c:12529

```
// preference makes it the correct reading — the by-name lookup
```

### ada83.c:12530

```
// (which commits to a user operator on bare profile match) must
```

### ada83.c:12531

```
// not run (c87b40a: `"-"(-1)` under a universal "<"). Under a
```

### ada83.c:12532

```
// CONCRETE context the universal result — an expression, not a
```

### ada83.c:12533

```
// convertible operand (RM 4.6) — could never convert to it, so
```

### ada83.c:12534

```
// the user operator is the only legal reading and the by-name
```

### ada83.c:12535

```
// lookup decides (c87b02a's operator-symbol initializers).
```

### ada83.c:12557

```
// By-name lookup: a user-defined operator whose profile matches.
```

### ada83.c:12561

```
// Neither universal nor user-named: the infix machinery still
```

### ada83.c:12562

```
// owns the predefined operators that are not named symbols
```

### ada83.c:12563

```
// (array "<", equality on composites, ...).
```

### ada83.c:12569

```
// This apply IS the operator (RM 6.7): the emitter lowers it
```

### ada83.c:12570

```
// through the same predefined-operator path as `A op B`.
```

### ada83.c:12571

```
// Leaving it APPLY_UNRESOLVED would trip the codegen's
```

### ada83.c:12572

```
// verdict guard.
```

### ada83.c:12587

```
// Fall back to simple lookup for non-callable names
```

### ada83.c:12598

```
// For complex prefix (selected, etc.), resolve normally
```

### ada83.c:12603

```
// Expanded-name call L.F (...) through a BLOCK LABEL prefix: the label
```

### ada83.c:12604

```
// branch of Resolve_Selected likewise commits to the first member with no
```

### ada83.c:12605

```
// view of the actuals — redo across the block's homonyms (skipping
```

### ada83.c:12606

```
// statement labels) so the profile decides (c83033a: B1.RED(3, B1.RED)
```

### ada83.c:12607

```
// is the derived function; bare B1.RED the literal).
```

### ada83.c:12644

```
// Expanded-name call P.F (...): F may be overloaded within P, but
```

### ada83.c:12645

```
// Resolve_Selected commits to the first matching member with no view of
```

### ada83.c:12646

```
// the actuals. Redo overload resolution across P's members so the right
```

### ada83.c:12647

```
// profile wins, exactly as the simple-name path does (RM 4.1.3, 6.6).
```

### ada83.c:12666

```
// RM 8.3 / 6.7: P."op"(A,B) may denote a predefined operator of a type
```

### ada83.c:12667

```
// declared in P that exists as no named symbol — array "<", composite
```

### ada83.c:12668

```
// equality, ... — so the by-name member lookup above can only land on a
```

### ada83.c:12669

```
// homograph of a DIFFERENT type (BPACK."/=" over T9A arrays finds the
```

### ada83.c:12670

```
// derived boolean's "/="). When the pick's profile does not cover the
```

### ada83.c:12671

```
// actuals, the infix machinery owns the operator, exactly as in the
```

### ada83.c:12672

```
// unqualified operator-call path below (c83051a).
```

### ada83.c:12707

```
// Handle based on what the prefix resolves to
```

### ada83.c:12708

```
// ─── Case 1: Function/Procedure Call ───────────────────────────────────────────────────────────
```

### ada83.c:12712

```
// Only treat as a call if prefix is an identifier referring to a callable.                    
```

### ada83.c:12713

```
// If prefix is a complex expression (e.g., func(...)), it's already resolved                   
```

### ada83.c:12714

```
// and we should check its result type for indexing instead.                                   
```

### ada83.c:12715

```
//                                                                                              
```

### ada83.c:12719

```
// A parameterless function whose result is (an access to) an array, written
```

### ada83.c:12720

```
// F(...), is not a call with arguments but an index or slice of the
```

### ada83.c:12721

```
// implicitly called result (RM 4.1, 4.1.3). Let it fall through to Case 3.
```

### ada83.c:12725

```
// A parameterless array-returning function written F(...) is always an
```

### ada83.c:12726

```
// index of its result. A function with defaultable parameters is ambiguous
```

### ada83.c:12727

```
// — F(x) could call it with x, or default it and index by x — so it is an
```

### ada83.c:12728

```
// index only when the call-and-index pass chose that reading for the
```

### ada83.c:12729

```
// context (c87b37f: index for a BOOLEAN target, direct call for BOOL_BOOL).
```

### ada83.c:12737

```
// NULL for procedures
```

### ada83.c:12739

```
// A predefined operator named as a function ("+" (X, Y)) is an operator,
```

### ada83.c:12740

```
// not an ordinary call: the emitter lowers it through the same path as
```

### ada83.c:12741

```
// `X + Y`. Every other callable is a genuine call.
```

### ada83.c:12745

```
// Re-resolve arguments based on parameter types.                                            
```

### ada83.c:12746

```
// This handles:                                                                              
```

### ada83.c:12747

```
// - Character literals like FN ('A') where 'A' must be resolved as                           
```

### ada83.c:12748

```
//   the enumeration literal for the parameter's type, not ASCII.                            
```

### ada83.c:12749

```
// - Aggregates like FN ((1,2,3)) where the aggregate needs the                               
```

### ada83.c:12750

```
//   parameter type to determine its type (RM 4.3).                                          
```

### ada83.c:12751

```
//                                                                                            
```

### ada83.c:12752

```
// RM 8.5: through a renaming, formal NAMES belong to the view the call
```

### ada83.c:12753

```
// denotes, while formal CONSTRAINTS belong to the RENAMED entity — the
```

### ada83.c:12754

```
// renaming's own constraints are ignored (c85013a case B: PROC2's
```

### ada83.c:12755

```
// STA2(11..15) view over PROC1's STA1(1..5) types an aggregate actual
```

### ada83.c:12756

```
// as STA1, giving it bounds 1..5). Profiles correspond by position.
```

### ada83.c:12766

```
// A named association maps to the formal it NAMES, not the i-th one
```

### ada83.c:12767

```
// (RM 6.4.1) — a renaming may reorder or rename formals relative to
```

### ada83.c:12768

```
// the renamed subprogram (c85013a: PROCA(D => (5,4,3,2,1)) must type
```

### ada83.c:12769

```
// the aggregate by formal D, not by first-position C).
```

### ada83.c:12790

```
// Propagate type to aggregate arguments. RM 4.3: an aggregate takes
```

### ada83.c:12791

```
// its type from context alone — a type guessed during a context-free
```

### ada83.c:12792

```
// pre-pass (needed to filter overloads) is never authoritative, so
```

### ada83.c:12793

```
// retype whenever the formal does not cover what the aggregate wears.
```

### ada83.c:12800

```
// A string-literal actual is context-determined: fix its bounds from
```

### ada83.c:12801

```
// the formal's array type (RM 4.2(4)) even though bottom-up resolution
```

### ada83.c:12802

```
// already gave it the predefined STRING type. Re-resolve so an
```

### ada83.c:12803

```
// unconstrained formal whose index subtype does not start at 1 yields
```

### ada83.c:12804

```
// the right 'FIRST.
```

### ada83.c:12810

```
// RM 6.6: re-resolve overloadable-shape args (identifier / operator /
```

### ada83.c:12811

```
// call) with the formal parameter type as context so their overload
```

### ada83.c:12812

```
// pick prefers a covering match over whatever eager first-pass picked
```

### ada83.c:12813

```
// (e.g. `5<5` as a BOOLEAN formal picks predefined `<`; a bare
```

### ada83.c:12814

```
// overloaded identifier picks the homograph returning the formal type).
```

### ada83.c:12822

```
// context hint
```

### ada83.c:12823

```
// clear stale pick
```

### ada83.c:12828

```
// RM 4.5.5: a fixed-point "*"/"/" is declared with a universal_fixed
```

### ada83.c:12829

```
// result, but the value must be rescaled to a concrete fixed type. Infix
```

### ada83.c:12830

```
// resolution gives it the (non-universal) real operand's type; give the
```

### ada83.c:12831

```
// function-call form STANDARD."*"(X,Y) the same type, so the emitter
```

### ada83.c:12832

```
// rescales the mantissa and a surrounding conversion sees a concrete
```

### ada83.c:12833

```
// fixed subtype rather than the operator's nominal universal result.
```

### ada83.c:12847

```
// Task entry call: T.E (X). Like a procedure, it yields no value; the entry
```

### ada83.c:12848

```
// symbol rides on the prefix for the emitter's entry path. Classifying it
```

### ada83.c:12849

```
// here spares Generate_Apply the old fall-through guesswork that left entry
```

### ada83.c:12850

```
// applications wearing no verdict at all.
```

### ada83.c:12852

```
// Overloaded entries share a name; pick the overload whose parameter
```

### ada83.c:12853

```
// profile matches the call's arguments (RM 9.5), not merely the first
```

### ada83.c:12854

```
// found by name. Match on argument count and types.
```

### ada83.c:12873

```
// The emitter reads the entry off the call's prefix, so update it too.
```

### ada83.c:12879

```
// An entry call's actuals resolve against the entry's profile like a
```

### ada83.c:12880

```
// procedure's (RM 9.5 / 6.4): a flexible actual — an aggregate, a
```

### ada83.c:12881

```
// string, a character literal — takes the formal's type
```

### ada83.c:12882

```
// (c95085b: TSK1.E ((2,"AA"))). Named associations map by name.
```

### ada83.c:12914

```
// Entry FAMILY call with parameters: T.E (IDX) (ARGS) — the prefix is
```

### ada83.c:12915

```
// itself an entry-call application carrying only the family index (RM 9.5).
```

### ada83.c:12916

```
// Flatten to the emitter's convention: one argument list with the family
```

### ada83.c:12917

```
// index first, then the parameters, prefixed by the selected entry name.
```

### ada83.c:12930

```
// A named association's choice is a FORMAL name, not an expression —
```

### ada83.c:12931

```
// the entry-call slot mapper resolves it against the entry's profile
```

### ada83.c:12932

```
// (c85018a: T.ENT1 (TRUE) (A => 6)). Only the value resolves here.
```

### ada83.c:12946

```
// ─── Case 2: Type Conversion or Constrained Subtype ──────────────────────────────────────────
```

### ada83.c:12951

```
// Check for constrained subtype indication: STRING (1..5)
```

### ada83.c:12952

```
// Check if arguments are ranges (subtype indication) vs values (indexing)
```

### ada83.c:12963

```
// Constrained array/string subtype indication
```

### ada83.c:12969

```
// Element type
```

### ada83.c:12976

```
// Process index constraints
```

### ada83.c:12984

```
// Inherit index_type from base type (e.g., POSITIVE for STRING).
```

### ada83.c:12985

```
// This is critical for RM 4.5.3 concatenation length checks.
```

### ada83.c:12989

```
// Default fallback
```

### ada83.c:13005

```
// P'RANGE index constraint (RM 3.6.1): a constrained prefix
```

### ada83.c:13006

```
// supplies static bounds, an unconstrained one its value's
```

### ada83.c:13007

```
// 'FIRST and 'LAST (see Resolve_Range_Attr_Index).
```

### ada83.c:13013

```
// Compute size - only for fully static bounds
```

### ada83.c:13022

```
// Regular type conversion: Integer (X)
```

### ada83.c:13031

```
// ─── Case 2b: Predefined operator called via string syntax ─────────────────────────────────────
```

### ada83.c:13032

```
// Ada allows "+"(X, Y) or "&"(A, B) as equivalent to X + Y or A & B.                            
```

### ada83.c:13033

```
// Handle predefined operators that don't have explicit symbol entries.                          
```

### ada83.c:13034

```
// Note: The lexer strips quotes from operator strings, so "&" becomes just &.                   
```

### ada83.c:13035

```
//                                                                                                
```

### ada83.c:13039

```
// Provisional verdict: a bare operator name with no symbol is an operator.
```

### ada83.c:13040

```
// Every return below confirms it. If no operator matches and control falls
```

### ada83.c:13041

```
// through, the indexing case (Case 3) or the error tail overwrites this.
```

### ada83.c:13044

```
// Check for single-character operators (lexer stripped quotes)
```

### ada83.c:13048

```
// "&"(A, B) is array/string concatenation
```

### ada83.c:13055

```
// Result is the array type (prefer left if array, else right)
```

### ada83.c:13065

```
// Component & component: the result array type is supplied by the
```

### ada83.c:13066

```
// applicable context already seeded on this node (RM 4.5.3). Keep it.
```

### ada83.c:13068

```
// CHARACTER & CHARACTER with no array context: the predefined
```

### ada83.c:13069

```
// catenation for STRING applies (RM 4.5.3, component CHARACTER).
```

### ada83.c:13075

```
// Handle arithmetic operators
```

### ada83.c:13082

```
// Result is the numeric type (prefer left)
```

### ada83.c:13088

```
// Check for two-character operators like <=, >=, /=, **
```

### ada83.c:13094

```
// Comparison operators return BOOLEAN
```

### ada83.c:13104

```
// Exponentiation
```

### ada83.c:13114

```
// Unary operators with one argument
```

### ada83.c:13124

```
// Unary word operators: "NOT"(X), "ABS"(X) (RM 4.5.6, 4.5.7)
```

### ada83.c:13129

```
// NOT preserves boolean array type
```

### ada83.c:13142

```
// Binary word operators: "AND"/"OR"/"XOR"/"MOD"/"REM"(L,R) (RM 4.5)
```

### ada83.c:13157

```
// ─── Case 3: Array Indexing/Slicing (with implicit access dereference) ─────────────────────────
```

### ada83.c:13158

```
// Per RM 4.1(3), A(I) where A is access-to-array is equivalent to A.ALL(I)
```

### ada83.c:13161

```
// Implicit dereference
```

### ada83.c:13163

```
// Check if this is a slice (range argument) vs indexing (scalar argument)
```

### ada83.c:13173

```
// RM 8.6: flow the outer context into a nested overloaded call prefix, so
```

### ada83.c:13174

```
// the F overload whose array result is being indexed/sliced is chosen by
```

### ada83.c:13175

```
// the target type (c87b37c). Recompute the indexed type after re-selection.
```

### ada83.c:13184

```
// Slice: result type is the same array/string type
```

### ada83.c:13188

```
// Normalize a non-NK_RANGE slice argument to an explicit low .. high
```

### ada83.c:13189

```
// range so the slice emitter (which expects an NK_RANGE) sees a uniform
```

### ada83.c:13190

```
// shape. Without this such an argument falls through to the scalar-index
```

### ada83.c:13191

```
// path and slices at a single index. Three forms (RM 4.1.2):
```

### ada83.c:13192

```
//   P'RANGE                 -> P'FIRST .. P'LAST
```

### ada83.c:13193

```
//   subtype mark DT         -> DT'FIRST .. DT'LAST
```

### ada83.c:13194

```
//   DT RANGE L .. H         -> L .. H  (the constraint's own range)
```

### ada83.c:13197

```
// prefix to take 'FIRST/'LAST of
```

### ada83.c:13211

```
// RM 3.5: `S RANGE L..H` also requires L..H compatible with S. Carry
```

### ada83.c:13212

```
// the mark so the slice emitter checks it (the RM 4.1.2 check against
```

### ada83.c:13213

```
// the array's index subtype is emitted separately).
```

### ada83.c:13242

```
// The bounds pre-resolved without the index context; a bound that
```

### ada83.c:13243

```
// came out UNIVERSAL re-arbitrates against the index subtype — an
```

### ada83.c:13244

```
// operator in call notation must then pick the concrete-returning
```

### ada83.c:13245

```
// user operator (c87b24b: PI ("+" (3) .. "-" (5)) calls the
```

### ada83.c:13246

```
// INTEGER-returning user "+"/"-", not the universal predefined ones).
```

### ada83.c:13268

```
// Indexing: result type is the element type
```

### ada83.c:13275

```
// A character-literal subscript denotes the index enumeration's literal
```

### ada83.c:13276

```
// (its position), not the CHARACTER code (RM 4.1.3). The argument was
```

### ada83.c:13277

```
// resolved without index context above; rebind it now that the array
```

### ada83.c:13278

```
// shape is known, so the subscript folds to the right position.
```

### ada83.c:13285

```
// An identifier subscript naming a homonymous literal rebinds to
```

### ada83.c:13286

```
// the INDEX type's literal — positions differ between sibling
```

### ada83.c:13287

```
// enumerations (c87b23a).
```

### ada83.c:13302

```
// ─── Case 3b: Generic subprogram recursive call ────────────────────────────────────────────────
```

### ada83.c:13303

```
// Within a generic subprogram body the name of the subprogram denotes the current instance
```

### ada83.c:13304

```
// (recursive call).                                                       
```

### ada83.c:13305

```
//                                                                                                
```

### ada83.c:13309

```
// A recursive call naming the enclosing generic. The emitter folds it into
```

### ada83.c:13310

```
// the ordinary call path (redirecting to the current instance), so it wears
```

### ada83.c:13311

```
// the call verdict.
```

### ada83.c:13314

```
// Return type from the generic function spec
```

### ada83.c:13322

```
// Procedure call - no return type
```

### ada83.c:13327

```
// ─── Case 4: Unresolved - report error and recover ─────────────────────────────────────────────
```

### ada83.c:13329

```
// no verdict; this is the error tail
```

### ada83.c:13334

```
// Error recovery
```

### ada83.c:13337

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:13338

```
// Evaluate Constant Numeric Expression (for delta, bounds in type defs)                            
```

### ada83.c:13339

```
// Returns NaN if not a static constant expression                                                  
```

### ada83.c:13340

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:13344

```
// Returns true if the expression is integer-typed (for division semantics)
```

### ada83.c:13348

```
// Check if named number/constant is integer
```

### ada83.c:13359

```
// Check type if available
```

### ada83.c:13369

```
// For type conversions TYPE (arg), check if arg is integer
```

### ada83.c:13382

```
// Compile-time numeric value of a static expression, memoized on the node so the
```

### ada83.c:13383

```
// recursive constant fold runs once per subtree instead of being re-walked by
```

### ada83.c:13384

```
// every resolution and codegen query. Non-static results (NaN) are not cached.
```

### ada83.c:13400

```
// Character literals have position value in symbol->frame_offset
```

### ada83.c:13405

```
// Fallback: use ASCII code - character literals store their value in string_val.text
```

### ada83.c:13406

```
// Format is 'X' (length 3) - extract the middle character at index 1
```

### ada83.c:13415

```
// Check for character/enum literal first
```

### ada83.c:13423

```
// Named number or constant - evaluate via symbol's declaration
```

### ada83.c:13429

```
// Predefined named number (e.g. ASCII.DEL) - value in frame_offset.
```

### ada83.c:13430

```
// Only named numbers stash their value there; any other constant
```

### ada83.c:13431

```
// without a static initializer (e.g. a generic formal object, whose
```

### ada83.c:13432

```
// value arrives per instance) has no compile-time value.
```

### ada83.c:13438

```
// Variable with constant init (typed constants get variable-like alloc)
```

### ada83.c:13449

```
// Qualified expression: TYPE'(expr) - evaluate the inner expression
```

### ada83.c:13453

```
// Handle character literal inside qualified expression:
```

### ada83.c:13454

```
// look up in the qualifying type's enumeration literals
```

### ada83.c:13459

```
// Walk up to find the base enumeration type with literals
```

### ada83.c:13467

```
// Extract the character from 'X' format
```

### ada83.c:13472

```
// Position in enumeration
```

### ada83.c:13479

```
// Type conversions: TYPE_NAME (expr) - evaluate the argument
```

### ada83.c:13492

```
// T'SIZE, T'FIRST, T'LAST, T'POS, T'LENGTH etc.
```

### ada83.c:13513

```
// 'LENGTH is static only for a CONSTRAINED array subtype. For an
```

### ada83.c:13514

```
// unconstrained formal (X : STRING), the bounds live in the actual's
```

### ada83.c:13515

```
// fat pointer at run time — the type's own index range is the full
```

### ada83.c:13516

```
// index subtype (POSITIVE), so folding it here yields a garbage length
```

### ada83.c:13517

```
// (2**31-1) and an oversized alloca (c67003d). Bail so the runtime
```

### ada83.c:13518

```
// 'LENGTH path reads the fat pointer.
```

### ada83.c:13530

```
// Fixed-point model attributes are static and exactly representable
```

### ada83.c:13531

```
// (RM 3.5.10): fold them so they may appear in static contexts — notably
```

### ada83.c:13532

```
// the value of a `for T'SMALL use S'SMALL` clause and named numbers built
```

### ada83.c:13533

```
// from them. Delegate to the single evaluator the runtime path also uses.
```

### ada83.c:13534

```
// Floating-point model attributes are deliberately NOT folded here: their
```

### ada83.c:13535

```
// value carries base-type accuracy that a plain double constant loses,
```

### ada83.c:13536

```
// which membership and equality against the base type rely on (c35712a).
```

### ada83.c:13540

```
// T'MANTISSA is a static universal_integer (RM 3.5.10). Folding it lets
```

### ada83.c:13541

```
// a static expression built from it — notably T'LARGE's defining formula
```

### ada83.c:13542

```
// (2**MANTISSA - 1) * SMALL — evaluate exactly here rather than falling
```

### ada83.c:13543

```
// to a runtime path that computes 2**MANTISSA in bounded INTEGER and
```

### ada83.c:13544

```
// overflows for a fine-grained type like DURATION (MANTISSA = 52).
```

### ada83.c:13551

```
// non-static range: not a compile-time constant
```

### ada83.c:13562

```
// RM 6.6 / 8.5: a binary op resolved to a user-defined function (or a
```

### ada83.c:13563

```
// rename of one) is a function call — its value depends on that
```

### ada83.c:13564

```
// function's body, not on the predefined arithmetic identity of the
```

### ada83.c:13565

```
// operator token. Folding predefined semantics here would silently
```

### ada83.c:13566

```
// replace F1 (renamed as "*") with 0*0=0 at every use site (c87b02a
```

### ada83.c:13567

```
// (C): CONSTANT INTEGER := 0*0 got 0 instead of the F1(0,0) = -1
```

### ada83.c:13568

```
// it should have received). Bail so the caller falls back to
```

### ada83.c:13569

```
// emitting the actual call.
```

### ada83.c:13582

```
// Ada integer division truncates toward zero (RM 4.5.5). Recognize
```

### ada83.c:13583

```
// an integer operand by its structure (Is_Integer_Expr) OR its
```

### ada83.c:13584

```
// resolved type — an attribute such as INTEGER'SIZE has no structural
```

### ada83.c:13585

```
// case but is universal_integer — so a static
```

### ada83.c:13586

```
// `(INTEGER'SIZE + STORAGE_UNIT - 1) / STORAGE_UNIT` truncates to 4
```

### ada83.c:13587

```
// rather than folding to 4.875 (needed for record layout positions).
```

### ada83.c:13607

```
// Set bound `slot` from `expr`: a statically evaluable value becomes a constant
```

### ada83.c:13608

```
// bound (BOUND_INTEGER when `as_int`, else BOUND_FLOAT); otherwise the
```

### ada83.c:13609

```
// expression is kept as a BOUND_EXPR. A NULL expr leaves the slot unchanged.
```

### ada83.c:13618

```
// RM 4.10: Exact rational evaluator for static universal_real expressions.                        
```

### ada83.c:13619

```
// Returns true and fills *out if the expression is a compile-time constant                         
```

### ada83.c:13620

```
// representable as an exact rational number. Used so that comparisons like                        
```

### ada83.c:13621

```
// 0.1*0.1 = 0.01 or (2/3)**10 chain correctly without IEEE rounding errors.                       
```

### ada83.c:13622

```
//                                                                                                  
```

### ada83.c:13628

```
// A based real literal carries its exact rational from the lexer.
```

### ada83.c:13638

```
// Fallback: wrap double as rational - loses exactness
```

### ada83.c:13668

```
// Fixed-point model attributes are static and EXACTLY rational (RM
```

### ada83.c:13669

```
// 3.5.10): T'SMALL is a power of two, T'MANTISSA an integer, T'LARGE the
```

### ada83.c:13670

```
// exact product (2**MANTISSA - 1) * SMALL. Folding them exactly here lets
```

### ada83.c:13671

```
// a static expression built from them fold too — the fold at the "/="
```

### ada83.c:13672

```
// comparison then avoids the runtime path, which computes 2**MANTISSA in
```

### ada83.c:13673

```
// bounded INTEGER and overflows for a fine type like DURATION (MANTISSA =
```

### ada83.c:13674

```
// 52). Only FIXED attributes fold: a floating type's model attributes
```

### ada83.c:13675

```
// carry base-type accuracy an exact rational would misrepresent (c35712a).
```

### ada83.c:13684

```
// non-static range
```

### ada83.c:13686

```
// 2**mantissa must fit
```

### ada83.c:13693

```
// ATTRIBUTE_LARGE = (2**MANTISSA - 1) * SMALL
```

### ada83.c:13714

```
// A binary op resolved to a user-defined function is a function call,
```

### ada83.c:13715

```
// not a predefined arithmetic identity — its exact value can't be
```

### ada83.c:13716

```
// folded here (same rule as Eval_Const_Numeric for the "==" / "/="
```

### ada83.c:13717

```
// comparison-fold path). c87b03a: R2 : CONSTANT FLOAT := 1.0 + 1.0
```

### ada83.c:13718

```
// with FUNCTION "+"(FLOAT,FLOAT) RETURN FLOAT RENAMES "-" runs the
```

### ada83.c:13719

```
// user "+" at elaboration and stores 0.0; the "R2 /= 0.0" fold must
```

### ada83.c:13720

```
// not treat R2's init as 2.0 by folding the "+" locally.
```

### ada83.c:13725

```
// For exponentiation, exponent must be integer
```

### ada83.c:13749

```
// Integer-precise constant evaluator for modular type modulus expressions.                        
```

### ada83.c:13750

```
// Unlike Eval_Const_Numeric (which uses double, losing precision above 2^53),                      
```

### ada83.c:13751

```
// this evaluates in uint128_t for exact results up to 2^128.                                      
```

### ada83.c:13752

```
// Returns true on success, false if the expression is not a compile-time                           
```

### ada83.c:13753

```
// integer constant. Handles 2**64, 2**128, and all intermediate values                            
```

### ada83.c:13754

```
// without sentinel hacks.                                                                         
```

### ada83.c:13755

```
//                                                                                                  
```

### ada83.c:13761

```
// integer_lit.value is int64_t; for values > INT64_MAX, use big_value
```

### ada83.c:13792

```
// Integer exponentiation: l ** r. For modular type declarations,                          
```

### ada83.c:13793

```
// the common case is 2**N. 2**64 and 2**128 compute exactly                               
```

### ada83.c:13794

```
// in uint128_t (2**128 wraps to 0, but we cap r at 127 for                                 
```

### ada83.c:13795

```
// the shift path below).                                                                  
```

### ada83.c:13796

```
//                                                                                          
```

### ada83.c:13803

```
// Fast path: 2**N via shift. 2**128 = 0 in uint128_t,                                   
```

### ada83.c:13804

```
// but we return it as 0 which the caller interprets as                                   
```

### ada83.c:13805

```
// "the 128-bit boundary" for mod types.                                                 
```

### ada83.c:13806

```
//                                                                                        
```

### ada83.c:13813

```
// Type conversions: TYPE_NAME (expr)
```

### ada83.c:13835

```
// A bound deferred to run time: BOUND_EXPR carrying its elaboration-time
```

### ada83.c:13836

```
// expression.
```

### ada83.c:13841

```
// Return bound value as int128_t. For most types this fits in 64 bits;
```

### ada83.c:13842

```
// for mod 2**128 the high bound is 2^128-1 which requires the full range.
```

### ada83.c:13847

```
// Try to evaluate expression bound at compile time
```

### ada83.c:13850

```
// Not NaN
```

### ada83.c:13852

```
// BOUND_NONE or unevaluable expression
```

### ada83.c:13855

```
// Fold a scalar bound to a static integer when possible, distinguishing a
```

### ada83.c:13856

```
// genuinely static bound from one that is only known at run time (which
```

### ada83.c:13857

```
// Type_Bound_Value cannot, since it returns 0 for both an unevaluable bound
```

### ada83.c:13858

```
// and a literal 0). BOUND_NONE — an open side of a range — yields the caller's
```

### ada83.c:13859

```
// supplied sentinel. Returns false for a bound resolved only at run time.
```

### ada83.c:13866

```
// not NaN
```

### ada83.c:13871

```
// Write the decimal digits of a nonzero value backward into a buffer,
```

### ada83.c:13872

```
// NUL-terminating at buffer_end. Returns the position of the first digit.
```

### ada83.c:13880

```
// Format an int128_t as a decimal string. Returns pointer to a static
```

### ada83.c:13881

```
// thread-local buffer. Handles the full range -2^127 .. 2^127-1.
```

### ada83.c:13882

```
// Used for emitting i128 constants in LLVM IR (which accepts arbitrary
```

### ada83.c:13883

```
// width decimal literals).
```

### ada83.c:13884

```
//
```

### ada83.c:13886

```
// -170141183460469231731687303715884105728 + NUL
```

### ada83.c:13895

```
// Format a uint128_t as a decimal string. Returns pointer to a static
```

### ada83.c:13896

```
// thread-local buffer. Handles 0 .. 2^128-1.
```

### ada83.c:13898

```
// 340282366920938463463374607431768211455 + NUL
```

### ada83.c:13903

```
// Effective bound of one array dimension (RM 3.6.1): the dimension's own
```

### ada83.c:13904

```
// bound when it carries one, else the index subtype's bound — an
```

### ada83.c:13905

```
// unconstrained dimension records its range only on the index type.
```

### ada83.c:13906

```
// out_from_index_type (optional) reports which source answered, so emit
```

### ada83.c:13907

```
// sites can attribute the bound in IR comments.
```

### ada83.c:13925

```
// Get array element count for constrained arrays, 0 for unconstrained
```

### ada83.c:13932

```
// Product of all dimension lengths (RM 3.6.1)
```

### ada83.c:13944

```
// Static byte size of one element of `array_type`, for size arithmetic.
```

### ada83.c:13945

```
// `missing_fallback` substitutes when the array or its element type is
```

### ada83.c:13946

```
// unknown; `zero_size_fallback` substitutes when the element's flat size is
```

### ada83.c:13947

```
// 0 (a dynamically sized or fat-stored element). Call sites deliberately
```

### ada83.c:13948

```
// choose different fallbacks, so both are explicit parameters.
```

### ada83.c:13958

```
// Compile-time storage size of an object type for the RM 11.1 stack-limit
```

### ada83.c:13959

```
// check, returned as a 128-bit value so a size approaching the whole address
```

### ada83.c:13960

```
// space does not wrap the 32-bit size field. A constrained array multiplies
```

### ada83.c:13961

```
// its element count by its element size (its own `size` field is populated
```

### ada83.c:13962

```
// only for types that reached Compute_Static_Array_Size); other types report
```

### ada83.c:13963

```
// their recorded byte size. Returns -1 when no static size is known (dynamic
```

### ada83.c:13964

```
// bounds), leaving the runtime storage check to catch an oversized elaboration.
```

### ada83.c:13975

```
// Get array low bound for index adjustment
```

### ada83.c:13982

```
// Compute the static storage size of a constrained array type: the element
```

### ada83.c:13983

```
// count (product of each dimension's extent, RM 3.6.1) times the element
```

### ada83.c:13984

```
// size. Only fully static bounds yield a size — a bound captured as an
```

### ada83.c:13985

```
// expression (Assign_Eval_Bound yields BOUND_EXPR only when it is not a
```

### ada83.c:13986

```
// compile-time constant) makes the size a runtime quantity, recorded as 0
```

### ada83.c:13987

```
// so later layout treats it as unknown (RM 3.7.1). An element type that is
```

### ada83.c:13988

```
// itself unknown falls back to default_element_size bytes; an element held
```

### ada83.c:13989

```
// through a fat pointer sizes as one.
```

### ada83.c:14009

```
// Maximum byte size of a constrained array whose bounds may be dynamic. Each
```

### ada83.c:14010

```
// dimension is taken at its widest possible extent so later components in the
```

### ada83.c:14011

```
// same record sit at fixed offsets (RM 3.7.1). A statically-known bound uses its
```

### ada83.c:14012

```
// value; a discriminant-named bound is capped by the dimension's INDEX SUBTYPE,
```

### ada83.c:14013

```
// because a discriminant used as an index bound is itself constrained to that
```

### ada83.c:14014

```
// subtype (any value outside it raises CONSTRAINT_ERROR). Capping by the index
```

### ada83.c:14015

```
// subtype rather than the discriminant's own type avoids overflowing the size
```

### ada83.c:14016

```
// when that type is unbounded (e.g. INTEGER, whose 'LAST is 2**31-1). Returns 0
```

### ada83.c:14017

```
// when no static cap is available (a genuinely unbounded array).
```

### ada83.c:14027

```
// Largest value a dynamic high bound can take is the smaller of two caps: the
```

### ada83.c:14028

```
// bound's own subtype'LAST (e.g. a discriminant constrained to 1..5) and the
```

### ada83.c:14029

```
// index subtype'LAST (the bound must be a valid index). A cap that is merely
```

### ada83.c:14030

```
// the base integer range (INTEGER'LAST, as for POSITIVE or an unconstrained
```

### ada83.c:14031

```
// INTEGER discriminant) is not a real bound — reserving that much storage
```

### ada83.c:14032

```
// inline is nonsensical, so the component is treated as genuinely unbounded
```

### ada83.c:14033

```
// and the array stays dynamically sized.
```

### ada83.c:14050

```
// unbounded — no real cap
```

### ada83.c:14063

```
// A component whose byte footprint is known only once the enclosing record's
```

### ada83.c:14064

```
// discriminants have values: a discriminant-dependent array with no STATIC cap,
```

### ada83.c:14065

```
// so its extent is set solely by a discriminant (STRING (1 .. D), STRING (D2 ..
```

### ada83.c:14066

```
// D3)). A discriminant-dependent array that its index subtype DOES cap is not of
```

### ada83.c:14067

```
// this kind — the base layout reserves it at that maximum, giving it a static
```

### ada83.c:14068

```
// footprint and leaving every later component at a static offset. This is the
```

### ada83.c:14069

```
// only component kind that forces the components after it to runtime offsets.
```

### ada83.c:14075

```
// Byte size a discriminant-dependent array component occupies once the enclosing
```

### ada83.c:14076

```
// record's discriminants are fixed to static values: fold each discriminant-named
```

### ada83.c:14077

```
// bound (a BOUND_EXPR over a SYMBOL_DISCRIMINANT) to its constraint value and take
```

### ada83.c:14078

```
// the resulting constrained extent. The component's Type_Info is NOT altered —
```

### ada83.c:14079

```
// only its footprint is computed — so the component keeps its dynamic-array LLVM
```

### ada83.c:14080

```
// representation and access path; solely the record's layout arithmetic changes.
```

### ada83.c:14108

```
// A record type whose byte size is not fixed by the type alone: its (base)
```

### ada83.c:14109

```
// layout contains a capless discriminant-dependent component, so its size varies
```

### ada83.c:14110

```
// with its own discriminants (RM 3.7.1).
```

### ada83.c:14126

```
// Resolve nested record subtype `ct`'s discriminants to concrete static values,
```

### ada83.c:14127

```
// given the enclosing record `rec`'s static discriminant values in `disc`. A
```

### ada83.c:14128

```
// nested constraint naming an enclosing discriminant takes that value; any other
```

### ada83.c:14129

```
// constraint is folded as a static expression. Returns false when a nested
```

### ada83.c:14130

```
// discriminant is not statically known (e.g. IDENT_INT (3), deliberately opaque),
```

### ada83.c:14131

```
// which punts the enclosing record to the runtime-descriptor path.
```

### ada83.c:14153

```
// The static byte footprint of component `ci` given the enclosing record's
```

### ada83.c:14154

```
// discriminant values in `disc`. Recurses for a nested dynamically-sized record
```

### ada83.c:14155

```
// component (its own discriminants substituted from `disc`) and an array whose
```

### ada83.c:14156

```
// element is such a record. Returns false — punting the whole record to the
```

### ada83.c:14157

```
// runtime path — when a footprint is not statically computable (a dynamic array
```

### ada83.c:14158

```
// element, a nested discriminant that is not static).
```

### ada83.c:14163

```
// A nested record whose size depends on its discriminants: substitute them
```

### ada83.c:14164

```
// from the enclosing discriminants and recurse.
```

### ada83.c:14173

```
// An array whose element is a dynamically-sized record: element count times the
```

### ada83.c:14174

```
// element's static size (element discriminants resolved from the element's own
```

### ada83.c:14175

```
// constraint, which does not see the enclosing discriminants).
```

### ada83.c:14185

```
// Element COUNT: fold the array's bounds with a unit-size element so
```

### ada83.c:14186

```
// Folded_..._Size returns the element count, then scale by the element size.
```

### ada83.c:14201

```
// capped dependent → reserved at max
```

### ada83.c:14203

```
// not statically computable — punt
```

### ada83.c:14208

```
// Static byte size of record subtype `ct` given static discriminant values
```

### ada83.c:14209

```
// `disc`: the sum of every component's static footprint. The recursion base for
```

### ada83.c:14210

```
// nested records.
```

### ada83.c:14223

```
// Whether every component of `rec` has a footprint the runtime layout paths can
```

### ada83.c:14224

```
// compute, and (via `has_dynamic`) whether at least one component's footprint
```

### ada83.c:14225

```
// depends on discriminant values. A capless dependent array, a nested
```

### ada83.c:14226

```
// dynamically-sized record, and an array of such records are all foldable at
```

### ada83.c:14227

```
// runtime; a size-0 component of any other shape is not, and leaves the record
```

### ada83.c:14228

```
// with its base handling rather than a wrong layout.
```

### ada83.c:14234

```
// A nested record whose size depends on its discriminants — foldable iff its
```

### ada83.c:14235

```
// own layout is.
```

### ada83.c:14243

```
// An array whose element is such a record — foldable iff the element is.
```

### ada83.c:14253

```
// A plain capless dependent array — foldable iff its extent arithmetic is
```

### ada83.c:14254

```
// simple enough to fold from the discriminants.
```

### ada83.c:14263

```
// unknown footprint
```

### ada83.c:14268

```
// A constrained discriminated subtype whose discriminant values are all STATIC
```

### ada83.c:14269

```
// has a definite size: each discriminant-dependent component occupies its folded
```

### ada83.c:14270

```
// extent, so the whole record gains a static size and every later component sits
```

### ada83.c:14271

```
// at a recomputed offset (RM 3.7.1). The base record left capless components size
```

### ada83.c:14272

```
// 0 and the record at its static-prefix size, deferring this to constraint time.
```

### ada83.c:14273

```
// This recomputes ALL byte offsets and the total size in place (over a private
```

### ada83.c:14274

```
// component array so the shared base is untouched), handling any number of
```

### ada83.c:14275

```
// discriminant-dependent components at any position, WITHOUT retyping any
```

### ada83.c:14276

```
// component — the dynamic-array components keep their fat representation and
```

### ada83.c:14277

```
// runtime access path; solely the record's layout arithmetic changes. No-op if
```

### ada83.c:14278

```
// any discriminant value is non-static (that subtype is runtime-sized), if the
```

### ada83.c:14279

```
// record has a variant part (laid out separately), or if any component's
```

### ada83.c:14280

```
// footprint is not statically foldable.
```

### ada83.c:14290

```
// a non-static discriminant → runtime-sized, not this path
```

### ada83.c:14305

```
// footprint became unfoldable — leave the record untouched
```

### ada83.c:14313

```
// If `idx` is a `P'RANGE` attribute used as an array index constraint, fill
```

### ada83.c:14314

```
// `info` with the prefix subtype's index bounds and index type, and return
```

### ada83.c:14315

```
// true. `P'RANGE(n)` selects dimension n (1-based, default 1). RM 3.6.1.
```

### ada83.c:14316

```
// Shared by the three array-index-constraint resolution paths (anonymous array
```

### ada83.c:14317

```
// type, indexed-name subtype, and subtype-indication subtype).
```

### ada83.c:14323

```
// RM 4.1(3): P'RANGE where P is access-to-array denotes the designated array.
```

### ada83.c:14341

```
// Resolve a P'RANGE attribute used as an array index constraint into INFO's
```

### ada83.c:14342

```
// bounds (RM 3.6.1). A CONSTRAINED array prefix carries the value's index
```

### ada83.c:14343

```
// bounds in its own type — read them directly. An UNCONSTRAINED prefix (an
```

### ada83.c:14344

```
// unconstrained-array object, a function result, a concatenation) has only its
```

### ada83.c:14345

```
// index subtype's full range on its type, which is not the value's actual
```

### ada83.c:14346

```
// bounds; capture the prefix value's 'FIRST and 'LAST as bound expressions so
```

### ada83.c:14347

```
// the subtype is sized against the actual value rather than, say, POSITIVE'LAST.
```

### ada83.c:14348

```
// Returns true iff RANGE was a 'RANGE attribute (and INFO was filled).
```

### ada83.c:14358

```
// RM 4.1(9): an implicit dereference in a P'RANGE where P is access must
```

### ada83.c:14359

```
// check P for null (c41401a case 11: ARRAY1 (PTR_ARA2'RANGE) with null
```

### ada83.c:14360

```
// PTR_ARA2 must raise CE at A's elaboration). Skip the static-bounds
```

### ada83.c:14361

```
// fast path for access prefixes so the bounds go through the FIRST/LAST
```

### ada83.c:14362

```
// attribute path, which emits the null check via Generate_Attribute.
```

### ada83.c:14388

```
// Resolve one discrete_range of an index constraint into DIMENSION
```

### ada83.c:14389

```
// (RM 3.6.1). The dimension's index type is inherited from the same
```

### ada83.c:14390

```
// dimension of the unconstrained base array type (e.g. POSITIVE for
```

### ada83.c:14391

```
// STRING). The discrete_range takes one of four forms, each with its own
```

### ada83.c:14392

```
// bounds capture: an explicit range (L .. H), a subtype indication with
```

### ada83.c:14393

```
// its own range (V RANGE L .. H), a P'RANGE attribute, or a bare scalar
```

### ada83.c:14394

```
// subtype mark whose own bounds span the dimension. When
```

### ada83.c:14395

```
// record_range_mark is set, an explicit subtype mark on the second form
```

### ada83.c:14396

```
// is recorded so elaboration also checks the range against that mark
```

### ada83.c:14397

```
// (RM 3.5); the access-designated caller does not apply that check.
```

### ada83.c:14405

```
// Inherit index_type from base (e.g., POSITIVE for STRING)
```

### ada83.c:14415

```
// An explicit range: a bound that evaluates at compile time becomes
```

### ada83.c:14416

```
// BOUND_INTEGER; otherwise the expression reference is stored for
```

### ada83.c:14417

```
// later evaluation.
```

### ada83.c:14422

```
// A subtype indication with its own range (e.g. U (V RANGE 1 .. X),
```

### ada83.c:14423

```
// STRING (POSITIVE RANGE X .. 10)): the bounds are that explicit
```

### ada83.c:14424

```
// range, captured dynamically.
```

### ada83.c:14429

```
// RM 3.5: the discrete_range's explicit subtype mark constrains its
```

### ada83.c:14430

```
// own range too (e.g. MID_WEEK RANGE SUN..TUE requires SUN..TUE
```

### ada83.c:14431

```
// within MID_WEEK), in ADDITION to the array's index subtype
```

### ada83.c:14432

```
// (RM 3.6.1). Record the mark so elaboration checks both;
```

### ada83.c:14433

```
// index_type stays the base index subtype for the 3.6.1 check.
```

### ada83.c:14439

```
// P'RANGE index constraint (RM 3.6.1): a constrained prefix supplies
```

### ada83.c:14440

```
// static bounds, an unconstrained one its value's 'FIRST and 'LAST
```

### ada83.c:14441

```
// (see Resolve_Range_Attr_Index).
```

### ada83.c:14443

```
// handled by the helper
```

### ada83.c:14445

```
// A bare subtype-mark index constraint (BOOLEAN, a subtype with its
```

### ada83.c:14446

```
// own range, possibly carrying a dynamic bound): the dimension spans
```

### ada83.c:14447

```
// that subtype's own bounds.
```

### ada83.c:14454

```
// RM 6.6 / 4.3: seed an as-yet-untyped expression with its expected type so
```

### ada83.c:14455

```
// that resolution can see the target before working bottom-up. Overload
```

### ada83.c:14456

```
// resolution by result type (`C : CITRUS := ORANGE`), unary/binary operator
```

### ada83.c:14457

```
// selection against a user-defined homograph (`C : QT := +0` must reach QT's
```

### ada83.c:14458

```
// "+"), aggregate typing, and string literals all hinge on knowing the target.
```

### ada83.c:14459

```
// Literals and other self-typing forms are left untouched so the ordinary
```

### ada83.c:14460

```
// bottom-up rules still apply. Shared by object declarations and record
```

### ada83.c:14461

```
// component defaults so the two never drift out of agreement.
```

### ada83.c:14474

```
// ═══════════════════════════════════════════════════════════════════════════
```

### ada83.c:14475

```
// Two-pass overload resolution (RM 8.6)
```

### ada83.c:14476

```
//
```

### ada83.c:14477

```
// Ada resolution is inherently two-pass. Analyze_Expr walks bottom-up and
```

### ada83.c:14478

```
// records on each node the SET of interpretations its subtree admits, without
```

### ada83.c:14479

```
// committing a type. Resolve_In_Context then walks top-down: given the type the
```

### ada83.c:14480

```
// context requires, it keeps only interpretations whose result covers that type
```

### ada83.c:14481

```
// (RM 8.6 — GNAT's `if not Covers (Typ, It.Typ)`), reduces the survivors by the
```

### ada83.c:14482

```
// preference rules, commits node->type / node->symbol, and recurses into the
```

### ada83.c:14483

```
// operands with the operand type the chosen interpretation dictates.
```

### ada83.c:14484

```
// ═══════════════════════════════════════════════════════════════════════════
```

### ada83.c:14494

```
// typ NULL is legitimate for procedures/entries (no result type). Only cap
```

### ada83.c:14495

```
// on capacity and deduplicate against nam+typ pairs already present.
```

### ada83.c:14503

```
// Two integer/real operands share an arithmetic result type (RM 4.5.5): a real
```

### ada83.c:14504

```
// operand wins over an integer one, a non-universal over a universal, else left.
```

### ada83.c:14512

```
// Do two candidate operand types take one predefined arithmetic/comparison
```

### ada83.c:14513

```
// operator (RM 4.5)?
```

### ada83.c:14516

```
// RM 4.6: universal_integer implicitly converts to any numeric type,
```

### ada83.c:14517

```
// including real (e.g. `2 * 3.5`, `2 * T_SMALL`). Cross-domain compat is
```

### ada83.c:14518

```
// otherwise disallowed (integer × non-universal-real is rejected).
```

### ada83.c:14524

```
// Same domain: a predefined operator exists per TYPE, so both operands must
```

### ada83.c:14525

```
// denote one type — subtypes conform, a universal converts to the other's
```

### ada83.c:14526

```
// type (RM 4.6). Distinct types merely sharing a root are NOT compatible:
```

### ada83.c:14527

```
// there is no predefined "/=" between INT and its parent INTEGER (c84009a).
```

### ada83.c:14532

```
// Build the interpretation set of a binary operator from its operands' sets.
```

### ada83.c:14533

```
// RM 8.6 name resolution — bottom-up half. Look the name up, then expose EVERY
```

### ada83.c:14534

```
// visible interpretation on the node's interp set. A bare identifier used as
```

### ada83.c:14535

```
// an expression can denote a subprogram *call* only if that subprogram is
```

### ada83.c:14536

```
// callable with zero arguments (RM 6.4), so overloaded callables are filtered
```

### ada83.c:14537

```
// on that predicate. The chosen interpretation is committed later, in
```

### ada83.c:14538

```
// Resolve_In_Context, from the applicable-context type — not here.
```

### ada83.c:14553

```
// recovery: keep analysis going
```

### ada83.c:14558

```
// Default binding (Symbol_Find returns the innermost). Resolve_In_Context
```

### ada83.c:14559

```
// will overwrite from ctx if a homograph is a better match.
```

### ada83.c:14564

```
// Also collect for enum literals — homographs across sibling enumeration
```

### ada83.c:14565

```
// types (BOOLEAN.FALSE, MYBOOL.FALSE) may not be flagged is_overloaded on
```

### ada83.c:14566

```
// the symbol, but their interpretations must all be exposed so
```

### ada83.c:14567

```
// Resolve_In_Context can pick the one matching the applicable ctx.
```

### ada83.c:14568

```
//
```

### ada83.c:14569

```
// A bare subprogram name likewise needs the full cross-scope overload set:
```

### ada83.c:14570

```
// an inner declaration hides an outer one only when they are homographs
```

### ada83.c:14571

```
// (RM 8.3), so an inner F RETURN INTEGER does not hide an outer
```

### ada83.c:14572

```
// F RETURN BOOLEAN. is_overloaded is a same-region flag and misses that
```

### ada83.c:14573

```
// case, so collect whenever the innermost binding is a subprogram and let
```

### ada83.c:14574

```
// Score_Interpretation pick the one matching the applicable context.
```

### ada83.c:14583

```
// Bare name of a subprogram is a zero-argument call — reject overloads
```

### ada83.c:14584

```
// with any required parameter.
```

### ada83.c:14591

```
// nothing survived filter
```

### ada83.c:14598

```
// What a "flexible" operand — one with no interpretations of its own — can
```

### ada83.c:14599

```
// possibly denote (RM 8.6 applicable interpretations): an aggregate only a
```

### ada83.c:14600

```
// composite type (RM 4.3), a string literal only a one-dimensional array
```

### ada83.c:14601

```
// with a character component (RM 4.2), any other node anything. Bounds both
```

### ada83.c:14602

```
// the sibling-type adoption in Analyze_Binary and the array-vs-component
```

### ada83.c:14603

```
// choice for catenation operands.
```

### ada83.c:14613

```
// RM 12.1.2(41-42): inside a generic instance, a formal type's operator is
```

### ada83.c:14614

```
// the FORMAL subprogram when the generic declares one — only otherwise does
```

### ada83.c:14615

```
// the actual's predefined operation apply. True when `t` is a generic-actual
```

### ada83.c:14616

```
// view (still opaque, i.e. we are inside the instance) whose instance binds a
```

### ada83.c:14617

```
// formal subprogram for `op` accepting this type; the predefined
```

### ada83.c:14618

```
// interpretation must then not be synthesized.
```

### ada83.c:14627

```
// During the instance body clone's own resolution the instance symbol has
```

### ada83.c:14628

```
// not adopted its scope yet — the live pushed scope owned by the instance
```

### ada83.c:14629

```
// is the binding home then.
```

### ada83.c:14656

```
// Membership (IN / NOT IN) is not an overloadable operator: BOOLEAN result,
```

### ada83.c:14657

```
// operands take the tested type. RM 4.5.2: the left operand takes the tested
```

### ada83.c:14658

```
// type (right side, a type mark or range) as its applicable context — so an
```

### ada83.c:14659

```
// aggregate/string on the left picks up the type-mark's type.
```

### ada83.c:14665

```
// Seed the BASE type, not the tested subtype: membership ASKS whether the
```

### ada83.c:14666

```
// value satisfies the subtype's constraint (RM 4.5.2), so the operand must
```

### ada83.c:14667

```
// carry its own bounds. Seeding the constrained subtype made a string
```

### ada83.c:14668

```
// literal inherit the subtype's bounds (membership then compared them
```

### ada83.c:14669

```
// against themselves — always TRUE) and made an aggregate raise
```

### ada83.c:14670

```
// CONSTRAINT_ERROR against the very constraint being tested.
```

### ada83.c:14671

```
// RM 4.5.2: the tested subtype supplies the left operand's applicable
```

### ada83.c:14672

```
// context. A bare character literal there denotes the tested type's own
```

### ada83.c:14673

```
// literal — its enumeration position — not predefined CHARACTER, so an
```

### ada83.c:14674

```
// unnatural-order user character type (`'M' in ST`) tests by position
```

### ada83.c:14675

```
// (c45211a). Resolve it against the tested base directly; the generic
```

### ada83.c:14676

```
// path would reset it to CHARACTER.
```

### ada83.c:14680

```
// resolved to the enumeration literal; nothing more to do for L
```

### ada83.c:14684

```
// seed applicable context
```

### ada83.c:14706

```
// Candidate operand-type lists. A "flexible" operand (an aggregate, or a
```

### ada83.c:14707

```
// string literal with no committed type) has no interps of its own — it takes
```

### ada83.c:14708

```
// its type from the sibling/context (RM 4.3, 4.2), so let it adopt the other
```

### ada83.c:14709

```
// operand's candidate types — bounded by Flexible_Can_Denote. Without the
```

### ada83.c:14710

```
// bound, a sibling whose interp set mixes scalar and composite results —
```

### ada83.c:14711

```
// `user_op(A, B) /= (1 .. 3 => 7)` where "<=" is redefined to return the
```

### ada83.c:14712

```
// array — leaks its scalar candidate into the aggregate and resolution
```

### ada83.c:14713

```
// drives the aggregate into a type it cannot denote (c44003a..g).
```

### ada83.c:14724

```
// Two sibling character literals give each other no context ('A' < 'B'):
```

### ada83.c:14725

```
// fall back to predefined CHARACTER, the RM 4.2 default when no other
```

### ada83.c:14726

```
// character type is applicable.
```

### ada83.c:14730

```
// Predefined interpretations, synthesized per operand-type pair. Predefined
```

### ada83.c:14731

```
// arithmetic operators are not stored per type; the result type comes from
```

### ada83.c:14732

```
// the operands themselves (which is why INT operands yield INT, not INTEGER).
```

### ada83.c:14738

```
// RM 7.4.2: where a private type's partial view governs, the type has
```

### ada83.c:14739

```
// no predefined arithmetic, ordering, logical or catenation operators —
```

### ada83.c:14740

```
// only =, /= and its explicitly declared ones (collected below). Without
```

### ada83.c:14741

```
// this, a synthesised predefined interpretation shadows the package's
```

### ada83.c:14742

```
// own operator: `10.9 + NOW` outside CALENDAR must denote
```

### ada83.c:14743

```
// CALENDAR."+"(DURATION, TIME), not a predefined TIME addition
```

### ada83.c:14744

```
// (c96005e).
```

### ada83.c:14748

```
// RM 12.1.2: where the instance declares a FORMAL subprogram for an
```

### ada83.c:14749

```
// operator of a formal type, that binding IS the type's operator
```

### ada83.c:14750

```
// inside the instance — the actual's predefined one does not exist
```

### ada83.c:14751

```
// there, so no predefined interpretation is synthesized (cc1301a:
```

### ada83.c:14752

```
// `Y * X` on the formal INTEGER must reach the formal "*").
```

### ada83.c:14758

```
// RM 4.5.6: "**"(T, Integer) return T. Base takes the result type,
```

### ada83.c:14759

```
// exponent is Standard.Integer; a universal_integer exponent coerces
```

### ada83.c:14760

```
// to it (so `FLOAT'(2.0) ** (3 + 1)` builds an interp even when the
```

### ada83.c:14761

```
// exponent's analyzed type is still universal).
```

### ada83.c:14768

```
// RM 4.5.5 mixed fixed-point: fixed * int, int * fixed, fixed / int
```

### ada83.c:14769

```
// yield the fixed type; the integer operand is Standard.Integer.
```

### ada83.c:14777

```
// RM 4.5.5 + 4.6: `int * universal_real` is legal when the applicable
```

### ada83.c:14778

```
// context supplies a fixed type — the universal_real coerces to that
```

### ada83.c:14779

```
// fixed subtype and mixed fixed×int applies. Result universal_real
```

### ada83.c:14780

```
// adopts the context in Resolve_In_Context; integer stays integer.
```

### ada83.c:14788

```
// RM 4.5: both operands convert to the (common) result type.
```

### ada83.c:14797

```
// RM 4.5.2 / 4.10: comparing a universal operand with a concrete-typed
```

### ada83.c:14798

```
// one performs the concrete comparison — the universal converts. Both
```

### ada83.c:14799

```
// operands take the concrete peer so it flows back down to the
```

### ada83.c:14800

```
// universal side, letting that subtree re-derive in the concrete type
```

### ada83.c:14801

```
// (c87b35b: `2.0/(1+1) /= REAL'(2.0)` drives the left side in REAL).
```

### ada83.c:14809

```
// AND THEN / OR ELSE / AND / OR / XOR are predefined per boolean type T
```

### ada83.c:14810

```
// with signature (T, T) -> T (RM 4.4 / 4.5.1). Distinct sibling booleans
```

### ada83.c:14811

```
// (YES from ON, BIT from BOOLEAN) share a Type_Root but are not the
```

### ada83.c:14812

```
// same type, so a mixed pair is illegal (c87b33a).
```

### ada83.c:14817

```
// RM 4.5.3: concatenation is the one operator with heterogeneous
```

### ada83.c:14818

```
// operands — each side is either the array (base) type or the element
```

### ada83.c:14819

```
// type, giving array&array, array&component and component&array (all
```

### ada83.c:14820

```
// yielding the base; component&component is context-determined and
```

### ada83.c:14821

```
// handled by the empty-set path below). An operand that is itself an
```

### ada83.c:14822

```
// array may be EITHER the array side or a component of an
```

### ada83.c:14823

```
// array-of-arrays (c45347b: A & A2(1) & AR1 where A's component type
```

### ada83.c:14824

```
// is the array ARR), so consider both operands as the candidate array
```

### ada83.c:14825

```
// type and add every form the operand pair satisfies — never assume
```

### ada83.c:14826

```
// an array-like operand is the array side.
```

### ada83.c:14837

```
// A string literal is context-typed (RM 4.2): even after the
```

### ada83.c:14838

```
// bottom-up pass stamped it STRING, it denotes ANY character-array
```

### ada83.c:14839

```
// base, so it satisfies the array side of that base's "&"
```

### ada83.c:14840

```
// (c45345a: SMALL_STRING'("ABCD") & "EF" must build the
```

### ada83.c:14841

```
// SMALL_STRING interp whose result bound check raises).
```

### ada83.c:14863

```
// User-defined operators (RM 6.7): any visible 2-formal function whose formals
```

### ada83.c:14864

```
// cover an (lt, rt) operand pair. For "/=", also expose user-defined "=" —
```

### ada83.c:14865

```
// RM 6.7 says "/=" is not independently declarable; it is *implicitly*
```

### ada83.c:14866

```
// derived from a user "=" as its negation.
```

### ada83.c:14886

```
// A flexible operand (aggregate) matches any formal it can adopt.
```

### ada83.c:14894

```
// RM 8.4: a use-visible user operator is cancelled by an immediately-visible
```

### ada83.c:14895

```
// homograph (e.g. a redefinition in the enclosing region).
```

### ada83.c:14898

```
// Never hand downstream an empty set: default to left type (BOOLEAN for cmp).
```

### ada83.c:14899

```
// A "&" whose operands built no interpretation is the (component & component)
```

### ada83.c:14900

```
// form (RM 4.5.3): its result array type is context-determined, so leave the
```

### ada83.c:14901

```
// set empty for Resolve_In_Context to derive it rather than fabricating a
```

### ada83.c:14902

```
// scalar interpretation from the left component.
```

### ada83.c:14908

```
// Build the interpretation set of a unary operator (+ - abs not).
```

### ada83.c:14919

```
// RM 7.4.2: no predefined operators where a private type's partial view
```

### ada83.c:14920

```
// governs (mirrors the binary synthesis).
```

### ada83.c:14948

```
// RM 8.4: cancel any use-visible unary operator hidden by an immediately
```

### ada83.c:14949

```
// visible homograph, as the binary path does.
```

### ada83.c:14957

```
// Bottom-up: fill node->interps. Operators are analyzed as two-pass; every other
```

### ada83.c:14958

```
// node kind is resolved by the single-pass driver and recorded as one interp.
```

### ada83.c:14964

```
// An aggregate has no type of its own — it is a flexible operand that takes
```

### ada83.c:14965

```
// its type from the operator's chosen operand type in the top-down pass
```

### ada83.c:14966

```
// (RM 4.3). The same holds for a character literal: RM 4.2 overloads it on
```

### ada83.c:14967

```
// EVERY visible character type (a derived character type inherits the
```

### ada83.c:14968

```
// literals, RM 3.4), so committing it to predefined CHARACTER here would
```

### ada83.c:14969

```
// exclude a user operator on such a type from the candidate set (cc3012a's
```

### ada83.c:14970

```
// "<" instance on a NEW CHARACTER type). Leave the interp set empty and
```

### ada83.c:14971

```
// defer resolution; Resolve_Operand hands the chosen operand type to
```

### ada83.c:14972

```
// Resolve_Char_As_Enum.
```

### ada83.c:14980

```
// Choose the interpretation compatible with ctx (RM 8.6). ctx NULL = no
```

### ada83.c:14981

```
// constraint. Predefined interpretations are recorded before user ones in the
```

### ada83.c:14982

```
// Analyze pass, so "first wins" ties resolve to the predefined operation.
```

### ada83.c:14986

```
// 1. Keep candidates whose result covers ctx; if none do (or ctx is NULL),
```

### ada83.c:14987

```
//    keep them all.
```

### ada83.c:14994

```
// 1b. RM 8.3(15): an enumeration literal is a predefined (implicit) operation,
```

### ada83.c:14995

```
//     hidden by an explicit homograph — a parameterless function of the same
```

### ada83.c:14996

```
//     name and result base type. When both are candidates, drop the literal so
```

### ada83.c:14997

```
//     the user function is the one directly visible (c34015b, c83031a).
```

### ada83.c:15016

```
// 2. RM 6.7 / 8.3: a user-declared operator hides its predefined homograph —
```

### ada83.c:15017

```
//    same operand profile AND same result base type. A predefined interp is
```

### ada83.c:15018

```
//    synthesized from the ACTUAL operand types, so a user op that hides one
```

### ada83.c:15019

```
//    is exactly the operand-exact match; prefer it. Because the profile
```

### ada83.c:15020

```
//    carries BOTH operand types this handles homogeneous ("*"(INT,INT),
```

### ada83.c:15021

```
//    "="(LP,LP)) and heterogeneous ("&"(CHARACTER,STRING), array
```

### ada83.c:15022

```
//    "<"(STRING,STRING)) redefinitions, and disambiguates sibling user
```

### ada83.c:15023

```
//    overloads (c74211b: `LT1(L) = LT1(R)` picks user "="(LT1), which hides
```

### ada83.c:15024

```
//    the predefined "="(LT1,LT1), over the visible user "="(LT3)). Also drop
```

### ada83.c:15025

```
//    any hidden predefined from the candidate set.
```

### ada83.c:15026

```
// A predefined interp's operand IS the actual operand type, synthesized from
```

### ada83.c:15027

```
// the operands; a user op hides it when their operand and result types denote
```

### ada83.c:15028

```
// the same type. On the clean graph that is Type_Base equality — a subtype
```

### ada83.c:15029

```
// matches its base (a user "<"(STRING) hides the predefined "<" carrying the
```

### ada83.c:15030

```
// actual STRING(1..2)), while sibling and derived types keep distinct bases
```

### ada83.c:15031

```
// (c74211b). Generic-actual views resolve by location through
```

### ada83.c:15032

```
// Effective_Type_View: opaque inside their instance, the actual outside.
```

### ada83.c:15037

```
// A view still opaque here (inside its instance) stashes its actual in
```

### ada83.c:15038

```
// base_type, so Type_Base would tunnel through it: compare opaque views
```

### ada83.c:15039

```
// by identity only (c74409b) — EXCEPT when the user candidate is the
```

### ada83.c:15040

```
// instance's own formal-subprogram binding: it carries the ACTUAL's
```

### ada83.c:15041

```
// parameter subtypes (RM 12.3) while the operands wear the view, and
```

### ada83.c:15042

```
// RM 12.1.2 makes it THE operator of the formal type inside the
```

### ada83.c:15043

```
// instance (cc1301a: a formal "*" must hide the view's predefined "*").
```

### ada83.c:15053

```
// The instance-scope view of a formal subprogram: a rename binding or a
```

### ada83.c:15054

```
// synthesized wrapper living directly in a generic instance's scope.
```

### ada83.c:15061

```
// user op that hides a predefined homograph
```

### ada83.c:15083

```
// 3. RM 4.10: a predefined operation whose operands are universal matches the
```

### ada83.c:15084

```
//    universal operands with NO implicit conversion, and is preferred over an
```

### ada83.c:15085

```
//    operation whose formals are a specific numeric type (which would convert
```

### ada83.c:15086

```
//    the universals). This is why `NATURAL'POS (3+3)` uses predefined "+"
```

### ada83.c:15087

```
//    (c87b07a). The exception: a context that concretely demands a candidate's
```

### ada83.c:15088

```
//    own result type (exact identity or a subtype relationship that is NOT a
```

### ada83.c:15089

```
//    NEW-derivation) keeps that candidate — RM 4.6: a universal OPERATOR
```

### ada83.c:15090

```
//    RESULT is not a convertible operand (only a literal, named number, or
```

### ada83.c:15091

```
//    attribute is), so the universal interpretation cannot legally satisfy a
```

### ada83.c:15092

```
//    concrete context; the literals convert instead and the concrete
```

### ada83.c:15093

```
//    operation governs. The concrete candidate may be user-declared
```

### ada83.c:15094

```
//    (c87b07a: NATURAL'POS (1+1) uses "+"(NATURAL)) or predefined with its
```

### ada83.c:15095

```
//    operand type contributed by a nested user op (c44003e: `-3.5 - 11.0`
```

### ada83.c:15096

```
//    against fixed USR takes predefined "-"(USR), whose operands then
```

### ada83.c:15097

```
//    resolve the inner unary minus to the user "-"(USR)). c87b07d guards
```

### ada83.c:15098

```
//    the derivation exclusion — a user "+"(NEW_INT) must NOT win for a
```

### ada83.c:15099

```
//    plain INTEGER context.
```

### ada83.c:15119

```
// 4. Prefer result==ctx. Among candidates that yield ctx, choose in order:
```

### ada83.c:15120

```
//    the operand-exact user op that hid a predefined (c74211b: two visible
```

### ada83.c:15121

```
//    user "="→BOOLEAN, pick the one hiding the predefined "="(LT1,LT1)); a
```

### ada83.c:15122

```
//    user op whose left operand is also ctx — a genuine operator for that
```

### ada83.c:15123

```
//    context, not a coincidental numeric match (c96005e: calendar
```

### ada83.c:15124

```
//    "+"(TIME,DURATION)→TIME over a spurious predefined "+"); then any.
```

### ada83.c:15125

```
//    A user op whose result is NOT ctx never preempts the ctx match
```

### ada83.c:15126

```
//    (c87b04a: user "+"→BASE10 wins over user "+"→INTEGER). Finally fall
```

### ada83.c:15127

```
//    back to same root type, any non-universal, else first.
```

### ada83.c:15142

```
// Apply the chosen operand type to one operand: recurse for nested operators;
```

### ada83.c:15143

```
// for a flexible/undertyped leaf (aggregate, string, an operand left untyped by
```

### ada83.c:15144

```
// Analyze) seed the operand type and resolve it now (RM 4.3/4.2).
```

### ada83.c:15150

```
// Overloaded bare identifier (e.g. an enum literal shared between multiple
```

### ada83.c:15151

```
// enum types) — pick the interpretation matching opnd_typ (c47002a: SUN in
```

### ada83.c:15152

```
// WEEKEND context picks WEEKEND.SUN, not WEEK.SUN).
```

### ada83.c:15155

```
// Character literal as an enum literal in operand context (c25004a:
```

### ada83.c:15156

```
// 'C' compared against an ENUM value picks ENUM.'C', not CHARACTER 'C').
```

### ada83.c:15157

```
// When the target has no such literal SYMBOL (predefined CHARACTER, whose
```

### ada83.c:15158

```
// literals are positional, not in the symbol table), commit the target
```

### ada83.c:15159

```
// type directly — a flexible literal reaches codegen typed either way.
```

### ada83.c:15165

```
// RM 4.3.2: an operator operand is not one of the applicable-index-
```

### ada83.c:15166

```
// constraint contexts (a)-(c), so a flexible array literal (a string
```

### ada83.c:15167

```
// literal or an array aggregate) fixes its own bounds from its length —
```

### ada83.c:15168

```
// it does not inherit a constrained operand subtype's bounds. Seeding the
```

### ada83.c:15169

```
// unconstrained base makes `S(3..4) = "CD"` compare by length rather than
```

### ada83.c:15170

```
// giving the literal the whole array's 1..N bounds (a length mismatch that
```

### ada83.c:15171

```
// wrongly makes the operands unequal). For a POSITIONAL aggregate against
```

### ada83.c:15172

```
// a user-declared constrained array TYPE, the unconstrained base is the
```

### ada83.c:15173

```
// RM 3.6 anonymous one (cc3225a: `(1, 2) /= PAC_VAR(1 .. 2)` compares two
```

### ada83.c:15174

```
// 2-element values, not a 3-element aggregate against a 2-element slice).
```

### ada83.c:15175

```
// A NAMED aggregate already carries its bounds in its choices, which the
```

### ada83.c:15176

```
// constrained-type lowering honors — and keeping it on the constrained
```

### ada83.c:15177

```
// view preserves the discriminant-dependent element handling
```

### ada83.c:15178

```
// (c37213e subtest 13: `X /= (1..5 => (-6, 0))` must not evaluate the
```

### ada83.c:15179

```
// unselected variant's component constraint).
```

### ada83.c:15188

```
// Likewise a record aggregate compared against a discriminated record
```

### ada83.c:15189

```
// view: a predefined-equality operand has the BASE type, and the
```

### ada83.c:15190

```
// aggregate carries its own discriminants — pinning it to the sibling
```

### ada83.c:15191

```
// operand's constrained view (which may carry its constraint through
```

### ada83.c:15192

```
// discriminant values rather than the constrained flag, e.g. the
```

### ada83.c:15193

```
// designated subtype of a constrained access type) raised
```

### ada83.c:15194

```
// CONSTRAINT_ERROR where equality must just yield FALSE (RM 4.5.2; the
```

### ada83.c:15195

```
// variant-aware comparator handles unequal discriminants).
```

### ada83.c:15204

```
// RM 4.5.3: choose the target type for one operand of "&" given the result
```

### ada83.c:15205

```
// array type ARR and its component type ELEM. An operand already typed as the
```

### ada83.c:15206

```
// array (or a same-base array) contributes the array; every other operand is a
```

### ada83.c:15207

```
// single component.
```

### ada83.c:15212

```
// An operand that is itself a catenation yields the array type (RM 4.5.3)
```

### ada83.c:15213

```
// — `R2 & A1(2) & (A2(1) & R1)` chains component catenations whose every
```

### ada83.c:15214

```
// link is a value of the one result array type (c45347a).
```

### ada83.c:15217

```
// An untyped flexible operand is the ARRAY operand whenever it cannot
```

### ada83.c:15218

```
// denote the component type: `(1, 2) & X` catenates a two-element array
```

### ada83.c:15219

```
// with the component X, not two components (cc3225a).
```

### ada83.c:15224

```
// RM 4.5.3 (component & component): the one catenation form whose result array
```

### ada83.c:15225

```
// type appears in neither operand — both sides are components — so the array
```

### ada83.c:15226

```
// type is taken solely from the applicable context. Reached from
```

### ada83.c:15227

```
// Resolve_In_Context when Analyze_Binary built no interpretation (no operand was
```

### ada83.c:15228

```
// an array); the (array & *) forms always build an interpretation and are
```

### ada83.c:15229

```
// resolved through the ordinary operand-propagation path below.
```

### ada83.c:15232

```
// With no array context the only predefined catenation whose array type is
```

### ada83.c:15233

```
// known without an array operand is STRING (component CHARACTER): `'a' & 'b'`.
```

### ada83.c:15252

```
// Keep the context subtype when it names one (its index constraint bounds the
```

### ada83.c:15253

```
// result); otherwise the unconstrained base is the result type.
```

### ada83.c:15258

```
// Top-down: commit the interpretation the context selects, then propagate each
```

### ada83.c:15259

```
// operand's target type down to the child. The interpretation's opnd[] profile
```

### ada83.c:15260

```
// already encodes heterogeneous operands (CHARACTER & STRING, fixed * INTEGER,
```

### ada83.c:15261

```
// T ** INTEGER), so a single uniform propagation covers every operator.
```

### ada83.c:15265

```
// Pick the ctx-compatible interpretation and commit it. count==1 still
```

### ada83.c:15266

```
// commits (statement-position filtering may have narrowed to one).
```

### ada83.c:15274

```
// leaf: already committed by Analyze
```

### ada83.c:15277

```
// The (component & component) catenation form builds no interpretation
```

### ada83.c:15278

```
// bottom-up — its array type is context-determined (RM 4.5.3).
```

### ada83.c:15283

```
// ARR & ARR is ambiguous between array&array (yielding ARR) and the
```

### ada83.c:15284

```
// component&component reading (yielding the context's array type) when the
```

### ada83.c:15285

```
// operand type is itself another array type's component (RM 4.5.3,
```

### ada83.c:15286

```
// c45347b: A'(AR2 & A1(2)) with A's component type ARR). The bottom-up set
```

### ada83.c:15287

```
// holds only the former; when the context demands an array the pick cannot
```

### ada83.c:15288

```
// deliver — and the pick's result would itself be a component of that
```

### ada83.c:15289

```
// context — the context reading is the legal one.
```

### ada83.c:15300

```
// RM 8.6: the context type constrains the whole expression. An overloaded
```

### ada83.c:15301

```
// call operand (`-IDENT(X)` with one IDENT instance per fixed type,
```

### ada83.c:15302

```
// c46033a) is committed bottom-up to an arbitrary overload; when that
```

### ada83.c:15303

```
// pick cannot satisfy the context, re-drive the call against it — the
```

### ada83.c:15304

```
// predefined arithmetic unary operators return their operand type
```

### ada83.c:15305

```
// (RM 4.5.4), so the operand's target IS the context.
```

### ada83.c:15325

```
// RM 4.5.5 fixed-point * / integer reached through universal operands: the
```

### ada83.c:15326

```
// operands were analyzed as universal (a real literal, a universal_integer
```

### ada83.c:15327

```
// sub-expression), so Analyze_Binary built only the universal interp — the
```

### ada83.c:15328

```
// fixed context now pins the result. Re-drive the real operand against the
```

### ada83.c:15329

```
// fixed type and the integer operand against Standard.Integer (honoring a
```

### ada83.c:15330

```
// user redefinition), which the pre-built universal profile can't express
```

### ada83.c:15331

```
// (c87b35b: `2.0 / (1 + 1)` in a REAL context, "+"(INTEGER) RENAMES "*").
```

### ada83.c:15350

```
// RM 4.5.3 + 4.2 + 4.3.2: an operand of "&" is never in an applicable-index-
```

### ada83.c:15351

```
// constraint context (contexts (a)-(c) of 4.3.2 do not include catenation
```

### ada83.c:15352

```
// operands), so a flexible operand — a string literal or an array aggregate —
```

### ada83.c:15353

```
// takes the result array type but fixes its lower bound from that type's index
```

### ada83.c:15354

```
// SUBTYPE 'FIRST, not from any constraint carried by the context. Seed such
```

### ada83.c:15355

```
// operands with the UNCONSTRAINED base of the applicable array type so their
```

### ada83.c:15356

```
// bounds fall out of the index subtype; recurse through nested "&" so every
```

### ada83.c:15357

```
// leaf operand of a catenation chain is seeded from the one result type.
```

### ada83.c:15370

```
// caller-seeded context hint, or NULL
```

### ada83.c:15377

```
// RM 4.7: resolve the operand of a qualified expression with the named
```

### ada83.c:15378

```
// subtype as its applicable context. An aggregate takes the type outright,
```

### ada83.c:15379

```
// and every overloadable form — a call, an operator, a bare identifier, a
```

### ada83.c:15380

```
// string — resolves its overloads against it (RM 6.6): `WHOLE'(F2(0,0))`
```

### ada83.c:15381

```
// picks the F2 returning WHOLE, `CITRUS'(ORANGE)` picks CITRUS.ORANGE over
```

### ada83.c:15382

```
// HUE.ORANGE, and `PREC'(F)` picks the parameterless F returning PREC
```

### ada83.c:15383

```
// (c47002d — reached through an allocator, whose parser destructures
```

### ada83.c:15384

```
// `NEW T'(expr)`; the allocator resolver shares this path).
```

### ada83.c:15395

```
// Re-resolve overloaded literals against the qualifying type (RM 4.7):
```

### ada83.c:15396

```
// WEEKEND'(SAT) must pick WEEKEND.SAT, not WEEK.SAT; CHAR'('B') must use
```

### ada83.c:15397

```
// CHAR's position for 'B', not the ASCII code.
```

### ada83.c:15417

```
// Applicable-context type overrides the default universal_integer when
```

### ada83.c:15418

```
// caller pre-seeded it with a compatible specific integer type (RM 4.6:
```

### ada83.c:15419

```
// universal_integer implicitly converts to any numeric type).
```

### ada83.c:15424

```
// Same rule for real literals — a caller-seeded fixed/float/component
```

### ada83.c:15425

```
// type takes precedence (RM 4.6).
```

### ada83.c:15431

```
// A string literal takes the applicable array type from context when one
```

### ada83.c:15432

```
// was propagated (RM 4.2(4) fixes its bounds from that type's index
```

### ada83.c:15433

```
// subtype); otherwise it is a value of the predefined STRING type.
```

### ada83.c:15440

```
// caller-poked hint, or NULL
```

### ada83.c:15447

```
// .ALL dereference is not an overloadable operator; resolve it
```

### ada83.c:15448

```
// structurally (its operand is analyzed here, not via Analyze_Unary,
```

### ada83.c:15449

```
// so there is no recursion back through this case).
```

### ada83.c:15451

```
// designated-type context, if any
```

### ada83.c:15453

```
// An overloaded operand (F.ALL where F has both an access and a
```

### ada83.c:15454

```
// non-access return type) must resolve to the access-typed overload —
```

### ada83.c:15455

```
// .ALL is only defined for access types (RM 4.1). Analyze bottom-up,
```

### ada83.c:15456

```
// then filter interps to the access-returning ones before commit.
```

### ada83.c:15468

```
// RM 8.6: flow the dereference context into a nested overloaded call
```

### ada83.c:15469

```
// F(args).ALL, so F's overload is the one whose access result designates
```

### ada83.c:15470

```
// the target type (c87b37e).
```

### ada83.c:15487

```
// Resolve attribute arguments with type context for certain attributes
```

### ada83.c:15492

```
// For POS, SUCC, PRED, IMAGE the operand is a value of the prefix type
```

### ada83.c:15493

```
// (RM 3.5.5), so it takes the prefix type as its expected type for
```

### ada83.c:15494

```
// overload resolution — true for any scalar prefix, not just
```

### ada83.c:15495

```
// enumerations (e.g. NATURAL'POS (1 + 1) must resolve "+" in NATURAL).
```

### ada83.c:15503

```
// The character-literal disambiguation below applies only when the
```

### ada83.c:15504

```
// prefix is an enumeration whose literals may be character literals.
```

### ada83.c:15512

```
// Check if character literal should be resolved as enum literal
```

### ada83.c:15513

```
// Get the character from the literal (format: 'X')
```

### ada83.c:15518

```
// Find enum type (handle derived types)
```

### ada83.c:15523

```
// Look for matching character literal in enum
```

### ada83.c:15529

```
// Found matching enum literal - set symbol with type match
```

### ada83.c:15541

```
// Only resolve if not already resolved as enum literal
```

### ada83.c:15543

```
// RM 6.6: pre-set arg type for context-sensitive
```

### ada83.c:15544

```
// attributes so overloaded names resolve correctly.
```

### ada83.c:15545

```
//   POS/SUCC/PRED/IMAGE: arg has prefix type.
```

### ada83.c:15546

```
//   VAL: arg is INTEGER (RM 3.5.5).
```

### ada83.c:15547

```
//   VALUE: arg is STRING (RM 3.5.5) — needed so
```

### ada83.c:15548

```
//   overloaded functions returning STRING vs other
```

### ada83.c:15549

```
//   types resolve to the STRING one (c87b07c).
```

### ada83.c:15564

```
// Attribute type depends on attribute kind and prefix type
```

### ada83.c:15569

```
// Implicit dereference for access types (RM 4.1(3))
```

### ada83.c:15570

```
// A1'FIRST where A1 is access-to-array is equivalent to A1.ALL'FIRST
```

### ada83.c:15571

```
// But NOT for type-level attributes like SIZE, STORAGE_SIZE, BASE (RM 13.7.2)
```

### ada83.c:15572

```
//
```

### ada83.c:15581

```
// FIRST, LAST return the index type for arrays, base type for scalars
```

### ada83.c:15585

```
// For arrays, FIRST/LAST return the actual index type for that dimension
```

### ada83.c:15587

```
// Default to first dimension (0-indexed)
```

### ada83.c:15595

```
// Get the index type for this dimension
```

### ada83.c:15602

```
// Default to INTEGER for strings or missing info
```

### ada83.c:15607

```
// For scalar types, return the type itself
```

### ada83.c:15613

```
// VAL, SUCC, PRED return the BASE type (RM 3.5.5): S'VAL(0) is a
```

### ada83.c:15614

```
// value of the base, checked against a subtype only when a later
```

### ada83.c:15615

```
// assignment or conversion imposes one. Typing it with the prefix
```

### ada83.c:15616

```
// SUBTYPE made downstream range checks elide (source bounds equal
```

### ada83.c:15617

```
// target bounds), losing the required constraint check. Strip only
```

### ada83.c:15618

```
// a SUBTYPE declaration's constraint (one base hop): a type
```

### ada83.c:15619

```
// declaration IS its own first subtype here, and hopping it (or
```

### ada83.c:15620

```
// walking Type_Base to the root) would land on a different type
```

### ada83.c:15621

```
// identity — the derivation parent or INTEGER — breaking overload
```

### ada83.c:15622

```
// resolution, which needs T'VAL to denote a value of type T.
```

### ada83.c:15626

```
// A SUBTYPE prefix is identified by its declaring symbol, not by
```

### ada83.c:15627

```
// the type graph (Type_Info is shared/copied too freely to stamp);
```

### ada83.c:15628

```
// one base hop strips its constraint. A generic formal's prefix is
```

### ada83.c:15629

```
// its actual-VIEW, whose base_type the binder already set to the
```

### ada83.c:15630

```
// ACTUAL's base (RM 12.1.2) — a formal bound to a subtype must not
```

### ada83.c:15631

```
// pin 'VAL to the subtype's constraint. In both cases the result
```

### ada83.c:15632

```
// is the prefix type's own base.
```

### ada83.c:15645

```
// POS returns universal integer
```

### ada83.c:15650

```
// IMAGE returns STRING
```

### ada83.c:15655

```
// SIZE, LENGTH, COUNT, WIDTH, MANTISSA, etc. return universal integer
```

### ada83.c:15675

```
// Floating-point type attributes returning universal_real (RM 3.5.8)
```

### ada83.c:15687

```
// Boolean attributes (RM 3.5.8, 3.7.1, 9.9)
```

### ada83.c:15696

```
// ADDRESS attribute returns SYSTEM.ADDRESS (RM 13.7.2)
```

### ada83.c:15701

```
// T'BASE denotes the BASE TYPE of T (RM 3.3.3) — a type, used as a
```

### ada83.c:15702

```
// prefix for further attributes (T'BASE'FIRST). That is Type_Base:
```

### ada83.c:15703

```
// a derived type's own (possibly anonymous) base, carrying the full
```

### ada83.c:15704

```
// unconstrained range — never an ancestor across the derivation
```

### ada83.c:15705

```
// boundary, which Type_Root would reach.
```

### ada83.c:15715

```
// VALUE attribute returns the type itself (converts string to type)
```

### ada83.c:15718

```
// T'VALUE (S) returns a value of type T
```

### ada83.c:15722

```
// Default to integer for unhandled attributes
```

### ada83.c:15731

```
// Resolve subtype mark first to get the type
```

### ada83.c:15742

```
// -2 = not yet computed
```

### ada83.c:15746

```
// For record aggregates, choices are field names - don't resolve as variables
```

### ada83.c:15749

```
// Find component type from first choice for nested aggregates
```

### ada83.c:15759

```
// Propagate component type to nested aggregates and to any
```

### ada83.c:15760

```
// overload-resolvable expression so record component context
```

### ada83.c:15761

```
// reaches the resolver (c87b30a).
```

### ada83.c:15773

```
// RM 4.3: character literal in record component context
```

### ada83.c:15774

```
// resolves as enum literal when component type is enum
```

### ada83.c:15775

```
// (e.g. D => 'A' for enum (A, 'A')).
```

### ada83.c:15785

```
// Positional item in record aggregate - propagate component type
```

### ada83.c:15787

```
// RM 4.3.1 + 3.7.3: positional element N maps to the Nth component
```

### ada83.c:15788

```
// of the fixed part plus the SELECTED variant, so skip the other
```

### ada83.c:15789

```
// variants' components. Selection is deferred until the walk
```

### ada83.c:15790

```
// reaches a variant component: by then the governing
```

### ada83.c:15791

```
// discriminant's item (positionally earlier, RM 4.3.1) has been
```

### ada83.c:15792

```
// resolved, so its enumeration-literal value is readable.
```

### ada83.c:15809

```
// RM 4.3.1: a positional record aggregate expression must
```

### ada83.c:15810

```
// resolve as the associated component's type; seed the
```

### ada83.c:15811

```
// context so overloaded names (c87b30a: F, G with several
```

### ada83.c:15812

```
// return types) pick the component-typed variant.
```

### ada83.c:15819

```
// For array aggregates, propagate element type to nested aggregates
```

### ada83.c:15826

```
// For multi-dimensional arrays (index_count > 1), inner aggregates                     
```

### ada83.c:15827

```
// represent "rows" (slices along the first dimension). Create an                      
```

### ada83.c:15828

```
// implicit 1-D array type from the remaining dimensions so that                        
```

### ada83.c:15829

```
// Generate_Aggregate can handle them as composite elements (RM 4.3.2).                
```

### ada83.c:15830

```
//                                                                                      
```

### ada83.c:15835

```
// Inherit the outer array's constrained-ness: a row of an
```

### ada83.c:15836

```
// UNCONSTRAINED multi-dim array (A_12 IS ARRAY (I<>, I<>)) is
```

### ada83.c:15837

```
// itself unconstrained, so its inner named aggregate defines its
```

### ada83.c:15838

```
// own bounds from the choices (RM 4.3.2(4)) rather than being
```

### ada83.c:15839

```
// pinned to — and checked against — the index subtype's full range.
```

### ada83.c:15841

```
// A row is a DIMENSION of the enclosing aggregate, not a component
```

### ada83.c:15842

```
// value: in an assignment it slides along with the whole array
```

### ada83.c:15843

```
// (RM 5.2.1), which the aggregate bounds check must honour.
```

### ada83.c:15849

```
// Copy remaining dimensions (skip first) and derive                                  
```

### ada83.c:15850

```
// bounds from index_type when BOUND_NONE (unconstrained).                           
```

### ada83.c:15851

```
// RM 4.3.3(6): lower bound comes from index subtype.                                
```

### ada83.c:15852

```
//                                                                                    
```

### ada83.c:15865

```
// Calculate row size
```

### ada83.c:15880

```
// RM 4.3.2: an array-aggregate component expression has the array's
```

### ada83.c:15881

```
// element type as its applicable context. Seed it so overloaded
```

### ada83.c:15882

```
// names resolve in that type and — for a string literal or nested
```

### ada83.c:15883

```
// aggregate whose bounds depend on context — its bounds come from
```

### ada83.c:15884

```
// the element subtype rather than defaulting to STRING's 1..N.
```

### ada83.c:15901

```
// A named choice denotes an index value, so a character-literal
```

### ada83.c:15902

```
// choice ('R' => ...) resolves against the array's index type — as
```

### ada83.c:15903

```
// that enumeration's 'R' literal (its position), not the CHARACTER
```

### ada83.c:15904

```
// code (RM 4.3.2, 4.1.3). Without this the choice folds to its code
```

### ada83.c:15905

```
// point and the element lands at the wrong — often out-of-range —
```

### ada83.c:15906

```
// index.
```

### ada83.c:15913

```
// A choice written `S RANGE L..H` parses to a subtype indication.
```

### ada83.c:15914

```
// Normalise it to its inner L..H range so the aggregate emitter
```

### ada83.c:15915

```
// sees a uniform NK_RANGE, and record the mark S so the RM 3.5
```

### ada83.c:15916

```
// range-compatibility check runs (mirrors the slice path).
```

### ada83.c:15937

```
// Type from context
```

### ada83.c:15940

```
// Propagate type to initializer (critical for aggregates).
```

### ada83.c:15941

```
// Parser destructures T'(agg) so expression is directly the aggregate
```

### ada83.c:15944

```
// Resolve subtype mark first to get allocated type; the initializer is
```

### ada83.c:15945

```
// the destructured qualified operand and resolves as one (RM 4.7/4.8).
```

### ada83.c:15955

```
// Create access type pointing to allocated type
```

### ada83.c:15970

```
// Ada RM 4.1.1: in a range L..H, character literals must be                                  
```

### ada83.c:15971

```
// resolved against the other operand's enum type (like binary ops).                         
```

### ada83.c:15972

```
// GNAT: overload resolution propagates expected type to both bounds.                        
```

### ada83.c:15973

```
//                                                                                            
```

### ada83.c:15984

```
// Also handle derived enum types via parent chain
```

### ada83.c:16000

```
// For case alternatives, expression is a block with statements
```

### ada83.c:16010

```
// Create array type info from syntax node
```

### ada83.c:16017

```
// Allocate index info
```

### ada83.c:16027

```
// Extract bounds from range or subtype indication
```

### ada83.c:16032

```
// Infer index type from range bounds' type
```

### ada83.c:16043

```
// Also use the subtype mark's type for index type
```

### ada83.c:16051

```
// Unconstrained index type (just a type mark, no constraint)
```

### ada83.c:16054

```
// P'RANGE as an index constraint (RM 3.6.1): a constrained prefix
```

### ada83.c:16055

```
// supplies static bounds, an unconstrained one its value's 'FIRST
```

### ada83.c:16056

```
// and 'LAST (see Resolve_Range_Attr_Index), so ARRAY (A'RANGE) of
```

### ada83.c:16057

```
// an unconstrained A is sized by A's value, not its index subtype.
```

### ada83.c:16059

```
// handled by the helper
```

### ada83.c:16061

```
// Use resolved type from identifier/expression (e.g., BOOLEAN)
```

### ada83.c:16069

```
// Helper to extract static integer from various expression forms
```

### ada83.c:16073

```
// Integer literal
```

### ada83.c:16079

```
// Character/enum literal (symbol with frame_offset as position)
```

### ada83.c:16085

```
// Qualified expression: TYPE'(expr) - evaluate inner expression
```

### ada83.c:16097

```
// Handle character literal inside qualified expression:                          
```

### ada83.c:16098

```
// look up in the qualifying type's enumeration literals                          
```

### ada83.c:16099

```
// First resolve the subtype_mark to get its type                                 
```

### ada83.c:16100

```
//                                                                                
```

### ada83.c:16107

```
// Walk up to find the base enumeration type with literals
```

### ada83.c:16115

```
// Extract the character from 'X' format
```

### ada83.c:16120

```
// Position in enumeration
```

### ada83.c:16126

```
// Try constant evaluation for other inner expressions
```

### ada83.c:16128

```
// Not NaN
```

### ada83.c:16134

```
// Try general constant evaluation
```

### ada83.c:16136

```
// Not NaN
```

### ada83.c:16149

```
// Non-static bound - store expression reference
```

### ada83.c:16162

```
// Non-static bound - store expression reference
```

### ada83.c:16171

```
// RM 3.6.1(2): a discrete range with universal_integer bounds
```

### ada83.c:16172

```
// (ARRAY (1 .. 3)) has index type INTEGER. The range itself
```

### ada83.c:16173

```
// stays in Index_Info — it is the array's index range, not a
```

### ada83.c:16174

```
// constraint narrowing the index type (a bounded index type
```

### ada83.c:16175

```
// would wrongly re-check nulls and formals against it). The
```

### ada83.c:16176

```
// universal type must not remain: it fails same-base-type
```

### ada83.c:16177

```
// matching against INTEGER subtypes (cc3224a's formal array
```

### ada83.c:16178

```
// ARRAY (INT) vs actual ARRAY (1 .. 3)).
```

### ada83.c:16182

```
// RM 3.6.1: an index given as a bare type/subtype mark (e.g.
```

### ada83.c:16183

```
// BOOLEAN, with no explicit range) takes that subtype's own
```

### ada83.c:16184

```
// bounds. Without this the dimension stays BOUND_NONE — length 1
```

### ada83.c:16185

```
// — so a multi-dimensional array silently drops every type-mark
```

### ada83.c:16186

```
// dimension from its element count and storage size.
```

### ada83.c:16195

```
// Resolve component type
```

### ada83.c:16203

```
// Compute size - only when ALL bounds are statically known.                               
```

### ada83.c:16204

```
// Skip if any bound is BOUND_EXPR that evaluates to a value                                
```

### ada83.c:16205

```
// suggesting the bound can't be determined at compile time                                 
```

### ada83.c:16206

```
// (e.g. discriminant references). RM 3.6.1                                                 
```

### ada83.c:16207

```
//                                                                                          
```

### ada83.c:16214

```
// Create enumeration type info from syntax node                                                
```

### ada83.c:16215

```
// Note: Literal symbols are created later in NK_TYPE_DECL processing                           
```

### ada83.c:16216

```
// so they reference the named type, not this anonymous type                                    
```

### ada83.c:16217

```
//                                                                                              
```

### ada83.c:16232

```
// Size based on number of literals
```

### ada83.c:16234

```
// Fits in 1 byte
```

### ada83.c:16236

```
// Fits in 2 bytes
```

### ada83.c:16238

```
// 4 bytes for large enums
```

### ada83.c:16247

```
// Derived type: type T is new Parent [constraint]
```

### ada83.c:16258

```
// Create new type that inherits from parent
```

### ada83.c:16268

```
// Copy kind-specific info
```

### ada83.c:16283

```
// RM 3.4: `type T is new PARENT(C)` introduces an unconstrained base
```

### ada83.c:16284

```
// type T'BASE and makes T the constrained subtype of it. When the parent
```

### ada83.c:16285

```
// subtype indication already carried an index constraint, mirror that
```

### ada83.c:16286

```
// here: `derived` is the constrained subtype, and its base_type is a
```

### ada83.c:16287

```
// fresh unconstrained derivation of the parent's base. Without this the
```

### ada83.c:16288

```
// base would be the constrained type itself, so T'BASE'SIZE and the
```

### ada83.c:16289

```
// result type of concatenation (RM 4.5.3) would wrongly carry T's
```

### ada83.c:16290

```
// index constraint.
```

### ada83.c:16295

```
// unconstrained indices
```

### ada83.c:16303

```
// RM 3.4: likewise for a derived discriminated record constrained at the
```

### ada83.c:16304

```
// derivation (type T is new PARENT(disc)). The base is the unconstrained
```

### ada83.c:16305

```
// record, so comparing against T strips the constraint (an aggregate of
```

### ada83.c:16306

```
// another variant must yield FALSE, not CONSTRAINT_ERROR) and T'BASE is
```

### ada83.c:16307

```
// the full type.
```

### ada83.c:16320

```
// RM 3.4: deriving from a constrained discrete or floating subtype
```

### ada83.c:16321

```
// (e.g. `type S is new P range L..H`, or `type S is new SUB` where SUB
```

### ada83.c:16322

```
// is a constrained subtype of P) introduces an unconstrained base
```

### ada83.c:16323

```
// S'BASE that holds every value of the parent's base type, while S
```

### ada83.c:16324

```
// itself carries the constraint. Without a distinct base, a base-range
```

### ada83.c:16325

```
// value (an inherited enumeration literal outside S's range) would be
```

### ada83.c:16326

```
// typed with S's constraint and wrongly pass an assignment range
```

### ada83.c:16327

```
// check, and S'BASE'FIRST would read S's own constrained bound
```

### ada83.c:16328

```
// (c34001d: `NEW SUBPARENT RANGE TRUE..TRUE` must keep BOOLEAN'FIRST
```

### ada83.c:16329

```
// on its base). A DIGITS constraint carried by the derivation itself
```

### ada83.c:16330

```
// (`NEW PARENT DIGITS 4`) equally makes the derived type a constrained
```

### ada83.c:16331

```
// first subtype — the base keeps the parent's accuracy, and the
```

### ada83.c:16332

```
// constraint is applied to `derived` below (c34003c: T'BASE'DIGITS
```

### ada83.c:16333

```
// stays 5).
```

### ada83.c:16340

```
// Enumeration, boolean and character types carry their literal
```

### ada83.c:16341

```
// table in the enumeration payload; 'IMAGE and 'WIDTH on the base
```

### ada83.c:16342

```
// need it.
```

### ada83.c:16355

```
// RM 3.5.9 / 3.4: a derived fixed-point type shares its parent's base
```

### ada83.c:16356

```
// model. T'BASE is the parent's anonymous base (full model range, the
```

### ada83.c:16357

```
// parent's DELTA), while T itself carries the derived accuracy and range
```

### ada83.c:16358

```
// applied below.
```

### ada83.c:16362

```
// RM 3.4: a derived access type whose parent subtype constrained its
```

### ada83.c:16363

```
// designated array (e.g. type S is new PARENT(5..7)) keeps that
```

### ada83.c:16364

```
// constraint for bounds checks, but its base is the parent's
```

### ada83.c:16365

```
// unconstrained access base so the fat-pointer decision and T'BASE see
```

### ada83.c:16366

```
// the underlying unconstrained designated (RM 3.10).
```

### ada83.c:16371

```
// unconstrained designated
```

### ada83.c:16377

```
// Apply constraint if present
```

### ada83.c:16381

```
// Handle real type constraints (DIGITS/DELTA with optional RANGE)
```

### ada83.c:16384

```
// Apply DIGITS constraint
```

### ada83.c:16391

```
// Adjust size if needed
```

### ada83.c:16396

```
// Apply RANGE constraint. A statically known bound becomes
```

### ada83.c:16397

```
// BOUND_FLOAT; otherwise the expression is deferred.
```

### ada83.c:16407

```
// Apply a DELTA [RANGE] accuracy constraint to a derived fixed-point
```

### ada83.c:16408

```
// type (RM 3.5.9): a coarser delta and a tighter range than the
```

### ada83.c:16409

```
// parent. Recompute small/scale from the new delta.
```

### ada83.c:16430

```
// Other constraint types handled in subtype_indication
```

### ada83.c:16436

```
// Create access type pointing to designated type
```

### ada83.c:16440

```
// Pointer size
```

### ada83.c:16443

```
// Resolve designated subtype
```

### ada83.c:16448

```
// An access to an unconstrained (or dynamic-bounds) array is a fat
```

### ada83.c:16449

```
// pointer { data, bounds } — 16 bytes, not a bare 8-byte pointer. The
```

### ada83.c:16450

```
// size must reflect the representation or a record/array component of
```

### ada83.c:16451

```
// this type reserves too little and a fat store overruns its slot
```

### ada83.c:16452

```
// (RM 3.10 / 3.6.1).
```

### ada83.c:16459

```
// Create record type info from syntax node (RM 3.7, 3.7.3)
```

### ada83.c:16464

```
// Helper: count components in a variant part recursively
```

### ada83.c:16480

```
// Helper: count variants (top-level only)
```

### ada83.c:16486

```
// Count total components (each decl may have multiple names)
```

### ada83.c:16495

```
// Also count components in variant parts
```

### ada83.c:16506

```
// Helper: add a fixed-part component to the list
```

### ada83.c:16514

```
// Discriminant-dependent array components: reserve the maximum
```

### ada83.c:16515

```
// inline size so a later component sits at a fixed offset (RM
```

### ada83.c:16516

```
// 3.7.1). The maximum extent of a bound named by a discriminant is
```

### ada83.c:16517

```
// capped by the INDEX SUBTYPE, not the discriminant's own type — a
```

### ada83.c:16518

```
// bound of `2 .. D3` where D3 : INTEGER cannot exceed the index
```

### ada83.c:16519

```
// subtype's last value, since a larger index is illegal.
```

### ada83.c:16520

```
// Max_Constrained_Array_Size applies exactly that cap (and returns
```

### ada83.c:16521

```
// 0 for a genuinely unbounded component, keeping the record
```

### ada83.c:16522

```
// dynamically sized); reusing it avoids a second, cap-less copy of
```

### ada83.c:16523

```
// the extent arithmetic that overflows to a bogus giant size.
```

### ada83.c:16528

```
// A component default is resolved with the component's typemark as
```

### ada83.c:16529

```
// its expected type, exactly as an object declaration's initializer
```

### ada83.c:16530

```
// is (RM 6.6 / 4.3).
```

### ada83.c:16539

```
// RM 3.2.1: a multiple-name component declaration is a series of
```

### ada83.c:16540

```
// single declarations; a non-static constraint is elaborated
```

### ada83.c:16541

```
// independently for each name. Replicate the subtype so each name
```

### ada83.c:16542

```
// owns its constraint.
```

### ada83.c:16550

```
// Fixed part
```

### ada83.c:16555

```
// Process fixed (non-variant) components
```

### ada83.c:16560

```
// Record the variant_offset = end of fixed part
```

### ada83.c:16564

```
// Process variant part: all variants OVERLAP at variant_offset (RM 3.7.3)
```

### ada83.c:16565

```
// Record size = fixed_part + MAX (variant_sizes), not SUM
```

### ada83.c:16570

```
// Allocate variant info array
```

### ada83.c:16579

```
// Extract discriminant value/range from first choice.                               
```

### ada83.c:16580

```
// Supports single values, negated literals, and ranges                               
```

### ada83.c:16581

```
// (RM 3.7.3: WHEN -5..10 => ...).                                                   
```

### ada83.c:16582

```
//                                                                                    
```

### ada83.c:16601

```
// Range choice: WHEN -5..10 => ...
```

### ada83.c:16608

```
// Enumeration literal - covers BOOLEAN, CHARACTER,                                 
```

### ada83.c:16609

```
// and user-defined enum types (RM 3.7.3).                                         
```

### ada83.c:16610

```
// frame_offset stores the ordinal position for all                                 
```

### ada83.c:16611

```
// SYMBOL_LITERAL symbols.                                                         
```

### ada83.c:16612

```
//                                                                                  
```

### ada83.c:16622

```
// Fallback: search enumeration literals by name
```

### ada83.c:16639

```
// Add variant components - all start at variant_offset
```

### ada83.c:16649

```
// A discriminant-dependent array component (e.g. S : STRING(1..L))
```

### ada83.c:16650

```
// has no static size, yet a later component in the same variant
```

### ada83.c:16651

```
// must sit past it at a fixed offset. Reserve its MAXIMUM size,
```

### ada83.c:16652

```
// taken from each index's upper bound — a discriminant's own
```

### ada83.c:16653

```
// subtype'LAST when the bound is that discriminant (RM 3.7.1).
```

### ada83.c:16676

```
// Handle nested variant parts recursively (simple flattening)
```

### ada83.c:16718

```
// Record size = fixed part + maximum variant size (RM 3.7.3)
```

### ada83.c:16725

```
// Grow the size to cover any discriminant-dependent array/string
```

### ada83.c:16726

```
// component whose maximum size is not part of the static offset sum,
```

### ada83.c:16727

```
// so the whole record has a static maximum size and can be stored
```

### ada83.c:16728

```
// inline (RM 3.7.1). The per-component maximum is the index-subtype-
```

### ada83.c:16729

```
// capped extent computed by Max_Constrained_Array_Size. A component
```

### ada83.c:16730

```
// whose constraint is a NON-discriminant runtime expression has no
```

### ada83.c:16731

```
// static cap; such a record is itself dynamically sized and reports
```

### ada83.c:16732

```
// size 0 (the same convention as an unconstrained array), so every
```

### ada83.c:16733

```
// downstream size check reaches the runtime-descriptor path rather
```

### ada83.c:16734

```
// than reading a fabricated static size.
```

### ada83.c:16754

```
// Helper: Try to extract a static integer value from a bound expression.
```

### ada83.c:16755

```
// Returns true if value was extracted, false otherwise.
```

### ada83.c:16776

```
// Character literal 'X' - extract ASCII value from text.                                
```

### ada83.c:16777

```
// Per Ada RM 3.5.2, character position = Character'Pos (C).                             
```

### ada83.c:16778

```
// Eval_Scalar_Literal handles char literals via                                          
```

### ada83.c:16779

```
// Enumeration_Rep of the entity.                                                        
```

### ada83.c:16780

```
//                                                                                        
```

### ada83.c:16787

```
// Handle TYPE'POS (X) or TYPE'VAL (N) as NK_APPLY
```

### ada83.c:16801

```
// Handle TYPE'VAL (N) where N is static
```

### ada83.c:16813

```
// Handle type conversions: TYPE_NAME (arg) where arg is static
```

### ada83.c:16826

```
// Handle TYPE'VAL (...) or TYPE'POS (...) as NK_ATTRIBUTE with arguments
```

### ada83.c:16845

```
// Fall back to the general static folder for compound static
```

### ada83.c:16846

```
// INTEGER expressions the structural cases above do not cover — e.g.
```

### ada83.c:16847

```
// a subtype declared INTEGER RANGE 1 .. 1+1+1, or N*2 with N a named
```

### ada83.c:16848

```
// number. The expression must be integer-typed: this path also serves
```

### ada83.c:16849

```
// fixed-point/float subtype ranges (e.g. G RANGE -1.0 .. 1.5) where
```

### ada83.c:16850

```
// folding to an int would drop the mantissa scaling and corrupt the
```

### ada83.c:16851

```
// bound — leave those as a deferred BOUND_EXPR. A dynamic bound (over
```

### ada83.c:16852

```
// a variable) yields NaN.
```

### ada83.c:16860

```
// Resolve the base type
```

### ada83.c:16867

```
// Check for index constraint (STRING (1..5) style)
```

### ada83.c:16870

```
// For access-to-array, the index constraint applies to the                                 
```

### ada83.c:16871

```
// designated type. Resolve by creating a constrained access                               
```

### ada83.c:16872

```
// type whose designated_type is a constrained array. (RM 3.7.1)                            
```

### ada83.c:16873

```
//                                                                                          
```

### ada83.c:16897

```
// RM 3.7.1: Access-to-discriminated-record with disc constraint.
```

### ada83.c:16898

```
// NEW A_UR1(6) → A_UR1 is ACCESS UR1, (6) constrains disc A=6.
```

### ada83.c:16899

```
// Create a constrained access type whose designated is UR1(A=6).
```

### ada83.c:16900

```
// The designated type may still be the PRIVATE view (a subtype like
```

### ada83.c:16901

```
// ACC6S IS ACC6(6) is legal in the visible part before the full type):
```

### ada83.c:16902

```
// a discriminated private view already carries its known discriminant
```

### ada83.c:16903

```
// part (RM 7.4.1), so constrain through it just as for a record.
```

### ada83.c:16918

```
// Allocate disc constraint values
```

### ada83.c:16932

```
// Compute size from disc constraints
```

### ada83.c:16947

```
// Create constrained array type
```

### ada83.c:16952

```
// Set base_type to original type for tracing unconstrained arrays
```

### ada83.c:16955

```
// For STRING, element type is CHARACTER
```

### ada83.c:16962

```
// Process index constraints
```

### ada83.c:16969

```
// RM 3.6.1: the bounds of an index constraint are of the
```

### ada83.c:16970

```
// index base type. Seed the bound expressions with that
```

### ada83.c:16971

```
// type before resolving so an overloaded operator resolves
```

### ada83.c:16972

```
// to the base-type homograph (c87b14d: SUBTYPE LIST1 IS
```

### ada83.c:16973

```
// LIST (1 + 1 .. 1 + 1) with LIST's index WHOLE and user
```

### ada83.c:16974

```
// "+"(WHOLE, WHOLE) RENAMES "*" in scope must apply the
```

### ada83.c:16975

```
// user "+" so LIST1 = LIST (1 .. 1)).
```

### ada83.c:16981

```
// The index type is the context for both bounds. A character
```

### ada83.c:16982

```
// literal bound of an enumeration-indexed array (`B ('B'..'D')`
```

### ada83.c:16983

```
// where B is indexed by an enumeration) must resolve to the
```

### ada83.c:16984

```
// enumeration literal — carrying its ORDINAL position — not to
```

### ada83.c:16985

```
// the CHARACTER type's code, or the constraint would be checked
```

### ada83.c:16986

```
// in the wrong units (RM 3.6.1 / 3.5.1).
```

### ada83.c:17003

```
// Compute size - only for fully static bounds
```

### ada83.c:17010

```
// Check for scalar range constraint (ENUM RANGE A..B or INTEGER RANGE X..Y)
```

### ada83.c:17015

```
// RM 3.5: range bounds in a scalar subtype indication are
```

### ada83.c:17016

```
// implicitly converted to the type mark's base type.
```

### ada83.c:17017

```
// Character literals as bounds resolve as enum literals
```

### ada83.c:17018

```
// when the base type is an enumeration containing them.
```

### ada83.c:17024

```
// RM 3.5: the range bounds of an enum subtype indication are
```

### ada83.c:17025

```
// implicitly of the type mark's base type; seed the bound
```

### ada83.c:17026

```
// identifier's context so overload resolution picks the enum
```

### ada83.c:17027

```
// literal in the type mark's type, not a homograph from a
```

### ada83.c:17028

```
// sibling type (c87b04c: ORB RANGE SUN..MARS with DAY.SUN in
```

### ada83.c:17029

```
// scope must reach ORB.SUN, not DAY.SUN).
```

### ada83.c:17037

```
// Same rule for scalar (integer / fixed / float) subtypes: the
```

### ada83.c:17038

```
// range bounds' expected type is the type mark, so seed any
```

### ada83.c:17039

```
// overload-driven bound (a call, an operator, an identifier) with
```

### ada83.c:17040

```
// it (c87b04a: AGE RANGE 1..F1 picks F1→AGE; BASE10 RANGE
```

### ada83.c:17041

```
// +(INTEGER'(0)).. 0 picks "+"→BASE10).
```

### ada83.c:17054

```
// Create a constrained subtype
```

### ada83.c:17060

```
// Copy type-specific info from base type
```

### ada83.c:17067

```
// Set bounds from range.                                                              
```

### ada83.c:17068

```
// Following GNAT's approach: try compile-time evaluation first,                        
```

### ada83.c:17069

```
// if not possible, store expression for later evaluation.                             
```

### ada83.c:17070

```
//                                                                                      
```

### ada83.c:17075

```
// Static bound -> BOUND_INTEGER; otherwise store the expression
```

### ada83.c:17076

```
// for later evaluation.
```

### ada83.c:17086

```
// T RANGE A'RANGE (RM 3.5(2)): the constraint is the prefix's
```

### ada83.c:17087

```
// index range for the chosen dimension. A statically-bounded
```

### ada83.c:17088

```
// array subtype prefix supplies its index bounds directly. A
```

### ada83.c:17089

```
// prefix whose bounds are known only at run time — a function
```

### ada83.c:17090

```
// result or a concatenation — makes the subtype's bounds the
```

### ada83.c:17091

```
// prefix value's 'FIRST and 'LAST, captured as bound
```

### ada83.c:17092

```
// expressions and evaluated where the subtype is elaborated.
```

### ada83.c:17093

```
// A CONSTRAINED array prefix (object or subtype) carries the
```

### ada83.c:17094

```
// value's index bounds in its own index_info — read them
```

### ada83.c:17095

```
// directly. An UNCONSTRAINED prefix (a function result, a
```

### ada83.c:17096

```
// concatenation, an unconstrained-array object) has only its
```

### ada83.c:17097

```
// index subtype's full range there, which is not the value's
```

### ada83.c:17098

```
// actual bounds; those are the prefix value's 'FIRST and 'LAST.
```

### ada83.c:17135

```
// Check for DELTA constraint (fixed-point subtypes with different delta)
```

### ada83.c:17141

```
// Create constrained fixed-point subtype with new delta
```

### ada83.c:17147

```
// Evaluate the delta expression
```

### ada83.c:17151

```
// Compute small as largest power of 2 <= delta (per RM 3.5.9)
```

### ada83.c:17160

```
// Compute scale factor: small = 2^scale
```

### ada83.c:17167

```
// Set bounds from range, or inherit from base type
```

### ada83.c:17168

```
// Start by inheriting base type bounds
```

### ada83.c:17172

```
// If explicit range given, override with evaluated bounds.                              
```

### ada83.c:17173

```
// Following GNAT's approach: try compile-time evaluation first,                          
```

### ada83.c:17174

```
// if not possible, store expression for later evaluation.                               
```

### ada83.c:17175

```
//                                                                                        
```

### ada83.c:17182

```
// Compile-time known bound -> BOUND_FLOAT; otherwise store the
```

### ada83.c:17183

```
// expression for later evaluation (per GNAT sem_attr).
```

### ada83.c:17191

```
// Check for DIGITS constraint (floating-point subtypes)
```

### ada83.c:17197

```
// Create constrained floating-point subtype with specified DIGITS
```

### ada83.c:17203

```
// Evaluate the digits expression
```

### ada83.c:17208

```
// Set bounds from range, or inherit from base type
```

### ada83.c:17212

```
// If explicit range given, override with evaluated bounds.
```

### ada83.c:17213

```
// Each bound is the static value when it evaluates at compile time,
```

### ada83.c:17214

```
// otherwise the expression itself (deferred to later evaluation).
```

### ada83.c:17227

```
// Reclassify NK_INDEX_CONSTRAINT as discriminant constraint                                
```

### ada83.c:17228

```
// when the base type is a discriminated record (RM 3.7.2).                                
```

### ada83.c:17229

```
// Also handles access-to-record types (RM 3.7.1).                                         
```

### ada83.c:17230

```
// Positional discriminant constraints like R1 (IDENT_BOOL (TRUE))                          
```

### ada83.c:17231

```
// get parsed as NK_INDEX_CONSTRAINT because they lack named assocs.                       
```

### ada83.c:17232

```
// Convert each range item to a positional association.                                    
```

### ada83.c:17233

```
//                                                                                          
```

### ada83.c:17257

```
// Wrap each expression in an association node (positional)
```

### ada83.c:17267

```
// Check for discriminant constraint (REC (A => E1, B => E2) style)                         
```

### ada83.c:17268

```
// Discriminant constraints use named associations where the choices                        
```

### ada83.c:17269

```
// are discriminant names - they should NOT be resolved as identifiers.                    
```

### ada83.c:17270

```
// Only the value expressions should be resolved. (RM 3.7.2)                                
```

### ada83.c:17271

```
//                                                                                          
```

### ada83.c:17277

```
// Only resolve the value expression, not the choices
```

### ada83.c:17281

```
// Locate the target discriminant. For positional assoc,
```

### ada83.c:17282

```
// it's i-th; for named, look up by choice name.
```

### ada83.c:17297

```
// RM 4.3.1: char literal vs enum disc type.
```

### ada83.c:17305

```
// RM 6.6: pre-set context type for overload resolution.
```

### ada83.c:17314

```
// Create a constrained subtype with discriminant values stored.                         
```

### ada83.c:17315

```
// For access-to-record, create a constrained designated type                             
```

### ada83.c:17316

```
// and wrap it in a new access type. (RM 3.7.1)                                           
```

### ada83.c:17317

```
//                                                                                        
```

### ada83.c:17323

```
// Copy all fields
```

### ada83.c:17328

```
// Allocate and fill constraint value + expression arrays
```

### ada83.c:17339

```
// Extract discriminant values from associations.                                      
```

### ada83.c:17340

```
// For static values, store the integer. For runtime expressions                        
```

### ada83.c:17341

```
// (function calls etc.), store the AST node for codegen evaluation.                   
```

### ada83.c:17342

```
//                                                                                      
```

### ada83.c:17356

```
// BOOLEAN literal: FALSE=0, TRUE=1. (A boolean-typed variable or
```

### ada83.c:17357

```
// parameter is NOT a literal — it falls through to runtime eval.)
```

### ada83.c:17365

```
// Enum literal: find position
```

### ada83.c:17378

```
// Named number or constant
```

### ada83.c:17381

```
// Fallback: try Eval_Const_Numeric for negative                                    
```

### ada83.c:17382

```
// literals like -6 (NK_UNARY_OP), type conversions,                                
```

### ada83.c:17383

```
// and other statically computable expressions.                                    
```

### ada83.c:17384

```
//                                                                                  
```

### ada83.c:17391

```
// Match association to discriminant by position or name
```

### ada83.c:17410

```
// Positional
```

### ada83.c:17417

```
// RM 3.7.2: note whether the record being constrained is not yet
```

### ada83.c:17418

```
// completely declared — it carries a component of a still-private
```

### ada83.c:17419

```
// or incomplete type. The non-discriminant parts of this constraint
```

### ada83.c:17420

```
// are then checked at the enclosing type's full declaration, not
```

### ada83.c:17421

```
// deferred to an explicit constraint (c37214a vs c37213a).
```

### ada83.c:17433

```
// RM 3.7.1: with the discriminants now fixed, a static constraint
```

### ada83.c:17434

```
// gives the subtype a definite size — fold the discriminant values
```

### ada83.c:17435

```
// into any dependent component's bounds so the layout is correct
```

### ada83.c:17436

```
// everywhere the subtype is used (object, aggregate, assignment,
```

### ada83.c:17437

```
// by-reference parameter). Skipped while the base is still
```

### ada83.c:17438

```
// incomplete; its layout is not yet known.
```

### ada83.c:17443

```
// RM 3.8.1: a constraint imposed on a still-incomplete type cannot
```

### ada83.c:17444

```
// have its discriminant-dependent component constraints checked
```

### ada83.c:17445

```
// until the full declaration provides the layout. Register the
```

### ada83.c:17446

```
// constrained view on the incomplete base so its full-declaration
```

### ada83.c:17447

```
// elaboration can flush the deferred check (c37216a).
```

### ada83.c:17464

```
// Wrap constrained record in a new access type
```

### ada83.c:17477

```
// Fallback: return base type if not a discriminated record
```

### ada83.c:17482

```
// Otherwise just use base type
```

### ada83.c:17489

```
// Modular type definition: type T is mod M (RM 3.5.4)                                      
```

### ada83.c:17490

```
// Range is 0 .. M-1, all arithmetic wraps modulo M.                                       
```

### ada83.c:17491

```
// The modulus expression is stored in integer_type.range.                                 
```

### ada83.c:17492

```
//                                                                                          
```

### ada83.c:17497

```
// Evaluate modulus using 128-bit-precise evaluator.                                     
```

### ada83.c:17498

```
// uint128_t handles 2**64 and 2**128 directly - no sentinel                              
```

### ada83.c:17499

```
// hacks needed. 2**128 wraps to 0 in uint128_t arithmetic                               
```

### ada83.c:17500

```
// (same as 2**64 wraps to 0 in uint64_t), so we detect it                                
```

### ada83.c:17501

```
// by checking if the exponent is 128. For 2**64, the                                    
```

### ada83.c:17502

```
// evaluator returns (uint128_t)1 << 64 = exact value.                                   
```

### ada83.c:17503

```
//                                                                                        
```

### ada83.c:17505

```
// true only for mod 2**128
```

### ada83.c:17508

```
// 2**128 overflows uint128_t to 0. Detect it
```

### ada83.c:17509

```
// explicitly from the AST exponent value.
```

### ada83.c:17522

```
// Non-constant modulus: use double evaluator as fallback
```

### ada83.c:17530

```
// Bounds: 0 .. modulus-1 (RM 3.5.4(9)).                                                 
```

### ada83.c:17531

```
// int128_t can represent 0 .. 2^127-1 directly. For                                     
```

### ada83.c:17532

```
// mod 2**128, the high bound is 2^128-1 which overflows                                  
```

### ada83.c:17533

```
// int128_t, so we store it as -1 (all bits set).                                        
```

### ada83.c:17534

```
//                                                                                        
```

### ada83.c:17537

```
// 2^128 - 1 = (int128_t)(-1) - all 128 bits set
```

### ada83.c:17544

```
// Compute size from modulus
```

### ada83.c:17553

```
// Max alignment 16 bytes for i128
```

### ada83.c:17557

```
// Signed integer type definition: range L .. H
```

### ada83.c:17561

```
// Resolve range bounds if present
```

### ada83.c:17566

```
// Extract bounds - integer literals, unary minus, or constants.
```

### ada83.c:17567

```
// A non-static bound leaves its slot unset (BOUND_NONE).
```

### ada83.c:17593

```
// Compute appropriate size from bounds
```

### ada83.c:17603

```
// Default to 64-bit
```

### ada83.c:17607

```
// RM 3.5.4: `TYPE T IS RANGE L..H` is a derivation from a predefined
```

### ada83.c:17608

```
// integer type — T is the constrained first subtype of an anonymous
```

### ada83.c:17609

```
// derived base holding the predefined type's full range. Build that
```

### ada83.c:17610

```
// base here so the type-graph invariant holds: base_type never
```

### ada83.c:17611

```
// crosses a derivation boundary; the base's parent_type records the
```

### ada83.c:17612

```
// derivation. (Stamping base_type = INTEGER directly would make T
```

### ada83.c:17613

```
// look like a subtype of INTEGER, erasing the distinction every
```

### ada83.c:17614

```
// overload-resolution predicate depends on.)
```

### ada83.c:17628

```
// Real type definition: digits D or delta D
```

### ada83.c:17632

```
// Fixed-point type: TYPE_FIXED with delta
```

### ada83.c:17637

```
// Extract delta value using constant expression evaluation
```

### ada83.c:17639

```
// NaN or invalid -> fallback
```

### ada83.c:17641

```
// Compute small (largest power of 2 <= delta) and scale (RM 3.5.9).
```

### ada83.c:17648

```
// Resolve range if present
```

### ada83.c:17653

```
// Extract bound values using constant expression evaluation.
```

### ada83.c:17654

```
// Only statically known bounds are recorded here.
```

### ada83.c:17661

```
// not NaN
```

### ada83.c:17667

```
// Size: typically 32 or 64 bits depending on range and precision
```

### ada83.c:17668

```
// 64-bit for safe default
```

### ada83.c:17671

```
// RM 3.5.9: the named fixed-point type is the constrained first-named
```

### ada83.c:17672

```
// subtype of an anonymous base spanning the full model range. T'BASE
```

### ada83.c:17673

```
// resolves through base_type to this wider type.
```

### ada83.c:17680

```
// Floating-point type: digits D
```

### ada83.c:17684

```
// Resolve digits expression
```

### ada83.c:17689

```
// Resolve range if present
```

### ada83.c:17694

```
// Size based on digits: <=6 = float, >6 = double
```

### ada83.c:17695

```
// Default double precision
```

### ada83.c:17704

```
// Store declared DIGITS
```

### ada83.c:17708

```
// Set bounds from range constraint if present.
```

### ada83.c:17709

```
// Each bound is the static value when it evaluates at compile time,
```

### ada83.c:17710

```
// otherwise the expression itself (deferred to later evaluation).
```

### ada83.c:17727

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:17728

```
// §12.2 Statement Resolution                                                                       
```

### ada83.c:17729

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:17731

```
// Freeze all types declared in a list
```

### ada83.c:17732

```
// Per RM 13.14: At the end of a declarative part, all entities are frozen
```

### ada83.c:17746

```
// Objects already freeze their type at declaration
```

### ada83.c:17751

```
// Subprogram bodies freeze all visible entities
```

### ada83.c:17759

```
// Populate a package symbol's exported[] array from its visible declarations.                     
```

### ada83.c:17760

```
// This must be called after all visible declarations are resolved so that                          
```

### ada83.c:17761

```
// decl->symbol pointers are valid. Used by both inline packages and loaded specs.                 
```

### ada83.c:17762

```
//                                                                                                  
```

### ada83.c:17763

```
// RM 3.4: a declaration that introduces an explicit subprogram — a subprogram
```

### ada83.c:17764

```
// declaration or body, a renaming, or a subprogram instantiation — into the
```

### ada83.c:17765

```
// visible part of a package. Such subprograms are exported and derivable of the
```

### ada83.c:17766

```
// first kind.
```

### ada83.c:17780

```
// RM 8.4: an implicit (inherited) operation declared in a package's visible
```

### ada83.c:17781

```
// part is made directly visible by a USE clause, exactly like an explicit one.
```

### ada83.c:17782

```
// Unlike explicit declarations it has no syntax node in the visible-declaration
```

### ada83.c:17783

```
// list — it lives in the spec scope as a bare symbol created by derivation — so
```

### ada83.c:17784

```
// it is gathered from the scope. Use-visible aliases (carrying `aliased`) belong
```

### ada83.c:17785

```
// to some other package and are excluded.
```

### ada83.c:17796

```
// Count exports including nested items (enum literals, multiple object names)
```

### ada83.c:17806

```
// Enumeration literals (direct or inherited from derived type)
```

### ada83.c:17813

```
// Derived enum type: count inherited literals
```

### ada83.c:17821

```
// Nested packages
```

### ada83.c:17823

```
// Nested generics (e.g., TEXT_IO.INTEGER_IO)
```

### ada83.c:17825

```
// Task type/object symbol
```

### ada83.c:17829

```
// Inherited operations of visible-part derived types (gathered from the scope,
```

### ada83.c:17830

```
// as they have no declaration node of their own).
```

### ada83.c:17839

```
// Allocate and fill
```

### ada83.c:17855

```
// Enumeration literals
```

### ada83.c:17867

```
// Derived enum type: export inherited literal symbols.
```

### ada83.c:17868

```
// These were created during NK_TYPE_DECL resolution for NK_DERIVED_TYPE.
```

### ada83.c:17873

```
// Find the literal symbol with matching name and type in scope
```

### ada83.c:17887

```
// If not found in scope (e.g., overloaded away), create a new symbol
```

### ada83.c:17897

```
// A subprogram in the visible part is derivable of the first kind (RM 3.4).
```

### ada83.c:17898

```
// Stamp it here too so separately loaded specs, whose resolution does not
```

### ada83.c:17899

```
// pass through the visible-part toggle, are still recognised.
```

### ada83.c:17912

```
// Export nested generic packages/subprograms (e.g., TEXT_IO.INTEGER_IO)
```

### ada83.c:17916

```
// Export task type/object symbol.                                                             
```

### ada83.c:17917

```
// For single tasks (not task types), decl->symbol is the type symbol                           
```

### ada83.c:17918

```
// which carries the TYPE_TASK info and exported entries.                                      
```

### ada83.c:17919

```
// RM 9.1: task objects declared in a package spec are visible via                              
```

### ada83.c:17920

```
// selected component notation (e.g., PKG.TASK_NAME.ENTRY).                                    
```

### ada83.c:17921

```
//                                                                                              
```

### ada83.c:17927

```
// Export the visible-part inherited operations counted above.
```

### ada83.c:17941

```
// Pre-register labels in a statement list to allow forward gotos.                                 
```

### ada83.c:17942

```
// Labels can appear as:                                                                            
```

### ada83.c:17943

```
//   1. NK_LABEL nodes wrapping other statements                                                    
```

### ada83.c:17944

```
//   2. The .label field of NK_BLOCK or NK_LOOP nodes (Ada allows naming blocks/loops)              
```

### ada83.c:17945

```
//                                                                                                  
```

### ada83.c:17948

```
// RM 5.1: labels, block names, and loop names are implicitly declared at
```

### ada83.c:17949

```
// the end of the declarative part of the innermost enclosing BODY or
```

### ada83.c:17950

```
// BLOCK — registration happens with that region's scope current. A
```

### ada83.c:17951

```
// construct that pushes a non-region scope around a statement list (an
```

### ada83.c:17952

```
// accept statement's formal scope) preregisters the list BEFORE pushing;
```

### ada83.c:17953

```
// idempotence here makes the later Resolve_Statement_List pass a no-op
```

### ada83.c:17954

```
// for already-registered labels.
```

### ada83.c:17963

```
// `<< A >> << B >> stmt` parses as a nested label chain; every link
```

### ada83.c:17964

```
// names this same list position and must be visible to forward
```

### ada83.c:17965

```
// GOTOs in the sequence (c59001b). Register the tail links here and
```

### ada83.c:17966

```
// let the shared path below register the head.
```

### ada83.c:18000

```
// RM 9.4: is the construct owning this declarative part a task master? True
```

### ada83.c:18001

```
// when the declarations create task dependents: a single task object; an
```

### ada83.c:18002

```
// object whose type contains a task component; an access-to-task(-bearing)
```

### ada83.c:18003

```
// type declaration (tasks its allocators create depend on this master); or
```

### ada83.c:18004

```
// any of those inside a nested package (the package itself is not a master).
```

### ada83.c:18005

```
// Inside a generic unit, an object of a formal private type counts
```

### ada83.c:18006

```
// conservatively: the actual may be task-bearing, instances share this
```

### ada83.c:18007

```
// resolved tree, and over-bracketing a taskless scope is harmless (an empty
```

### ada83.c:18008

```
// master joins nothing). Computed once at resolution and stored on the
```

### ada83.c:18009

```
// owning node — codegen only reads the flag.
```

### ada83.c:18024

```
// Generic formal private type: the actual may carry tasks.
```

### ada83.c:18030

```
// Access-to-task(-bearing) type: allocator-created tasks depend on the
```

### ada83.c:18031

```
// master that elaborates the access type definition (RM 9.4). A derived
```

### ada83.c:18032

```
// access type strictly defers to its ultimate parent's master; counting
```

### ada83.c:18033

```
// it here only over-brackets, which is harmless. Type declarations
```

### ada83.c:18034

```
// carry their type on the SYMBOL, not the node.
```

### ada83.c:18049

```
// A generic package instantiation declares whatever the instance's
```

### ada83.c:18050

```
// expanded spec and body declare (RM 12.3): a task inside the instance
```

### ada83.c:18051

```
// depends on the scope performing the instantiation (RM 9.4). Without
```

### ada83.c:18052

```
// this the scope never becomes a master — its done flag is never
```

### ada83.c:18053

```
// raised and an instance task parked at a terminate alternative sleeps
```

### ada83.c:18054

```
// through the teardown join (c94008d's SHARE tasks).
```

### ada83.c:18074

```
// Program-unique master scope identifiers (RM 9.4). Assigned at resolution
```

### ada83.c:18075

```
// to every construct that is a task master; baked into the emitted
```

### ada83.c:18076

```
// __ada_master_enter calls and into access-to-task types, so an allocator
```

### ada83.c:18077

```
// can find the record of the master that elaborated its access type
```

### ada83.c:18078

```
// (__ada_master_find walks the running thread's master chain — under
```

### ada83.c:18079

```
// recursion the nearest, i.e. current, elaboration wins).
```

### ada83.c:18085

```
// Tag every access-to-task(-bearing) type declared in this master's
```

### ada83.c:18086

```
// declarative part (including nested packages — they are not masters) with
```

### ada83.c:18087

```
// the master's scope id. A derived access type defers to its ultimate
```

### ada83.c:18088

```
// parent's master (RM 9.4): copy the parent's id — the parent is declared
```

### ada83.c:18089

```
// before it, so its id is already final.
```

### ada83.c:18095

```
// Type declarations carry their type on the SYMBOL, not the node.
```

### ada83.c:18117

```
// Install the parameters of a subprogram profile into the current scope
```

### ada83.c:18118

```
// (RM 6.1): resolve each parameter specification's type mark, create a
```

### ada83.c:18119

```
// SYMBOL_PARAMETER for every defining name, and link the name node to it.
```

### ada83.c:18120

```
// When the owning subprogram symbol is supplied, each created symbol is also
```

### ada83.c:18121

```
// recorded in the corresponding Parameter_Info slot so code generation
```

### ada83.c:18122

```
// reaches the same symbols the resolved body references.
```

### ada83.c:18137

```
// Mark at resolution (not first at prologue emission) so a nested
```

### ada83.c:18138

```
// body emitted before its encloser's prologue still sees the flag
```

### ada83.c:18139

```
// and aliases the frame-published copy (RM 3.7.4, c64106d).
```

### ada83.c:18153

```
// Resolve the interior of a subprogram body: the declarative part, the
```

### ada83.c:18154

```
// RM 9.4 task-master bookkeeping that part implies, the statement sequence,
```

### ada83.c:18155

```
// and the exception handlers. An ordinary body freezes its declarative part
```

### ada83.c:18156

```
// at its end (RM 13.14); generic template bodies and instance body clones
```

### ada83.c:18157

```
// resolve without that freeze, so those callers pass false.
```

### ada83.c:18177

```
// First pass: register all labels to allow forward gotos
```

### ada83.c:18180

```
// Second pass: resolve all statements
```

### ada83.c:18191

```
// Propagate target type to value for context-dependent typing:
```

### ada83.c:18192

```
// aggregates (RM 4.3), calls/binary ops/identifiers needing
```

### ada83.c:18193

```
// overload res by return type (RM 6.6).
```

### ada83.c:18203

```
// RM 3.4: a .ALL dereference of a constrained access may denote an
```

### ada83.c:18204

```
// object whose actual discriminants — a base-range value reached
```

### ada83.c:18205

```
// through an inherited operation — differ from the access subtype's
```

### ada83.c:18206

```
// constraint. Type the source against the unconstrained base so it is
```

### ada83.c:18207

```
// matched to the run-time object, not to the static constraint.
```

### ada83.c:18216

```
// A string-literal source needs the target's COMPONENT type — so a
```

### ada83.c:18217

```
// non-CHARACTER character component (a user char enum, or a CHARACTER
```

### ada83.c:18218

```
// subtype with a component range) drives the correct element codes and
```

### ada83.c:18219

```
// the RM 4.2 component-subtype check. It must NOT inherit the target's
```

### ada83.c:18220

```
// index bounds, however: RM 3.6.1 supplies an applicable index constraint
```

### ada83.c:18221

```
// to an assignment, but for a SLICE target that constraint is the slice's
```

### ada83.c:18222

```
// range, which lives in the target node, not its type (the type is the
```

### ada83.c:18223

```
// whole array's subtype). Rather than mis-bound the literal, seed an
```

### ada83.c:18224

```
// unconstrained STRING-shaped type (POSITIVE index, so the literal keeps
```

### ada83.c:18225

```
// its own 1..LENGTH bounds) carrying the target's element type; the
```

### ada83.c:18226

```
// assignment's own length check then relates the two. A plain-CHARACTER
```

### ada83.c:18227

```
// component needs none of this and stays on the default STRING path.
```

### ada83.c:18242

```
// Resolve character literal as enum literal in assignment (RM 3.5.1)
```

### ada83.c:18247

```
// Follow parent_type (private>full), base_type chains to enum
```

### ada83.c:18253

```
// Type check: value must be compatible with target
```

### ada83.c:18258

```
// RM 8.6: Overload resolution retry.  When target is
```

### ada83.c:18259

```
// X.COMPONENT where X is an overloaded function call, try
```

### ada83.c:18260

```
// alternative overloads whose result type has a matching
```

### ada83.c:18261

```
// component.  E.g., FUNC(2).A := NEW INTEGER'(2) where two
```

### ada83.c:18262

```
// FUNCs return different record types — pick the one whose
```

### ada83.c:18263

```
// .A component matches the RHS type.
```

### ada83.c:18277

```
// Dereference access type to get record type
```

### ada83.c:18282

```
// Found matching overload - update prefix symbol and types
```

### ada83.c:18291

```
// RM 8.6, value side: a bare identifier may name BOTH a
```

### ada83.c:18292

```
// parameterized subprogram and an enumeration literal
```

### ada83.c:18293

```
// (FUNCTION TRUE (X : INTEGER) alongside BOOLEAN's TRUE) —
```

### ada83.c:18294

```
// only the literal can be a value of the target's type, so
```

### ada83.c:18295

```
// re-resolve by the expected type.
```

### ada83.c:18315

```
// Propagate enclosing function's return type to untyped
```

### ada83.c:18316

```
// aggregates and concatenations (RM 5.8, 4.5.3)
```

### ada83.c:18318

```
// A procedure call statement's target is a PROCEDURE (or an ENTRY) —
```

### ada83.c:18319

```
// never a function. If the name is overloaded across procedure and
```

### ada83.c:18320

```
// function homographs, drop the function interpretations before top-down
```

### ada83.c:18321

```
// resolution picks (RM 6.4 / 8.6: syntactic position selects).
```

### ada83.c:18346

```
// Pre-set type on aggregates, calls, binary ops, and
```

### ada83.c:18347

```
// identifiers so overload resolution (RM 6.6) sees the
```

### ada83.c:18348

```
// function's declared return type as context (e.g. RETURN
```

### ada83.c:18349

```
// ORANGE picks CITRUS.ORANGE vs HUE.ORANGE).
```

### ada83.c:18360

```
// RM 5.3: the condition of IF / ELSIF is of type BOOLEAN.
```

### ada83.c:18386

```
// FOR loop - create loop variable in new scope.
```

### ada83.c:18387

```
// Inherit owner from enclosing scope for proper name mangling
```

### ada83.c:18392

```
// RM 4.1.3(19): the loop name prefixes the loop parameter —
```

### ada83.c:18393

```
// LP.I inside loop LP denotes I (c41307d). Link the scope so the
```

### ada83.c:18394

```
// label-prefix lookup can search it.
```

### ada83.c:18399

```
// Resolve range expression FIRST to get its type
```

### ada83.c:18407

```
// Loop variable type comes from the range expression.                                 
```

### ada83.c:18408

```
// Per Ada RM 5.5(6): if the range is universal_integer,                                
```

### ada83.c:18409

```
// the loop parameter is of type INTEGER. Standard:                                     
```

### ada83.c:18410

```
// see Emit_Loop_Statement - uses Standard_Integer for                                  
```

### ada83.c:18411

```
// universal ranges.                                                                   
```

### ada83.c:18412

```
//                                                                                      
```

### ada83.c:18423

```
// WHILE or bare LOOP
```

### ada83.c:18426

```
// RM 5.5: the condition of a WHILE loop is of type BOOLEAN.
```

### ada83.c:18439

```
// Inherit owner from enclosing scope for proper symbol parenting
```

### ada83.c:18442

```
// RM 8.3: a block label introduces a declarative region. Attach the
```

### ada83.c:18443

```
// pushed scope to the label symbol so expanded names like LABEL.X
```

### ada83.c:18444

```
// can resolve X in the block's declarations.
```

### ada83.c:18448

```
// Resolve declarations first (adds symbols to scope)
```

### ada83.c:18458

```
// Freeze all types at end of declarative part (RM 13.14)
```

### ada83.c:18461

```
// Then resolve statements that use those symbols
```

### ada83.c:18475

```
// RM 5.7: the condition of an EXIT WHEN is of type BOOLEAN. Seed the
```

### ada83.c:18476

```
// context so an overloaded call (c87b42a: overloaded F) resolves
```

### ada83.c:18477

```
// to the BOOLEAN-returning homograph.
```

### ada83.c:18493

```
// Resolve exception names
```

### ada83.c:18504

```
// Case alternative - resolve choices and body
```

### ada83.c:18517

```
// Label symbol was pre-registered by Preregister_Labels.
```

### ada83.c:18518

```
// Just resolve the labeled statement.
```

### ada83.c:18527

```
// Look up the target label
```

### ada83.c:18540

```
// Store resolved label for code generation
```

### ada83.c:18548

```
// ACCEPT statement for task entry - resolve index and parameters.                           
```

### ada83.c:18549

```
// Each accept statement has its own scope for parameters to avoid                            
```

### ada83.c:18550

```
// naming conflicts with parameters from other accept statements                              
```

### ada83.c:18551

```
// in the same selective wait (e.g., multiple accepts with param X).                         
```

### ada83.c:18552

```
//                                                                                            
```

### ada83.c:18554

```
// Look up the entry symbol by name - it should be in the task's scope
```

### ada83.c:18559

```
// Select-alternative guard (RM 9.7.1).
```

### ada83.c:18564

```
// The accept body's labels declare in the ENCLOSING region (RM 5.1:
```

### ada83.c:18565

```
// an accept's formal scope is not a body or block) — register them
```

### ada83.c:18566

```
// now, before the formal scope is pushed, so a label inside the
```

### ada83.c:18567

```
// accept is reachable as T.LABEL through the task name (c41307d's
```

### ada83.c:18568

```
// T.BLK.B). Preregister_Labels is idempotent; the body's own
```

### ada83.c:18569

```
// Resolve_Statement_List pass re-registers nothing.
```

### ada83.c:18572

```
// Push new scope for accept parameters - use current scope owner
```

### ada83.c:18573

```
// so parameters are considered local (not global) for naming.
```

### ada83.c:18574

```
// Each accept statement needs its own scope because multiple accepts
```

### ada83.c:18575

```
// in a selective wait may have parameters with the same name.
```

### ada83.c:18576

```
//
```

### ada83.c:18579

```
// Resolve accept parameters
```

### ada83.c:18582

```
// Accept formals marshal BY VALUE through the rendezvous record (with
```

### ada83.c:18583

```
// copy-back at the accept's end) — EXCEPT records, whose slot holds
```

### ada83.c:18584

```
// the CALLER'S RECORD ADDRESS (the accept lowering binds them by
```

### ada83.c:18585

```
// reference, RM 6.2). Flag record formals NOW, at resolution time: a
```

### ada83.c:18586

```
// nested unit inside the accept body may have its frame aliases
```

### ada83.c:18587

```
// EMITTED before the accept's own lowering runs (deferred-body
```

### ada83.c:18588

```
// ordering), and its alias must load the pointer rather than treat
```

### ada83.c:18589

```
// the slot as the record itself (c85005c: PROC1 reads MAIN_TASK's
```

### ada83.c:18590

```
// IN OUT record accept formal uplevel).
```

### ada83.c:18602

```
// Overloaded entries share a name; the accept denotes the overload whose
```

### ada83.c:18603

```
// parameter profile matches this accept's, not merely the first found by
```

### ada83.c:18604

```
// name (RM 9.5(2)). Match on parameter count and types so each accept
```

### ada83.c:18605

```
// binds the entry index of the entry it actually accepts.
```

### ada83.c:18631

```
// Resolve accept body statements. While they resolve, the entry's
```

### ada83.c:18632

```
// symbol carries this accept's formal scope so the RM 4.1.3(20)
```

### ada83.c:18633

```
// expanded name E.FORMAL finds the formal (c41307d's E1.A); the
```

### ada83.c:18634

```
// previous value is restored on exit — nested accepts for the SAME
```

### ada83.c:18635

```
// entry are illegal (RM 9.5), so one saved slot suffices.
```

### ada83.c:18644

```
// Pop accept scope
```

### ada83.c:18652

```
// SELECT statement - resolve alternatives and ELSE part
```

### ada83.c:18657

```
// A terminate alternative's WHEN guard (RM 9.7.1) — a BOOLEAN
```

### ada83.c:18658

```
// condition, evaluated once at the start of the wait.
```

### ada83.c:18675

```
// DELAY statement - resolve delay expression and, for a select delay
```

### ada83.c:18676

```
// alternative, its WHEN guard (RM 9.7.1).
```

### ada83.c:18679

```
// RM 9.6: the delay expression is of the predefined fixed-point
```

### ada83.c:18680

```
// type DURATION. Seed the expression's type when overloaded so
```

### ada83.c:18681

```
// the resolver reaches the DURATION-returning homograph
```

### ada83.c:18682

```
// (c87b54a: F with several return types).
```

### ada83.c:18698

```
// ABORT statement - resolve task name expressions
```

### ada83.c:18708

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:18709

```
// §12.3 Declaration Resolution                                                                     
```

### ada83.c:18710

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:18712

```
// Install all exported symbols from a declaration list into the symbol table.                     
```

### ada83.c:18713

```
// Handles objects, exceptions, and enumeration literals uniformly.                                
```

### ada83.c:18714

```
// Extracted from two identical visible/private installation blocks.                               
```

### ada83.c:18715

```
//                                                                                                  
```

### ada83.c:18716

```
// Re-install into the current (body) scope the implicit derived operations
```

### ada83.c:18717

```
// (RM 3.4) that were created while analysing a package spec. They are not
```

### ada83.c:18718

```
// declaration nodes, so Install_Declaration_Symbols does not reach them, yet
```

### ada83.c:18719

```
// the body must see them exactly as the spec did (RM 7.1).
```

### ada83.c:18723

```
// Walk the hash buckets and overload chains, not the flat symbol array: an
```

### ada83.c:18724

```
// overloaded homograph is chained but not recorded in symbols[]. Symbol_Add
```

### ada83.c:18725

```
// relinks next_in_bucket/next_overload into the body scope, so each successor
```

### ada83.c:18726

```
// pointer is captured before the symbol is re-added.
```

### ada83.c:18733

```
// Symbol_Add overwrites parent with the body scope's owner; an
```

### ada83.c:18734

```
// inherited operation keeps the parent operation's nesting for
```

### ada83.c:18735

```
// mangling, exactly as Create_Derived_Operation set it.
```

### ada83.c:18750

```
// For single task declarations (RM 9.1), both a type symbol and an                             
```

### ada83.c:18751

```
// anonymous object variable were created during semantic analysis.                            
```

### ada83.c:18752

```
// decl->symbol is the type; we must also install the variable so that                          
```

### ada83.c:18753

```
// codegen can find it for global allocation and task start.                                   
```

### ada83.c:18754

```
// Grab the variable from the type's original defining_scope (the spec                          
```

### ada83.c:18755

```
// scope) BEFORE Symbol_Add changes defining_scope to the body scope.                          
```

### ada83.c:18756

```
//                                                                                              
```

### ada83.c:18775

```
// RM 8.4: a USE clause in the spec extends its effect to the end of the
```

### ada83.c:18776

```
// package's scope — including the private part and the BODY. Re-apply
```

### ada83.c:18777

```
// it in the scope being populated so its use-visible aliases exist
```

### ada83.c:18778

```
// there too (c84008a: types from USE'd PACK1 named in PACK2's body).
```

### ada83.c:18782

```
// Multi-name object declarations: install each named symbol
```

### ada83.c:18789

```
// Exception declarations: same multi-name pattern
```

### ada83.c:18796

```
// Enumeration type: install each literal as a separate symbol
```

### ada83.c:18810

```
// RM 3.9: a standalone forward subprogram spec in a subprogram's
```

### ada83.c:18811

```
// declarative part is callable before its later body elaborates, so it
```

### ada83.c:18812

```
// earns an access-before-elaboration flag (codegen defines the flag at this
```

### ada83.c:18813

```
// same spec). A body's or stub's own specification is a child of its body
```

### ada83.c:18814

```
// node, never a list item, so a spec ITEM here is always a genuine forward
```

### ada83.c:18815

```
// declaration. Recording the flag during resolution — not codegen — lets a
```

### ada83.c:18816

```
// SEPARATE subunit, which resolves but never codegens the parent, still
```

### ada83.c:18817

```
// check the flag on a call back into the parent (c39006c).
```

### ada83.c:18827

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:18828

```
// §12.4.1 Derived Type Operation Inheritance (RM 3.4)                                              
```

### ada83.c:18829

```
//                                                                                                  
```

### ada83.c:18830

```
// When TYPE T IS NEW PARENT is declared, T inherits primitive operations of                        
```

### ada83.c:18831

```
// PARENT. A primitive operation is a subprogram declared in the same scope as                      
```

### ada83.c:18832

```
// the type, with a parameter or return type of that type.                                         
```

### ada83.c:18833

```
//                                                                                                  
```

### ada83.c:18834

```
// The inherited operation has T substituted for PARENT in its profile. We create                   
```

### ada83.c:18835

```
// a new symbol that wraps the parent's implementation with type conversions.                      
```

### ada83.c:18836

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:18838

```
// Check if two types represent the same named type.                                               
```

### ada83.c:18839

```
// Handles private types where partial (visible) and full (private) views                           
```

### ada83.c:18840

```
// have different Type_Info pointers but represent the same type.                                  
```

### ada83.c:18841

```
//                                                                                                  
```

### ada83.c:18842

```
// RM 12.1.2: a generic formal type denotes its actual within an instance. The
```

### ada83.c:18843

```
// binding is a renamed copy of the actual (so the formal's predefined operators
```

### ada83.c:18844

```
// stay bound to the actual's); peel it back to the actual type the instance was
```

### ada83.c:18845

```
// given — the generic_actual link recorded at the binding — which is what an
```

### ada83.c:18846

```
// instance's external profile must expose. The loop resolves an instance
```

### ada83.c:18847

```
// nested in an instance, whose actual is itself a view.
```

### ada83.c:18858

```
// For private types, partial and full views have same name but different Type_Info.
```

### ada83.c:18862

```
// Same name: check if they share a defining symbol
```

### ada83.c:18866

```
// For types declared in the same package, same name = same type
```

### ada83.c:18874

```
// Check if a subprogram has the given type in its profile (parameter or return)
```

### ada83.c:18879

```
// Check return type for functions
```

### ada83.c:18882

```
// Check parameter types
```

### ada83.c:18889

```
// Helper to create a derived operation from a parent operation
```

### ada83.c:18893

```
// reserved for future use
```

### ada83.c:18896

```
// Link to parent's implementation
```

### ada83.c:18898

```
// Needs unique_id suffix
```

### ada83.c:18900

```
// Copy and adjust parameters - substitute derived_type for parent_type
```

### ada83.c:18911

```
// Adjust return type for functions
```

### ada83.c:18917

```
// Make visible so overload resolution can find it
```

### ada83.c:18920

```
// Add to current scope
```

### ada83.c:18923

```
// Set parent to match the parent operation's parent for proper name mangling                     
```

### ada83.c:18924

```
// and to avoid false nested function detection. The derived operation                            
```

### ada83.c:18925

```
// wraps the parent operation so should be at the same nesting level.                            
```

### ada83.c:18926

```
// Done AFTER Symbol_Add since it overwrites parent.                                             
```

### ada83.c:18927

```
//                                                                                                
```

### ada83.c:18931

```
// RM 3.4: follow a derived operation's inheritance chain to the ultimate
```

### ada83.c:18932

```
// operation that actually carries a body (GNAT's Ultimate_Alias). A derived
```

### ada83.c:18933

```
// subprogram has no code of its own — a call to it binds to the parent's
```

### ada83.c:18934

```
// implementation, and a multi-level derivation (S is new T is new P) chains
```

### ada83.c:18935

```
// derived op -> derived op -> real body, so only the last has code. The parent
```

### ada83.c:18936

```
// of a derived operation may itself be a renaming (RM 8.5), so the two peels
```

### ada83.c:18937

```
// are interleaved until a symbol that is neither is reached.
```

### ada83.c:18947

```
// RM 3.4: a derivable subprogram of the "first kind" is an explicit subprogram
```

### ada83.c:18948

```
// declared immediately within the visible part of the package that also holds
```

### ada83.c:18949

```
// the parent type. Such a subprogram hides a homographic derived (second-kind)
```

### ada83.c:18950

```
// subprogram for the purpose of further derivation.
```

### ada83.c:18962

```
// RM 3.4 derivability test for a candidate primitive operation `sub` living in
```

### ada83.c:18963

```
// the parent type's declarative region `scope`.
```

### ada83.c:18964

```
//   * A derived (second-kind) operation is derivable unless a first-kind
```

### ada83.c:18965

```
//     homograph hides it.
```

### ada83.c:18966

```
//   * An explicit operation is derivable only as a first kind, i.e. when it is
```

### ada83.c:18967

```
//     declared immediately within a package's visible part. An explicit
```

### ada83.c:18968

```
//     operation in a private part, package body, or block declarative part is
```

### ada83.c:18969

```
//     not derivable.
```

### ada83.c:18976

```
// Create inherited operations for a derived type (RM 3.4)
```

### ada83.c:18981

```
// `TYPE T IS NEW P.INT RANGE ...` wraps a scalar parent in an anonymous
```

### ada83.c:18982

```
// constrained-base intermediate whose `defining_symbol` is null; only the
```

### ada83.c:18983

```
// named ancestor (P.INT) owns the primitive-op scope, so for scalar
```

### ada83.c:18984

```
// parents walk the base/parent chain to reach it. Array/record parents
```

### ada83.c:18985

```
// already carry `defining_symbol` on the wrapper (constrained subtypes are
```

### ada83.c:18986

```
// handled separately by the composite-type paths), so the walk stays
```

### ada83.c:18987

```
// limited to scalars to avoid disturbing the composite derivation path.
```

### ada83.c:18999

```
// The primitive operations were declared with the NAMED parent's Type_Info,
```

### ada83.c:19000

```
// not any intermediate wrapper. Match against that; otherwise a
```

### ada83.c:19001

```
// constrained-base wrapper hides every operator from the primitiveness
```

### ada83.c:19002

```
// check and Derive_Subprograms silently produces no derived ops.
```

### ada83.c:19007

```
// Scan the parent type's declarative region. Unlike the package export list,
```

### ada83.c:19008

```
// this sees every primitive operation including the implicit (derived) ones,
```

### ada83.c:19009

```
// which a further derivation must reconsider (RM 3.4 second kind). The
```

### ada83.c:19010

```
// derivability test then keeps exactly the operations the derived type
```

### ada83.c:19011

```
// inherits.
```

### ada83.c:19025

```
// No declarative scope available (a minimally loaded package): fall back to
```

### ada83.c:19026

```
// the export list, whose entries are visible-part operations by construction.
```

### ada83.c:19038

```
// Lay down one Component_Info per discriminant name (RM 3.7.1) into `comps`,
```

### ada83.c:19039

```
// returning the total byte size. Shared by the record-completion and private-
```

### ada83.c:19040

```
// type discriminant builders, which laid out byte-identical component records.
```

### ada83.c:19067

```
// pragma Import/Export carry an optional third argument: the external link
```

### ada83.c:19068

```
// name as a string literal. Record it on the entity when present.
```

### ada83.c:19076

```
// Map the convention-name argument of pragma Import/Convention to a Convention.
```

### ada83.c:19088

```
// Shared prologue of the Import/Export/Convention pragma arms, whose argument
```

### ada83.c:19089

```
// list starts (convention, entity, ...): unwrap both arguments and look the
```

### ada83.c:19090

```
// entity up. Returns NULL when the argument list is too short, the entity is
```

### ada83.c:19091

```
// not an identifier, or no symbol with that name is visible.
```

### ada83.c:19092

```
// *out_convention receives the unwrapped convention argument (or NULL).
```

### ada83.c:19102

```
// Match a name against an instance's resolved actual bindings; return the
```

### ada83.c:19103

```
// bound actual type, or NULL when the name is not a formal type of the
```

### ada83.c:19104

```
// instance.
```

### ada83.c:19114

```
// Parameter types of a generic formal subprogram, with formal types
```

### ada83.c:19115

```
// substituted by the instantiation's actuals. Fills at most two slots of
```

### ada83.c:19116

```
// `out` and returns the number filled.
```

### ada83.c:19139

```
// Resolve the "=" a "/=" generic actual denotes (RM 6.7: "/=" is never a
```

### ada83.c:19140

```
// declarable function — as an actual it names the negation of the
```

### ada83.c:19141

```
// corresponding "="). Builds the formal subprogram's two-parameter profile
```

### ada83.c:19142

```
// with formal types substituted by their actuals, then overload-resolves
```

### ada83.c:19143

```
// "=" against it. Returns the user-defined "=" or NULL when only the
```

### ada83.c:19144

```
// predefined equality applies.
```

### ada83.c:19156

```
// Does a candidate "=" function's parameter profile match a generic formal
```

### ada83.c:19157

```
// subprogram's substituted profile? Used to validate an expanded-name "/="
```

### ada83.c:19158

```
// actual (PK."/="): the package's "=" applies only when its operand types
```

### ada83.c:19159

```
// agree with the formal's (RM 12.3); otherwise the actual denotes the
```

### ada83.c:19160

```
// predefined inequality.
```

### ada83.c:19171

```
// unresolved side: not a mismatch
```

### ada83.c:19188

```
// Formal type (RM 12.1.2): a distinct type whose kind gives the
```

### ada83.c:19189

```
// operations of its class inside the template.
```

### ada83.c:19216

```
// A formal access type names its designated subtype in the formal
```

### ada83.c:19217

```
// part (RM 12.1.2: TYPE L IS ACCESS S): resolve the mark so an
```

### ada83.c:19218

```
// implicit dereference in the template — LINK1.D — reaches the
```

### ada83.c:19219

```
// designated record's components (cc3128a).
```

### ada83.c:19227

```
// Known discriminants of a formal private type (RM 12.1.2):
```

### ada83.c:19228

```
// resolve each discriminant's type and lay the discriminants out
```

### ada83.c:19229

```
// as components so selected names like P.D resolve in the
```

### ada83.c:19230

```
// template.
```

### ada83.c:19261

```
// Formal object (RM 12.1.1): mode IN is a constant, mode IN OUT is a
```

### ada83.c:19262

```
// variable; mode OUT does not exist for generic formal objects.
```

### ada83.c:19277

```
// The default expression resolves against the formal's type and
```

### ada83.c:19278

```
// is only legal for mode IN (RM 12.1.1).
```

### ada83.c:19310

```
// Formal subprogram (RM 12.1.3): full parameter profile so calls in
```

### ada83.c:19311

```
// the template overload-resolve against it.
```

### ada83.c:19313

```
// RM 12.1/12.1.3: the formal part elaborates LINEARLY — an IS-name
```

### ada83.c:19314

```
// default resolves seeing only the formals declared BEFORE it, never
```

### ada83.c:19315

```
// itself or a later homograph (cc1301a: `WITH FUNCTION "*" IS "-"`
```

### ada83.c:19316

```
// ahead of a formal "-" must bind the enclosing scope's "-").
```

### ada83.c:19317

```
// Resolving here, before this formal's own symbol installs, gives
```

### ada83.c:19318

```
// exactly that horizon; the post-install sweep skips already-resolved
```

### ada83.c:19319

```
// names.
```

### ada83.c:19329

```
// The profile's type marks resolve under the same linear horizon:
```

### ada83.c:19330

```
// a mark BEFORE the formal type declaration denotes the enclosing
```

### ada83.c:19331

```
// scope's type, one AFTER denotes the formal (cc1301a declares
```

### ada83.c:19332

```
// NEXT (Q : INTEGER) on both sides of TYPE INTEGER IS RANGE <>).
```

### ada83.c:19390

```
// RM 12.1.3: calls through the formal use the FORMAL's own
```

### ada83.c:19391

```
// default expressions (never the actual's).
```

### ada83.c:19490

```
// Parameters L and R, in a scope of the wrapper's own.
```

### ada83.c:19510

```
// return not "="(L, R);
```

### ada83.c:19546

```
// Parameters mirror the formal subprogram's profile; their type marks
```

### ada83.c:19547

```
// resolve against the instance's bindings (formal types -> actual views).
```

### ada83.c:19548

```
// Each parameter becomes an argument of the wrapped attribute call.
```

### ada83.c:19552

```
// RM 12.3.6: a default subprogram given by an attribute (T'SUCC, T'PRED,
```

### ada83.c:19553

```
// T'IMAGE, ...) denotes the attribute's OWN operation, whose parameter and
```

### ada83.c:19554

```
// result subtypes are the base type — not the formal subprogram's declared
```

### ada83.c:19555

```
// (possibly constrained) subtype. Keeping the formal's subtype would
```

### ada83.c:19556

```
// range-check a base-valued argument or result against the constraint —
```

### ada83.c:19557

```
// e.g. SUCC(T'LAST) yields the value one past a constrained T (cc1302a).
```

### ada83.c:19558

```
// Widen the wrapper's scalar profile subtypes to their base accordingly.
```

### ada83.c:19587

```
// A two-parameter profile wraps a binary operator, a one-parameter
```

### ada83.c:19588

```
// profile a unary one; any other arity synthesizes no body.
```

### ada83.c:19612

```
// Slot accounting matches Install_Generic_Formal_Symbols: object formals
```

### ada83.c:19613

```
// consume one slot per name, every other formal one slot.
```

### ada83.c:19624

```
// Formal type: the instance-scope name denotes the actual subtype
```

### ada83.c:19625

```
// (RM 12.3 rule (d)). Predefined operations of the formal rebind to
```

### ada83.c:19626

```
// the actual's through ordinary resolution against this binding.
```

### ada83.c:19631

```
// Capture the RM 12.3(81) subtype-agreement pairs now, while the
```

### ada83.c:19632

```
// formal part's marks resolve (the emission point has no scopes).
```

### ada83.c:19633

```
// The emitter (Emit_Subtype_Agreement_Check) dispatches on the
```

### ada83.c:19634

```
// pair's shape and ignores shapes it cannot compare, so capture
```

### ada83.c:19635

```
// is unconditional.
```

### ada83.c:19656

```
// A constrained actual's index range lives in its Index_Info,
```

### ada83.c:19657

```
// not on the index type (ARRAY (1 .. 3) has index type
```

### ada83.c:19658

```
// INTEGER, RM 3.6.1(2)). Build a bounded view of the index
```

### ada83.c:19659

```
// for the comparison so the agreement check sees 1 .. 3, not
```

### ada83.c:19660

```
// INTEGER'RANGE (cc3224a: formal ARRAY (INT) with INT being
```

### ada83.c:19661

```
// INTEGER RANGE 1 .. 3).
```

### ada83.c:19723

```
// The binding is a VIEW of the actual: same structure and
```

### ada83.c:19724

```
// base, but the formal's predefined operators denote the
```

### ada83.c:19725

```
// actual's PREDEFINED operations even when the actual's are
```

### ada83.c:19726

```
// redefined (RM 12.1.2(41-42)).
```

### ada83.c:19732

```
// 'BASE of the formal denotes the actual's BASE (RM 3.3.3):
```

### ada83.c:19733

```
// chain to it, not to the first subtype, or attributes like
```

### ada83.c:19734

```
// 'BASE'SMALL would see subtype values.
```

### ada83.c:19744

```
// The struct copy above brought the ACTUAL's defining_symbol
```

### ada83.c:19745

```
// along, which would make the view claim the actual's home
```

### ada83.c:19746

```
// package as its own. The view is a new declaration whose
```

### ada83.c:19747

```
// declaration is this binding: inside-the-instance tests
```

### ada83.c:19748

```
// (Outside_Defining_Package, via Effective_Type_View) hinge on
```

### ada83.c:19749

```
// the view's home being the instance, not the actual's package.
```

### ada83.c:19752

```
// RM 12.3.5: a formal access type's designated subtype is the
```

### ada83.c:19753

```
// one STATED IN THE FORMAL — inside the generic, allocators and
```

### ada83.c:19754

```
// constraint checks act on it, not on whatever (matching but
```

### ada83.c:19755

```
// possibly differently constrained) designated subtype the
```

### ada83.c:19756

```
// actual carries (cc3128a). A designated mark naming an earlier
```

### ada83.c:19757

```
// formal type maps to that formal's own actual view.
```

### ada83.c:19772

```
// RM 12.1: within the generic, the discriminants of a formal
```

### ada83.c:19773

```
// private type are known by the FORMAL's names — RECVAR3.G with
```

### ada83.c:19774

```
// formal (G : INTEGER) bound to actual REC (Z : INTEGER)
```

### ada83.c:19775

```
// selects the actual's first discriminant (c83027c). The view
```

### ada83.c:19776

```
// keeps the ACTUAL's component names (an instance subtype
```

### ada83.c:19777

```
// escaping the generic must still select by them, cc3203a's
```

### ada83.c:19778

```
// P1VP.D) and carries the formal view for the positional
```

### ada83.c:19779

```
// name-fallback in component lookup.
```

### ada83.c:19800

```
// Formal object: mode IN binds a per-instance constant whose value
```

### ada83.c:19801

```
// stores once at the instantiation point; mode IN OUT binds a
```

### ada83.c:19802

```
// per-instance pointer cell renaming the actual variable
```

### ada83.c:19803

```
// (RM 12.1.1, 12.3 rules (b)/(c), 12.3(80)). Both elaborate at the
```

### ada83.c:19804

```
// instantiation site — in the instantiation's own context — and
```

### ada83.c:19805

```
// the instance body reads them as globals, so no cross-frame
```

### ada83.c:19806

```
// access is ever needed.
```

### ada83.c:19813

```
// The object's type mark names a formal type: the binding is
```

### ada83.c:19814

```
// typed by that formal's actual-VIEW (already bound above in
```

### ada83.c:19815

```
// this same scope), so formal-typed objects carry RM
```

### ada83.c:19816

```
// 12.1.2(41-42) operator semantics.
```

### ada83.c:19830

```
// A defaulted formal evaluates its default at the instantiation
```

### ada83.c:19831

```
// (RM 12.1(11)), with the names it contains still denoting what
```

### ada83.c:19832

```
// they denoted at the generic declaration (RM 12.1(13)) — except
```

### ada83.c:19833

```
// references to earlier formals, which denote their bindings
```

### ada83.c:19834

```
// (RM 12.3(79)). The decorated clone keeps the declaration-point
```

### ada83.c:19835

```
// resolution; the remap swings formal references to bindings.
```

### ada83.c:19836

```
// The actual-resolution passes hand back the template's own
```

### ada83.c:19837

```
// default node when no actual was given; it is never evaluated
```

### ada83.c:19838

```
// as-is.
```

### ada83.c:19857

```
// RM 12.1.1: a formal IN OUT object renames the actual, so it
```

### ada83.c:19858

```
// carries the actual's SUBTYPE (its constraint), not merely the
```

### ada83.c:19859

```
// formal's base type — an assignment or entry copy-back checks
```

### ada83.c:19860

```
// against the actual's constraint (c95085o's Z : T (1..5)).
```

### ada83.c:19864

```
// A formal-typed object keeps the RM 12.1 discriminant name
```

### ada83.c:19865

```
// view even while wearing the actual's subtype: TX.A inside
```

### ada83.c:19866

```
// the generic selects the discriminant the FORMAL calls A
```

### ada83.c:19867

```
// (cc3240a). The copy leaves the shared actual subtype
```

### ada83.c:19868

```
// untouched — and is made only when some formal discriminant
```

### ada83.c:19869

```
// NAME actually differs, so same-named formals keep the one
```

### ada83.c:19870

```
// shared subtype and stay assignment-identical (cc1204a).
```

### ada83.c:19897

```
// RM 12.3(80): the elaboration order distinguishes an explicit
```

### ada83.c:19898

```
// actual from a defaulted one — record which this binding is.
```

### ada83.c:19906

```
// RM 12.1.1: the binding renames the actual. Its global cell
```

### ada83.c:19907

```
// holds the actual's address — exactly what an RM 8.5 object-
```

### ada83.c:19908

```
// rename address slot holds — so point rename_address_slot at
```

### ada83.c:19909

```
// the cell itself (self-reference: Emit_Symbol_Storage yields
```

### ada83.c:19910

```
// the cell's own global). Every storage access then routes
```

### ada83.c:19911

```
// through the one rename-aware deref (Emit_Symbol_Addr,
```

### ada83.c:19912

```
// Emit_Load_From_Symbol, Emit_Load_Fat_Pointer, …) rather than
```

### ada83.c:19913

```
// a per-site is_instance_in_out_binding branch; the array-index
```

### ada83.c:19914

```
// and fat-pointer-load paths, which never had such a branch,
```

### ada83.c:19915

```
// become correct for free.
```

### ada83.c:19931

```
// Formal subprogram: the instance-scope name renames the actual
```

### ada83.c:19932

```
// (RM 12.3 rule (f)); calls in the clone resolve to this binding
```

### ada83.c:19933

```
// and emission follows the renaming chain. An attribute actual
```

### ada83.c:19934

```
// (T'SUCC) travels on the binding for the call emitter to expand.
```

### ada83.c:19959

```
// "/=" actual (RM 6.7): the matched subprogram is the
```

### ada83.c:19960

```
// corresponding "=". Synthesize a real wrapper body
```

### ada83.c:19961

```
//   function F (L, R) return BOOLEAN is
```

### ada83.c:19962

```
//   begin return not "="(L, R); end;
```

### ada83.c:19963

```
// so calls through the formal are ordinary calls — no
```

### ada83.c:19964

```
// emission-time special case exists or is needed.
```

### ada83.c:19974

```
// RM 12.3(19)/12.1.3: a call through the formal uses the
```

### ada83.c:19975

```
// ACTUAL's parameter subtypes but the FORMAL's own default
```

### ada83.c:19976

```
// expressions — and no default at all where the formal
```

### ada83.c:19977

```
// declares none, even if the actual has one. Sharing the
```

### ada83.c:19978

```
// actual's parameter array leaked its defaults into the
```

### ada83.c:19979

```
// instance; build the binding's own array instead, with each
```

### ada83.c:19980

```
// formal default cloned and remapped into the instance.
```

### ada83.c:20001

```
// The template never resolves formal-part defaults (their
```

### ada83.c:20002

```
// formal types are per-instance); resolve the clone here,
```

### ada83.c:20003

```
// where the instance bindings are in scope, so the call
```

### ada83.c:20004

```
// sites can emit it.
```

### ada83.c:20014

```
// A wrapper body lives at the INSTANTIATION's static level —
```

### ada83.c:20015

```
// a sibling of the instance, not a child: its target (the
```

### ada83.c:20016

```
// bound "=", attribute prefix, or operator operand types)
```

### ada83.c:20017

```
// resolves in the instantiation's environment, so its static
```

### ada83.c:20018

```
// chain must be the instance's own parent frame, and callers
```

### ada83.c:20019

```
// inside the instance then pass their %__parent_frame, which
```

### ada83.c:20020

```
// always exists. (Symbol_Add stamped the instance as parent;
```

### ada83.c:20021

```
// re-point it.)
```

### ada83.c:20041

```
// The actual type a formal-part type mark denotes at this instantiation:
```

### ada83.c:20042

```
// a formal of the same generic maps through the bindings; anything else
```

### ada83.c:20043

```
// resolves from scope.
```

### ada83.c:20050

```
// A mark already resolved to a CONCRETE type denotes that type — the
```

### ada83.c:20051

```
// name-keyed formal mapping must not capture it: a formal type may share
```

### ada83.c:20052

```
// its name with an enclosing type it hides only from its declaration
```

### ada83.c:20053

```
// onward (cc1301a: NEXT (Q : INTEGER) before TYPE INTEGER IS RANGE <>
```

### ada83.c:20054

```
// means STANDARD.INTEGER; after it, the formal).
```

### ada83.c:20064

```
// Same base type, looking through subtype and actual-view chains. Root
```

### ada83.c:20065

```
// equality is NOT enough: a derived type never conforms to its parent's
```

### ada83.c:20066

```
// profile (RM 12.3.6; cc1301a's std and NEWINT homonym formals must bind
```

### ada83.c:20067

```
// distinct actuals).
```

### ada83.c:20069

```
// unresolved side: no verdict
```

### ada83.c:20075

```
// A name legal as the actual for an IN OUT formal object: a variable, a
```

### ada83.c:20076

```
// non-OUT formal parameter, or a component/element of one (RM 12.3.1,
```

### ada83.c:20077

```
// renameable per RM 8.5).
```

### ada83.c:20084

```
// unresolved: no verdict
```

### ada83.c:20090

```
// An expanded name Package.Entity denotes Entity, so its variable-ness
```

### ada83.c:20091

```
// is the selected entity's — not the package prefix's. A record
```

### ada83.c:20092

```
// component or element instead inherits it from the prefixed object.
```

### ada83.c:20101

```
// X.ALL
```

### ada83.c:20108

```
// RM 12.3.6: when the actual's name is overloaded, the meaning whose
```

### ada83.c:20109

```
// parameter and result type profile matches the formal's (formal types
```

### ada83.c:20110

```
// mapped to their actuals) is the actual. Returns the matching overload,
```

### ada83.c:20111

```
// or the first symbol when none matches (the validator then diagnoses).
```

### ada83.c:20170

```
// RM 12.3.6: does a candidate actual subprogram CONFORM to a generic formal
```

### ada83.c:20171

```
// subprogram's profile, with the instance's formal-type marks substituted?
```

### ada83.c:20172

```
// Pure query — the caller decides whether a mismatch is an error or a cue to
```

### ada83.c:20173

```
// try another overload of the same name (c94008c: two use-visible SETs; the
```

### ada83.c:20174

```
// one whose profile matches the instance's ELEMENT_TYPE is the actual).
```

### ada83.c:20245

```
// RM 12.3.3: enumeration or integer
```

### ada83.c:20252

```
// RM 12.3.3: integer
```

### ada83.c:20260

```
// RM 12.3.3: floating point
```

### ada83.c:20267

```
// RM 12.3.3: fixed point
```

### ada83.c:20274

```
// RM 12.3.2
```

### ada83.c:20275

```
// Judge limitedness by the FULL view: after the full type
```

### ada83.c:20276

```
// declaration, the partial limited-private view no longer
```

### ada83.c:20277

```
// governs (RM 7.4.1) — an instantiation in the private part
```

### ada83.c:20278

```
// with the completed type is legal (cd1009i).
```

### ada83.c:20294

```
// The actual may itself be a formal private type of an
```

### ada83.c:20295

```
// enclosing generic (cc1207b: `NEW PACK (R)` inside a generic
```

### ada83.c:20296

```
// whose formal R (D : INTEGER) IS PRIVATE): private views
```

### ada83.c:20297

```
// carry their known discriminants in .record too (RM 12.1.2).
```

### ada83.c:20316

```
// RM 12.3.2(95): anything
```

### ada83.c:20318

```
// RM 12.3.4
```

### ada83.c:20367

```
// RM 12.3.5
```

### ada83.c:20388

```
// parent-type matching not yet enforced
```

### ada83.c:20393

```
// RM 12.3.1
```

### ada83.c:20429

```
// RM 12.3.6
```

### ada83.c:20445

```
// The binding resolved by NAME; when that name is overloaded, the
```

### ada83.c:20446

```
// actual is whichever visible overload CONFORMS to the substituted
```

### ada83.c:20447

```
// formal profile (RM 12.3.6) — rebind before validating, so the
```

### ada83.c:20448

```
// detailed diagnostics below fire only when nothing conforms.
```

### ada83.c:20465

```
// An operator-string actual whose predefined operation has no
```

### ada83.c:20466

```
// materialized symbol (only INTEGER and FLOAT operators are
```

### ada83.c:20467

```
// pre-built): synthesize one carrying the formal's substituted
```

### ada83.c:20468

```
// profile — by construction conforming — and let the operator
```

### ada83.c:20469

```
// emitters dispatch on its name (cc3601a: "XOR", "<" on BOOLEAN).
```

### ada83.c:20575

```
// Resolve a package instance's body clone inside the instance's own scope,
```

### ada83.c:20576

```
// under the template body's visibility horizon. Shared by initial
```

### ada83.c:20577

```
// instantiation and by late completion (a SEPARATE or later-in-file body
```

### ada83.c:20578

```
// arriving after the instantiation resolved, RM 12.2).
```

### ada83.c:20587

```
// The instance body must see the declarations of the package body that
```

### ada83.c:20588

```
// textually encloses the generic body (RM 8.1, 12.3) -- the instance spec
```

### ada83.c:20589

```
// resolved under the spec scope (instance_scope->parent), but the body
```

### ada83.c:20590

```
// needs the enclosing body scope, which re-installs all spec entities plus
```

### ada83.c:20591

```
// the body's own. The enclosing package's current scope is that body scope
```

### ada83.c:20592

```
// once its body has been loaded (the spec scope's owner reaches it).
```

### ada83.c:20593

```
// Reparent for the body resolution and restore after, so later member
```

### ada83.c:20594

```
// lookups on the instance keep their spec-scope parent.
```

### ada83.c:20624

```
// Late body completion: the template body arrived (later in the file, or as
```

### ada83.c:20625

```
// a SEPARATE subunit) after the instantiation resolved its spec.
```

### ada83.c:20653

```
// The body source: the resolved in-file body when the completion has been
```

### ada83.c:20654

```
// seen, else the disk-parsed cache (parsed once per template).
```

### ada83.c:20657

```
// a stub; the subunit completes it later
```

### ada83.c:20678

```
// The instance is a copy of the generic unit (RM 12.3), renamed.
```

### ada83.c:20684

```
// Resolve the clones in the environment of the generic's declaration
```

### ada83.c:20685

```
// (RM 12.3 rule (i)), with the formal bindings layered on top.
```

### ada83.c:20698

```
// The visible and private parts resolve as ordinary declarations; their
```

### ada83.c:20699

```
// entities are the instance's own (RM 12.3 rule (h)).
```

### ada83.c:20708

```
// Package-level entities of the instance parent under it (per-instance
```

### ada83.c:20709

```
// global mangling <instance>__<name>); the visible part becomes the
```

### ada83.c:20710

```
// instance's export list, replacing the old export-cloning machinery.
```

### ada83.c:20716

```
// The scope's flat symbol array holds only overload-chain HEADS; the
```

### ada83.c:20717

```
// export list flattens each chain so homographs (e.g. RESET/RESET) are
```

### ada83.c:20718

```
// individually visible to USE clauses and member lookup. The creation-
```

### ada83.c:20719

```
// sequence window keeps bindings and later (private-part / body)
```

### ada83.c:20720

```
// homographs out — and it is the ONLY reliable filter: a spec subprogram
```

### ada83.c:20721

```
// homonymous with a formal-subprogram binding (SET in an instance whose
```

### ada83.c:20722

```
// formal actual is also named SET, c94008c) chains onto the BINDING's
```

### ada83.c:20723

```
// overload head, below binding_count in the flat array, so every head
```

### ada83.c:20724

```
// must be scanned.
```

### ada83.c:20742

```
// The body resolves in the same scope (spec entities directly visible),
```

### ada83.c:20743

```
// under the visibility horizon of the template body's own analysis point.
```

### ada83.c:20766

```
// A bodyless generic subprogram (the UNCHECKED_CONVERSION intrinsic)
```

### ada83.c:20767

```
// still needs its instance PROFILE — parameter and result types bound to
```

### ada83.c:20768

```
// the actuals — for call sites; there is no body to clone or emit.
```

### ada83.c:20777

```
// The instance is a copy of the generic unit (RM 12.3), renamed to the
```

### ada83.c:20778

```
// instance's own name so its emitted symbol is the instance's.
```

### ada83.c:20785

```
// Resolve the clone in the environment of the generic's declaration
```

### ada83.c:20786

```
// (RM 12.3 rule (i): global names denote the same entities as in the
```

### ada83.c:20787

```
// template), with the instance's formal bindings layered on top.
```

### ada83.c:20799

```
// Bind each formal to its actual as a symbol of the instance scope.
```

### ada83.c:20803

```
// Parameters of the instance enter its scope; their types resolve against
```

### ada83.c:20804

```
// the formal bindings just installed. The instance symbol's parameter
```

### ada83.c:20805

```
// array is rebuilt from the clone so each entry carries its parameter
```

### ada83.c:20806

```
// symbol — the body prologue allocates slots through param_sym and the
```

### ada83.c:20807

```
// resolved body references those same symbols.
```

### ada83.c:20857

```
// Within its own declarative region the generic unit's name denotes the
```

### ada83.c:20858

```
// current instance (RM 12.1(7)): bind the template's name to the
```

### ada83.c:20859

```
// instance so recursive calls in the clone target the instance.
```

### ada83.c:20871

```
// Resolve the clone as an ordinary body: declarations (the bindings
```

### ada83.c:20872

```
// first), master bookkeeping, statements, handlers.
```

### ada83.c:20886

```
// RM 8.5: a slice renaming (`X : STRING renames S(L..H)`) denotes the slice,
```

### ada83.c:20887

```
// whose index range L..H is the renamed object's own constraint — the type mark
```

### ada83.c:20888

```
// in the renaming supplies no bounds (c85006g). Build the constrained
```

### ada83.c:20889

```
// one-dimensional subtype carrying L..H so X'FIRST/'LAST/'LENGTH and indexing
```

### ada83.c:20890

```
// use the slice's range, not the base array's. Returns NULL for a non-slice or a
```

### ada83.c:20891

```
// slice whose bounds are not both static (a dynamic slice would need runtime
```

### ada83.c:20892

```
// bounds carried in a fat pointer, which the rename's single-address slot does
```

### ada83.c:20893

```
// not hold).
```

### ada83.c:20910

```
// element type, index subtype
```

### ada83.c:20922

```
// A slice's type is a constrained view of the SAME array type (RM 4.1.2) —
```

### ada83.c:20923

```
// a subtype link, not a derivation.
```

### ada83.c:20935

```
// RM 6.1: a character literal denotes an enumeration literal, which is a
```

### ada83.c:20936

```
// parameterless function. Resolve one standing in a function-name position
```

### ada83.c:20937

```
// (a renaming target, a generic formal subprogram actual or default) against
```

### ada83.c:20938

```
// the expected result type. Predefined CHARACTER installs no literal symbols,
```

### ada83.c:20939

```
// so its literals are synthesized here with position = character code.
```

### ada83.c:20970

```
// Resolve type
```

### ada83.c:20974

```
// Object declarations freeze the type (RM 13.14)
```

### ada83.c:20979

```
// Resolve initializer/renamed object - propagate type to aggregates first
```

### ada83.c:20980

```
// Propagate type to aggregate initializer from declared type
```

### ada83.c:20984

```
// RM 3.2.2: a number declaration (CONSTANT without a type mark) has
```

### ada83.c:20985

```
// an expression whose type must be UNIVERSAL_INTEGER or
```

### ada83.c:20986

```
// UNIVERSAL_REAL — non-universal user-defined homographs of the
```

### ada83.c:20987

```
// operators are not applicable. Seed the operator context with a
```

### ada83.c:20988

```
// universal type so Resolve_Overloaded_Call filters them out
```

### ada83.c:20989

```
// (c87b03a: I1 : CONSTANT := 1 + 1 must reach the predefined
```

### ada83.c:20990

```
// universal_integer "+", not the user-defined INTEGER "+"). Pick
```

### ada83.c:20991

```
// between int/real based on the operand shape available so far.
```

### ada83.c:21006

```
// Give the initializer the declared type as context so overload
```

### ada83.c:21007

```
// resolution by return type (RM 6.6, `C : CITRUS := ORANGE`) and
```

### ada83.c:21008

```
// aggregate typing (RM 4.3) see it before it is resolved.
```

### ada83.c:21013

```
// Resolve character literal as enumeration literal (RM 3.5.1)
```

### ada83.c:21022

```
// Add symbols for each name
```

### ada83.c:21030

```
// Object renames: type comes from renamed object
```

### ada83.c:21033

```
// A slice rename carries the slice's own index constraint (RM 8.5),
```

### ada83.c:21034

```
// not the base array's bounds nor any type mark in the renaming.
```

### ada83.c:21037

```
// Point to renamed
```

### ada83.c:21040

```
// RM 8.5: the rename evaluates its target ONCE at elaboration; the
```

### ada83.c:21041

```
// renamed name then denotes whatever entity that evaluation
```

### ada83.c:21042

```
// designated. Reserve a hidden pointer slot the elaborator stores
```

### ada83.c:21043

```
// the target's address into, so every subsequent read/write of the
```

### ada83.c:21044

```
// rename loads that captured address (subsequent changes to sub-
```

### ada83.c:21045

```
// parts of the renamed expression — e.g. reassigning P after
```

### ada83.c:21046

```
// `V renames P.ALL` — no longer affect V). Give the slot the same
```

### ada83.c:21047

```
// package/frame residence as the rename itself.
```

### ada83.c:21055

```
// The slot holds a pointer (the target's address), NOT a value of
```

### ada83.c:21056

```
// the renamed type — that would size the slot as the array/record
```

### ada83.c:21057

```
// and start rebuilding an aggregate for it. Leave type NULL so the
```

### ada83.c:21058

```
// frame layout treats it as a pointer-sized cell.
```

### ada83.c:21064

```
// For named numbers (constant without type mark), use init expression's type
```

### ada83.c:21069

```
// Named number: use universal type from init expression
```

### ada83.c:21072

```
// Mark as named number for inline generation
```

### ada83.c:21080

```
// RM 7.4: Deferred constant completion — Symbol_Add flagged this
```

### ada83.c:21081

```
// sym as a completion of an existing (visible) deferred symbol.
```

### ada83.c:21082

```
// Rebind THIS name node to the storage-owning symbol; other names
```

### ada83.c:21083

```
// in the same multi-name decl get their own bindings on their own
```

### ada83.c:21084

```
// iteration (RM 3.2.1(11)).
```

### ada83.c:21085

```
//
```

### ada83.c:21093

```
// Check for existing incomplete type to complete (RM 3.8.1)
```

### ada83.c:21094

```
// Must be in same declarative region (current scope)
```

### ada83.c:21108

```
// Complete the existing incomplete type
```

### ada83.c:21112

```
// Create new type symbol
```

### ada83.c:21122

```
// For discriminated types, add discriminant symbols to scope first
```

### ada83.c:21123

```
// so they can be referenced in the type definition (e.g., CASE A IS)
```

### ada83.c:21126

```
// Total number of discriminant names and whether every discriminant
```

### ada83.c:21127

```
// carries a default expression (RM 3.7.1). Computed once and reused by
```

### ada83.c:21128

```
// both the record-completion and private-type discriminant builders.
```

### ada83.c:21145

```
// Resolve discriminant type first
```

### ada83.c:21152

```
// Resolve default expression if present (RM 3.7.1).
```

### ada83.c:21153

```
// Pre-set type on calls/binary ops/aggregates/identifiers
```

### ada83.c:21154

```
// so overload resolution (RM 6.6) sees the discriminant's
```

### ada83.c:21155

```
// declared type as context.
```

### ada83.c:21165

```
// Add each discriminant name as a symbol (RM 3.7.1)
```

### ada83.c:21179

```
// Resolve definition and copy type info
```

### ada83.c:21183

```
// Copy type info from definition to named type
```

### ada83.c:21191

```
// RM 13.1: a derived type inherits the parent's size representation.
```

### ada83.c:21192

```
// The definition (a derived-type or constraint expression) already
```

### ada83.c:21193

```
// carries the inherited 'SIZE clause value; the named first subtype
```

### ada83.c:21194

```
// must keep it, otherwise T'SIZE falls back to the byte size and a
```

### ada83.c:21195

```
// derived type loses its parent's specified size (cd1c03a).
```

### ada83.c:21198

```
// For derived and subtype types, preserve base/parent chain
```

### ada83.c:21202

```
// The completion may have turned a private view into a
```

### ada83.c:21203

```
// task-bearing composite: forget any memoized answer.
```

### ada83.c:21214

```
// Constrained views registered against this type while it was
```

### ada83.c:21215

```
// incomplete (RM 3.8.1) live in the record sub-struct; the
```

### ada83.c:21216

```
// wholesale copy from the definition would drop them, so carry
```

### ada83.c:21217

```
// them across the completion (c37216a).
```

### ada83.c:21226

```
// Add discriminants as accessible record components (RM 3.7.1)
```

### ada83.c:21227

```
// Discriminants can be accessed like normal components: R.D1
```

### ada83.c:21228

```
//
```

### ada83.c:21231

```
// Create new components array with discriminants at the start
```

### ada83.c:21237

```
// Add discriminants first (they come before other components)
```

### ada83.c:21241

```
// Copy existing components after discriminants, adjusting offsets
```

### ada83.c:21247

```
// Adjust variant_offset to account for discriminant size
```

### ada83.c:21251

```
// Also adjust variant component offsets
```

### ada83.c:21257

```
// Update record size to include discriminants
```

### ada83.c:21262

```
// Set discriminant tracking fields
```

### ada83.c:21272

```
// Create symbols for enumeration literals
```

### ada83.c:21273

```
// They must reference the named type (type), not the anonymous def_type
```

### ada83.c:21280

```
// Reference the named type
```

### ada83.c:21281

```
// Store ordinal position
```

### ada83.c:21287

```
// For derived enumeration types (TYPE T IS NEW BOOLEAN),
```

### ada83.c:21288

```
// create inherited literal symbols (RM 3.4(12)). An inherited
```

### ada83.c:21289

```
// literal is a value of the base type, so when the derived first
```

### ada83.c:21290

```
// subtype is constrained it carries the unconstrained base — its
```

### ada83.c:21291

```
// value may lie outside the subtype, and an assignment to the
```

### ada83.c:21292

```
// subtype must then be range-checked.
```

### ada83.c:21307

```
// Propagate limited flag from AST node (RM 7.5)
```

### ada83.c:21312

```
// RM 7.4.1: Propagate private→record completion to constrained                         
```

### ada83.c:21313

```
// subtypes. SUBTYPE S IS T(V) copies T's Type_Info when T is                          
```

### ada83.c:21314

```
// still TYPE_PRIVATE, creating a snapshot with stale `kind` and                        
```

### ada83.c:21315

```
// partial `components`. After the full declaration completes T                        
```

### ada83.c:21316

```
// to TYPE_RECORD, refresh every such snapshot so that record                           
```

### ada83.c:21317

```
// aggregates, selected components, and codegen see the full view.                     
```

### ada83.c:21318

```
// Constraint-specific fields (disc_constraint_values/exprs) are                        
```

### ada83.c:21319

```
// untouched - only definition-derived fields are updated.                             
```

### ada83.c:21320

```
//                                                                                      
```

### ada83.c:21345

```
// Handle private type declarations (TYPE T (...) IS [LIMITED] PRIVATE).
```

### ada83.c:21346

```
// Set up type kind and discriminant components so subtypes like
```

### ada83.c:21347

```
// SUBTYPE S IS T(V) can be created before the full declaration.
```

### ada83.c:21348

```
//
```

### ada83.c:21355

```
// Limitedness is a property of the PRIVATE view and survives the
```

### ada83.c:21356

```
// full-view completion (RM 7.4.4): no assignment, no predefined
```

### ada83.c:21357

```
// equality, no literals, whatever the completion turns out to be.
```

### ada83.c:21360

```
// Privateness likewise survives completion: outside the defining
```

### ada83.c:21361

```
// package the partial view governs, and only =, /= and the
```

### ada83.c:21362

```
// explicitly declared operators exist (RM 7.4.2).
```

### ada83.c:21377

```
// RM 3.8.1: an incomplete type declaration with a discriminant part
```

### ada83.c:21378

```
// (TYPE REC (D : WEEKDAY);) needs its discriminant info populated too
```

### ada83.c:21379

```
// so that subtypes like ACCESS REC (V) built between the incomplete
```

### ada83.c:21380

```
// and full declarations can carry the constraint through to the
```

### ada83.c:21381

```
// subtype-elaboration disc-range check (c37211d).
```

### ada83.c:21397

```
// For derived types (TYPE T IS NEW PARENT), inherit primitive
```

### ada83.c:21398

```
// subprograms from the parent type (RM 3.4)
```

### ada83.c:21403

```
// Pop discriminant scope if we pushed one
```

### ada83.c:21421

```
// Count total parameters first (needed for overload matching)
```

### ada83.c:21434

```
// Check if there's already a matching symbol from package spec.                           
```

### ada83.c:21435

```
// This happens when resolving a subprogram body that completes a spec.                    
```

### ada83.c:21436

```
// For overloaded subprograms, must match parameter count AND types.                       
```

### ada83.c:21437

```
// Per RM 6.3, only match specs declared in the same scope - not                            
```

### ada83.c:21438

```
// USE-visible homographs from other packages (those are hidden).                          
```

### ada83.c:21439

```
//                                                                                          
```

### ada83.c:21445

```
// Only match specs from the current declarative region. Owner
```

### ada83.c:21446

```
// equality alone is not the region test: an unlabeled inner block
```

### ada83.c:21447

```
// shares its owner with the enclosing subprogram, and a same-profile
```

### ada83.c:21448

```
// body there is a hiding homograph (RM 8.3), not a completion
```

### ada83.c:21449

```
// (cc1301a's two BUMPs). The discriminator: a spec whose defining
```

### ada83.c:21450

```
// scope STRICTLY ENCLOSES the current one belongs to an outer
```

### ada83.c:21451

```
// region; a spec in the same scope, or in the sibling scope a
```

### ada83.c:21452

```
// package spec resolved under (its body completes it, RM 7.1),
```

### ada83.c:21453

```
// shares the region. An implicitly declared inherited operation
```

### ada83.c:21454

```
// (RM 3.4) is never completed by an explicit declaration.
```

### ada83.c:21469

```
// Check parameter types match
```

### ada83.c:21475

```
// Resolve param type first
```

### ada83.c:21504

```
// Found matching spec - check it's not already claimed
```

### ada83.c:21514

```
// No matching spec found - create new symbol
```

### ada83.c:21523

```
// Resolve parameter type
```

### ada83.c:21531

```
// Resolve default expression, propagating param type
```

### ada83.c:21532

```
// to untyped aggregates/calls/binary ops/identifiers so
```

### ada83.c:21533

```
// overload resolution (RM 6.6) sees the param's typemark
```

### ada83.c:21534

```
// as context (RM 6.1, 3.2.1).
```

### ada83.c:21564

```
// Resolve the renamed subprogram
```

### ada83.c:21567

```
// RM 8.5: PACKAGE L RENAMES D;
```

### ada83.c:21568

```
// L denotes the same package D - any selected component L.X
```

### ada83.c:21569

```
// resolves to D.X. Create a SYMBOL_PACKAGE whose scope and
```

### ada83.c:21570

```
// exports alias D's.
```

### ada83.c:21578

```
// RM 12.3: within a generic package, the unit's name denotes the
```

### ada83.c:21579

```
// current instance — so a renaming of the generic inside its own
```

### ada83.c:21580

```
// template (`PACKAGE PACKB RENAMES GPACK` in GPACK) denotes each
```

### ada83.c:21581

```
// instance itself. When this declaration is re-resolved inside an
```

### ada83.c:21582

```
// instance, rebind the target to that instance's package symbol
```

### ada83.c:21583

```
// (c85011a: PACK2.PACKB.J).
```

### ada83.c:21606

```
// PROCEDURE P RENAMES Q; or FUNCTION F RENAMES G;
```

### ada83.c:21608

```
// Resolve the renamed reference. RM 8.5.4: when Q is overloaded,
```

### ada83.c:21609

```
// disambiguate by the rename's own profile (parameter types,
```

### ada83.c:21610

```
// result type). Build that profile first, then look up the
```

### ada83.c:21611

```
// specific overload, then create our symbol.
```

### ada83.c:21617

```
// Build sym->parameters / sym->return_type from rename's own spec
```

### ada83.c:21663

```
// Resolve the renamed name using rename's own profile to
```

### ada83.c:21664

```
// disambiguate overloads (e.g. `FUNCTION "*"(X,Y:WHOLE) RETURN
```

### ada83.c:21665

```
// WHOLE RENAMES F1` must pick the F1 returning WHOLE, not the
```

### ada83.c:21666

```
// first F1 visible).
```

### ada83.c:21683

```
// RM 8.5.1: `renames F.ENTER` where F is an overloaded
```

### ada83.c:21684

```
// parameterless function — pick F (hence the task type, hence the
```

### ada83.c:21685

```
// entry) by the rename's own profile, not the first F.
```

### ada83.c:21688

```
// RM 8.5.4: `renames PKG.NAME` where the package exports
```

### ada83.c:21689

```
// homographs of NAME — a generic instance exports both its own
```

### ada83.c:21690

```
// UPDATE and the UPDATE bound as a formal-subprogram actual
```

### ada83.c:21691

```
// (c94008c) — picks the one conforming to the rename's own
```

### ada83.c:21692

```
// profile, not the first exported name match.
```

### ada83.c:21698

```
// Candidates: the exported list, then the package scope's
```

### ada83.c:21699

```
// bucket chain (a generic instance keeps most of its
```

### ada83.c:21700

```
// declarations there), each with its overload chain.
```

### ada83.c:21747

```
// RM 8.5(3): FUNCTION SEA RETURN CHARACTER RENAMES 'C' — the
```

### ada83.c:21748

```
// literal is the renamed parameterless function.
```

### ada83.c:21752

```
// RM 8.5(3): renaming an attribute that denotes a function
```

### ada83.c:21753

```
// (INTEGER'SUCC, 'PRED, 'IMAGE, 'VALUE — c85016a). Synthesize
```

### ada83.c:21754

```
// a wrapper whose body applies the attribute to the rename's
```

### ada83.c:21755

```
// own parameters, exactly as generic attribute actuals do.
```

### ada83.c:21792

```
// Fall back to renamed signature if rename had no own spec
```

### ada83.c:21801

```
// RM 8.5: a renaming of an entry (or entry-family member) denotes
```

### ada83.c:21802

```
// the entity determined when the renaming declaration is
```

### ada83.c:21803

```
// ELABORATED — the task prefix and any family index are evaluated
```

### ada83.c:21804

```
// then, not at each call. Keep the renamed name (it carries both)
```

### ada83.c:21805

```
// and give the rename two hidden locals; elaboration stores the
```

### ada83.c:21806

```
// task pointer and index there, calls through the rename read
```

### ada83.c:21807

```
// them back.
```

### ada83.c:21842

```
// Check if this body completes a generic declaration
```

### ada83.c:21850

```
// RM 10.2 / 12.1: a body STUB completing a generic subprogram still
```

### ada83.c:21851

```
// resolves its profile within the generic's formal part (X : T,
```

### ada83.c:21852

```
// c83030c); the subunit later provides the actual generic body and
```

### ada83.c:21853

```
// pairs itself through this same branch when it resolves.
```

### ada83.c:21865

```
// This body completes a generic - store it and resolve it.
```

### ada83.c:21866

```
// Push scope with generic formals so T, F etc. are visible.
```

### ada83.c:21874

```
// Push scope for generic body resolution
```

### ada83.c:21877

```
// Formals into scope (RM 12.1); symbols already exist when the
```

### ada83.c:21878

```
// declaration was analyzed, and re-install here for the body.
```

### ada83.c:21881

```
// Add body parameters to scope (types may reference generic formals)
```

### ada83.c:21885

```
// Formals are in scope: resolve IS-name formal-subprogram
```

### ada83.c:21886

```
// defaults (RM 12.1.3).
```

### ada83.c:21892

```
// Resolve declarations, statements, and handlers in the generic body
```

### ada83.c:21898

```
// Resolve spec if present
```

### ada83.c:21904

```
// A spec-less LIBRARY subprogram body (e.g. a loaded parent unit)
```

### ada83.c:21905

```
// still needs its symbol: scope ownership, mangling, and the
```

### ada83.c:21906

```
// subunit machinery's stability walk all read it. The direct
```

### ada83.c:21907

```
// compile path sets one later; the loader path never did, leaving
```

### ada83.c:21908

```
// the body's scope owner NULL and degrading every name derived
```

### ada83.c:21909

```
// through it in subunit compilations.
```

### ada83.c:21931

```
// For stubs (IS SEPARATE), don't claim the symbol - the separate
```

### ada83.c:21932

```
// subunit will provide the actual body and should claim it.
```

### ada83.c:21937

```
// Push scope for body
```

### ada83.c:21940

```
// Link the scope to the symbol for static link access
```

### ada83.c:21945

```
// Add parameters to scope and link to Parameter_Info
```

### ada83.c:21950

```
// Resolve declarations, statements, and handlers
```

### ada83.c:21956

```
// Task declaration creates a task type and optionally an object
```

### ada83.c:21959

```
// RM 7.4.1: a TASK TYPE in a package private part completes a
```

### ada83.c:21960

```
// matching LIMITED PRIVATE declaration in the visible part.
```

### ada83.c:21961

```
// Reuse the existing symbol so the private->full link is intact.
```

### ada83.c:21973

```
// The completed type is a TCB pointer (see Type_New's TYPE_TASK case)
```

### ada83.c:21974

```
// and is limited (RM 9.2).
```

### ada83.c:21991

```
// If not a task TYPE, also create an object of that type
```

### ada83.c:21997

```
// Add the object symbol - it will shadow the type for normal lookups
```

### ada83.c:22001

```
// Push scope for task entries
```

### ada83.c:22004

```
// Resolve and add entry declarations
```

### ada83.c:22005

```
// Counter for unique entry indices
```

### ada83.c:22013

```
// Assign unique entry index
```

### ada83.c:22015

```
// Count entry parameters
```

### ada83.c:22034

```
// Resolve default expression if present
```

### ada83.c:22044

```
// RM 6.6 / 9.5: seed the default expression's type from
```

### ada83.c:22045

```
// the parameter's typemark so overload resolution
```

### ada83.c:22046

```
// (c87b45c: F1 / "*" with several return types) picks
```

### ada83.c:22047

```
// the parameter-typed variant.
```

### ada83.c:22062

```
// RM 9.5: the family index is a discrete range; resolve it so its
```

### ada83.c:22063

```
// bounds carry types for the out-of-range check at call/accept.
```

### ada83.c:22074

```
// Add entry to type's exported symbols
```

### ada83.c:22087

```
// Find the task spec symbol
```

### ada83.c:22092

```
// Create a symbol for the task body if spec wasn't found
```

### ada83.c:22100

```
// Push scope for task body. Link it to the symbol (as subprogram and
```

### ada83.c:22101

```
// package analysis do): the task body owns a per-instance frame, and
```

### ada83.c:22102

```
// the static-chain walkers need its scope's layout to alias its
```

### ada83.c:22103

```
// locals from nested units (RM 9.1).
```

### ada83.c:22107

```
// Import entries from task spec into body scope (RM 9.1)                                   
```

### ada83.c:22108

```
// Entries declared in the task spec are visible inside the task body.                     
```

### ada83.c:22109

```
// For single tasks, task_sym is SYMBOL_VARIABLE with TYPE_TASK;                            
```

### ada83.c:22110

```
// the entries are on the type's defining_symbol.                                          
```

### ada83.c:22111

```
//                                                                                          
```

### ada83.c:22114

```
// task_sym is the type symbol directly (task type declaration)
```

### ada83.c:22120

```
// task_sym is the variable; get the type's defining symbol
```

### ada83.c:22132

```
// Resolve declarations and statements
```

### ada83.c:22143

```
// Resolve exception handlers
```

### ada83.c:22153

```
// Store declaration for body to find
```

### ada83.c:22157

```
// Link scope to symbol for P.X lookups
```

### ada83.c:22159

```
// RM 3.4: only subprograms declared immediately within the visible part
```

### ada83.c:22160

```
// are derivable of the first kind. Saved/restored to nest correctly.
```

### ada83.c:22168

```
// End of package spec freezes all declared entities (RM 13.14)
```

### ada83.c:22172

```
// Populate exports for nested/inline package access (e.g., INNER.ACC)
```

### ada83.c:22178

```
// Find or create package symbol for proper name mangling
```

### ada83.c:22184

```
// Mark this body as loaded BEFORE trying to load the spec.                                
```

### ada83.c:22185

```
// This prevents Load_Package_Spec from recursively loading                                 
```

### ada83.c:22186

```
// the same body when we're compiling a .adb file directly.                                
```

### ada83.c:22187

```
//                                                                                          
```

### ada83.c:22192

```
// Try to load corresponding package spec first.
```

### ada83.c:22193

```
// This ensures body uses same symbol IDs as spec
```

### ada83.c:22201

```
// Create package symbol if spec not found
```

### ada83.c:22210

```
// The spec's scope (carrying its implicit derived operations) before
```

### ada83.c:22211

```
// the body overwrites pkg_sym->scope with its own.
```

### ada83.c:22215

```
// Set package symbol's scope for separate subunit resolution.
```

### ada83.c:22216

```
// This allows SEPARATE (Parent) subunits to find stub symbols.
```

### ada83.c:22221

```
// Install visible and private declarations from package spec
```

### ada83.c:22222

```
// into the body's scope (RM 7.1, 7.2)
```

### ada83.c:22225

```
// Handle generic packages: formals and unit are in the generic declaration
```

### ada83.c:22226

```
// Store this body as the generic's body for later instantiation
```

### ada83.c:22234

```
// Record the package-body scope textually enclosing this generic
```

### ada83.c:22235

```
// body (the scope just pushed for it parents off the enclosing
```

### ada83.c:22236

```
// body), so an instance body re-resolved at the instantiation point
```

### ada83.c:22237

```
// can still see the enclosing body's declarations (RM 8.1, 12.3) --
```

### ada83.c:22238

```
// e.g. TEXT_IO.INTEGER_IO's body using TEXT_IO's file-control state.
```

### ada83.c:22239

```
// Stored on the body NODE (shared by the template parse) so it is
```

### ada83.c:22240

```
// reachable even when spec and body load created distinct symbols.
```

### ada83.c:22244

```
// Formals into scope (RM 12.1); created once, re-installed here.
```

### ada83.c:22247

```
// The spec was resolved when the generic declaration was analyzed
```

### ada83.c:22248

```
// (or by the §17 loaded-unit path); the shared install below puts
```

### ada83.c:22249

```
// its decorated entities into the body's scope.
```

### ada83.c:22262

```
// Package-level (module-global) storage only for packages with no
```

### ada83.c:22263

```
// enclosing subprogram: a nested package's state lives in the
```

### ada83.c:22264

```
// enclosing frame — and a SUBUNIT package body of a nested
```

### ada83.c:22265

```
// package must agree with the parent's module about that.
```

### ada83.c:22269

```
// Freeze all types at end of declarative part (RM 13.14)
```

### ada83.c:22273

```
// Resolve exception handlers (RM 11.4) - local variables and
```

### ada83.c:22274

```
// parameters remain visible in the handler body.
```

### ada83.c:22288

```
// Make package contents directly visible (Ada 83 8.4)                                        
```

### ada83.c:22289

```
// "A use clause achieves direct visibility of declarations                                   
```

### ada83.c:22290

```
//  that appear in the visible parts of the named packages"                                   
```

### ada83.c:22291

```
//                                                                                            
```

### ada83.c:22296

```
// Find the package symbol. A renaming denotes the SAME package
```

### ada83.c:22297

```
// (RM 8.5) — peel so USE installs the complete, live export list
```

### ada83.c:22298

```
// (c85011a: USE PACK1.PACKA, where PACKA renames PACK1 from inside).
```

### ada83.c:22307

```
// Helper macro: add use-visible alias for a symbol
```

### ada83.c:22310

```
/* Check if alias already exists for this symbol */
```

### ada83.c:22331

```
/* An enumeration literal carries its ordinal position in */
```

### ada83.c:22332

```
/* frame_offset; a USE-visible view denotes the same literal, */
```

### ada83.c:22333

```
/* so 'POS/'SUCC/'VAL through the alias need that position */
```

### ada83.c:22334

```
/* (else a use-visible 'C' reports position 0). */
```

### ada83.c:22339

```
/* A use-visible view of a renaming or an inherited operation */
```

### ada83.c:22340

```
/* must dispatch to the same body, so carry the links that */
```

### ada83.c:22341

```
/* Ultimate_Operation follows (RM 8.5, 3.4). */
```

### ada83.c:22347

```
/* mangle as the original entity */
```

### ada83.c:22352

```
// For loaded packages with exported array, use it
```

### ada83.c:22358

```
// For inline packages, iterate visible declarations
```

### ada83.c:22366

```
// Handle object_decl names (constants, variables)
```

### ada83.c:22374

```
// Handle enumeration literals
```

### ada83.c:22385

```
// Handle exception declarations
```

### ada83.c:22400

```
// Process pragma effects
```

### ada83.c:22404

```
// pragma Inline (subprogram_name, ...)
```

### ada83.c:22418

```
// pragma Pack (type_name)
```

### ada83.c:22432

```
// pragma Suppress (check_name) or pragma Suppress (check_name, entity_name)
```

### ada83.c:22463

```
// Apply to specific entity or current scope
```

### ada83.c:22473

```
// else: would apply to enclosing scope
```

### ada83.c:22476

```
// pragma RESTRICTIONS (restriction {, restriction}) — implementation-
```

### ada83.c:22477

```
// defined (RM 2.8 lets an implementation define pragmas; others must
```

### ada83.c:22478

```
// ignore this one). Each named restriction makes its construct a
```

### ada83.c:22479

```
// compile-time error from here on.
```

### ada83.c:22500

```
// pragma PROFILE (RAVENSCAR) — implementation-defined: the
```

### ada83.c:22501

```
// deterministic-tasking bundle, this implementation's Ada 83
```

### ada83.c:22502

```
// analogue of the Ravenscar static subset. Simple entries and
```

### ada83.c:22503

```
// plain accepts stay legal (Ada 83 has no protected objects to
```

### ada83.c:22504

```
// carry them instead); everything dynamic or nondeterministic —
```

### ada83.c:22505

```
// selective waits, conditional/timed calls, ABORT, task
```

### ada83.c:22506

```
// allocators, entry families — is rejected at compile time.
```

### ada83.c:22520

```
// pragma Import (Convention, Entity, External_Name, Link_Name)
```

### ada83.c:22528

```
// Get external name if provided
```

### ada83.c:22533

```
// pragma Export (Convention, Entity, External_Name)
```

### ada83.c:22540

```
// Unlike the Import arm, only convention C is recognized here;
```

### ada83.c:22541

```
// STDCALL and INTRINSIC name no export mechanism and are ignored.
```

### ada83.c:22551

```
// pragma Unreferenced (name, ...)
```

### ada83.c:22563

```
// pragma Convention (convention, entity)
```

### ada83.c:22570

```
// pragma Pure / pragma Preelaborate (RM 10.5): mark the named
```

### ada83.c:22571

```
// unit (or the enclosing package when unnamed) so the
```

### ada83.c:22572

```
// elaboration graph can relax its ordering requirements.
```

### ada83.c:22595

```
// pragma System_Name etc. - informational only, accepted and ignored              
```

### ada83.c:22596

```
// pragma Elaborate, pragma Elaborate_All - handled at link time                            
```

### ada83.c:22597

```
// pragma Restrictions - accepted and ignored                                               
```

### ada83.c:22598

```
//                                                                                          
```

### ada83.c:22604

```
// Exception declaration: E : exception;
```

### ada83.c:22605

```
// or renaming: E2 : exception RENAMES E1 (RM 8.5) — a NEW NAME for
```

### ada83.c:22606

```
// the SAME exception: raises and handlers through either name must
```

### ada83.c:22607

```
// agree on identity, so the alias records its target and
```

### ada83.c:22608

```
// Emit_Exception_Ref peels to the root (one @__exc global, c85009a).
```

### ada83.c:22626

```
// Add to global exception list for codegen — an alias emits no
```

### ada83.c:22627

```
// global of its own; its target already has one.
```

### ada83.c:22637

```
// Generic declaration: generic ... procedure/function/package spec
```

### ada83.c:22642

```
// Get name from the unit
```

### ada83.c:22650

```
// Create generic symbol
```

### ada83.c:22655

```
// Store formals list for later
```

### ada83.c:22662

```
// Analyze the template at its declaration (RM 12.1): the formals
```

### ada83.c:22663

```
// enter a scope of their own and the generic unit's specification
```

### ada83.c:22664

```
// resolves against them, so template errors surface here and both
```

### ada83.c:22665

```
// instantiation and the body (when one arrives) start from a
```

### ada83.c:22666

```
// decorated specification. IS-name defaults of formal subprograms
```

### ada83.c:22667

```
// resolve in the same scope (RM 12.1.3) — a default like T'SUCC or
```

### ada83.c:22668

```
// one naming an earlier formal binds against the installed formals.
```

### ada83.c:22691

```
// Declaration-point visibility horizon: instance spec clones
```

### ada83.c:22692

```
// resolve as if at this exact point (RM 12.3 rule (i)).
```

### ada83.c:22697

```
// Find the generic template
```

### ada83.c:22700

```
// Generic instantiation: procedure/function X is new GENERIC_NAME (actuals)
```

### ada83.c:22708

```
// Resolve the generic name to find template
```

### ada83.c:22714

```
// Handle qualified names like P.PP (NK_SELECTED resolved to a symbol)
```

### ada83.c:22719

```
// A USE-visible alias denotes the original generic — and only the
```

### ada83.c:22720

```
// original receives its body when the package body arrives later
```

### ada83.c:22721

```
// (c74305b: USE PK before PACKAGE BODY PK left the alias bodyless,
```

### ada83.c:22722

```
// so the instance was never cloned).
```

### ada83.c:22729

```
// Create instance symbol
```

### ada83.c:22739

```
// The instance nests where the instantiation occurs: its parent
```

### ada83.c:22740

```
// anchors static-chain emission (uplevel access from the instance
```

### ada83.c:22741

```
// body to the instantiation's enclosing frames).
```

### ada83.c:22743

```
// Propagate INTRINSIC convention from template so the call site
```

### ada83.c:22744

```
// can recognize the instance as needing inline lowering (e.g.
```

### ada83.c:22745

```
// UNCHECKED_CONVERSION) instead of an external call.
```

### ada83.c:22746

```
// The pragma CONVENTION(INTRINSIC, ...) trailing the library unit in
```

### ada83.c:22747

```
// rts/unchecked_conversion.ads and rts/unchecked_deallocation.ads is
```

### ada83.c:22748

```
// not yet applied by the loader; recognize the two predefined
```

### ada83.c:22749

```
// intrinsic generics (RM 13.10) by name as a fallback.
```

### ada83.c:22757

```
// Process generic actuals and build mapping
```

### ada83.c:22761

```
// Count total actual slots: multi-name object formals                                      
```

### ada83.c:22762

```
// like "F, L : E" consume one actual per name.                                            
```

### ada83.c:22763

```
// Type and subprogram formals consume one actual each.                                    
```

### ada83.c:22764

```
//                                                                                          
```

### ada83.c:22774

```
// Use the larger of computed slots and actual params provided
```

### ada83.c:22783

```
// Place each actual into its formal's slot (RM 12.3): a
```

### ada83.c:22784

```
// positional actual takes the next slot in formal order; a named
```

### ada83.c:22785

```
// association (FORMAL => ACTUAL) takes the slot of the formal it
```

### ada83.c:22786

```
// names. The resolution passes below then read actuals by slot,
```

### ada83.c:22787

```
// never by list position.
```

### ada83.c:22841

```
// First pass: resolve type formals (using actual_idx to track
```

### ada83.c:22842

```
// the formal's slot, since object formals with multiple names
```

### ada83.c:22843

```
// consume multiple actual slots).
```

### ada83.c:22844

```
//
```

### ada83.c:22868

```
// Second pass: resolve object formals with substituted types.
```

### ada83.c:22869

```
// Each name in a multi-name object formal gets its own actual slot.
```

### ada83.c:22874

```
// Look up the formal's type, substituting any type formals
```

### ada83.c:22879

```
// When the object formal's type is itself a type formal, take its
```

### ada83.c:22880

```
// actual by the formal type's slot decoration rather than by name.
```

### ada83.c:22883

```
// A type formal of the same generic matches by name against
```

### ada83.c:22884

```
// the bound actuals; otherwise it is a concrete scope type.
```

### ada83.c:22887

```
// Concrete types: resolve from scope (RM 12.3)
```

### ada83.c:22902

```
// Map each name to its own actual
```

### ada83.c:22914

```
// Character literal actuals for enum formal types need                         
```

### ada83.c:22915

```
// explicit resolution to find their position (RM 3.5.1).                      
```

### ada83.c:22916

```
// Resolve_Expression sets type to CHARACTER, losing the                        
```

### ada83.c:22917

```
// enum context; restore and resolve as enum literal.                          
```

### ada83.c:22918

```
//                                                                              
```

### ada83.c:22930

```
// No actual: the formal's default expression supplies the
```

### ada83.c:22931

```
// value (RM 12.1.1, evaluated for instantiations that use
```

### ada83.c:22932

```
// the default). Bound like an actual so references inside
```

### ada83.c:22933

```
// the instance substitute it.
```

### ada83.c:22939

```
// A character-literal default resolves against the formal's
```

### ada83.c:22940

```
// type, exactly as a character-literal actual does — its
```

### ada83.c:22941

```
// value is the enumeration position, not the character code.
```

### ada83.c:22960

```
// Third pass: resolve subprogram formals to actual subprograms
```

### ada83.c:22975

```
// Resolve actual subprogram name
```

### ada83.c:22980

```
// Handle operator designators: "&" is the "&" operator
```

### ada83.c:22981

```
// Look up operator by name
```

### ada83.c:22985

```
// "/=" as an actual (RM 6.7): it denotes the negation of
```

### ada83.c:22986

```
// the corresponding "=". A user-defined "=" matching the
```

### ada83.c:22987

```
// formal's profile binds with the negation flag; anything
```

### ada83.c:22988

```
// else lowers as the predefined inequality.
```

### ada83.c:23005

```
// Check type profile matches: the formal's type parameter
```

### ada83.c:23006

```
// must match the found operator's return type. Otherwise
```

### ada83.c:23007

```
// fall through to built-in operator path.
```

### ada83.c:23008

```
//
```

### ada83.c:23011

```
// Find the actual type for the first type formal
```

### ada83.c:23024

```
// no type info to check
```

### ada83.c:23032

```
// Check for built-in operators
```

### ada83.c:23035

```
// String lexer strips quotes: "&" arrives as &, "and"
```

### ada83.c:23036

```
// as and. Token_From_Op_Name covers the multi-character
```

### ada83.c:23037

```
// designators too.
```

### ada83.c:23047

```
// A character literal as the actual: it denotes an
```

### ada83.c:23048

```
// enumeration literal, which is a parameterless function
```

### ada83.c:23049

```
// (RM 12.3.6). Resolve it against the substituted formal
```

### ada83.c:23050

```
// type so an overloaded literal picks the right type's
```

### ada83.c:23051

```
// literal, not whichever homograph the table finds first.
```

### ada83.c:23064

```
// Expanded-name "/=" actual (PK."/="): the same RM 6.7
```

### ada83.c:23065

```
// negation as the direct designator — resolve the package's
```

### ada83.c:23066

```
// "=" and bind it with the negation flag.
```

### ada83.c:23087

```
// An attribute as the actual (RM 12.3.6: 'SUCC, 'PRED,
```

### ada83.c:23088

```
// 'IMAGE, 'VALUE denote functions): record it — the wrapper
```

### ada83.c:23089

```
// synthesizer turns it into a callable body, exactly as for
```

### ada83.c:23090

```
// an attribute DEFAULT (cc3601a: "WEEKDAY'SUCC").
```

### ada83.c:23099

```
// Look up the actual subprogram symbol; an overloaded
```

### ada83.c:23100

```
// name disambiguates by the formal's profile (RM 12.3.6).
```

### ada83.c:23112

```
// IS <> default: resolve to a visible subprogram with the
```

### ada83.c:23113

```
// same name as the formal (RM 12.3(6)) — the one whose
```

### ada83.c:23114

```
// profile CONFORMS after formal-type substitution, not
```

### ada83.c:23115

```
// whichever homograph the table finds first (cc3607b).
```

### ada83.c:23117

```
// RM 12.3(6): the box default binds a VISIBLE subprogram of
```

### ada83.c:23118

```
// the formal's name whose profile conforms after formal-type
```

### ada83.c:23119

```
// substitution. Every visible homonym is a candidate — one
```

### ada83.c:23120

```
// scope's bucket chain is not the visibility set (cc1301a:
```

### ada83.c:23121

```
// NEXT (NEWINT) lives two scopes out from the instantiation,
```

### ada83.c:23122

```
// past a std-profile NEXT in the enclosing block). Collected
```

### ada83.c:23123

```
// near-to-far, the first conforming candidate is the RM 8.3
```

### ada83.c:23124

```
// hiding winner; RM 8.4(5) keeps use-visible aliases behind
```

### ada83.c:23125

```
// directly visible ones.
```

### ada83.c:23143

```
// An operator formal with no conforming user operator
```

### ada83.c:23144

```
// takes the formal type's PREDEFINED operator (RM 12.1.3,
```

### ada83.c:23145

```
// 12.3.6) — the wrapper synthesizer builds its body.
```

### ada83.c:23162

```
// IS name default (RM 12.1.3): the name was resolved when
```

### ada83.c:23163

```
// the generic's body was analyzed (formals in scope). A
```

### ada83.c:23164

```
// bare identifier that has no binding yet is retried here
```

### ada83.c:23165

```
// quietly — spec-only generics never went through body
```

### ada83.c:23166

```
// analysis. Attribute defaults (T'SUCC) carry no symbol
```

### ada83.c:23167

```
// and bind nothing; calls through such formals keep their
```

### ada83.c:23168

```
// existing emission path.
```

### ada83.c:23181

```
// Attribute default (IS T'SUCC, IS T'IMAGE, ...): calls
```

### ada83.c:23182

```
// through the formal apply the attribute (RM 12.1.3).
```

### ada83.c:23187

```
// Character-literal default (IS 'S'): the literal is a
```

### ada83.c:23188

```
// parameterless function of the formal's result type
```

### ada83.c:23189

```
// (RM 12.1.3), resolved against the substituted actual type.
```

### ada83.c:23202

```
// Operator-designator default (IS "&"): a visible user
```

### ada83.c:23203

```
// operator rode in on default_sym above; otherwise the
```

### ada83.c:23204

```
// designator denotes the PREDEFINED operator, bound as a
```

### ada83.c:23205

```
// builtin exactly like an operator-string actual
```

### ada83.c:23206

```
// (RM 12.1.3, cc1307a).
```

### ada83.c:23223

```
// RM 12.3.1 .. 12.3.6: every actual must match its formal's class
```

### ada83.c:23224

```
// and profile; diagnose before instantiating.
```

### ada83.c:23228

```
// RM 12.3: an instantiation may not depend on itself — directly or
```

### ada83.c:23229

```
// through a chain of instantiations. Reject the cycle here rather
```

### ada83.c:23230

```
// than recursing into it.
```

### ada83.c:23240

```
// Tree-copy instantiation (RM 12.3): a subprogram instance carries
```

### ada83.c:23241

```
// its own resolved clone of the template body and emits through the
```

### ada83.c:23242

```
// ordinary subprogram paths. When the template body has not been
```

### ada83.c:23243

```
// seen yet the instantiation stays deferred (codegen retries once
```

### ada83.c:23244

```
// the body completes the generic).
```

### ada83.c:23251

```
// Tree-copy instantiation (RM 12.3): a package instance carries
```

### ada83.c:23252

```
// its own resolved clones of the template spec and body and emits
```

### ada83.c:23253

```
// through the ordinary package paths.
```

### ada83.c:23261

```
// Resolve entity name
```

### ada83.c:23264

```
// Representation clause: FOR T'SIZE USE 32; or FOR T USE RECORD ...
```

### ada83.c:23269

```
// Get target type or symbol
```

### ada83.c:23278

```
// Process attribute clauses: FOR T'SIZE USE 32;
```

### ada83.c:23282

```
// RM 13.2: a SIZE/STORAGE_SIZE specification is an expression
```

### ada83.c:23283

```
// of some integer type — that context resolves an overloaded
```

### ada83.c:23284

```
// name in the clause (c87b62b: FOR L'STORAGE_SIZE USE F(1024)
```

### ada83.c:23285

```
// must pick the integer-returning F).
```

### ada83.c:23292

```
// Evaluate constant expression (handles T'SIZE/2 etc.)
```

### ada83.c:23296

```
// Apply representation
```

### ada83.c:23297

```
// Size in bits - store exact and convert to bytes
```

### ada83.c:23303

```
// A fixed-point type squeezed below its declared range keeps
```

### ada83.c:23304

```
// only the model numbers its representation can hold: clamp
```

### ada83.c:23305

```
// static bounds to the sized mantissa, so T'LAST is the
```

### ada83.c:23306

```
// largest representable multiple of SMALL rather than a
```

### ada83.c:23307

```
// mantissa that wraps in the narrowed rep (cd2a52e: DELTA
```

### ada83.c:23308

```
// 2**-4 RANGE 0.0 .. 8.0 with 'SIZE 7 has T'LAST = 7.9375).
```

### ada83.c:23329

```
// RM 13.2: a collection/task storage size need not be static.
```

### ada83.c:23330

```
// Record the expression; the clause's elaboration point
```

### ada83.c:23331

```
// freezes its value into a module global that the attribute
```

### ada83.c:23332

```
// reads back (cd2b11d: FOR T'STORAGE_SIZE USE IDENT_INT(256)).
```

### ada83.c:23336

```
// For fixed-point: set small value. The specification is a
```

### ada83.c:23337

```
// static real expression (RM 13.2) — often a named number such
```

### ada83.c:23338

```
// as `for T'SMALL use BASIC_SMALL` — so take the folded value,
```

### ada83.c:23339

```
// not only a literal. The small governs representation for the
```

### ada83.c:23340

```
// whole type family, so record it on the base type, which is
```

### ada83.c:23341

```
// where Fixed_Repr_Small reads it.
```

### ada83.c:23349

```
// Any other attribute clause (ADDRESS, ...) has no effect on
```

### ada83.c:23350

```
// code generation — warn instead of silently accepting.
```

### ada83.c:23359

```
// Process record representation: FOR T USE RECORD ...
```

### ada83.c:23363

```
// Process alignment clause
```

### ada83.c:23372

```
// Process component clauses (record layout)
```

### ada83.c:23373

```
// Each clause: component_name AT byte_position [RANGE bits];
```

### ada83.c:23381

```
// Find matching component in record type
```

### ada83.c:23385

```
// RM 13.4: `comp AT p RANGE f .. l` places the component at
```

### ada83.c:23386

```
// byte p with its stored bits spanning f .. l. All three of
```

### ada83.c:23387

```
// p, f, and l are static expressions (often products of named
```

### ada83.c:23388

```
// numbers such as 2 * UNITS_PER_INTEGER), so fold them rather
```

### ada83.c:23389

```
// than accepting only integer literals. The range width
```

### ada83.c:23390

```
// l - f + 1 becomes the component's bit_size, which feeds the
```

### ada83.c:23391

```
// 'LAST_BIT attribute and the bit-level equality accounting.
```

### ada83.c:23392

```
// Component load/store keys off byte_offset (GEP), not
```

### ada83.c:23393

```
// bit_size, so recording a sub-natural width here is safe.
```

### ada83.c:23423

```
// Recompute the record's total size from the explicit layout: the
```

### ada83.c:23424

```
// extent is the furthest component end (byte_offset + its byte
```

### ada83.c:23425

```
// size). Without this the object keeps its default size and a
```

### ada83.c:23426

```
// component the clause moved beyond it (E AT 3 * UNITS) writes out
```

### ada83.c:23427

```
// of bounds; the enclosing object then loses that field and the
```

### ada83.c:23428

```
// record's own size no longer matches what the clause dictates
```

### ada83.c:23429

```
// (cd1c03h). A derived type copies these figures after the clause.
```

### ada83.c:23442

```
// Address clause (RM 13.5): FOR X USE AT expr makes the object an
```

### ada83.c:23443

```
// overlay on the given address. Mechanism: a hidden cell variable
```

### ada83.c:23444

```
// (X__AT, an access value designating X's type) holds the address,
```

### ada83.c:23445

```
// evaluated where the clause is elaborated; X itself redirects
```

### ada83.c:23446

```
// through renamed_object to CELL.ALL, so every read, write, and
```

### ada83.c:23447

```
// 'ADDRESS flows through the established dereference paths. The
```

### ada83.c:23448

```
// cell's storage and the object's declared initialization are
```

### ada83.c:23449

```
// emitted at the clause's elaboration point (codegen
```

### ada83.c:23450

```
// NK_REPRESENTATION_CLAUSE).
```

### ada83.c:23454

```
// A USE-visible alias denotes the same entity as the original;
```

### ada83.c:23455

```
// the overlay must hang off the original symbol every access
```

### ada83.c:23456

```
// path resolves to.
```

### ada83.c:23497

```
// Process enumeration representation: FOR T USE (RED => code, ...);
```

### ada83.c:23498

```
// The clause binds each literal to its internal code (RM 13.3); the
```

### ada83.c:23499

```
// codes must be given in increasing position order. Objects then store
```

### ada83.c:23500

```
// the code, and positions are recovered on demand (see Enum_Rep_Base).
```

### ada83.c:23509

```
// identity default
```

### ada83.c:23517

```
// The choice names the literal (RED, or 'R'); positional
```

### ada83.c:23518

```
// associations bind literals in declaration order.
```

### ada83.c:23540

```
// An object is stored by position; the codes surface only where
```

### ada83.c:23541

```
// the representation is observed — the object's 'Size and an
```

### ada83.c:23542

```
// unchecked conversion out of the type (RM 13.3). Keeping storage
```

### ada83.c:23543

```
// in position space leaves indexing, ordering, aggregates and
```

### ada83.c:23544

```
// loops untouched.
```

### ada83.c:23558

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:23559

```
// §12.4 Compilation Unit Resolution                                                                
```

### ada83.c:23560

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:23562

```
// Materialize a subunit's parent chain (RM 10.2). The root is a library
```

### ada83.c:23563

```
// unit loaded by name; every later component names a subunit whose source
```

### ada83.c:23564

```
// the program library catalog knows — it is parsed and resolved through
```

### ada83.c:23565

```
// Resolve_Compilation_Unit (recursing through THIS machinery for its own
```

### ada83.c:23566

```
// prefix), so each level's body scope exists before the next descends.
```

### ada83.c:23567

```
// Returns the immediate parent's symbol, or NULL.
```

### ada83.c:23568

```
// Adopt a unit's PUBLISHED frame layout (ALI F/FS lines): the module that
```

### ada83.c:23569

```
// compiled the unit owns the frame, so every frame-resident symbol this
```

### ada83.c:23570

```
// compilation re-resolved takes the published offset, and the scope's
```

### ada83.c:23571

```
// frame_size (the static-chain slot base) takes the published size.
```

### ada83.c:23572

```
// Matching is by emitted name — the same contract the linker enforces.
```

### ada83.c:23622

```
// Materialize this level's subunit BODY (once per compilation): the
```

### ada83.c:23623

```
// SPEC symbol existing is not enough — the body's own declarations are
```

### ada83.c:23624

```
// part of the environment the next level resolves in (RM 10.2).
```

### ada83.c:23647

```
// Adopt the subunit's own PUBLISHED layout: its body-level
```

### ada83.c:23648

```
// variables (invisible to the parent's module) were placed by the
```

### ada83.c:23649

```
// subunit's compilation — that module owns those slots.
```

### ada83.c:23658

```
// The simple name a compilation unit defines — the key under which the
```

### ada83.c:23659

```
// program library catalogs it. Empty slice for kinds that are not library
```

### ada83.c:23660

```
// units in their own right.
```

### ada83.c:23676

```
// RM 10.1.1: reconstruct the context clause of a separately compiled library
```

### ada83.c:23677

```
// unit DECLARATION, so its body can inherit it. The declaration lives in the
```

### ada83.c:23678

```
// program library; its source file may hold several units, so parse them all
```

### ada83.c:23679

```
// and return the context of the one whose name matches. NULL when no spec for
```

### ada83.c:23680

```
// the name is on record (e.g. a subprogram that is its own declaration).
```

### ada83.c:23697

```
// RM 10.1.1: the with and use clauses of a library unit's DECLARATION also
```

### ada83.c:23698

```
// apply to the secondary unit that gives its body — whether repeated there or
```

### ada83.c:23699

```
// not. Fold the spec's context into this body's own so the "applicable" clause
```

### ada83.c:23700

```
// set is a single list every later pass reads: name resolution, extern
```

### ada83.c:23701

```
// emission, and elaboration ordering all consult node->compilation_unit.context.
```

### ada83.c:23702

```
// A subunit inherits its parent's context through the parent scope instead, and
```

### ada83.c:23703

```
// the fold is idempotent (guarded) since the node is shared between passes.
```

### ada83.c:23732

```
// Load WITH'd packages from include paths
```

### ada83.c:23738

```
// WITH clause contains a list of package names
```

### ada83.c:23747

```
// Resolve the identifier to the package symbol
```

### ada83.c:23753

```
// Resolve USE clauses (make package contents visible) and context
```

### ada83.c:23754

```
// pragmas (configuration pragmas take effect for this unit).
```

### ada83.c:23758

```
// Handle separate subunits (SEPARATE (parent) ...)
```

### ada83.c:23764

```
// RM 10.2/10.3: a subunit compiles against its parent's BODY, found
```

### ada83.c:23765

```
// through the program library; a dotted parent (A.B) names a
```

### ada83.c:23766

```
// transitive subunit chain that materializes level by level.
```

### ada83.c:23770

```
// A subunit of a GENERIC unit completes the template's stub: graft the
```

### ada83.c:23771

```
// body into the template AST so every instantiation clones the real
```

### ada83.c:23772

```
// body (RM 10.2 + 12.3). The subunit has no standalone compilation of
```

### ada83.c:23773

```
// its own — its formal types exist only inside instances — so the CU
```

### ada83.c:23774

```
// is marked and skips ordinary resolution and emission.
```

### ada83.c:23837

```
// Enter the parent's scope (lexical chain intact — the stub's whole
```

### ada83.c:23838

```
// enclosing environment must resolve, RM 10.2).
```

### ada83.c:23845

```
// Resolve main unit
```

### ada83.c:23849

```
// A subunit PACKAGE BODY whose chain nests in a subprogram: its
```

### ada83.c:23850

```
// body-level state is INVISIBLE to the parent's module (which sized
```

### ada83.c:23851

```
// the frame first, without it) — so it belongs to the subunit's own
```

### ada83.c:23852

```
// module as package-level globals. Their mangled names are stable
```

### ada83.c:23853

```
// (globals carry no per-compilation suffix), so every other module
```

### ada83.c:23854

```
// names the same storage.
```

### ada83.c:23873

```
// An RM 13.5 address clause turned this object into an overlay
```

### ada83.c:23874

```
// whose hidden __at cell holds its storage — the cell is the
```

### ada83.c:23875

```
// object's real state and must move with it, or its frame slot
```

### ada83.c:23876

```
// lands past the frame the parent's module already sized
```

### ada83.c:23877

```
// (cd5003c).
```

### ada83.c:23894

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:23895

```
// §13. LLVM IR CODE GENERATION                                                                     
```

### ada83.c:23896

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:23899

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:23900

```
// §13.0 Code Generator State                                                                       
```

### ada83.c:23901

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:23933

```
// Construct a typed SSA value: the type travels with the value.
```

### ada83.c:23934

```
// ── Debug producer-honesty checker ────────────────────────────────────────
```

### ada83.c:23935

```
// Copy a single LLVM type token from `p` into `out`, handling aggregate /
```

### ada83.c:23936

```
// array braces ({ ptr, ptr }, [4 x i8]) and plain tokens (i32, ptr, double).
```

### ada83.c:23960

```
// Infer the LLVM result type of a single emitted instruction line of the form
```

### ada83.c:23961

```
// `%t<N> = <opcode> ...`. Returns false (no assertion) for opcodes whose result
```

### ada83.c:23962

```
// type isn't locally determinable (extractvalue) - conservative by design.
```

### ada83.c:23992

```
// skip "i1 %cond,"
```

### ada83.c:24006

```
// Record a temp's emitted result type into the debug shadow (called from
```

### ada83.c:24007

```
// Emit_Impl on the line that defines the temp). -g only.
```

### ada83.c:24030

```
// Canonical LLVM_Value constructor. The producer-honesty check (-g only)
```

### ada83.c:24031

```
// compares the producer's claimed rep against the rep parsed from the actual
```

### ada83.c:24032

```
// emitted IR for the register; a mismatch is a logged mislabel.
```

### ada83.c:24039

```
// reg_emit_type is still the IR-text the shadow recorded. Compare via
```

### ada83.c:24040

```
// string equality once for now; later this whole shadow becomes rep-typed.
```

### ada83.c:24052

```
// Constructors.
```

### ada83.c:24053

```
//   Val_Of_Type(reg, ada_type) - preferred. Derives rep from the Ada Type_Info.
```

### ada83.c:24054

```
//   Val_Rep(reg, rep)          - structural intermediates with no Ada type
```

### ada83.c:24055

```
//                                (raw ptr from a fat extract, C-ABI i64, etc.).
```

### ada83.c:24059

```
// Materialize an i1 computation result (icmp/and/or/xor) as a BOOLEAN *value*.
```

### ada83.c:24060

```
// BOOLEAN's representation is i8 (RM 3.5.3 - an enumeration, byte-stored); i1 is
```

### ada83.c:24061

```
// only LLVM's transient for branch conditions. Every boolean-producing site
```

### ada83.c:24062

```
// returns through here so the value that escapes is honestly i8; consumers that
```

### ada83.c:24063

```
// need a branch condition convert back to i1 at the br/select (the one place
```

### ada83.c:24064

```
// i1 is legitimate).
```

### ada83.c:24071

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:24072

```
// §13.0.2 Type-Derived Codegen Helpers
```

### ada83.c:24073

```
//                                                                                                  
```

### ada83.c:24074

```
// Standard never hardcodes type widths - it derives them from the front-end                        
```

### ada83.c:24075

```
// type system (see GL_Type, Bound_Sub_GT). These helpers do the same: they                        
```

### ada83.c:24076

```
// query sm to derive LLVM IR types from the Ada type system at the point                           
```

### ada83.c:24077

```
// of emission, so every codegen site gets its types from a single source.                         
```

### ada83.c:24078

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:24080

```
// File-scope map of generic formal>actual types for the current instance
```

### ada83.c:24081

```
// being code-generated. Used by Type_To_Rep to resolve generic formal
```

### ada83.c:24082

```
// types (TYPE_PRIVATE) to their actual types.
```

### ada83.c:24083

```
//                                                  
```

### ada83.c:24084

```
// Canonical Ada → LLVM representation lowering. Single source of truth for
```

### ada83.c:24085

```
// fat-vs-thin policy. Callers that need IR text spell it at the Emit() site
```

### ada83.c:24086

```
// via LLVM_Rep_To_String(Type_To_Rep(t)).
```

### ada83.c:24094

```
// Private/limited private/incomplete: resolve through parent_type
```

### ada83.c:24095

```
// (RM 7.4.1, 3.4).
```

### ada83.c:24100

```
// A private view without a completion in sight reps as an opaque
```

### ada83.c:24101

```
// pointer. (Generic formal types never reach representation queries:
```

### ada83.c:24102

```
// instances are resolved clones whose types are the actuals.)
```

### ada83.c:24107

```
// RM 3.5.3 (i8, not i1)
```

### ada83.c:24112

```
// scaled-integer repr
```

### ada83.c:24114

```
// RM 3.5.4 (unsigned)
```

### ada83.c:24118

```
// A float subtype shares its base type's representation (RM 3.5.7): a
```

### ada83.c:24119

```
// DIGITS constraint narrows accuracy, not the machine width. A generic
```

### ada83.c:24120

```
// formal float type resolves to its instantiation actual (RM 12.1.2).
```

### ada83.c:24121

```
// Float_LLVM_Rep_Of handles both (formal resolve + base-type walk).
```

### ada83.c:24124

```
// Representation follows the ROOT access type (RM 3.10): an access SUBTYPE
```

### ada83.c:24125

```
// constraint — `type A is access STRING; subtype A1 is A(1..3)` — cannot
```

### ada83.c:24126

```
// change the pointer width, so A1 stays fat like A (the bounds travel for
```

### ada83.c:24127

```
// the subtype check). Only an access type that itself DIRECTLY designates
```

### ada83.c:24128

```
// a static-bounds-constrained array (`access STRING(1..2)`) is thin.
```

### ada83.c:24145

```
// Unconstrained or dynamic-bounds-constrained: fat. Static-bounds
```

### ada83.c:24146

```
// constrained: plain ptr to flat storage. (RM 3.6.1)
```

### ada83.c:24157

```
// Derive LLVM float type string from a Type_Info.
```

### ada83.c:24158

```
// Falls back to "double" for UNIVERSAL_REAL or when type info is unavailable.
```

### ada83.c:24159

```
// Structured float rep for an Ada type. Defaults to 64-bit for UNIVERSAL_REAL
```

### ada83.c:24160

```
// or when type info is unavailable.
```

### ada83.c:24162

```
// A float subtype shares its base type's representation (RM 3.5.7); resolve
```

### ada83.c:24163

```
// the width through the base-type chain so a DIGITS-constrained subtype keeps
```

### ada83.c:24164

```
// its base type's machine width (derived types link via parent_type instead).
```

### ada83.c:24172

```
// Check whether a float type maps to IEEE single precision (32-bit).
```

### ada83.c:24177

```
// Canonical bound rep. Picks the widest index-type rep across dimensions so a
```

### ada83.c:24178

```
// BOOLEAN index can't truncate an INTEGER one in a mixed-index array.
```

### ada83.c:24182

```
// default STRING bound width
```

### ada83.c:24185

```
// Access > designated type
```

### ada83.c:24189

```
// Private/incomplete > parent
```

### ada83.c:24206

```
// Widest index rep across dimensions. Compare .bits because Type_Info.size
```

### ada83.c:24207

```
// may be unresolved (0) at codegen for subtypes.
```

### ada83.c:24220

```
// No index type info: infer width from static bound values when available.
```

### ada83.c:24235

```
// Get the LLVM bounds struct type string for a given bound type.                                  
```

### ada83.c:24236

```
// e.g., Bounds_Type_For ("i32") > "{ i32, i32 }".                                                 
```

### ada83.c:24237

```
// Used when allocating/loading/storing the bounds struct behind                                    
```

### ada83.c:24238

```
// the second pointer in a { ptr, ptr } fat pointer.                                               
```

### ada83.c:24239

```
//                                                                                                  
```

### ada83.c:24241

```
// Standard style: bounds struct uses the NATIVE index type.
```

### ada83.c:24242

```
// e.g. Bounds_Type_For (i32-rep) > "{ i32, i32 }".
```

### ada83.c:24243

```
// Dispatch by bit width.
```

### ada83.c:24255

```
// Return the allocation size in bytes for a bounds struct.
```

### ada83.c:24256

```
// Used when allocating bounds on the secondary stack (for returned fat ptrs).
```

### ada83.c:24258

```
// Size = 2 * (bits / 8). E.g. { i32, i32 } = 8 bytes.
```

### ada83.c:24264

```
// Derive the LLVM bound type for STRING from the type system.
```

### ada83.c:24265

```
// Follows: STRING > index_type (POSITIVE) > Type_To_Rep > LLVM_Rep_To_String.
```

### ada83.c:24266

```
// This is Standard's Bound_Sub_GT path.                                                           
```

### ada83.c:24267

```
//                                                                                                  
```

### ada83.c:24273

```
// Derive the LLVM type for Standard.INTEGER (the universal arithmetic type).                      
```

### ada83.c:24274

```
// In Standard, integer computation uses the GL_Type of Standard.Integer.                          
```

### ada83.c:24275

```
// This replaces hardcoded "i64" in expression evaluation paths.                                   
```

### ada83.c:24276

```
//                                                                                                  
```

### ada83.c:24277

```
// Standard.Integer's canonical rep — the default arithmetic width.
```

### ada83.c:24282

```
// The rep for byte-size and byte-offset arithmetic. C-level allocation and
```

### ada83.c:24283

```
// copying (memcpy, alloca, malloc, secondary stack) take i64 sizes on the
```

### ada83.c:24284

```
// LP64 target regardless of Ada INTEGER width; dynamic strides, footprints,
```

### ada83.c:24285

```
// and record offsets are computed at this width so they never truncate.
```

### ada83.c:24290

```
// The rep of one runtime type-descriptor cell: the value counterpart of the
```

### ada83.c:24291

```
// RT_DESC_TYPE global element type. Bounds and discriminant values frozen
```

### ada83.c:24292

```
// into @__rt_* globals are stored and read back at this width.
```

### ada83.c:24297

```
// Emit to string constant buffer instead of main output
```

### ada83.c:24304

```
// Format error
```

### ada83.c:24307

```
// Expand buffer if needed
```

### ada83.c:24325

```
// Append the symbol's EMITTED name (every Emit_Symbol_Name rule applied,
```

### ada83.c:24326

```
// including the instance-prefix forms) to the accumulator. Captures the
```

### ada83.c:24327

```
// emitter's output through a temporary stream so the definition and every
```

### ada83.c:24328

```
// reference agree byte for byte.
```

### ada83.c:24343

```
// Emit a single char to string constant buffer
```

### ada83.c:24356

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:24357

```
// §13.1 Core IR Emission Primitives (body)                                                                        
```

### ada83.c:24358

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:24366

```
// Emit IR text. When `cg->emit_debug_locations` is true, append a trailing
```

### ada83.c:24367

```
// `; @ FUNC:LINE` comment to every emitted line, attributed to whichever C
```

### ada83.c:24368

```
// site *started* the line (subsequent fragments contributing to the same
```

### ada83.c:24369

```
// line do not overwrite the attribution). Embedded newlines in a single
```

### ada83.c:24370

```
// format string are all attributed to the same captured site, which is what
```

### ada83.c:24371

```
// we want for multi-line block emissions.
```

### ada83.c:24372

```
//
```

### ada83.c:24373

```
// Note: Emit is the macro defined alongside the forward decl; callers stay
```

### ada83.c:24374

```
// `Emit (...)` unchanged. The macro injects __func__ / __LINE__.
```

### ada83.c:24375

```
// TODO: Emit-macro: claude added this undef and redef after the function. Do we need that?
```

### ada83.c:24378

```
// Capture the originating site when we are at the start of a fresh line.
```

### ada83.c:24396

```
// Debug mode: format into a buffer so we can scan for `\n` and inject the
```

### ada83.c:24397

```
// trailing site-comment before each one. Use a stack buffer for the common
```

### ada83.c:24398

```
// case and fall back to the heap for the rare long line.
```

### ada83.c:24413

```
// Producer-honesty shadow: if this fragment begins an instruction line that
```

### ada83.c:24414

```
// defines a temp, record the temp's actual emitted result type.
```

### ada83.c:24417

```
// Walk the buffer; on each '\n', inject `  ; @ FUNC:LINE` before the newline.
```

### ada83.c:24418

```
// Annotate iff the current IR line had any non-whitespace content (tracked
```

### ada83.c:24419

```
// across calls via `emit_line_has_content`) — this skips pure paragraph-break
```

### ada83.c:24420

```
// blank lines while still annotating lines built by multiple Emit calls
```

### ada83.c:24421

```
// (e.g. `Emit("  store ...")` + `Emit_Symbol_Name(sym)` + `Emit("\n")`).
```

### ada83.c:24428

```
// Update the line-content tracker over the chunk we are about to write.
```

### ada83.c:24446

```
// Next IR line starts. Re-capture from this call so multi-line emissions
```

### ada83.c:24447

```
// attribute internal lines to whichever Emit_Impl invocation wrote them.
```

### ada83.c:24459

```
// Mint a fresh SSA temporary and emit the instruction line defining it.
```

### ada83.c:24460

```
// The caller's arguments are formatted into a buffer FIRST (stack buffer with
```

### ada83.c:24461

```
// heap fallback, same style as Emit_Impl), the temporary is minted second, and
```

### ada83.c:24462

```
// the assembled `  %tN = <text>` line goes through Emit_Impl so -g site
```

### ada83.c:24463

```
// attribution and the producer-honesty shadow both see one complete line.
```

### ada83.c:24487

```
// Emit a source location comment for debugging: ; [filename:line:col]
```

### ada83.c:24492

```
// Extract just the basename for brevity
```

### ada83.c:24501

```
// Emit a label, inserting a fallthrough branch if the prior block is open
```

### ada83.c:24510

```
// Emit a branch only if block is not already terminated
```

### ada83.c:24518

```
// Close an emitted runtime-support function that returns void. These
```

### ada83.c:24519

```
// hand-written IR bodies use named labels (a shared `done` exit block), not
```

### ada83.c:24520

```
// the numbered %LN labels the block-tracking helpers manage.
```

### ada83.c:24528

```
// Emit a floating-point constant at the correct precision for the given
```

### ada83.c:24529

```
// float rep. For single (32-bit) emits `fadd float 0.0, 0x<hex>` after a
```

### ada83.c:24530

```
// float>double widening of the hex pattern; for double emits the value's
```

### ada83.c:24531

```
// own bit pattern directly. Replaces scattered hardcoded "fadd double".
```

### ada83.c:24536

```
// LLVM represents float hex as double-precision hex.
```

### ada83.c:24537

```
// Convert to float, then back to double to get exact representation.
```

### ada83.c:24553

```
// Check if symbol is package-level (global storage with @ prefix in LLVM).                        
```

### ada83.c:24554

```
// A symbol is global only if it's at package level AND no ancestor is a subprogram.               
```

### ada83.c:24555

```
// This handles nested packages inside subprogram bodies correctly.                                
```

### ada83.c:24556

```
//                                                                                                  
```

### ada83.c:24558

```
// A USE-visible alias denotes the same entity as the original; its
```

### ada83.c:24559

```
// storage class is the original's (mirrors Symbol_Mangle_Name's peel).
```

### ada83.c:24562

```
// A FOR-loop parameter lives in the frame of whatever function emits
```

### ada83.c:24563

```
// the loop — including a loop in a package body's statement part,
```

### ada83.c:24564

```
// whose parent chain otherwise looks package-level.
```

### ada83.c:24567

```
// Tree-copy instance formal-object bindings are per-instance globals by
```

### ada83.c:24568

```
// construction: they elaborate at the instantiation point and the
```

### ada83.c:24569

```
// instance body reads them as module-level storage (RM 12.3(80)).
```

### ada83.c:24573

```
// Subunit-added package state: global in the subunit's own module by
```

### ada83.c:24574

```
// construction (the parent's frame was sized before this state existed).
```

### ada83.c:24576

```
// Top-level
```

### ada83.c:24578

```
// Walk up parent chain - if any ancestor is a subprogram, symbol is local.                      
```

### ada83.c:24579

```
// Also check for SYMBOL_GENERIC whose generic_unit is a subprogram spec                          
```

### ada83.c:24580

```
// (not a package spec) - symbols inside generic subprogram bodies must be                        
```

### ada83.c:24581

```
// treated as local when instantiated. Generic package variables remain                          
```

### ada83.c:24582

```
// global because they're allocated as package-level storage.                                    
```

### ada83.c:24583

```
//                                                                                                
```

### ada83.c:24586

```
// A generic package's PACKAGE-LEVEL variables (spec or body
```

### ada83.c:24587

```
// declarative part) are per-instance globals named <instance>__<var>
```

### ada83.c:24588

```
// even when the generic declaration or the instantiation is nested
```

### ada83.c:24589

```
// inside a subprogram. Both the template symbol (parent: the
```

### ada83.c:24590

```
// SYMBOL_GENERIC) and the instance's exported clone (parent: the
```

### ada83.c:24591

```
// instance package) classify as global here so every reference path
```

### ada83.c:24592

```
// agrees with the storage emission. Block-locals and subprogram
```

### ada83.c:24593

```
// locals inside the generic are NOT package-level and fall through
```

### ada83.c:24594

```
// to the ordinary nesting walk.
```

### ada83.c:24606

```
// Inside a subprogram - use local (%) prefix
```

### ada83.c:24608

```
// A task body is emitted as a function: a local whose parent chain
```

### ada83.c:24609

```
// passes through a task (task type, or single task object) lives in
```

### ada83.c:24610

```
// that function's frame exactly like a subprogram local. Without this
```

### ada83.c:24611

```
// the walk sails on to the enclosing package and classifies a library
```

### ada83.c:24612

```
// task's body local as module-global — the declaration emits an alloca
```

### ada83.c:24613

```
// while every use reads an undefined @global (c94004a).
```

### ada83.c:24619

```
// Inside a generic subprogram - local
```

### ada83.c:24623

```
// No subprogram ancestor - use global (@) prefix
```

### ada83.c:24626

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:24627

```
// Symbol_Mangle_Name - Unified LLVM-compatible name mangling                                       
```

### ada83.c:24628

```
//                                                                                                  
```

### ada83.c:24629

```
// Produces consistent mangled names for:                                                           
```

### ada83.c:24630

```
//   1. Code generation (LLVM IR identifiers)                                                       
```

### ada83.c:24631

```
//   2. ALI files (linkage names)                                                                   
```

### ada83.c:24632

```
//   3. Cross-compilation linking                                                                   
```

### ada83.c:24633

```
//                                                                                                  
```

### ada83.c:24634

```
// Format: parent__name[_SN] (all lowercase, special chars as _XX)                                  
```

### ada83.c:24635

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:24637

```
// Internal recursive helper                                                                        
```

### ada83.c:24638

```
// Mangle a name slice into buf at pos: lowercase, escape non-alphanum as _XX.                     
```

### ada83.c:24639

```
// GNAT convention: operator symbols become _op_, special chars become _HH.                        
```

### ada83.c:24640

```
//                                                                                                  
```

### ada83.c:24662

```
// Package members qualify through their package; a tree-copy instance's
```

### ada83.c:24663

```
// formal-object binding qualifies through its instance, whatever the
```

### ada83.c:24664

```
// instance's kind, so distinct instances never share a binding global.
```

### ada83.c:24669

```
// A suffix-exempt (library-stable) name nested under a library
```

### ada83.c:24670

```
// SUBPROGRAM must carry the subprogram's name too: a local package P
```

### ada83.c:24671

```
// inside procedure M would otherwise collide with a LIBRARY package P
```

### ada83.c:24672

```
// (ca1002: subunits deliberately reuse library-unit names).
```

### ada83.c:24684

```
// Get mangled name for a symbol (thread-safe via rotation)
```

### ada83.c:24685

```
// A SEPARATE stub — and the subunit body that completes it in another
```

### ada83.c:24686

```
// compilation — must carry the SAME emitted name everywhere: the parent's
```

### ada83.c:24687

```
// module declares it extern, the subunit's module defines it. Parent-chain
```

### ada83.c:24688

```
// qualification already makes the name unique (RM 10.2: subunit names are
```

### ada83.c:24689

```
// unique within their parent), so the per-compilation _s suffix is omitted.
```

### ada83.c:24706

```
// A name is library-stable when its qualified parent chain alone
```

### ada83.c:24707

```
// identifies it in EVERY compilation that can see it: each enclosing
```

### ada83.c:24708

```
// level is a package, up to the library unit at the root. Such entities
```

### ada83.c:24709

```
// are reachable across subunit boundaries (the parent's module calls
```

### ada83.c:24710

```
// them, the subunit's module may define them), so the per-compilation
```

### ada83.c:24711

```
// _s suffix must not differ — the chain is collision-free by Ada's
```

### ada83.c:24712

```
// no-homograph rules, blocks and nested subprograms break the chain.
```

### ada83.c:24717

```
// Only PROGRAM UNITS are nameable across modules; data lives in frames
```

### ada83.c:24718

```
// or as globals under the ordinary rules.
```

### ada83.c:24723

```
// Walk the LEXICAL scope chain (the symbol-parent chain cannot see
```

### ada83.c:24724

```
// DECLARE blocks): every enclosing level must be a package scope, up to
```

### ada83.c:24725

```
// the library unit's own scope just under the global scope. An
```

### ada83.c:24726

```
// anonymous block anywhere makes same-named siblings possible, so the
```

### ada83.c:24727

```
// suffix stays.
```

### ada83.c:24731

```
// anonymous block scope
```

### ada83.c:24742

```
// A USE-visible alias denotes the same entity as the original; mangle it as
```

### ada83.c:24743

```
// the original so a cross-USE call matches the definition (the alias may have
```

### ada83.c:24744

```
// been flagged overloaded by an unrelated same-name homograph in the using
```

### ada83.c:24745

```
// scope, which must not change the emitted symbol name).
```

### ada83.c:24748

```
// A name already emitted is FROZEN: mangling reads mutable facts
```

### ada83.c:24749

```
// (is_overloaded, the defining scope chain), and codegen itself creates
```

### ada83.c:24750

```
// homographs (synthesized operator wrappers) after some references have
```

### ada83.c:24751

```
// been emitted — the definition and every reference must keep one
```

### ada83.c:24752

```
// spelling (cc1301a: the formal "-" default's wrapper flipped the user
```

### ada83.c:24753

```
// "-" to overloaded between its define and a later call).
```

### ada83.c:24762

```
// Build mangled name from parent chain
```

### ada83.c:24765

```
// Add unique_id suffix for local/overloaded symbols — and for instance
```

### ada83.c:24766

```
// formal-object bindings, whose readable prefix alone cannot distinguish
```

### ada83.c:24767

```
// two same-named instantiations in different scopes.
```

### ada83.c:24777

```
// Freeze only once emission is underway: during resolution the facts the
```

### ada83.c:24778

```
// name depends on are still legitimately settling.
```

### ada83.c:24786

```
// Get mangled name for parent+name pair (for ALI generation before symbols exist).
```

### ada83.c:24787

```
// Reuses Mangle_Slice_Into for consistent GNAT-style mangling.
```

### ada83.c:24800

```
// Start a local SSA assignment line: "  %<symbol-name>" (caller appends " = ...").
```

### ada83.c:24806

```
// Emit a global reference "@<symbol-name>".
```

### ada83.c:24812

```
// Materialize the address of `sym`'s storage into register `reg`
```

### ada83.c:24813

```
// (`%t<reg> = getelementptr i8, ptr <storage>, i64 0`). For an object
```

### ada83.c:24814

```
// renaming, the rename symbol has no storage of its own — its address is
```

### ada83.c:24815

```
// whatever the rename's target evaluated to at elaboration, captured in the
```

### ada83.c:24816

```
// rename_address_slot. Load that instead so every caller of Emit_Symbol_Addr
```

### ada83.c:24817

```
// gets the target address uniformly.
```

### ada83.c:24826

```
// An RM 13.5 overlay (FOR X USE AT ...) allocates no storage of its own:
```

### ada83.c:24827

```
// the object lives at the clause's address, held by the hidden address
```

### ada83.c:24828

```
// cell. Its address is the cell's VALUE — uniform here so every consumer
```

### ada83.c:24829

```
// (indexed lvalue bases, loads, parameter passing) dereferences the
```

### ada83.c:24830

```
// overlay instead of naming never-allocated storage.
```

### ada83.c:24842

```
// Load the captured target address of an object renaming (RM 8.5) from its
```

### ada83.c:24843

```
// hidden address slot into a fresh temp. Every access through the rename —
```

### ada83.c:24844

```
// read, write, taken address for parameter passing — routes through this
```

### ada83.c:24845

```
// helper so the rename stays bound to the object first designated.
```

### ada83.c:24853

```
// The exact byte sequence emitted as `sym`'s LLVM identifier — the symbol's
```

### ada83.c:24854

```
// module-level identity. Anything that reasons about emitted names (the
```

### ada83.c:24855

```
// declare-once registry) must key on THIS, not on Symbol_Mangle_Name alone,
```

### ada83.c:24856

```
// whose spelling the rules below override for imports and instance clones.
```

### ada83.c:24860

```
// A USE-visible alias denotes the same entity as the original; every
```

### ada83.c:24861

```
// naming rule below must see the original (mirrors Symbol_Mangle_Name).
```

### ada83.c:24864

```
// For imported symbols with external name, use that directly
```

### ada83.c:24865

```
// (stripping the quotes a pragma Import string literal carries).
```

### ada83.c:24875

```
// Tree-copy instance formal-object bindings own their storage name —
```

### ada83.c:24876

```
// Symbol_Mangle_Name already yields <instance>__<formal>_sN — and are
```

### ada83.c:24877

```
// exempt from the instance-name rewriting rule below (it serves the
```

### ada83.c:24878

```
// substitution-at-emission machinery).
```

### ada83.c:24882

```
// An instance's exported variable/constant clone names the same storage
```

### ada83.c:24883

```
// the template path above names: <instance>__<var>. References from
```

### ada83.c:24884

```
// outside the instance (PP.D) bind to the clone; references from inside
```

### ada83.c:24885

```
// the instance's own statements and subprograms bind to the template
```

### ada83.c:24886

```
// symbol — both must produce one name, byte for byte.
```

### ada83.c:24905

```
// Unified mangling (lowercase, parent__name format, _sN suffix)
```

### ada83.c:24909

```
// Emit symbol name for LLVM identifier.
```

### ada83.c:24916

```
// Emit symbol reference with appropriate prefix (@ for global, % for local)
```

### ada83.c:24922

```
// Emit a `@__exc.<mangled> = private constant [N x i8] c"NAME\00"` definition.
```

### ada83.c:24923

```
// The contents are the upper-cased printable name, used by the unhandled-
```

### ada83.c:24924

```
// exception diagnostic in `__ada_raise`. Linkage is `private` because we
```

### ada83.c:24925

```
// emit this per compilation unit; address comparisons across units for the
```

### ada83.c:24926

```
// same exception currently rely on `linkonce_odr` (standards), and that
```

### ada83.c:24927

```
// asymmetry is a pre-existing design choice we don't touch here.
```

### ada83.c:24938

```
// Emit @__exc.<name> reference for an exception symbol and track the name
```

### ada83.c:24939

```
// so Generate_Exception_Globals can emit matching definitions.
```

### ada83.c:24942

```
// RM 8.5: a renamed exception IS its target — one identity, one global.
```

### ada83.c:24947

```
// Capture the exception name by temporarily redirecting output
```

### ada83.c:24959

```
// Emit the reference
```

### ada83.c:24962

```
// Record the name if not already tracked
```

### ada83.c:24972

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:24973

```
// §13.0.1 Codegen Predicates - Eliminate Duplicated Inline Checks                                  
```

### ada83.c:24974

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:24976

```
// Find the nearest enclosing function/procedure by walking up the parent chain.                   
```

### ada83.c:24977

```
// This handles nested packages: a procedure inside a package inside a procedure                    
```

### ada83.c:24978

```
// needs access to the outermost procedure's frame.                                                
```

### ada83.c:24979

```
// Returns NULL if no enclosing function/procedure found.                                          
```

### ada83.c:24980

```
//                                                                                                  
```

### ada83.c:24981

```
// The nearest enclosing FRAME-OWNING level of the static chain: a
```

### ada83.c:24982

```
// subprogram, or a task body (RM 9.1 — a task body owns a per-instance
```

### ada83.c:24983

```
// frame exactly like a subprogram when it declares nested units; its owner
```

### ada83.c:24984

```
// symbol is the task type, or the object for a single task). Packages are
```

### ada83.c:24985

```
// transparent — their declarations share the enclosing frame.
```

### ada83.c:24999

```
// Check if a subprogram needs static chain (is nested transitively inside                          
```

### ada83.c:25000

```
// another function/procedure). This handles package subprograms:                                   
```

### ada83.c:25001

```
//   procedure Outer is                                                                             
```

### ada83.c:25002

```
//     package P is                                                                                 
```

### ada83.c:25003

```
//       procedure Inner;  -- Inner needs access to Outer's frame                                   
```

### ada83.c:25004

```
//     end P;                                                                                       
```

### ada83.c:25005

```
//   ...                                                                                           
```

### ada83.c:25006

```
//                                                                                                  
```

### ada83.c:25008

```
// A pragma Import subprogram uses the foreign (C) calling convention and
```

### ada83.c:25009

```
// never receives a static link, however deeply it is nested — passing one
```

### ada83.c:25010

```
// would shift its real arguments by a register. This matters inside a
```

### ada83.c:25011

```
// generic body, where an imported operation is lexically nested in the
```

### ada83.c:25012

```
// instance and would otherwise look chain-carrying.
```

### ada83.c:25017

```
// Follow the rename chain for subprogram renamings.
```

### ada83.c:25018

```
//
```

### ada83.c:25019

```
// A renaming declaration (RM 8.5) — e.g. `FUNCTION "+" RENAMES "*"` — does
```

### ada83.c:25020

```
// not generate its own body. Calls to the rename must dispatch to the
```

### ada83.c:25021

```
// renamed entity. Code-generation paths that emit `@name` for a call, or
```

### ada83.c:25022

```
// query `Subprogram_Needs_Static_Chain` on the callee, must peel through
```

### ada83.c:25023

```
// the chain first; otherwise they emit a call to a never-defined function
```

### ada83.c:25024

```
// and (if the rename appeared inside another subprogram) pass `%__frame_base`
```

### ada83.c:25025

```
// from a function that never allocated one.
```

### ada83.c:25026

```
//
```

### ada83.c:25027

```
// The chain may be deeper than one link: renaming a rename is legal.
```

### ada83.c:25028

```
//
```

### ada83.c:25029

```
// For object renames (`X : T RENAMES Y`) the `renamed_object` field stores
```

### ada83.c:25030

```
// a `Syntax_Node*` (the renamed expression) — NOT a Symbol — and is peeled
```

### ada83.c:25031

```
// elsewhere in lvalue/identifier generation. We detect subprogram renames
```

### ada83.c:25032

```
// by the symbol kind, which is set when the rename declaration is processed.
```

### ada83.c:25033

```
//
```

### ada83.c:25034

```
// For an operator node, the result is the symbol the call actually
```

### ada83.c:25035

```
// dispatches to — a generic formal subprogram is a rename of its actual, so
```

### ada83.c:25036

```
// the peel covers instances too. Generate_Binary_Op and Expression_Is_Boolean
```

### ada83.c:25037

```
// both route through here so the emitted call and its consumers agree on the
```

### ada83.c:25038

```
// result representation.
```

### ada83.c:25039

```
//
```

### ada83.c:25042

```
// memoized peel
```

### ada83.c:25048

```
// A terminal non-subprogram target IS the denoted entity: an ENTRY
```

### ada83.c:25049

```
// (RM 8.5(8) — calls through the rename are entry calls) or an
```

### ada83.c:25050

```
// enumeration LITERAL standing for a parameterless function
```

### ada83.c:25051

```
// (RM 12.3.6). The call emitters dispatch on the target's kind.
```

### ada83.c:25061

```
// Map an Ada operator's source-text name to its Token_Kind. Returns TK_EOF
```

### ada83.c:25062

```
// for names that are not operators (used as a sentinel). The translation is
```

### ada83.c:25063

```
// needed when a subprogram rename peels to a predefined operator whose name
```

### ada83.c:25064

```
// differs from the source expression's op token — e.g. `FUNCTION "+" RENAMES "*"`
```

### ada83.c:25065

```
// turns `a + b` into `a * b`. The emitter dispatches by Token_Kind, so we
```

### ada83.c:25066

```
// re-express the rename's name as the matching token.
```

### ada83.c:25067

```
//
```

### ada83.c:25073

```
// Is sym an uplevel reference requiring access through __parent_frame?
```

### ada83.c:25075

```
// Globals are never uplevel: their storage is module-level no matter how
```

### ada83.c:25076

```
// deep the reference (per-instance generic state included).
```

### ada83.c:25081

```
// Parameter of the current function is local; parameter of an
```

### ada83.c:25082

```
// enclosing function (e.g., outer subprogram's IN OUT param read by
```

### ada83.c:25083

```
// a nested helper) is uplevel and needs %__frame.X.
```

### ada83.c:25088

```
// Emitting a subunit package-body elaboration function (current_function
```

### ada83.c:25089

```
// is the PACKAGE symbol): the function has no frame of its own, so every
```

### ada83.c:25090

```
// frame-resident symbol lives in the parent subprogram's frame and is
```

### ada83.c:25091

```
// reached through its %__frame. alias.
```

### ada83.c:25099

```
// Local to current function or its generic template
```

### ada83.c:25103

```
// In generic instances, locals from the template body have their owner
```

### ada83.c:25104

```
// set to the template's function symbol. Walk up the scope chain to see
```

### ada83.c:25105

```
// if any scope's owner matches the current function (direct or via template).
```

### ada83.c:25112

```
// Match by unique_id: generic expansion can clone functions
```

### ada83.c:25117

```
// A symbol whose owner chain never reaches a subprogram or task body has
```

### ada83.c:25118

```
// no frame at all — it is a library-package-level GLOBAL (e.g. a package
```

### ada83.c:25119

```
// body variable read by the package's own library-level task).
```

### ada83.c:25133

```
// Emit the storage location for a symbol, automatically handling uplevel                           
```

### ada83.c:25134

```
// access through __frame.                                                                         
```

### ada83.c:25135

```
// Emits either "%__frame.<name>" (uplevel) or the normal "%<name>"/"@<name>".                     
```

### ada83.c:25136

```
//                                                                                                  
```

### ada83.c:25143

```
// A package-level global may be DEFINED by another module (subunit
```

### ada83.c:25144

```
// package state): note it, and the post-generation flush declares it
```

### ada83.c:25145

```
// external if no definition appeared here.
```

### ada83.c:25155

```
// An accept formal is frame-resident whenever a nested subprogram in the
```

### ada83.c:25156

```
// accept body can read it uplevel (RM 6.2): the formal has a slot in the
```

### ada83.c:25157

```
// TASK BODY's own frame — the task is a static-chain level — so bind the
```

### ada83.c:25158

```
// formal's local name to that slot. The accept body (through %<name>) and
```

### ada83.c:25159

```
// any nested reader (through its %__frame.<name> alias off this task's
```

### ada83.c:25160

```
// frame) then share one storage location; the caller writes the incoming
```

### ada83.c:25161

```
// value through %<name>. A leaf task body allocates no frame, and can
```

### ada83.c:25162

```
// contain no nested reader — return false so the caller keeps the formal
```

### ada83.c:25163

```
// as a plain local binding.
```

### ada83.c:25175

```
// Load a scalar of representation `rep` from `sym`'s storage into register `reg`.
```

### ada83.c:25187

```
// Load a pointer (e.g. an access value) from `sym`'s storage into `reg`.
```

### ada83.c:25199

```
// RM 8.3 Static Chain for Deeply Nested Calls - two-phase design.                                 
```

### ada83.c:25200

```
//                                                                                                  
```

### ada83.c:25201

```
// Phase 1 (Precompute_Nested_Frame_Arg): call BEFORE building the call                             
```

### ada83.c:25202

```
//   instruction. For depth ≥ 2 it emits GEP/load instructions to chase the                        
```

### ada83.c:25203

```
//   static chain and returns the temp holding the ancestor frame pointer.                         
```

### ada83.c:25204

```
//   For depth 0-1 it returns 0 (no precomputation needed).                                        
```

### ada83.c:25205

```
//                                                                                                  
```

### ada83.c:25206

```
// Phase 2 (Emit_Nested_Frame_Arg): call INSIDE the call instruction's                              
```

### ada83.c:25207

```
//   argument list. Emits "ptr %__frame_base", "ptr %__parent_frame", or                           
```

### ada83.c:25208

```
//   "ptr %tN" depending on depth.                                                                 
```

### ada83.c:25209

```
//                                                                                                  
```

### ada83.c:25210

```
// Each intermediate frame stores its received __parent_frame at byte                               
```

### ada83.c:25211

```
// offset scope->frame_size (see Generate_Subprogram_Body prologue).                               
```

### ada83.c:25212

```
//                                                                                                  
```

### ada83.c:25215

```
// Find the actual enclosing subprogram (skips packages)
```

### ada83.c:25223

```
// shouldn't happen in valid code
```

### ada83.c:25227

```
// no precomputation needed
```

### ada83.c:25255

```
// Emit raise sequence for a named exception: ptrtoint + call __ada_raise + unreachable.
```

### ada83.c:25260

```
// unreachable terminates block
```

### ada83.c:25265

```
// RM 13.2 / 11.1: an allocator for an access type whose collection size was
```

### ada83.c:25266

```
// specified draws its storage from that bounded collection. Debit the
```

### ada83.c:25267

```
// collection's budget global by the request, raising STORAGE_ERROR when the
```

### ada83.c:25268

```
// remaining units cannot cover it. Derived access types and access subtypes
```

### ada83.c:25269

```
// share the root type's collection, reached through the base/parent chain.
```

### ada83.c:25270

```
// `size64` is the request in storage units as an i64 temp.
```

### ada83.c:25296

```
// RM 13.10.1: deallocating an object returns its storage to the access
```

### ada83.c:25297

```
// type's bounded collection — the inverse of Emit_Collection_Budget_Debit,
```

### ada83.c:25298

```
// reached through the same base/parent chain. No-op when no collection size
```

### ada83.c:25299

```
// was specified.
```

### ada83.c:25317

```
// RM 11.1: raise STORAGE_ERROR when an automatic object's storage exceeds what
```

### ada83.c:25318

```
// the stack can provide. Emit at the elaboration point so the exception reaches
```

### ada83.c:25319

```
// the enclosing scope (RM 11.2), never the object's own block handler.
```

### ada83.c:25320

```
//
```

### ada83.c:25321

```
// Emit_Storage_Error_If_Oversized_Static covers the STRUCTURAL case: an object
```

### ada83.c:25322

```
// beyond MAX_STACK_OBJECT_BYTES received only a placeholder slot (no alloca of
```

### ada83.c:25323

```
// its real size exists), so its elaboration must raise no matter how much stack
```

### ada83.c:25324

```
// the machine actually has. The check is conditional (not an unconditional
```

### ada83.c:25325

```
// terminator) so control flow after the declaration stays well-formed IR.
```

### ada83.c:25326

```
//
```

### ada83.c:25327

```
// The two probe variants cover the EXTENT case: the object's alloca is emitted,
```

### ada83.c:25328

```
// and __ada_stack_check compares its size against the current thread's actual
```

### ada83.c:25329

```
// remaining stack (getrlimit-derived) at run time, raising STORAGE_ERROR when
```

### ada83.c:25330

```
// it cannot fit. The static variant skips the call for objects too small to
```

### ada83.c:25331

```
// matter; the dynamic variant probes unconditionally since the size is unknown.
```

### ada83.c:25350

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:25351

```
// §13.2.1 Granular Runtime Check Emission                                                         
```

### ada83.c:25352

```
//                                                                                                  
```

### ada83.c:25353

```
// Each Emit_*_Check function:                                                                      
```

### ada83.c:25354

```
//   1. Consults Check_Is_Suppressed () - returns early if suppressed.                             
```

### ada83.c:25355

```
//   2. Emits the check logic (comparison + conditional branch).                                   
```

### ada83.c:25356

```
//   3. On failure, branches to a block that calls Emit_Raise_Constraint_Error.                    
```

### ada83.c:25357

```
//   4. On success, falls through to a continuation block.                                         
```

### ada83.c:25358

```
//                                                                                                  
```

### ada83.c:25359

```
// LLVM IR pattern for every check:                                                                 
```

### ada83.c:25360

```
//   %cmp = icmp <pred> <type> %val, <bound>                                                        
```

### ada83.c:25361

```
//   br i1 %cmp, label %raise, label %cont                                                          
```

### ada83.c:25362

```
//   raise:                                                                                         
```

### ada83.c:25363

```
//     <raise constraint_error>                                                                     
```

### ada83.c:25364

```
//   cont:                                                                                          
```

### ada83.c:25365

```
//     ; continue execution                                                                         
```

### ada83.c:25366

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:25368

```
// Emit_Overflow_Checked_Op - signed integer overflow check via LLVM intrinsics.                   
```

### ada83.c:25369

```
// Uses llvm.sadd/ssub/smul.with.overflow.iN for signed types.                                     
```

### ada83.c:25370

```
// Modular (unsigned) types are exempt - they wrap (RM 3.5.4).                                     
```

### ada83.c:25371

```
// Returns the temp ID holding the checked result.                                                 
```

### ada83.c:25372

```
//                                                                                                  
```

### ada83.c:25375

```
// "add", "sub", "mul"
```

### ada83.c:25376

```
// width to perform the operation at (operands already at this width)
```

### ada83.c:25383

```
// Plain operation (wraps for modular, or check suppressed)
```

### ada83.c:25387

```
// Modular non-power-of-2: caller handles urem wrapping
```

### ada83.c:25391

```
// Signed overflow check via LLVM intrinsic
```

### ada83.c:25398

```
// No intrinsic for div/rem - fall back to plain
```

### ada83.c:25403

```
// Emit: %pair = call {iN, i1} @llvm.sOP.with.overflow.iN(iN %left, iN %right)
```

### ada83.c:25406

```
// Extract result and overflow flag
```

### ada83.c:25410

```
// Branch on overflow
```

### ada83.c:25415

```
// Emit_Division_Check - division by zero check (RM 4.5.5).                                        
```

### ada83.c:25416

```
// Also checks for signed MIN_INT / -1 overflow on two's complement.                               
```

### ada83.c:25417

```
// Emits before sdiv/udiv/srem/urem.                                                               
```

### ada83.c:25418

```
//                                                                                                  
```

### ada83.c:25422

```
// Check divisor == 0
```

### ada83.c:25427

```
// Emit_Signed_Division_Overflow_Check - check for MIN_INT / -1.                                   
```

### ada83.c:25428

```
// Only for signed types: Integer'First / (-1) overflows on two's complement.                      
```

### ada83.c:25429

```
// Emits after the division-by-zero check, before the actual sdiv.                                 
```

### ada83.c:25430

```
//                                                                                                  
```

### ada83.c:25436

```
// Check: divisor == -1 AND dividend == type_min
```

### ada83.c:25437

```
// type_min for iN is -(2^(N-1)) which is the LLVM representation of
```

### ada83.c:25438

```
// the minimum signed value. We use a two-step check.
```

### ada83.c:25441

```
// Check divisor == -1
```

### ada83.c:25444

```
// Get type minimum: for iN, min = -(1 << (N-1)).
```

### ada83.c:25445

```
// In LLVM, the min of i32 is -2147483648, i64 is -9223372036854775808.
```

### ada83.c:25446

```
// We compute the min using shl + sign.
```

### ada83.c:25450

```
// Check dividend == min
```

### ada83.c:25453

```
// Both conditions must be true for overflow
```

### ada83.c:25458

```
// Emit_Index_Check - array index bounds check (RM 4.1.1).                                         
```

### ada83.c:25459

```
// Checks index against array low and high bounds.                                                 
```

### ada83.c:25460

```
// Bounds are provided as temp IDs (may be static or dynamic).                                     
```

### ada83.c:25461

```
//                                                                                                  
```

### ada83.c:25467

```
// Ensure all operands match index_rep
```

### ada83.c:25476

```
// index < low?
```

### ada83.c:25479

```
// index > high?
```

### ada83.c:25486

```
// RM 4.1.2: non-null slice A(L..H) requires L and H both in A'FIRST..A'LAST.
```

### ada83.c:25487

```
// Skip check if slice is null (L > H). Emits CE on violation.
```

### ada83.c:25513

```
// Emit_Length_Check - array length mismatch check (RM 4.5.2, 5.2.1).
```

### ada83.c:25514

```
// Checks that source and destination arrays have matching lengths.
```

### ada83.c:25523

```
// Emit_Access_Check - null pointer dereference check (RM 4.1).
```

### ada83.c:25524

```
// Emits before every .ALL dereference and implicit dereference.
```

### ada83.c:25528

```
// If the value is a fat pointer { ptr, ptr }, extract the data pointer first
```

### ada83.c:25531

```
// The value may be a pointer to fat-pointer storage (alloca); load it
```

### ada83.c:25532

```
// to an SSA {ptr,ptr} before extracting the data pointer.
```

### ada83.c:25541

```
// Emit_Discriminant_Check - variant record discriminant check (RM 3.7.1).
```

### ada83.c:25542

```
// Verifies that a discriminant has the expected value before component access.
```

### ada83.c:25550

```
// Return the LLVM fcmp predicate for a comparison operator (from the
```

### ada83.c:25551

```
// operator table). Defaults to "oeq" for non-comparison tokens.
```

### ada83.c:25557

```
// Return the LLVM icmp predicate for a comparison operator (from the
```

### ada83.c:25558

```
// operator table). Selects signed or unsigned predicate based on the
```

### ada83.c:25559

```
// is_unsigned flag; defaults to "eq" for non-comparison tokens.
```

### ada83.c:25567

```
// Check if expression produces boolean (i1) result directly.                                      
```

### ada83.c:25568

```
// Only comparisons and logical operators produce i1 - loaded variables                             
```

### ada83.c:25569

```
// are widened to i64 even for BOOLEAN type.                                                       
```

### ada83.c:25570

```
//                                                                                                  
```

### ada83.c:25573

```
// A user-defined operator (relational/logical overload) returns its result
```

### ada83.c:25574

```
// via a function call at the declared BOOLEAN storage width (i8), NOT an i1.
```

### ada83.c:25584

```
// Membership test
```

### ada83.c:25585

```
// NOT IN (binary) is also a membership test
```

### ada83.c:25590

```
// NOT produces i1 only when its operand does (e.g. NOT of a comparison).
```

### ada83.c:25591

```
// NOT of an i8-stored BOOLEAN value yields i8 (the producer zext's it), so
```

### ada83.c:25592

```
// its i1-ness mirrors the operand's.
```

### ada83.c:25597

```
// Note: Boolean-valued attributes (CONSTRAINED, CALLABLE, TERMINATED) produce i8
```

### ada83.c:25598

```
// (Boolean storage type), not i1. Only comparisons/logical ops produce i1.
```

### ada83.c:25602

```
// Emit a representation conversion. Dispatches on LLVM_Rep fields (kind,
```

### ada83.c:25603

```
// bits, signedness); IR text rendered from the reps only at Emit() boundary.
```

### ada83.c:25604

```
//
```

### ada83.c:25605

```
// is_unsigned: zext instead of sext for integer extensions, fptoui/uitofp for
```

### ada83.c:25606

```
// float↔int conversions. Required for Ada modular types (RM 3.5.4).
```

### ada83.c:25637

```
// float → integer (or other non-ptr); fat handled by fall-through below.
```

### ada83.c:25665

```
// Lose bounds, keep data ptr — used for fat-to-constrained-param passing
```

### ada83.c:25666

```
// and assignment to plain access types.
```

### ada83.c:25670

```
// Pointer to unconstrained-array storage: load fat value, null-guarded.
```

### ada83.c:25716

```
// Signed (default) conversion wrapper.
```

### ada83.c:25721

```
// Bring a value from `from` to `to`, a no-op when they already match. Wraps the
```

### ada83.c:25722

```
// common "convert this operand if its rep differs" idiom so call sites stop
```

### ada83.c:25723

```
// hand-rolling the equality check around Emit_Convert.
```

### ada83.c:25728

```
// Coerce a typed value to the desired LLVM rep. Reads the value's current rep
```

### ada83.c:25729

```
// from .rep (the structured field). If LL_VOID source or already-matching rep,
```

### ada83.c:25730

```
// returns the value unchanged.
```

### ada83.c:25734

```
// Debug-only internal honesty check (like the producer-lie detector):
```

### ada83.c:25735

```
// stays on raw stderr deliberately — it reports a compiler-internal
```

### ada83.c:25736

```
// tracking gap, not a property of the user's program.
```

### ada83.c:25747

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:25748

```
// §13.2.2 Constraint Checks                                                                        
```

### ada83.c:25749

```
//                                                                                                  
```

### ada83.c:25750

```
// Per RM 3.5.4: CONSTRAINT_ERROR raised when value falls outside subtype range.                   
```

### ada83.c:25751

```
// Handles both static (BOUND_INTEGER) and dynamic (BOUND_EXPR) bounds.                            
```

### ada83.c:25752

```
// Generates: if (val < low or val > high) __ada_raise(CONSTRAINT_ERROR)                            
```

### ada83.c:25753

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:25758

```
// If a pre-evaluated cached temp exists, use it directly instead of
```

### ada83.c:25759

```
// re-generating the expression. This prevents side-effectful bound
```

### ada83.c:25760

```
// expressions (function calls) from being evaluated multiple times
```

### ada83.c:25761

```
// during constraint checking. RM 3.2.2(5).
```

### ada83.c:25769

```
// Use i64 if the value exceeds i32 range
```

### ada83.c:25787

```
// BOUND_EXPR: generate the expression and report its actual rep.
```

### ada83.c:25794

```
// Cannot determine bound
```

### ada83.c:25802

```
// RM 3.5.9: Emit a fixed-point bound as its scaled-integer mantissa
```

### ada83.c:25803

```
// (value / SMALL), which is the in-memory representation of a fixed-point
```

### ada83.c:25804

```
// object. A literal real bound folds at compile time; a bound expression is
```

### ada83.c:25805

```
// generated then divided by SMALL when it denotes a real value, or converted
```

### ada83.c:25806

```
// directly when it is already a scaled mantissa. `fix_rep` is the fixed type's
```

### ada83.c:25807

```
// native integer width (i64 unless a narrower one suffices).
```

### ada83.c:25829

```
// An integer bound on a real-typed range holds the mathematical value (a
```

### ada83.c:25830

```
// folded conversion such as FIX (3)), same as FOLD_BOUND and
```

### ada83.c:25831

```
// Bound_Static_Double read it — scale it to mantissa units like any other
```

### ada83.c:25832

```
// real bound.
```

### ada83.c:25857

```
// RM 3.5: General scalar constraint check - dispatches to integer or                               
```

### ada83.c:25858

```
// float path based on the target type's kind. Covers:                                             
```

### ada83.c:25859

```
//   Integer, enumeration, character > icmp slt/sgt on native type (Integer_Arith_Rep)
```

### ada83.c:25860

```
//   Float, fixed, universal_real    > fcmp olt/ogt on double                                       
```

### ada83.c:25861

```
//                                                                                                  
```

### ada83.c:25862

```
// Internal: range-check a value at its actual emitted rep against target's
```

### ada83.c:25863

```
// subtype bounds. Public entry is Emit_Constraint_Check_Val below.
```

### ada83.c:25869

```
// Respect pragma Suppress (Range_Check) on the target type
```

### ada83.c:25876

```
// scaled integer repr
```

### ada83.c:25880

```
// Elide check when static bounds cover the full representation range.                           
```

### ada83.c:25881

```
// Common case: INTEGER element in array aggregates has bounds                                    
```

### ada83.c:25882

```
// -2147483648..2147483647 which any i32 value trivially satisfies.                              
```

### ada83.c:25883

```
// This avoids 3 basic blocks per element - critical for JIT perf.                               
```

### ada83.c:25884

```
//                                                                                                
```

### ada83.c:25899

```
// Also elide if source fits entirely within target bounds
```

### ada83.c:25909

```
// Need either static or dynamic bounds
```

### ada83.c:25921

```
// Float/fixed path - compare at the target's native float type
```

### ada83.c:25926

```
// Widen/narrow a copy to the comparison rep if needed. The original val is
```

### ada83.c:25927

```
// returned to the caller unchanged (matching the integer path) so the
```

### ada83.c:25928

```
// check does not silently re-represent the value — the caller converts to
```

### ada83.c:25929

```
// the target rep when it actually needs to.
```

### ada83.c:25982

```
// val < low?
```

### ada83.c:25988

```
// val > high?
```

### ada83.c:25993

```
// Integer/enum/char/fixed path - all compare as i64.                                            
```

### ada83.c:25994

```
// For TYPE_FIXED, bounds may be BOUND_FLOAT or BOUND_EXPR                                        
```

### ada83.c:25995

```
// (e.g. subtype F is Fix range -101.0..0.0). Scale float                                        
```

### ada83.c:25996

```
// bounds to match the mantissa repr: scaled = bound / small.                                    
```

### ada83.c:25997

```
// BOUND_EXPR bounds are generated as double then fptosi'd.                                      
```

### ada83.c:25998

```
//                                                                                                
```

### ada83.c:26005

```
// Materialize fixed-point bounds in a width that holds the scaled
```

### ada83.c:26006

```
// mantissa, not the object's storage width. A small 'SMALL clause makes
```

### ada83.c:26007

```
// the scaled bound exceed the object rep — CHECK_TYPE range 0.0..8.0 with
```

### ada83.c:26008

```
// 'SMALL 2**-4 scales its upper bound to 128, which does not fit the
```

### ada83.c:26009

```
// 7-bit (i8) object and would wrap to -128, raising CONSTRAINT_ERROR on
```

### ada83.c:26010

```
// an in-range value (cd2a54e). The comparison below widens the value to
```

### ada83.c:26011

```
// match, and the object itself is unaffected.
```

### ada83.c:26019

```
// Frozen scalar constraint (RM 3.2.1): read the bounds elaborated once
```

### ada83.c:26020

```
// into the runtime descriptor, never re-evaluate the constraint here.
```

### ada83.c:26032

```
// Compare at wider of source/target rep. Modular types use unsigned preds.
```

### ada83.c:26036

```
// Use actual rep if provided (may differ from Ada type info).
```

### ada83.c:26048

```
// Widen val and bounds to chk_rep for comparison only. The original val
```

### ada83.c:26049

```
// is returned to the caller unchanged.
```

### ada83.c:26050

```
// Value used ONLY for the comparison widening below.
```

### ada83.c:26054

```
// Fixed-point target with a float value: scale val/SMALL before the
```

### ada83.c:26055

```
// integer comparison so we don't sext a double register.
```

### ada83.c:26065

```
// Default any unset bound reps to Integer arith.
```

### ada83.c:26069

```
// Ensure chk_rep is wide enough for value and both bounds (int-only).
```

### ada83.c:26083

```
// val > high?
```

### ada83.c:26088

```
// raise CONSTRAINT_ERROR
```

### ada83.c:26094

```
// Typed entry point: value carries its true rep, internal never re-widens.
```

### ada83.c:26102

```
// RM 3.5(3): When a subtype_indication includes a range constraint                                 
```

### ada83.c:26103

```
// (e.g. I5 RANGE 0..P), the constraint bounds must lie within the                                  
```

### ada83.c:26104

```
// base type's range. Emit runtime checks for any BOUND_EXPR bound.                               
```

### ada83.c:26105

```
//                                                                                                  
```

### ada83.c:26106

```
// Shared tail of the range-constraint compatibility check (RM 3.5(4)): a
```

### ada83.c:26107

```
// null constraint range (low > high) is always compatible, so the bound
```

### ada83.c:26108

```
// comparisons are skipped for it; a non-null range raises CONSTRAINT_ERROR
```

### ada83.c:26109

```
// when either constraint bound lies outside the mark's range. All four
```

### ada83.c:26110

```
// operands are temps at `rep`.
```

### ada83.c:26139

```
// Fully static: decide here. A null range is always compatible
```

### ada83.c:26140

```
// (RM 3.5(4)); a non-null range with a bound outside the type mark
```

### ada83.c:26141

```
// raises CONSTRAINT_ERROR unconditionally at elaboration.
```

### ada83.c:26152

```
// Try a numeric-static path for real (float/fixed) subtypes: bounds may
```

### ada83.c:26153

```
// arrive as BOUND_FLOAT (pre-evaluated) or BOUND_EXPR that folds to a
```

### ada83.c:26154

```
// compile-time constant. Fold via Eval_Const_Numeric and decide here;
```

### ada83.c:26155

```
// otherwise fall through to the runtime compare below.
```

### ada83.c:26177

```
// null range: RM 3.5(4)
```

### ada83.c:26184

```
// Fixed-point mark with a non-static bound (`0.0 .. 101.0 + FIX1 (...)`):
```

### ada83.c:26185

```
// the mark's bounds are scaled floats while the dynamic constraint bound
```

### ada83.c:26186

```
// evaluates to a mantissa. Normalise all four to the mark's mantissa units
```

### ada83.c:26187

```
// (as the value-range check does) and compare there — the generic integer
```

### ada83.c:26188

```
// path below rejects BOUND_FLOAT marks and would emit no check at all.
```

### ada83.c:26190

```
// A derived fixed type with no explicit constraint (`TYPE FIXED IS NEW
```

### ada83.c:26191

```
// DURATION`) copies the mark's bounds, so the range is a no-op — and those
```

### ada83.c:26192

```
// copies may not be independently evaluable. Skip when the constraint
```

### ada83.c:26193

```
// matches the mark, or when any bound is unset, exactly as the integer path
```

### ada83.c:26194

```
// below treats identical/absent bounds.
```

### ada83.c:26229

```
// Identical bound terms cannot disagree (a constraint built from the
```

### ada83.c:26230

```
// mark's own bounds); skip the no-op comparison.
```

### ada83.c:26240

```
// Emit type mark bounds - widen to iat for comparison
```

### ada83.c:26249

```
// Check each constraint bound against the mark's range.
```

### ada83.c:26250

```
// RM 3.5(4): the bounds must belong to the type mark's range only for a
```

### ada83.c:26251

```
// NON-null range; a null range (low > high) is always legal.
```

### ada83.c:26264

```
// A type declaration's FIRST subtype has no constraint-vs-mark relation
```

### ada83.c:26265

```
// to elaborate: an integer type definition's range picks its base width
```

### ada83.c:26266

```
// at compile time (RM 3.5.4), it is not an RM 3.5(4) range constraint.
```

### ada83.c:26267

```
// Only subtype declarations and subtype indications carry that check.
```

### ada83.c:26277

```
// RM 3.5: for each array dimension whose index constraint was written
```

### ada83.c:26278

```
// `S RANGE L..H`, check L..H against S (the range_mark) — in addition to the
```

### ada83.c:26279

```
// array's index subtype (RM 3.6.1, done separately). Shared by the type,
```

### ada83.c:26280

```
// subtype, and object elaboration sites.
```

### ada83.c:26292

```
// RM 3.5: a discrete_range written `S RANGE L..H` (in a slice or an aggregate
```

### ada83.c:26293

```
// choice) requires L..H to be compatible with the subtype mark S — a non-null
```

### ada83.c:26294

```
// range with a bound outside S raises CONSTRAINT_ERROR. No-op for a bare
```

### ada83.c:26295

```
// `L..H` (no mark) or a range whose mark was not recorded.
```

### ada83.c:26307

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:26308

```
// §13.3 Fat-Pointer Operations
```

### ada83.c:26309

```
//                                                                                                  
```

### ada83.c:26310

```
// fat pointer = { data_ptr, bounds_ptr } = { ptr, ptr }.                                          
```

### ada83.c:26311

```
// Bounds live behind the second pointer as a struct { bt, bt } where                               
```

### ada83.c:26312

```
// bt = native index type (i32 for STRING, i8 for CHARACTER, etc.).                                
```

### ada83.c:26313

```
//                                                                                                  
```

### ada83.c:26314

```
// All helpers take a `bt` (bound type) parameter - the LLVM type string                            
```

### ada83.c:26315

```
// for the bounds (e.g., "i32", "i8"). The bounds struct type is derived                           
```

### ada83.c:26316

```
// from bt via Bounds_Type_For ().                                                                 
```

### ada83.c:26317

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:26319

```
// Build a fat pointer SSA value { data, bounds } from two pointer registers via
```

### ada83.c:26320

```
// the canonical insertvalue pair; returns the result register.
```

### ada83.c:26327

```
// Deep-copy a fat array value's data and bounds onto the secondary stack,
```

### ada83.c:26328

```
// returning a fat pointer that owns independent storage. Used where by-value
```

### ada83.c:26329

```
// semantics require insulation from later changes to the source: an
```

### ada83.c:26330

```
// unconstrained-array function result outliving its callee frame (RM 6.5) and
```

### ada83.c:26331

```
// a generic IN formal-object binding of a dynamically-bounded array, which
```

### ada83.c:26332

```
// RM 12.3 copies at instantiation. The copy lives for the current dynamic
```

### ada83.c:26333

```
// scope (secondary-stack extent), which spans the instance's use in both cases.
```

### ada83.c:26368

```
// Create a fat pointer from data pointer and constant bounds.
```

### ada83.c:26369

```
// bt = bound LLVM type (e.g., "i32").
```

### ada83.c:26370

```
// Allocates bounds struct on stack, stores lo/hi, builds { ptr, ptr }.
```

### ada83.c:26371

```
//
```

### ada83.c:26372

```
// Store `operand` (pre-formatted LLVM value text: an SSA name or a decimal
```

### ada83.c:26373

```
// literal) into field `field_index` (0 = low, 1 = high) of the bounds struct
```

### ada83.c:26374

```
// { bt, bt } at `bounds_ptr`.
```

### ada83.c:26382

```
// Store the low/high bound temps (already at bt) into the bounds struct at
```

### ada83.c:26383

```
// `bounds_ptr`.
```

### ada83.c:26395

```
// Allocate bounds struct { bt, bt } on stack - native type
```

### ada83.c:26402

```
// Widen a value to INTEGER width (Integer_Arith_Rep) for use at
```

### ada83.c:26403

```
// LLVM intrinsic boundaries (memcpy length, alloca size, malloc size, RTS ABI).                   
```

### ada83.c:26404

```
// Uses sext for signed types, zext for unsigned (modular) types.                                  
```

### ada83.c:26405

```
// No-op if from_type is already at INTEGER width.                                                 
```

### ada83.c:26406

```
//                                                                                                  
```

### ada83.c:26415

```
// Extend value to i64 for C-level operations (memcpy, alloca, malloc, sec_stack_alloc).
```

### ada83.c:26416

```
// These always require i64 on 64-bit targets regardless of Ada INTEGER width.
```

### ada83.c:26425

```
// All fat-pointer producers now return SSA { ptr, ptr } values directly;
```

### ada83.c:26426

```
// nothing wraps a fat value in an alloca anymore. This is a pure pass-through
```

### ada83.c:26427

```
// kept as documentation at sites that historically needed the unwrap.
```

### ada83.c:26432

```
// Raw fat-pointer field extraction from an SSA fat value: field 0 is the data
```

### ada83.c:26433

```
// pointer, field 1 is the bounds pointer. Both return the result register.
```

### ada83.c:26443

```
// Load a fat pointer { data, bounds } from the memory at pointer register
```

### ada83.c:26444

```
// `ptr_reg`; returns the loaded SSA fat value's register.
```

### ada83.c:26450

```
// Load a scalar of representation `rep` from `base + byte_offset` via an
```

### ada83.c:26451

```
// i8-indexed GEP; returns the loaded register. For discriminant/component
```

### ada83.c:26452

```
// field reads where no per-field debug annotation is wanted.
```

### ada83.c:26459

```
// Address of byte `byte_offset` within local symbol `sym`'s storage (i8-indexed
```

### ada83.c:26460

```
// GEP); returns the result register.
```

### ada83.c:26468

```
// Load a scalar of representation `rep` from byte `byte_offset` within local
```

### ada83.c:26469

```
// symbol `sym`'s storage; returns the loaded register.
```

### ada83.c:26476

```
// Allocate a scalar slot of representation `rep`, store `val` into it, and
```

### ada83.c:26477

```
// return the slot pointer register.
```

### ada83.c:26484

```
// Freeze a value into a module global and return its id. A per-function alloca
```

### ada83.c:26485

```
// (Emit_Alloca_Store) cannot be recorded on a SHARED type object: the type may
```

### ada83.c:26486

```
// be elaborated in one function and its constraint checked in another, where
```

### ada83.c:26487

```
// the alloca's temp id names an undefined value (an LLVM dominance error). A
```

### ada83.c:26488

```
// module global is reachable from every function, so a frozen discriminant
```

### ada83.c:26489

```
// constraint value (RM 3.7.1 evaluate-once) round-trips across function
```

### ada83.c:26490

```
// boundaries. Read it back with Emit_Load_Preeval_Global.
```

### ada83.c:26504

```
// memcpy `size_reg` bytes from src register to dst register (both pointers).
```

### ada83.c:26510

```
// Shared core of the symbol-destination memcpy emitters: `size_operand` is
```

### ada83.c:26511

```
// the already-formatted i64 byte count (a decimal constant or an SSA name).
```

### ada83.c:26520

```
// memcpy `bytes` constant bytes from value register `src` into symbol `dst`'s
```

### ada83.c:26521

```
// storage. `comment`, when non-NULL, is appended as a trailing IR comment.
```

### ada83.c:26528

```
// Register-sized variant of Emit_Memcpy_To_Symbol: the byte count comes from a
```

### ada83.c:26529

```
// runtime SSA value rather than a compile-time constant. Used when the size is
```

### ada83.c:26530

```
// only known at elaboration (e.g. Emit_Type_Byte_Size for a dynamic record).
```

### ada83.c:26537

```
// Extract data pointer from fat pointer.
```

### ada83.c:26538

```
// bt parameter retained for API compatibility but unused.
```

### ada83.c:26545

```
// Extract bound at field_index from fat pointer's bounds struct.                                  
```

### ada83.c:26546

```
// Parametric over index: 0 = low, 1 = high. Handles both SSA and                                  
```

### ada83.c:26547

```
// alloca-style fat pointers transparently via Fat_Ptr_As_Value.                                   
```

### ada83.c:26548

```
//                                                                                                  
```

### ada83.c:26560

```
// Extract low bound for dimension `dim` from fat pointer.                                         
```

### ada83.c:26561

```
// Bounds are stored as flat pairs: [low0, high0, low1, high1, ...].                               
```

### ada83.c:26562

```
// Uses flat GEP with index 2*dim for low, 2*dim+1 for high.                                       
```

### ada83.c:26563

```
//                                                                                                  
```

### ada83.c:26564

```
// Load the bound at flat slot index `slot` from a fat pointer's bounds block
```

### ada83.c:26565

```
// (dimension d's low is at 2d, high at 2d+1).
```

### ada83.c:26579

```
// Compute length for dimension `dim` from fat pointer: high - low + 1
```

### ada83.c:26580

```
// with null-array clamping (RM 3.6.2).
```

### ada83.c:26588

```
// Allocate a multi-dimension bounds block on the stack.                                           
```

### ada83.c:26589

```
// Layout: [low0, high0, low1, high1, ...] as flat array of 2*ndims values.                        
```

### ada83.c:26590

```
// bounds_lo/bounds_hi are arrays of ndims temp IDs.                                               
```

### ada83.c:26591

```
// Returns the alloca temp ID (ptr to the bounds memory).                                          
```

### ada83.c:26592

```
//                                                                                                  
```

### ada83.c:26593

```
// Load each dimension's runtime low/high bound from the @__rt_type_<id>_lo/hi
```

### ada83.c:26594

```
// globals into blo[]/bhi[], converting from the i64 descriptor rep to `work`.
```

### ada83.c:26606

```
// Store a dimension's low/high bounds into a flat bounds block at [dim*2] and
```

### ada83.c:26607

```
// [dim*2+1], at element width `bt`.
```

### ada83.c:26621

```
// Bounds temps arrive at src_rep (the rep the caller built them at); coerce
```

### ada83.c:26622

```
// to the bounds block's element type bt (e.g. i32 -> i8 for a small-range
```

### ada83.c:26623

```
// index like `RANGE -3..3`) so the stores match and aren't `store i8 %i32`.
```

### ada83.c:26631

```
// Create a fat pointer with multi-dim bounds.
```

### ada83.c:26632

```
// bounds_lo/bounds_hi are arrays of ndims temp IDs.
```

### ada83.c:26640

```
// Build a fat pointer { data, bounds } aliasing a constrained array stored
```

### ada83.c:26641

```
// inline at data_addr, deriving its bounds from array_type. Handles both
```

### ada83.c:26642

```
// rt-elaborated dynamic bounds (recorded in @__rt_type_<id>_lo/hi globals)
```

### ada83.c:26643

```
// and static bounds. Used to pass a constrained array actual (e.g. a record
```

### ada83.c:26644

```
// component) to an unconstrained-array formal, which expects a fat pointer.
```

### ada83.c:26645

```
//
```

### ada83.c:26654

```
// Emit_Fat_Pointer_MultiDim / Emit_Alloc_Bounds_MultiDim expect the bound
```

### ada83.c:26655

```
// temps at the integer working type (Integer_Arith_Rep) and coerce them to
```

### ada83.c:26656

```
// bt themselves, so build the bounds at that rep here.
```

### ada83.c:26659

```
// RT-elaborated bounds live in type globals (one lo/hi pair per dimension).
```

### ada83.c:26665

```
// Static bounds: materialise each dimension's lo/hi as a literal.
```

### ada83.c:26675

```
// Linearize the index arguments of an indexed component into a zero-based flat
```

### ada83.c:26676

```
// element index (RM 3.6.1), applying the RM 4.1.1 index checks. Static bounds
```

### ada83.c:26677

```
// are read from array_type; dynamic bounds come from the fat pointer `dyn_fat`
```

### ada83.c:26678

```
// (low/high value temps `dyn_low`/`dyn_high` at `dyn_bt`) when has_dynamic_low.
```

### ada83.c:26679

```
// Shared by the value path (Generate_Apply) and the lvalue path
```

### ada83.c:26680

```
// (Generate_Composite_Address), which once carried byte-identical copies.
```

### ada83.c:26690

```
// Multi-dimension: flat = Σ (idx[d] - lo[d]) · (Π lengths of inner dims).
```

### ada83.c:26699

```
// Subtract the dimension's low bound.
```

### ada83.c:26712

```
// Multiply by the stride: the product of the inner dimensions' lengths.
```

### ada83.c:26747

```
// Single dimension: (idx - lo), index-checked.
```

### ada83.c:26778

```
// Create a fat pointer from data pointer and dynamic bounds (temp IDs).
```

### ada83.c:26779

```
// bt = bound LLVM type of the input temps (already in native type).                               
```

### ada83.c:26780

```
// Allocates bounds struct on stack, stores in native bt, builds { ptr, ptr }.                     
```

### ada83.c:26781

```
// Allocate a bounds struct { bt, bt } on the stack and store lo/hi into it.                       
```

### ada83.c:26782

```
// Returns the alloca temp ID (a ptr to the bounds struct).                                        
```

### ada83.c:26783

```
//                                                                                                  
```

### ada83.c:26797

```
// Like Emit_Alloc_Bounds_Struct but uses malloc instead of alloca.                                
```

### ada83.c:26798

```
// Necessary for allocator (NEW) results where the fat pointer crosses                              
```

### ada83.c:26799

```
// function boundaries and the bounds must outlive the creating function.                          
```

### ada83.c:26800

```
//                                                                                                  
```

### ada83.c:26803

```
// Compute size of bounds struct: 2 * sizeof (bound_type)
```

### ada83.c:26812

```
// Like Emit_Fat_Pointer_Dynamic but heap-allocates the bounds struct.
```

### ada83.c:26813

```
// Used for allocator (NEW) results that must survive across function returns.
```

### ada83.c:26821

```
// Compute length from fat pointer bounds: high - low + 1
```

### ada83.c:26822

```
// Returns temp ID holding the length in native bound type (bt).
```

### ada83.c:26825

```
// A null array's length is 0, never a negative/wrapped value (RM 3.6.2). The
```

### ada83.c:26826

```
// clamp lives in Emit_Length_From_Bounds; sizing an alloca / memcpy by an
```

### ada83.c:26827

```
// unclamped high-low+1 of a null range (e.g. (-3 .. -5 => 'A')) would request
```

### ada83.c:26828

```
// ~2^64 bytes and crash.
```

### ada83.c:26834

```
// Total element count of a fat pointer: the product of every dimension's
```

### ada83.c:26835

```
// length (each clamped at 0 for a null dimension). A multidimensional array
```

### ada83.c:26836

```
// is null whenever any one dimension is null, so this product is 0 exactly
```

### ada83.c:26837

```
// when the array has no components.
```

### ada83.c:26849

```
// Emit length from two temp IDs (raw bounds, not from fat pointer):                                
```

### ada83.c:26850

```
//   result = max(high - low + 1, 0)                                                                
```

### ada83.c:26851

```
// Null ranges (high < low) yield 0, not a negative / wrapped value.                               
```

### ada83.c:26852

```
// Lighter than Emit_Fat_Pointer_Length when you already have low/high.                            
```

### ada83.c:26853

```
//                                                                                                  
```

### ada83.c:26856

```
// high - low overflows the bound width when a null range inverts across
```

### ada83.c:26857

```
// most of the type: 3 .. INTEGER'FIRST must yield 0, not a wrapped
```

### ada83.c:26858

```
// 2^31-scale count that a memcpy then trusts (c95085c). Compute in a
```

### ada83.c:26859

```
// wider register, clamp, and narrow back.
```

### ada83.c:26877

```
// Total byte size (i64) of the data behind a fat-pointer value:
```

### ada83.c:26878

```
//   (product over dims of length_d) * element_size, clamped to >= 0.
```

### ada83.c:26879

```
// Per-dimension lengths come out at the index/bound width (i8/i32/i64 per
```

### ada83.c:26880

```
// RM 3.6.1) and are coerced up to i64 before multiplying, so a CHARACTER- or
```

### ada83.c:26881

```
// LONG_INTEGER-indexed array does not mismatch the size arithmetic. The i64
```

### ada83.c:26882

```
// result is what alloca/memcpy consume, so no trailing sext is needed.
```

### ada83.c:26890

```
// A fat-stored element (dynamic-bound array / string component) occupies a
```

### ada83.c:26891

```
// { ptr, ptr } slot, not its zero flat size.
```

### ada83.c:26897

```
// Product of per-dimension lengths, accumulated in i64.
```

### ada83.c:26903

```
// sext from index width
```

### ada83.c:26911

```
// Clamp negative (null-range) sizes to 0.
```

### ada83.c:26917

```
// Emit_Fat_To_Array_Memcpy: Copy data from a fat pointer source to a destination.                 
```

### ada83.c:26918

```
// Extracts data pointer, computes byte length from bounds * element size, emits memcpy.           
```

### ada83.c:26919

```
// Used when assigning fat pointer (unconstrained string/array) to constrained component.          
```

### ada83.c:26920

```
//                                                                                                  
```

### ada83.c:26940

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:26941

```
// §13.2.3 Consolidated Check and Length Helpers                                                                     
```

### ada83.c:26942

```
//                                                                                                  
```

### ada83.c:26943

```
// Length computation with null-array clamping (RM 3.6.2), static integer constant emission,        
```

### ada83.c:26944

```
// memcmp-based array comparison, and conditional check + raise sequences.                         
```

### ada83.c:26945

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:26947

```
// Emit length from bounds with null-array clamping (RM 3.6.2):                                     
```

### ada83.c:26948

```
//   result = (low > high) ? 0 : (high - low + 1)                                                   
```

### ada83.c:26949

```
// Handles the Ada rule that arrays with first > last have length 0.                               
```

### ada83.c:26950

```
//                                                                                                  
```

### ada83.c:26960

```
// Emit a static integer constant using the add idiom:                                              
```

### ada83.c:26961

```
//   %tN = add <type> 0, <value>                                                                    
```

### ada83.c:26962

```
// This is the standard LLVM IR pattern for loading constants.                                     
```

### ada83.c:26963

```
//                                                                                                  
```

### ada83.c:26969

```
// As Emit_Static_Int, but annotates the emitted line with a trailing IR comment
```

### ada83.c:26970

```
// (`  ; <comment>`). Used where the constant-load carries a human-readable tag.
```

### ada83.c:26976

```
// Emit `%t = icmp <op> <type> %lhs, %rhs` and return the i1 result.
```

### ada83.c:26982

```
// Emit `%t = icmp <op> <type> %lhs, <integer literal>` and return the i1 result.
```

### ada83.c:26988

```
// Emit `%t = icmp <op> ptr %ptr, null` and return the i1 result.
```

### ada83.c:26994

```
// Truth test of a stored BOOLEAN value: BOOLEAN's representation is i8
```

### ada83.c:26995

```
// (RM 3.5.3), so a branch condition needs `icmp ne <bool> %value, 0` to
```

### ada83.c:26996

```
// recover the transient i1. The boolean type string is derived from
```

### ada83.c:26997

```
// Standard.BOOLEAN's rep, never hardcoded.
```

### ada83.c:27002

```
// RM 4.8(6) / 6.4.1: when an access value flows to a context with a constrained
```

### ada83.c:27003

```
// access subtype, the designated object's discriminants must match the
```

### ada83.c:27004

```
// subtype's constraint. Extracts the data pointer (fat-aware), null-checks
```

### ada83.c:27005

```
// (null is always allowed), and raises CONSTRAINT_ERROR on any mismatch.
```

### ada83.c:27008

```
// a private view reps as its access completion
```

### ada83.c:27014

```
// Constrained array designated subtype (e.g. ACCESS ARR (2..5)): the
```

### ada83.c:27015

```
// designated object's bounds must equal the subtype constraint (RM 4.8(6)).
```

### ada83.c:27016

```
// Only meaningful when the access carries runtime bounds (fat) — a thin
```

### ada83.c:27017

```
// access-to-constrained already has the static bounds by construction.
```

### ada83.c:27060

```
// Emit `%t = icmp <op> ptr %lhs, %rhs` and return the i1 result.
```

### ada83.c:27066

```
// Emit `%t = fcmp <op> <type> %lhs, 0.0` and return the i1 result.
```

### ada83.c:27072

```
// Emit `%t = fcmp <op> <type> %lhs, %rhs` and return the i1 result.
```

### ada83.c:27078

```
// RM 3.5.9: scale a real value to/from a fixed-point type's stored mantissa.
```

### ada83.c:27079

```
// Emits `val / SMALL` (divide=true, real -> mantissa) or `val * SMALL`
```

### ada83.c:27080

```
// (divide=false, mantissa -> real) at float rep `frep`, materializing SMALL as
```

### ada83.c:27081

```
// a constant. Single source for the scaling that was open-coded ~17 times.
```

### ada83.c:27091

```
// Emit `%t = and i1 %a, %b` and return the i1 result.
```

### ada83.c:27097

```
// Emit `lo <= val <= hi` as a single i1. Joins the three operands to a common
```

### ada83.c:27098

```
// rep (float join, or the widest int incl. the arithmetic width), coerces each,
```

### ada83.c:27099

```
// and emits the two ordered comparisons + AND. Single source for the in-range
```

### ada83.c:27100

```
// shape duplicated across membership tests and case-choice range matching.
```

### ada83.c:27101

```
// `is_unsigned` selects unsigned integer predicates (modular types).
```

### ada83.c:27119

```
// Emit `%t = xor i1 %a, 1` (boolean negation) and return the i1 result.
```

### ada83.c:27125

```
// Emit `%t = add i1 0, <0|1>  ; <comment>` (a materialized i1 constant).
```

### ada83.c:27131

```
// Emit the merge phi for a short-circuit AND THEN / OR ELSE:
```

### ada83.c:27132

```
//   %t = phi i1 [ <first_const>, %<prefix>_check<cl> ], [ %<reg>, %<prefix>_merge<ml> ]
```

### ada83.c:27133

```
// prefix is "Landthen" or "Lorelse"; first_const is "false" or "true".
```

### ada83.c:27141

```
// Emit memcmp and return i1 equality result (true if arrays equal).
```

### ada83.c:27142

```
// Handles both static (literal size) and dynamic (temp size) cases.
```

### ada83.c:27155

```
// internal helper: i1 (callers do `and i1` or wrap)
```

### ada83.c:27158

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:27159

```
// §13.4 Bound and Length Emission (body)                                                                 
```

### ada83.c:27160

```
//                                                                                                  
```

### ada83.c:27161

```
// Ada types carry bounds as either:                                                                
```

### ada83.c:27162

```
//   - BOUND_INTEGER: compile-time constant                                                         
```

### ada83.c:27163

```
//   - BOUND_EXPR: runtime expression (dynamic subtypes)                                            
```

### ada83.c:27164

```
//   - BOUND_FLOAT: floating-point constant (for float types)                                       
```

### ada83.c:27165

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:27167

```
// Bound_Temps: Holds emitted temps for a dimension's low and high bounds.
```

### ada83.c:27168

```
// The bound_type field records the LLVM type used (e.g., "i32", "i64").
```

### ada83.c:27170

```
// Emit a single bound (low or high) from a Type_Bound structure.
```

### ada83.c:27171

```
// Handles all three bound kinds: INTEGER, EXPR, FLOAT.
```

### ada83.c:27173

```
// A bound already evaluated into a register (a memoized constraint
```

### ada83.c:27174

```
// expression, or an applicable index constraint published as a runtime
```

### ada83.c:27175

```
// value) is emitted directly — its register is authoritative regardless of
```

### ada83.c:27176

```
// the source kind, and re-deriving it would re-run any side effects.
```

### ada83.c:27188

```
// RM 3.3.1: Use cached temp if pre-evaluated, to avoid re-calling
```

### ada83.c:27189

```
// side-effectful expressions (function calls) during aggregate gen.
```

### ada83.c:27207

```
// Fallback for unset bounds
```

### ada83.c:27210

```
// One dimension bound of a constrained-but-dynamic array, at `target_rep`
```

### ada83.c:27211

```
// (RM 3.3.1 / 3.6.1). A static bound is materialised directly; a bound
```

### ada83.c:27212

```
// expression reuses the subtype elaboration's cached temp so a side-effectful
```

### ada83.c:27213

```
// expression is not re-evaluated, and is coerced from its real (possibly
```

### ada83.c:27214

```
// narrow) integer type - float-represented bound expressions are left at
```

### ada83.c:27215

```
// their own representation. A dimension with no bound of its own falls back
```

### ada83.c:27216

```
// to its index type's bound, and failing that to the 1 .. 0 defaults.
```

### ada83.c:27224

```
// RM 3.3.1: reuse the cached temp from subtype elaboration to avoid
```

### ada83.c:27225

```
// re-evaluating side-effectful expressions.
```

### ada83.c:27229

```
// Both freshly-generated and cached temps may be at the bound's narrow
```

### ada83.c:27230

```
// type (e.g. SMALL RANGE 1..100 as i8). Coerce from the value's REAL
```

### ada83.c:27231

```
// type to target_rep so downstream sub/add see matching widths;
```

### ada83.c:27232

```
// assuming i32 here would no-op an i8 bound and leave it.
```

### ada83.c:27239

```
// Derive from the index type's bound (e.g. BOOLEAN'FIRST=0, NI'FIRST=-3).
```

### ada83.c:27255

```
// Emit_Bounds: Extract both bounds from a Type_Info for a specific dimension.                     
```

### ada83.c:27256

```
// For arrays: uses indices[dim].low_bound/high_bound                                               
```

### ada83.c:27257

```
// For scalars: uses type->low_bound/high_bound (dim ignored)                                       
```

### ada83.c:27258

```
// Returns Bound_Temps with both temps and the bound type string.                                  
```

### ada83.c:27259

```
//                                                                                                  
```

### ada83.c:27266

```
// For array types, use the appropriate dimension's bounds
```

### ada83.c:27271

```
// Determine bound type from index type if available
```

### ada83.c:27278

```
// Use array bound LLVM type if this is an array
```

### ada83.c:27288

```
// Emit_Bounds_From_Fat: Extract bounds from a fat pointer value.                                  
```

### ada83.c:27289

```
// Fat pointers have structure { ptr data, ptr bounds } where bounds                                
```

### ada83.c:27290

```
// points to a { low, high } pair. Uses existing Emit_Fat_Pointer_Low/High.                        
```

### ada83.c:27291

```
//                                                                                                  
```

### ada83.c:27301

```
// Emit_Bounds_From_Fat_Dim: Extract bounds from fat pointer for specific dimension.
```

### ada83.c:27302

```
// Supports multi-dimensional unconstrained arrays (RM 3.6.1).
```

### ada83.c:27312

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:27313

```
// §13.2.4 Conditional Check + Raise                                                                
```

### ada83.c:27314

```
//                                                                                                  
```

### ada83.c:27315

```
// Ada runtime checks follow a common pattern:                                                      
```

### ada83.c:27316

```
//   1. Emit comparison (icmp/fcmp)                                                                 
```

### ada83.c:27317

```
//   2. Branch to raise block if check fails                                                        
```

### ada83.c:27318

```
//   3. Raise CONSTRAINT_ERROR (or other exception)                                                 
```

### ada83.c:27319

```
//   4. Continue to next instruction                                                                
```

### ada83.c:27320

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:27322

```
// Emit_Check_With_Raise: Conditional branch + raise + continue.                                   
```

### ada83.c:27323

```
// If raise_on_true is true: raises when cond is true, continues when false.                       
```

### ada83.c:27324

```
// If raise_on_true is false: raises when cond is false, continues when true.                      
```

### ada83.c:27325

```
//                                                                                                  
```

### ada83.c:27333

```
// conditional branch terminates block
```

### ada83.c:27344

```
// Raise CONSTRAINT_ERROR when `condition` holds, else fall through to a
```

### ada83.c:27345

```
// fresh continuation block. Mints the continuation label BEFORE the raise
```

### ada83.c:27346

```
// label — the complement of Emit_Check_With_Raise, which mints raise-then-
```

### ada83.c:27347

```
// continue. Each minting order keeps its own helper; do not fold them, or
```

### ada83.c:27348

```
// every emitted label in one family renumbers.
```

### ada83.c:27360

```
// RM 3.9: a call to a subprogram whose body has not yet been elaborated raises
```

### ada83.c:27361

```
// PROGRAM_ERROR. The callee's elaboration flag is a local alloca that is false
```

### ada83.c:27362

```
// until the body declaration elaborates; the check loads it and raises when it
```

### ada83.c:27363

```
// is still false. The flag lives in its owning function's frame, so the check
```

### ada83.c:27364

```
// is only emitted while generating that same function.
```

### ada83.c:27365

```
// Load `callee`'s body-elaborated flag (RM 3.9), or 0 when the symbol
```

### ada83.c:27366

```
// carries no flag. A call back across a SEPARATE boundary reads the parent
```

### ada83.c:27367

```
// unit's flag; the parent's module owns the definition, so declare it
```

### ada83.c:27368

```
// external here (once).
```

### ada83.c:27388

```
// RM 4.6: an array type conversion checks that any constraint on the component
```

### ada83.c:27389

```
// subtype is the same for the operand and target array types, raising
```

### ada83.c:27390

```
// CONSTRAINT_ERROR when they differ. The component base types are already known
```

### ada83.c:27391

```
// to match (a legality rule), so only their subtype constraints can differ -
```

### ada83.c:27392

```
// scalar ranges, real accuracy, nested array bounds, or record discriminants.
```

### ada83.c:27393

```
// Dynamic constraints are compared at run time; a purely static mismatch
```

### ada83.c:27394

```
// (real accuracy) lowers to an unconditional raise.
```

### ada83.c:27399

```
// Scalar discrete component: the range constraint must match.
```

### ada83.c:27413

```
// Real component: the accuracy constraint (DIGITS / DELTA) is static.
```

### ada83.c:27427

```
// Access component: the constraint lives on the designated subtype.
```

### ada83.c:27434

```
// Array component: each index bound must match, then the element subtype.
```

### ada83.c:27453

```
// Record component: each discriminant constraint must match.
```

### ada83.c:27477

```
// Emit_Range_Check_With_Raise: Checks val in [lo_val, hi_val], raises if not.
```

### ada83.c:27478

```
// Emits two icmp + br with shared raise label for efficiency.
```

### ada83.c:27497

```
// Emit a type bound as a temp: handles BOUND_INTEGER, BOUND_EXPR, BOUND_FLOAT.
```

### ada83.c:27498

```
// Returns temp ID with value at specified type width.
```

### ada83.c:27499

```
// Emit a value of BOUND at representation TY. The single upstream point every
```

### ada83.c:27500

```
// constraint-checking site — allocator bounds, index/discriminant checks, array
```

### ada83.c:27501

```
// aggregate frames — reaches to ask "what does this bound evaluate to right
```

### ada83.c:27502

```
// now?". For a dynamic (BOUND_EXPR) bound the answer is a side-effectful
```

### ada83.c:27503

```
// expression, so we consult `cached_temp` first: object elaboration populates
```

### ada83.c:27504

```
// it once per name, and every later reader reuses that value instead of
```

### ada83.c:27505

```
// re-running the expression (RM 3.7.1 / RM 4.8(6)). A float target that misses
```

### ada83.c:27506

```
// the cache still spills the value to the float rep the callee wants.
```

### ada83.c:27533

```
// Copy data from fat pointer to a named destination.
```

### ada83.c:27534

```
// Emits: memcpy (dst, src_data, length). bt = bound type.
```

### ada83.c:27539

```
// The fat-pointer length counts elements; memcpy counts bytes. Scale by the
```

### ada83.c:27540

```
// destination component's byte width, else a FLOAT or record array copies
```

### ada83.c:27541

```
// only its first 1/element_size of the data.
```

### ada83.c:27556

```
// Load fat pointer from a symbol's storage. bt = bound type (unused for load).
```

### ada83.c:27571

```
// Load fat pointer from a temp pointer (%%t<N>). bt = bound type (unused for load).
```

### ada83.c:27577

```
// Store a fat pointer value into a symbol's storage. bt = bound type (unused).
```

### ada83.c:27586

```
// Store fat pointer fields (data ptr, low, high) into a symbol using GEP+store.                   
```

### ada83.c:27587

```
// Allocates bounds struct, stores lo/hi, then stores { ptr, ptr } fields.                         
```

### ada83.c:27588

```
// bt = bound type.                                                                                
```

### ada83.c:27589

```
//                                                                                                  
```

### ada83.c:27595

```
// Store data ptr (field 0 of { ptr, ptr })
```

### ada83.c:27601

```
// Store bounds ptr (field 1 of { ptr, ptr })
```

### ada83.c:27608

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:27609

```
// §13.2.5 Additional Helpers                                                                       
```

### ada83.c:27610

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:27613

```
// Emit minimum of two values:  result = (left < right) ? left : right
```

### ada83.c:27620

```
// Structure returned by Emit_Exception_Handler_Setup
```

### ada83.c:27622

```
// Setup exception handler with setjmp. Emits alloca, push_handler, setjmp, branch.                
```

### ada83.c:27623

```
// Caller must emit labels and code for normal/handler paths.                                      
```

### ada83.c:27624

```
// Returns structure with handler_frame, jmp_buf temps, and label IDs.                             
```

### ada83.c:27625

```
//                                                                                                  
```

### ada83.c:27629

```
// Capture the thread's current master record. When the handler catches,
```

### ada83.c:27630

```
// every master entered after this point was abandoned by the longjmp;
```

### ada83.c:27631

```
// the unwind completes them (RM 9.4: a master left by an exception still
```

### ada83.c:27632

```
// waits for its dependents) before the handler code runs.
```

### ada83.c:27648

```
// conditional branch terminates block
```

### ada83.c:27650

```
// Handler entry: complete the abandoned masters, then the caller's
```

### ada83.c:27651

```
// handler code at handler_label.
```

### ada83.c:27659

```
// Emit code to get current exception identity (after exception raised).
```

### ada83.c:27660

```
// Returns temp holding i64 exception ID.
```

### ada83.c:27666

```
// Generate exception handler dispatch code for a list of handlers.                                
```

### ada83.c:27667

```
// exc_id = temp holding current exception identity (i64)                                           
```

### ada83.c:27668

```
// end_label = label to branch to after handler completes                                           
```

### ada83.c:27669

```
// handlers = list of NK_EXCEPTION_HANDLER nodes                                                    
```

### ada83.c:27670

```
//                                                                                                  
```

### ada83.c:27685

```
// Check each exception name in the handler
```

### ada83.c:27718

```
// Handler body
```

### ada83.c:27724

```
// No handler matched. In a subprogram/block/package the exception
```

### ada83.c:27725

```
// propagates (reraise); in a task body (RM 11.4.1) an unmatched exception
```

### ada83.c:27726

```
// simply completes the task, so fall through to the caller's end label.
```

### ada83.c:27739

```
// RM 11.4: a package body's elaboration — task ACTIVATION first (RM 9.3(3):
```

### ada83.c:27740

```
// activation happens after `begin`, so an activation PROGRAM_ERROR such as
```

### ada83.c:27741

```
// a not-yet-elaborated task body, RM 3.9/c93007a, is handleable), then the
```

### ada83.c:27742

```
// initialization statements — runs under the body's own exception handlers.
```

### ada83.c:27743

```
// With no handlers it runs bare; otherwise the normal path pops the handler
```

### ada83.c:27744

```
// and jumps to the end and the handler path dispatches (and re-raises on no
```

### ada83.c:27745

```
// match, since a package elaboration is not a task). Every package-body
```

### ada83.c:27746

```
// elaboration path shares this — nested, library-level, and SEPARATE
```

### ada83.c:27747

```
// subunit — so an exception raised while elaborating is caught the same way
```

### ada83.c:27748

```
// in each (an access-before-elaboration PROGRAM_ERROR in c39006c).
```

### ada83.c:27749

```
// `activate_spec` (may be NULL) supplies a spec's recorded activation range;
```

### ada83.c:27750

```
// `activate_from` (UINT32_MAX = none) activates everything pending since it.
```

### ada83.c:27781

```
// Emit lexicographic array comparison for unconstrained arrays.
```

### ada83.c:27782

```
// Compares common prefix via memcmp, then compares lengths.                                       
```

### ada83.c:27783

```
// Returns i32 result: <0 if left<right, 0 if equal, >0 if left>right.                             
```

### ada83.c:27784

```
// left_ptr/right_ptr are fat pointers, bt = bound type.                                           
```

### ada83.c:27785

```
//                                                                                                  
```

### ada83.c:27789

```
// Extract bounds and data pointers
```

### ada83.c:27799

```
// min_len = min(left_len, right_len)
```

### ada83.c:27802

```
// byte_size = min_len * elem_size
```

### ada83.c:27806

```
// prefix_cmp = memcmp (left_data, right_data, byte_size)
```

### ada83.c:27809

```
// If prefix differs, return prefix_cmp; otherwise return len_diff. The
```

### ada83.c:27810

```
// length difference is signed (shorter array sorts first) and must match
```

### ada83.c:27811

```
// memcmp's i32 result rep; convert from the bound rep, which may be
```

### ada83.c:27812

```
// narrower, equal, or wider than i32.
```

### ada83.c:27820

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:27821

```
// §13.3.1 Named-SSA Fat Pointer Helpers for RTS Functions                                          
```

### ada83.c:27822

```
//                                                                                                  
```

### ada83.c:27823

```
// RTS function bodies (emitted as LLVM IR text with named registers like                           
```

### ada83.c:27824

```
// %fat1, %data, etc.) cannot use the temp-ID helpers above. These helpers                         
```

### ada83.c:27825

```
// emit the same patterns but with caller-supplied named SSA prefixes.                             
```

### ada83.c:27826

```
//                                                                                                  
```

### ada83.c:27827

```
// With { ptr, ptr } layout, bounds must be allocated and filled first,                             
```

### ada83.c:27828

```
// then the fat pointer is built as { data_ptr, bounds_ptr }.                                      
```

### ada83.c:27829

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:27831

```
// Build a fat pointer { ptr, ptr } from named SSA values.                                         
```

### ada83.c:27832

```
// Allocates a bounds struct on the secondary stack (not alloca) so the                             
```

### ada83.c:27833

```
// bounds pointer remains valid after the function returns. This is critical                       
```

### ada83.c:27834

```
// for RTS functions that return fat pointers.                                                     
```

### ada83.c:27835

```
// Emits: %<prefix>_bnd = call ptr @__ada_sec_stack_alloc(i64 N)                                    
```

### ada83.c:27836

```
//        %<prefix>_lo_gep, %<prefix>_hi_gep = GEP + store                                          
```

### ada83.c:27837

```
//        %<prefix>1 = insertvalue { ptr, ptr } undef, <data_expr>, 0                               
```

### ada83.c:27838

```
//        %<prefix>2 = insertvalue { ptr, ptr } %<prefix>1, ptr %<prefix>_bnd, 1                    
```

### ada83.c:27839

```
// The result value is %<prefix>2.                                                                 
```

### ada83.c:27840

```
//                                                                                                  
```

### ada83.c:27846

```
// Allocate bounds struct { bt, bt } on secondary stack (survives function return)
```

### ada83.c:27850

```
// Store low bound in native bt
```

### ada83.c:27855

```
// Store high bound in native bt
```

### ada83.c:27860

```
// Build { ptr, ptr } via insertvalue
```

### ada83.c:27866

```
// Extract data pointer, low bound, and high bound from a named SSA fat pointer.                   
```

### ada83.c:27867

```
// Extracts bounds_ptr (field 1), then GEP+load for low and high.                                  
```

### ada83.c:27868

```
// src_name:       name of the source fat pointer SSA value  (e.g., "str" for %str)                 
```

### ada83.c:27869

```
// data_name:      name for extracted data pointer            (e.g., "data")                        
```

### ada83.c:27870

```
// low_name:       name for extracted low bound               (e.g., "low32")                       
```

### ada83.c:27871

```
// high_name:      name for extracted high bound              (e.g., "high32")                      
```

### ada83.c:27872

```
// bt:             bound type string                           (e.g., "i32")                        
```

### ada83.c:27873

```
//                                                                                                  
```

### ada83.c:27879

```
// Extract data pointer (field 0)
```

### ada83.c:27882

```
// Extract bounds pointer (field 1)
```

### ada83.c:27885

```
// GEP + load low bound in native bt
```

### ada83.c:27890

```
// GEP + load high bound in native bt
```

### ada83.c:27896

```
// Emit a named-SSA width conversion: `%dst = <op> <from> %src to <to>`.
```

### ada83.c:27897

```
// If the two representations are equal, emits a no-op copy (add 0) instead,
```

### ada83.c:27898

```
// so the destination name is always defined.
```

### ada83.c:27909

```
// Emit a named-SSA widen from bound type to INTEGER width for intrinsic/RTS use.
```

### ada83.c:27910

```
// src_name:  name of the source SSA value (in bt)
```

### ada83.c:27911

```
// dst_name:  name for the widened value
```

### ada83.c:27912

```
// bt:        the bound type
```

### ada83.c:27913

```
// Uses sext (signed extension).
```

### ada83.c:27919

```
// Emit a named-SSA narrow from INTEGER width to bound type after intrinsic/RTS.
```

### ada83.c:27920

```
// src_name:  name of the source SSA value (INTEGER width)
```

### ada83.c:27921

```
// dst_name:  name for the narrowed bt value
```

### ada83.c:27922

```
// bt:        the target bound type
```

### ada83.c:27928

```
// Build a null fat pointer value: { ptr null, ptr null }.
```

### ada83.c:27929

```
// Returns temp ID of the constructed value. bt = bound type (unused).
```

### ada83.c:27939

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:27940

```
// §13.6 Expression Code Generation                                                                 
```

### ada83.c:27941

```
//                                                                                                  
```

### ada83.c:27942

```
// Returns the LLVM SSA value ID holding the expression result.                                    
```

### ada83.c:27943

```
// Every expression yields a value, and in SSA form every value has one definition.                
```

### ada83.c:27944

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:27946

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:27947

```
// Generate_Lvalue - Return a ptr to the storage location of an lvalue.                            
```

### ada83.c:27948

```
//                                                                                                  
```

### ada83.c:27949

```
// Handles:                                                                                         
```

### ada83.c:27950

```
//   NK_IDENTIFIER   - symbol storage (local, global, or uplevel)                                   
```

### ada83.c:27951

```
//   NK_SELECTED     - record field offset from base address                                        
```

### ada83.c:27952

```
//   NK_APPLY        - array element address (indexed component)                                    
```

### ada83.c:27953

```
//   NK_UNARY_OP/ALL - .ALL dereference (load pointer value)                                        
```

### ada83.c:27954

```
//                                                                                                  
```

### ada83.c:27955

```
// Returns a temp holding ptr to the storage. Caller can then:                                     
```

### ada83.c:27956

```
//   - load from it  (expression context)                                                           
```

### ada83.c:27957

```
//   - store to it   (assignment context)                                                           
```

### ada83.c:27958

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:27964

```
// NK_IDENTIFIER: return ptr to the symbol's storage
```

### ada83.c:27969

```
// For RENAMES: redirect to renamed object. A rename exported from a
```

### ada83.c:27970

```
// generic instance denotes an entity of THAT instance: activate the
```

### ada83.c:27971

```
// instance context so formal-object references inside the renamed
```

### ada83.c:27972

```
// expression substitute their actuals even when the reference comes
```

### ada83.c:27973

```
// from outside the instance.
```

### ada83.c:27974

```
// A USE-visible alias denotes the original entity — follow it so a
```

### ada83.c:27975

```
// rename (including an RM 13.5 overlay) attached to the original fires.
```

### ada83.c:27982

```
// RM 4.1.3: a parameterless FUNCTION name as a composite prefix is an
```

### ada83.c:27983

```
// implicit call; the lvalue (for component selection on the result)
```

### ada83.c:27984

```
// is the call's result pointer, not the function symbol's storage.
```

### ada83.c:27992

```
// NK_SELECTED: record field access - compute base + field offset
```

### ada83.c:27996

```
// Explicit .ALL dereference: load pointer value
```

### ada83.c:28000

```
// The pointer IS the lvalue target
```

### ada83.c:28006

```
// Determine effective record type (handle implicit dereference)
```

### ada83.c:28015

```
// Resolve generic formal types to actual types (RM 12.3)
```

### ada83.c:28018

```
// Find field offset and component index
```

### ada83.c:28028

```
// Get base pointer
```

### ada83.c:28037

```
// GEP to field offset - use runtime offset for dynamic records
```

### ada83.c:28041

```
// Package-qualified name: return lvalue (address) of the resolved symbol.
```

### ada83.c:28042

```
// E.g. PACK1.ARG1 where PACK1 is a package and ARG1 is a variable inside it.
```

### ada83.c:28043

```
// We must NOT fall through to Generate_Expression which would load the value.
```

### ada83.c:28044

```
//
```

### ada83.c:28046

```
// A renamed entity (including an RM 13.5 address-clause overlay)
```

### ada83.c:28047

```
// denotes its target, exactly as the NK_IDENTIFIER arm redirects.
```

### ada83.c:28054

```
// RM 4.1.3: a package-qualified parameterless FUNCTION name (PKG.F)
```

### ada83.c:28055

```
// as a composite prefix is an implicit call — same as the
```

### ada83.c:28056

```
// NK_IDENTIFIER arm; a function symbol has no storage to address.
```

### ada83.c:28069

```
// NK_UNARY_OP with TK_ALL: .ALL dereference - load the pointer value.
```

### ada83.c:28070

```
// For access-to-fat-storage types, the access value is itself a fat
```

### ada83.c:28071

```
// pointer { data, bounds }; extract the data slot so callers (memcpy,
```

### ada83.c:28072

```
// store, indexing) get a plain ptr to the dereferenced storage.
```

### ada83.c:28076

```
// RM 4.1: null .ALL -> CE
```

### ada83.c:28084

```
// NK_APPLY: array indexed component - compute element address
```

### ada83.c:28096

```
// Get array base pointer
```

### ada83.c:28104

```
// Access-to-array: load the access value to get array data pointer.
```

### ada83.c:28105

```
// If the access is stored as a fat pointer (designated type is or
```

### ada83.c:28106

```
// derives from an unconstrained array), extract the data slot so the
```

### ada83.c:28107

```
// GEP and bounds check operate on a plain ptr.
```

### ada83.c:28126

```
// Unconstrained array: load fat pointer, extract data ptr
```

### ada83.c:28141

```
// Non-symbol prefix (selected component, function call, ...)
```

### ada83.c:28142

```
// of a dynamic-bounds array: the expression yields a fat
```

### ada83.c:28143

```
// pointer value; unpack data and bounds as in the symbol case.
```

### ada83.c:28154

```
// A statically-bounded prefix can still ARRIVE as a fat value —
```

### ada83.c:28155

```
// a slice expression (N3(2..5)(2) := 8) yields { data, bounds }
```

### ada83.c:28156

```
// whose data points at the slice's first element and whose bounds
```

### ada83.c:28157

```
// are the slice's own. Unpack it exactly as the dynamic case does;
```

### ada83.c:28158

```
// treating the fat struct as the data pointer indexes garbage.
```

### ada83.c:28173

```
// Slice as lvalue (RM 4.1.2): a slice A(L..H) denotes the sub-array
```

### ada83.c:28174

```
// starting at index L. Its address is the base offset by (L - array's
```

### ada83.c:28175

```
// low bound) * element_size; the length is (H - L + 1), captured in the
```

### ada83.c:28176

```
// slice's own type. Callers that need the length come at it through the
```

### ada83.c:28177

```
// rename/subtype rather than through this pointer.
```

### ada83.c:28185

```
// The offset is (L - base_index), where base_index is the index the
```

### ada83.c:28186

```
// `base` pointer already denotes. When the prefix is ITSELF a slice
```

### ada83.c:28187

```
// (a nested slice A(M..N)(L..H)), base points at the prefix slice's
```

### ada83.c:28188

```
// first element, whose index is the prefix slice's OWN low bound M —
```

### ada83.c:28189

```
// not the underlying array's low bound (which the slice's result type
```

### ada83.c:28190

```
// still carries). Using the type's low bound would double-count the
```

### ada83.c:28191

```
// outer slide and land at the wrong element.
```

### ada83.c:28217

```
// Generate index expression (supports multi-dimensional arrays)
```

### ada83.c:28224

```
// Compute element address. Composite elements (records, constrained
```

### ada83.c:28225

```
// arrays of fixed size) are stored inline as N-byte blobs; their
```

### ada83.c:28226

```
// Type_To_Rep is LL_PTR which would give the wrong stride. Index
```

### ada83.c:28227

```
// by byte offset for those.
```

### ada83.c:28231

```
// An array whose element is a dynamically-sized record: the element
```

### ada83.c:28232

```
// stride is the element's runtime byte size (RM 3.6.1), not a static
```

### ada83.c:28233

```
// type size. Address = base + flat_idx * runtime_element_size.
```

### ada83.c:28264

```
// Fallback: treat expression result as pointer (for composite types
```

### ada83.c:28265

```
// that Generate_Expression already returns as ptr)
```

### ada83.c:28269

```
// Generate code to evaluate a type bound at runtime.
```

### ada83.c:28270

```
// Returns the temp register containing the bound value.
```

### ada83.c:28282

```
// "double" here is intentional - %e produces a double-precision LLVM IR
```

### ada83.c:28283

```
// literal, matching the fptosi source type.
```

### ada83.c:28308

```
// The exact value (Big_Integer overflow path included).
```

### ada83.c:28316

```
// Emit at the expression's native type width. A UNIVERSAL literal has no
```

### ada83.c:28317

```
// representation of its own — pick the narrowest standard width that
```

### ada83.c:28318

```
// holds the VALUE, so the immediate never wraps (an i32 emission of
```

### ada83.c:28319

```
// 2**60 silently truncates); consumers convert as their context needs.
```

### ada83.c:28333

```
// Use IEEE 754 hex encoding for full precision.                                                 
```

### ada83.c:28334

```
// This preserves all 53 bits of mantissa (vs %f which loses precision).                         
```

### ada83.c:28335

```
// The double value was computed from Big_Real during parsing.                                   
```

### ada83.c:28336

```
//                                                                                                
```

### ada83.c:28343

```
// RM 3.6.1(2): ARRAY (1 .. 3) has index TYPE plain INTEGER — the range
```

### ada83.c:28344

```
// lives only in Index_Info. An index type that is absent, boundless, or
```

### ada83.c:28345

```
// spans all of INTEGER therefore does not constrain; a consumer needing
```

### ada83.c:28346

```
// the index SUBTYPE must fall back to the dimension's own bounds.
```

### ada83.c:28358

```
// Allocate string constant
```

### ada83.c:28362

```
// Component type of the string literal. Normally the predefined CHARACTER,
```

### ada83.c:28363

```
// but RM 3.5.2/4.2 admit any character type as the component — including a
```

### ada83.c:28364

```
// user-defined enumeration whose values include character literals
```

### ada83.c:28365

```
// (e.g. TYPE T IS ('A', 'B'); TYPE S IS ARRAY (POSITIVE RANGE <>) OF T).
```

### ada83.c:28366

```
// For such a component the stored element value is the enumeration code, not
```

### ada83.c:28367

```
// the source character's ASCII byte; and each character must belong to the
```

### ada83.c:28368

```
// component subtype or CONSTRAINT_ERROR is raised at run time (RM 4.2).
```

### ada83.c:28378

```
// Compute the stored code for each character and whether any character falls
```

### ada83.c:28379

```
// outside the component subtype's bounds (a statically-known violation, but
```

### ada83.c:28380

```
// one RM 4.2 requires be reported as a run-time CONSTRAINT_ERROR since the
```

### ada83.c:28381

```
// enclosing context may catch it).
```

### ada83.c:28385

```
// Component-subtype bounds. When both fold to a static value the range check
```

### ada83.c:28386

```
// is decided at compile time; a component subtype whose range is only known
```

### ada83.c:28387

```
// at run time (a range over variables) defers to a run-time check below.
```

### ada83.c:28398

```
// Position of the character literal 'ch' among the component enum's
```

### ada83.c:28399

```
// literals. A character absent from the base type cannot be a value of
```

### ada83.c:28400

```
// it at all, so treat it as out of range (RM 4.2).
```

### ada83.c:28404

```
// predefined CHARACTER: position == ASCII code
```

### ada83.c:28409

```
// Stored representation: the enum code when a representation clause remaps
```

### ada83.c:28410

```
// it, otherwise the position itself (identity for CHARACTER).
```

### ada83.c:28417

```
// Emit the backing global. When every element fits an i8 (the universal case
```

### ada83.c:28418

```
// for character types) use a byte string; otherwise emit a typed aggregate
```

### ada83.c:28419

```
// so a wide enumeration code is stored at its natural width.
```

### ada83.c:28441

```
// Get pointer to string data
```

### ada83.c:28444

```
// A character outside the component subtype makes the aggregate value
```

### ada83.c:28445

```
// illegal; raise now (RM 4.2). The raise terminates the block, so return an
```

### ada83.c:28446

```
// (unreachable) placeholder fat pointer immediately rather than emitting the
```

### ada83.c:28447

```
// bounds computation into a dead block.
```

### ada83.c:28454

```
// A component subtype with run-time bounds: the literal's element positions
```

### ada83.c:28455

```
// are static, so it suffices to check the range's extremes against the
```

### ada83.c:28456

```
// evaluated bounds and raise CONSTRAINT_ERROR conditionally (RM 4.2). The
```

### ada83.c:28457

```
// check emits a conditional branch and continues in the in-range block.
```

### ada83.c:28474

```
// Return fat pointer with Ada STRING bounds.
```

### ada83.c:28475

```
// Default is 1..length (RM 4.2(9) - POSITIVE'FIRST).
```

### ada83.c:28476

```
// When the applicable index constraint specifies different bounds
```

### ada83.c:28477

```
// (e.g. STRING (3..5)), use those bounds instead.
```

### ada83.c:28478

```
//
```

### ada83.c:28483

```
// A parenthesized string literal is a general expression, not a literal
```

### ada83.c:28484

```
// directly in an applicable-index-constraint context (RM 4.3.2 (a)-(c)), so
```

### ada83.c:28485

```
// it ignores any constraint on its context type and takes the index
```

### ada83.c:28486

```
// subtype's 'FIRST as its lower bound. The index subtype is the named index
```

### ada83.c:28487

```
// subtype when the context type has one (BASE(3..5) keeps SF1's 'FIRST = 2),
```

### ada83.c:28488

```
// and otherwise the constraint itself, since a directly constrained array
```

### ada83.c:28489

```
// type is its own index subtype (ARRAY(3..6) keeps 3). The resulting value
```

### ada83.c:28490

```
// is checked against the target subtype by the enclosing context, which is
```

### ada83.c:28491

```
// where a bound mismatch is reported.
```

### ada83.c:28523

```
// A constrained subtype with dynamic bounds (e.g. built with IDENT_INT):
```

### ada83.c:28524

```
// evaluate both bounds of the constraint at run time.
```

### ada83.c:28534

```
// Unconstrained applicable type: the lower bound is the index subtype's
```

### ada83.c:28535

```
// 'FIRST and the string's length fixes the upper bound (RM 4.2(4)).
```

### ada83.c:28543

```
// RM 4.2(4): the bounds belong to the base type of the index subtype;
```

### ada83.c:28544

```
// a null string at INTEGER'FIRST computes hi = INTEGER'FIRST - 1 which
```

### ada83.c:28545

```
// is outside the base type and must raise CONSTRAINT_ERROR (c42005a).
```

### ada83.c:28553

```
// Dynamic index-subtype bound (e.g. a range built with IDENT_INT):
```

### ada83.c:28554

```
// evaluate the lower bound at run time; the length fixes the upper bound.
```

### ada83.c:28558

```
// RM 4.2(4): a null string yields hi = lo - 1; when lo is the index
```

### ada83.c:28559

```
// base type's 'FIRST that computation underflows and must raise
```

### ada83.c:28560

```
// CONSTRAINT_ERROR (c42005a). Use signed subtract-with-overflow so
```

### ada83.c:28561

```
// the check fires even when the base-type range isn't statically
```

### ada83.c:28562

```
// known here.
```

### ada83.c:28583

```
// Position of an enumeration literal by name (RM 3.5.1), or 0 if absent.
```

### ada83.c:28591

```
// The nearest enclosing enumeration type carrying a representation clause, so
```

### ada83.c:28592

```
// a subtype or derived type inherits its parent's codes (RM 13.3). NULL when
```

### ada83.c:28593

```
// the mapping is the identity (code == position) — the common case.
```

### ada83.c:28606

```
// Convert a stored code to its position. Codes are strictly increasing with
```

### ada83.c:28607

```
// position (RM 13.3), so the position is the count of codes the value reaches:
```

### ada83.c:28608

```
// pos = sum over i>=1 of (code >= code[i]). Branchless; the identity map when
```

### ada83.c:28609

```
// there is no representation clause.
```

### ada83.c:28624

```
// Convert a position to its stored code via a select chain over the (small)
```

### ada83.c:28625

```
// literal set. The identity map when there is no representation clause.
```

### ada83.c:28646

```
// For RENAMES: redirect to the renamed object. A rename exported from
```

### ada83.c:28647

```
// a generic instance denotes an entity of THAT instance: activate the
```

### ada83.c:28648

```
// instance context so formal-object references inside the renamed
```

### ada83.c:28649

```
// expression substitute their actuals even when the reference comes
```

### ada83.c:28650

```
// from outside the instance. Subprogram renames are NOT objects — their
```

### ada83.c:28651

```
// renamed_object holds a Symbol, not an expression (see
```

### ada83.c:28652

```
// Resolve_Subprogram_Rename) — and fall through to the SYMBOL_FUNCTION
```

### ada83.c:28653

```
// parameterless-call case below, which peels the rename chain.
```

### ada83.c:28654

```
// A USE-visible alias denotes the original entity — follow it so a
```

### ada83.c:28655

```
// rename (including an RM 13.5 overlay) attached to the original fires.
```

### ada83.c:28659

```
// RM 8.5: read through the address captured at the rename's elaboration
```

### ada83.c:28660

```
// so a later change to the renamed expression's sub-parts (e.g. an access
```

### ada83.c:28661

```
// reassigned after `V renames P.ALL`) does not affect what V denotes.
```

### ada83.c:28664

```
// Read the captured object exactly as the ordinary variable path does
```

### ada83.c:28665

```
// (the SYMBOL_VARIABLE case below): a by-reference composite's address IS
```

### ada83.c:28666

```
// its value, so return it without a load — loading would read the array's
```

### ada83.c:28667

```
// or record's first word as a spurious pointer (a segfault when that value
```

### ada83.c:28668

```
// is then used as an address, e.g. an array rename passed by reference as
```

### ada83.c:28669

```
// an entry parameter). Only a by-value scalar or access is loaded through
```

### ada83.c:28670

```
// the captured address.
```

### ada83.c:28685

```
// Resolve generic formal types to their actuals for correct LLVM type sizing.
```

### ada83.c:28686

```
// E.g. generic formal T (<>) mapped to COLOR (i8) - must load as i8, not i32.
```

### ada83.c:28689

```
// Discriminant with aggregate-local temp: load from temp alloca
```

### ada83.c:28698

```
// Check if symbol is stored as a fat pointer (dynamic/unconstrained arrays).          
```

### ada83.c:28699

```
// This flag is set at symbol creation time when bounds were dynamic.                        
```

### ada83.c:28700

```
// Must take priority over Type_Is_Constrained_Array which may change                         
```

### ada83.c:28701

```
// after bounds are resolved to static values.                                               
```

### ada83.c:28702

```
//                                                                                            
```

### ada83.c:28709

```
// Constrained arrays and records are composite types stored as flat
```

### ada83.c:28710

```
// allocas: the alloca address IS the value, so no load is needed.
```

### ada83.c:28715

```
// Fall back to the expression's resolved type if the symbol's type
```

### ada83.c:28716

```
// field was never populated (some symbol-creation paths skip it).
```

### ada83.c:28721

```
// No widening - value stays at native type width.                                           
```

### ada83.c:28722

```
// The value's rep is the native type, and Emit_Convert
```

### ada83.c:28723

```
// handles any needed conversions at use sites.                                              
```

### ada83.c:28724

```
//                                                                                            
```

### ada83.c:28727

```
// Find position in enumeration
```

### ada83.c:28731

```
// Enumeration literal or constant
```

### ada83.c:28738

```
// Boolean literal: TRUE=1, FALSE=0 - native type width
```

### ada83.c:28745

```
// Character literal - native type width
```

### ada83.c:28751

```
// Constant stored as fat pointer (dynamic bounds at declaration)
```

### ada83.c:28757

```
// Constrained array constant - return pointer to data.
```

### ada83.c:28758

```
// Fat pointer wrapping is handled at call sites when needed.
```

### ada83.c:28763

```
// Named number (constant without explicit type) - evaluate initializer                       
```

### ada83.c:28764

```
// Named numbers in Ada are compile-time constants with no storage.                          
```

### ada83.c:28765

```
// Per RM 3.2.2: "A named number provides a name for a numeric value                          
```

### ada83.c:28766

```
// known at compile time."                                                                    
```

### ada83.c:28767

```
//                                                                                            
```

### ada83.c:28771

```
// Try compile-time evaluation first. Named numbers are                                    
```

### ada83.c:28772

```
// UNIVERSAL_INTEGER / UNIVERSAL_REAL and may involve                                       
```

### ada83.c:28773

```
// expressions like 2**31 that overflow i32 at runtime.                                    
```

### ada83.c:28774

```
//                                                                                          
```

### ada83.c:28792

```
// Predefined constant (e.g. ASCII.NUL) with value in frame_offset
```

### ada83.c:28804

```
// Record constant: return pointer to storage (no load)
```

### ada83.c:28807

```
// Typed constant: try compile-time evaluation first.                                        
```

### ada83.c:28808

```
// Constants from WITHed packages (e.g. TEXT_IO.UNBOUNDED)                                    
```

### ada83.c:28809

```
// may not have global definitions if the package body was                                    
```

### ada83.c:28810

```
// separately compiled. Inlining avoids unresolved symbols.                                 
```

### ada83.c:28811

```
//                                                                                            
```

### ada83.c:28819

```
// A fixed-point constant inlines as its scaled mantissa, not the
```

### ada83.c:28820

```
// real value (DAY_SECONDS = 86_400.0 is mantissa 8_640_000_000).
```

### ada83.c:28834

```
// Every sub-path above emits in the constant's own type rep; return
```

### ada83.c:28835

```
// that, not node->type (a shadowed/base name can disagree).
```

### ada83.c:28844

```
// Parameterless function call: F is syntactically an identifier                                
```

### ada83.c:28845

```
// but semantically a function call with zero arguments.                                       
```

### ada83.c:28846

```
// Generic formal subprogram substitution: if this is a formal subprogram                       
```

### ada83.c:28847

```
// inside a generic instantiation, substitute with actual.                                     
```

### ada83.c:28848

```
//                                                                                              
```

### ada83.c:28851

```
// A rename (including a generic formal-subprogram binding) denotes
```

### ada83.c:28852

```
// its target: peel the chain before dispatching the call. A derived
```

### ada83.c:28853

```
// operation (RM 3.4) forwards to the parent's body (its ultimate
```

### ada83.c:28854

```
// operation), as the inherited subprogram has no code of its own.
```

### ada83.c:28858

```
// A literal used as a function (RM 6.1: an enumeration literal is a
```

### ada83.c:28859

```
// parameterless function). A literal of a non-enumeration character
```

### ada83.c:28860

```
// type (predefined CHARACTER, BOOLEAN) carries its position directly
```

### ada83.c:28861

```
// in frame_offset.
```

### ada83.c:28871

```
// Generate actual function call.
```

### ada83.c:28872

```
// If the function has parameters (all with defaults, per RM 6.4.2),
```

### ada83.c:28873

```
// synthesize an NK_APPLY(actual, [defaults]) and route through
```

### ada83.c:28874

```
// Generate_Apply. All params must have defaults; otherwise it's a
```

### ada83.c:28875

```
// signature mismatch and we leave it to the fallback (which will
```

### ada83.c:28876

```
// emit a wrong 0-arg call but at least won't crash here).
```

### ada83.c:28879

```
// RM 6.4.2: default expressions belong to the VIEW the call names —
```

### ada83.c:28880

```
// for a generic formal subprogram, the formal's own declared
```

### ada83.c:28881

```
// defaults, never the bound actual's (RM 12.1.3). The peeled symbol
```

### ada83.c:28882

```
// supplies the body; the named symbol supplies the defaults. Only a
```

### ada83.c:28883

```
// RESOLVED default can be emitted — a formal default the template
```

### ada83.c:28884

```
// never resolved (it references formal types re-typed per instance)
```

### ada83.c:28885

```
// falls back to the actual's profile rather than reaching codegen
```

### ada83.c:28886

```
// untyped. Resolving those at template time is the remaining gap.
```

### ada83.c:28904

```
// synthesized; bypasses Resolve_Apply
```

### ada83.c:28913

```
// Fall through to original emit (zero-arg call) — may produce
```

### ada83.c:28914

```
// wrong IR but maintains existing behavior for malformed calls.
```

### ada83.c:28919

```
// A parameterless call whose result builds in place (§13.10) must go
```

### ada83.c:28920

```
// through Generate_Apply — the direct emit below knows nothing of the
```

### ada83.c:28921

```
// BIP protocol and would call with the wrong signature. Use the same
```

### ada83.c:28922

```
// predicate the body emitter uses so the two sides never disagree.
```

### ada83.c:28931

```
// RM 3.9: access-before-elaboration check on a parameterless call.
```

### ada83.c:28932

```
// Check the subprogram actually invoked (a generic formal FF resolves
```

### ada83.c:28933

```
// to its actual INIT_2, which owns the body and the flag; c39006b), not
```

### ada83.c:28934

```
// the formal name.
```

### ada83.c:28952

```
// Return the rep actually emitted (the callee's return type), not
```

### ada83.c:28953

```
// node->type — a shadowed name (e.g. a user function named INTEGER)
```

### ada83.c:28954

```
// can leave node->type pointing at an unrelated predefined type.
```

### ada83.c:28963

```
// RM 9.1: within its own body, a task unit's name denotes the task
```

### ada83.c:28964

```
// currently executing that body — the TCB the body received.
```

### ada83.c:28965

```
// 'CALLABLE, 'TERMINATED, membership tests, and ABORT all consume it
```

### ada83.c:28966

```
// as a task handle (c91004b: `IF NOT TT1'CALLABLE` inside TT1's body).
```

### ada83.c:28975

```
// The value's type is the identifier's resolved type (matches what each
```

### ada83.c:28976

```
// branch's load/call emitted); fall back to INTEGER only when untyped.
```

### ada83.c:28980

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:28981

```
// §13.6.1 Implicit Operators for Composite Types                                                   
```

### ada83.c:28982

```
//                                                                                                  
```

### ada83.c:28983

```
// Ada requires equality operators for all non-limited types. For composite                         
```

### ada83.c:28984

```
// types (records, arrays), equality is defined component-wise.                                    
```

### ada83.c:28985

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:28987

```
// Generate equality comparison for record types (component-by-component)
```

### ada83.c:28993

```
// Empty record or invalid - always equal
```

### ada83.c:28999

```
// Accumulate equality in memory, not an SSA chain: a variant component is
```

### ada83.c:29000

```
// compared under a runtime guard that branches around it when the
```

### ada83.c:29001

```
// discriminant selects a different variant, so the AND must survive across
```

### ada83.c:29002

```
// basic blocks. Comparing an inactive variant's components would read the
```

### ada83.c:29003

```
// unused tail of the record and spuriously report inequality (RM 4.5.2).
```

### ada83.c:29007

```
// The discriminant governing the variant part; loaded once from the left
```

### ada83.c:29008

```
// record (the discriminants are compared as ordinary components, so an
```

### ada83.c:29009

```
// unequal discriminant already makes the records unequal).
```

### ada83.c:29026

```
// Get pointers to components — via the offset primitive so a component that
```

### ada83.c:29027

```
// sits after a capless one is compared at its runtime offset, not a stale
```

### ada83.c:29028

```
// static one (both operands share the subtype, hence the same layout).
```

### ada83.c:29032

```
// Compare component - handle arrays/strings specially
```

### ada83.c:29036

```
// A component whose footprint depends on discriminant values (a capless
```

### ada83.c:29037

```
// array, a nested dynamically-sized record, or an array of such records) is
```

### ada83.c:29038

```
// stored inline with no padding, so byte equality over its exact runtime
```

### ada83.c:29039

```
// footprint IS value equality — and it is the only way to compare a nested
```

### ada83.c:29040

```
// dynamic layout without re-deriving every sub-offset here.
```

### ada83.c:29062

```
// Unconstrained array/string - load fat pointer values from storage
```

### ada83.c:29068

```
// Constrained array with dynamic bounds (discriminant-dependent).                             
```

### ada83.c:29069

```
// Data stored inline; compute runtime byte size from discriminant.                            
```

### ada83.c:29070

```
// For ARRAY (LOW..DISC) OF ELEM: size = max(0, disc - low + 1) * elem_size                     
```

### ada83.c:29071

```
//                                                                                              
```

### ada83.c:29080

```
// Find the discriminant that controls the high bound.
```

### ada83.c:29081

```
// Match the bound expression's symbol against the record's discriminant components.
```

### ada83.c:29099

```
// Load discriminant from left record and compute memcmp size
```

### ada83.c:29110

```
// Clamp to 0 for null arrays
```

### ada83.c:29119

```
// Constrained array with static bounds - use array equality directly on pointers
```

### ada83.c:29123

```
// Nested record - recurse
```

### ada83.c:29127

```
// ACCESS to unconstrained array — RM 4.5.2 equality of access values
```

### ada83.c:29128

```
// is identity of the designated object (or both NULL). Only the data
```

### ada83.c:29129

```
// pointer distinguishes them; bounds are metadata attached to whatever
```

### ada83.c:29130

```
// the data pointer designates, so ignore them. Loading bounds off a
```

### ada83.c:29131

```
// NULL access would fault (the bounds ptr would be dereferenced).
```

### ada83.c:29140

```
// Scalar type - load and compare
```

### ada83.c:29153

```
// AND this component's result into the accumulator (inside the guard, so a
```

### ada83.c:29154

```
// skipped variant component leaves the accumulator untouched = equal).
```

### ada83.c:29164

```
// RM 4.5.2: two composite values are equal iff their matching components are
```

### ada83.c:29165

```
// equal. A flat memcmp is only sound when every byte of the storage is part of
```

### ada83.c:29166

```
// some live component — false for a record with a variant part or a
```

### ada83.c:29167

```
// discriminant-dependent (dynamic-size) component, whose inactive tail holds
```

### ada83.c:29168

```
// don't-care bytes. Such a type (or any array of such) must be compared
```

### ada83.c:29169

```
// component-by-component instead.
```

### ada83.c:29175

```
// A variant part makes the inactive variant's storage don't-care bytes, so a
```

### ada83.c:29176

```
// byte compare is unsound — the record must be compared component-wise.
```

### ada83.c:29178

```
// sum of component bit footprints
```

### ada83.c:29182

```
// A discriminant-dependent array/string component (bounds named by a
```

### ada83.c:29183

```
// discriminant) occupies less than its reserved maximum, so the tail is
```

### ada83.c:29184

```
// don't-care and a byte compare is unsound.
```

### ada83.c:29190

```
// Padding (RM 4.5.2): if the components do not densely tile the record's
```

### ada83.c:29191

```
// footprint — a discriminant-only NULL record whose sole discriminant is
```

### ada83.c:29192

```
// narrower than the record's alignment, or any field followed by an
```

### ada83.c:29193

```
// alignment gap — a flat memcmp would compare tail/gap bits that belong to
```

### ada83.c:29194

```
// no component and may hold arbitrary values (an aggregate leaves them
```

### ada83.c:29195

```
// unset where a default-initialised object memsets them to zero). Compare
```

### ada83.c:29196

```
// component-wise instead. Bit-level accounting keeps genuinely bit-packed
```

### ada83.c:29197

```
// records (fields laid end-to-end sub-byte) on the fast memcmp path.
```

### ada83.c:29203

```
// Generate equality comparison for constrained array types (element-by-element)
```

### ada83.c:29213

```
// For constrained arrays with static bounds, use memcmp over the static
```

### ada83.c:29214

```
// element count — but only when the operands are plain data pointers. When
```

### ada83.c:29215

```
// either operand is a fat pointer the actual length is a runtime quantity
```

### ada83.c:29216

```
// that need not equal the nominal type's count (a slice carries its base
```

### ada83.c:29217

```
// array's constrained type yet spans fewer elements), so the length-based
```

### ada83.c:29218

```
// path below must govern. Dynamic-bounds constrained arrays likewise use
```

### ada83.c:29219

```
// fat pointers at runtime and fall through.
```

### ada83.c:29220

```
//
```

### ada83.c:29239

```
// A flat memcmp would compare the don't-care tail of any element that is a
```

### ada83.c:29240

```
// record with a variant part or a discriminant-dependent component (RM
```

### ada83.c:29241

```
// 4.5.2). Compare such elements one at a time through the record/array
```

### ada83.c:29242

```
// equality that respects the active discriminant; AND the results in memory
```

### ada83.c:29243

```
// since each record comparison may span basic blocks.
```

### ada83.c:29266

```
// Use memcmp for byte-by-byte comparison
```

### ada83.c:29270

```
// Unconstrained array equality (per RM 4.5.2):                                                   
```

### ada83.c:29271

```
// Two arrays are equal iff they have the same length and matching components.                   
```

### ada83.c:29272

```
// Bounds themselves need not match-only length and content.                                     
```

### ada83.c:29273

```
//                                                                                                
```

### ada83.c:29274

```
// For fat pointers: compare lengths, then data if lengths match.                                
```

### ada83.c:29275

```
// Use select instead of phi to avoid block label complications.                                 
```

### ada83.c:29276

```
//                                                                                                
```

### ada83.c:29278

```
// For unconstrained arrays, left_ptr/right_ptr are fat pointer VALUES                            
```

### ada83.c:29279

```
// (not pointers to storage). All callers must ensure they pass loaded                           
```

### ada83.c:29280

```
// fat pointer values: { ptr, { bound, bound } }.                                                
```

### ada83.c:29281

```
//                                                                                                
```

### ada83.c:29283

```
// Extract bounds and compute lengths for ALL dimensions (RM 4.5.2).                             
```

### ada83.c:29284

```
// For multidimensional arrays, each dimension's length must match and                            
```

### ada83.c:29285

```
// the total byte count is the product of all dimension lengths × elem_size.                     
```

### ada83.c:29286

```
//                                                                                                
```

### ada83.c:29292

```
// Compare lengths for each dimension and accumulate each side's element count
```

### ada83.c:29293

```
// AND of all dimension length comparisons
```

### ada83.c:29294

```
// product of the left array's dimension lengths
```

### ada83.c:29295

```
// product of the right array's dimension lengths
```

### ada83.c:29320

```
// Extract data pointers
```

### ada83.c:29324

```
// Compute byte size for memcmp: total_elems * scalar_elem_size
```

### ada83.c:29330

```
// Call memcmp
```

### ada83.c:29333

```
// RM 4.5.2: two null arrays of the same type are always equal, whatever their
```

### ada83.c:29334

```
// bounds. Otherwise equality needs every dimension's length to match and the
```

### ada83.c:29335

```
// components to be equal.
```

### ada83.c:29344

```
// Generate the address of a composite type expression (for equality comparison)
```

### ada83.c:29347

```
// Generate the ADDRESS of an lvalue (identifier, selected, indexed)
```

### ada83.c:29351

```
// A rename redirects to the renamed entity's storage, under the
```

### ada83.c:29352

```
// owning instance's context when the rename came from one.
```

### ada83.c:29353

```
// A USE-visible alias denotes the original entity — follow it.
```

### ada83.c:29360

```
// RM 4.1.3: a parameterless FUNCTION name as a composite prefix is
```

### ada83.c:29361

```
// an implicit call; the composite's address is the call's result
```

### ada83.c:29362

```
// pointer, not the function symbol's (nonexistent) storage.
```

### ada83.c:29371

```
// RM 4.1.3: a package-qualified parameterless FUNCTION name (PKG.F) used
```

### ada83.c:29372

```
// as a composite prefix is an implicit call — the composite's address is
```

### ada83.c:29373

```
// the call's result pointer, mirroring the direct-name case above.
```

### ada83.c:29384

```
// Field access: Rec.Field - compute address of field
```

### ada83.c:29388

```
// Handle implicit dereference: U.A where U is access-to-record.
```

### ada83.c:29389

```
// First load the pointer, then compute field address.
```

### ada83.c:29400

```
// Load the access value (pointer to record). Evaluating the prefix
```

### ada83.c:29401

```
// as an expression handles every prefix form uniformly: a plain
```

### ada83.c:29402

```
// access variable (one load), a rename or USE-visible alias, and a
```

### ada83.c:29403

```
// parameterless function returning an access value (implicit call,
```

### ada83.c:29404

```
// RM 4.1.3).
```

### ada83.c:29412

```
// Find field offset and component index
```

### ada83.c:29425

```
// .ALL dereference: address of P.ALL = value of P (the pointer itself).
```

### ada83.c:29426

```
// Evaluating the operand as an expression handles every operand form
```

### ada83.c:29427

```
// uniformly — access variable, rename, alias, or parameterless function
```

### ada83.c:29428

```
// returning an access value (implicit call, RM 4.1.3). An access to an
```

### ada83.c:29429

```
// unconstrained array is a FAT value; the designated storage is its data
```

### ada83.c:29430

```
// slot, not the descriptor pair (c64108a: F1(2).ALL as an IN OUT actual).
```

### ada83.c:29433

```
// RM 4.1: null .ALL -> CE
```

### ada83.c:29439

```
// Array indexing or slice: Arr (I) or Arr (low..high).
```

### ada83.c:29440

```
// First reject type conversions T(X) — Arr-shaped at the AST but semantically
```

### ada83.c:29441

```
// a value-producing conversion, not an lvalue. Route to Generate_Expression
```

### ada83.c:29442

```
// so the conversion runs through Generate_Apply's type_conversion: branch,
```

### ada83.c:29443

```
// then take the result's address via the NK_APPLY composite-result handling
```

### ada83.c:29444

```
// further down (or, for an SSA fat result, the caller's fat-aware path).
```

### ada83.c:29455

```
// A function-call apply (including an UNCHECKED_CONVERSION instance whose
```

### ada83.c:29456

```
// result is composite) produces a value; the composite's address is the
```

### ada83.c:29457

```
// call result's storage. The resolver's APPLY_CALL verdict is the oracle —
```

### ada83.c:29458

```
// without this, a call with one argument whose callee returns an array
```

### ada83.c:29459

```
// falls into the indexing path below, which "calls" the callee with no
```

### ada83.c:29460

```
// arguments and indexes the result by the actual (cda201a).
```

### ada83.c:29470

```
// Handle implicit dereference: PT (I) where PT is access-to-array —
```

### ada83.c:29471

```
// including access-to-STRING (RM 4.1; c64108a's F1(2)(1)).
```

### ada83.c:29487

```
// Any fat-pointer-stored array — unconstrained, or constrained with
```

### ada83.c:29488

```
// dynamic bounds — carries its data pointer and low bound at run time; the
```

### ada83.c:29489

```
// flat-data path below would treat the fat-pointer struct itself as the
```

### ada83.c:29490

```
// element storage (the narrower dynamic-bounds test missed the
```

### ada83.c:29491

```
// unconstrained case, so a composite element of an unconstrained array
```

### ada83.c:29492

```
// read the descriptor bytes as data — RM 3.6). This mirrors the scalar
```

### ada83.c:29493

```
// index path's Type_Needs_Fat_Pointer test. An access-to-fat-array is
```

### ada83.c:29494

```
// handled here too (RM 3.10): the access value the prefix evaluates to
```

### ada83.c:29495

```
// IS itself the fat pointer, so the same data/low extraction applies —
```

### ada83.c:29496

```
// the plain-access branches below stay for the thin (static-constrained)
```

### ada83.c:29497

```
// case, where the access value already IS the array data pointer.
```

### ada83.c:29519

```
// Slice: ARR (low..high) - return address at slice start (RM 4.1.2)
```

### ada83.c:29522

```
// When the prefix is ITSELF a slice (a nested slice A(M..N)(L..H)),
```

### ada83.c:29523

```
// base points at the prefix slice's first element, whose index is the
```

### ada83.c:29524

```
// prefix slice's OWN low bound M — not the underlying array's low bound,
```

### ada83.c:29525

```
// which the slice's result type still carries. Subtracting the type's
```

### ada83.c:29526

```
// low bound would double-count the outer slide.
```

### ada83.c:29533

```
// Evaluate the prefix as an expression: handles a plain access
```

### ada83.c:29534

```
// variable, a rename/alias, and an access-returning
```

### ada83.c:29535

```
// parameterless function (implicit call, RM 4.1.3) uniformly.
```

### ada83.c:29567

```
// Dereference access-to-array: evaluate the prefix (the access
```

### ada83.c:29568

```
// value IS the array base) — uniform over variables, renames, and
```

### ada83.c:29569

```
// access-returning parameterless functions (RM 4.1.3).
```

### ada83.c:29578

```
// Adjust index: byte_offset = (idx - low) * elem_size
```

### ada83.c:29581

```
// Widen index to native integer (enum/bool indices may be i8/i1)
```

### ada83.c:29586

```
// A slice prefix's base pointer denotes the slice's first element,
```

### ada83.c:29587

```
// whose index is the slice's OWN low bound — the slice's result type
```

### ada83.c:29588

```
// still carries the underlying array's low bound, and subtracting
```

### ada83.c:29589

```
// that would land on the wrong element (N3(2..5)(4) is N3(4), at
```

### ada83.c:29590

```
// offset 4 - 2 from the slice base; RM 4.1.2).
```

### ada83.c:29617

```
// Handle unconstrained arrays passed as fat pointers
```

### ada83.c:29618

```
// Fat pointer - extract data and compute element address
```

### ada83.c:29633

```
// Multi-dimensional indexed component or unhandled access: delegate to
```

### ada83.c:29634

```
// Generate_Lvalue, which computes the element address regardless of
```

### ada83.c:29635

```
// dimensionality. Needed for IN OUT parameter actuals like X(I,J).
```

### ada83.c:29647

```
// Fallback: for expressions that return a pointer (like composite values).
```

### ada83.c:29648

```
// A dynamic-bounds aggregate returns an SSA fat pointer { ptr, ptr }; the
```

### ada83.c:29649

```
// caller wants a plain data ptr, so extract field 0 in that case.
```

### ada83.c:29658

```
// ─── Boolean Array Elementwise Op (RM 4.5.1) ─────────────────────────────────────────────────────
```

### ada83.c:29659

```
// Static-bound twin of Emit_Bool_Array_Op_Fat: unrolled loop applying `ir_op`
```

### ada83.c:29660

```
// (and/or/xor) byte-by-byte over raw data pointers. Returns alloca ptr to the
```

### ada83.c:29661

```
// result. `right` is ignored for NOT (is_not); NOT is realised as `xor 1`.
```

### ada83.c:29662

```
//
```

### ada83.c:29692

```
// Boolean array elementwise op on dynamic-bound (fat) operands (RM 4.5.1).
```

### ada83.c:29693

```
// The operands carry runtime bounds, so the result length is unknown at compile
```

### ada83.c:29694

```
// time: extract data+bounds from the left fat pointer, allocate a runtime-sized
```

### ada83.c:29695

```
// result, loop over the runtime length, and return a fat pointer carrying the
```

### ada83.c:29696

```
// operands' bounds. `right_fat` is ignored for NOT (is_not). `ir_op` is the
```

### ada83.c:29697

```
// per-byte LLVM op ("and"/"or"/"xor"); NOT is realised as `xor 1`.
```

### ada83.c:29709

```
// RM 4.5.1: the two operands of a logical array operation must have the
```

### ada83.c:29710

```
// same length, else CONSTRAINT_ERROR. (NOT has a single operand.)
```

### ada83.c:29751

```
// Test whether a type is a boolean array (for AND/OR/XOR/NOT dispatch).
```

### ada83.c:29756

```
// ─── Normalize Expression to Fat Pointer ─────────────────────────────────────────────────────────
```

### ada83.c:29757

```
// Given an expression and its type, produce a fat pointer value.                                  
```

### ada83.c:29758

```
// Handles: already-fat, CHARACTER (alloca+store+wrap), constrained array (wrap).                  
```

### ada83.c:29759

```
//                                                                                                  
```

### ada83.c:29771

```
// RM 4.5.3: a by-value element (scalar, or an access value) used as a
```

### ada83.c:29772

```
// concatenation element — wrap it as a single-element array. The value is held
```

### ada83.c:29773

```
// in a register, so spill it to a slot and use that as the one-element data
```

### ada83.c:29774

```
// pointer (bounds 1..1).
```

### ada83.c:29783

```
// RM 4.5.3: a record (or other by-reference composite) used as a concatenation
```

### ada83.c:29784

```
// element — e.g. R & A or R1 & R2 where R is the array's component type. The
```

### ada83.c:29785

```
// value is already an address, so it IS the data pointer of a single-element
```

### ada83.c:29786

```
// array; wrap it with bounds 1..1 (the element byte width comes from the
```

### ada83.c:29787

```
// result array type at the concat site).
```

### ada83.c:29791

```
// Multidimensional constrained array: wrap with every dimension's bounds.
```

### ada83.c:29792

```
// The single-dimension path below carries the aggregate count adjustment, so
```

### ada83.c:29793

```
// keep it for 1-D; Emit_Fat_Pointer_For_Lvalue owns the N-D case.
```

### ada83.c:29800

```
// Discriminant-dependent bounds: Type_Bound_Value returns 0 for                                
```

### ada83.c:29801

```
// BOUND_EXPR. For aggregates, derive bounds from positional count.                           
```

### ada83.c:29802

```
// For non-aggregates, load the discriminant at runtime. (RM 3.7.1)                             
```

### ada83.c:29803

```
//                                                                                              
```

### ada83.c:29815

```
// fallback: assume already fat
```

### ada83.c:29818

```
// Check whether a positional aggregate of the given constrained array type                         
```

### ada83.c:29819

```
// will produce a fat pointer from Generate_Aggregate. Generate_Aggregate                          
```

### ada83.c:29820

```
// overrides dim_hi[d] with a BOUND_INTEGER when dim_lo[d] is BOUND_INTEGER,                        
```

### ada83.c:29821

```
// collapsing dynamic bounds to static. So an aggregate only stays dynamic                         
```

### ada83.c:29822

```
// when at least one dimension has a BOUND_EXPR lower bound (typically from                         
```

### ada83.c:29823

```
// runtime expressions like IDENT_INT, NOT from discriminant references whose                       
```

### ada83.c:29824

```
// low bound is a literal). Returns false for non-array / non-dynamic types.                      
```

### ada83.c:29825

```
//                                                                                                  
```

### ada83.c:29842

```
// Assign a runtime elaboration ID to a constrained array type with                                 
```

### ada83.c:29843

```
// BOUND_EXPR bounds and emit its LLVM globals (@__rt_type_<id>_size,                               
```

### ada83.c:29844

```
// per-dimension _lo/_hi). Idempotent: returns immediately if ID                                   
```

### ada83.c:29845

```
// already assigned or if the type has only static bounds.                                         
```

### ada83.c:29846

```
//                                                                                                  
```

### ada83.c:29859

```
// Trigger for any non-discriminant runtime bound: a function call
```

### ada83.c:29860

```
// (IDENT_INT (-3)) or a reference to a variable/constant/parameter
```

### ada83.c:29861

```
// (STRING (1 .. X)). Only a DISCRIMINANT reference is excluded — those
```

### ada83.c:29862

```
// bounds are handled by the existing discriminant-dependent path.
```

### ada83.c:29863

```
//
```

### ada83.c:29883

```
// Generate expression and wrap as fat pointer if needed.                                          
```

### ada83.c:29884

```
// Like Normalize_To_Fat_Pointer but generates the expression internally.                          
```

### ada83.c:29885

```
// Handles aggregates of unconstrained types specially (load pre-built fat ptr).                   
```

### ada83.c:29886

```
//                                                                                                  
```

### ada83.c:29892

```
// Aggregates of unconstrained or truly-dynamic array types already build                         
```

### ada83.c:29893

```
// their own fat pointer alloca in Generate_Aggregate. Load it.                                 
```

### ada83.c:29894

```
// NOTE: Generate_Aggregate overrides dim_hi from positional element count                        
```

### ada83.c:29895

```
// when dim_lo is BOUND_INTEGER, converting bounds to static. So only                            
```

### ada83.c:29896

```
// aggregates whose type has a BOUND_EXPR *lower* bound (in any dimension)                        
```

### ada83.c:29897

```
// actually produce a fat pointer. Discriminant-dependent bounds like                            
```

### ada83.c:29898

```
// TB (1..A) have static lower bound → positional override → static array.                       
```

### ada83.c:29899

```
//                                                                                                
```

### ada83.c:29902

```
// Aggregate returns SSA fat pointer { ptr, ptr } directly.
```

### ada83.c:29907

```
// Multidimensional array: wrap every dimension's bounds. A single-dimension
```

### ada83.c:29908

```
// fat pointer would leave the inner dimensions' bounds undefined, so a
```

### ada83.c:29909

```
// length comparison (RM 4.5.2) against it reads garbage. The 1-D path below
```

### ada83.c:29910

```
// carries the aggregate positional-count adjustment, so keep it separate.
```

### ada83.c:29919

```
// Discriminant-dependent bounds: for aggregates, use positional count
```

### ada83.c:29933

```
// ─── Exact integer rescale through 128-bit arithmetic ────────────────────────────────────────────
```

### ada83.c:29934

```
// value * multiplier / divisor with a 128-bit intermediate, truncating toward
```

### ada83.c:29935

```
// zero, returned at `destination_rep`. This is the exact form of fixed-point
```

### ada83.c:29936

```
// mantissa rescaling (RM 4.5.5, 4.6): the multiplier/divisor pair comes from
```

### ada83.c:29937

```
// the SMALLs involved (Fixed_Small_As_Rational), and the wide intermediate
```

### ada83.c:29938

```
// keeps a full 64-bit mantissa times a SMALL ratio from overflowing.
```

### ada83.c:29957

```
// ─── Convert Universal_Real to Fixed-Point Scaled Integer ────────────────────────────────────────
```

### ada83.c:29958

```
// For fixed type with small S, value V converts to the nearest mantissa
```

### ada83.c:29959

```
// round(V / S). Rounding (not truncation) matters because V/S carries the
```

### ada83.c:29960

```
// double's representation error: 2.0 / 1e-5 = 199999.999..., which fptosi
```

### ada83.c:29961

```
// alone would truncate to 199999.
```

### ada83.c:29978

```
// Generate_Binary_Op — dispatch a binary operator node to either a user-defined
```

### ada83.c:29979

```
// function call (RM 6.7) or, after rename peeling (RM 8.5), the predefined-op
```

### ada83.c:29980

```
// emitter `Emit_Binary_Op_Predefined`. The latter owns all the per-op semantics
```

### ada83.c:29981

```
// (overflow checks, slice/fat-pointer comparison, AND_THEN/OR_ELSE, `&` concat,
```

### ada83.c:29982

```
// membership, fixed-point, float, universal int128, scalar arith/cmp).
```

### ada83.c:29983

```
//
```

### ada83.c:29984

```
// Stage 1 of dedup: extracted the predefined body into its own function so a
```

### ada83.c:29985

```
// future stage can route `P."+"(X,Y)`-style calls in Generate_Apply through it
```

### ada83.c:29986

```
// instead of maintaining a parallel ~200-line name-dispatcher.
```

### ada83.c:29987

```
//
```

### ada83.c:29990

```
// Peel renames (RM 8.5) and substitute a generic formal subprogram with
```

### ada83.c:29991

```
// the instantiation's actual (RM 12.3) — a rename does not emit its own
```

### ada83.c:29992

```
// body, and a formal's calls dispatch to the actual. A derived operator
```

### ada83.c:29993

```
// (RM 3.4) further forwards to the parent's body via its ultimate operation.
```

### ada83.c:29996

```
// User-defined operator: generate function call (RM 6.7)
```

### ada83.c:30012

```
// A constrained (inline) array actual passed to an unconstrained array
```

### ada83.c:30013

```
// formal must be marshalled into a fat pointer {data, bounds}, exactly as
```

### ada83.c:30014

```
// Generate_Apply does — its storage is the raw element array, so a bare
```

### ada83.c:30015

```
// rep conversion emits `load {ptr,ptr}` off a short object and reads
```

### ada83.c:30016

```
// garbage bounds (c67003d: `S1 & S2` with S1, S2 : STRING(1..2) passed to
```

### ada83.c:30017

```
// a user "&"(STRING, STRING)). An already-unconstrained actual is a fat
```

### ada83.c:30018

```
// pointer value and skips this.
```

### ada83.c:30024

```
// Real literal operand against a fixed-point formal: scale to the
```

### ada83.c:30025

```
// mantissa (RM 4.5.5), as the Generate_Apply marshalling does. A bare
```

### ada83.c:30026

```
// rep conversion would round 10.9 to mantissa 11.
```

### ada83.c:30039

```
// RM 3.5.4 / 4.6: each actual has to satisfy its formal's subtype. A
```

### ada83.c:30040

```
// universal-integer literal like `A + 0` where `+` takes INT (1..100)
```

### ada83.c:30041

```
// must range-check 0 before the call (c34018a). Predefined ops handle
```

### ada83.c:30042

```
// this in their own emitter — this branch is for user-defined ops.
```

### ada83.c:30062

```
// RM 6.7: "/=" is not independently declarable; when the syntactic
```

### ada83.c:30063

```
// operator is "/=" but the call target is a user "=", negate the result
```

### ada83.c:30064

```
// (c67002a: user "="(LP,LP) → BOOLEAN; `X /= Y` calls user "=" and
```

### ada83.c:30065

```
// negates).
```

### ada83.c:30075

```
// Rename-to-different-predefined: dispatch by the *target's* op token, not
```

### ada83.c:30076

```
// the source's. `FUNCTION "+" RENAMES "*"` must turn `a + b` into multiply.
```

### ada83.c:30077

```
// We shadow `node` with a shallow copy whose op is overridden; the rest of
```

### ada83.c:30078

```
// the AST (operands, type, location) is shared by pointer.
```

### ada83.c:30091

```
// Emit_Binary_Op_Predefined — the predefined binary-operator emitter. Handles
```

### ada83.c:30092

```
// all built-in operators on Ada's scalar and composite types. Reads `node->
```

### ada83.c:30093

```
// binary.{op,left,right}` and `node->type`. Returns the result temp.
```

### ada83.c:30094

```
//
```

### ada83.c:30095

```
// This is the *only* place these semantics live. Called from Generate_Binary_Op
```

### ada83.c:30096

```
// after rename peeling, and (Stage 2) from Generate_Apply when a predefined
```

### ada83.c:30097

```
// operator is invoked in functional form (`P."+"(X,Y)`).
```

### ada83.c:30098

```
//
```

### ada83.c:30099

```
// RM 4.5.2 membership: AND onto `belongs` one equality per discriminant of
```

### ada83.c:30100

```
// the record at `record_pointer` against `constrained_type`'s discriminant
```

### ada83.c:30101

```
// constraint (an expression, or a static value). Returns false - leaving
```

### ada83.c:30102

```
// `belongs` partially accumulated - when some discriminant has no recorded
```

### ada83.c:30103

```
// constraint source; the caller then falls through to its default handling.
```

### ada83.c:30125

```
// Check if this is equality/inequality on composite types
```

### ada83.c:30128

```
// A static universal_real arithmetic expression folds to an exact rational
```

### ada83.c:30129

```
// (RM 4.10). Emit the folded constant instead of generating operand code:
```

### ada83.c:30130

```
// the runtime path would compute an integer factor like (2**MANTISSA - 1) in
```

### ada83.c:30131

```
// bounded INTEGER and overflow for a fine fixed type (cc1223a: DURATION has
```

### ada83.c:30132

```
// MANTISSA = 52, so 2**MANTISSA does not fit Standard.Integer). Comparisons
```

### ada83.c:30133

```
// keep their own dedicated fold further below; this covers the arithmetic.
```

### ada83.c:30148

```
// Composite type comparison
```

### ada83.c:30157

```
// Handle slice comparisons specially - they have mixed representations
```

### ada83.c:30158

```
// At least one slice with constrained type - generate inline comparison
```

### ada83.c:30166

```
// Generate left operand
```

### ada83.c:30180

```
// Generate right operand
```

### ada83.c:30195

```
// Compute lengths and compare
```

### ada83.c:30200

```
// Compare data with memcmp
```

### ada83.c:30205

```
// Result: lengths match AND data matches
```

### ada83.c:30208

```
// At least one operand produces a fat pointer.
```

### ada83.c:30209

```
// Normalize both to fat pointers for uniform comparison.
```

### ada83.c:30214

```
// Normalize both operands to fat pointers for uniform comparison.
```

### ada83.c:30215

```
// Constrained arrays get wrapped with static bounds.
```

### ada83.c:30220

```
// Use the unconstrained array equality path (compares lengths then data)
```

### ada83.c:30224

```
// Use unconstrained type for comparison
```

### ada83.c:30227

```
// Both operands were normalized to fat pointers above, so label them
```

### ada83.c:30228

```
// as such - LLVM_Rep_To_String (Type_To_Rep (cmp_type)) is "ptr" for a constrained array and
```

### ada83.c:30229

```
// would mislabel the fat value.
```

### ada83.c:30232

```
// Standard path: both operands are constrained (same representation).                         
```

### ada83.c:30233

```
// However, some expressions (concatenation, function calls) may still                          
```

### ada83.c:30234

```
// produce fat pointers. Extract data ptr if needed.                                          
```

### ada83.c:30235

```
//                                                                                              
```

### ada83.c:30238

```
// If both are constrained arrays but with different static sizes,
```

### ada83.c:30239

```
// they cannot be equal (RM 4.5.2(9)). Emit constant false.
```

### ada83.c:30277

```
// For /= operator, negate the result
```

### ada83.c:30282

```
// Comparisons stay as i1 (Boolean_Data relationship). Record the type
```

### ada83.c:30283

```
// so callers (e.g. Generate_Apply's operator-as-function path) can detect
```

### ada83.c:30284

```
// it and widen to BOOLEAN i8 at the call boundary.
```

### ada83.c:30288

```
// Array relational comparisons (lexicographic)
```

### ada83.c:30293

```
// Get addresses of both arrays for comparison
```

### ada83.c:30297

```
// An aggregate of an unconstrained type already builds its own fat                             
```

### ada83.c:30298

```
// pointer alloca inside Generate_Aggregate. Detect this so we can                             
```

### ada83.c:30299

```
// just load it instead of double-wrapping with wrong bounds.                                  
```

### ada83.c:30300

```
//                                                                                              
```

### ada83.c:30311

```
// Generate each operand as fat pointer, wrapping constrained if needed
```

### ada83.c:30321

```
// Constrained array: same size, use memcmp directly
```

### ada83.c:30331

```
// Unconstrained array: lexicographic comparison via helper
```

### ada83.c:30338

```
// Compare memcmp result with 0 based on operator
```

### ada83.c:30342

```
// AND THEN: if left is false, result is false (don't evaluate right)
```

### ada83.c:30343

```
//           if left is true, result is right
```

### ada83.c:30352

```
// Comparisons stay as i1. Record the type so call-form callers can widen.
```

### ada83.c:30356

```
// Short-circuit boolean operators: AND THEN, OR ELSE
```

### ada83.c:30358

```
// These must NOT evaluate the right operand if the left operand                                  
```

### ada83.c:30359

```
// determines the result (Ada RM 4.5.1).                                                         
```

### ada83.c:30360

```
//                                                                                                
```

### ada83.c:30361

```
// AND THEN and OR ELSE share one control-flow skeleton: evaluate the left
```

### ada83.c:30362

```
// operand, branch on it, and only fall through to the right operand when the
```

### ada83.c:30363

```
// left does not already settle the result. They differ in three details
```

### ada83.c:30364

```
// captured here: the label prefix, the conditional branch's target order
```

### ada83.c:30365

```
// (AND THEN evaluates the right when the left is true; OR ELSE when the left
```

### ada83.c:30366

```
// is false), and the phi's constant for the short-circuited edge.
```

### ada83.c:30367

```
//
```

### ada83.c:30379

```
// Save current block for phi
```

### ada83.c:30383

```
// AND THEN takes the right-evaluation edge when the left is true; OR ELSE
```

### ada83.c:30384

```
// when the left is false. Emit the branch targets in that operand-specific
```

### ada83.c:30385

```
// order so the i1 test routes to the correct successor block.
```

### ada83.c:30393

```
// Evaluate right if left did not settle the result
```

### ada83.c:30402

```
// Merge point
```

### ada83.c:30405

```
// short-circuit -> i8
```

### ada83.c:30408

```
// String/array concatenation
```

### ada83.c:30413

```
// Generate both operands.                                                                     
```

### ada83.c:30414

```
// Each operand may be: fat pointer (STRING/unconstrained/literal/slice/concat),                
```

### ada83.c:30415

```
// ptr (constrained array), or i64 (CHARACTER).                                                
```

### ada83.c:30416

```
// We normalize each to a fat pointer before proceeding.                                       
```

### ada83.c:30417

```
//                                                                                              
```

### ada83.c:30421

```
// The index-bound width and the component byte-width both come from the
```

### ada83.c:30422

```
// *result* array type, never from an operand: in `C & D` (component &
```

### ada83.c:30423

```
// component) and `C & A` (component & array) the left operand is a scalar
```

### ada83.c:30424

```
// component, and asking it for an array bound is the "non-array kind" bug.
```

### ada83.c:30429

```
// Normalize both operands to fat pointers (handles character, constrained,
```

### ada83.c:30430

```
// already-fat). When the result's component type is itself an array (an array
```

### ada83.c:30431

```
// of arrays), an operand whose type is that component is a single element —
```

### ada83.c:30432

```
// its value is already an address, so wrap it as a one-element array (bounds
```

### ada83.c:30433

```
// 1..1) rather than letting Normalize read it as a nested array with its own
```

### ada83.c:30434

```
// bounds. Non-array component types (scalars, records) are handled by
```

### ada83.c:30435

```
// Normalize itself.
```

### ada83.c:30449

```
// Extract data pointers and bounds
```

### ada83.c:30457

```
// Calculate lengths: high - low + 1
```

### ada83.c:30461

```
// Total length
```

### ada83.c:30464

```
// RM 4.5.3(7): the check that the result's bounds belong to the index
```

### ada83.c:30465

```
// subtype is emitted below, once the result's actual lower/upper bounds are
```

### ada83.c:30466

```
// known — the lower bound is inherited from the left operand, so a length-
```

### ada83.c:30467

```
// only test would miss a result that starts above the subtype's 'FIRST and
```

### ada83.c:30468

```
// runs its upper bound past 'LAST (c45345c).
```

### ada83.c:30471

```
// Lengths so far count *elements*; storage counts *bytes*. The operands
```

### ada83.c:30472

```
// and result share one component type, so a single byte-width scales every
```

### ada83.c:30473

```
// length that addresses memory (the allocation extent, each memcpy, and the
```

### ada83.c:30474

```
// offset to the right half). The result bounds stay in element units and
```

### ada83.c:30475

```
// are computed below: they index the array, they do not size it.
```

### ada83.c:30489

```
// Allocate space on secondary stack
```

### ada83.c:30492

```
// Copy left string using llvm.memcpy
```

### ada83.c:30495

```
// Calculate destination for right string
```

### ada83.c:30498

```
// Copy right string
```

### ada83.c:30501

```
// Result bounds (RM 4.5.3). When the LEFT operand is itself a one-
```

### ada83.c:30502

```
// dimensional array, the result's lower bound is the left operand's own
```

### ada83.c:30503

```
// lower bound — or, if the left operand is a null array, the right
```

### ada83.c:30504

```
// operand's. Only when the left operand is a single component does the
```

### ada83.c:30505

```
// result take the index subtype's 'FIRST (POSITIVE'FIRST = 1 for STRING,
```

### ada83.c:30506

```
// or e.g. STE'FIRST = 2 for a custom array type). The upper bound always
```

### ada83.c:30507

```
// follows from the lower bound and the total length.
```

### ada83.c:30508

```
//
```

### ada83.c:30519

```
// default for STRING (POSITIVE'FIRST)
```

### ada83.c:30528

```
// high = low + total_len - 1
```

### ada83.c:30532

```
// RM 4.5.3: when the left operand is a null array the result IS the right
```

### ada83.c:30533

```
// operand, so its high bound is the right operand's own high — not
```

### ada83.c:30534

```
// low + total_len - 1, which for a null right yields right_low - 1 and
```

### ada83.c:30535

```
// loses the right operand's bounds (e.g. A(1..0) & A(2..0) must be 2..0).
```

### ada83.c:30544

```
// RM 4.5.3(7): a non-null catenation result must have both bounds within the
```

### ada83.c:30545

```
// index subtype. The lower bound is inherited from the left operand, so the
```

### ada83.c:30546

```
// upper bound can exceed the subtype's 'LAST even when the length fits the
```

### ada83.c:30547

```
// subtype's extent (c45345c: A(6..9) & ... starts at 6, so ten elements run
```

### ada83.c:30548

```
// to 15). The check is skipped for a null result, and for an index subtype
```

### ada83.c:30549

```
// spanning the full index base type (STRING's POSITIVE) where an i32-domain
```

### ada83.c:30550

```
// bound cannot overflow it.
```

### ada83.c:30554

```
// The index subtype's range (RM 3.6(9)) is a property of the array TYPE,
```

### ada83.c:30555

```
// not of an object's constraint: for a named unconstrained type used with
```

### ada83.c:30556

```
// a constraint (STRING(1..5), a user CH_ARR(1..7)) it is the type's own
```

### ada83.c:30557

```
// index subtype (POSITIVE, SMALL), so `S & S` at 1..10 is legal; for an
```

### ada83.c:30558

```
// anonymous constrained array (`ARRAY (1..10)`, `ARRAY (MON..FRI)`) the
```

### ada83.c:30559

```
// discrete range itself defines the index subtype. Type_Base strips a
```

### ada83.c:30560

```
// named type's object-constraint while leaving an anonymous array's own
```

### ada83.c:30561

```
// range, so its first index constraint is the index subtype range in both
```

### ada83.c:30562

```
// cases.
```

### ada83.c:30568

```
// RM 4.5.3 checks only the UPPER bound of the result against the index
```

### ada83.c:30569

```
// subtype (the lower bound is inherited from an operand already within
```

### ada83.c:30570

```
// range). A static 'LAST equal to the index base type's own maximum
```

### ada83.c:30571

```
// (STRING's POSITIVE'LAST = INTEGER'LAST) needs no check — an i32-domain
```

### ada83.c:30572

```
// bound cannot pass it. A dynamic bound (`ARRAY (1..IDENT_INT(10))`,
```

### ada83.c:30573

```
// c45345d) is evaluated at run time via Emit_Single_Bound.
```

### ada83.c:30593

```
// Return fat pointer to result
```

### ada83.c:30599

```
// NK_RANGE right operand is generated inside the IN/NOT IN handler.                             
```

### ada83.c:30600

```
// For membership tests (IN/NOT IN), type names are also handled specially                        
```

### ada83.c:30601

```
// and should not be evaluated as expressions.                                                   
```

### ada83.c:30602

```
//                                                                                                
```

### ada83.c:30616

```
// Native LLVM rep for integer operations on left/right. The PRODUCED rep is
```

### ada83.c:30617

```
// authoritative (e.g. an integer `**` sub-expression is emitted at the i32
```

### ada83.c:30618

```
// working width even when the AST predicts the i8 result subtype); the
```

### ada83.c:30619

```
// range/membership right operand has no value and works at the arith rep.
```

### ada83.c:30629

```
// Float rep from result type (float vs double).
```

### ada83.c:30632

```
// Mixed-mode arithmetic: when result is float but operands are integer,
```

### ada83.c:30633

```
// convert integer operands to float for proper arithmetic (RM 4.5.5)
```

### ada83.c:30635

```
// Classify and size operands by their PRODUCED reps, not the AST
```

### ada83.c:30636

```
// prediction: an operand can be emitted at a different float width than
```

### ada83.c:30637

```
// its declared type (e.g. a universal-real literal emitted as double for
```

### ada83.c:30638

```
// a single-precision FLOAT operand), and the narrowing must still fire.
```

### ada83.c:30663

```
// Fixed-point uses scaled integer representation at the                                          
```

### ada83.c:30664

```
// result type's native width. Widen operands to match.                                         
```

### ada83.c:30665

```
// Skip universal_real operands - they'll be handled below via                                    
```

### ada83.c:30666

```
// the fdiv/fptosi path which produces the correct scaled integer.                               
```

### ada83.c:30667

```
//                                                                                                
```

### ada83.c:30678

```
// Mixed fixed-point / universal_real arithmetic (RM 4.5.5, 4.10):                                
```

### ada83.c:30679

```
// When result is fixed-point but an operand is universal_real, convert                           
```

### ada83.c:30680

```
// the universal_real to the fixed-point's scaled integer representation.                        
```

### ada83.c:30681

```
// For fixed type with small S, value V converts to: floor (V / S)                                
```

### ada83.c:30682

```
// Skip for exponentiation which has its own special handling.                                   
```

### ada83.c:30683

```
//                                                                                                
```

### ada83.c:30693

```
// Fixed-point multiplication/division rescaling (RM 4.5.5).
```

### ada83.c:30694

```
// A fixed value is its integer mantissa times the type's SMALL, so with
```

### ada83.c:30695

```
// operand SMALLs Sa, Sb and result SMALL Sr:
```

### ada83.c:30696

```
//   (a*Sa) * (b*Sb) = r*Sr  =>  r = a*b * (Sa*Sb/Sr)
```

### ada83.c:30697

```
//   (a*Sa) / (b*Sb) = r*Sr  =>  r = a/b * (Sa/(Sb*Sr))
```

### ada83.c:30698

```
// The factor is formed exactly as a ratio of integers (each SMALL is a
```

### ada83.c:30699

```
// power of two or of ten, see Fixed_Small_As_Rational) and applied in
```

### ada83.c:30700

```
// 128-bit arithmetic so the intermediate products cannot overflow the
```

### ada83.c:30701

```
// 64-bit mantissa range. An integer operand has an effective SMALL of 1;
```

### ada83.c:30702

```
// a universal_real operand was converted to a mantissa just above using
```

### ada83.c:30703

```
// the result's SMALL, so the result's SMALL is its effective SMALL.
```

### ada83.c:30704

```
//
```

### ada83.c:30727

```
// reduce by gcd
```

### ada83.c:30730

```
// A reduced factor of 1 (e.g. Fixed * Integer) needs no rescale; fall
```

### ada83.c:30731

```
// through to the plain mul/sdiv emission below.
```

### ada83.c:30755

```
// RM 4.5.5: a fixed-point division by zero raises NUMERIC_ERROR /
```

### ada83.c:30756

```
// CONSTRAINT_ERROR, not a hardware trap. The rescale widens the divisor
```

### ada83.c:30757

```
// into i128, so guard the source operand (zero there ⇒ zero divisor)
```

### ada83.c:30758

```
// before the sdiv, exactly as the integer path does.
```

### ada83.c:30778

```
// modular (unsigned) types use unsigned division/remainder.                                     
```

### ada83.c:30779

```
// Ada MOD vs REM differ for signed types (RM 4.5.5), but for modular                             
```

### ada83.c:30780

```
// types both map to urem since all values are non-negative.                                     
```

### ada83.c:30781

```
//                                                                                                
```

### ada83.c:30792

```
// Exponentiation: base ** exponent                                                           
```

### ada83.c:30793

```
// For floating-point: use llvm.pow.f64 intrinsic (requires double)                           
```

### ada83.c:30794

```
// For integer: use __ada_integer_pow                                                         
```

### ada83.c:30795

```
//                                                                                            
```

### ada83.c:30800

```
// Float ** Integer: use native-precision pow intrinsic.                                   
```

### ada83.c:30801

```
// LLVM provides llvm.pow.f32 and llvm.pow.f64.                                            
```

### ada83.c:30802

```
// RM 4.5.6(12): 0.0 ** negative must raise CONSTRAINT_ERROR.                              
```

### ada83.c:30803

```
//                                                                                          
```

### ada83.c:30809

```
// Check: if base == 0.0 and exponent < 0, raise CONSTRAINT_ERROR
```

### ada83.c:30815

```
// Convert integer exponent to matching float type
```

### ada83.c:30821

```
// Integer ** Integer: use integer power function.                                         
```

### ada83.c:30822

```
// Signed types use overflow-checked __ada_integer_pow.                                    
```

### ada83.c:30823

```
// Modular types use wrapping __ada_modular_pow (RM 3.5.4).                                
```

### ada83.c:30824

```
//                                                                                          
```

### ada83.c:30827

```
// The power helpers run at 64-bit width so any integer base
```

### ada83.c:30828

```
// type's values fit (an i32 working width overflowed BIG'(2)**60
```

### ada83.c:30829

```
// spuriously); the result narrows back below.
```

### ada83.c:30839

```
// The `**` node's Ada type is result_type; return at its storage rep
```

### ada83.c:30840

```
// (the pow runs at the i32 working width but the result is e.g. i8),
```

### ada83.c:30841

```
// so an enclosing operator sees a correctly-typed operand.
```

### ada83.c:30846

```
// An i64-clean result can still overflow a narrower base type
```

### ada83.c:30847

```
// (RM 4.5.6 with RM 11.6): check against the destination
```

### ada83.c:30848

```
// width's own bounds before narrowing — the generic constraint
```

### ada83.c:30849

```
// check treats a base type as uncheckable, so the compare is
```

### ada83.c:30850

```
// explicit here.
```

### ada83.c:30868

```
// Bitwise/logical operations (RM 4.5.1):                                                       
```

### ada83.c:30869

```
// - Modular types: bitwise at native width                                                     
```

### ada83.c:30870

```
// - Boolean arrays: element-wise                                                               
```

### ada83.c:30871

```
// - Boolean scalars: convert to i1, operate, widen back                                        
```

### ada83.c:30872

```
//                                                                                              
```

### ada83.c:30882

```
// Dynamic-bound (fat) operands: the result length is only known at
```

### ada83.c:30883

```
// runtime, so produce a fat result carrying the operands' bounds.
```

### ada83.c:30889

```
// Static operands: Emit_Bool_Array_Op needs raw data pointers for
```

### ada83.c:30890

```
// its byte-wise GEPs, so extract data ptr from any fat value.
```

### ada83.c:30911

```
// Check operand types for typed comparisons
```

### ada83.c:30920

```
// Float-ness is producer-honest: an operand physically emitted as a
```

### ada83.c:30921

```
// float register is float even when the AST type prediction disagrees
```

### ada83.c:30922

```
// (e.g. a generic formal real resolved through the actual). Mirrors the
```

### ada83.c:30923

```
// PRODUCED-rep width determination further below.
```

### ada83.c:30931

```
// Determine actual LLVM types of operands for type-safe comparison.                       
```

### ada83.c:30932

```
// Access types produce ptr, arrays produce ptr or fat_ptr,                                 
```

### ada83.c:30933

```
// integers/enums produce i64.
```

### ada83.c:30934

```
//                                                                                          
```

### ada83.c:30935

```
// Each operand's actual emitted rep, not the AST-derived prediction:
```

### ada83.c:30936

```
// the prediction can disagree with what was physically emitted, making
```

### ada83.c:30937

```
// the convert-to-common-type a no-op and emitting an icmp whose operand
```

### ada83.c:30938

```
// width mislabels the register. Both operands are always generated on
```

### ada83.c:30939

```
// the comparison path (membership/range dispatch elsewhere).
```

### ada83.c:30943

```
// Boolean sub-expressions may produce i1 (raw comparisons) or                              
```

### ada83.c:30944

```
// i64 (widened booleans). Use actual expression type to avoid                             
```

### ada83.c:30945

```
// double-widening when the value is already i64.                                          
```

### ada83.c:30946

```
//                                                                                          
```

### ada83.c:30959

```
// For non-float, non-boolean: ensure operands are same type.                              
```

### ada83.c:30960

```
// If one is ptr and other is i64, convert to common type.                                 
```

### ada83.c:30961

```
// Fat pointers: extract data pointer for comparison.                                      
```

### ada83.c:30962

```
//                                                                                          
```

### ada83.c:30966

```
// Handle fat pointer operands - extract data pointer
```

### ada83.c:30976

```
// Normalize: if one is ptr and other is integer, convert ptr to integer.
```

### ada83.c:30977

```
// integer side may be native type (i8/i16/i32/i64).
```

### ada83.c:30990

```
// Both ptr: will use icmp eq ptr below
```

### ada83.c:30991

```
// Both integer: will use common type below
```

### ada83.c:30994

```
// Determine float widths from the operands' PRODUCED reps when they are
```

### ada83.c:30995

```
// float (the AST prediction can disagree, e.g. a single-precision typed
```

### ada83.c:30996

```
// value physically emitted as double), falling back to the declared
```

### ada83.c:30997

```
// type otherwise.
```

### ada83.c:31003

```
// UNIVERSAL_REAL always uses double (Generate_Real_Literal produces double)
```

### ada83.c:31005

```
// Convert operands to same type if needed                                                  
```

### ada83.c:31006

```
// Convert right to float. If it's fixed-point, multiply by SMALL.                         
```

### ada83.c:31007

```
// use actual integer type; uitofp for unsigned.                                           
```

### ada83.c:31008

```
//                                                                                          
```

### ada83.c:31011

```
// Fixed-point right: compare in the mantissa domain — convert the
```

### ada83.c:31012

```
// float (a universal_real value) to the nearest mantissa instead of
```

### ada83.c:31013

```
// scaling the mantissa up to float, where SMALL's representation
```

### ada83.c:31014

```
// error makes exact equalities like D = 86_400.0 fail.
```

### ada83.c:31030

```
// Both operands are now float_type; record their reps so the float
```

### ada83.c:31031

```
// comparison below sees their true widths.
```

### ada83.c:31037

```
// Convert right float to integer for fixed-point comparison.
```

### ada83.c:31038

```
// Fixed-point left compares in the mantissa domain (see above).
```

### ada83.c:31059

```
// RM 4.10: If both sides are static universal_real, fold
```

### ada83.c:31060

```
// the comparison at compile time using exact rationals. Restrict to
```

### ada83.c:31061

```
// genuine universal_real operands: a specific floating type (e.g.
```

### ada83.c:31062

```
// FLOAT5(big_int)) rounds its value to a model number, and comparing
```

### ada83.c:31063

```
// that rounded value against the OTHER operand's exact rational is
```

### ada83.c:31064

```
// asymmetric — it wrongly reports FLOAT5(N) /= N.0 as unequal (c46022a).
```

### ada83.c:31065

```
// Let such typed comparisons fall through to the runtime path, which
```

### ada83.c:31066

```
// rounds both operands to the same machine type.
```

### ada83.c:31098

```
// Ordered comparisons on pointers: convert to integer first
```

### ada83.c:31107

```
// Float comparison: either operand is float (both-float, derived float,
```

### ada83.c:31108

```
// or universal real). Compare at the join of the two operands' float
```

### ada83.c:31109

```
// widths, coercing both (int->float, or float->float narrow/widen) so
```

### ada83.c:31110

```
// the fcmp operands match even when their produced widths differ
```

### ada83.c:31111

```
// (e.g. a single-precision value compared against a double).
```

### ada83.c:31118

```
// integer comparison using common native type.
```

### ada83.c:31119

```
// Modular (unsigned) types use unsigned predicates.
```

### ada83.c:31130

```
// comparison result stays as i1; widened at store boundary
```

### ada83.c:31134

```
// Membership test - two forms:                                                                 
```

### ada83.c:31135

```
//   X IN  low .. high   >  low <= X <= high                                                    
```

### ada83.c:31136

```
//   X IN  T             >  T'FIRST <= X <= T'LAST                                              
```

### ada83.c:31137

```
//                                                                                              
```

### ada83.c:31139

```
// NOT IN encoded as TK_NOT binary (RM 4.4)
```

### ada83.c:31145

```
// Dynamic range: generate both bounds from AST
```

### ada83.c:31153

```
// Float membership: compare the value against the bounds at the
```

### ada83.c:31154

```
// joined float width (Emit_In_Range coerces int bounds via sitofp).
```

### ada83.c:31158

```
// Integer / fixed-point membership: fixed-point bounds (and value)
```

### ada83.c:31159

```
// scale by SMALL to the stored mantissa, then an integer compare.
```

### ada83.c:31189

```
// X IN A'RANGE or X IN A'RANGE (N) - expand to A'FIRST (N) <= X <= A'LAST (N)
```

### ada83.c:31197

```
// The bounds carry the index subtype's width (e.g. i8 for a small
```

### ada83.c:31198

```
// integer index); Emit_In_Range widens against the value, but only
```

### ada83.c:31199

```
// if told the bounds' true rep — never assume Integer_Arith_Rep.
```

### ada83.c:31221

```
// Fallback - can't determine bounds
```

### ada83.c:31229

```
// Compare left IN lo..hi (A'FIRST .. A'LAST)
```

### ada83.c:31235

```
// Type or subtype name: generate bounds at runtime (RM 4.4)
```

### ada83.c:31239

```
// Membership in a constrained array subtype: the value belongs iff
```

### ada83.c:31240

```
// each dimension's length equals the subtype's (RM 4.5.2 / 3.6.1).
```

### ada83.c:31241

```
// Array values slide, so the lengths must match, not the absolute
```

### ada83.c:31242

```
// bounds. Lengths are compared at runtime; an aggregate contributes
```

### ada83.c:31243

```
// its positional count and a typed value its own bounds, so a
```

### ada83.c:31244

```
// statically-decidable case folds to a constant. The subtype bounds
```

### ada83.c:31245

```
// may themselves be dynamic, e.g. new P(IDENT(5)..IDENT(7)), and
```

### ada83.c:31246

```
// Emit_Bound_Value_Typed evaluates them then.
```

### ada83.c:31255

```
// Positional count of dimension `dim` of a purely-positional literal
```

### ada83.c:31256

```
// aggregate value, or -1 when the value is not such an aggregate.
```

### ada83.c:31282

```
// RM 4.5.2 / 3.6.1: a value belongs to a constrained array subtype
```

### ada83.c:31283

```
// iff at each index position it has the SAME bounds, not merely the
```

### ada83.c:31284

```
// same length — two distinct null ranges (e.g. 3..2 and 2..1) match
```

### ada83.c:31285

```
// in length but not in bounds. A positional aggregate has no bounds
```

### ada83.c:31286

```
// of its own; it takes them from the applicable index constraint, so
```

### ada83.c:31287

```
// for that operand the length (component count) is the right test.
```

### ada83.c:31315

```
// Membership in a *constrained discriminated record* subtype: the
```

### ada83.c:31316

```
// value belongs iff its discriminants equal the subtype's constraint
```

### ada83.c:31317

```
// (RM 4.5.2). Compare each discriminant of the value against the
```

### ada83.c:31318

```
// constraint value (static, or evaluated when the constraint is an
```

### ada83.c:31319

```
// expression such as IDENT(3)).
```

### ada83.c:31332

```
// Membership in a constrained access-to-array subtype (RM 3.3.2):
```

### ada83.c:31333

```
// a null value always belongs; a non-null value belongs iff its
```

### ada83.c:31334

```
// designated array's index bounds equal the subtype's constraint.
```

### ada83.c:31335

```
// The bounds live behind the value's descriptor pointer, so they
```

### ada83.c:31336

```
// may be READ only on the non-null path — a null access's bounds
```

### ada83.c:31337

```
// pointer is garbage and dereferencing it traps (c45282a).
```

### ada83.c:31345

```
// The constraint's own bounds need no value: evaluate them first
```

### ada83.c:31346

```
// to learn whether the test is expressible at all.
```

### ada83.c:31384

```
// Membership in a constrained access-to-record subtype (RM 3.3.2):
```

### ada83.c:31385

```
// a null value belongs; a non-null value belongs iff its designated
```

### ada83.c:31386

```
// record's discriminants equal the subtype's constraint — read only
```

### ada83.c:31387

```
// on the non-null path, exactly as for arrays above.
```

### ada83.c:31412

```
// Other composites (records, strings), access and task types: the
```

### ada83.c:31413

```
// value is already of the type, so membership is always TRUE
```

### ada83.c:31414

```
// (RM 4.5.2). Access types have no range.
```

### ada83.c:31422

```
// X IN T where the operand's nominal subtype is statically known to
```

### ada83.c:31423

```
// be covered by T folds to a constant TRUE (RM 4.5.2): every value
```

### ada83.c:31424

```
// of the operand's subtype belongs to T, so the test neither needs
```

### ada83.c:31425

```
// nor may read the operand, which can be an as-yet-unassigned scalar
```

### ada83.c:31426

```
// whose stored value is meaningless. This mirrors GNAT's
```

### ada83.c:31427

```
// Compile_Time_Compare, which proves the bounds from the operand's
```

### ada83.c:31428

```
// nominal subtype rather than its runtime value; the test stays a
```

### ada83.c:31429

```
// runtime check whenever T is a strictly tighter subtype.
```

### ada83.c:31466

```
// Fixed-point bounds are real literals, but the value is held as a
```

### ada83.c:31467

```
// scaled-integer mantissa (value / SMALL); scale the bounds the
```

### ada83.c:31468

```
// same way (and the value, if it arrived as a real) so the integer
```

### ada83.c:31469

```
// comparison is in matching units.
```

### ada83.c:31486

```
// Bounds could not be determined - assume always in range
```

### ada83.c:31496

```
// value IN T'FIRST .. T'LAST. Emit_In_Range joins the value and
```

### ada83.c:31497

```
// bound reps (float compare if any is float, else widest int with
```

### ada83.c:31498

```
// unsigned predicates for modular types).
```

### ada83.c:31505

```
// A TYPE MARK with no expressible range (a float DIGITS type
```

### ada83.c:31506

```
// whose bounds were never constrained — c74004a's FLT): every
```

### ada83.c:31507

```
// value of the type belongs (RM 4.5.2). The right operand of a
```

### ada83.c:31508

```
// membership is never evaluated as a value, so the equality
```

### ada83.c:31509

```
// fallback below must not fire for type marks.
```

### ada83.c:31518

```
// Fallback: equality with right operand value
```

### ada83.c:31524

```
// use common native type for equality.
```

### ada83.c:31535

```
// membership result stays as i1; widened at store boundary
```

### ada83.c:31539

```
// Type system said not float, but LLVM types are float (derived float).
```

### ada83.c:31540

```
// Promote to float arithmetic.
```

### ada83.c:31548

```
// The opcode was chosen while the operator still looked integral (a fixed
```

### ada83.c:31549

```
// ÷ integer in a float representation, a derived float, …) — re-select it
```

### ada83.c:31550

```
// for float arithmetic or the emitted instruction is ill-typed (sdiv double).
```

### ada83.c:31569

```
// use common native integer type for arithmetic.
```

### ada83.c:31575

```
// Division/remainder: emit division-by-zero check and MIN_INT/-1 check
```

### ada83.c:31576

```
// before the actual sdiv/udiv/srem/urem (RM 4.5.5).
```

### ada83.c:31585

```
// Ada MOD correction: MOD result has sign of divisor (RM 4.5.5)
```

### ada83.c:31586

```
// srem result has sign of dividend. When signs differ, add divisor.
```

### ada83.c:31598

```
// Add/sub/mul: use overflow-checked intrinsics for signed types (RM 4.5).
```

### ada83.c:31599

```
// Modular types wrap naturally; Emit_Overflow_Checked_Op handles this.
```

### ada83.c:31606

```
// Modular wrapping: Ada modular arithmetic wraps modulo M (RM 4.5.3).                         
```

### ada83.c:31607

```
// For power-of-2 moduli, LLVM's natural integer wrapping suffices.                            
```

### ada83.c:31608

```
// For non-power-of-2 moduli (e.g. mod 100), emit: urem result, modulus.                       
```

### ada83.c:31609

```
// Only applies to add, sub, mul - div/rem already produce in-range values.                    
```

### ada83.c:31610

```
//                                                                                              
```

### ada83.c:31614

```
// Check if modulus is a power of 2: if so, no masking needed
```

### ada83.c:31625

```
// Integer result carries its integer LLVM rep, not float_type_str
```

### ada83.c:31626

```
// (which Float_LLVM_Rep_Of derives as float/double from the result
```

### ada83.c:31627

```
// size and would mislabel the i-typed register).
```

### ada83.c:31630

```
// RM 4.5.5: float division by zero raises CONSTRAINT_ERROR
```

### ada83.c:31643

```
// Peel renames (RM 8.5) and substitute a generic formal subprogram with
```

### ada83.c:31644

```
// the instantiation's actual (RM 12.3). If the resolved target is a
```

### ada83.c:31645

```
// predefined operator (`"-" RENAMES "abs"` etc.), fall through to the
```

### ada83.c:31646

```
// inline-operator path below instead of calling a never-emitted body.
```

### ada83.c:31649

```
// RM 8.5: a user-defined operator renaming a predefined operator with a
```

### ada83.c:31650

```
// DIFFERENT symbol name (FUNCTION "+" (X : NEW_INT) RETURN NEW_INT
```

### ada83.c:31651

```
// RENAMES "-") must apply the renamed target's semantics — the inline
```

### ada83.c:31652

```
// predefined path below reads node->unary.op, so a "+" alias for "-"
```

### ada83.c:31653

```
// would silently emit the identity instead of the negation. Overwrite
```

### ada83.c:31654

```
// the op token from the peeled predefined operator's name (c87b07e).
```

### ada83.c:31672

```
// User-defined unary operator: generate function call (RM 6.7)
```

### ada83.c:31676

```
// Resolve through parent_operation for derived operators (RM 3.4).
```

### ada83.c:31677

```
// parent_operation is orthogonal to renaming; follow it to the ultimate
```

### ada83.c:31678

```
// operation (through any chain of derivations) so we land on the body.
```

### ada83.c:31708

```
// Determine LLVM float type from operand type
```

### ada83.c:31711

```
// determine native integer type for unary operations.
```

### ada83.c:31712

```
// Fixed-point types use integer representation at LLVM level.
```

### ada83.c:31714

```
// A float/fixed negate emits `fsub <float_type>`, so the result is that
```

### ada83.c:31715

```
// float type - not the integer working type. Labeling it as the int type
```

### ada83.c:31716

```
// is a lie that makes downstream consumers mis-widen the register.
```

### ada83.c:31723

```
// Unary negation: 0 - operand. Overflow check for signed types:
```

### ada83.c:31724

```
// -Integer'First overflows on two's complement (RM 4.5).
```

### ada83.c:31730

```
// Modular wrapping for unary minus (RM 4.5.3): -x = modulus - x.                          
```

### ada83.c:31731

```
// For power-of-2 moduli, the sub already wraps correctly.                                 
```

### ada83.c:31732

```
// For non-power-of-2, emit urem to wrap into 0..M-1.                                      
```

### ada83.c:31733

```
//                                                                                          
```

### ada83.c:31751

```
// Modular NOT is bitwise complement modulo M (RM 4.5.6).                                  
```

### ada83.c:31752

```
// For power-of-2 moduli: XOR with (M-1) gives correct masking.                            
```

### ada83.c:31753

```
// For non-power-of-2: same - XOR with (M-1) is the Ada definition.                        
```

### ada83.c:31754

```
//                                                                                          
```

### ada83.c:31760

```
// Dynamic-bound (fat) operand: produce a fat result of runtime length.
```

### ada83.c:31763

```
// The result is a pointer to the freshly built array; return it with
```

### ada83.c:31764

```
// its real (ptr) rep rather than falling through to the common scalar
```

### ada83.c:31765

```
// return, which would relabel the pointer as the operand's int width
```

### ada83.c:31766

```
// and feed a bogus i8 to an array consumer (e.g. NOT A = B).
```

### ada83.c:31769

```
// Boolean NOT: flip as i1, then return at the operand's storage width
```

### ada83.c:31770

```
// (BOOLEAN is i8). result_type is op_type below, so the register must
```

### ada83.c:31771

```
// match it - emitting a bare i1 here labeled i8 is the lie that breaks
```

### ada83.c:31772

```
// honest consumers.
```

### ada83.c:31793

```
// Integer ABS with overflow check: ABS (MIN_INT) overflows.
```

### ada83.c:31794

```
// Use Emit_Overflow_Checked_Op for negation, then select.
```

### ada83.c:31806

```
// .ALL dereference: operand is pointer, load the value
```

### ada83.c:31810

```
// Set by resolution
```

### ada83.c:31811

```
// Fall back: derive the designated type from the operand's access
```

### ada83.c:31812

```
// type if resolution didn't annotate the .ALL node.
```

### ada83.c:31816

```
// For composite types the access value IS the dereferenced value.
```

### ada83.c:31817

```
// Label it by how the access is represented: a fat access yields the
```

### ada83.c:31818

```
// loaded { ptr, ptr } (data + bounds) even when the designated subtype
```

### ada83.c:31819

```
// is statically constrained (the bounds still travel); a thin access
```

### ada83.c:31820

```
// yields a bare data pointer typed by the designated subtype.
```

### ada83.c:31827

```
// For scalar types, load the value from the pointer
```

### ada83.c:31833

```
// no widening at load - value stays at native type width.
```

### ada83.c:31834

```
// Conversions happen at use sites via Emit_Convert.
```

### ada83.c:31845

```
// RM 3.7.2(3) / 6.4.1: every discriminant of the record actual living at
```

### ada83.c:31846

```
// `record_address` must equal the formal subtype's constraint. The expected
```

### ada83.c:31847

```
// value is the dynamic constraint expression when one is recorded (e.g. a
```

### ada83.c:31848

```
// runtime call in NEW PARENT (Ident (T))), otherwise the static constraint
```

### ada83.c:31849

```
// value. Shared by the IN-actual and the by-reference OUT / IN OUT paths, which
```

### ada83.c:31850

```
// once carried byte-identical copies of this loop.
```

### ada83.c:31879

```
// The entry index handed to the rendezvous runtime (RM 9.5.2): the entry's base
```

### ada83.c:31880

```
// index times one thousand, plus the family argument for an entry family.
```

### ada83.c:31881

```
// `family_index` is a value temp at Integer_Arith_Rep, or 0 for a simple entry.
```

### ada83.c:31882

```
// One spelling shared by entry calls and accept/select alternatives.
```

### ada83.c:31896

```
// The bare low..high range inside a family index's discrete-range definition,
```

### ada83.c:31897

```
// which may be written directly (1..10) or as a subtype indication with a range
```

### ada83.c:31898

```
// constraint (ET RANGE DLB..E2). A subtype mark or 'RANGE attribute carries no
```

### ada83.c:31899

```
// explicit range node — the check falls back to the resolved type's bounds.
```

### ada83.c:31905

```
// Widen a scalar into the entry ABI's raw i64 parameter slot. A float's BITS
```

### ada83.c:31906

```
// are preserved — bitcast to a same-width integer, then zero-extended — so the
```

### ada83.c:31907

```
// acceptor reads the same value back (a numeric fptosi would truncate 1.25 to 1,
```

### ada83.c:31908

```
// c95092a); integers and thin accesses sign/zero-extend as usual.
```

### ada83.c:31920

```
// Evaluate an omitted entry parameter's default (RM 6.4.2) and widen it into the
```

### ada83.c:31921

```
// raw i64 slot in the PARAMETER's own representation: a fixed-point default
```

### ada83.c:31922

```
// scales to its mantissa, a float keeps its bits, everything else extends — so
```

### ada83.c:31923

```
// the acceptor reads back the value the formal's type expects (c95092a).
```

### ada83.c:31924

```
// RM 4.3.2(6) / 6.4.1: an aggregate belonging to a constrained array/string
```

### ada83.c:31925

```
// subtype must have, in each dimension, exactly as many elements as the
```

### ada83.c:31926

```
// subtype's constraint requires. The constraint may be non-static — elaborated
```

### ada83.c:31927

```
// once and frozen in the runtime descriptor (RM 3.6.1) — so read those frozen
```

### ada83.c:31928

```
// bounds rather than re-evaluating the (possibly side-effecting) constraint
```

### ada83.c:31929

```
// expression. A dimension covered by an OTHERS choice matches any length and is
```

### ada83.c:31930

```
// exempt. For a parameter default the mismatch is thereby reported when the
```

### ada83.c:31931

```
// aggregate is materialised (at the call), not when the entry is declared.
```

### ada83.c:31932

```
// No-op unless `agg` is an aggregate against a constrained array/string subtype.
```

### ada83.c:31948

```
// Count actual elements: a named range choice L..H spans (H-L+1) of them.
```

### ada83.c:31969

```
// positional
```

### ada83.c:31976

```
// Descend into the first sub-aggregate to measure the next dimension.
```

### ada83.c:31994

```
// OTHERS covers the whole range
```

### ada83.c:32007

```
// A null constraint range yields a negative span; clamp to 0 (RM 3.6.1).
```

### ada83.c:32015

```
// A dynamically-bounded array/string formal is passed as a fat pointer
```

### ada83.c:32016

```
// {data,bounds} that does not fit the i64 ABI slot. Mirror the explicit-actual
```

### ada83.c:32017

```
// path (Generate_Entry_Call): spill the fat pointer and pass its address, which
```

### ada83.c:32018

```
// the acceptor binds the formal to by reference. Without this the 16-byte fat
```

### ada83.c:32019

```
// pointer is truncated into the i64 slot and the acceptor dereferences a
```

### ada83.c:32020

```
// garbage bounds pointer (c42007j CASE_J5: ENTRY E1 (A : STRING(IDENT_INT(2)..5)
```

### ada83.c:32021

```
// := "FIVE") called with the default). A static-bounds array is thin and
```

### ada83.c:32022

```
// widens normally through the scalar path below.
```

### ada83.c:32034

```
// An array/string default's element count is checked against the parameter
```

### ada83.c:32035

```
// subtype at the root (Generate_Aggregate) when the aggregate is generated
```

### ada83.c:32036

```
// just below; only the scalar case needs handling here.
```

### ada83.c:32048

```
// RM 6.4.2: when the default is actually used at a call, its value must
```

### ada83.c:32049

```
// satisfy the parameter's subtype (c95065b: I : INT := -1, INT is 0 .. 63).
```

### ada83.c:32056

```
// RM 9.10(6): emit a blocking entry call. The rendezvous is an abort-completion
```

### ada83.c:32057

```
// point: if an abort was delivered to this task while it was engaged in the
```

### ada83.c:32058

```
// rendezvous, __ada_entry_call returns normally and, on return, the body checks
```

### ada83.c:32059

```
// abort_pending (TCB+61) and branches to its termination epilogue
```

### ada83.c:32060

```
// (task_body_done_label) without running the rest of the body or its handlers.
```

### ada83.c:32061

```
// The flag is read under @__rv_mutex so the abort's store (made under the same
```

### ada83.c:32062

```
// lock) is visible. Outside a task body there is no %__self_tcb; a plain call.
```

### ada83.c:32070

```
// RM 9.10 abort completion point: after returning from a blocking tasking
```

### ada83.c:32071

```
// primitive, a task body checks abort_pending (TCB+61) and branches to its
```

### ada83.c:32072

```
// termination epilogue (task_body_done_label) without running the rest of
```

### ada83.c:32073

```
// the body or its handlers. The flag is read under @__rv_mutex so the
```

### ada83.c:32074

```
// abort's store (made under the same lock) is visible. No-op outside a
```

### ada83.c:32075

```
// task body's statements.
```

### ada83.c:32089

```
// Convert a timed entry call's delay expression to an i64 microsecond budget
```

### ada83.c:32090

```
// (RM 9.7.3). A fixed-point delay is scaled by its 'SMALL to seconds first.
```

### ada83.c:32105

```
// Dispatch an entry call (RM 9.5). Under a conditional or timed select
```

### ada83.c:32106

```
// (entry_call_try_mode, RM 9.7.2/9.7.3) the non-blocking / deadline variant
```

### ada83.c:32107

```
// runs and records its rendezvous flag in entry_call_result_temp; otherwise the
```

### ada83.c:32108

```
// ordinary blocking call. `entry_idx` is widened to the RTS ABI's i64 here.
```

### ada83.c:32112

```
// RM 9.7.3: a timed call's delay is evaluated only now — after the entry
```

### ada83.c:32113

```
// name, family index, and parameters (all folded into task_ptr / entry_idx /
```

### ada83.c:32114

```
// param_block above) — never before them (c97302a).
```

### ada83.c:32134

```
// RM 9.5(6): an entry-family index outside the family's declared discrete range
```

### ada83.c:32135

```
// raises CONSTRAINT_ERROR — checked both at a call (T.E (I)) and at an accept
```

### ada83.c:32136

```
// (ACCEPT E (I)). idx_val is a value temp already at Integer_Arith_Rep.
```

### ada83.c:32154

```
// A subtype mark or 'RANGE: check against the subtype's own bounds.
```

### ada83.c:32159

```
// The task object pointer for an entry call whose prefix is the selected
```

### ada83.c:32160

```
// component T.E (or P.ALL.E for an access-to-task). Returns a ptr temp: the
```

### ada83.c:32161

```
// designated task for access-to-task, the task object's address otherwise, and
```

### ada83.c:32162

```
// a null pointer (meaning the current task) when no task symbol resolves.
```

### ada83.c:32163

```
// `selected_prefix` is the node before the dot, or NULL.
```

### ada83.c:32164

```
// A callee with a library-stable name whose body is unclaimed in this
```

### ada83.c:32165

```
// compilation and whose enclosing package body is a SEPARATE stub lives
```

### ada83.c:32166

```
// in another module: note it, and the post-generation flush declares
```

### ada83.c:32167

```
// whichever notes never got a define here.
```

### ada83.c:32172

```
// Inside a subunit module nearly every callee is external — bodies the
```

### ada83.c:32173

```
// parent chain "claims" are defined in OTHER modules. Elsewhere, only a
```

### ada83.c:32174

```
// callee behind a SEPARATE package-body stub is external.
```

### ada83.c:32192

```
// Register an ___elab function for @main's elaboration sequence.
```

### ada83.c:32193

```
// Arena-grown: a silent cap here would silently skip elaborating a unit.
```

### ada83.c:32207

```
// Low-level append for the SEPARATE-boundary declare flush. Used by the
```

### ada83.c:32208

```
// call-site noting above and directly by stub emission (a stub IS the
```

### ada83.c:32209

```
// boundary; no condition to probe).
```

### ada83.c:32226

```
// Composite prefix — a record component (OBJ.B) or array element (A(I))
```

### ada83.c:32227

```
// naming a task: its symbol (a component, or none) has no standalone
```

### ada83.c:32228

```
// storage, so evaluate the prefix as an expression. A task-typed prefix
```

### ada83.c:32229

```
// yields the TCB directly; an access-typed one yields the access value,
```

### ada83.c:32230

```
// dereferenced below. Package-qualified names (PKG.T) keep their symbol
```

### ada83.c:32231

```
// route: their symbol IS the stored task object.
```

### ada83.c:32247

```
// RM 4.1.3: a parameterless FUNCTION as the task prefix (F.E) is an
```

### ada83.c:32248

```
// implicit call; the call's result IS the task value (the TCB pointer),
```

### ada83.c:32249

```
// or an access value designating the task object (one more load).
```

### ada83.c:32263

```
// Explicit .ALL over an access-returning parameterless FUNCTION
```

### ada83.c:32264

```
// (F.ALL.E, RM 4.1.3): the implicit call's result is the access value;
```

### ada83.c:32265

```
// one load reaches the task object's TCB.
```

### ada83.c:32274

```
// Explicit .ALL: the prefix is NK_UNARY_OP (TK_ALL) over the access variable.
```

### ada83.c:32281

```
// Implicit dereference (RM 4.1.3): the access value designates the
```

### ada83.c:32282

```
// allocated task OBJECT, which itself holds the TCB pointer — so two
```

### ada83.c:32283

```
// loads, not one. Passing the object address as the TCB made the
```

### ada83.c:32284

```
// entry-call dead-task check read garbage and raise TASKING_ERROR.
```

### ada83.c:32285

```
// A null access value passes null through; the entry call raises.
```

### ada83.c:32304

```
// RM 9.1: within its own body, the task type's name denotes the task
```

### ada83.c:32305

```
// currently executing the body (c91004c: `TT1.E1` — a conditional
```

### ada83.c:32306

```
// entry call on self). The type symbol has no storage to load.
```

### ada83.c:32309

```
// A task object variable holds a pointer to its TCB; load it. The entry
```

### ada83.c:32310

```
// queue lives in the TCB, so the caller must pass the TCB value (matching
```

### ada83.c:32311

```
// the acceptor's %__self_tcb), not the address of the variable.
```

### ada83.c:32320

```
// Resolve which formal slot an entry-call actual binds — positional order,
```

### ada83.c:32321

```
// or by name for an NK_ASSOCIATION (NAME => VALUE) — and surface the value
```

### ada83.c:32322

```
// expression. Single source for the marshalling loop and the OUT-parameter
```

### ada83.c:32323

```
// copy-back, which must agree on slot numbering.
```

### ada83.c:32349

```
// Chained apply `f(...)(I)`: the parser left the prefix as a function-call
```

### ada83.c:32350

```
// NK_APPLY whose `.symbol` is the called function. Dispatching on that symbol
```

### ada83.c:32351

```
// would re-emit the call with the wrong arguments (this outer apply's index,
```

### ada83.c:32352

```
// not the original call's args). Strip the symbol so dispatch falls through
```

### ada83.c:32353

```
// to the array-indexing path, which calls Generate_Expression on the prefix
```

### ada83.c:32354

```
// to emit the inner call honestly.
```

### ada83.c:32355

```
// The prefix's symbol is the callee for a call or conversion, and the array
```

### ada83.c:32356

```
// object for an index or slice. Resolve_Apply has already decided which shape
```

### ada83.c:32357

```
// this application is, so we no longer guess from the symbol: a chained
```

### ada83.c:32358

```
// `f(…)(I)` keeps f on the prefix here and is routed to the indexing emitter
```

### ada83.c:32359

```
// by its APPLY_INDEX verdict, which reads the array object off the prefix and
```

### ada83.c:32360

```
// never consults this symbol.
```

### ada83.c:32363

```
// The entry rename a call peeled through, if any: the rename's hidden
```

### ada83.c:32364

```
// locals carry the task pointer and family index bound at elaboration
```

### ada83.c:32365

```
// (RM 8.5); entry_path reads them instead of the call's own syntax.
```

### ada83.c:32370

```
// A call emitted inside a package's library-level elaboration is an
```

### ada83.c:32371

```
// invocation-time dependency on the callee's unit (RM 10.5).
```

### ada83.c:32376

```
// Emit call/apply comment if symbol is known
```

### ada83.c:32392

```
// RM 6.4.2: default expressions belong to the VIEW the call names — for a
```

### ada83.c:32393

```
// generic formal subprogram, the formal's own declared defaults, never the
```

### ada83.c:32394

```
// bound actual's (RM 12.1.3). The rename peel finds the BODY to call; the
```

### ada83.c:32395

```
// named view is kept for the default-argument lookup further down.
```

### ada83.c:32399

```
// A call through a procedure-renames-entry peeled to the entry symbol:
```

### ada83.c:32400

```
// route to the entry-call emitter regardless of the resolver's verdict
```

### ada83.c:32401

```
// (which saw an ordinary procedure call).
```

### ada83.c:32405

```
// Obey Resolve_Apply's verdict. Indexing, slicing, and a stray constrained
```

### ada83.c:32406

```
// subtype indication share the indexed-component emitter; conversion and
```

### ada83.c:32407

```
// entry calls each have their own. An operator falls into the operator region
```

### ada83.c:32408

```
// immediately below and, if its spelling is not a recognised predefined
```

### ada83.c:32409

```
// operator, onward into the call path — the same fall-through an ordinary call
```

### ada83.c:32410

```
// takes. APPLY_UNRESOLVED here is a resolver bug, not a shape to emit.
```

### ada83.c:32424

```
// Predefined operator named as a function: "="(X,Y), "NOT"(X), etc. The
```

### ada83.c:32425

```
// operand on the prefix is either the predefined-operator symbol or a bare
```

### ada83.c:32426

```
// operator-name identifier; either way Resolve_Apply has already settled that
```

### ada83.c:32427

```
// this is an operator. Also entered from below when a formal-subprogram
```

### ada83.c:32428

```
// substitution or a rename peels to a predefined operator — those have no
```

### ada83.c:32429

```
// body to call.
```

### ada83.c:32438

```
// Get first argument (unwrap NK_ASSOCIATION). Note: operand evaluation
```

### ada83.c:32439

```
// is *not* done here for the 2-arg path — the predefined-binary helper
```

### ada83.c:32440

```
// does its own Generate_Expression on left/right, so doing it here would
```

### ada83.c:32441

```
// double-evaluate. The 1-arg path below evaluates its single argument
```

### ada83.c:32442

```
// explicitly because there's no shared unary helper yet.
```

### ada83.c:32450

```
// Stage 2 of dedup: route operator-as-function calls through the same
```

### ada83.c:32451

```
// predefined-binary emitter that `X op Y` uses. The helper owns all the
```

### ada83.c:32452

```
// semantics — overflow checks, slice/fat-pointer comparisons, type
```

### ada83.c:32453

```
// widening, fixed-point, float, universal int128, scalar arith/cmp.
```

### ada83.c:32454

```
// Previously, this site reimplemented a subset of those semantics,
```

### ada83.c:32455

```
// missing overflow checks and slice handling for `P."+"(X,Y)`-style
```

### ada83.c:32456

```
// calls; now the two paths share one implementation.
```

### ada83.c:32464

```
// The call expression's resolved type drives fixed-point rescaling
```

### ada83.c:32465

```
// (RM 4.5.5): the resolver gives a fixed "*"/"/" the concrete operand
```

### ada83.c:32466

```
// type, so prefer it over the operator symbol's nominal universal_fixed
```

### ada83.c:32467

```
// return. Fall back to the symbol's return type for everything else.
```

### ada83.c:32473

```
// The helper returns i1 for comparisons and scalar boolean ops; the
```

### ada83.c:32474

```
// function-call form expects the declared return type (BOOLEAN = i8
```

### ada83.c:32475

```
// in Ada). Widen here so the caller of Generate_Apply sees i8.
```

### ada83.c:32490

```
// Operator name not recognised; fall through to the regular-call path
```

### ada83.c:32491

```
// below. This shouldn't trigger for standard Ada operators.
```

### ada83.c:32493

```
// Unary operators: abs, not, unary -
```

### ada83.c:32500

```
// ABS with overflow check: ABS (MIN_INT) overflows
```

### ada83.c:32516

```
// NOT of a boolean array (RM 4.5.6) complements each component and
```

### ada83.c:32517

```
// yields an array, not a scalar — route it the same way the infix unary
```

### ada83.c:32518

```
// NOT does. Without this the function-notation form "not"(RIGHT => A)
```

### ada83.c:32519

```
// would xor the array's address as an i1 and hand a bogus i8 to an array
```

### ada83.c:32520

```
// consumer.
```

### ada83.c:32538

```
// Unary "+" is identity (RM 4.5.4): yield the operand unchanged rather
```

### ada83.c:32539

```
// than calling a never-emitted predefined-operator body.
```

### ada83.c:32545

```
// Fall through to regular call for unhandled operators
```

### ada83.c:32548

```
// The substituted actual may itself be an entry or a procedure renaming
```

### ada83.c:32549

```
// one (cc1310a: WITH PROCEDURE P3 (I : INTEGER) IS T.ENT2): peel and
```

### ada83.c:32550

```
// route to the entry-call emitter.
```

### ada83.c:32557

```
// A substituted actual or rename that peeled to a predefined operator
```

### ada83.c:32558

```
// (FUNCTION NE (...) RENAMES "/=" bound to a formal): predefined
```

### ada83.c:32559

```
// operators have no body — route through the operator-as-function
```

### ada83.c:32560

```
// emitter instead of calling a never-emitted symbol.
```

### ada83.c:32567

```
// Function call - generate arguments                                                             
```

### ada83.c:32568

```
// For OUT/IN OUT parameters, we need to pass the ADDRESS, not the value.                        
```

### ada83.c:32569

```
// RM 6.2: Scalar and access types are passed by copy (copy-in/copy-out).                        
```

### ada83.c:32570

```
// This prevents aliasing - P(I, I, I) uses independent copies.                                  
```

### ada83.c:32571

```
//                                                                                                
```

### ada83.c:32574

```
// RM 13.10.1: UNCHECKED_DEALLOCATION is an intrinsic generic; an
```

### ada83.c:32575

```
// instance has no body to call. FREE (X): when X is not null, reclaim
```

### ada83.c:32576

```
// its object's storage — crediting the access type's bounded collection
```

### ada83.c:32577

```
// (RM 13.2) — then set X to null. A designated TASK keeps executing
```

### ada83.c:32578

```
// (RM 13.10.1): only the access variable is nulled, its control block
```

### ada83.c:32579

```
// outlives the name (cda101b).
```

### ada83.c:32605

```
// Storage reclaimed: credit the collection by the same measure the
```

### ada83.c:32606

```
// allocator debited — the data byte size, computed from the fat
```

### ada83.c:32607

```
// descriptor's bounds for an unconstrained designated array, or the
```

### ada83.c:32608

```
// designated subtype's static size otherwise.
```

### ada83.c:32642

```
// X := null, unconditionally — an already-null actual stays null.
```

### ada83.c:32650

```
// Arguments marshal into PARAMETER-SLOT order: a named association
```

### ada83.c:32651

```
// (NAME => V) lands in its formal's slot, and defaults fill any
```

### ada83.c:32652

```
// uncovered slot — leading or trailing (RM 6.4.2). The old model
```

### ada83.c:32653

```
// emitted the actuals in list order and appended defaults at the
```

### ada83.c:32654

```
// end, which mis-bound `P (Y => V)` with a leading defaulted X.
```

### ada83.c:32670

```
// Track copy-back info for scalar/access OUT/IN OUT params
```

### ada83.c:32676

```
// Hidden 'CONSTRAINED flag reg per slot (RM 3.7.4), 0 = slot needs none.
```

### ada83.c:32680

```
// RM 8.5 / 12.1.3: formal NAMES and DEFAULTS belong to the view the call
```

### ada83.c:32681

```
// denotes — a renaming (or generic formal subprogram) declares its own,
```

### ada83.c:32682

```
// possibly renamed and re-defaulted relative to the renamed subprogram
```

### ada83.c:32683

```
// (c85013a: PROCA(D => V) names the rename's D, and an omitted C takes
```

### ada83.c:32684

```
// PROCA's default, not PROC1's A default). Modes, types, and the body
```

### ada83.c:32685

```
// stay with the peeled symbol. Shape guard: fall back to the peeled
```

### ada83.c:32686

```
// symbol when the view carries no aligned profile of its own.
```

### ada83.c:32693

```
// Actual expression to evaluate
```

### ada83.c:32694

```
// Index into sym->parameters[]
```

### ada83.c:32696

```
// Handle named association: PARAM_NAME => expression
```

### ada83.c:32700

```
// Look up formal parameter by name — in the VIEW's profile — to get
```

### ada83.c:32701

```
// the correct slot index.
```

### ada83.c:32716

```
// Final slot for this actual: record coverage and the actual node
```

### ada83.c:32717

```
// (the copy-back pass reads the actual's subtype off it).
```

### ada83.c:32723

```
// For ALI-loaded symbols, parameters may be NULL - default to pass by value
```

### ada83.c:32728

```
// OUT/IN OUT: pass address of variable
```

### ada83.c:32733

```
// RM 6.4.1(7): a type conversion as an OUT/IN OUT actual is
```

### ada83.c:32734

```
// equivalent to the conversion of its operand. Peel TYPE(X) so
```

### ada83.c:32735

```
// we take the address of X, not the value of the conversion.
```

### ada83.c:32743

```
// Get the actual's address. Use Emit_Symbol_Storage so uplevel
```

### ada83.c:32744

```
// references (from a nested subprogram into the enclosing
```

### ada83.c:32745

```
// frame) emit %__frame.X instead of %X.
```

### ada83.c:32748

```
// X.ALL of a FAT access value: the access value IS the actual's
```

### ada83.c:32749

```
// descriptor. Evaluate it exactly once — the data slot serves as
```

### ada83.c:32750

```
// the address, and the whole value serves an unconstrained formal
```

### ada83.c:32751

```
// (c64108a: P1 (F1(2).ALL, ...)).
```

### ada83.c:32756

```
// Expanded names (STANDARD.PKG.X) resolve to the same variable
```

### ada83.c:32757

```
// symbol as a bare identifier - take its address directly via
```

### ada83.c:32758

```
// Emit_Symbol_Storage so we don't go through Generate_Composite_Address
```

### ada83.c:32759

```
// (which loads the value instead of the address for scalars).
```

### ada83.c:32778

```
// RM 8.5: pass by-ref through the address captured at the rename's
```

### ada83.c:32779

```
// elaboration, not the rename symbol's own (unused) storage.
```

### ada83.c:32785

```
// RM 6.2: scalar and access types use copy-in/copy-out
```

### ada83.c:32791

```
// Alloca temp for isolated copy
```

### ada83.c:32794

```
// Copy-in: load from actual, check constraint, store to temp.
```

### ada83.c:32795

```
// RM 6.4.1(7): for a type-conversion actual TYPE(X) the value passed
```

### ada83.c:32796

```
// is X converted to the formal subtype, so the check's source type is
```

### ada83.c:32797

```
// the operand X's, not the conversion target's — otherwise a source
```

### ada83.c:32798

```
// equal to the formal subtype makes the range check a no-op and an
```

### ada83.c:32799

```
// out-of-range operand slips through before the call.
```

### ada83.c:32805

```
// RM 6.4.1: constrained access subtype formal — the designated
```

### ada83.c:32806

```
// object's discriminants must match the subtype constraint.
```

### ada83.c:32814

```
// Composite: pass actual address directly (by reference)
```

### ada83.c:32816

```
// A constrained array stored inline as raw data, passed to an
```

### ada83.c:32817

```
// unconstrained-array formal, must be wrapped: the formal is passed by
```

### ada83.c:32818

```
// reference as a pointer to an in-memory fat pointer { data, bounds }.
```

### ada83.c:32819

```
// Build one aliasing the actual and spill it so the callee can load
```

### ada83.c:32820

```
// bounds (RM 6.4.1).
```

### ada83.c:32821

```
//
```

### ada83.c:32822

```
// "Inline" means actual_addr points to flat element data: a static
```

### ada83.c:32823

```
// array, or a dynamically-bounded array embedded in a record or array
```

### ada83.c:32824

```
// (a selected/indexed prefix). A *simple* reference to a fat-stored
```

### ada83.c:32825

```
// object (a variable or by-reference parameter of dynamic-bounds type)
```

### ada83.c:32826

```
// is NOT inline: actual_addr already points to its { data, bounds }
```

### ada83.c:32827

```
// struct, so pass it directly. Wrapping that would alias the data
```

### ada83.c:32828

```
// pointer onto the struct and a callee write would clobber the bounds.
```

### ada83.c:32839

```
// RM 6.4.1 / 3.6.1: when the formal's type mark denotes a constrained
```

### ada83.c:32840

```
// array subtype, the actual must *belong* to that subtype — at each
```

### ada83.c:32841

```
// index position the actual and formal must have the SAME bounds (no
```

### ada83.c:32842

```
// sliding; sliding applies only to assignment, RM 5.2.1). The actual's
```

### ada83.c:32843

```
// subtype is the parameter expression's nominal subtype (arg->type) —
```

### ada83.c:32844

```
// for a type-conversion actual that is the conversion's target, which
```

### ada83.c:32845

```
// already performed its own RM 4.6 re-bounding, not the underlying
```

### ada83.c:32846

```
// object's bounds. When both subtypes are statically bounded the
```

### ada83.c:32847

```
// mismatch is known at compile time; emit an unconditional raise before
```

### ada83.c:32848

```
// the call (the actual is passed by reference, so the value path's
```

### ada83.c:32849

```
// checks never run here).
```

### ada83.c:32875

```
// A slice actual carries its OWN bounds, not the underlying array's:
```

### ada83.c:32876

```
// actual_addr already points at the slice's first element, so the fat
```

### ada83.c:32877

```
// pointer must use the slice range (RM 4.1.2). Without this a slice
```

### ada83.c:32878

```
// passed to an unconstrained formal would claim the whole array.
```

### ada83.c:32886

```
// The dereferenced access value carries the designated array's
```

### ada83.c:32887

```
// own descriptor — pass a spilled copy of it (RM 6.4.1).
```

### ada83.c:32907

```
// A discriminant-dependent component (R.S : STRING (1..N)):
```

### ada83.c:32908

```
// its bounds live in the record instance, and Generate_Selected
```

### ada83.c:32909

```
// already builds the aliasing fat descriptor from the field
```

### ada83.c:32910

```
// address plus the loaded discriminant (RM 3.7.1) — spill that
```

### ada83.c:32911

```
// (c64108a: P1 (R1.S, R2.S, R3.S)).
```

### ada83.c:32938

```
// IN OUT constraint check for composites
```

### ada83.c:32945

```
// RM 6.4.1: for an IN OUT / OUT record formal with constrained
```

### ada83.c:32946

```
// discriminants, the actual's discriminants must match the constraint.
```

### ada83.c:32947

```
// A derived operation forwards to the parent, whose formal is the
```

### ada83.c:32948

```
// unconstrained base, so the actual is passed as the base and carries
```

### ada83.c:32949

```
// no constraint to check (RM 3.4) — use the parent's formal type.
```

### ada83.c:32956

```
// RM 4.8: Propagate target access type to allocator in parameter
```

### ada83.c:32957

```
// context so disc/bounds constraint checks fire (like assignments).
```

### ada83.c:32965

```
// A task-typed IN formal is by reference (Parameter_Passed_By_Reference):
```

### ada83.c:32966

```
// pass the address of the actual's task slot. Generate_Expression
```

### ada83.c:32967

```
// would load the TCB handle — the value form of a task — but the
```

### ada83.c:32968

```
// callee aliases %p as the slot address, and abort/'TERMINATED/
```

### ada83.c:32969

```
// entry calls through the formal all read the handle through it.
```

### ada83.c:32970

```
// Records and arrays already arrive as addresses; the task is the
```

### ada83.c:32971

```
// one by-reference type whose value form differs from its address.
```

### ada83.c:32975

```
// RM 6.4.1 / 4.6 / 4.7: a task type conversion (PARENT (X)) or
```

### ada83.c:32976

```
// qualified expression (T'(X)) actual is a view change denoting the
```

### ada83.c:32977

```
// same task object; take the address of the underlying object,
```

### ada83.c:32978

```
// exactly as the OUT/IN OUT path peels TYPE (X).
```

### ada83.c:32991

```
// A task value with no storage of its own — a function result
```

### ada83.c:32992

```
// like F2 (F1) — gets spilled to a fresh slot whose address is
```

### ada83.c:32993

```
// passed; the slot is the anonymous object holding the result.
```

### ada83.c:33012

```
// IN parameter: check constraint before call (RM 4.6)
```

### ada83.c:33017

```
// The produced value's own rep is authoritative; an AST-type-derived
```

### ada83.c:33018

```
// prediction can disagree when Generate_Expression already coerced
```

### ada83.c:33019

```
// to a context type (e.g. a universal-real expression emitted as
```

### ada83.c:33020

```
// single-precision for a FLOAT formal).
```

### ada83.c:33024

```
// RM 6.4.1: an IN actual of a constrained access subtype must
```

### ada83.c:33025

```
// designate an object whose discriminants or index bounds satisfy
```

### ada83.c:33026

```
// that subtype's constraint, checked before the call.
```

### ada83.c:33030

```
// Discriminant constraint check for a constrained record subtype
```

### ada83.c:33031

```
// (RM 3.7.2(3)): when a formal is e.g. S_TRUE IS VAR_REC (TRUE),
```

### ada83.c:33032

```
// verify the actual's discriminant matches. A derived operation
```

### ada83.c:33033

```
// forwards to the parent whose formal is the unconstrained base, so
```

### ada83.c:33034

```
// the actual carries no constraint to check (RM 3.4).
```

### ada83.c:33040

```
// Constrained array > unconstrained formal: build fat pointer (RM 6.4.1)                 
```

### ada83.c:33041

```
// When passing a constrained array to an unconstrained formal, we must                   
```

### ada83.c:33042

```
// create a fat pointer with the constrained type's bounds.                              
```

### ada83.c:33043

```
//                                                                                        
```

### ada83.c:33053

```
// RM 6.4.1 / 3.6.1: when the formal's type mark denotes a constrained
```

### ada83.c:33054

```
// array subtype, the actual must belong to it — at each index position
```

### ada83.c:33055

```
// the actual and formal must have the SAME bounds (no sliding; sliding
```

### ada83.c:33056

```
// applies only to assignment, RM 5.2.1). When both subtypes are
```

### ada83.c:33057

```
// statically bounded the mismatch is known at compile time and the
```

### ada83.c:33058

```
// fat-pointer checks below never run (neither operand is fat), so emit
```

### ada83.c:33059

```
// the raise here for an unconditional static mismatch.
```

### ada83.c:33081

```
// Constrained array to unconstrained formal: build fat pointer.
```

### ada83.c:33082

```
// Generate_Expression returns ptr for constrained arrays; wrap                           
```

### ada83.c:33083

```
// with the type's static bounds for the unconstrained formal.                           
```

### ada83.c:33084

```
// But if the expression already produces a fat pointer (e.g.                            
```

### ada83.c:33085

```
// string literals, slices), skip the wrapping.                                          
```

### ada83.c:33086

```
//                                                                                        
```

### ada83.c:33088

```
// Use the PRODUCED rep, not the AST prediction: a value can already
```

### ada83.c:33089

```
// be a fat pointer at runtime (concat, slice, string literal, a
```

### ada83.c:33090

```
// dynamic boolean-array op) while its declared type's Llvm form is
```

### ada83.c:33091

```
// "ptr". Re-wrapping such a value treats the fat struct as a data
```

### ada83.c:33092

```
// ptr.
```

### ada83.c:33095

```
// Wrap the constrained actual's data pointer in a fat pointer
```

### ada83.c:33096

```
// carrying its bounds, across every dimension. Emit_Fat_Pointer_For_Lvalue
```

### ada83.c:33097

```
// owns the static and rt-elaborated cases alike.
```

### ada83.c:33105

```
// Fat pointer actual (string literal, slice) to constrained
```

### ada83.c:33106

```
// formal with fat-pointer storage (dyn bounds): rebuild fat
```

### ada83.c:33107

```
// pointer with the formal type's bounds, across every dimension.
```

### ada83.c:33108

```
// E.g. "ABCDE" passed to STRING (11..15) - bounds must be 11..15,
```

### ada83.c:33109

```
// not the literal's default 1..5 (RM 4.3.2); a multidimensional
```

### ada83.c:33110

```
// formal needs every dimension's pair, not just the first.
```

### ada83.c:33122

```
// RM 6.4.1: the actual must belong to the constrained formal's
```

### ada83.c:33123

```
// subtype - each dimension's length must match (bounds themselves
```

### ada83.c:33124

```
// slide). Check before re-anchoring to the formal's bounds, which
```

### ada83.c:33125

```
// would otherwise mask a length mismatch.
```

### ada83.c:33138

```
// Fat pointer actual to a statically-constrained formal (flat ptr).
```

### ada83.c:33139

```
// RM 6.4.1: the actual must belong to the formal's subtype — each
```

### ada83.c:33140

```
// dimension's length must match (the bounds themselves slide).
```

### ada83.c:33141

```
// Check against the formal's static bounds before discarding the
```

### ada83.c:33142

```
// actual's bounds with the data-pointer extract.
```

### ada83.c:33159

```
// Use the produced value's own rep (see arg_llvm above); the AST
```

### ada83.c:33160

```
// prediction can disagree after a context coercion.
```

### ada83.c:33163

```
// Real literal > fixed-point param: scale by 1/SMALL
```

### ada83.c:33175

```
// For derived type operations (RM 3.4), emit direct call to the parent's
```

### ada83.c:33176

```
// implementation. Derived types share the parent's representation in Ada 83,
```

### ada83.c:33177

```
// so no wrapper is needed; the front end inserts the parameter/result
```

### ada83.c:33178

```
// conversions (RM 3.4(17-20)). Follow the inheritance chain to the ultimate
```

### ada83.c:33179

```
// operation that actually has a body (mirrors GNAT's Ultimate_Alias): a
```

### ada83.c:33180

```
// multi-level derivation S is new T is new P chains derived op -> derived op
```

### ada83.c:33181

```
// -> real body, and only the last carries code.
```

### ada83.c:33184

```
// RM 3.9: access-before-elaboration check on the subprogram actually
```

### ada83.c:33185

```
// invoked. Ultimate_Operation follows a generic formal / derived-operation
```

### ada83.c:33186

```
// chain to the body that owns the flag (c39006b), so check it, falling back
```

### ada83.c:33187

```
// to the name when the chain does not carry one.
```

### ada83.c:33191

```
// RM 13.10.2 / GNAT: UNCHECKED_CONVERSION is an intrinsic generic.
```

### ada83.c:33192

```
// Each instantiation creates a SYMBOL_FUNCTION with no body.
```

### ada83.c:33193

```
// Lower the call inline as a bit-reinterpret instead of emitting
```

### ada83.c:33194

```
// an external call. Pattern: alloca buffer wide enough for both
```

### ada83.c:33195

```
// src and dst, store src into it, load dst out. Works for any
```

### ada83.c:33196

```
// scalar/access/composite-of-equal-size combination because
```

### ada83.c:33197

```
// LLVM's untyped load/store reinterprets bits.
```

### ada83.c:33217

```
// When the target is wider than the source, the surplus bytes would
```

### ada83.c:33218

```
// otherwise read uninitialized stack. Zero the buffer first so the padding
```

### ada83.c:33219

```
// is deterministic (RM 13.10.2) — two conversions of equal values then
```

### ada83.c:33220

```
// compare equal, and no result depends on prior stack contents.
```

### ada83.c:33225

```
// Source is a composite (array/record). For array sources with
```

### ada83.c:33226

```
// fat-pointer storage, args[0] is a {ptr, ptr} value — extract
```

### ada83.c:33227

```
// the data pointer before memcpy. For flat-storage composites,
```

### ada83.c:33228

```
// args[0] is already a ptr to the data.
```

### ada83.c:33239

```
// An enumeration with a representation clause stores its position; its
```

### ada83.c:33240

```
// observable representation is the code (RM 13.3). Fold to the code
```

### ada83.c:33241

```
// before reinterpreting the bits so an unchecked conversion out of the
```

### ada83.c:33242

```
// type sees the specified internal value.
```

### ada83.c:33250

```
// Array dst with fat-pointer storage: wrap buf as { data, {lo,hi} }
```

### ada83.c:33251

```
// using the dst type's known bounds so the caller (typically an
```

### ada83.c:33252

```
// assignment or comparison) sees a proper fat pointer value
```

### ada83.c:33253

```
// instead of a bare data pointer.
```

### ada83.c:33283

```
// Flat-storage composite dst (constrained static array, record):
```

### ada83.c:33284

```
// return the ptr to the buffer; caller will memcpy it.
```

### ada83.c:33288

```
// An unchecked conversion INTO an enumeration with a representation clause
```

### ada83.c:33289

```
// receives the internal code; fold it back to the stored position so the
```

### ada83.c:33290

```
// result is a well-formed value (the inverse of the source fold above).
```

### ada83.c:33297

```
// Check if calling a nested function (transitively inside another subprogram)
```

### ada83.c:33300

```
// Precompute static chain pointer before building call (RM 8.3)
```

### ada83.c:33304

```
// Check if callee is a BIP function (returns limited type)
```

### ada83.c:33306

```
// Destination for BIP result
```

### ada83.c:33308

```
// For BIP functions, allocate space for the result
```

### ada83.c:33311

```
// Default for opaque types
```

### ada83.c:33317

```
// Trailing IN-mode default arguments (RM 6.4.2): when the call omits
```

### ada83.c:33318

```
// actuals for formals that have a default expression, evaluate the
```

### ada83.c:33319

```
// defaults here, at the call site, in formal order. Pre-compute before
```

### ada83.c:33320

```
// the call instruction text so the default expressions' IR does not
```

### ada83.c:33321

```
// interleave with the call's argument list.
```

### ada83.c:33322

```
// Fill every uncovered slot from its default (RM 6.4.2) — leading
```

### ada83.c:33323

```
// or trailing; a named association may skip any defaulted formal.
```

### ada83.c:33324

```
// Defaults come from the VIEW's profile, like the name lookup above.
```

### ada83.c:33329

```
// not reachable for a legal call
```

### ada83.c:33335

```
// A task-typed default is by reference like every task formal:
```

### ada83.c:33336

```
// pass the address of the default object's task slot
```

### ada83.c:33337

```
// (Parameter_Passed_By_Reference).
```

### ada83.c:33352

```
// An unconstrained-array formal is passed by value as a fat
```

### ada83.c:33353

```
// pointer (matching the non-default actual path); the formal's
```

### ada83.c:33354

```
// LLVM type is {ptr, ptr}, not a pointer to one.
```

### ada83.c:33365

```
// Coerce from the value's PRODUCED rep, not the AST prediction,
```

### ada83.c:33366

```
// which can disagree (e.g. a small-int default at base width).
```

### ada83.c:33372

```
// RM 12.3.6 / 6.4.1: a default value used for an omitted actual must,
```

### ada83.c:33373

```
// like an explicit actual, satisfy the ACTUAL subprogram's parameter
```

### ada83.c:33374

```
// subtype. For a generic formal subprogram or a renaming, that subtype
```

### ada83.c:33375

```
// lives on the peeled symbol (sym) and can differ from the view's
```

### ada83.c:33376

```
// declared subtype the default expression was written against —
```

### ada83.c:33377

```
// cc1311b defaults a value of the formal's subtype into an actual whose
```

### ada83.c:33378

```
// parameter subtype is narrower and expects CONSTRAINT_ERROR. Mirror
```

### ada83.c:33379

```
// the explicit-actual checks, keyed to sym's profile rather than pt.
```

### ada83.c:33391

```
// Precompute the hidden 'CONSTRAINED flag (RM 3.7.4) for each by-reference
```

### ada83.c:33392

```
// mutable formal, before the call-instruction text so the flag's own IR
```

### ada83.c:33393

```
// does not interleave with the argument list.
```

### ada83.c:33404

```
// BIP functions return void - result is built into destination
```

### ada83.c:33416

```
// Pass frame pointer to nested functions (RM 8.3 static chain)
```

### ada83.c:33423

```
// BIP extra arguments: allocation form and destination pointer
```

### ada83.c:33427

```
// Determine allocation form from context:                                                    
```

### ada83.c:33428

```
// - has_target = true (we allocate stack space above)                                        
```

### ada83.c:33429

```
// - is_allocator = false (not a new expression)                                              
```

### ada83.c:33430

```
// - in_return_stmt = false (not in return context)                                           
```

### ada83.c:33431

```
//                                                                                            
```

### ada83.c:33434

```
// is_allocator, in_return_stmt, has_target
```

### ada83.c:33438

```
// Pass task formals if callee's return type has task components
```

### ada83.c:33441

```
// Pass master and chain (0 for now - full tasking support later)
```

### ada83.c:33447

```
// Arguments in PARAMETER order — actuals and defaults interleave by
```

### ada83.c:33448

```
// slot (RM 6.4.2).
```

### ada83.c:33470

```
// Hidden 'CONSTRAINED flags (RM 3.7.4), appended in parameter order to
```

### ada83.c:33471

```
// match the callee's signature.
```

### ada83.c:33481

```
// no actual (unreachable for a legal call)
```

### ada83.c:33485

```
// For BIP functions, the result is now at bip_dest.
```

### ada83.c:33489

```
// A task result is a TCB pointer STORED into the buffer (a "BIP scalar
```

### ada83.c:33490

```
// return"), not a composite object built at the buffer's address. Load the
```

### ada83.c:33491

```
// handle so the value is the task itself — otherwise callers treat the
```

### ada83.c:33492

```
// buffer address as the TCB and 'CALLABLE/'TERMINATED/entry calls read the
```

### ada83.c:33493

```
// wrong memory.
```

### ada83.c:33500

```
// Copy-out: for scalar/access OUT/IN OUT, copy from temp back to actual.
```

### ada83.c:33501

```
// This only runs on normal return - exceptions skip via longjmp (RM 6.2).
```

### ada83.c:33504

```
// The actual expression behind this slot, with NK_ASSOCIATION
```

### ada83.c:33505

```
// (NAME => VALUE) peeled. Default-filled slots have no actual and
```

### ada83.c:33506

```
// nothing to copy back.
```

### ada83.c:33511

```
// Scalar/access copy-back
```

### ada83.c:33515

```
// RM 6.4.1(17): on return from OUT/IN OUT scalar param, the
```

### ada83.c:33516

```
// value must satisfy the actual's subtype constraint. Apply
```

### ada83.c:33517

```
// check before storing back.
```

### ada83.c:33519

```
// RM 6.4.1(7): a type-conversion actual TYPE(X) copies back to its
```

### ada83.c:33520

```
// operand X, so the return value is checked against X's subtype, not
```

### ada83.c:33521

```
// the conversion target's. Peel the conversion to reach the operand.
```

### ada83.c:33528

```
// RM 12.3 (shared-generic view): when the callee's formal parameter
```

### ada83.c:33529

```
// is of a generic formal PRIVATE type, its body saw only the
```

### ada83.c:33530

```
// constraint-free private view, so no scalar range check exists on
```

### ada83.c:33531

```
// the copy-back — a value read from an external file that lies
```

### ada83.c:33532

```
// outside the actual subtype yields DATA_ERROR-or-nothing, never
```

### ada83.c:33533

```
// CONSTRAINT_ERROR (ce2205b, ce2405a).
```

### ada83.c:33539

```
// RM 6.4.1: on return through an access OUT/IN OUT actual of a
```

### ada83.c:33540

```
// constrained access subtype, the copied-back value's designated
```

### ada83.c:33541

```
// object must satisfy that subtype's discriminant/bounds constraint.
```

### ada83.c:33548

```
// Composite by-ref: check the returned value against the actual's
```

### ada83.c:33549

```
// subtype (RM 6.4.1(17)). A by-reference call writes straight through to
```

### ada83.c:33550

```
// the actual, so a constrained discriminated actual must still satisfy
```

### ada83.c:33551

```
// its discriminant constraint after the call (the callee may have
```

### ada83.c:33552

```
// assigned a value of another variant).
```

### ada83.c:33569

```
// return value stays at native type width.                                                    
```

### ada83.c:33570

```
// No widening to INTEGER - callers use Emit_Convert at use sites.                             
```

### ada83.c:33571

```
// Track the actual LLVM type of the call result so that Emit_Convert                           
```

### ada83.c:33572

```
// at use sites (e.g. return statements) knows the true generated type.                        
```

### ada83.c:33573

```
//                                                                                              
```

### ada83.c:33576

```
// For fat pointer returns (dynamic array), copy the data from the                            
```

### ada83.c:33577

```
// callee's stack to a local alloca so it survives. The callee's                             
```

### ada83.c:33578

```
// alloca is freed on return, making the data pointer dangling.                              
```

### ada83.c:33579

```
//                                                                                            
```

### ada83.c:33585

```
// Extract data and bounds pointers
```

### ada83.c:33588

```
// Compute data size from bounds. Byte size is i64 with per-dimension
```

### ada83.c:33589

```
// lengths derived at the index width (RM 3.6.1) instead of a hardcoded
```

### ada83.c:33590

```
// `mul i32` that mismatches CHARACTER-/LONG_INTEGER-indexed arrays.
```

### ada83.c:33594

```
// Allocate local buffer and copy data
```

### ada83.c:33598

```
// Also copy bounds to a local alloca. The bounds struct is
```

### ada83.c:33599

```
// [2 * dimension_count x bound_type]; its byte width derives from the
```

### ada83.c:33600

```
// index width (RM 3.6.1), not a hardcoded 4 bytes - so CHARACTER- (i8)
```

### ada83.c:33601

```
// and LONG_INTEGER-indexed (i64) arrays copy the right number of bytes.
```

### ada83.c:33609

```
// Rebuild fat pointer with local copies
```

### ada83.c:33617

```
// Entry call (task rendezvous): pack parameters, call the entry, wait for the
```

### ada83.c:33618

```
// accept to complete. Reached by dispatch (APPLY_ENTRY_CALL) with the entry
```

### ada83.c:33619

```
// symbol on `sym`.
```

### ada83.c:33625

```
// Check if this is an entry family - first argument is family index, not a parameter
```

### ada83.c:33629

```
// Index of first actual parameter in arguments
```

### ada83.c:33631

```
// Call through an entry rename: every argument is a parameter — the
```

### ada83.c:33632

```
// call's syntax never carried a family index. The index (if the renamed
```

### ada83.c:33633

```
// name had one) was evaluated when the renaming was elaborated (RM 8.5)
```

### ada83.c:33634

```
// and waits in the rename's hidden local.
```

### ada83.c:33644

```
// Direct call: the resolver flattened index+parameters into one
```

### ada83.c:33645

```
// argument list, so the first argument is the family index — widen to
```

### ada83.c:33646

```
// i32 for index arithmetic.
```

### ada83.c:33651

```
// Skip family index when processing parameters
```

### ada83.c:33654

```
// RM 9.5(6): a family index outside the entry's declared range raises
```

### ada83.c:33655

```
// CONSTRAINT_ERROR before the call is queued.
```

### ada83.c:33659

```
// Allocate the parameter block (excluding any family index). It must hold
```

### ada83.c:33660

```
// every formal, not just the arguments supplied, so a call that omits
```

### ada83.c:33661

```
// defaulted parameters still gives the acceptor a full block to read and
```

### ada83.c:33662

```
// leaves room for the default values filled in below (RM 6.4.2).
```

### ada83.c:33667

```
// RM 3.7.4: mutable OUT/IN OUT formals carry a hidden 'CONSTRAINED flag.
```

### ada83.c:33668

```
// The rendezvous shares the caller's block by pointer, so the flags ride in
```

### ada83.c:33669

```
// extra i64 slots appended after the parameters; the acceptor reads them at
```

### ada83.c:33670

```
// the same indices. Count them so the block is sized to hold them.
```

### ada83.c:33679

```
// The parameter block (and every argument temp built into it) lives
```

### ada83.c:33680

```
// only until the copy-backs below finish; scope it so an entry call
```

### ada83.c:33681

```
// inside a loop does not grow the frame every iteration.
```

### ada83.c:33691

```
// Store arguments into parameter block (skip family index for entry families).
```

### ada83.c:33692

```
// Each actual may be NK_ASSOCIATION (NAME => VALUE); reorder by matching
```

### ada83.c:33693

```
// the choice name against the VIEW's formal parameter list — through a
```

### ada83.c:33694

```
// renaming, the rename's own formal names govern (RM 8.5, c85018a).
```

### ada83.c:33695

```
// OUT / IN OUT scalar actuals get their values copied back from the
```

### ada83.c:33696

```
// parameter block after the rendezvous (RM 9.5, 6.4). Capture each
```

### ada83.c:33697

```
// actual's address now, before the call.
```

### ada83.c:33719

```
// An access-to-unconstrained formal is a fat pointer (16 B) marshalled
```

### ada83.c:33720

```
// through the spilled-and-addressed slot, like a fat array; its value is
```

### ada83.c:33721

```
// read back from the spill on return, not from the raw i64 slot.
```

### ada83.c:33729

```
// RM 6.4.1(7): a type conversion as an OUT/IN OUT actual is the
```

### ada83.c:33730

```
// conversion of its operand — the copy-back writes to the OPERAND, not
```

### ada83.c:33731

```
// the (address-less) conversion. Peel TYPE (X) to X so the write lands
```

### ada83.c:33732

```
// in real storage and the return constraint check is against X's own
```

### ada83.c:33733

```
// subtype; taking Generate_Lvalue of the conversion itself yields a
```

### ada83.c:33734

```
// null address and the copy-back stores through it (segfault).
```

### ada83.c:33748

```
// RM 6.4.1(7): an OUT-mode conversion's pre-call value is never
```

### ada83.c:33749

```
// read, so the conversion itself must not be evaluated on the way
```

### ada83.c:33750

```
// in — its range check would raise on a stale out-of-range operand.
```

### ada83.c:33751

```
// Marshal the bare operand's current value instead.
```

### ada83.c:33759

```
// RM 6.4.1: constrained access-subtype entry-formal — check the
```

### ada83.c:33760

```
// designated object's discriminants against the subtype constraint.
```

### ada83.c:33761

```
// Only for IN / IN OUT: an OUT formal's pre-call value is not read,
```

### ada83.c:33762

```
// so the actual need not satisfy the constraint on the way in.
```

### ada83.c:33768

```
// RM 6.4.1: an IN / IN OUT scalar actual is checked against the
```

### ada83.c:33769

```
// ENTRY's formal subtype before the rendezvous — through a rename
```

### ada83.c:33770

```
// this is the renamed entry's profile (RM 8.5), never the rename's
```

### ada83.c:33771

```
// own, because `sym` here is the peeled entry.
```

### ada83.c:33782

```
// RM 6.4.1: a constrained-array actual must match the entry formal's
```

### ada83.c:33783

```
// bounds exactly — a rendezvous does not slide. Static bounds compare
```

### ada83.c:33784

```
// at compile time; a mismatch raises CONSTRAINT_ERROR at the call.
```

### ada83.c:33812

```
// Marshal the argument into its i64 slot. An unconstrained-array actual
```

### ada83.c:33813

```
// is a fat pointer {data, bounds} (16 bytes) that does not fit a slot;
```

### ada83.c:33814

```
// spill it and store the ADDRESS, which the acceptor binds the formal to
```

### ada83.c:33815

```
// by reference (a constrained actual is first widened to a fat pointer).
```

### ada83.c:33816

```
// Everything else — a scalar, a thin access, or a by-reference composite
```

### ada83.c:33817

```
// already passed by its address — widens to i64.
```

### ada83.c:33823

```
// An access-to-unconstrained actual is itself a fat pointer {data,bounds}
```

### ada83.c:33824

```
// and does not fit an i64 slot either; spill it and pass its address, so
```

### ada83.c:33825

```
// the acceptor binds the formal to the same storage and the caller reads
```

### ada83.c:33826

```
// the (possibly updated) access value back on return (RM 6.2/6.4.1).
```

### ada83.c:33831

```
// RM 6.4.1: a record actual must BELONG to the formal's constrained
```

### ada83.c:33832

```
// subtype — each discriminant equal to the constraint — checked at the
```

### ada83.c:33833

```
// call, before any rendezvous (c95085b: TSK1.E ((IDENT_INT(2),"AA"))
```

### ada83.c:33834

```
// and TSK1.E (R) with R.N /= 3 raise CONSTRAINT_ERROR in the caller).
```

### ada83.c:33847

```
// RM 6.4.1: an array actual's BOUNDS must equal a statically-
```

### ada83.c:33848

```
// constrained formal's — a rendezvous does not slide — checked in
```

### ada83.c:33849

```
// the caller, before any rendezvous (c95085c: "AB" for STRING(1..3),
```

### ada83.c:33850

```
// and dynamically-bounded variables for static formals, all raise
```

### ada83.c:33851

```
// CONSTRAINT_ERROR). A fat actual carries its bounds; a flat one
```

### ada83.c:33852

```
// reads them from its subtype's (possibly runtime) bounds.
```

### ada83.c:33905

```
// Fill the default value for each formal the call omitted (RM 6.4.2): the
```

### ada83.c:33906

```
// default expression is evaluated at the point of the call, once per
```

### ada83.c:33907

```
// call. Through a renaming the RENAME's own defaults govern (RM 8.5);
```

### ada83.c:33908

```
// profiles correspond by position (c85018a: ENTA's C := 5 default, not
```

### ada83.c:33909

```
// the renamed entry's A := 1).
```

### ada83.c:33922

```
// Store the hidden 'CONSTRAINED flags (RM 3.7.4) into the trailing slots,
```

### ada83.c:33923

```
// in formal order, matching the acceptor's read indices. The actual for a
```

### ada83.c:33924

```
// formal is found by the same choice-matching the parameter loop used.
```

### ada83.c:33931

```
// Locate the actual bound to formal p (positional or named).
```

### ada83.c:33943

```
// defaulted formal: fresh object, unconstrained
```

### ada83.c:33954

```
// Get task object (from prefix if it's a selected component like Task_Obj.Entry).
```

### ada83.c:33955

```
// For access-to-task (P.E1), load the pointer to get the designated task.                     
```

### ada83.c:33956

```
// For .ALL dereference (P.ALL.E1), unwrap NK_UNARY_OP (TK_ALL) to get access var.             
```

### ada83.c:33957

```
//                                                                                              
```

### ada83.c:33961

```
// Call through an entry rename: the task object was determined when
```

### ada83.c:33962

```
// the renaming was elaborated (RM 8.5) — read it back, never
```

### ada83.c:33963

```
// re-evaluate the prefix.
```

### ada83.c:33970

```
// Entry call with no task prefix — e.g. the entry was passed as a generic
```

### ada83.c:33971

```
// actual subprogram and is called by its formal name. The target is the
```

### ada83.c:33972

```
// entry's enclosing single-task object (the entry symbol's parent). The
```

### ada83.c:33973

```
// old global rendezvous queue matched by entry index alone, so a missing
```

### ada83.c:33974

```
// target went unnoticed; the per-task queue needs the real TCB.
```

### ada83.c:33982

```
// Entry index, combining the base with the family argument when present.
```

### ada83.c:33985

```
// Call runtime entry call function - extend entry index to i64 for RTS ABI
```

### ada83.c:33988

```
// Copy OUT / IN OUT scalars back from the parameter block (the acceptor
```

### ada83.c:33989

```
// wrote them before completing the rendezvous). On a timed/conditional
```

### ada83.c:33990

```
// call that did not rendezvous, the block still holds the value stored
```

### ada83.c:33991

```
// above, so the store is a self-assignment.
```

### ada83.c:33995

```
// A fat access was spilled: the slot holds the spill address, and the
```

### ada83.c:33996

```
// acceptor updated the fat pointer there in place. Load the returned
```

### ada83.c:33997

```
// access value from the spill, check its designated object against the
```

### ada83.c:33998

```
// actual's subtype (RM 6.4.1), then store it into the actual.
```

### ada83.c:34014

```
// RM 6.4.1: the value copied back is checked against the ACTUAL's
```

### ada83.c:34015

```
// subtype (the entry's formal subtype governed the way in). A scalar
```

### ada83.c:34016

```
// checks its range; a constrained access-subtype actual checks the
```

### ada83.c:34017

```
// copied-back value's designated object against its discriminant or
```

### ada83.c:34018

```
// index-bounds constraint, exactly as a subprogram copy-back does.
```

### ada83.c:34035

```
// Indexed component or slice: A (I) or A (L .. H), with implicit dereference
```

### ada83.c:34036

```
// of an access-to-array prefix (RM 4.1(3)). Reached by dispatch, so the
```

### ada83.c:34037

```
// verdict is APPLY_INDEX, APPLY_SLICE, or a stray APPLY_SUBTYPE_CONSTRAINT;
```

### ada83.c:34038

```
// the slice/index split below is by argument shape. Type conversions can no
```

### ada83.c:34039

```
// longer arrive here — they were routed to the conversion emitter directly —
```

### ada83.c:34040

```
// so the old "check conversion before indexing" guard is gone.
```

### ada83.c:34043

```
// Type to use for indexing
```

### ada83.c:34046

```
// Handle implicit dereference: A(I) where A is access-to-array
```

### ada83.c:34053

```
// For chained applies (call-then-index), the prefix's symbol is the
```

### ada83.c:34054

```
// called function — irrelevant here; treat as un-symboled and route the
```

### ada83.c:34055

```
// prefix value through Generate_Expression below.
```

### ada83.c:34059

```
// A parameterless function as the prefix must be called to yield the array
```

### ada83.c:34060

```
// (or access) value, not loaded as if it were an object (RM 4.1.3).
```

### ada83.c:34065

```
// For index checks
```

### ada83.c:34067

```
// bound type when has_dynamic_low
```

### ada83.c:34069

```
// Load the access value then use as base.                                                     
```

### ada83.c:34070

```
// For access-to-unconstrained-array, the access value is a fat                                 
```

### ada83.c:34071

```
// pointer { ptr, ptr } and we must extract data + bounds.                                     
```

### ada83.c:34072

```
//                                                                                              
```

### ada83.c:34074

```
// Aligned with Type_To_Rep and Type_Needs_Fat_Pointer so the load
```

### ada83.c:34075

```
// type, Val claim, and convert-time predictions all agree on whether
```

### ada83.c:34076

```
// the access value is fat (unconstrained or dynamic-bounds designated).
```

### ada83.c:34096

```
// base is the data pointer already extracted from the fat value;
```

### ada83.c:34097

```
// null-check it directly as a plain pointer.
```

### ada83.c:34100

```
// Evaluate the prefix as an expression — uniform over access
```

### ada83.c:34101

```
// variables, renames/aliases, and access-returning parameterless
```

### ada83.c:34102

```
// functions (implicit call, RM 4.1.3).
```

### ada83.c:34108

```
// Check if unconstrained array OR constrained array with dynamic bounds
```

### ada83.c:34109

```
// needing fat pointer handling. Both are stored as fat pointers.
```

### ada83.c:34114

```
// Load fat pointer and extract data pointer and low bound
```

### ada83.c:34126

```
// Constrained array - get direct pointer to data
```

### ada83.c:34132

```
// Complex prefix (e.g., array field of indexed record)
```

### ada83.c:34137

```
// A fat-pointer value (slice, unconstrained/dynamic array, or fat
```

### ada83.c:34138

```
// function result) carries its data pointer and bounds inline and must
```

### ada83.c:34139

```
// be unpacked before indexing. Drive off the value's actual rep, not the
```

### ada83.c:34140

```
// static array_type — a slice of a constrained array reads as constrained
```

### ada83.c:34141

```
// yet still produces a { ptr, { bound, bound } } value.
```

### ada83.c:34142

```
//
```

### ada83.c:34153

```
// Constrained array - the expression result is the base pointer.
```

### ada83.c:34154

```
// An access value indexed as a prefix (e.g. X(I)(J), where X(I) is an
```

### ada83.c:34155

```
// access-to-array element loaded at its scalar rep) arrives as an
```

### ada83.c:34156

```
// integer, not a pointer; reinterpret it as the base address.
```

### ada83.c:34162

```
// Check for slice: ARR (low..high)
```

### ada83.c:34165

```
// Array slice - return fat pointer {ptr, {low, high}}                                          
```

### ada83.c:34166

```
// Slice bounds are absolute indices into the source array.                                    
```

### ada83.c:34167

```
// The fat pointer stores: {data_ptr_at_slice_start, {slice_low, slice_high}}                   
```

### ada83.c:34168

```
//                                                                                              
```

### ada83.c:34174

```
// Generate slice bounds (the logical bounds of the slice). Normalise to
```

### ada83.c:34175

```
// the index arithmetic type so a small index subtype (e.g. i8) mixes
```

### ada83.c:34176

```
// correctly with the array's own bounds in the check and offset below.
```

### ada83.c:34182

```
// RM 3.5: a slice written `A (S RANGE L..H)` also requires the range
```

### ada83.c:34183

```
// L..H to be compatible with the subtype mark S, independent of whether
```

### ada83.c:34184

```
// L..H lies within the array's own bounds (checked below).
```

### ada83.c:34187

```
// RM 4.1.2: for a non-null slice A(L..H), L and H must each be
```

### ada83.c:34188

```
// in A'FIRST..A'LAST.
```

### ada83.c:34211

```
// Compute zero-based offset for slice start
```

### ada83.c:34212

```
// offset = (slice_low - array_low_bound) * elem_size
```

### ada83.c:34227

```
// Compute data pointer at slice start
```

### ada83.c:34238

```
// Build fat pointer with slice bounds using helper
```

### ada83.c:34247

```
// Linearize the (possibly multi-dimensional) index into a flat element
```

### ada83.c:34248

```
// index. idx_iat is retained for the element-address arithmetic below.
```

### ada83.c:34254

```
// Get pointer to element and load. If the array Type_Info was built
```

### ada83.c:34255

```
// without its `element_type` populated, recover from the indexed-access
```

### ada83.c:34256

```
// expression's own resolved type (the value type of `arr(i)` IS the
```

### ada83.c:34257

```
// element type).
```

### ada83.c:34268

```
// Element stored as fat pointer { ptr, ptr } (dynamic-bound or
```

### ada83.c:34269

```
// unconstrained array/string component) - load the pair.
```

### ada83.c:34278

```
// Composite element - use byte array for getelementptr
```

### ada83.c:34283

```
// Return pointer to composite element (don't load)
```

### ada83.c:34292

```
// no widening at load - value stays at native type width.
```

### ada83.c:34293

```
// Conversions happen at use sites via Emit_Convert.
```

### ada83.c:34298

```
// Type conversion: Type_Name (Expression)
```

### ada83.c:34301

```
// Type conversion: TYPE_NAME (expression) per RM 4.6.
```

### ada83.c:34302

```
// Handles scalar, array, and record conversions.
```

### ada83.c:34307

```
// Get source and destination types
```

### ada83.c:34311

```
// RM 4.6: converting between a task type and a related task type (derived
```

### ada83.c:34312

```
// <-> parent) is a view change only — a task object is its TCB pointer, so
```

### ada83.c:34313

```
// the value passes through unchanged. Falling into the scalar path below
```

### ada83.c:34314

```
// would misinterpret the pointer as an integer and corrupt it.
```

### ada83.c:34319

```
// Array type conversions (RM 4.6(24)):
```

### ada83.c:34320

```
// Constrained>Unconstrained: wrap data+bounds into fat pointer.                             
```

### ada83.c:34321

```
// Unconstrained>Constrained: extract data (bounds checked at runtime).                      
```

### ada83.c:34322

```
// Same representation: pass through.                                                        
```

### ada83.c:34323

```
//                                                                                            
```

### ada83.c:34330

```
// RM 4.6: the operand and target component subtypes must carry the same
```

### ada83.c:34331

```
// constraint; otherwise the conversion raises CONSTRAINT_ERROR.
```

### ada83.c:34335

```
// Check if the source expression actually produces a fat pointer value.
```

### ada83.c:34336

```
// This covers: unconstrained parameters/variables, function calls returning
```

### ada83.c:34337

```
// unconstrained arrays, slices, concatenations, string literals, etc.
```

### ada83.c:34338

```
//
```

### ada83.c:34341

```
// Whether the destination is stored as a fat pointer. Constrained
```

### ada83.c:34342

```
// arrays with DYNAMIC bounds (e.g., T NEW PARENT(IDENT_INT(5)..IDENT_INT(7)))
```

### ada83.c:34343

```
// use fat-pointer storage even though dst_unc is false.
```

### ada83.c:34346

```
// Flat alloca > fat pointer destination: build { data, bounds } over
```

### ada83.c:34347

```
// every dimension from the source's own bounds (a multidimensional
```

### ada83.c:34348

```
// source would otherwise keep only its first dimension's pair).
```

### ada83.c:34352

```
// Fat pointer > flat alloca destination: extract data pointer.
```

### ada83.c:34358

```
// Both fat: an array conversion shares the operand's data (the
```

### ada83.c:34359

```
// component type is unchanged) but must produce a fat pointer whose
```

### ada83.c:34360

```
// bounds match the target type (RM 4.6). The bound source depends on
```

### ada83.c:34361

```
// whether the target is constrained.
```

### ada83.c:34372

```
// Unconstrained target: the result carries the operand's own
```

### ada83.c:34373

```
// bounds, converted to the target index type. RM 4.6 also requires
```

### ada83.c:34374

```
// that, for each non-null dimension, both result bounds belong to
```

### ada83.c:34375

```
// the target's index subtype, or CONSTRAINT_ERROR is raised.
```

### ada83.c:34397

```
// When the bound reps already match there is nothing to rebuild.
```

### ada83.c:34400

```
// Constrained target: the result takes the target subtype's own
```

### ada83.c:34401

```
// bounds, and a per-dimension length check guards the conversion
```

### ada83.c:34402

```
// (RM 4.6) - CONSTRAINT_ERROR when the operand length differs.
```

### ada83.c:34417

```
// Same representation (both fat or both flat): pass through.
```

### ada83.c:34422

```
// Actual emitted rep of `result`, tracked so the scalar return below can
```

### ada83.c:34423

```
// reconcile it to the destination even on the same-type pass-through,
```

### ada83.c:34424

```
// where the resolver narrowed the operand's node type to the conversion
```

### ada83.c:34425

```
// target but the value was still emitted at the wider base-type width.
```

### ada83.c:34428

```
// RM 4.6: a conversion to a constrained record/private subtype checks that
```

### ada83.c:34429

```
// the operand's discriminants equal the target subtype's constraint
```

### ada83.c:34430

```
// (REC3 (R) with R.D = 1 and REC3 = REC (3) raises CONSTRAINT_ERROR).
```

### ada83.c:34436

```
// RM 4.6: a conversion to a constrained access subtype checks a non-null
```

### ada83.c:34437

```
// operand's designated object against the constraint (index bounds or
```

### ada83.c:34438

```
// discriminants); null converts freely (c46054a).
```

### ada83.c:34443

```
// Special handling for fixed-point conversions (RM 4.6)
```

### ada83.c:34444

```
// Fixed-point uses scaled integer representation: value = integer * SMALL
```

### ada83.c:34447

```
// Fixed > Float: convert integer to float, multiply by SMALL
```

### ada83.c:34454

```
// Float > Fixed: divide by SMALL, round to the nearest mantissa.
```

### ada83.c:34455

```
// Rounding (not truncation) repairs the representation error of the
```

### ada83.c:34456

```
// quotient (2.0 / 1e-5 = 199999.999...).
```

### ada83.c:34458

```
// Produced rep, not the AST prediction (which can be a stale
```

### ada83.c:34459

```
// generic-formal width); the SMALL divisor runs at the value's real
```

### ada83.c:34460

```
// float width.
```

### ada83.c:34469

```
// RM 4.6
```

### ada83.c:34472

```
// Integer > Fixed (RM 4.6): mantissa = value / SMALL, exactly:
```

### ada83.c:34473

```
// with SMALL = num/den that is value * den / num in 128-bit.
```

### ada83.c:34478

```
// Produced rep, not the AST prediction (which can be a stale
```

### ada83.c:34479

```
// generic-formal or overflow-checked width); the rescale must label
```

### ada83.c:34480

```
// `result` at the width it was physically emitted.
```

### ada83.c:34485

```
// RM 4.6
```

### ada83.c:34488

```
// Fixed > Fixed (different SMALL): rescale the mantissa by
```

### ada83.c:34489

```
// SMALL_src/SMALL_dst, exactly, in 128-bit.
```

### ada83.c:34502

```
// RM 4.6: the rescaled mantissa must satisfy the target subtype's
```

### ada83.c:34503

```
// range (a tighter range over the same SMALL is otherwise unchecked).
```

### ada83.c:34507

```
// Fixed > Integer (RM 4.6): convert via float, scaling by SMALL.
```

### ada83.c:34508

```
// Fixed-point storage is scaled (value = stored * SMALL); integer
```

### ada83.c:34509

```
// result is the rounded real value, so descale first.
```

### ada83.c:34517

```
// Stored scaled integer > double > * SMALL > round > fptosi
```

### ada83.c:34534

```
// Types differ - need to convert.
```

### ada83.c:34535

```
// Use signedness-aware conversion: zext for unsigned src/dst.
```

### ada83.c:34536

```
// The generated value's actual emitted rep is authoritative; an
```

### ada83.c:34537

```
// AST-type-derived prediction can disagree - e.g. a
```

### ada83.c:34538

```
// subtraction emitted at i32 whose node type the resolver narrowed to
```

### ada83.c:34539

```
// the conversion's target type, or an unconstrained-array value that
```

### ada83.c:34540

```
// is fat when the prediction says thin. Converting from anything but
```

### ada83.c:34541

```
// the value's real rep emits a cast that mislabels the operand.
```

### ada83.c:34550

```
// RM 4.6: a conversion to a scalar subtype checks the result against
```

### ada83.c:34551

```
// the subtype unconditionally. Pass no source subtype so the
```

### ada83.c:34552

```
// "source range ⊆ target range" skip cannot suppress it: an operand
```

### ada83.c:34553

```
// nominally of the target type can still hold an out-of-subtype value
```

### ada83.c:34554

```
// (e.g. INT'FIRST - 1, evaluated in INT'BASE), which must raise
```

### ada83.c:34555

```
// CONSTRAINT_ERROR. A full-base target still short-circuits inside.
```

### ada83.c:34560

```
// Same-type conversion to a constrained scalar subtype still checks the
```

### ada83.c:34561

```
// value (RM 4.6): base-type arithmetic can produce a result outside the
```

### ada83.c:34562

```
// nominally identical subtype. A full-base target short-circuits inside.
```

### ada83.c:34567

```
// Reconcile to the destination rep. Covers the same-type pass-through:
```

### ada83.c:34568

```
// the value's real rep can be wider than dst's (base-type arithmetic on
```

### ada83.c:34569

```
// a narrow derived/subtype operand), and returning it mislabeled emits a
```

### ada83.c:34570

```
// store/use at the wrong width.
```

### ada83.c:34585

```
// Generate code for A.B (selected component)
```

### ada83.c:34590

```
// Handle explicit .ALL dereference (RM 4.1)
```

### ada83.c:34595

```
// Generate_Lvalue returns the loaded pointer (address of designated)
```

### ada83.c:34599

```
// For composite types (records, arrays), return pointer with rep matching
```

### ada83.c:34600

```
// the value produced by Generate_Lvalue: a fat access yields the loaded
```

### ada83.c:34601

```
// { ptr, ptr } (data + bounds), so tag it fat even when the designated
```

### ada83.c:34602

```
// subtype is statically constrained (the bounds still travel). A thin
```

### ada83.c:34603

```
// access yields a bare data pointer typed by the designated subtype.
```

### ada83.c:34610

```
// For scalar types, load the value
```

### ada83.c:34614

```
// no widening at load - value stays at native type width.
```

### ada83.c:34615

```
// Conversions happen at use sites via Emit_Convert.
```

### ada83.c:34619

```
// Handle implicit dereference: R.C where R is access-to-record (RM 4.1(3)).
```

### ada83.c:34620

```
// The designated subtype may be an incomplete type completed after the access
```

### ada83.c:34621

```
// type was declared, so unwrap to the underlying record first.
```

### ada83.c:34631

```
// Resolve generic formal types to their actual types (RM 12.3), then unwrap
```

### ada83.c:34632

```
// through the parent_type chain.
```

### ada83.c:34635

```
// Package-qualified name - use the resolved symbol via Generate_Identifier
```

### ada83.c:34636

```
// This handles named numbers, constants, variables, and literals properly
```

### ada83.c:34640

```
// Create a temporary identifier node to reuse Generate_Identifier logic
```

### ada83.c:34650

```
// Record field access - find component by name
```

### ada83.c:34661

```
// Resolve generic formal field types to their actual so we load at
```

### ada83.c:34662

```
// the right width (e.g., FIXED_ELEMENT formal -> CHECK_TYPE actual
```

### ada83.c:34663

```
// with 'SIZE USE 7 -> i8 load, not i32).
```

### ada83.c:34666

```
// Runtime discriminant check for variant component access (RM 3.7.3)                             
```

### ada83.c:34667

```
// If accessing a component that belongs to a variant, verify the                                 
```

### ada83.c:34668

```
// discriminant value matches the variant's expected value.                                      
```

### ada83.c:34669

```
//                                                                                                
```

### ada83.c:34675

```
// Load the discriminant that GOVERNS this variant part (RM 3.7.3): the one
```

### ada83.c:34676

```
// named in the variant part, not necessarily the first discriminant
```

### ada83.c:34677

```
// (`CASE E IS ...` when D precedes E). Falls back to the first.
```

### ada83.c:34687

```
// Get base address of record for discriminant check.                                          
```

### ada83.c:34688

```
// For implicit dereference, load the pointer to get the record address.                       
```

### ada83.c:34689

```
// For direct records, get the storage address via Generate_Lvalue.                            
```

### ada83.c:34690

```
//                                                                                              
```

### ada83.c:34705

```
// Single value: equality check
```

### ada83.c:34710

```
// Range: check disc_val in [low..high]
```

### ada83.c:34723

```
// RM 3.7.3(6): a WHEN OTHERS variant is present iff the discriminant
```

### ada83.c:34724

```
// matches NONE of the sibling variants' choices. Accessing a component
```

### ada83.c:34725

```
// of the OTHERS variant when the discriminant DOES match a sibling
```

### ada83.c:34726

```
// raises CONSTRAINT_ERROR (c41304b: VR.Y after VR:=(DISC=>1, X=>3) —
```

### ada83.c:34727

```
// Y lives in the OTHERS arm but DISC=1 puts us in the X arm).
```

### ada83.c:34756

```
// Compute field address directly using already-resolved record_type.                            
```

### ada83.c:34757

```
// We avoid calling Generate_Lvalue (node) here because it would                                  
```

### ada83.c:34758

```
// need to re-resolve generic formal types and could lead to infinite                             
```

### ada83.c:34759

```
// recursion when the prefix type is a generic formal private type.                              
```

### ada83.c:34760

```
//                                                                                                
```

### ada83.c:34768

```
// Find component index for runtime offset lookup
```

### ada83.c:34773

```
// Discriminant-dependent array/string component: data is stored inline                           
```

### ada83.c:34774

```
// in the record, so we must construct a fat pointer from the field                               
```

### ada83.c:34775

```
// address and bounds derived from the discriminant (RM 3.7.1).                                  
```

### ada83.c:34776

```
//                                                                                                
```

### ada83.c:34787

```
// RT-elaborated array in record: build fat ptr from type globals.
```

### ada83.c:34788

```
// Multi-dim layout: [lo0, hi0, lo1, hi1, ...]
```

### ada83.c:34798

```
// Build fat pointer: { ptr data, ptr bounds }
```

### ada83.c:34799

```
// Bounds layout: [lo0, hi0, lo1, hi1, ...] as flat array.
```

### ada83.c:34805

```
// The record base is `base` directly — reconstructing it as ptr minus the
```

### ada83.c:34806

```
// STATIC byte_offset would be wrong for a component after a capless one,
```

### ada83.c:34807

```
// whose field pointer sits at a runtime offset the static value no longer
```

### ada83.c:34808

```
// matches. The discriminants are read from this base to derive the bounds.
```

### ada83.c:34814

```
// A bound that names a discriminant of THIS record is read from the
```

### ada83.c:34815

```
// record's discriminant field; any other dynamic bound (a variable or
```

### ada83.c:34816

```
// expression, e.g. ARR (INIT .. D)) is evaluated directly. The previous
```

### ada83.c:34817

```
// code assumed every symbol-bearing bound was a discriminant and, on a
```

### ada83.c:34818

```
// non-discriminant, fell back to offset 0 — reading the wrong field.
```

### ada83.c:34822

```
// A non-disc bound frozen at the record type's elaboration
```

### ada83.c:34823

```
// (RM 3.7.1): read the stored value, never re-evaluate.
```

### ada83.c:34844

```
// Construct fat pointer { data_ptr, bounds_ptr }
```

### ada83.c:34848

```
// Non-disc-dependent: load fat pointer from memory
```

### ada83.c:34852

```
// Other composite types (records, constrained arrays): rep follows
```

### ada83.c:34853

```
// Type_To_Rep so a dynamic-bounds-constrained array component is honestly
```

### ada83.c:34854

```
// labelled LL_REP_FAT, not silently downgraded to LL_REP_PTR.
```

### ada83.c:34858

```
// For access-type components, load without converting to i64.
```

### ada83.c:34859

```
// Access-to-unconstrained-array / access-to-dyn-bound-array values
```

### ada83.c:34860

```
// are themselves fat pointers { ptr, ptr } (16 bytes), so load with
```

### ada83.c:34861

```
// the matching width.
```

### ada83.c:34872

```
// no widening at load - value stays at native type width.
```

### ada83.c:34873

```
// Conversions happen at use sites via Emit_Convert.
```

### ada83.c:34877

```
// Check if a type bound can be evaluated at compile time.                                         
```

### ada83.c:34878

```
// Returns true if the bound is compile-time known, false otherwise.                               
```

### ada83.c:34879

```
// Per GNAT's sem_eval.ads, compile-time known is broader than static -                             
```

### ada83.c:34880

```
// it includes values that can be determined at compile time even if                                
```

### ada83.c:34881

```
// they technically involve non-static expressions.                                                
```

### ada83.c:34882

```
//                                                                                                  
```

### ada83.c:34887

```
// Returns true if not NaN
```

### ada83.c:34895

```
// Try to evaluate expression bound at compile time
```

### ada83.c:34898

```
// Not NaN
```

### ada83.c:34903

```
// Check if a type bound is explicitly set (not default/unset)
```

### ada83.c:34909

```
// Emit implementation-defined float limit: is_low=true > -FLT/DBL_MAX, else +FLT/DBL_MAX.         
```

### ada83.c:34910

```
// Per Ada RM 3.5.7, unconstrained types use at least ±SAFE_LARGE.                                 
```

### ada83.c:34911

```
// LLVM requires 64-bit double hex format for float constants.                                     
```

### ada83.c:34912

```
//                                                                                                  
```

### ada83.c:34918

```
// Single-precision: emit via bitcast i32 > float
```

### ada83.c:34921

```
// -FLT_MAX = 0xFF7FFFFF, +FLT_MAX = 0x7F7FFFFF
```

### ada83.c:34926

```
// Double-precision: emit via fadd double 0.0, 0x<hex>
```

### ada83.c:34929

```
// -DBL_MAX
```

### ada83.c:34930

```
// +DBL_MAX
```

### ada83.c:34938

```
// Get dimension index from attribute argument (1-based, default 1)
```

### ada83.c:34940

```
// Default to first dimension
```

### ada83.c:34942

```
// RM 3.6.2: the dimension is a static universal_integer expression. Fold a
```

### ada83.c:34943

```
// named number or static constant (e.g. N : constant := 2) to its value;
```

### ada83.c:34944

```
// otherwise it is not a literal 1, so default to the first dimension.
```

### ada83.c:34950

```
// Emit an array bound that is a dynamic expression: evaluate it, widen to the
```

### ada83.c:34951

```
// index arithmetic rep, and re-tag as `%t` with an attribute comment.
```

### ada83.c:34963

```
// Emit an array bound that is a static integer (or the 0 fallback).
```

### ada83.c:34971

```
// Emit T'FIRST or T'LAST - unified handler for both bound attributes.
```

### ada83.c:34972

```
// is_low=true > FIRST (low_bound), is_low=false > LAST (high_bound).                              
```

### ada83.c:34973

```
// For arrays: runtime bounds via fat pointer, or static from index_type.                          
```

### ada83.c:34974

```
// For floats: runtime BOUND_EXPR, static float, or implementation limit.                          
```

### ada83.c:34975

```
// For scalars: static integer or runtime BOUND_EXPR.                                              
```

### ada83.c:34976

```
//                                                                                                  
```

### ada83.c:34981

```
// The LLVM type the bound is actually emitted at, so the caller labels it
```

### ada83.c:34982

```
// honestly (array bounds are stored at the bound width, not the index Ada
```

### ada83.c:34983

```
// type's width). Defaults to the integer working type; set per branch.
```

### ada83.c:34993

```
// return bound at native type width, no widening.
```

### ada83.c:34994

```
// Use dim-aware accessor to support multi-dimensional arrays.
```

### ada83.c:35005

```
// Dynamic bound expression (e.g., TYPE AA IS ARRAY (SNI,..) where SNI has dynamic range)
```

### ada83.c:35013

```
// Fallback: emit 0
```

### ada83.c:35024

```
// Float type - determine LLVM type, then emit bound
```

### ada83.c:35041

```
// Prefix type's LLVM width, the rep consumers read the bound at.
```

### ada83.c:35042

```
// Inside generic instantiations, resolve through the base type chain to
```

### ada83.c:35043

```
// pick up the actual type's native width (e.g., BOOLEAN > i8).
```

### ada83.c:35049

```
// Fixed-point FIRST/LAST returns scaled integer representation.
```

### ada83.c:35050

```
// Internal repr = abstract_value / SMALL, in the base type's units (the
```

### ada83.c:35051

```
// representation SMALL), matching how fixed objects are stored.
```

### ada83.c:35082

```
// No declared range: the type spans its full mantissa width
```

### ada83.c:35083

```
// (e.g. DURATION'FIRST/'LAST on the bare 64-bit DURATION).
```

### ada83.c:35108

```
// The magnitude of a fixed-point subtype's range expressed as an i64 mantissa
```

### ada83.c:35109

```
// in units of SMALL: max(|low|, |high|) / SMALL. Used by 'MANTISSA and 'FORE
```

### ada83.c:35110

```
// when the range is non-static (its bounds are built from an expression such
```

### ada83.c:35111

```
// as IDENT_INT, so they cannot be folded at compile time).
```

### ada83.c:35130

```
// RM 3.5.10(5): T'MANTISSA for a non-static fixed-point range, evaluated at
```

### ada83.c:35131

```
// runtime. MANTISSA = ceiling(log2(max_magnitude / SMALL)), computed exactly
```

### ada83.c:35132

```
// from the mantissa M as the bit width of M - 1 (the count-leading-zeros
```

### ada83.c:35133

```
// complement), with a floor of 1.
```

### ada83.c:35146

```
// RM 3.5.10(5): T'FORE for a non-static fixed-point range, evaluated at
```

### ada83.c:35147

```
// runtime. FORE is one (for the sign) plus the number of decimal digits in the
```

### ada83.c:35148

```
// integer part of the largest magnitude. The integer part is the mantissa
```

### ada83.c:35149

```
// shifted by the binary scale (SMALL = 2**scale); its digit count is found by
```

### ada83.c:35150

```
// a divide-by-ten loop.
```

### ada83.c:35166

```
// digit count loop: nd starts at 1, divide intpart by 10 while >= 10.
```

### ada83.c:35190

```
// RM 3.5.10(7): T'LARGE for a non-static fixed-point range, evaluated at
```

### ada83.c:35191

```
// runtime as (2**MANTISSA - 1) * SMALL, returned as a double (universal_real).
```

### ada83.c:35213

```
// Extract a 'VALUE string argument's data pointer and length from its fat
```

### ada83.c:35214

```
// pointer and strip leading and trailing blanks (RM 3.5.5) via the runtime
```

### ada83.c:35215

```
// space-counting helpers, which this marks as needed.
```

### ada83.c:35236

```
// diagnostics and IR comments
```

### ada83.c:35243

```
// If the attribute prefix is T'BASE, resolve BASE after generic substitution.                   
```

### ada83.c:35244

```
// T'BASE with T=C (constrained subtype) should use C's unconstrained base type.                 
```

### ada83.c:35245

```
// Without this, T'BASE'FIRST/LAST incorrectly uses the subtype's bounds (RM 3.3.3).             
```

### ada83.c:35246

```
//                                                                                                
```

### ada83.c:35252

```
// For subtypes of generic formals (SUBTYPE S IS T where T is a formal),                          
```

### ada83.c:35253

```
// resolve through the base chain to find the actual type for classification.                    
```

### ada83.c:35254

```
// prefix_type keeps subtype bounds (for FIRST/LAST); classify_type has the                       
```

### ada83.c:35255

```
// actual type kind (for IMAGE, VALUE, etc.) (RM 12.3).                                          
```

### ada83.c:35256

```
//                                                                                                
```

### ada83.c:35259

```
// Implicit dereference for access-to-array (RM 4.1(3))
```

### ada83.c:35260

```
// But NOT for type-level attributes (RM 13.7.2) or the RM 13.7.2 /
```

### ada83.c:35261

```
// 13.7.3 storage-layout attributes 'FIRST_BIT / 'LAST_BIT / 'POSITION
```

### ada83.c:35262

```
// which do not evaluate the prefix's designated object (c41402a).
```

### ada83.c:35266

```
// RM 4.1(9): dereferencing a null access raises CONSTRAINT_ERROR.
```

### ada83.c:35267

```
// The attribute is defined in terms of the designated object, so it
```

### ada83.c:35268

```
// must be evaluated before the attribute is applied — emit the check
```

### ada83.c:35269

```
// here rather than deep inside each attribute path (c41401a's 'FIRST,
```

### ada83.c:35270

```
// 'LAST, 'LENGTH, 'RANGE, 'CALLABLE, 'TERMINATED all need it).
```

### ada83.c:35281

```
// Determine if prefix needs runtime bounds (fat pointer)
```

### ada83.c:35294

```
// ─── Array/Scalar Bound Attributes: unified FIRST/LAST ─────────────────────────────────────────
```

### ada83.c:35296

```
// 'FIRST/'LAST have the index (or scalar) subtype as their type. The bound
```

### ada83.c:35297

```
// helper emits at varying widths (i32 static, bound-type runtime); coerce to
```

### ada83.c:35298

```
// node->type's width (e.g. i8 for a narrow index) instead of feeding an
```

### ada83.c:35299

```
// i32 bound to an i8-typed consumer.
```

### ada83.c:35316

```
// A parameterless-function prefix (e.g. V'LENGTH where V is a function
```

### ada83.c:35317

```
// returning an array) must be called, not loaded as a variable.
```

### ada83.c:35321

```
// Complex prefix expression - generate it to get fat pointer value
```

### ada83.c:35327

```
// 'LENGTH is INTEGER (universal). The length is computed at the
```

### ada83.c:35328

```
// bound width (len_bt may be i8 for a narrow index), so widen to the
```

### ada83.c:35329

```
// integer working type to match the returned label and INTEGER
```

### ada83.c:35330

```
// semantics - returning Val(len, i32) on an i8 register would lie.
```

### ada83.c:35342

```
// Dynamic length: generate high - low + 1 at runtime
```

### ada83.c:35349

```
// Copy to result temp t
```

### ada83.c:35363

```
// Range attribute - for general expression contexts, return low bound.
```

### ada83.c:35364

```
// For loops handle RANGE specially in Generate_For_Loop.
```

### ada83.c:35370

```
// A parameterless-function prefix must be called, not loaded.
```

### ada83.c:35374

```
// Complex prefix expression - generate it to get fat pointer value
```

### ada83.c:35379

```
// return RANGE low at native type width, no widening.
```

### ada83.c:35380

```
// Use dim-aware accessor for multi-dimensional arrays.
```

### ada83.c:35402

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:35403

```
// Size and Representation Attributes                                                             
```

### ada83.c:35404

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:35406

```
// 'SIZE returns size in bits.                                                                   
```

### ada83.c:35407

```
// Use specified_bit_size if a SIZE clause was given (exact value),                               
```

### ada83.c:35408

```
// otherwise compute from byte size.                                                             
```

### ada83.c:35409

```
//                                                                                                
```

### ada83.c:35415

```
// RM 13.7.2: the prefix of X'SIZE is an object, and the object must be
```

### ada83.c:35416

```
// evaluated even if the size is deducible from its type (c41403a:
```

### ada83.c:35417

```
// OBJ_ARA(F(1))'SIZE must call F). Force the evaluation for complex
```

### ada83.c:35418

```
// prefixes whose evaluation the type-based size computation would
```

### ada83.c:35419

```
// otherwise skip.
```

### ada83.c:35424

```
// An array's size is its element count times the component size. The stored
```

### ada83.c:35425

```
// type->size already captures this for a statically-bounded subtype, but two
```

### ada83.c:35426

```
// cases need more (RM 13.7.2): an *unconstrained* base has no single size, so
```

### ada83.c:35427

```
// report its index subtype's maximum extent; a *dynamically* bounded subtype
```

### ada83.c:35428

```
// (or object thereof) must be measured at run time from its actual bounds.
```

### ada83.c:35436

```
// The extent in elements: for an unconstrained base it is the product of
```

### ada83.c:35437

```
// the index *subtypes'* lengths (the largest a value may grow to); for a
```

### ada83.c:35438

```
// dynamically-bounded subtype it is the product of the actual index
```

### ada83.c:35439

```
// lengths. Either way each dimension's bounds come from a Type_Bound pair.
```

### ada83.c:35442

```
// Try a fully static fold first (no IR), then fall back to runtime.
```

### ada83.c:35460

```
// Runtime, object prefix: read the length from the value's own fat
```

### ada83.c:35461

```
// pointer, whose bounds were established from the object (the same
```

### ada83.c:35462

```
// source 'LENGTH uses), then multiply by the component size. This is the
```

### ada83.c:35463

```
// only correct source when an index bound references a discriminant,
```

### ada83.c:35464

```
// which has no meaning without the object. A type prefix (a type name or
```

### ada83.c:35465

```
// T'BASE) has no object, so it evaluates the index bounds below instead.
```

### ada83.c:35486

```
// Runtime: product of per-dimension lengths times component bits.
```

### ada83.c:35539

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:35540

```
// Address Attribute                                                                              
```

### ada83.c:35541

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:35543

```
// Generate address of prefix object
```

### ada83.c:35545

```
// RM 13.7.2: 'ADDRESS on an indexed or selected component (or any complex
```

### ada83.c:35546

```
// prefix beyond a bare name) is defined in terms of the DESIGNATED
```

### ada83.c:35547

```
// storage — the array element / record component. Its evaluation must
```

### ada83.c:35548

```
// therefore evaluate the prefix, including any function-call index that
```

### ada83.c:35549

```
// sits inside it (c41403a: OBJ_ARA(F(2))'ADDRESS must call F). Route
```

### ada83.c:35550

```
// through Generate_Lvalue so the index expression runs.
```

### ada83.c:35561

```
// A USE-visible alias denotes the original entity — follow it so an
```

### ada83.c:35562

```
// RM 13.5 overlay's address_cell is seen.
```

### ada83.c:35566

```
// Label address - use blockaddress for code location
```

### ada83.c:35577

```
// Subprogram address - use function pointer directly
```

### ada83.c:35584

```
// Package/generic address - use a global marker
```

### ada83.c:35591

```
// Mark that we need to emit this global marker
```

### ada83.c:35597

```
// Task type address - use a global marker
```

### ada83.c:35608

```
// Overlay object (RM 13.5 address clause): its address IS the value
```

### ada83.c:35609

```
// stored in the hidden cell, not the (nonexistent) own storage.
```

### ada83.c:35617

```
// Variable/task address - emit via uplevel predicate
```

### ada83.c:35630

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:35631

```
// Enumeration Attributes                                                                         
```

### ada83.c:35632

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:35634

```
// T'POS (x) - position of enumeration value (RM 3.5.5).                                         
```

### ada83.c:35635

```
// POS returns universal_integer - convert to Integer_Arith_Rep,
```

### ada83.c:35636

```
// the universal-integer working width.
```

### ada83.c:35637

```
//                                                                                                
```

### ada83.c:35646

```
// T'VAL (n) - value at position n (RM 3.5.5(5))
```

### ada83.c:35647

```
// Raises CONSTRAINT_ERROR if n is outside T'BASE range
```

### ada83.c:35653

```
// Get BASE TYPE bounds for range check (RM 3.5.5(5))
```

### ada83.c:35669

```
// Range check at i32 width (argument is integer)
```

### ada83.c:35676

```
// T'VAL returns at type T's width - convert from argument type.
```

### ada83.c:35677

```
// Resolve generic formal types to their actuals.
```

### ada83.c:35688

```
// T'SUCC (x) / T'PRED (x) - RM 3.5.5: operates on base type.
```

### ada83.c:35689

```
// SUCC adds 1 and checks against high bound; PRED subtracts 1 and checks low.
```

### ada83.c:35695

```
// Operate in i64 to avoid overflow when SUCC (LAST) or PRED (FIRST)
```

### ada83.c:35696

```
// would wrap in the native type width (e.g. i32 INTEGER).
```

### ada83.c:35702

```
// Resolve root base type for 'PRED/'SUCC bound check (RM 3.5.5(6)):                          
```

### ada83.c:35703

```
// Result must be in T'BASE range, which for derived types                                    
```

### ada83.c:35704

```
// includes ALL values of the parent type's base range.                                      
```

### ada83.c:35705

```
//                                                                                            
```

### ada83.c:35708

```
// Bound check: SUCC vs high, PRED vs low
```

### ada83.c:35716

```
// Convert result back to prefix type width.
```

### ada83.c:35717

```
// Resolve generic formal types to their actuals.
```

### ada83.c:35726

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:35727

```
// Scalar Type Attributes                                                                         
```

### ada83.c:35728

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:35730

```
// T'MIN (a, b) / T'MAX (a, b) - minimum / maximum of two values.
```

### ada83.c:35731

```
// The two differ only in the comparison sense: MIN keeps the left operand
```

### ada83.c:35732

```
// when it is the smaller (slt), MAX when it is the greater (sgt).
```

### ada83.c:35741

```
// Select the chosen operand using icmp and select
```

### ada83.c:35753

```
// T'ABS (x) with overflow check: ABS (MIN_INT) overflows
```

### ada83.c:35768

```
// T'MOD - returns the modulus of a modular type (RM 3.5.4(17))
```

### ada83.c:35778

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:35779

```
// String/Image Attributes                                                                        
```

### ada83.c:35780

```
// These call runtime functions for string conversion.                                           
```

### ada83.c:35781

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:35783

```
// T'IMAGE (x) - string representation (RM 3.5.5)
```

### ada83.c:35791

```
// Integer'IMAGE - widen to INTEGER arith type for RTS ABI
```

### ada83.c:35796

```
// Character'IMAGE - convert to i8 for RTS ABI
```

### ada83.c:35802

```
// Boolean'IMAGE - switch on 0>"FALSE", 1>"TRUE" (RM 3.5.5)
```

### ada83.c:35816

```
// FALSE case
```

### ada83.c:35824

```
// TRUE case
```

### ada83.c:35832

```
// End
```

### ada83.c:35834

```
// merge point restores flow
```

### ada83.c:35843

```
// Float'IMAGE -- the runtime helper takes a double, so widen a
```

### ada83.c:35844

```
// single-precision argument (a `digits 6` type lowers to float).
```

### ada83.c:35850

```
// Enumeration'IMAGE - return literal name as string
```

### ada83.c:35853

```
// Find root enumeration type with literals
```

### ada83.c:35863

```
// Generate inline switch for literal lookup.
```

### ada83.c:35864

```
// Length stored in STRING bound type (derived from type system).
```

### ada83.c:35874

```
// Widen arg to i64 for switch - arg may be i8 for small enums
```

### ada83.c:35882

```
// Generate case labels
```

### ada83.c:35886

```
// Generate string constant name
```

### ada83.c:35890

```
// Character literals: IMAGE returns 'x' with quotes preserved.
```

### ada83.c:35891

```
// Identifier literals: IMAGE returns uppercased name. (RM 3.5.5)
```

### ada83.c:35894

```
// Character literal - emit as-is (e.g. 'a')
```

### ada83.c:35903

```
// Identifier - uppercase (Ada identifiers are case-insensitive)
```

### ada83.c:35917

```
// Default case - return empty string
```

### ada83.c:35923

```
// merge point restores flow
```

### ada83.c:35929

```
// Build fat pointer result: { ptr_load, { 1, len_load } }
```

### ada83.c:35933

```
// No literals found, fallback to integer image
```

### ada83.c:35940

```
// Default: treat as integer
```

### ada83.c:35948

```
// 'IMAGE no arg
```

### ada83.c:35952

```
// T'VALUE (s) - parse string to type (RM 3.5.5)
```

### ada83.c:35957

```
// The runtime takes a FAT string; a statically constrained argument
```

### ada83.c:35958

```
// (ST : STRING (1..2), c74004a's PR'VALUE (ST)) arrives as a plain
```

### ada83.c:35959

```
// data pointer — wrap it with its subtype's bounds.
```

### ada83.c:35967

```
// Integer'VALUE - parse string as integer
```

### ada83.c:35972

```
// Convert to prefix type width if different
```

### ada83.c:35977

```
// Float'VALUE - parse string as float
```

### ada83.c:35981

```
// __ada_float_value yields double; narrow to the prefix's float rep
```

### ada83.c:35982

```
// (e.g. single-precision FLOAT) and tag honestly.
```

### ada83.c:35990

```
// Character'VALUE - parse "'x'" format, strip leading/trailing spaces (RM 3.5.5)
```

### ada83.c:35994

```
// Trim leading/trailing spaces
```

### ada83.c:35999

```
// Must be exactly 3 chars: 'x'
```

### ada83.c:36007

```
// Check first and last chars are single quotes
```

### ada83.c:36019

```
// Extract the character at position 1
```

### ada83.c:36025

```
// Fail - raise CONSTRAINT_ERROR
```

### ada83.c:36028

```
// Lcval_end is the live continuation reached by the extract branch;
```

### ada83.c:36029

```
// the raise above marked the block terminated, so clear that before
```

### ada83.c:36030

```
// the caller emits code after the label.
```

### ada83.c:36033

```
// CHARACTER'VALUE yields the i8 character code; tag it honestly rather
```

### ada83.c:36034

```
// than the shared i32 fall-through.
```

### ada83.c:36037

```
// Boolean'VALUE - match "TRUE"/"FALSE" case-insensitively, strip spaces (RM 3.5.5)
```

### ada83.c:36041

```
// Trim leading/trailing spaces
```

### ada83.c:36046

```
// Check for "TRUE" (length 4) and "FALSE" (length 5)
```

### ada83.c:36061

```
// Check length == 4 (TRUE)
```

### ada83.c:36072

```
// Check length == 5 (FALSE)
```

### ada83.c:36084

```
// No match - raise CONSTRAINT_ERROR
```

### ada83.c:36087

```
// Lbval_end is the live continuation reached by the match branches;
```

### ada83.c:36088

```
// the raise above marked the block terminated, so clear that before
```

### ada83.c:36089

```
// emitting code that follows the label.
```

### ada83.c:36096

```
// Return at the value's real width, not the shared i32 fall-through:
```

### ada83.c:36097

```
// BOOLEAN'VALUE stores at i8, so tagging i32 mislabels the register.
```

### ada83.c:36100

```
// Enumeration'VALUE - find literal by name and return position
```

### ada83.c:36103

```
// Find root enumeration type with literals
```

### ada83.c:36113

```
// Enum'VALUE: compare input against each literal (case-insensitive).
```

### ada83.c:36114

```
// Ada RM 3.5.5: leading/trailing blanks are stripped first.
```

### ada83.c:36117

```
// Trimmed pointer and length
```

### ada83.c:36129

```
// Character literals are case-sensitive; identifiers are uppercased
```

### ada83.c:36130

```
// and compared case-insensitively (RM 3.5.5)
```

### ada83.c:36141

```
// Check trimmed length matches literal
```

### ada83.c:36147

```
// Case-sensitive comparison for character literals (RM 3.5.5)
```

### ada83.c:36152

```
// Case-insensitive comparison for identifiers
```

### ada83.c:36166

```
// No match - raise CONSTRAINT_ERROR (RM 3.5.5)
```

### ada83.c:36168

```
// Lval_end is the live continuation reached by the match branches;
```

### ada83.c:36169

```
// the no-match raise above marked the block terminated, so clear
```

### ada83.c:36170

```
// that before emitting code that follows the label.
```

### ada83.c:36175

```
// Result is the enumeration value computed at the working integer
```

### ada83.c:36176

```
// width; coerce to the enum's storage type (e.g. i8 for small
```

### ada83.c:36177

```
// enums) so the returned value carries its storage rep.
```

### ada83.c:36181

```
// No literals found, fallback to integer value
```

### ada83.c:36190

```
// Default: treat as integer
```

### ada83.c:36203

```
// T'WIDTH - maximum image width for type (RM 3.5.5)
```

### ada83.c:36204

```
// Per GNAT exp_imgv.adb Expand_Width_Attribute:                                                  
```

### ada83.c:36205

```
// - For null range (FIRST > LAST), WIDTH is 0                                                    
```

### ada83.c:36206

```
// - For enumeration: max length of literal names in range                                        
```

### ada83.c:36207

```
// - For integer: max width of first/last images                                                  
```

### ada83.c:36208

```
// - For boolean: max("FALSE", "TRUE") = 5                                                        
```

### ada83.c:36209

```
// - For character: 3 ('X')                                                                       
```

### ada83.c:36210

```
//                                                                                                
```

### ada83.c:36211

```
// When bounds are not compile-time known, generate runtime code.                                
```

### ada83.c:36212

```
//                                                                                                
```

### ada83.c:36218

```
// Find root enumeration type (traversing base_type and parent_type chains)
```

### ada83.c:36228

```
// Both bounds are compile-time known - compute WIDTH statically
```

### ada83.c:36234

```
// Empty range - width is 0
```

### ada83.c:36238

```
// Enumeration: max length of literal names in range
```

### ada83.c:36247

```
// Boolean: "FALSE" is 5, "TRUE" is 4
```

### ada83.c:36251

```
// Character: 'X' is 3 chars
```

### ada83.c:36255

```
// Integer types: max width of first/last images
```

### ada83.c:36258

```
// Width includes leading space for non-negative
```

### ada83.c:36264

```
// +1 for leading space or minus sign
```

### ada83.c:36268

```
// Bounds not compile-time known - generate runtime code.                                    
```

### ada83.c:36269

```
// Per GNAT exp_imgv.adb: generate if FIRST > LAST then 0 else <width>                        
```

### ada83.c:36270

```
// For enumeration types with runtime bounds, we compute the full-range                       
```

### ada83.c:36271

```
// width at compile time (since literals are known) and use 0 for null range.                
```

### ada83.c:36272

```
//                                                                                            
```

### ada83.c:36277

```
// Compare: is_null = (lo > hi)
```

### ada83.c:36280

```
// legacy: used as select operand below
```

### ada83.c:36282

```
// Compute the width for non-null range at compile time
```

### ada83.c:36285

```
// For enumeration: compute max width over full base type range
```

### ada83.c:36292

```
// Boolean: WIDTH depends on which values are in range.                                    
```

### ada83.c:36293

```
// FALSE=0 has width 5, TRUE=1 has width 4.                                                
```

### ada83.c:36294

```
// If lo <= 0 (FALSE is in range): width = 5                                                
```

### ada83.c:36295

```
// Otherwise (only TRUE in range): width = 4                                                
```

### ada83.c:36296

```
// Generate: select (lo <= 0), 5, 4                                                         
```

### ada83.c:36297

```
//                                                                                          
```

### ada83.c:36303

```
// Select: if is_null then 0 else bool_width
```

### ada83.c:36308

```
// 'X'
```

### ada83.c:36310

```
// Integer: compute WIDTH based on actual runtime bounds.                                  
```

### ada83.c:36311

```
// WIDTH = max(digits(abs(lo)), digits(abs(hi))) + 1                                        
```

### ada83.c:36312

```
// Generate runtime code to compute this.                                                  
```

### ada83.c:36313

```
//                                                                                          
```

### ada83.c:36316

```
// Compute abs(lo): if lo < 0 then -lo else lo
```

### ada83.c:36323

```
// Compute abs(hi): if hi < 0 then -hi else hi
```

### ada83.c:36330

```
// max_abs = max(abs_lo, abs_hi)
```

### ada83.c:36335

```
// Count digits using comparison chain.
```

### ada83.c:36336

```
// Derive max digits from type width (works for i8..i128):
```

### ada83.c:36337

```
//   i8: 2, i16: 4, i32: 9, i64: 18, i128: 38
```

### ada83.c:36338

```
//
```

### ada83.c:36357

```
// width = digits + 1 (for leading space or minus sign)
```

### ada83.c:36360

```
// Not used - we have runtime value
```

### ada83.c:36362

```
// Select: if is_null then 0 else width_val
```

### ada83.c:36368

```
// Select: if is_null then 0 else full_width
```

### ada83.c:36376

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:36377

```
// Access Type Attributes                                                                         
```

### ada83.c:36378

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:36380

```
// X'ACCESS / X'UNCHECKED_ACCESS - identical codegen (RM 3.10.2)
```

### ada83.c:36392

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:36393

```
// Floating-Point Type Attributes (RM 3.5.8)                                                      
```

### ada83.c:36394

```
// These attributes return compile-time values for floating-point types.                         
```

### ada83.c:36395

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:36397

```
// T'DIGITS - number of significant decimal digits (RM 3.5.7)
```

### ada83.c:36405

```
// T'MANTISSA - number of binary digits (RM 3.5.8, 3.5.10)                                        
```

### ada83.c:36406

```
// For floating-point: ceiling(D * log(10)/log(2)) + 1                                            
```

### ada83.c:36407

```
// For fixed-point: ceiling(log2 (bound / small))                                                 
```

### ada83.c:36408

```
//                                                                                                
```

### ada83.c:36410

```
// Default for double
```

### ada83.c:36419

```
// Non-static range (bounds built from an expression): evaluate at runtime.
```

### ada83.c:36429

```
// T'EMAX - maximum binary exponent (RM 3.5.8)
```

### ada83.c:36430

```
// For model numbers: EMAX = 4 * MANTISSA
```

### ada83.c:36432

```
// Default for double
```

### ada83.c:36439

```
// T'SAFE_EMAX - safe maximum exponent (RM 3.5.8)
```

### ada83.c:36440

```
// For IEEE: MACHINE_EMAX - 1
```

### ada83.c:36447

```
// ─── Real model attributes: EPSILON, SMALL, LARGE, SAFE_SMALL, SAFE_LARGE ─────────────────────
```

### ada83.c:36448

```
// Each is a compile-time double (RM 3.5.8, 3.5.10), generated at double
```

### ada83.c:36449

```
// (UNIVERSAL_REAL) - callers convert, matching the double rep the value
```

### ada83.c:36450

```
// was emitted at. The one exception:
```

### ada83.c:36451

```
// 'LARGE of a fixed-point type whose bounds are not static folds its
```

### ada83.c:36452

```
// (2**MANTISSA - 1) * SMALL at run time instead.
```

### ada83.c:36474

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:36475

```
// Fixed-Point Type Attributes (RM 3.5.9)                                                         
```

### ada83.c:36476

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:36478

```
// T'DELTA - delta for fixed-point type (universal real)
```

### ada83.c:36489

```
// T'FORE - minimum field width for integer part (RM 3.5.10(5))                                   
```

### ada83.c:36490

```
// Includes a one-character prefix (minus sign or space).                                        
```

### ada83.c:36491

```
// FORE = 2 when integer part is 0, otherwise 1 + 1 + floor (log10 (int_part))                    
```

### ada83.c:36492

```
//                                                                                                
```

### ada83.c:36494

```
// minimum: sign + at least one digit
```

### ada83.c:36500

```
// Non-static range (bounds built from an expression): evaluate at runtime.
```

### ada83.c:36511

```
// T'AFT - decimal digits after point in default output (RM 3.5.10)
```

### ada83.c:36512

```
// AFT = smallest N such that 10^(-N) <= T'DELTA
```

### ada83.c:36525

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:36526

```
// Machine Model Attributes (RM 3.5.8)
```

### ada83.c:36527

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:36529

```
// T'MACHINE_ROUNDS through T'MACHINE_EMIN: every machine model attribute is
```

### ada83.c:36530

```
// an IEEE 754 constant that depends only on whether the prefix is single or
```

### ada83.c:36531

```
// double precision. MACHINE_ROUNDS is TRUE (IEEE hardware rounds) and
```

### ada83.c:36532

```
// MACHINE_OVERFLOWS is FALSE (overflow yields infinity, no trap); those two
```

### ada83.c:36533

```
// are BOOLEAN-valued and load at the Boolean storage width, the rest are
```

### ada83.c:36534

```
// universal integers.
```

### ada83.c:36592

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:36593

```
// Object Attributes (RM 3.7.1, 9.9)                                                              
```

### ada83.c:36594

```
// ───────────────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:36596

```
// X'CONSTRAINED - is the object constrained? (RM 3.7.1)                                          
```

### ada83.c:36597

```
// Returns TRUE if:                                                                               
```

### ada83.c:36598

```
//   - Object type has no discriminants (always constrained)                                      
```

### ada83.c:36599

```
//   - Object was declared with explicit discriminant constraint                                  
```

### ada83.c:36600

```
//   - Object type has no default discriminant values (immutable)                                 
```

### ada83.c:36601

```
// Returns FALSE if:                                                                              
```

### ada83.c:36602

```
//   - Object type has discriminants with defaults and no explicit constraint                     
```

### ada83.c:36603

```
// Use i64 for consistency with Boolean storage/comparison.                                      
```

### ada83.c:36604

```
//                                                                                                
```

### ada83.c:36609

```
// Default: constrained
```

### ada83.c:36611

```
// RM 3.7.4: an object designated by an access value is always constrained
```

### ada83.c:36612

```
// - a heap object's discriminants are fixed when it is allocated, so an
```

### ada83.c:36613

```
// explicit .ALL dereference yields TRUE regardless of the designated
```

### ada83.c:36614

```
// subtype.
```

### ada83.c:36617

```
// RM 3.7.4: an OUT/IN OUT formal of an unconstrained mutable type inherits
```

### ada83.c:36618

```
// the actual's constrained state, which is only known at runtime — read the
```

### ada83.c:36619

```
// hidden flag spilled in the prologue.
```

### ada83.c:36627

```
// RM 12.1.1 / 3.7.4: an IN OUT generic formal object renames the actual, so
```

### ada83.c:36628

```
// its 'CONSTRAINED is the actual's — known statically at instantiation.
```

### ada83.c:36642

```
// A formal whose actual itself carries a runtime flag would need that
```

### ada83.c:36643

```
// flag threaded through; such nesting is out of scope here (the common
```

### ada83.c:36644

```
// case is a plain object actual). Emit the static value.
```

### ada83.c:36660

```
// Explicitly constrained subtype
```

### ada83.c:36662

```
// Object declared with constraint
```

### ada83.c:36665

```
// RM 3.7.4: an IN formal parameter is constant,
```

### ada83.c:36666

```
// so 'CONSTRAINED is always TRUE
```

### ada83.c:36668

```
// RM 3.7.4: a constant is constrained
```

### ada83.c:36670

```
// Mutable: defaults, no constraint
```

### ada83.c:36674

```
// Boolean-valued attributes produce i8 (Boolean storage type)
```

### ada83.c:36679

```
// T'CALLABLE (RM 9.9): load TCB handle, check completed flag.
```

### ada83.c:36680

```
// Result is BOOLEAN (i8) — the returned rep tracks it so downstream
```

### ada83.c:36681

```
// conversions need no prediction.
```

### ada83.c:36688

```
// T'TERMINATED (RM 9.9): load TCB handle, check the TERMINATED byte
```

### ada83.c:36689

```
// (TCB+57) — set only after the task's dependents are awaited. The
```

### ada83.c:36690

```
// completed byte (TCB+24) flips earlier, at the end of the statements
```

### ada83.c:36691

```
// (callers then get TASKING_ERROR), while 'TERMINATED stays false.
```

### ada83.c:36692

```
// Result is BOOLEAN (i8) — same tracking note as CALLABLE.
```

### ada83.c:36709

```
// E'COUNT / E(I)'COUNT (RM 9.9): number of entry calls queued on entry E (or
```

### ada83.c:36710

```
// family member E(I)) of the current task. Evaluated inside the task body, so
```

### ada83.c:36711

```
// the queue is on %__self_tcb.
```

### ada83.c:36716

```
// A family member's queue is keyed by base*1000 + the family index, exactly
```

### ada83.c:36717

```
// as an entry call computes it (Emit_Entry_Index); a bare entry uses index
```

### ada83.c:36718

```
// 0. Reading only the base would query the wrong queue and never see calls
```

### ada83.c:36719

```
// enqueued on E(I) (c34008a's WHILE H(2)'COUNT < 2 loop would never end).
```

### ada83.c:36735

```
// Outside a task body or unresolved entry: no queue, count is 0.
```

### ada83.c:36739

```
// T'STORAGE_SIZE (RM 13.7.1) - return user-specified value if set,
```

### ada83.c:36740

```
// otherwise return a reasonable implementation-defined default.                                 
```

### ada83.c:36741

```
// For derived access types, walk parent chain to find the clause.                               
```

### ada83.c:36742

```
//                                                                                                
```

### ada83.c:36749

```
// A non-static clause expression was frozen at the clause's elaboration
```

### ada83.c:36750

```
// (RM 13.2): read the module global back.
```

### ada83.c:36762

```
// Record component representation attributes (RM 13.7.2):                                        
```

### ada83.c:36763

```
// X.C'FIRST_BIT, X.C'LAST_BIT, X.C'POSITION                                                      
```

### ada83.c:36764

```
// Prefix must be a selected component of a record.                                              
```

### ada83.c:36765

```
//                                                                                                
```

### ada83.c:36771

```
// Determine the record type from the prefix of the selected component
```

### ada83.c:36787

```
// POSITION
```

### ada83.c:36796

```
// Unhandled attribute
```

### ada83.c:36804

```
// Helper: Find component index by name in record type
```

### ada83.c:36805

```
// A record subtype carries no runtime descriptor of its own; its rt_global_id
```

### ada83.c:36806

```
// lives on a base/parent in the derivation chain. Return the first non-zero id
```

### ada83.c:36807

```
// found walking that chain (0 if the record has no runtime descriptor).
```

### ada83.c:36816

```
// Unwrap a type to the underlying record it denotes for component access:
```

### ada83.c:36817

```
// follow private/incomplete views down the parent chain (RM 7.4.1).
```

### ada83.c:36818

```
// Returns the input unchanged when no record is reached.
```

### ada83.c:36824

```
// A constrained subtype of an incomplete type completed afterwards keeps
```

### ada83.c:36825

```
// the INCOMPLETE kind but records the completed record as base_type
```

### ada83.c:36826

```
// (RM 3.8.1); a derived/private one uses parent_type.
```

### ada83.c:36837

```
// Follow the parent/base chain to the underlying enumeration type a derived or
```

### ada83.c:36838

```
// subtype denotes; returns the chain end (caller checks kind == TYPE_ENUMERATION).
```

### ada83.c:36845

```
// Address of component `comp_idx` (static byte offset `byte_offset`) within the
```

### ada83.c:36846

```
// record at base pointer `base`. Three regimes for the offset:
```

### ada83.c:36847

```
//   * a constrained subtype with a runtime descriptor reads its precomputed
```

### ada83.c:36848

```
//     @__rt_rec_N_offK (offsets folded once at elaboration);
```

### ada83.c:36849

```
//   * an unconstrained view of a record that needs runtime layout — a mutable
```

### ada83.c:36850

```
//     record or an OUT/IN OUT formal of the base type — whose accessed component
```

### ada83.c:36851

```
//     sits after a capless one, sums the footprints of the preceding components
```

### ada83.c:36852

```
//     from the instance's own discriminants (loaded from `base`);
```

### ada83.c:36853

```
//   * otherwise the static byte offset (correct for every component up to and
```

### ada83.c:36854

```
//     including the first capless one).
```

### ada83.c:36855

```
// Returns the field pointer register.
```

### ada83.c:36880

```
// The component slot a selected node denotes: the resolver's recorded pick
```

### ada83.c:36881

```
// when present — mandatory under a formal/actual discriminant name collision
```

### ada83.c:36882

```
// (RM 12.1, cc3240a), which a by-name lookup cannot re-arbitrate — else a
```

### ada83.c:36883

```
// by-name lookup for nodes synthesized after resolution.
```

### ada83.c:36893

```
// A type whose record view is live: a proper record, or a private /
```

### ada83.c:36894

```
// limited-private view carrying discriminant components (RM 3.7.4, 7.4.2).
```

### ada83.c:36900

```
// True when the current resolution point lies within the region of the
```

### ada83.c:36901

```
// instance (or its template) that installed `view` as a formal-type binding —
```

### ada83.c:36902

```
// where RM 12.1 says the FORMAL's discriminant names govern.
```

### ada83.c:36917

```
// RM 12.1: inside the generic (an instance body clone re-resolving there),
```

### ada83.c:36918

```
// the discriminants of a formal private type are known by the FORMAL's
```

### ada83.c:36919

```
// names, mapping positionally onto the actual's — and those names take
```

### ada83.c:36920

```
// precedence over a same-named component of the actual (cc3240a: TX.A is
```

### ada83.c:36921

```
// the discriminant the formal calls A, not the actual's component A).
```

### ada83.c:36922

```
// Outside the instance the actual's own names govern (cc3203a: P1VP.D).
```

### ada83.c:36941

```
// The component index of the discriminant that GOVERNS the variant part
```

### ada83.c:36942

```
// (RM 3.7.3): the one named in the variant part, not necessarily the first
```

### ada83.c:36943

```
// (`CASE E IS ...` when D precedes E). Returns -1 when the type has no
```

### ada83.c:36944

```
// variant part or the named discriminant is not among its discriminant
```

### ada83.c:36945

```
// components. Memoized on the type, since record layouts are immutable once
```

### ada83.c:36946

```
// the discriminants are laid out and this is consulted per variant guard.
```

### ada83.c:36965

```
// The variant a record aggregate selects (RM 3.7.3 / 4.3.1). Both the
```

### ada83.c:36966

```
// resolver's and the generator's positional component mapping must skip the
```

### ada83.c:36967

```
// components of every other variant — otherwise the Nth positional element
```

### ada83.c:36968

```
// lands on the Nth FLAT component, which for a variant record can be a
```

### ada83.c:36969

```
// different variant's field (e.g. an ARRAY_OF_INT mapped onto the
```

### ada83.c:36970

```
// overlapping B:BOOLEAN slot). The governing discriminant's value comes
```

### ada83.c:36971

```
// from the aggregate itself — authoritative, since it may select a variant
```

### ada83.c:36972

```
// other than the subtype's default constraint — or, when the aggregate does
```

### ada83.c:36973

```
// not supply it, from the subtype constraint. Returns the variant index, or
```

### ada83.c:36974

```
// -1 when the type has no variant part or the governing value is not
```

### ada83.c:36975

```
// static. Callers treat -1 as "no skipping".
```

### ada83.c:36983

```
// Locate the aggregate item that supplies the governing discriminant:
```

### ada83.c:36984

```
// the gov_index'th positional item, or the association naming it.
```

### ada83.c:37002

```
// Its static value: an integer literal, a resolved enumeration literal, or
```

### ada83.c:37003

```
// any other static expression the folder can evaluate. A negative literal
```

### ada83.c:37004

```
// is unary minus over an integer (`-6`, not an NK_INTEGER), and a named
```

### ada83.c:37005

```
// number folds too; NaN means "not static" (c37213g).
```

### ada83.c:37025

```
// Range match wins; OTHERS is the fallback when no later range matches.
```

### ada83.c:37037

```
// Check if a choice is "others"
```

### ada83.c:37044

```
// True when an array aggregate has an `others` association. Such an aggregate
```

### ada83.c:37045

```
// has no choices to fix its own bounds and must take them from the applicable
```

### ada83.c:37046

```
// index constraint of its context (RM 4.3.2).
```

### ada83.c:37052

```
// ── Is_Static_Int_Node / Static_Int_Value ────────────────────────────────────────────────────────
```

### ada83.c:37053

```
// Recognise compile-time integer nodes so the aggregate codegen can
```

### ada83.c:37054

```
// fold them into static operands. Covers:
```

### ada83.c:37055

```
//   NK_INTEGER              - positive literal  (e.g. 3)                                          
```

### ada83.c:37056

```
//   NK_UNARY_OP (-, int)     - negated literal   (e.g. -1)                                         
```

### ada83.c:37057

```
//   NK_UNARY_OP (+, int)     - explicit positive (e.g. +1)                                         
```

### ada83.c:37058

```
//                                                                                                  
```

### ada83.c:37066

```
// RM 4.9: an enumeration literal — including a CHARACTER or BOOLEAN literal —
```

### ada83.c:37067

```
// is a static scalar whose discrete value is its position in the enumeration.
```

### ada83.c:37068

```
// Discrete aggregate choices and case alternatives use these as index values
```

### ada83.c:37069

```
// exactly like integer literals (e.g. (WED..FRI => 0)), so they must fold
```

### ada83.c:37070

```
// statically too; otherwise the named-choice machinery falls back to the whole
```

### ada83.c:37071

```
// aggregate range and mis-fills every element.
```

### ada83.c:37085

```
// Enumeration / boolean / character literal denoted by name: its static value
```

### ada83.c:37086

```
// is the internal code, which equals the position (frame_offset) unless a
```

### ada83.c:37087

```
// representation clause remaps it (RM 13.3). frame_offset is robust where the
```

### ada83.c:37088

```
// literal's type is a subtype with an empty literal list — e.g. BOOLEAN RANGE
```

### ada83.c:37089

```
// TRUE..TRUE — for which Enum_Literal_Position would wrongly return 0.
```

### ada83.c:37093

```
// Character literal with no resolved symbol: the position equals the code
```

### ada83.c:37094

```
// point for the predefined CHARACTER type.
```

### ada83.c:37104

```
// ── § 13a: Array Aggregate Helpers (RM 4.3.2) ────────────────────────────────────────────────────
```

### ada83.c:37105

```
//
```

### ada83.c:37106

```
// These helpers factor out the repeated patterns in Generate_Aggregate:
```

### ada83.c:37107

```
//   Scan_Aggregate_Shape - classify items and span the static choices
```

### ada83.c:37108

```
//   Agg_Resolve_Elem     - generate element value, extract from fat ptr
```

### ada83.c:37109

```
//   Agg_Store_At         - store element at array index (scalar or composite)
```

### ada83.c:37110

```
//
```

### ada83.c:37111

```
// Design: each helper is a pure function of its arguments - no hidden
```

### ada83.c:37112

```
// state, no implicit coupling. Haskell-flavoured C99 with `and`/`or`.
```

### ada83.c:37113

```
//
```

### ada83.c:37115

```
// ── Scan_Aggregate_Shape: one pass over an aggregate's items ─────────────────────────────────────
```

### ada83.c:37116

```
// Classification (positional / named / OTHERS) plus the static choice span:
```

### ada83.c:37117

```
// the least and greatest values named by static single-index and range
```

### ada83.c:37118

```
// choices, and whether any choice bound needs runtime evaluation.
```

### ada83.c:37177

```
// Check if a pair of static integer bounds spans an unreasonably large range                       
```

### ada83.c:37178

```
// (e.g. full index subtype: INTEGER -2^31..2^31-1) meaning the type checker                        
```

### ada83.c:37179

```
// couldn't determine narrower constraints. Such "placeholder" bounds overflow                     
```

### ada83.c:37180

```
// uint32 size calculations and must be replaced with actual choice bounds.                        
```

### ada83.c:37181

```
//                                                                                                  
```

### ada83.c:37190

```
// Check whether an aggregate will produce a fat pointer (dynamic alloca path).                    
```

### ada83.c:37191

```
// True when the type has dynamic bounds OR the aggregate's own named choices                       
```

### ada83.c:37192

```
// contain non-static range expressions (e.g. desugared T'RANGE).                                  
```

### ada83.c:37193

```
// ── Agg_Resolve_Elem: generate element value and unwrap fat ptrs. ────────────────────────────────
```

### ada83.c:37194

```
//                                                                                                  
```

### ada83.c:37195

```
// Given an expression node, generates the value and, depending on the                              
```

### ada83.c:37196

```
// target context, extracts the data pointer from fat pointers or                                   
```

### ada83.c:37197

```
// converts the scalar type. Returns the SSA temp ready for storage.                              
```

### ada83.c:37198

```
//                                                                                                  
```

### ada83.c:37199

```
//   multidim          - are we generating a multi-dimensional array?                               
```

### ada83.c:37200

```
//   elem_is_composite - is the element type composite (record/array/row)?                          
```

### ada83.c:37201

```
//   agg_type          - the enclosing array's Type_Info                                            
```

### ada83.c:37202

```
//   elem_type         - LLVM type string for scalar elements (e.g. "i32")                          
```

### ada83.c:37203

```
//   elem_ti           - Type_Info* for the element subtype (for checks)                            
```

### ada83.c:37204

```
//                                                                                                  
```

### ada83.c:37205

```
// RM 4.3.2 / 3.6.1: the value of an array-typed aggregate component must
```

### ada83.c:37206

```
// BELONG to the component subtype — for a constrained array subtype that means
```

### ada83.c:37207

```
// identical bounds in every dimension (an aggregate component does not slide).
```

### ada83.c:37208

```
// When the value arrives as a fat pointer carrying its own bounds (a
```

### ada83.c:37209

```
// parenthesized string literal, a slice, an unconstrained result), compare
```

### ada83.c:37210

```
// each dimension against the constraint and raise on the first mismatch. A
```

### ada83.c:37211

```
// value already shaped as the constrained subtype (a plain data pointer)
```

### ada83.c:37212

```
// matches by construction and never reaches here.
```

### ada83.c:37217

```
// Restrict the check to a subtype whose every bound is a compile-time
```

### ada83.c:37218

```
// constant. A dynamic bound (e.g. ARR (1 .. F)) is re-evaluated here and
```

### ada83.c:37219

```
// compared against bounds the value was built with; the two evaluations can
```

### ada83.c:37220

```
// legitimately differ (F called twice, or a slid value), so comparing them
```

### ada83.c:37221

```
// would raise spuriously. The static case — the one string-literal bounds
```

### ada83.c:37222

```
// (RM 4.2/4.3.2) actually turn on — is exact and safe.
```

### ada83.c:37223

```
// A null array value belongs to any null array subtype irrespective of its
```

### ada83.c:37224

```
// nominal bounds (RM 3.6.1), so a subtype that is null in any dimension is
```

### ada83.c:37225

```
// exempt from the exact-bound comparison below. Require every bound static.
```

### ada83.c:37255

```
// Fat pointer sources: extract data pointer for composite elements.
```

### ada83.c:37256

```
// The value's real type drives the choice — bound-pair overflow on the AST
```

### ada83.c:37257

```
// type only flags that an inner sub-aggregate *might* have built a fat
```

### ada83.c:37258

```
// pointer; a static-bounds inner still returns a plain ptr.
```

### ada83.c:37259

```
//
```

### ada83.c:37262

```
// The fat value still carries its own bounds here; verify they belong to
```

### ada83.c:37263

```
// the component subtype (RM 4.3.2) before discarding them for the copy.
```

### ada83.c:37274

```
// Fat-pointer element (dynamic-bound array/string): rebuild the                                  
```

### ada83.c:37275

```
// fat pointer with the correct bounds from the element type's                                    
```

### ada83.c:37276

```
// constraint, since the source (e.g. string literal) may have                                    
```

### ada83.c:37277

```
// default bounds (1..N) instead of the target bounds (3..5).                                    
```

### ada83.c:37278

```
//                                                                                                
```

### ada83.c:37286

```
// Scalar: type-convert and apply fixed-point SMALL if needed.
```

### ada83.c:37297

```
// ── Aggregate_Operand constructors and materialization ───────────────────────────────────────────
```

### ada83.c:37303

```
// Materialize an operand as an SSA register at the context's index rep. A
```

### ada83.c:37304

```
// static operand with a pre-materialized register reuses it; otherwise the
```

### ada83.c:37305

```
// constant is emitted afresh at the point of use (so the result always
```

### ada83.c:37306

```
// dominates its consumer, even inside emitted loops).
```

### ada83.c:37313

```
// The operand of one choice bound expression. `side` says which aggregate
```

### ada83.c:37314

```
// bound the expression may coincide with: 0 = low, 1 = high, -1 = a single
```

### ada83.c:37315

```
// (non-range) choice. A static integer node folds to a constant; the
```

### ada83.c:37316

```
// expression that itself produced the aggregate's bound reuses that SSA
```

### ada83.c:37317

```
// value rather than re-evaluating a side-effecting expression (RM 3.2.1);
```

### ada83.c:37318

```
// anything else is generated once (RM 4.3.2(6)) and, for an unconstrained
```

### ada83.c:37319

```
// aggregate, seeds the fat-pointer bound of its side.
```

### ada83.c:37338

```
// ── Agg_Store_At: store one element value at a flat (zero-based) index. ──────────────────────────
```

### ada83.c:37339

```
// The index and the byte stride are each independently compile-time or
```

### ada83.c:37340

```
// runtime; all four combinations funnel through here.
```

### ada83.c:37346

```
// Dynamically-sized element or row: byte offset and copy length are the
```

### ada83.c:37347

```
// runtime stride (RM 3.6.1).
```

### ada83.c:37367

```
// Composite element: memcpy elem_size bytes at a byte offset.
```

### ada83.c:37382

```
// Scalar (or fat-pointer-valued) element: typed gep and store.
```

### ada83.c:37398

```
// ── Agg_Emit_Component: one aggregate component (RM 4.3.2(6)) ─────────────────────────────────────
```

### ada83.c:37399

```
// Resolve the element value (evaluated once per component), track inner
```

### ada83.c:37400

```
// sub-aggregate bounds, apply the scalar subtype constraint check, store at
```

### ada83.c:37401

```
// the flat index, and mark the slot initialized when coverage is tracked.
```

### ada83.c:37406

```
// An out-of-window compile-time index (already diagnosed against the
```

### ada83.c:37407

```
// index subtype) stores nothing.
```

### ada83.c:37424

```
// The same, at an Ada index value: flat = index - low, folded when both are
```

### ada83.c:37425

```
// compile-time constants.
```

### ada83.c:37442

```
// ── Agg_Fill_Range: a range choice L..H filled with one expression ───────────────────────────────
```

### ada83.c:37443

```
// The expression is evaluated once per component (RM 4.3.2(6)). Compile-time
```

### ada83.c:37444

```
// bounds of a coverage-tracked aggregate unroll to per-index emissions; any
```

### ada83.c:37445

```
// runtime operand lowers to a loop evaluating and storing one component per
```

### ada83.c:37446

```
// iteration.
```

### ada83.c:37471

```
// Stop on equality BEFORE incrementing: a bound at the index rep's
```

### ada83.c:37472

```
// maximum (e.g. SYSTEM.MAX_INT) must not wrap the loop variable.
```

### ada83.c:37484

```
// ── Agg_Fill_Others: fill the components no other association covers ─────────────────────────────
```

### ada83.c:37485

```
// RM 4.3.3(5): the expression is evaluated once per associated component, so
```

### ada83.c:37486

```
// allocators produce distinct objects for each element. With compile-time
```

### ada83.c:37487

```
// coverage the uninitialized slots are filled exactly; a runtime-bounds
```

### ada83.c:37488

```
// aggregate loops over [low + positional_count .. high] and skips the spans
```

### ada83.c:37489

```
// registered by the named choices, so their components keep their explicit
```

### ada83.c:37490

```
// values and the OTHERS expression is evaluated only for its own components.
```

### ada83.c:37536

```
// Stop on equality BEFORE incrementing: a bound at the index rep's
```

### ada83.c:37537

```
// maximum (e.g. SYSTEM.MAX_INT) must not wrap the loop variable.
```

### ada83.c:37550

```
// ── Choice bookkeeping: named-span tracking, index-subtype checks, coverage ──────────────────────
```

### ada83.c:37552

```
// Widen the tracked named-choice span (min-low into named_low_variable,
```

### ada83.c:37553

```
// max-high into named_high_variable).
```

### ada83.c:37562

```
// RM 4.3.2(3): the bounds of a non-null choice must belong to the index
```

### ada83.c:37563

```
// subtype (a single choice passes low == high and is always non-null). Fully
```

### ada83.c:37564

```
// static operands fold to a compile-time verdict; otherwise a runtime check
```

### ada83.c:37565

```
// is emitted, loading the subtype's own bounds when they too are dynamic.
```

### ada83.c:37611

```
// The index subtype governing dimension 1 of a multi-dimensional aggregate's
```

### ada83.c:37612

```
// type, when the type records one: the unconstrained base's second index, or
```

### ada83.c:37613

```
// the aggregate type's own when it is itself unconstrained. NULL when neither
```

### ada83.c:37614

```
// records an index type (the caller falls back to the dimension bounds).
```

### ada83.c:37626

```
// Record a named choice's span so the runtime OTHERS fill can skip it.
```

### ada83.c:37635

```
// ── Tracker initialization and per-component consistency tracking ────────────────────────────────
```

### ada83.c:37659

```
// Zero-fill: a sub-aggregate level that produced no elements reports
```

### ada83.c:37660

```
// these slots upward; they must read deterministically.
```

### ada83.c:37670

```
// RM 4.3.2(6): after a component whose evaluation reported sub-aggregate
```

### ada83.c:37671

```
// bounds, fold them into the consistency trackers.
```

### ada83.c:37680

```
// ── Agg_Elem_Is_Composite: check if array element needs memcpy vs store. ─────────────────────────
```

### ada83.c:37681

```
// A multi-dimensional aggregate's outer element is a row (always memcpy).
```

### ada83.c:37682

```
// Otherwise records and inline constrained arrays are memcpy'd — but a
```

### ada83.c:37683

```
// fat-stored element (dynamic-bound array / string component) is a value-typed
```

### ada83.c:37684

```
// { ptr, ptr } that is *stored*, not memcpy'd, even though it is nominally a
```

### ada83.c:37685

```
// constrained array. Keeping that distinction here makes every call site
```

### ada83.c:37686

```
// (the temp's sizing, the positional store, the dynamic range-choice loop)
```

### ada83.c:37687

```
// agree on one verdict.
```

### ada83.c:37694

```
// RM 3.7.1: After memcpy-ing a composite value into a record component,
```

### ada83.c:37695

```
// verify that the stored discriminant(s) match the component type's
```

### ada83.c:37696

```
// discriminant constraint(s). E.g. (H => INIT (1)) where H : PRIV (0).
```

### ada83.c:37697

```
//
```

### ada83.c:37715

```
// RM 4.3.2(6) helper: emit inner sub-aggregate bounds consistency tracking.                       
```

### ada83.c:37716

```
// Called after processing each element of a multidim aggregate. Reads the                         
```

### ada83.c:37717

```
// child sub-aggregate's reported bounds (cg->inner_agg_bnd_lo/hi[0..n-1])                          
```

### ada83.c:37718

```
// and compares them against first-seen expected values. Uses a single                             
```

### ada83.c:37719

```
// first-seen flag and single mismatch flag shared across all dimensions.                          
```

### ada83.c:37720

```
//                                                                                                  
```

### ada83.c:37721

```
//  inner_trk_lo/hi: arrays of alloca SSA ids, one per tracked dimension                            
```

### ada83.c:37722

```
//  inner_trk_first: alloca i1 for first-seen flag                                                  
```

### ada83.c:37723

```
//  inner_trk_mm:    alloca i1 for mismatch accumulator                                             
```

### ada83.c:37724

```
//  n_inner_dims:    max number of dimension levels to track                                        
```

### ada83.c:37725

```
//                                                                                                  
```

### ada83.c:37735

```
// Capture child bounds into local C arrays (SSA temp ids).
```

### ada83.c:37736

```
// Child values may be wider than `bt` (parent tracker's bound type)
```

### ada83.c:37737

```
// e.g. child reports i32 low_val but parent allocated i8 trackers.
```

### ada83.c:37738

```
// Coerce each child value to bt so store/compare types line up.
```

### ada83.c:37741

```
// Child values may be wider than bt; coerce each to bt so store/compare
```

### ada83.c:37742

```
// types line up. The child's produced rep is recorded alongside its temp.
```

### ada83.c:37747

```
// consumed
```

### ada83.c:37749

```
// Emit runtime branching: first-seen → store; else → compare
```

### ada83.c:37758

```
// First time: store expected bounds for all child dims
```

### ada83.c:37770

```
// Subsequent: compare all dims against expected, accumulate mismatch
```

### ada83.c:37772

```
// tracks accumulated mismatch SSA
```

### ada83.c:37787

```
// Merge into the global mismatch flag
```

### ada83.c:37794

```
// RM 4.3.2: T'RANGE in a choice position denotes the discrete range                                
```

### ada83.c:37795

```
// T'FIRST..T'LAST. Normalize these into NK_RANGE nodes so downstream                              
```

### ada83.c:37796

```
// choice-scanning code has a single representation to handle.                                     
```

### ada83.c:37797

```
// Applied recursively to inner sub-aggregates for multidim arrays.                                
```

### ada83.c:37798

```
//                                                                                                  
```

### ada83.c:37828

```
// RM 4.3.2: a choice that is a subtype mark (e.g. `BOOLEAN => …`)
```

### ada83.c:37829

```
// denotes that subtype's whole discrete range. Desugar it to
```

### ada83.c:37830

```
// T'FIRST..T'LAST so the choice scanner sees a range, not a scalar
```

### ada83.c:37831

```
// expression (which would resolve a type name to "unhandled symbol").
```

### ada83.c:37849

```
// RM 4.3.2: a choice given as a constrained subtype indication
```

### ada83.c:37850

```
// (`DT RANGE L .. R => …`) denotes that explicit range.
```

### ada83.c:37856

```
// Recurse into inner sub-aggregate (multidim)
```

### ada83.c:37866

```
// ── § 13b.0: Record Aggregate Component Store ────────────────────────────────────────────────────
```

### ada83.c:37867

```
//                                                                                                  
```

### ada83.c:37868

```
// Literate summary:  a record aggregate (A => 1, B => "hello", C => rec)                           
```

### ada83.c:37869

```
// must store each component value to the correct byte offset. Three                               
```

### ada83.c:37870

```
// representations arise - fat pointers, composite pointers, and scalars                            
```

### ada83.c:37871

```
// - each requiring distinct LLVM IR. This helper unifies the logic that                           
```

### ada83.c:37872

```
// was previously duplicated across named, positional, and OTHERS paths.                           
```

### ada83.c:37873

```
//                                                                                                  
```

### ada83.c:37874

```
//   data Src = Fat { ptr, ptr }   -- unconstrained array / dynamic bounds                          
```

### ada83.c:37875

```
//            | Ptr ptr             -- composite record / constrained array                         
```

### ada83.c:37876

```
//            | Val T               -- scalar / enumeration                                         
```

### ada83.c:37877

```
//                                                                                                  
```

### ada83.c:37878

```
//   store :: Src → CompPtr → IO ()                                                                 
```

### ada83.c:37879

```
//   store (Fat fp) dst = Emit_Fat_To_Array_Memcpy fp dst                                           
```

### ada83.c:37880

```
//   store (Ptr p)  dst = memcpy dst p (sizeof comp)                                                
```

### ada83.c:37881

```
//   store (Val v)  dst = store v dst                                                               
```

### ada83.c:37882

```
//                                                                                                  
```

### ada83.c:37883

```
// After storage, discriminant values are mirrored to disc_agg_temps and                            
```

### ada83.c:37884

```
// constraint-checked against the enclosing type (RM 3.7.1, 4.3.1).                                
```

### ada83.c:37885

```
// Disc_Alloc_Entry and Disc_Alloc_Info are defined earlier in §13 declarations                                            
```

### ada83.c:37886

```
//                                                                                                  
```

### ada83.c:37888

```
// Compute the byte size for a composite component memcpy. For disc-
```

### ada83.c:37889

```
// dependent arrays (size==0), derive from the source expression.
```

### ada83.c:37903

```
// Store a generated value into a record component slot.
```

### ada83.c:37904

```
// Handles all three cases: fat pointer, composite ptr, scalar. `rt_size` is the
```

### ada83.c:37905

```
// runtime byte count for a dynamically-sized composite component (0 = use the
```

### ada83.c:37906

```
// static type size); a nested dynamic record or an array of such records needs it
```

### ada83.c:37907

```
// so the memcpy does not drop the discriminant-dependent tail.
```

### ada83.c:37912

```
// The produced rep is authoritative — the AST-type prediction can disagree
```

### ada83.c:37913

```
// (e.g. a boolean-array operator result is physically a ptr while the
```

### ada83.c:37914

```
// component type predicts the i8 element).
```

### ada83.c:37920

```
// An array of dynamically-sized records arrives as a fat pointer, but its byte
```

### ada83.c:37921

```
// count is not derivable from a static element size: use the runtime size and
```

### ada83.c:37922

```
// copy from the fat pointer's data field (RM 3.6.1).
```

### ada83.c:37933

```
// Fat → constrained: the value carries its own bounds, so verify it belongs
```

### ada83.c:37934

```
// to the component subtype (RM 4.3.2) before the bounds are dropped for the
```

### ada83.c:37935

```
// copy, then extract data+bounds, compute length, and memcpy.
```

### ada83.c:37940

```
// Composite → memcpy with size from type or source expression. A
```

### ada83.c:37941

```
// discriminant-dependent array component (`S1 : STR (T'FIRST .. D)` in a
```

### ada83.c:37942

```
// variant) is array-like with dynamic bounds — not a statically-constrained
```

### ada83.c:37943

```
// array — but still stored INLINE, so its data must be copied, never treated
```

### ada83.c:37944

```
// as a scalar pointer.
```

### ada83.c:37954

```
// Scalar: convert and store, with constraint check (RM 4.3.1)
```

### ada83.c:37967

```
// The runtime byte size to memcpy for aggregate component `ci` of `agg_type`
```

### ada83.c:37968

```
// into the record at `base`, or 0 when the component's size is static. A nested
```

### ada83.c:37969

```
// dynamically-sized record and an array of such records have footprints known
```

### ada83.c:37970

```
// only from the enclosing record's discriminants, which are stored (as leading
```

### ada83.c:37971

```
// components) by the time any later component is filled.
```

### ada83.c:37983

```
// After storing a discriminant, mirror it to disc_agg_temps and verify
```

### ada83.c:37984

```
// against the enclosing type's discriminant constraint (RM 3.7.1).
```

### ada83.c:37990

```
// Coerce val to the component's storage type (e.g. i32→i8 for narrow
```

### ada83.c:37991

```
// discrete types) before mirroring and constraint-checking.
```

### ada83.c:37994

```
// Mirror to disc_agg_temp alloca
```

### ada83.c:38003

```
// RM 3.7.1: if the aggregate type is constrained, check that the
```

### ada83.c:38004

```
// supplied discriminant value matches the expected constraint.
```

### ada83.c:38019

```
// Count how many discriminants precede component index `ci`.
```

### ada83.c:38027

```
// ── § 13b: Generate_Aggregate - array & record aggregate codegen. ────────────────────────────────
```

### ada83.c:38028

```
//                                                                                                  
```

### ada83.c:38029

```
// Structure:                                                                                       
```

### ada83.c:38030

```
//   1. Array aggregate (RM 4.3.2):                                                                 
```

### ada83.c:38031

```
//      a. Classify items, compute dimension bounds                                                 
```

### ada83.c:38032

```
//      b. Dynamic-bounds path (any BOUND_EXPR): alloca, loop, fat ptr                              
```

### ada83.c:38033

```
//      c. Static-bounds path: alloca[N], unrolled init, optional fat ptr                           
```

### ada83.c:38034

```
//   2. Record aggregate (RM 4.3.1):                                                                
```

### ada83.c:38035

```
//      a. Alloca, positional/named stores, others fill                                             
```

### ada83.c:38036

```
//                                                                                                  
```

### ada83.c:38037

```
// Widen the tracked aggregate bound slots `lo_var`/`hi_var` to cover the range
```

### ada83.c:38038

```
// [lo_val..hi_val] (min into lo_var, max into hi_var), all at width `ait`.
```

### ada83.c:38039

```
// Update a running bound slot `var` against candidate `val`: keep whichever the
```

### ada83.c:38040

```
// comparison `pred` selects ("slt" widens a low/min, "sgt" widens a high/max).
```

### ada83.c:38055

```
// Constraint-check a (possibly null) index range against subtype `idx_ty`: null
```

### ada83.c:38056

```
// ranges (low > high, RM 3.6.1) are skipped at runtime, and a zero bound reg
```

### ada83.c:38057

```
// means "absent" and is simply not checked.
```

### ada83.c:38073

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:38074

```
// §13.9 Aggregate Helpers (body)
```

### ada83.c:38075

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:38077

```
// Establish the RM 5.2.1 slide inheritance for one aggregate, then generate it.
```

### ada83.c:38078

```
// An array aggregate slides when it is the top-level RHS of an assignment, or
```

### ada83.c:38079

```
// when it is an inner DIMENSION ROW of an aggregate that itself slides — a row
```

### ada83.c:38080

```
// is part of the same array value, not a component. Any other nested aggregate
```

### ada83.c:38081

```
// (a record aggregate, or a true component value such as an array-of-arrays
```

### ada83.c:38082

```
// element) resets the inheritance: components never slide (RM 4.3.2 / 3.6.1).
```

### ada83.c:38098

```
// Normalize T'RANGE choices → T'FIRST..T'LAST (recursively)
```

### ada83.c:38106

```
// Array aggregate - allocate on stack and initialize
```

### ada83.c:38116

```
// RM 4.3.3(6): For positional aggregates of unconstrained array types,                         
```

### ada83.c:38117

```
// the lower bound of each dimension is the index subtype's 'FIRST.                            
```

### ada83.c:38118

```
// Derive bounds for ALL dimensions from index_type when BOUND_NONE.                           
```

### ada83.c:38119

```
//                                                                                              
```

### ada83.c:38132

```
// Classify the items and span the static choices in one scan (RM 4.3.2)
```

### ada83.c:38137

```
// RM 4.3.2: an OTHERS aggregate has no first-dimension bounds of its own —
```

### ada83.c:38138

```
// it takes the applicable index constraint's. When that constraint is an
```

### ada83.c:38139

```
// unconstrained array target, the assignment published its runtime bounds
```

### ada83.c:38140

```
// here; adopt them as this dimension's (BOUND_EXPR + cached register) so the
```

### ada83.c:38141

```
// aggregate sizes to the target through the runtime-bounds regime instead of
```

### ada83.c:38142

```
// the index subtype's full range. Snapshot then clear, so a nested element
```

### ada83.c:38143

```
// aggregate falls back to its own type's bounds rather than the outer's.
```

### ada83.c:38156

```
// RM 3.6.1: a constrained array subtype whose constraint is non-static is
```

### ada83.c:38157

```
// elaborated ONCE; its bounds are then frozen in the type's runtime
```

### ada83.c:38158

```
// descriptor. A NAMED / OTHERS aggregate of such a subtype must read those
```

### ada83.c:38159

```
// frozen bounds, not re-evaluate the (side-effecting) constraint — pin each
```

### ada83.c:38160

```
// dimension's bound to the descriptor load via the cache slot. A POSITIONAL
```

### ada83.c:38161

```
// aggregate is left alone: its element count drives the bound and the
```

### ada83.c:38162

```
// RM 4.3.2 count-vs-constraint check, which the pin would otherwise mask.
```

### ada83.c:38177

```
// For positional aggregates, override upper bound from element count:                        
```

### ada83.c:38178

```
// high = low + N - 1. For multidimensional aggregates, also adjust                          
```

### ada83.c:38179

```
// inner dimension bounds from the first inner aggregate (RM 4.3.2).                         
```

### ada83.c:38180

```
//                                                                                            
```

### ada83.c:38189

```
// RM 4.3.2: For constrained arrays, the positional element count
```

### ada83.c:38190

```
// at this aggregate level must match the FIRST dimension's length.
```

### ada83.c:38191

```
// Inner dimensions are checked recursively when nested aggregates
```

### ada83.c:38192

```
// are walked. Use agg_type's ORIGINAL bounds (dim_hi[0] may have
```

### ada83.c:38193

```
// been overridden above to lo + N - 1, which would make the check
```

### ada83.c:38194

```
// vacuous). Skip for unconstrained types — bounds derive from count.
```

### ada83.c:38203

```
// Only raise on positional OVERFLOW. Underflow (fewer items
```

### ada83.c:38204

```
// than the formal dim) is legitimate when the aggregate's
```

### ada83.c:38205

```
// type carries the index subtype's full range but the
```

### ada83.c:38206

```
// aggregate becomes a sub-range whose actual bounds are
```

### ada83.c:38207

```
// derived from the positional count. The dim_hi[0] override
```

### ada83.c:38208

```
// earlier in this block handles the bound adjustment.
```

### ada83.c:38215

```
// Walk into nested inner aggregates to fix inner dimension bounds.                          
```

### ada83.c:38216

```
// E.g. ((5,4,3),(2,1,0)) for ARRAY (STC1 RANGE <>, STC2 RANGE <>)                            
```

### ada83.c:38217

```
// - the inner aggregate has 3 elements, so dim_hi[1] = STC2'FIRST + 3 - 1.                  
```

### ada83.c:38218

```
//                                                                                            
```

### ada83.c:38222

```
// A string-literal sub-aggregate stands for a positional array of its
```

### ada83.c:38223

```
// characters (RM 2.6): its length fixes this — the final — inner
```

### ada83.c:38224

```
// dimension. Without this the dimension keeps the index subtype's
```

### ada83.c:38225

```
// full range, so the row size overflows to zero (a [0 x i8] store)
```

### ada83.c:38226

```
// and the value compares unequal. The named-choice path below already
```

### ada83.c:38227

```
// handles this; mirror it for positional sub-aggregates.
```

### ada83.c:38242

```
// a string literal is one-dimensional; no deeper dims
```

### ada83.c:38252

```
// The inner dimension's length is the positional count; its low bound
```

### ada83.c:38253

```
// is the index subtype's FIRST. Fold that low bound to an integer even
```

### ada83.c:38254

```
// when it is a named-number expression (e.g. INDEX with a static
```

### ada83.c:38255

```
// constant FIRST), otherwise the dimension would keep the subtype's
```

### ada83.c:38256

```
// full range and report the wrong length.
```

### ada83.c:38270

```
// Descend into the first element of this inner aggregate
```

### ada83.c:38271

```
// for the next dimension (3-D, 4-D, ...)
```

### ada83.c:38277

```
// Named aggregates: compute outer bounds from explicit choice indices.                      
```

### ada83.c:38278

```
// E.g. (1 => "WHEN", 2 => "WHAT") → dim_lo[0]=1, dim_hi[0]=2.                               
```

### ada83.c:38279

```
// Also compute inner dimension from first inner element's size.                             
```

### ada83.c:38280

```
//                                                                                            
```

### ada83.c:38288

```
// Inner dimension: if inner elements are string literals,
```

### ada83.c:38289

```
// use string length for the second dimension extent.
```

### ada83.c:38299

```
// Inner aggregate: count its elements
```

### ada83.c:38318

```
// For multidim aggregates with unconstrained types (or constrained                             
```

### ada83.c:38319

```
// types whose inner bounds span the full index subtype range), walk                            
```

### ada83.c:38320

```
// into inner sub-aggregates and propagate their choice bounds to                               
```

### ada83.c:38321

```
// dim_lo[1..N]/dim_hi[1..N]. Without this, inner dims default to                              
```

### ada83.c:38322

```
// the full index subtype range (e.g. INTEGER: -2^31..2^31-1),                                  
```

### ada83.c:38323

```
// causing 4 GB allocations.                                                                   
```

### ada83.c:38324

```
// RANGE choices have already been desugared to NK_RANGE by                                     
```

### ada83.c:38325

```
// Desugar_Aggregate_Range_Choices, so only NK_RANGE is checked.                               
```

### ada83.c:38326

```
// Only propagate when inner bounds are effectively unconstrained:                              
```

### ada83.c:38327

```
// unconstrained type, or constrained type where inner dim bounds                               
```

### ada83.c:38328

```
// are full-range placeholders that would overflow size calcs.                                 
```

### ada83.c:38329

```
//                                                                                              
```

### ada83.c:38350

```
// Extract choice bounds from the inner sub-aggregate. RM 4.3.2(5):
```

### ada83.c:38351

```
// the aggregate's bounds are the MINIMUM low and MAXIMUM high over
```

### ada83.c:38352

```
// all its choices — range choices AND single index choices alike
```

### ada83.c:38353

```
// (e.g. (3 => -2, 4 => -3) spans 3..4). When every choice is
```

### ada83.c:38354

```
// static, fold the span; otherwise fall back to the first dynamic
```

### ada83.c:38355

```
// range's expressions.
```

### ada83.c:38396

```
// Descend into first inner association's value for next dim
```

### ada83.c:38407

```
// end need_propagation scope
```

### ada83.c:38410

```
// For multi-dimensional arrays, the effective "element" of the outer                           
```

### ada83.c:38411

```
// aggregate is a row (slice along the first dimension), not the scalar                         
```

### ada83.c:38412

```
// element_type. Compute the row size so memcpy uses the right length.                        
```

### ada83.c:38413

```
//                                                                                              
```

### ada83.c:38432

```
// outer dim "element" is a row
```

### ada83.c:38436

```
// Non-static named choice bounds (e.g. T'RANGE desugared to
```

### ada83.c:38437

```
// T'FIRST..T'LAST, or function calls). Negated integer literals like -1
```

### ada83.c:38438

```
// are static and must NOT force runtime bounds - only genuine runtime
```

### ada83.c:38439

```
// expressions do. For CONSTRAINED types the type already supplies static
```

### ada83.c:38440

```
// bounds; we only override dim_lo/dim_hi for UNCONSTRAINED types where
```

### ada83.c:38441

```
// the choices determine the aggregate's bounds (RM 4.3.2(4)).
```

### ada83.c:38442

```
//
```

### ada83.c:38446

```
// RM 4.3.2(4): For unconstrained types, named aggregate
```

### ada83.c:38447

```
// bounds come from the choices. Also override for
```

### ada83.c:38448

```
// constrained types whose dim-0 bounds are full-range
```

### ada83.c:38449

```
// placeholders (would overflow size calculations).
```

### ada83.c:38450

```
//
```

### ada83.c:38465

```
// RM 4.3.2: a non-static choice is only legal as the aggregate's sole
```

### ada83.c:38466

```
// choice, so a dynamic single choice determines BOTH bounds exactly:
```

### ada83.c:38467

```
// (I => E) has bounds I .. I.
```

### ada83.c:38474

```
// RM 3.5: a named choice written `S RANGE L..H` requires the range L..H to
```

### ada83.c:38475

```
// be compatible with the subtype mark S, checked when the aggregate is
```

### ada83.c:38476

```
// evaluated — independently of the index-subtype membership check below.
```

### ada83.c:38477

```
// Emitted once per choice here so it covers both the static and dynamic
```

### ada83.c:38478

```
// element-placement paths uniformly.
```

### ada83.c:38486

```
// Any dimension with dynamic bounds makes this a runtime-bounds
```

### ada83.c:38487

```
// aggregate (RM 3.6.1): Type_Bound_Value returns 0 for BOUND_EXPR, so a
```

### ada83.c:38488

```
// compile-time size calculation would be wrong; the runtime-bounds
```

### ada83.c:38489

```
// preamble evaluates bounds via Emit_Single_Bound instead.
```

### ada83.c:38490

```
// Note: a dynamic range choice only forces dynamic_bounds when it
```

### ada83.c:38491

```
// actually changed dim_lo/dim_hi to BOUND_EXPR (unconstrained types).
```

### ada83.c:38492

```
// Constrained types keep their static compile-time bounds; their dynamic
```

### ada83.c:38493

```
// choice expressions become runtime operands of the same pipeline.
```

### ada83.c:38494

```
//
```

### ada83.c:38503

```
// ── One per-choice lowering pipeline ───────────────────────────────────
```

### ada83.c:38504

```
// The two bound-regime preambles below establish the aggregate's bounds
```

### ada83.c:38505

```
// as Aggregate_Operands (compile-time constants, or SSA values for a
```

### ada83.c:38506

```
// runtime-bounds subtype), allocate the storage, and apply the
```

### ada83.c:38507

```
// aggregate-level constraint checks of their regime. Everything after —
```

### ada83.c:38508

```
// the per-choice lowering, the OTHERS fill, the bound reporting, and the
```

### ada83.c:38509

```
// fat-pointer result — is one pipeline over those operands: "static" is
```

### ada83.c:38510

```
// a property of an operand, not a separate lowering path.
```

### ada83.c:38512

```
// Check whether the element is composite (record or constrained array):
```

### ada83.c:38513

```
// Generate_Expression then returns a ptr to an alloca, and elements are
```

### ada83.c:38514

```
// copied with memcpy instead of a store. A multi-dimensional aggregate's
```

### ada83.c:38515

```
// outer "element" is a ROW (memcpy), even when the scalar component is a
```

### ada83.c:38516

```
// fat pointer — the fat store path applies only at the innermost (1-D)
```

### ada83.c:38517

```
// level. This one verdict drives the storage allocation and every store.
```

### ada83.c:38528

```
// compile-time bounds (static regime)
```

### ada83.c:38529

```
// runtime bounds (runtime regime)
```

### ada83.c:38530

```
// runtime regime: count-derived bounds
```

### ada83.c:38531

```
// post-check tracked span vs constraint
```

### ada83.c:38532

```
// SSA temp for runtime row size
```

### ada83.c:38538

```
// RM 3.2.1: When BOUND_EXPR has no cached_temp, the expression                               
```

### ada83.c:38539

```
// would be re-evaluated with stale side effects (e.g., F(I) long                             
```

### ada83.c:38540

```
// after the declaration that created this constrained subtype).                             
```

### ada83.c:38541

```
// For positional aggregates, derive bounds from the count instead                            
```

### ada83.c:38542

```
// of re-evaluating the type's constraint expression.                                        
```

### ada83.c:38543

```
//                                                                                            
```

### ada83.c:38548

```
// Positional aggregate with stale constraint: use count as bound.
```

### ada83.c:38549

```
// low = index type FIRST (usually 1 for NATURAL/POSITIVE),
```

### ada83.c:38550

```
// high = low + n_positional - 1.
```

### ada83.c:38551

```
//
```

### ada83.c:38555

```
// A dynamic constraint low (e.g. SUBDESIGNATED(IDENT_INT(5)..)):
```

### ada83.c:38556

```
// evaluate it, and derive the high from the positional element count.
```

### ada83.c:38557

```
// (Falling back to the index subtype's first would give 0-based
```

### ada83.c:38558

```
// bounds and fail the constrained-target bound check.)
```

### ada83.c:38580

```
// Report bounds to outer multidim aggregate for consistency check
```

### ada83.c:38588

```
// Track which expression nodes produced low_val / high_val so
```

### ada83.c:38589

```
// the range-choice loop below can reuse them (avoid double eval).
```

### ada83.c:38595

```
// For multidim with dynamic inner bounds, compute row_size and
```

### ada83.c:38596

```
// total_flat_count at runtime so allocation is correct.
```

### ada83.c:38598

```
// Evaluate inner dimension bounds at runtime
```

### ada83.c:38614

```
// RM 4.3.2(3): inner dimension choice bounds must belong to                                
```

### ada83.c:38615

```
// the corresponding index subtype, even for null outer ranges.                            
```

### ada83.c:38616

```
// Check at runtime before computing row_size.                                             
```

### ada83.c:38617

```
//                                                                                          
```

### ada83.c:38631

```
// Skip if full INTEGER range (no real subtype constraint)
```

### ada83.c:38654

```
// A fat-stored scalar element occupies a { ptr, ptr } slot.
```

### ada83.c:38661

```
// Clamp to >= 0: null inner ranges produce negative sizes
```

### ada83.c:38668

```
// A 1-D array whose element is a dynamically-sized record: the per-element
```

### ada83.c:38669

```
// stride and memcpy size are the element's runtime byte size (RM 3.6.1),
```

### ada83.c:38670

```
// not a static type size. Fill and allocation both flow through rt_row_size.
```

### ada83.c:38681

```
// Calculate count and byte size
```

### ada83.c:38685

```
// Runtime elem_size: total = outer_count * row_byte_size
```

### ada83.c:38696

```
// Clamp byte_size to 0 for null ranges (RM 3.6.1)
```

### ada83.c:38701

```
// Dynamic stack allocation
```

### ada83.c:38705

```
// RM 4.3.2(6): Dynamic aggregate bounds vs constraint check.                                
```

### ada83.c:38706

```
// When the aggregate type is a constrained subtype of an unconstrained                       
```

### ada83.c:38707

```
// base and the constraint bounds are runtime-determined, verify at                           
```

### ada83.c:38708

```
// runtime that the aggregate's "natural" bounds match the constraint.                       
```

### ada83.c:38709

```
// Positional: per RM 4.3.2(4), lower bound = applicable index                                
```

### ada83.c:38710

```
//   constraint's first, so bounds automatically match if count = constraint                  
```

### ada83.c:38711

```
//   size. Check n_positional vs constraint element count.                                  
```

### ada83.c:38712

```
// Named (static choices): choice bounds must equal constraint bounds.                       
```

### ada83.c:38713

```
// Skip when bounds reference discriminants whose disc_agg_temp                               
```

### ada83.c:38714

```
// hasn't been set up yet (not safely evaluable; RM 3.7.1).                                  
```

### ada83.c:38715

```
//                                                                                            
```

### ada83.c:38728

```
// RM 3.2.1: Also skip when BOUND_EXPR has no cached                                    
```

### ada83.c:38729

```
// temp - the expression would be re-evaluated with                                     
```

### ada83.c:38730

```
// stale side effects (e.g., F(I) after elaboration).                                  
```

### ada83.c:38731

```
//                                                                                      
```

### ada83.c:38748

```
// RM 4.3.2(5): For a positional aggregate of a constrained                           
```

### ada83.c:38749

```
// subtype, only the count must match - the lower bound is                            
```

### ada83.c:38750

```
// determined by the constraint.                                                     
```

### ada83.c:38751

```
//                                                                                    
```

### ada83.c:38760

```
// RM 4.3.2: Parenthesized aggregate - INDEX_SUBTYPE'FIRST                              
```

### ada83.c:38761

```
// may differ from the runtime constraint lower bound.                                 
```

### ada83.c:38762

```
// Compare against the type's actual constraint bound, not                              
```

### ada83.c:38763

```
// clo (which may be a stale-bounds fallback).                                         
```

### ada83.c:38764

```
//                                                                                      
```

### ada83.c:38779

```
// Each named dimension's explicit choice bounds must equal that
```

### ada83.c:38780

```
// dimension's constraint (RM 4.3.2). Walk down one aggregate level
```

### ada83.c:38781

```
// per dimension, comparing the level's static choice span against
```

### ada83.c:38782

```
// the matching index constraint. A multidimensional aggregate is
```

### ada83.c:38783

```
// checked at every level, not just the outermost.
```

### ada83.c:38797

```
// next dimension's sub-aggregate
```

### ada83.c:38802

```
// RM 4.3.2: When bounds are stale (bounds_ref_unset_disc), the                               
```

### ada83.c:38803

```
// constraint checks above are skipped. But parenthesized                                    
```

### ada83.c:38804

```
// aggregates still need a lower-bound check: INDEX_SUBTYPE'FIRST                             
```

### ada83.c:38805

```
// may differ from the constraint's lower bound. Evaluate the                                
```

### ada83.c:38806

```
// constraint bound directly - safe for variable references.                                 
```

### ada83.c:38807

```
//                                                                                            
```

### ada83.c:38826

```
// end bounds_ref_disc scope
```

### ada83.c:38828

```
// ── Populate the emission context (runtime-bounds regime) ────────────
```

### ada83.c:38838

```
// RM 4.3.2(3): choices are checked against the first index subtype
```

### ada83.c:38839

```
// when it carries meaningful static bounds (a full-INTEGER range is
```

### ada83.c:38840

```
// no real constraint).
```

### ada83.c:38857

```
// RM 4.3.2: for constrained arrays WITHOUT OTHERS, the aggregate span
```

### ada83.c:38858

```
// (min-low, max-high across all named choices) must match the
```

### ada83.c:38859

```
// constraint bounds - even for null ranges - so track it at runtime
```

### ada83.c:38860

```
// and compare after the choices are lowered.
```

### ada83.c:38865

```
// RM 4.3.2(6): all inner sub-aggregates of a multidim aggregate must
```

### ada83.c:38866

```
// have the same bounds. A null aggregate (clamped byte size 0) has no
```

### ada83.c:38867

```
// components, so its consistency check is vacuous.
```

### ada83.c:38874

```
// Static bounds: use compile-time allocation and unrolled initialization
```

### ada83.c:38879

```
// Ensure at least 1 element for safety
```

### ada83.c:38881

```
// Report bounds to outer multidim aggregate for consistency check
```

### ada83.c:38890

```
// RM 4.3.2(3): index subtype bounds for aggregate constraint checking.                        
```

### ada83.c:38891

```
// Each choice bound of a non-null range must belong to the index                               
```

### ada83.c:38892

```
// subtype. This check applies when:                                                           
```

### ada83.c:38893

```
//   (a) the type is unconstrained (choices define aggregate bounds),                           
```

### ada83.c:38894

```
//   (b) it's a constrained subtype of an unconstrained base                                    
```

### ada83.c:38895

```
//       (T IS BASE (5..7) where BASE IS ARRAY (ST RANGE <>)),                                  
```

### ada83.c:38896

```
//   (c) directly constrained with named index type                                             
```

### ada83.c:38897

```
//       (ARRAY (STA RANGE 5..6, ...) where STA IS INTEGER RANGE 4..7);                         
```

### ada83.c:38898

```
//       choice bounds must belong to the index type (STA = 4..7).                             
```

### ada83.c:38899

```
//                                                                                              
```

### ada83.c:38903

```
// Index subtype bounds: from the unconstrained base's index type, else from
```

### ada83.c:38904

```
// a directly-unconstrained type's index type.
```

### ada83.c:38906

```
// index subtype bounds known at compile time
```

### ada83.c:38907

```
// the index subtype, for runtime bound eval
```

### ada83.c:38917

```
// A dynamic index subtype (e.g. INTEGER RANGE IDENT_INT(1)..IDENT_INT(30))
```

### ada83.c:38918

```
// cannot be folded — Type_Bound_Value yields 0, which would make the
```

### ada83.c:38919

```
// compile-time check reject every choice. Mark it so the check uses the
```

### ada83.c:38920

```
// subtype's runtime bounds instead.
```

### ada83.c:38927

```
// RM 4.3.2(3): for named aggregates of unconstrained types, the
```

### ada83.c:38928

```
// bounds are determined by the choices. The lower bound is the
```

### ada83.c:38929

```
// minimum of all range-low values; the upper bound is the maximum
```

### ada83.c:38930

```
// of all range-high values. For a null range (L..H where L>H),
```

### ada83.c:38931

```
// the bounds stay L..H because lows and highs are tracked separately.
```

### ada83.c:38932

```
//
```

### ada83.c:38935

```
// non-integer range bounds
```

### ada83.c:38938

```
// RM 4.3.2(5): Named aggregate bounds are determined by the
```

### ada83.c:38939

```
// lowest and highest choices. For aggregates without OTHERS,
```

### ada83.c:38940

```
// the aggregate storage uses choice bounds (sliding occurs at
```

### ada83.c:38941

```
// assignment for constrained targets). With OTHERS, the
```

### ada83.c:38942

```
// aggregate must cover the full type range. Dynamic choice bounds
```

### ada83.c:38943

```
// leave the folded low/high alone: their runtime values are seeded
```

### ada83.c:38944

```
// into the fat-pointer bounds when the choices are lowered.
```

### ada83.c:38945

```
//
```

### ada83.c:38953

```
// must match the constraint bounds. For positional aggregates, the                            
```

### ada83.c:38954

```
// lower bound is INDEX_SUBTYPE'FIRST (from the base unconstrained                              
```

### ada83.c:38955

```
// type), which may differ from the constraint. For named aggregates                           
```

### ada83.c:38956

```
// without OTHERS, the bounds come from the choices.                                           
```

### ada83.c:38957

```
//                                                                                              
```

### ada83.c:38964

```
// RM 4.3.2(5): Count must match constraint size.
```

### ada83.c:38971

```
// RM 4.3.2: A parenthesized aggregate ((a,b,c)) uses                                       
```

### ada83.c:38972

```
// INDEX_SUBTYPE'FIRST as lower bound, which may differ from                                
```

### ada83.c:38973

```
// the constraint. Detect and raise CONSTRAINT_ERROR.                                     
```

### ada83.c:38974

```
//                                                                                          
```

### ada83.c:38985

```
// Dynamic INDEX_SUBTYPE'FIRST: runtime comparison
```

### ada83.c:39000

```
// RM 4.3.2(4): a named array aggregate without OTHERS takes its bounds
```

### ada83.c:39001

```
// from its choices (low..high here), independently of the target subtype.
```

### ada83.c:39002

```
// What the bounds must then satisfy depends on the consumer:
```

### ada83.c:39003

```
//   • Only an array ASSIGNMENT slides (RM 5.2.1): when this aggregate is
```

### ada83.c:39004

```
//     the top-level RHS of an assignment statement, just the LENGTH need
```

### ada83.c:39005

```
//     agree, e.g. (WED..FRI => 0, SAT..SUN => 1) := ARR_DAY(MON..FRI).
```

### ada83.c:39006

```
//   • An inner DIMENSION ROW of a sliding aggregate slides with it —
```

### ada83.c:39007

```
//     every dimension of the assigned value converts together.
```

### ada83.c:39008

```
//   • Every other consumer — a parameter/default value, a qualified
```

### ada83.c:39009

```
//     expression, or a COMPONENT of an enclosing aggregate — requires the
```

### ada83.c:39010

```
//     value to BELONG to the constrained subtype (RM 4.3.2 / 3.6.1), so
```

### ada83.c:39011

```
//     its bounds must EQUAL the constraint; a component never slides.
```

### ada83.c:39012

```
// Index-value legality against the index subtype is enforced separately by
```

### ada83.c:39013

```
// the per-choice index-subtype check. (con_lo/con_hi is the dim-0
```

### ada83.c:39014

```
// constraint; inner sub-aggregates are checked against the inner one.)
```

### ada83.c:39015

```
// The eligibility itself is computed once per aggregate by the
```

### ada83.c:39016

```
// Generate_Aggregate wrapper, including the row inheritance.
```

### ada83.c:39019

```
// A "constraint" that is merely the index subtype's own full range is
```

### ada83.c:39020

```
// not a narrowing constraint: the target is unconstrained (A : A_12,
```

### ada83.c:39021

```
// A_12 IS ARRAY (I RANGE <>, ...)), so the named aggregate defines its
```

### ada83.c:39022

```
// own bounds within the index subtype (RM 4.3.2(4)), and there is no
```

### ada83.c:39023

```
// constraint for them to equal. Per-choice index-subtype membership is
```

### ada83.c:39024

```
// checked separately. Only a real narrowing constraint requires the
```

### ada83.c:39025

```
// aggregate bounds to match.
```

### ada83.c:39051

```
// RM 4.3.2(6): dynamic constraint check under static bounds.
```

### ada83.c:39052

```
// Positional overrides may convert BOUND_EXPR → BOUND_INTEGER, landing
```

### ada83.c:39053

```
// in this regime even though the TYPE's constraint is dynamic.
```

### ada83.c:39054

```
// Detect this and emit a runtime check against the original bounds.                           
```

### ada83.c:39055

```
// Skip when bounds reference discriminants whose disc_agg_temp                                 
```

### ada83.c:39056

```
// hasn't been set up yet (not safely evaluable; RM 3.7.1).                                    
```

### ada83.c:39057

```
//                                                                                              
```

### ada83.c:39063

```
// Unsafe to evaluate here: a discriminant bound whose
```

### ada83.c:39064

```
// disc_agg_temp is unset (RM 3.7.1), or any BOUND_EXPR without a cached
```

### ada83.c:39065

```
// temp (re-evaluating would repeat side effects, RM 3.2.1).
```

### ada83.c:39088

```
// RM 4.3.2(5): For a positional aggregate of a constrained                             
```

### ada83.c:39089

```
// subtype (whether sub-aggregate or direct), only the count                            
```

### ada83.c:39090

```
// must match the constraint - the lower bound is determined                            
```

### ada83.c:39091

```
// by the constraint, not by INDEX_SUBTYPE'FIRST.                                      
```

### ada83.c:39092

```
//                                                                                      
```

### ada83.c:39101

```
// Parenthesized: INDEX_SUBTYPE'FIRST vs runtime constraint
```

### ada83.c:39125

```
// end bounds_ref_disc_s scope
```

### ada83.c:39127

```
// RM 4.3.2(6): an aggregate belonging to a DIRECTLY-declared constrained
```

### ada83.c:39128

```
// subtype whose constraint is non-static is covered by none of the checks
```

### ada83.c:39129

```
// above — the static regime needs BOUND_INTEGER bounds, the unconstrained-base
```

### ada83.c:39130

```
// path needs a base to slide from, and the dynamic-choice path re-evaluates
```

### ada83.c:39131

```
// the (unsafe) bound. Its bounds are frozen in the runtime descriptor
```

### ada83.c:39132

```
// (rt_global_id), so the count check reads those instead of re-evaluating
```

### ada83.c:39133

```
// the constraint. This is the single site that reports a mismatch for such
```

### ada83.c:39134

```
// a subtype, whether the aggregate is an object initializer, a qualified
```

### ada83.c:39135

```
// expression, or a parameter default materialised at a call.
```

### ada83.c:39143

```
// Check if element type is composite (record or constrained array).                           
```

### ada83.c:39144

```
// For composite elements, Generate_Expression returns a ptr to an alloca,                      
```

### ada83.c:39145

```
// so we must use memcpy to copy element data instead of store.                                
```

### ada83.c:39146

```
// Multi-dimensional arrays are always composite at the outer level                             
```

### ada83.c:39147

```
// because each "element" is a row (inner array).                                              
```

### ada83.c:39148

```
// Exception: fat-pointer elements ({ ptr, ptr }) are stored via                                
```

### ada83.c:39149

```
// `store` like scalars, not via memcpy.                                                       
```

### ada83.c:39150

```
//                                                                                              
```

### ada83.c:39152

```
// An array of dynamically-sized records: every element occupies its runtime
```

### ada83.c:39153

```
// byte size, so the aggregate storage, the per-element offsets, and the
```

### ada83.c:39154

```
// per-element memcpy all flow through this size rather than a static one.
```

### ada83.c:39159

```
// TODO: mem-safety remove this after investigating OOM
```

### ada83.c:39165

```
// Allocate as byte array so the size matches the actual data layout
```

### ada83.c:39179

```
// Track which elements are initialized (for others clause)
```

### ada83.c:39183

```
// ── Populate the emission context (static-bounds regime) ─────────────
```

### ada83.c:39199

```
// RM 4.3.2(6): a constrained aggregate whose named choices carry
```

### ada83.c:39200

```
// runtime bounds tracks its actual span, for upward bound reporting
```

### ada83.c:39201

```
// to an enclosing aggregate. (Static choice spans were checked against
```

### ada83.c:39202

```
// the constraint at compile time above; sliding contexts are exempt,
```

### ada83.c:39203

```
// RM 5.2.1(3).)
```

### ada83.c:39207

```
// RM 4.3.2(6): inner sub-aggregate consistency across rows; the check
```

### ada83.c:39208

```
// is deferred so all sub-aggregates are evaluated (for side effects)
```

### ada83.c:39209

```
// before raising CONSTRAINT_ERROR.
```

### ada83.c:39212

```
// Pre-materialize the low bound where a runtime store will subtract
```

### ada83.c:39213

```
// it, so emitted loops do not rebuild the constant per iteration.
```

### ada83.c:39226

```
// The runtime OTHERS fill must skip the components covered by the named
```

### ada83.c:39227

```
// choices; reserve one span slot per choice (RM 4.3.3(5)).
```

### ada83.c:39240

```
// ── Per-choice lowering (RM 4.3.2) ─────────────────────────────────────
```

### ada83.c:39241

```
// Clear inner bounds before the association loop so that stale values
```

### ada83.c:39242

```
// from early bounds reporting don't cause false inner-consistency
```

### ada83.c:39243

```
// mismatches when an element is a non-aggregate (e.g. string literal).
```

### ada83.c:39249

```
// Positional component: its flat index is its ordinal (RM 4.3.2(4)).
```

### ada83.c:39261

```
// deferred to the fill pass
```

### ada83.c:39263

```
// Single choice: one component at one index, always non-null.
```

### ada83.c:39264

```
// (Type-mark and T'RANGE choices were desugared to ranges up front.)
```

### ada83.c:39275

```
// Range choice L..H: the expression is evaluated once per component.
```

### ada83.c:39282

```
// Multidim inner aggregates with runtime choice bounds cannot be
```

### ada83.c:39283

```
// lowered by recursing per row: the inner bounds H,I of
```

### ada83.c:39284

```
// (F..G => (H..I => J)) are evaluated ONCE, while J is evaluated
```

### ada83.c:39285

```
// once per component (RM 4.3.2(6)). Detect and inline them.
```

### ada83.c:39306

```
// The outer window: the choice span when compile-time known, else
```

### ada83.c:39307

```
// the whole aggregate (preserving null ranges: high < low iterates
```

### ada83.c:39308

```
// zero times).
```

### ada83.c:39320

```
// Evaluate inner choice bounds once for side effects, even when
```

### ada83.c:39321

```
// the outer range is null (RM 4.3.2(6)). Also check inner bounds
```

### ada83.c:39322

```
// against the dim 1 index subtype and capture SSA values for the
```

### ada83.c:39323

```
// fat pointer.
```

### ada83.c:39325

```
// A compile-time verdict (and the static-bound runtime checks below)
```

### ada83.c:39326

```
// is only honest when the subtype's bounds are compile-time known:
```

### ada83.c:39327

```
// Type_Bound_Value on a dynamic bound (SUBINT RANGE IDENT_INT(1) ..
```

### ada83.c:39328

```
// IDENT_INT(6), cc1224a) yields nothing meaningful and folded an
```

### ada83.c:39329

```
// unconditional raise for a legal aggregate. When dynamic, skip —
```

### ada83.c:39330

```
// the inner aggregate's own emission checks its choices against the
```

### ada83.c:39331

```
// subtype's runtime bounds (Agg_Check_Choice_Index_Subtype).
```

### ada83.c:39369

```
// RM 4.3.2(3): inner dim non-null range bounds must belong
```

### ada83.c:39370

```
// to the dim 1 index subtype.
```

### ada83.c:39396

```
// RM 4.3.2(6): for a CONSTRAINED type, an inner aggregate
```

### ada83.c:39397

```
// consisting of one named range must fit the inner
```

### ada83.c:39398

```
// dimension's constraint — a dynamic (1..IDENT_INT(2) => 0)
```

### ada83.c:39399

```
// row for a 1..3 dimension raises CONSTRAINT_ERROR
```

### ada83.c:39400

```
// (c95085c subtest B). In a SLIDING context (assignment RHS,
```

### ada83.c:39401

```
// RM 5.2.1) only the LENGTH need agree (c43211a case B:
```

### ada83.c:39402

```
// (IDENT(4)..6 => 1) assigns into a 5..7 dimension).
```

### ada83.c:39429

```
// Nested loop: for each (row, col) cell, evaluate J and store at
```

### ada83.c:39430

```
// the flat offset in row-major order. The stored cell is a SCALAR
```

### ada83.c:39431

```
// at a scalar flat index, not a row.
```

### ada83.c:39452

```
// For multidim with compile-time inner bounds, check the inner
```

### ada83.c:39453

```
// aggregate's static choices against the dim 1 index subtype.
```

### ada83.c:39458

```
// Same honesty rule as the row fast path above: fold a verdict only
```

### ada83.c:39459

```
// from compile-time-known subtype bounds; a dynamic subtype's check
```

### ada83.c:39460

```
// is the inner aggregate's own runtime one.
```

### ada83.c:39504

```
// RM 4.3.2: the overall named-choice span must match the constraint
```

### ada83.c:39505

```
// bounds of a runtime-bounds constrained aggregate without OTHERS -
```

### ada83.c:39506

```
// even for null ranges.
```

### ada83.c:39519

```
// For multidim constrained arrays, also check inner dimension
```

### ada83.c:39520

```
// bounds from the first inner sub-aggregate's named range
```

### ada83.c:39521

```
// against the type's inner constraint. Runs OUTSIDE the outer
```

### ada83.c:39522

```
// loop so null outer ranges still get checked (RM 4.3.2).
```

### ada83.c:39523

```
// Skipped when dim-1's "constraint" is merely its index subtype's full
```

### ada83.c:39524

```
// range (an unconstrained target: A : A_12, A_12 IS ARRAY (I<>, I<>)) —
```

### ada83.c:39525

```
// the inner aggregate then defines its own bounds within the index
```

### ada83.c:39526

```
// subtype, with no constraint to equal.
```

### ada83.c:39545

```
// Find first named range in inner aggregate
```

### ada83.c:39561

```
// RM 5.2.1: in an assignment every dimension slides — the rows
```

### ada83.c:39562

```
// need only the constrained LENGTH, not the same bounds.
```

### ada83.c:39577

```
// only check first non-others association
```

### ada83.c:39581

```
// RM 4.3.2: a NAMED inner sub-aggregate states its bounds explicitly; those
```

### ada83.c:39582

```
// do not slide and must equal the corresponding inner index constraint
```

### ada83.c:39583

```
// (positional inner aggregates do slide and are handled by the consistency
```

### ada83.c:39584

```
// check plus assignment). Walk one level per inner dimension and, where the
```

### ada83.c:39585

```
// sub-aggregate's choice span and the constraint are both static, compare.
```

### ada83.c:39608

```
// RM 5.2.1: in an assignment every dimension slides — the rows
```

### ada83.c:39609

```
// need only the constrained LENGTH; fold the check statically.
```

### ada83.c:39633

```
// RM 4.3.2(6): after all rows are processed, raise if any inner
```

### ada83.c:39634

```
// sub-aggregate had mismatched bounds. Vacuous when no inner
```

### ada83.c:39635

```
// sub-aggregate was tracked (a null aggregate tracks none).
```

### ada83.c:39653

```
// For a CONSTRAINED aggregate type the inner dimensions must also
```

### ada83.c:39654

```
// match the TYPE's own constraint — mutually consistent rows of the
```

### ada83.c:39655

```
// wrong LENGTH are still wrong (c95085c: (1..3 => (1..IDENT_INT(2)
```

### ada83.c:39656

```
// => 0)) for a 3x3 type raises CONSTRAINT_ERROR).
```

### ada83.c:39688

```
// OTHERS fills the components no other association covers (RM 4.3.3(5)).
```

### ada83.c:39693

```
// Report actual aggregate bounds to the enclosing multidim aggregate.
```

### ada83.c:39694

```
// bnd[0] = this aggregate's own first-dimension bounds; bnd[1..] = the
```

### ada83.c:39695

```
// deeper dimensions' bounds from inner tracking. This overrides the
```

### ada83.c:39696

```
// early report, and runs after the OTHERS fill so a sub-aggregate
```

### ada83.c:39697

```
// generated there cannot clobber it. Skipped when the bounds are
```

### ada83.c:39698

```
// synthetic (count-derived, not the actual constraint values).
```

### ada83.c:39703

```
// The tracked named span IS the aggregate's actual bounds when the
```

### ada83.c:39704

```
// choices carried runtime bounds within a static-bounds subtype.
```

### ada83.c:39716

```
// Propagate tracked inner dimensions' bounds upward
```

### ada83.c:39734

```
// ── Result: raw storage, or a fat pointer when consumers need bounds ───
```

### ada83.c:39735

```
// A fat pointer { ptr, ptr } is required for unconstrained types AND
```

### ada83.c:39736

```
// constrained types with dynamic bounds; a multidim bounds block carries
```

### ada83.c:39737

```
// ALL dimension bounds in flat layout [lo0, hi0, lo1, hi1, ...].
```

### ada83.c:39747

```
// Dimension 0: the aggregate's own bounds; choice-seeded when an
```

### ada83.c:39748

```
// unconstrained aggregate evaluated runtime choice bounds.
```

### ada83.c:39754

```
// Inner dimensions: runtime-evaluated bounds when present, dim-1
```

### ada83.c:39755

```
// choice seeds from an inlined inner aggregate, else type bounds.
```

### ada83.c:39778

```
// Record aggregate - allocate [N x i8] and fill fields by offset
```

### ada83.c:39783

```
// Runtime-sized record: use the size from its rt descriptor.
```

### ada83.c:39788

```
// Unconstrained discriminated type sized by the aggregate's own
```

### ada83.c:39789

```
// discriminant (RM 4.3.1): the trailing capless component's extent is set
```

### ada83.c:39790

```
// by the supplied value, so the base's static size under-allocates it.
```

### ada83.c:39796

```
// Adjust record_size for discriminant-dependent array/string components
```

### ada83.c:39797

```
// whose sizes are not included in the type's static size (RM 3.7.1). The
```

### ada83.c:39798

```
// maximum extent of a bound named by a discriminant is capped by the INDEX
```

### ada83.c:39799

```
// SUBTYPE, not the discriminant's type — Max_Constrained_Array_Size applies
```

### ada83.c:39800

```
// that cap; a cap-less extent (discriminant of unbounded INTEGER) would
```

### ada83.c:39801

```
// overflow the size arithmetic to a bogus giant allocation.
```

### ada83.c:39814

```
// Pre-allocate temp-based discriminant storage for dependent array
```

### ada83.c:39815

```
// bounds (RM 3.7.1). Uses temp IDs instead of symbol names so each                            
```

### ada83.c:39816

```
// aggregate gets its own allocas (avoiding dominance violations when                           
```

### ada83.c:39817

```
// the same record type appears in multiple blocks).                                           
```

### ada83.c:39818

```
//                                                                                              
```

### ada83.c:39825

```
// Gather the discriminant-referencing expressions this component needs an
```

### ada83.c:39826

```
// alloca for: array index bounds, or nested-record disc constraints.
```

### ada83.c:39845

```
// Reserve one alloca per distinct discriminant referenced.
```

### ada83.c:39863

```
// Track initialized components for others clause
```

### ada83.c:39868

```
// The OTHERS association, whose value generation is deferred to the
```

### ada83.c:39869

```
// third pass.
```

### ada83.c:39874

```
// Wrap disc_alloc array for the helper API
```

### ada83.c:39877

```
// Pre-determine the selected variant (RM 3.7.3) so positional component
```

### ada83.c:39878

```
// mapping skips components belonging to other variants.
```

### ada83.c:39881

```
// ── Second pass: initialize fields ───────────────────────────────────────────────────────────
```

### ada83.c:39882

```
// Both named (field_name => expr) and positional (expr, expr, ...)                             
```

### ada83.c:39883

```
// forms use the unified Agg_Rec_Store / Agg_Rec_Disc_Post helpers.                            
```

### ada83.c:39884

```
//                                                                                              
```

### ada83.c:39886

```
// RM 3.7.2: a discriminant governs the subtype — and so the size — of its
```

### ada83.c:39887

```
// dependent components, so its value must reach the aggregate's
```

### ada83.c:39888

```
// discriminant bindings before a dependent component evaluates its bounds.
```

### ada83.c:39889

```
// Association order is the writer's choice ((REC => "ABC", A => 5),
```

### ada83.c:39890

```
// c43214f case 4) and RM 4.3 leaves component evaluation order
```

### ada83.c:39891

```
// unspecified: walk named discriminant associations first, everything
```

### ada83.c:39892

```
// else second. Positional items need no hoisting — discriminants are the
```

### ada83.c:39893

```
// leading components, and positional associations precede named ones.
```

### ada83.c:39898

```
// Named: field_name => value
```

### ada83.c:39924

```
// Positional: initialize component by position
```

### ada83.c:39926

```
// Skip components of non-selected variants so positional element N maps
```

### ada83.c:39927

```
// to the Nth component of the fixed part + the active variant.
```

### ada83.c:39938

```
// Type a nested aggregate as its component so it produces the right
```

### ada83.c:39939

```
// representation — a discriminant-dependent array component must come
```

### ada83.c:39940

```
// back as a fat pointer, not a value misread as a scalar.
```

### ada83.c:39960

```
// Determine selected variant for OTHERS filtering (RM 3.7.3).                                 
```

### ada83.c:39961

```
// In a variant record aggregate, OTHERS only applies to components                             
```

### ada83.c:39962

```
// in the fixed part and the selected variant - not all variants.                              
```

### ada83.c:39963

```
// selected_variant may already be set from the discriminant above; the
```

### ada83.c:39964

```
// methods below only refine it when still unknown.
```

### ada83.c:39968

```
// Method 1: infer from explicitly named variant components.                                 
```

### ada83.c:39969

```
// If C => 3 was named and C is in the WHEN TRUE variant, the                                 
```

### ada83.c:39970

```
// selected variant must be WHEN TRUE.                                                       
```

### ada83.c:39971

```
//                                                                                            
```

### ada83.c:39983

```
// Methods 2 & 3: select the variant from a known discriminant value -
```

### ada83.c:39984

```
// the OTHERS expression first (when present), else the type's constraint.
```

### ada83.c:40009

```
// ── Third pass: fill uninitialized with OTHERS value (RM 4.3.1) ──────────────────────────────
```

### ada83.c:40018

```
// Set type context so nested aggregates resolve correctly
```

### ada83.c:40032

```
// RM 3.6.1(7): Check disc-dependent array bounds against index subtype.                       
```

### ada83.c:40033

```
// This catches cases like SM_ARR (1..D1) when D1 exceeds the SM range.                        
```

### ada83.c:40034

```
// RM 3.7.3: Skip checks for components in unselected variants.                                
```

### ada83.c:40035

```
//                                                                                              
```

### ada83.c:40038

```
// Skip variant components not in the selected variant
```

### ada83.c:40053

```
// Only check bounds that reference discriminants.
```

### ada83.c:40078

```
// Clear disc_agg_temp so it doesn't leak into other contexts
```

### ada83.c:40079

```
// Type'(expression) - generate expression and convert to target type if needed
```

### ada83.c:40090

```
// RM 4.3.2 / 4.7: the bounds of a named array aggregate come from its sole
```

### ada83.c:40091

```
// choice, and qualification by a constrained array subtype requires them to
```

### ada83.c:40092

```
// EQUAL the type mark's first-dimension bounds — even between two null
```

### ada83.c:40093

```
// ranges (c48009e: CA3_2'(4..2 => 5) with CA3_2 bounds 3..2 raises
```

### ada83.c:40094

```
// CONSTRAINT_ERROR). Both spellings of qualification reach this: the
```

### ada83.c:40095

```
// explicit T'(agg) via Generate_Qualified, and NEW T'(agg) via the
```

### ada83.c:40096

```
// allocator, whose parser unwraps the qualified node.
```

### ada83.c:40116

```
// (I => E) has bounds I .. I
```

### ada83.c:40140

```
// RM 4.8: Propagate target access type to allocator in qualified expression
```

### ada83.c:40141

```
// context so disc/bounds constraint checks fire (e.g., ACTB'(NEW TB(2))).
```

### ada83.c:40154

```
// Get source and destination types
```

### ada83.c:40158

```
// Use the produced value's own rep, not the AST prediction (which can be a
```

### ada83.c:40159

```
// stale generic-formal width).
```

### ada83.c:40162

```
// A real value qualified to a fixed-point subtype converts to that type's
```

### ada83.c:40163

```
// scaled-integer representation (RM 4.7); a plain float-to-int conversion
```

### ada83.c:40164

```
// would drop the SMALL scaling.
```

### ada83.c:40173

```
// RM 4.7: Qualified expression checks value against subtype constraint
```

### ada83.c:40178

```
// RM 4.7(3): When the type mark is a constrained record subtype, the
```

### ada83.c:40179

```
// operand's discriminants must equal the type mark's — a mismatch raises
```

### ada83.c:40180

```
// CONSTRAINT_ERROR (c47008a). The operand for a record qualified is passed
```

### ada83.c:40181

```
// as a pointer; load each of its discriminants and compare to the type
```

### ada83.c:40182

```
// mark's constrained value.
```

### ada83.c:40201

```
// RM 4.7(4)/4.7(3): When the type mark is a constrained array subtype, the
```

### ada83.c:40202

```
// operand's index bounds must equal the type mark's bounds — sliding does
```

### ada83.c:40203

```
// not apply here (it applies only to assignment/param passing). Compare per
```

### ada83.c:40204

```
// dimension and raise CONSTRAINT_ERROR on any mismatch. Skip when the
```

### ada83.c:40205

```
// operand is not a fat pointer (its bounds already come from the type mark
```

### ada83.c:40206

```
// itself, so the check is a no-op) — c47007a.
```

### ada83.c:40227

```
// RM 4.3.2 / 4.7: a named array aggregate takes its bounds from its sole
```

### ada83.c:40228

```
// choice, but the constrained-type aggregate emitter materializes it
```

### ada83.c:40229

```
// directly in type-mark layout — no fat pointer, so the general check
```

### ada83.c:40230

```
// above never sees the choice's bounds.
```

### ada83.c:40234

```
// RM 3.7.2: a type mark denoting a constrained access subtype checks that the
```

### ada83.c:40235

```
// operand's designated object matches the access constraint (index bounds or
```

### ada83.c:40236

```
// discriminants); a non-null mismatch raises CONSTRAINT_ERROR.
```

### ada83.c:40248

```
// no widening - value stays at native type width.
```

### ada83.c:40249

```
// Callers use Emit_Convert at use sites.
```

### ada83.c:40252

```
// Flattened element-count*element-size for a (possibly multi-dimensional)
```

### ada83.c:40253

```
// array, computed from its runtime bounds array: product over dimensions of
```

### ada83.c:40254

```
// max(0, high-low+1), times the element size. `bounds_ptr` addresses low/high
```

### ada83.c:40255

```
// pairs (dimension d at indices d*2 and d*2+1). Returns a temp at `bt` width.
```

### ada83.c:40271

```
// Store a record component's default value into comp_ptr (RM 3.7). Single
```

### ada83.c:40272

```
// source of truth shared by allocator and object-declaration init. Handles:
```

### ada83.c:40273

```
//   - fixed/rt-size composite: memcpy (extracting fat-pointer data first)
```

### ada83.c:40274

```
//   - dynamic disc-dependent composite array: flatten bounds -> size -> memcpy
```

### ada83.c:40275

```
//   - scalar/fixed/float: convert and store
```

### ada83.c:40284

```
// Default produced a fat value directly (SSA aggregate, dynamic op, …);
```

### ada83.c:40285

```
// the memcpy source is its data field.
```

### ada83.c:40307

```
// Dynamic-size composite (disc-dependent array): extract data from the
```

### ada83.c:40308

```
// fat-pointer aggregate, compute byte size from bounds, and memcpy.
```

### ada83.c:40315

```
// val is the SSA fat pointer { ptr, ptr } from the aggregate.
```

### ada83.c:40349

```
// RM 4.8(6): check one allocated array dimension's actual bounds against the
```

### ada83.c:40350

```
// target access type's declared index bounds, raising CONSTRAINT_ERROR on
```

### ada83.c:40351

```
// mismatch. No-op when the target bounds are not statically known.
```

### ada83.c:40366

```
// RM 4.8(6)/3.6.1: check an allocated dimension's bounds (`lo`/`hi`, at width
```

### ada83.c:40367

```
// `from_bt`) lie within the designated array's index subtype. No-op when the
```

### ada83.c:40368

```
// index type is non-scalar. Bounds are widened to the integer arithmetic rep so
```

### ada83.c:40369

```
// the check does not assume the narrow subtype width.
```

### ada83.c:40380

```
// Compare a named aggregate's bounds (a_lo/a_hi) against the target subtype
```

### ada83.c:40381

```
// bounds (b_lo/b_hi) at `ait` width; raise CONSTRAINT_ERROR (tagged raise_msg)
```

### ada83.c:40382

```
// on any mismatch. All operands are temps already converted to `ait`.
```

### ada83.c:40386

```
// RM 4.3.2(6) / 4.6(11): the aggregate's inferred index range must equal
```

### ada83.c:40387

```
// the applicable constraint's endpoints — except when both sides are
```

### ada83.c:40388

```
// null. Two null ranges have length 0 regardless of their endpoints, so
```

### ada83.c:40389

```
// a mismatched-but-both-null pairing (agg 3..2 into target 2..1, per
```

### ada83.c:40390

```
// c52103s) is a semantic no-op that must not raise. All three other
```

### ada83.c:40391

```
// corners must raise: agg 1..3 vs target 2..4 (same length, shifted —
```

### ada83.c:40392

```
// c43004b), null agg into non-null target (c32112a), non-null agg into
```

### ada83.c:40393

```
// null target (c32112b). RM 4.7 qualified-expression context requires
```

### ada83.c:40394

```
// exact bounds match with no null exemption (c47007a: agg 2..1 into
```

### ada83.c:40395

```
// NULLA 1..0 both null but the qualified check must still raise).
```

### ada83.c:40415

```
// Runtime variant guard for dynamic discriminant values: when a component
```

### ada83.c:40416

```
// belongs to a non-`others` variant, branch past its initialization if the
```

### ada83.c:40417

```
// runtime disc value falls outside the variant's range. Positions emission at
```

### ada83.c:40418

```
// the in-range "check" block and returns the skip label (0 when no guard was
```

### ada83.c:40419

```
// emitted). Close with Emit_Close_Variant_Guard after the guarded init.
```

### ada83.c:40441

```
// RM 3.7.3: an OTHERS-arm component is present iff the discriminant lies
```

### ada83.c:40442

```
// outside every explicit WHEN choice's range. Skip it when the value
```

### ada83.c:40443

```
// falls in some explicit arm — otherwise its default overwrites the
```

### ada83.c:40444

```
// present arm's component in the shared variant storage.
```

### ada83.c:40473

```
// Close a runtime variant guard opened by Emit_Variant_Disc_Guard: branch to
```

### ada83.c:40474

```
// the skip label and position there. No-op when skip_label is 0.
```

### ada83.c:40481

```
// Open a "skip the guarded work when `ptr_reg` is null" region (RM 4.8(4): null
```

### ada83.c:40482

```
// satisfies any access constraint). Emits the null test and branch, positions
```

### ada83.c:40483

```
// emission at the non-null block, and returns the skip label to hand to
```

### ada83.c:40484

```
// Emit_Close_Null_Skip after the guarded work.
```

### ada83.c:40485

```
// Open a region skipped when `cond_reg` (an i1) is true: emits the branch,
```

### ada83.c:40486

```
// positions at the do-the-work block, returns the skip label for
```

### ada83.c:40487

```
// Emit_Close_Null_Skip.
```

### ada83.c:40502

```
// Unconditionally raise CONSTRAINT_ERROR (tagged `msg`), then open a fresh
```

### ada83.c:40503

```
// continuation block so following code is well-formed.
```

### ada83.c:40511

```
// Initialize record `rec`'s discriminant fields at `base` from its recorded
```

### ada83.c:40512

```
// discriminant constraint values, defaulting an absent value to 0. When
```

### ada83.c:40513

```
// `check_subtype` is set, each scalar value is range-checked against its
```

### ada83.c:40514

```
// discriminant subtype first (RM 3.7.2).
```

### ada83.c:40528

```
// For each discriminant of record `des`, load its value from `base + offset`
```

### ada83.c:40529

```
// and raise CONSTRAINT_ERROR (tagged `msg`) when it differs from the type's
```

### ada83.c:40530

```
// recorded discriminant constraint. Discriminants with no static constraint
```

### ada83.c:40531

```
// value are skipped.
```

### ada83.c:40544

```
// RM 3.7: initialize record `rec` at `base` — store its discriminant constraint
```

### ada83.c:40545

```
// values (if constrained), then apply each non-discriminant component default,
```

### ada83.c:40546

```
// memcpy'ing composite defaults and storing scalar ones.
```

### ada83.c:40547

```
// True when the type carries component defaults anywhere in its composition:
```

### ada83.c:40548

```
// a record with a defaulted component (directly or inside a composite
```

### ada83.c:40549

```
// component), or a constrained array whose element type does (RM 3.7).
```

### ada83.c:40569

```
// RM 3.2.1(20): access objects default to NULL. For a composite variable
```

### ada83.c:40570

```
// without an explicit initializer, that guarantee reaches inside — each
```

### ada83.c:40571

```
// access-typed sub-component (records, arrays, arrays-of-records, nested
```

### ada83.c:40572

```
// records) must start NULL. Returns true iff any leaf component of `t`
```

### ada83.c:40573

```
// is an access type, so the object-decl emit path knows to zero-init.
```

### ada83.c:40597

```
// Apply this record's own component defaults through the general machinery —
```

### ada83.c:40598

```
// it binds each discriminant to its stored field so a self-referential
```

### ada83.c:40599

```
// default (D : INTEGER := A, with A a discriminant) resolves through the
```

### ada83.c:40600

```
// record's storage, guards run-time variants, and range-checks scalars.
```

### ada83.c:40604

```
// Keep the discriminants bound (disc_agg_temp) through the nested-component
```

### ada83.c:40605

```
// recursion below: a subcomponent constrained by an enclosing discriminant
```

### ada83.c:40606

```
// (B : REC1 (D)) resolves its constraint through Emit_Disc_Constraint_Value,
```

### ada83.c:40607

```
// which reads the bound discriminant. Resetting the bindings before the
```

### ada83.c:40608

```
// recursion would leave that reference unbound and store a static 0 for the
```

### ada83.c:40609

```
// subcomponent discriminant instead of the enclosing value (cc3121a).
```

### ada83.c:40614

```
// A composite component without its own default may still carry defaults
```

### ada83.c:40615

```
// INSIDE its type (a record component whose type defaults its fields, or
```

### ada83.c:40616

```
// an array of such records) — recurse into it (RM 3.7).
```

### ada83.c:40634

```
// The nested recursion is done; drop the discriminant bindings before the
```

### ada83.c:40635

```
// constraint check below so it reads each discriminant's STORED runtime value
```

### ada83.c:40636

```
// (c37213d: an index check keyed on a defaulted discriminant must see the
```

### ada83.c:40637

```
// actual value, not the bound default).
```

### ada83.c:40640

```
// RM 3.7.2: with the discriminants now stored (from the constraint or the
```

### ada83.c:40641

```
// defaults just applied), check every discriminant-dependent component
```

### ada83.c:40642

```
// constraint against its subtype. This runs for each record initialized here
```

### ada83.c:40643

```
// — a nested record component and every element of an array of records — so
```

### ada83.c:40644

```
// the check reaches those the top-level object path does not.
```

### ada83.c:40648

```
// RM 3.7: apply every component default of record `ty` into the storage at base
```

### ada83.c:40649

```
// register `base`. Components in unselected variants are skipped when
```

### ada83.c:40650

```
// `sel_variant` != -2; discriminant defaults are skipped when already fixed by
```

### ada83.c:40651

```
// a constraint (`skip_disc_defaults`). Scalar defaults are range-checked against
```

### ada83.c:40652

```
// their subtype, and a discriminant default's value is bound (via an alloca,
```

### ada83.c:40653

```
// recorded in seen[*count]) so dependent component sizes resolve. Honors the
```

### ada83.c:40654

```
// record's runtime component-offset table when present.
```

### ada83.c:40658

```
// When the selected variant is only known at run time (a default-discriminant
```

### ada83.c:40659

```
// object), each variant component's default is applied under a guard keyed on
```

### ada83.c:40660

```
// the governing discriminant — read lazily below, once the discriminants (the
```

### ada83.c:40661

```
// leading components) have been stored. Applying an absent arm's default would
```

### ada83.c:40662

```
// overwrite the present arm's component, which shares its union storage.
```

### ada83.c:40666

```
// Bind every discriminant that carries no default of its own to its stored
```

### ada83.c:40667

```
// field first. A self-referential non-discriminant default (T : INTEGER := A)
```

### ada83.c:40668

```
// must read the discriminant from the record; the main loop below only binds
```

### ada83.c:40669

```
// discriminants that have a default (it skips a component with no
```

### ada83.c:40670

```
// default_expr), leaving a constrained discriminant like A unbound and its
```

### ada83.c:40671

```
// reference lowered to an undefined symbol (c38107b).
```

### ada83.c:40675

```
// bound by the main loop
```

### ada83.c:40692

```
// The discriminant value already lives in the record (stored from the
```

### ada83.c:40693

```
// constraint, not from this default). Still bind dependent component
```

### ada83.c:40694

```
// bounds to read it from that field, so a default like STRING(1..L)
```

### ada83.c:40695

```
// resolves L (RM 3.7.1) instead of an unbound discriminant symbol.
```

### ada83.c:40710

```
// Runtime variant guard for a default-discriminant object. Read the
```

### ada83.c:40711

```
// governing discriminant lazily — the discriminants precede the variant
```

### ada83.c:40712

```
// components, so it is already stored by the time one is reached.
```

### ada83.c:40729

```
// RM 3.2.1/3.6.1: a multiple-name declaration's default expression node is
```

### ada83.c:40730

```
// shared, but each name has its own (copied) subtype with its own frozen
```

### ada83.c:40731

```
// runtime bounds. Retarget the shared aggregate to THIS component's subtype
```

### ada83.c:40732

```
// so it reads the descriptor instead of re-evaluating the constraint.
```

### ada83.c:40738

```
// RM 4.8: an allocator default for an access component is constrained by
```

### ada83.c:40739

```
// the component's access subtype — propagate it so the allocated object's
```

### ada83.c:40740

```
// bounds/value are checked against the designated subtype.
```

### ada83.c:40755

```
// RM 3.6.1: an array/string record-component default does NOT slide — the
```

### ada83.c:40756

```
// value's bounds must equal the component's (unlike an object init, which
```

### ada83.c:40757

```
// slides). Comparing each dimension's 'FIRST suffices to detect the
```

### ada83.c:40758

```
// mismatch (e.g. STRING (2..4) into a STRING (1..3) component).
```

### ada83.c:40791

```
// RM 3.7.2: for each dimension of array type `arr` whose bounds depend on a
```

### ada83.c:40792

```
// discriminant of `rec` (held at `sym` + `base_off` + the disc's offset),
```

### ada83.c:40793

```
// resolve the actual low/high values and — skipping null ranges — check the
```

### ada83.c:40794

```
// disc-derived bounds against the dimension's index subtype.
```

### ada83.c:40804

```
// A bound that is a discriminant (value taken from the record's storage) or
```

### ada83.c:40805

```
// a non-discriminant expression frozen once at type elaboration (RM 3.7.1)
```

### ada83.c:40806

```
// carries a runtime value: `dynamic` marks such a bound. When at least one
```

### ada83.c:40807

```
// bound is dynamic the array subtype (e.g. MY_ARR (0 .. D3)) is determined
```

### ada83.c:40808

```
// only at this elaboration — it has no static elaboration point of its own —
```

### ada83.c:40809

```
// so RM 3.6.1 requires BOTH its bounds to lie in the index subtype once the
```

### ada83.c:40810

```
// range is known non-empty, including a static bound like 0 that a plain
```

### ada83.c:40811

```
// static array would have had checked at elaboration.
```

### ada83.c:40832

```
// A fully static range is checked when the array type elaborates; skip it.
```

### ada83.c:40842

```
// RM 3.7: scan record `rec`'s array components for index bounds that reference
```

### ada83.c:40843

```
// the discriminant named `disc_name`, binding each matching discriminant
```

### ada83.c:40844

```
// symbol's disc_agg_temp to `slot` (so dependent component sizes resolve through
```

### ada83.c:40845

```
// the record's storage). Newly-bound symbols are appended to seen[*count] up to
```

### ada83.c:40846

```
// `cap`, for later cleanup.
```

### ada83.c:40860

```
// A discriminant referenced in a dependent array bound (STRING (1 .. L)).
```

### ada83.c:40869

```
// RM 3.7.1: a discriminant referenced in another component's default
```

### ada83.c:40870

```
// expression (Y : INTEGER := D1 + D2) resolves to the stored discriminant.
```

### ada83.c:40879

```
// RM 3.7.2(3): check the discriminant constraints of `ty`'s nested record/array
```

### ada83.c:40880

```
// components. Each parent discriminant referenced by a nested constraint
```

### ada83.c:40881

```
// expression or dependent array bound is bound to its field address within
```

### ada83.c:40882

```
// `base` (so the recursive check resolves it through the record's own storage),
```

### ada83.c:40883

```
// the nested checks run, then the bindings are cleared.
```

### ada83.c:40919

```
// Read the governing variant discriminant from the object so the nested check
```

### ada83.c:40920

```
// can guard each variant component by the actual (default or constrained)
```

### ada83.c:40921

```
// value — otherwise it cannot tell which arm is present (RM 3.7.3).
```

### ada83.c:40940

```
// RM 4.8(6): dereference the access value stored at `slot` and, skipping null,
```

### ada83.c:40941

```
// check each discriminant of the designated record `des` against its constraint,
```

### ada83.c:40942

```
// raising CONSTRAINT_ERROR (tagged `msg`) on mismatch. `slot_is_fat` selects fat
```

### ada83.c:40943

```
// vs thin pointer load. Shared by the access-to-constrained allocator cases.
```

### ada83.c:40958

```
// RM 4.8(6): dereference the fat-pointer access value stored at `slot` and,
```

### ada83.c:40959

```
// skipping null, check the first dimension's actual bounds against the declared
```

### ada83.c:40960

```
// index bounds of the constrained designated array `des`, raising
```

### ada83.c:40961

```
// CONSTRAINT_ERROR (tagged `msg`) on mismatch. Twin of Emit_Deref_Disc_Checks
```

### ada83.c:40962

```
// for the access-to-constrained-array allocator cases.
```

### ada83.c:40981

```
// RM 3.7.2: converting or qualifying a value to a CONSTRAINED access subtype
```

### ada83.c:40982

```
// requires a non-null operand's designated object to have the index bounds or
```

### ada83.c:40983

```
// discriminant values fixed by the constraint; a mismatch raises
```

### ada83.c:40984

```
// CONSTRAINT_ERROR. NULL satisfies any access constraint (RM 4.8(4)), so each
```

### ada83.c:40985

```
// check skips a null designator. The operand value is in hand — a fat pointer
```

### ada83.c:40986

```
// for access-to-unconstrained-array (whose bounds are the designated object's)
```

### ada83.c:40987

```
// or a plain pointer to the designated record — so this is the value-carrying
```

### ada83.c:40988

```
// twin of the slot-based Emit_Deref_* checks the allocator uses.
```

### ada83.c:41022

```
// new T or new T'(value)
```

### ada83.c:41023

```
// The access type being created
```

### ada83.c:41027

```
// The NAMED access type whose collection this allocation draws from
```

### ada83.c:41028

```
// (RM 13.2): the context access type when one was propagated — node->type
```

### ada83.c:41029

```
// may be an anonymous access view that never carries the collection budget.
```

### ada83.c:41034

```
// Fat pointer decision must match Type_Needs_Fat_Pointer / Type_To_Rep.
```

### ada83.c:41035

```
// Access to unconstrained arrays always uses fat pointer, even when
```

### ada83.c:41036

```
// the access subtype adds a constraint (RM 3.10).
```

### ada83.c:41037

```
//                                                                                                
```

### ada83.c:41040

```
// The result SHAPE follows the context access type when one was propagated
```

### ada83.c:41041

```
// (assignment / object decl / return / param / qualified). An allocator's
```

### ada83.c:41042

```
// anonymous access-to-the-qualified-subtype is thin, but if it flows into a
```

### ada83.c:41043

```
// fat access context the value must be built fat (data + bounds), else the
```

### ada83.c:41044

```
// bounds are lost (RM 3.10). The allocated object's bounds still come from
```

### ada83.c:41045

```
// `designated` / `subtype_mark`.
```

### ada83.c:41050

```
// RM 11.6: when the allocated array subtype and the target access subtype
```

### ada83.c:41051

```
// both have static bounds that differ, the RM 4.8 designated-bounds check is
```

### ada83.c:41052

```
// statically known to fail. Warn (a quality compiler flags it); the runtime
```

### ada83.c:41053

```
// raise is still emitted below so the program behaves per the RM.
```

### ada83.c:41077

```
// NEW T'(X) where the initializer is itself an allocator and the designated
```

### ada83.c:41078

```
// type is an access type (T is access-to-access): X must be built at the
```

### ada83.c:41079

```
// designated access type's shape (fat for access-to-unconstrained), so
```

### ada83.c:41080

```
// propagate it as the inner allocator's context (RM 4.8 / 3.10).
```

### ada83.c:41086

```
// Access to unconstrained array with initializer
```

### ada83.c:41090

```
// Check what LLVM type the expression actually returns.                                       
```

### ada83.c:41091

```
// Constrained array aggregates return ptr, unconstrained return fat pointer.                  
```

### ada83.c:41092

```
// For qualified expressions, look at the inner expression.                                    
```

### ada83.c:41093

```
//                                                                                              
```

### ada83.c:41098

```
// Also check if the aggregate is constrained
```

### ada83.c:41101

```
// Use outer type
```

### ada83.c:41107

```
// Aggregates of constrained-but-dynamic-bounds types now produce SSA fat;
```

### ada83.c:41108

```
// treat them as fat regardless of the static `is_constrained` flag.
```

### ada83.c:41111

```
// true if len_t is in bytes, false if elements
```

### ada83.c:41113

```
// Array-bound representation of the designated type. It is a pure function
```

### ada83.c:41114

```
// of `designated`, so compute it once and reuse it for the bound literals,
```

### ada83.c:41115

```
// the fat-pointer extraction, and the length/check arithmetic below.
```

### ada83.c:41118

```
// Constrained array initializer: returns ptr, not fat pointer.
```

### ada83.c:41119

```
// Extract bounds from the type and use the ptr directly.
```

### ada83.c:41121

```
// Already a pointer to array data
```

### ada83.c:41123

```
// Get bounds from the constrained type.
```

### ada83.c:41124

```
// Bounds must be in the designated type's bt for Emit_Fat_Pointer_Dynamic.
```

### ada83.c:41125

```
// len_t stays as i64 since it's used for malloc/memcpy.
```

### ada83.c:41126

```
//
```

### ada83.c:41135

```
// Total element count is the product of every dimension's length, not
```

### ada83.c:41136

```
// just the first — a multidimensional designated must allocate them all.
```

### ada83.c:41141

```
// Null range (RM 3.6.1)
```

### ada83.c:41147

```
// Dynamic bounds - use 1-based defaults
```

### ada83.c:41157

```
// Unconstrained array or string: returns fat pointer VALUE.
```

### ada83.c:41158

```
// Fat_Ptr_As_Value handles alloca-style fat pointers transparently.
```

### ada83.c:41167

```
// Widen len to i64 for system calls (malloc/memcpy)
```

### ada83.c:41170

```
// len_t is in Integer_Arith_Rep (i32) from constrained path, already in bytes
```

### ada83.c:41174

```
// len_t is in new_bt (element count) from Emit_Fat_Pointer_Length.
```

### ada83.c:41175

```
// Multiply by element size to get byte count for malloc/memcpy.
```

### ada83.c:41186

```
// Per-dimension ACTUAL bounds of a multidimensional initializer. A fat
```

### ada83.c:41187

```
// initializer value carries every dimension's real bounds; a non-fat one
```

### ada83.c:41188

```
// takes them from its static type. The byte length, the RM 4.8(6) checks,
```

### ada83.c:41189

```
// and the result's published bounds must all use these — the static type
```

### ada83.c:41190

```
// of `T'(aggregate)` can be unconstrained, whose index bounds are the
```

### ada83.c:41191

```
// full base range (c46054a).
```

### ada83.c:41207

```
// A multidimensional designated array's byte length is the product of
```

### ada83.c:41208

```
// every dimension's length times the element size — a fat initializer
```

### ada83.c:41209

```
// value's first-dimension length alone would under-allocate the data.
```

### ada83.c:41225

```
// Allocate heap space for array data
```

### ada83.c:41229

```
// Copy data: memcpy (heap_ptr, src_data, length)
```

### ada83.c:41232

```
// RM 4.8(6): check the allocated array's bounds against the designated
```

### ada83.c:41233

```
// index subtypes and the target access subtype, for every dimension, then
```

### ada83.c:41234

```
// build the result fat pointer carrying all dimensions' bounds. Allocator
```

### ada83.c:41235

```
// results outlive the current frame, so the bounds live on the heap.
```

### ada83.c:41269

```
// Handle NEW T(bounds) without initializer - allocate unconstrained array.
```

### ada83.c:41270

```
// Supports multi-dimensional arrays (RM 4.8, 3.6.1).
```

### ada83.c:41280

```
// Generate bounds for all dimensions, compute total element count
```

### ada83.c:41287

```
// RM 4.8(6): Check bounds against index subtype
```

### ada83.c:41306

```
// RM 9.2/9.3: an allocated array whose elements contain tasks activates
```

### ada83.c:41307

```
// every one of those tasks as part of evaluating the allocator. The
```

### ada83.c:41308

```
// element count is a run-time value here, so create, activate, then wait
```

### ada83.c:41309

```
// in runtime-counted loops — all tasks of the allocation activate
```

### ada83.c:41310

```
// together before the allocator value is available.
```

### ada83.c:41330

```
// RM 4.8(6): allocated bounds are checked against the target access
```

### ada83.c:41331

```
// type's designated array bounds, in both the multi-dim and 1D paths.
```

### ada83.c:41337

```
// For multi-dim arrays, emit bounds for all dimensions into heap struct
```

### ada83.c:41339

```
// Allocate bounds: 2 * ndims entries
```

### ada83.c:41344

```
// RM 4.8(6): Check allocated bounds against target access type bounds
```

### ada83.c:41351

```
// Build fat pointer directly using multi-dim heap bounds
```

### ada83.c:41355

```
// RM 4.8(6): Check allocated bounds against target access type bounds (1D)
```

### ada83.c:41362

```
// Simple allocation (constrained types, scalar access, or no initializer).                      
```

### ada83.c:41363

```
// For composite designated types (arrays, records), allocate the designated                      
```

### ada83.c:41364

```
// type's storage and memcpy the initializer. For scalar types, use store.                      
```

### ada83.c:41365

```
//                                                                                                
```

### ada83.c:41366

```
// Default: pointer-sized
```

### ada83.c:41370

```
// Constrained array: compute actual byte size from element count
```

### ada83.c:41379

```
// A designated access value that is itself fat (access-to-unconstrained)
```

### ada83.c:41380

```
// occupies a { ptr, ptr } = 16 bytes, not the 8-byte `size` of an access.
```

### ada83.c:41386

```
// Dynamically-sized designated record: take its byte count from the rt
```

### ada83.c:41387

```
// descriptor rather than the fabricated static size; the memcpy below is
```

### ada83.c:41388

```
// gated by the same predicate for consistency.
```

### ada83.c:41399

```
// If there's an initializer, copy it into the allocated memory
```

### ada83.c:41404

```
// RM 4.8(6): NEW T'(X) bound to a constrained access subtype whose
```

### ada83.c:41405

```
// designated is itself a constrained access (e.g. AC is access A_UA(2..4),
```

### ada83.c:41406

```
// X an A_UA): the allocated access value X must satisfy that designated
```

### ada83.c:41407

```
// subtype's constraint. Check X against it.
```

### ada83.c:41414

```
// Composite type: memcpy from initializer to heap
```

### ada83.c:41417

```
// If the initializer is an SSA fat pointer (dynamic-bounds aggregate),
```

### ada83.c:41418

```
// extract the data field for the memcpy source. Only the composite path
```

### ada83.c:41419

```
// wants the bare data ptr; the scalar path below keeps the full value.
```

### ada83.c:41426

```
// RM 4.8(6): For NEW T'(X) where T is constrained array, check that
```

### ada83.c:41427

```
// X has the right number of elements before memcpy. The aggregate may
```

### ada83.c:41428

```
// have fewer or more elements than T expects.
```

### ada83.c:41442

```
// Positional element count mismatch — raise CE
```

### ada83.c:41444

```
// Cap memcpy at source aggregate size to prevent read overflow
```

### ada83.c:41448

```
// NEW T'(choice => ..) is a qualification the parser unwrapped:
```

### ada83.c:41449

```
// a sole named choice's bounds must equal T's (RM 4.3.2, 4.7).
```

### ada83.c:41463

```
// Scalar type: store value - convert to designated type if needed
```

### ada83.c:41466

```
// Prefer the value's actual generated rep over the AST prediction:
```

### ada83.c:41467

```
// an aggregate-built fat value carries LL_FAT_PTR but the AST node
```

### ada83.c:41468

```
// may say "ptr". Wrong label here emits sext/zext on a fat value.
```

### ada83.c:41476

```
// NEW T without initializer for record types (RM 4.8):                                           
```

### ada83.c:41477

```
// Initialize discriminant constraints and component defaults.                                   
```

### ada83.c:41478

```
// The designated type (or its subtype constraint) provides                                       
```

### ada83.c:41479

```
// discriminant values; component default_expr nodes provide defaults.                           
```

### ada83.c:41480

```
//                                                                                                
```

### ada83.c:41484

```
// Zero-initialize first to handle unset fields cleanly
```

### ada83.c:41489

```
// Initialize discriminant constraint values if constrained subtype
```

### ada83.c:41493

```
// RM 3.7.2 / 4.8: allocating a constrained discriminated record checks
```

### ada83.c:41494

```
// its discriminant-dependent component constraints, as at any elaboration
```

### ada83.c:41495

```
// of the subtype.
```

### ada83.c:41499

```
// Apply component defaults (RM 3.7).
```

### ada83.c:41500

```
// Track disc_agg_temp for discriminant defaults so that dependent
```

### ada83.c:41501

```
// component expressions (e.g., STRING(1..L)) can resolve disc values.
```

### ada83.c:41502

```
//
```

### ada83.c:41506

```
// disc_agg_temp cleanup is deferred until after the nested-record
```

### ada83.c:41507

```
// default init below so nested R's disc constraints (REC(L)) can resolve
```

### ada83.c:41508

```
// the parent's L through disc_agg_temp instead of emitting a bare
```

### ada83.c:41509

```
// `%l_s<id>` reference to a never-created local.
```

### ada83.c:41511

```
// RM 3.7: Initialize nested record components that have their own
```

### ada83.c:41512

```
// defaults but no default_expr on the parent component. E.g.,
```

### ada83.c:41513

```
// component C : LPRIV0 where LPRIV0 has (Q : INTEGER := 7).
```

### ada83.c:41516

```
// Already handled above
```

### ada83.c:41521

```
// Check if ct has any component defaults to apply
```

### ada83.c:41528

```
// Initialize nested record's disc constraints and component defaults.
```

### ada83.c:41534

```
// Now safe to clean up disc_agg_temp set by the default-component loop.
```

### ada83.c:41538

```
// RM 3.7.2: For unconstrained record types with disc defaults,
```

### ada83.c:41539

```
// check that default disc values are compatible with nested
```

### ada83.c:41540

```
// disc constraints and array bounds. Disc symbols resolve
```

### ada83.c:41541

```
// via disc_agg_temp pointing to the disc's memory in the record.
```

### ada83.c:41542

```
//
```

### ada83.c:41548

```
// RM 4.8(4): NEW T where T is an access type - default value is null.
```

### ada83.c:41549

```
// Also covers NEW T where T is any scalar - zero-init the allocation.
```

### ada83.c:41550

```
// For composite types without initializers (arrays, records), zero-init
```

### ada83.c:41551

```
// ensures predictable values for components without explicit defaults.
```

### ada83.c:41557

```
// Zero-init array elements first
```

### ada83.c:41561

```
// RM 3.3.1: If array element type is a record with defaults,
```

### ada83.c:41562

```
// iterate over elements and apply component defaults.
```

### ada83.c:41573

```
// Initialize element record's disc constraints and defaults.
```

### ada83.c:41581

```
// RM 9.4: a task created by an allocator depends on the master that
```

### ada83.c:41582

```
// elaborated the ACCESS TYPE definition — not the master in whose dynamic
```

### ada83.c:41583

```
// context the allocator happens to run. Resolve that master by the access
```

### ada83.c:41584

```
// type's static scope id on the running thread's master chain (under
```

### ada83.c:41585

```
// recursion the nearest elaboration instance wins). Id 0 = the access
```

### ada83.c:41586

```
// type is library-level: its master is the program (null record — the
```

### ada83.c:41587

```
// program-exit join covers it).
```

### ada83.c:41593

```
// RM 9.2: Allocating a task type via NEW activates the task immediately.
```

### ada83.c:41594

```
// The allocated memory stores the thread handle (ptr-sized).
```

### ada83.c:41596

```
// RM 3.9 / AI-00149: activating the task before its body is elaborated
```

### ada83.c:41597

```
// raises PROGRAM_ERROR (not TASKING_ERROR).
```

### ada83.c:41605

```
// Composite allocation containing task components: create, activate and
```

### ada83.c:41606

```
// wait for each embedded task as part of evaluating the allocator
```

### ada83.c:41607

```
// (RM 9.3: same immediate activation as a plain task allocator; the
```

### ada83.c:41608

```
// component tasks' bodies must already be elaborated, RM 3.9/AI-00149).
```

### ada83.c:41622

```
// ═══════════════════════════════════════════════════════════════════════
```

### ada83.c:41623

```
// RM 4.8(6): Allocator constraint checks against the target access type.
```

### ada83.c:41624

```
//
```

### ada83.c:41625

```
// When the target access type constrains its designated subtype (e.g.,
```

### ada83.c:41626

```
// TYPE A_UR IS ACCESS UR(1,9)), the allocated object must satisfy those
```

### ada83.c:41627

```
// constraints. Three cases:
```

### ada83.c:41628

```
//
```

### ada83.c:41629

```
//   (a) NEW T'(X) — scalar: check X against target's designated range
```

### ada83.c:41630

```
//   (b) NEW T'(X) — record: check X's discriminants against target's
```

### ada83.c:41631

```
//   (c) NEW T — record with defaults: check defaults against target's
```

### ada83.c:41632

```
//   (d) NEW T(bounds) or NEW T'(X) — array: check bounds against target's
```

### ada83.c:41633

```
// ═══════════════════════════════════════════════════════════════════════
```

### ada83.c:41641

```
// (a) Scalar qualified expression: NEW T'(X) where T is a scalar subtype
```

### ada83.c:41642

```
//     Check value against BOTH T's range and the designated subtype range.
```

### ada83.c:41644

```
// Load the stored value back for checking
```

### ada83.c:41650

```
// Also check the subtype mark's own range for scalar qualified exprs
```

### ada83.c:41657

```
// (b)/(c) Check each allocated discriminant against the target access type's
```

### ada83.c:41658

```
// constraint -- for a qualified expression NEW T'(X), or for NEW T whose
```

### ada83.c:41659

```
// record defaults the target may constrain differently.
```

### ada83.c:41667

```
// (d) Array bounds compatibility checks.
```

### ada83.c:41668

```
//     Check allocated array bounds against target access type's designated array bounds.
```

### ada83.c:41682

```
// (e) Access-to-constrained: NEW T'(X) where T is an access type whose
```

### ada83.c:41683

```
//     designated type is constrained. Dereference stored pointer and check
```

### ada83.c:41684

```
//     discriminants/bounds against T's designated subtype constraints.
```

### ada83.c:41685

```
//     E.g. NEW A_CR'(NEW UR(3)) where A_CR = ACCESS UR(2) — disc A must be 2.
```

### ada83.c:41689

```
// Check discriminant constraints on access-to-constrained-record.
```

### ada83.c:41690

```
// RM 4.8(4): NULL satisfies any access constraint — skip check if null.
```

### ada83.c:41697

```
// Check array bounds on access-to-constrained-array. Only when the
```

### ada83.c:41698

```
// inner value is a fat pointer (unconstrained base with constraints).
```

### ada83.c:41699

```
// Constrained array subtypes w/o fat pointer have matching bounds.
```

### ada83.c:41700

```
// RM 4.8(4): NULL satisfies any constraint — skip if null.
```

### ada83.c:41706

```
// (f) NEW T(constraints) where T is an access type — check disc/index
```

### ada83.c:41707

```
//     constraint values against subtype ranges. RM 4.8(6).
```

### ada83.c:41708

```
//     E.g. NEW A_UR1(6) where A_UR1 is ACCESS UR1(A : INT range 1..5).
```

### ada83.c:41709

```
//     E.g. NEW A_UA1(1..6) where A_UA1 is ACCESS UA1(INT RANGE <>) with INT 1..5.
```

### ada83.c:41713

```
// (f.1) Record disc constraint values vs disc subtypes
```

### ada83.c:41730

```
// (f.2) Array index bounds vs index subtypes
```

### ada83.c:41743

```
// (g) Target-constrained access: the target access type's designated type
```

### ada83.c:41744

```
//     is itself an access type with constrained designated. Check the stored
```

### ada83.c:41745

```
//     access value's pointed-to object against the constraint.
```

### ada83.c:41746

```
//     E.g. AC_A_UR : ACCESS A_UR(2); NEW A_UR'(NEW UR(3)) → disc 3 vs 2.
```

### ada83.c:41750

```
// Record disc check
```

### ada83.c:41757

```
// Array bounds check
```

### ada83.c:41772

```
// Character literal - extract char from text "'X'"
```

### ada83.c:41776

```
// Check if resolved as enumeration literal
```

### ada83.c:41782

```
// If the node's type is a user-defined enumeration containing
```

### ada83.c:41783

```
// character literals, use the position within the enumeration
```

### ada83.c:41784

```
// rather than the ASCII code (RM 3.5.1).
```

### ada83.c:41785

```
//
```

### ada83.c:41797

```
// character literals use native type width (i8),
```

### ada83.c:41798

```
// not Integer_Arith_Rep. Widening at use sites.
```

### ada83.c:41812

```
// Array aggregates of unconstrained or dynamic-bounds types now return
```

### ada83.c:41813

```
// SSA { ptr, ptr } from Generate_Aggregate; everything else (records,
```

### ada83.c:41814

```
// static-bounds arrays) is a plain ptr to flat storage.
```

### ada83.c:41827

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:41828

```
// §13.7 Statement Code Generation                                                                  
```

### ada83.c:41829

```
//                                                                                                  
```

### ada83.c:41830

```
// Statements modify state while expressions compute values, a distinction Ada enforces.           
```

### ada83.c:41831

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:41838

```
// After a terminator (ret/br), we need a new basic block.                                     
```

### ada83.c:41839

```
// Labeled statements (NK_LABEL, NK_BLOCK with label, NK_LOOP with label)                       
```

### ada83.c:41840

```
// emit their own labels. For unlabeled statements, emit a fresh label.                        
```

### ada83.c:41841

```
//                                                                                              
```

### ada83.c:41847

```
// Emit a fresh basic block for unreachable code. The subsequent
```

### ada83.c:41848

```
// statement will generate instructions that fill this block.
```

### ada83.c:41853

```
// The next statement emits its own label - reset block_terminated
```

### ada83.c:41854

```
// so it adds an instruction to its label block properly
```

### ada83.c:41862

```
// Materialize the address a symbol-targeted assignment stores through:
```

### ada83.c:41863

```
// ordinarily the symbol's own storage, but for an RM 8.5 object rename or an
```

### ada83.c:41864

```
// RM 12.1.1 instance IN OUT binding (both carrying rename_address_slot)
```

### ada83.c:41865

```
// Emit_Symbol_Addr loads the captured target address, so the write reaches
```

### ada83.c:41866

```
// the renamed object / actual variable rather than the indirection cell.
```

### ada83.c:41873

```
// Only a SYNTACTIC literal (integer or character) is trusted as a static index
```

### ada83.c:41874

```
// value. A bare identifier may carry an alternate enumeration-literal
```

### ada83.c:41875

```
// interpretation (an overloaded name that is also an enum literal, c87b31a's
```

### ada83.c:41876

```
// G/H), whose position is unrelated to the choice's resolved value — reading it
```

### ada83.c:41877

```
// would spuriously reject a valid aggregate.
```

### ada83.c:41883

```
// One dimension of the recursive named-choice check: test this level's choices
```

### ada83.c:41884

```
// against dimension `dim`'s index subtype, then recurse into each nested
```

### ada83.c:41885

```
// aggregate for the next dimension (a multi-dimensional aggregate nests one
```

### ada83.c:41886

```
// aggregate per dimension, RM 4.3.2).
```

### ada83.c:41919

```
// RM 4.3.2: a named choice of an aggregate for a CONSTRAINED array subtype
```

### ada83.c:41920

```
// denotes an index that must belong to the INDEX SUBTYPE — the choices are
```

### ada83.c:41921

```
// index values, not slidable bounds — so a static choice outside the index
```

### ada83.c:41922

```
// subtype makes the enclosing construct raise CONSTRAINT_ERROR (`A1 (1..3) :=
```

### ada83.c:41923

```
// (2..4 => 0)` where A1's index subtype is 1..3). The subtype is the object's,
```

### ada83.c:41924

```
// not its constraint: an assignment slides, so `A : BASE(5..7) := (6..8 => ...)`
```

### ada83.c:41925

```
// is legal when BASE's index subtype (ST, 4..8) contains 6..8. Type_Base strips
```

### ada83.c:41926

```
// the object constraint (BASE(5..7) -> ST) while keeping an anonymous
```

### ada83.c:41927

```
// constrained array's own range as its subtype. Every dimension is checked. The
```

### ada83.c:41928

```
// check is done at the statement level rather than inside Generate_Aggregate,
```

### ada83.c:41929

```
// whose element/loop IR would entangle with a raise injected mid-build.
```

### ada83.c:41943

```
// The type whose RANGE bounds the assignment's RHS VALUE, for range-check
```

### ada83.c:41944

```
// elision. A name (variable, component, call) yields a value of its subtype,
```

### ada83.c:41945

```
// so the subtype's bounds hold; an operator node's result ranges over the
```

### ada83.c:41946

```
// BASE type (RM 4.5) even though it is typed by the operand subtype — using
```

### ada83.c:41947

```
// the subtype's bounds there would elide the very check that catches
```

### ada83.c:41948

```
// I + 1 overflowing I's own subtype (cb2005a).
```

### ada83.c:41960

```
// Raise cleanly at the statement level before any sliding/aggregate IR.
```

### ada83.c:41968

```
// RM 5.2.1: an array assignment slides — a named-aggregate RHS without OTHERS
```

### ada83.c:41969

```
// need only match the target's LENGTH, not its bounds. Mark the context so the
```

### ada83.c:41970

```
// aggregate machinery picks the length check over bounds-equality; every other
```

### ada83.c:41971

```
// context (parameter/default, qualified expression, aggregate component) keeps
```

### ada83.c:41972

```
// the belong (bounds-equality) rule. Restored on every return path below.
```

### ada83.c:41979

```
// Emit source location and assignment target info
```

### ada83.c:41991

```
// For RENAMES: redirect to the renamed object. A USE-visible alias
```

### ada83.c:41992

```
// denotes the original entity — follow it so a rename (including an
```

### ada83.c:41993

```
// RM 13.5 overlay) attached to the original fires.
```

### ada83.c:42001

```
// RM 4.8 / RM 13.2: an allocator assigned to an access-typed target draws
```

### ada83.c:42002

```
// from the TARGET's named access type — its designated-subtype checks and
```

### ada83.c:42003

```
// its bounded collection both key off that type, and the allocator node's
```

### ada83.c:42004

```
// own type is an anonymous access view that carries neither. Propagate
```

### ada83.c:42005

```
// before ANY target-shape branch evaluates the value, so an array-element
```

### ada83.c:42006

```
// or record-component target (cd2b11a's ACC_ARRAY (I) := NEW ...) is
```

### ada83.c:42007

```
// covered the same as a plain variable.
```

### ada83.c:42008

```
//
```

### ada83.c:42009

```
// A component's access subtype may carry PER-OBJECT constraints
```

### ada83.c:42010

```
// (discriminant-dependent bounds, RM 3.7.1) whose values live in the
```

### ada83.c:42011

```
// enclosing object, not the type — re-evaluating their bound expressions
```

### ada83.c:42012

```
// here is wrong (c37010a re-runs a side-effectful function, c37010b names
```

### ada83.c:42013

```
// a discriminant with no storage in scope). Until per-object constraints
```

### ada83.c:42014

```
// are materialized, a non-identifier target contributes only its BASE
```

### ada83.c:42015

```
// type: the collection identity survives, the unevaluable subtype check
```

### ada83.c:42016

```
// does not.
```

### ada83.c:42024

```
// Handle indexed component target (array element or slice assignment)
```

### ada83.c:42027

```
// A slice of a slice (RM 4.1.2): X(a..b)(c..d) denotes X(c..d). A slice
```

### ada83.c:42028

```
// keeps the original index values, so the outer range is already absolute;
```

### ada83.c:42029

```
// collapse to a single slice on the base array so the slice-assignment path
```

### ada83.c:42030

```
// sees an ordinary array prefix.
```

### ada83.c:42044

```
// RM 4.1(3): X(L..H) where X is access-to-array dereferences X implicitly.
```

### ada83.c:42045

```
// Only a slice needs this array-target path; element assignment to an
```

### ada83.c:42046

```
// access-to-array is handled by the general indexed-lvalue path below. The
```

### ada83.c:42047

```
// dereferenced array's value is the access value itself (a fat pointer when
```

### ada83.c:42048

```
// the designated array is unconstrained or dynamically bounded).
```

### ada83.c:42058

```
// For unconstrained (STRING / unconstrained array) the variable                              
```

### ada83.c:42059

```
// holds a fat pointer - we must load it and extract the data ptr.                           
```

### ada83.c:42060

```
// For constrained arrays the variable IS the data pointer.                                  
```

### ada83.c:42061

```
//                                                                                            
```

### ada83.c:42066

```
// A *constrained* array whose bounds are dynamic (e.g. a derived subtype
```

### ada83.c:42067

```
// `new P(IDENT(5)..IDENT(7))`) is stored as a fat pointer just like an
```

### ada83.c:42068

```
// unconstrained one, so its data pointer and low bound must be loaded at
```

### ada83.c:42069

```
// runtime. An access-to-array prefix is likewise a fat value when its
```

### ada83.c:42070

```
// designated array is unconstrained or dynamically bounded. The flat-data
```

### ada83.c:42071

```
// path below only applies to static storage.
```

### ada83.c:42076

```
// ARR (A'RANGE) := source is a slice (RM 4.1.2), not an element. Normalise
```

### ada83.c:42077

```
// the 'RANGE attribute into a synthetic low..high range (A'FIRST .. A'LAST)
```

### ada83.c:42078

```
// so the slice path below handles it; otherwise it falls through to the
```

### ada83.c:42079

```
// element path and stores an aggregate RHS as a scalar (a NO_VALUE reg).
```

### ada83.c:42096

```
// A positional aggregate assigned to a slice slides to the slice (RM
```

### ada83.c:42097

```
// 5.2.1); its applicable bounds are its own element count, not the whole
```

### ada83.c:42098

```
// array type's. Without this, the aggregate carries the slice target's
```

### ada83.c:42099

```
// full-array type and the positional count-vs-constraint check (e.g. 5
```

### ada83.c:42100

```
// elements vs ARR(1..10)) raises CONSTRAINT_ERROR spuriously.
```

### ada83.c:42118

```
// An `others`-driven aggregate assigned to a slice has no choices to fix
```

### ada83.c:42119

```
// its own bounds; per RM 4.3.2 it takes them from the slice's index
```

### ada83.c:42120

```
// range. Bind its subtype to the slice bounds (kept as expressions so a
```

### ada83.c:42121

```
// dynamic slice works) so it sizes to the slice — in particular a null
```

### ada83.c:42122

```
// slice yields a null aggregate, not a single defaulted element.
```

### ada83.c:42138

```
// Check for slice assignment: ARR (low .. high) := source
```

### ada83.c:42139

```
// Array slice assignment using memcpy
```

### ada83.c:42145

```
// RM 3.5: a destination slice written `A (S RANGE L..H)` requires the
```

### ada83.c:42146

```
// range compatible with S; a source slice does the same at its own site.
```

### ada83.c:42149

```
// Get destination base address
```

### ada83.c:42153

```
// Load fat pointer, extract data ptr and low bound
```

### ada83.c:42156

```
// Obtain the target array as a fat-pointer value. Generate_Expression
```

### ada83.c:42157

```
// yields the right value for every fat form: a stored fat-pointer
```

### ada83.c:42158

```
// variable loads its slot, an implicit access dereference yields the
```

### ada83.c:42159

```
// access value, and a discriminant-dependent array COMPONENT (stored
```

### ada83.c:42160

```
// inline in its record, not as a fat pointer) has its fat pointer
```

### ada83.c:42161

```
// constructed from the field address plus the discriminant bounds —
```

### ada83.c:42162

```
// mirroring the slice-read path. Loading a fat pointer straight from
```

### ada83.c:42163

```
// the component's lvalue would instead read the inline array bytes as
```

### ada83.c:42164

```
// {data, bounds} pointers and crash.
```

### ada83.c:42168

```
// Low bound comes from the fat pointer at runtime
```

### ada83.c:42169

```
// We'll use dynamic low below
```

### ada83.c:42174

```
// Calculate destination start offset from slice low bound. The bound
```

### ada83.c:42175

```
// expressions carry the index subtype's own representation (e.g. i8 for
```

### ada83.c:42176

```
// a small range); widen them to the index arithmetic type before mixing
```

### ada83.c:42177

```
// them with the (already-widened) fat-pointer low bound.
```

### ada83.c:42181

```
// Destination start = dest_base + (slice_low - array_low) elements.
```

### ada83.c:42182

```
// The GEP is byte-addressed, so the element offset must be scaled by
```

### ada83.c:42183

```
// the element size — otherwise a slice that does not start at the
```

### ada83.c:42184

```
// array's low bound writes element_size-1 bytes short of its target
```

### ada83.c:42185

```
// (harmless only for one-byte elements, which is why it long escaped
```

### ada83.c:42186

```
// notice).
```

### ada83.c:42195

```
// Generate source and copy
```

### ada83.c:42199

```
// RM 4.1.2: a null slice (high < low) has length 0, not a negative
```

### ada83.c:42200

```
// count. Clamp so the length check matches a null source and the
```

### ada83.c:42201

```
// memcpy copies nothing.
```

### ada83.c:42207

```
// Get source data
```

### ada83.c:42215

```
// RM 5.2.1: the source length must equal the slice length.
```

### ada83.c:42235

```
// Constrained array slice assignment (original code)
```

### ada83.c:42239

```
// Get destination base address. For an implicit access dereference the
```

### ada83.c:42240

```
// access value is the data pointer; otherwise take the prefix lvalue.
```

### ada83.c:42244

```
// Evaluate slice bounds and compute copy length from original range.
```

### ada83.c:42245

```
// Length must use raw bounds (not index-adjusted) per Ada RM 5.2.1. The
```

### ada83.c:42246

```
// bound expressions carry the index subtype's representation (e.g. i8 for
```

### ada83.c:42247

```
// a small range); widen to the index arithmetic type before use.
```

### ada83.c:42253

```
// RM 4.1.2: non-null slice bounds must be in array's range.
```

### ada83.c:42265

```
// Destination pointer: adjust low bound to array-relative index, then
```

### ada83.c:42266

```
// scale to bytes (the GEP is byte-addressed, so an element index must
```

### ada83.c:42267

```
// be multiplied by the element size - otherwise a non-zero slice start
```

### ada83.c:42268

```
// on a wider-than-byte element lands at the wrong offset).
```

### ada83.c:42280

```
// Ada RM 5.2.1: sliding semantics - source slides to destination.
```

### ada83.c:42286

```
// Determine source data pointer
```

### ada83.c:42291

```
// Source is a slice: SRC_ARR (lo..hi)
```

### ada83.c:42295

```
// RM 3.5: a source slice written `A (S RANGE L..H)` checks its range
```

### ada83.c:42296

```
// against S here (this path handles the source slice directly rather
```

### ada83.c:42297

```
// than through Generate_Apply's slice emitter).
```

### ada83.c:42322

```
// RM 5.2.1: the source slice and the target slice must have the
```

### ada83.c:42323

```
// same length (the value slides, but the lengths must match).
```

### ada83.c:42332

```
// RM 4.1.2: source slice bounds must be in src array's range.
```

### ada83.c:42351

```
// Scale the element index to bytes for the byte-addressed GEP.
```

### ada83.c:42360

```
// Fallback: evaluate as general expression
```

### ada83.c:42367

```
// General source: identifier, string literal, function call, etc.                       
```

### ada83.c:42368

```
// Ada RM 5.2.1: source slides to destination index range.                               
```

### ada83.c:42369

```
// We evaluate the source, extract its data pointer, and memcpy.                         
```

### ada83.c:42370

```
//                                                                                        
```

### ada83.c:42371

```
// The VALUE's own rep is ground truth — a concatenation always
```

### ada83.c:42372

```
// yields a fat pointer even when the context type is a
```

### ada83.c:42373

```
// constrained array (whose predicted rep is a thin pointer).
```

### ada83.c:42381

```
// Whole constrained array: get address of storage
```

### ada83.c:42386

```
// Array aggregate / array-returning call already yields a pointer to
```

### ada83.c:42387

```
// the element storage; use it directly as the copy source. (Spilling
```

### ada83.c:42388

```
// it would memcpy the pointer bits, not the array data.)
```

### ada83.c:42392

```
// Scalar value fallback - spill to alloca and copy from it.
```

### ada83.c:42397

```
// RM 5.2.1: the whole-array or string source must have the same
```

### ada83.c:42398

```
// length as the target slice. A fat source carries its length; a
```

### ada83.c:42399

```
// constrained-array source derives it from its bounds.
```

### ada83.c:42418

```
// Array element assignment: DATA (I) := value                                                
```

### ada83.c:42419

```
// Generate_Lvalue handles both unconstrained (fat pointer) and                               
```

### ada83.c:42420

```
// constrained arrays - computes element address in one call.                                
```

### ada83.c:42421

```
//                                                                                            
```

### ada83.c:42444

```
// Access-to-array implicit dereference + indexing: ACC_ARR (I) := val                          
```

### ada83.c:42445

```
// where ACC_ARR is an access type whose designated type is an array.                          
```

### ada83.c:42446

```
// Generate_Lvalue already handles this case properly.                                         
```

### ada83.c:42447

```
//                                                                                              
```

### ada83.c:42460

```
// Handle .ALL dereference assignment (NK_UNARY_OP with TK_ALL)
```

### ada83.c:42467

```
// Generate_Lvalue loads the pointer value (the storage address)
```

### ada83.c:42470

```
// Composite types: copy contents via memcpy (RM 5.2)
```

### ada83.c:42475

```
// Dynamic-bounds aggregate sources arrive as SSA fat - extract data.
```

### ada83.c:42478

```
// RM 5.2: assigning through an access to a CONSTRAINED subtype
```

### ada83.c:42479

```
// (ACCESS REC1 (6)), the source's discriminants must equal the
```

### ada83.c:42480

```
// designated subtype's constraint — BUT only when the designated type
```

### ada83.c:42481

```
// is genuinely immutable. A type with default discriminants designates
```

### ada83.c:42482

```
// a mutable object through an access (its discriminants may change), so
```

### ada83.c:42483

```
// it is exempt.
```

### ada83.c:42487

```
// RM 4.8 / 5.2: an object created by an allocator is always constrained —
```

### ada83.c:42488

```
// by an explicit discriminant constraint (NEW REC'(5,0) / NEW REC(5)) or,
```

### ada83.c:42489

```
// failing that, by the type's default discriminant values (NEW REC). An
```

### ada83.c:42490

```
// access value in Ada 83 can only designate such an allocated object, so a
```

### ada83.c:42491

```
// .ALL target's discriminants are never mutable, even for a type with
```

### ada83.c:42492

```
// default discriminants. A complete record assignment through .ALL cannot
```

### ada83.c:42493

```
// change them: compare the source's discriminants against the target's
```

### ada83.c:42494

```
// CURRENT (runtime) values and raise CONSTRAINT_ERROR on any mismatch,
```

### ada83.c:42495

```
// leaving the target unaltered (the check precedes the copy).
```

### ada83.c:42511

```
// An unconstrained or dynamically bounded designated array has no static
```

### ada83.c:42512

```
// byte size; derive it from the access value's fat-pointer bounds (the
```

### ada83.c:42513

```
// product of every dimension's length times the element size).
```

### ada83.c:42527

```
// Scalar / access types: store value directly
```

### ada83.c:42537

```
// Handle selected component target (record field assignment or .ALL)                             
```

### ada83.c:42538

```
// Generate_Lvalue computes the storage address - we just determine                               
```

### ada83.c:42539

```
// the store type and let Generate_Lvalue handle address computation.                            
```

### ada83.c:42540

```
//                                                                                                
```

### ada83.c:42545

```
// Determine the target type being assigned to
```

### ada83.c:42550

```
// .ALL dereference: target is designated type
```

### ada83.c:42553

```
// Record field: find component type
```

### ada83.c:42566

```
// An expanded name (BLOCK.V / PACKAGE.V, where the prefix is a scope label
```

### ada83.c:42567

```
// rather than a record object) is not a record-field access: the selector
```

### ada83.c:42568

```
// denotes the object itself, so the component search above finds nothing.
```

### ada83.c:42569

```
// Fall back to the LHS's own resolved type so a composite object is copied
```

### ada83.c:42570

```
// as a whole, not scalar-stored as a pointer.
```

### ada83.c:42573

```
// Generate_Lvalue handles .ALL dereference, implicit dereference,
```

### ada83.c:42574

```
// and direct field offset - all address computation is unified.
```

### ada83.c:42579

```
// Composite types: copy contents via memcpy (RM 5.2). A dynamically-sized
```

### ada83.c:42580

```
// record takes its byte count from the type's runtime descriptor.
```

### ada83.c:42596

```
// Discriminant-dependent array stored inline (size=0 at compile time).                        
```

### ada83.c:42597

```
// RHS is a fat pointer {ptr,ptr} - extract data ptr and copy bytes                             
```

### ada83.c:42598

```
// using the bounds from the fat pointer. (RM 5.2, 3.7.1)                                       
```

### ada83.c:42599

```
//                                                                                              
```

### ada83.c:42605

```
// Extract data pointer and bounds from fat pointer
```

### ada83.c:42610

```
// Get length from bounds
```

### ada83.c:42630

```
// Clamp to 0 for null arrays
```

### ada83.c:42637

```
// Value is already a pointer to data - use memcpy with a safe size
```

### ada83.c:42646

```
// Scalar / access types: store value directly. If component-type lookup
```

### ada83.c:42647

```
// didn't yield a type (e.g. record component not found by name, or
```

### ada83.c:42648

```
// prefix->type was unset), recover from the target's resolved type
```

### ada83.c:42649

```
// before falling back to the RHS type.
```

### ada83.c:42659

```
// Simple variable target
```

### ada83.c:42668

```
// Fall back to expression types if the symbol has no type set
```

### ada83.c:42669

```
// (some symbol-creation paths skip the type field).
```

### ada83.c:42673

```
// Handle record assignment (use memcpy) (RM 5.2, 3.7.2)
```

### ada83.c:42677

```
// A fat-valued source (e.g. dynamic-bounds aggregate) needs its data ptr
```

### ada83.c:42678

```
// extracted before memcpy — passing the SSA fat directly emits an invalid
```

### ada83.c:42679

```
// `memcpy ... ptr %fatreg`.
```

### ada83.c:42685

```
// The target's storage address — for a generic IN OUT formal object this
```

### ada83.c:42686

```
// dereferences the binding's pointer cell so the write reaches the actual
```

### ada83.c:42687

```
// (RM 12.1.1), not the cell itself.
```

### ada83.c:42690

```
// RM 3.7.4: an IN OUT generic formal object bound to a constrained actual
```

### ada83.c:42691

```
// may not have its discriminants changed. The actual is known statically at
```

### ada83.c:42692

```
// instantiation, so the constraint is a compile-time property of the binding.
```

### ada83.c:42707

```
// A dynamically-sized record is NOT copied early here: the discriminant
```

### ada83.c:42708

```
// match check below (RM 5.2) must run first — skipping it would let a
```

### ada83.c:42709

```
// whole-record assignment silently change a constrained record's
```

### ada83.c:42710

```
// discriminants. Its byte count is chosen at the memcpy below.
```

### ada83.c:42713

```
// For constrained discriminated records, verify source discriminants match
```

### ada83.c:42714

```
// target constraints before assignment (Constraint_Error if mismatch).
```

### ada83.c:42715

```
// Mutable records (all_defaults, no constraint) allow discriminant change.
```

### ada83.c:42716

```
// Records without default discriminants are permanently constrained by their
```

### ada83.c:42717

```
// initial discriminants (RM 3.7.1(7)) — the check applies even when the
```

### ada83.c:42718

```
// formal/local's type mark is unconstrained (c37206a: OUT/IN OUT of REC).
```

### ada83.c:42719

```
// Load each discriminant from source and compare with target
```

### ada83.c:42720

```
//
```

### ada83.c:42728

```
// Load source discriminant
```

### ada83.c:42734

```
// Load target discriminant
```

### ada83.c:42739

```
// Compare and raise Constraint_Error on mismatch
```

### ada83.c:42744

```
// RM 3.7.4: a whole-record assignment through an OUT/IN OUT formal of an
```

### ada83.c:42745

```
// unconstrained mutable type may change the discriminants only when the
```

### ada83.c:42746

```
// ACTUAL is unconstrained. The formal's constrained state is the hidden
```

### ada83.c:42747

```
// runtime flag; when it is set, a discriminant change raises
```

### ada83.c:42748

```
// CONSTRAINT_ERROR. The "ok" condition is (flag = 0) OR (discriminants
```

### ada83.c:42749

```
// unchanged), so a constrained actual with a differing discriminant traps.
```

### ada83.c:42773

```
// Copy record data (whole record for mutable, or non-discriminant part for
```

### ada83.c:42774

```
// constrained). An unconstrained view of a discriminated type whose trailing
```

### ada83.c:42775

```
// component is discriminant-sized has no static size — the discriminants,
```

### ada83.c:42776

```
// just verified equal on both sides, give the instance's byte count.
```

### ada83.c:42780

```
// rt descriptor size for other shapes
```

### ada83.c:42790

```
// Handle constrained array assignment (use memcpy, not store)                                    
```

### ada83.c:42791

```
// Check if source is unconstrained (fat pointer) or constrained (ptr).                          
```

### ada83.c:42792

```
// Fat pointer sources: STRING type, unconstrained arrays, string literals,                       
```

### ada83.c:42793

```
// concatenation results, and slice expressions.                                                 
```

### ada83.c:42794

```
//                                                                                                
```

### ada83.c:42800

```
// Constrained arrays with dynamic bounds are stored as fat pointers.                          
```

### ada83.c:42801

```
// Aggregates produce fat pointer allocas but Expression_Produces_Fat_Pointer                   
```

### ada83.c:42802

```
// returns false for NK_AGGREGATE. Detect this case.                                          
```

### ada83.c:42803

```
//                                                                                              
```

### ada83.c:42811

```
// Aggregate returns SSA fat pointer { ptr, ptr }; store to target.
```

### ada83.c:42817

```
// Source is unconstrained/string - extract data pointer from fat pointer
```

### ada83.c:42820

```
// Length check: verify source length matches constrained target length.
```

### ada83.c:42821

```
// When target has dynamic bounds (BOUND_EXPR), Type_Bound_Value returns
```

### ada83.c:42822

```
// 0 → dst_length=1 (wrong). Load target's fat ptr at runtime and use
```

### ada83.c:42823

```
// its length instead. Also memcpy into target's .data ptr (not over
```

### ada83.c:42824

```
// the fat ptr struct itself).
```

### ada83.c:42833

```
// Load target fat ptr, extract length
```

### ada83.c:42852

```
// RM 4.6: array assignment matches components, not bounds. Two null
```

### ada83.c:42853

```
// arrays (zero total components) match whatever their per-dimension
```

### ada83.c:42854

```
// bounds, so suppress the dimension-length check when both sides are
```

### ada83.c:42855

```
// null; a genuine length mismatch between non-null arrays still raises.
```

### ada83.c:42866

```
// Memcpy source data into target's data ptr (preserve fat struct). The
```

### ada83.c:42867

```
// byte count is the product of every dimension's length times the
```

### ada83.c:42868

```
// component size; using dimension 0 alone would copy only the first row
```

### ada83.c:42869

```
// of a multidimensional array.
```

### ada83.c:42879

```
// Target is fat pointer but source isn't aggregate/fat - store as fat ptr
```

### ada83.c:42886

```
// Source is constrained - memcpy directly
```

### ada83.c:42888

```
// RM 5.2.1: array assignment requires equal lengths in each dimension;
```

### ada83.c:42889

```
// the bounds may differ (the value slides). Compare the source and
```

### ada83.c:42890

```
// target lengths per dimension and raise CONSTRAINT_ERROR on a mismatch.
```

### ada83.c:42891

```
// Static bounds fold to a constant comparison; dynamic bounds check at
```

### ada83.c:42892

```
// run time.
```

### ada83.c:42917

```
// Handle unconstrained array/STRING variable assignment.                                        
```

### ada83.c:42918

```
// These variables store a fat pointer { ptr, { bound, bound } }.                                
```

### ada83.c:42919

```
// IMPORTANT: In Ada, unconstrained objects have fixed constraints                                
```

### ada83.c:42920

```
// after elaboration. Assignment copies data INTO the existing                                   
```

### ada83.c:42921

```
// data storage - it does NOT replace the fat pointer.                                           
```

### ada83.c:42922

```
// (Replacing the fat pointer would orphan the data alloca and                                    
```

### ada83.c:42923

```
// create dangling pointers to source storage.)                                                   
```

### ada83.c:42924

```
//                                                                                                
```

### ada83.c:42925

```
// Algorithm:                                                                                     
```

### ada83.c:42926

```
//   1. Load existing fat pointer to get data pointer and length                                  
```

### ada83.c:42927

```
//   2. Generate source expression                                                                
```

### ada83.c:42928

```
//   3. Extract source data pointer (from fat ptr or constrained ptr)                             
```

### ada83.c:42929

```
//   4. memcpy source data to existing data storage                                               
```

### ada83.c:42930

```
//                                                                                                
```

### ada83.c:42937

```
// Load existing fat pointer from the target variable
```

### ada83.c:42942

```
// Compute total flat element count: product of all dimension lengths.                         
```

### ada83.c:42943

```
// For 1D arrays this is just (high - low + 1). For multidim arrays                            
```

### ada83.c:42944

```
// (e.g. 3D: dim1_count * dim2_count * dim3_count) we must multiply                             
```

### ada83.c:42945

```
// all dimension lengths to get the correct byte size.                                         
```

### ada83.c:42946

```
//                                                                                              
```

### ada83.c:42961

```
// Convert element count to byte count. For STRING/CHARACTER                                   
```

### ada83.c:42962

```
// arrays the element size is 1, so this is a no-op. For arrays                                
```

### ada83.c:42963

```
// of larger types (INTEGER, records, etc.) we must scale.                                     
```

### ada83.c:42964

```
//                                                                                              
```

### ada83.c:42976

```
// RM 4.3.2: the target is the applicable index constraint for an OTHERS
```

### ada83.c:42977

```
// aggregate RHS. Its bounds are only known at run time (this is an
```

### ada83.c:42978

```
// unconstrained array — a fat pointer), so publish them for the aggregate
```

### ada83.c:42979

```
// about to be generated; it adopts them for its first dimension instead of
```

### ada83.c:42980

```
// the index subtype's full range (which would overflow aggregate sizing).
```

### ada83.c:42988

```
// Generate source and copy data to existing storage
```

### ada83.c:42990

```
// consumed by the aggregate, or moot
```

### ada83.c:42992

```
// Aggregates with unconstrained type return a fat pointer ALLOCA
```

### ada83.c:42993

```
// (ptr to { ptr, ptr }), not a loaded value. Promote to fat.
```

### ada83.c:43001

```
// Source is fat pointer - extract data pointer, check length, copy.                           
```

### ada83.c:43002

```
// For multidim arrays, only check dim-1 length here; the aggregate's                           
```

### ada83.c:43003

```
// own constraint checking handles inner dimension consistency.                                
```

### ada83.c:43004

```
//                                                                                              
```

### ada83.c:43008

```
// RM 4.5.2: only NONNULL arrays must have matching per-dimension lengths;
```

### ada83.c:43009

```
// two null arrays (zero total components) match whatever their bounds. So
```

### ada83.c:43010

```
// suppress the check when both sides are null — e.g. assigning a 3x0
```

### ada83.c:43011

```
// aggregate to a 0x3 target (c95086e (E)). A genuine mismatch between
```

### ada83.c:43012

```
// non-null arrays still raises.
```

### ada83.c:43027

```
// Source is constrained (ptr) - memcpy directly
```

### ada83.c:43041

```
// Determine source type from the value expression.
```

### ada83.c:43042

```
// Resolve through generic actuals so that a formal TYPE_PRIVATE is
```

### ada83.c:43043

```
// treated as its actual representation (e.g., FLOAT > double).
```

### ada83.c:43044

```
//
```

### ada83.c:43049

```
// Convert between float and integer if needed                                                    
```

### ada83.c:43050

```
// Float to integer: use fptosi. For fixed-point targets, divide by                               
```

### ada83.c:43051

```
// SMALL first to get the scaled integer representation (RM 4.5.5)                                
```

### ada83.c:43052

```
//                                                                                                
```

### ada83.c:43058

```
// Ada RM 4.6: round to nearest before truncation
```

### ada83.c:43063

```
// Integer to float: use sitofp - use native src/dst types
```

### ada83.c:43070

```
// Float to float: may need conversion if sizes differ.
```

### ada83.c:43071

```
// Determine actual source float type from the expression's type info.
```

### ada83.c:43073

```
// Use the produced value's rep, not the AST prediction: the resolver may
```

### ada83.c:43074

```
// have narrowed the RHS literal's type to the target (single) while the
```

### ada83.c:43075

```
// value was still emitted as double, which would skip the needed fptrunc.
```

### ada83.c:43079

```
// Integer/boolean to target type: use actual expression type.                                   
```

### ada83.c:43080

```
// constraint check BEFORE conversion, so the check                                               
```

### ada83.c:43081

```
// sees the value at its actual type (not yet converted).                                        
```

### ada83.c:43082

```
//                                                                                                
```

### ada83.c:43084

```
// `value` is unmodified on this integer/boolean path, so use its REAL
```

### ada83.c:43085

```
// emitted rep rather than the Ada-type prediction (which can disagree -
```

### ada83.c:43086

```
// e.g. a narrow enum/CHARACTER value the prediction calls i32).
```

### ada83.c:43095

```
// Float scalar constraint check - done after float conversion since
```

### ada83.c:43096

```
// Emit_Constraint_Check's float path handles its own type conversion.
```

### ada83.c:43097

```
// Pass actual_val_type so the check knows the current value width
```

### ada83.c:43098

```
// (value may have been fptrunc'd/fpext'd above).
```

### ada83.c:43099

```
//
```

### ada83.c:43104

```
// RM 3.10 / 5.2: assigning to a variable of a constrained access subtype
```

### ada83.c:43105

```
// (e.g. S2 IS ARR_NAME(3..6) or ACCESS REC(5)), a non-null value must
```

### ada83.c:43106

```
// designate an object whose bounds/discriminants equal the subtype's
```

### ada83.c:43107

```
// constraint, else CONSTRAINT_ERROR. Null is always assignable. The check
```

### ada83.c:43108

```
// reads the designated object's actual bounds from the value's fat pointer,
```

### ada83.c:43109

```
// so use the produced value (pre-conversion) which still carries them.
```

### ada83.c:43136

```
// ELSIF parts: each is an NK_IF node with condition + then_stmts
```

### ada83.c:43165

```
// Emit LLVM label for Ada label (enables GOTO targeting this loop)
```

### ada83.c:43176

```
// loop label
```

### ada83.c:43177

```
// New block started
```

### ada83.c:43189

```
// Store exit label on loop symbol for named EXIT statements
```

### ada83.c:43195

```
// Condition check for WHILE loops (FOR loops dispatched to Generate_For_Loop)
```

### ada83.c:43221

```
// RM 5.8/9.5: a return statement directly within an accept body does not
```

### ada83.c:43222

```
// return from the task body — it completes the ACCEPT (writes OUT/IN OUT
```

### ada83.c:43223

```
// parameters back, wakes the caller, pops the accept's handler) and continues
```

### ada83.c:43224

```
// the task body after the accept. Without this a return skipped the copy-back
```

### ada83.c:43225

```
// and __ada_accept_complete, so the caller was never woken (TASKING_ERROR) and
```

### ada83.c:43226

```
// its OUT parameters were left unwritten.
```

### ada83.c:43237

```
// RM 9.4: leaving via RETURN must still complete every enclosing master —
```

### ada83.c:43238

```
// signal terminate alternatives and join dependent tasks — before the frame
```

### ada83.c:43239

```
// those tasks read through their parent-frame pointer dies.
```

### ada83.c:43242

```
// Check if we're in a BIP function - result built into __BIPaccess
```

### ada83.c:43254

```
// For BIP functions, build result directly into destination
```

### ada83.c:43258

```
// Generate expression and copy to __BIPaccess destination
```

### ada83.c:43259

```
// Composite: generate expression (gives ptr), memcpy to dest
```

### ada83.c:43264

```
// A composite lowered to a first-class scalar (e.g. a folded
```

### ada83.c:43265

```
// aggregate of a single-scalar record) arrives as the value
```

### ada83.c:43266

```
// itself, not an address to copy from.
```

### ada83.c:43279

```
// Scalar: generate, convert, store to destination
```

### ada83.c:43289

```
// Do not end BIP state here: a function may have several RETURNs (e.g. one
```

### ada83.c:43290

```
// per CASE arm), and the remaining ones must still be seen as BIP. State
```

### ada83.c:43291

```
// is cleared once, at end of function generation.
```

### ada83.c:43295

```
// RM 4.8: Propagate return type to allocator for constraint checks.
```

### ada83.c:43300

```
// RM 4.8(4): RETURN NULL for fat-pointer access types must return
```

### ada83.c:43301

```
// {null, null} directly — loading a fat pointer from address 0 crashes.
```

### ada83.c:43315

```
// Returning a *constrained* array as an *unconstrained* array result: the
```

### ada83.c:43316

```
// value is a plain data pointer, but the result is a fat pointer. Build the
```

### ada83.c:43317

```
// fat pointer from the array's data and its own bounds. Without this the
```

### ada83.c:43318

```
// generic ptr-to-fat conversion would load a fat pointer out of the array's
```

### ada83.c:43319

```
// first 16 data bytes, giving garbage pointers and then a wild dereference.
```

### ada83.c:43332

```
// Float expression > fixed-point return: divide by SMALL, fptosi
```

### ada83.c:43336

```
// RM 4.6: round to nearest before truncating to the mantissa. SMALL is not
```

### ada83.c:43337

```
// exactly representable in binary, so value/SMALL lands just under the
```

### ada83.c:43338

```
// intended integer (8.5/1e-5 ≈ 849999.9999); a bare fptosi truncates it to
```

### ada83.c:43339

```
// 849999 while every other float→fixed conversion rounds to 850000 — the
```

### ada83.c:43340

```
// two representations of the same literal must match (c44003e).
```

### ada83.c:43346

```
// Convert from the value's own emitted rep, not a re-derivation of the
```

### ada83.c:43347

```
// expression's rep from the AST: the two can disagree (e.g. a user-defined
```

### ada83.c:43348

```
// "=" call emits i8 but the AST re-derivation yields i32), and value.reg
```

### ada83.c:43349

```
// was physically emitted with actual_rep.
```

### ada83.c:43353

```
// RM 6.5: the result is converted to the result subtype, an unconditional
```

### ada83.c:43354

```
// check. Pass no source subtype so the "source range ⊆ target range" skip
```

### ada83.c:43355

```
// cannot suppress it: an expression typed as the result type can still
```

### ada83.c:43356

```
// yield an out-of-subtype value (e.g. T'VAL of an out-of-range position),
```

### ada83.c:43357

```
// and that must raise CONSTRAINT_ERROR on return. A full-base result type
```

### ada83.c:43358

```
// still short-circuits inside the check.
```

### ada83.c:43363

```
// RM 6.5(3): For constrained access subtypes, check that the                                   
```

### ada83.c:43364

```
// designated object's discriminant/bounds match the constraint.                               
```

### ada83.c:43365

```
// Apply_Type_Conversion with access target calls                                               
```

### ada83.c:43366

```
// Apply_Discriminant_Check on the designated object.                                          
```

### ada83.c:43367

```
//                                                                                              
```

### ada83.c:43368

```
// RM 6.5(3) / 4.8(6): constrained access subtype return — the designated
```

### ada83.c:43369

```
// object's discriminants (record) or bounds (array) must match the subtype
```

### ada83.c:43370

```
// constraint. Single source of truth, fat-aware, reads bounds from field 1.
```

### ada83.c:43374

```
// RM 6.5: For unconstrained array returns, the data pointer in the fat                         
```

### ada83.c:43375

```
// pointer points to a local alloca that becomes invalid after return.                         
```

### ada83.c:43376

```
// Copy the data to the secondary stack so it survives (Standard:                               
```

### ada83.c:43377

```
// Build_Allocate_For_Return with secondary stack allocation).                                 
```

### ada83.c:43378

```
//                                                                                              
```

### ada83.c:43384

```
// RM 6.5: a by-value *constrained* array result is likewise a pointer to
```

### ada83.c:43385

```
// callee-local storage (typically a local alloca holding the array data);
```

### ada83.c:43386

```
// the array is returned by value, so copy it to the secondary stack so the
```

### ada83.c:43387

```
// returned pointer stays valid once the callee frame is gone. Unconstrained
```

### ada83.c:43388

```
// (fat-pointer) results are copied above; this covers the plain-data-pointer
```

### ada83.c:43389

```
// constrained case, which otherwise returns a dangling stack pointer.
```

### ada83.c:43407

```
// RM 6.5: a by-value record result must outlive the callee frame. The value
```

### ada83.c:43408

```
// is a pointer to record storage that is usually a local alloca; copy it to
```

### ada83.c:43409

```
// the secondary stack so the caller's reference stays valid after return.
```

### ada83.c:43420

```
// Task entry points return ptr for pthread compatibility
```

### ada83.c:43430

```
// A case choice's branch tail (RM 5.4): take the alternative when `match_reg`
```

### ada83.c:43431

```
// holds, else fall to the next choice — a fresh label when more choices follow,
```

### ada83.c:43432

```
// otherwise the caller's `next_check` (next alternative or the case end label).
```

### ada83.c:43441

```
// A range-valued case choice (WHEN low..high, or a subtype's range).
```

### ada83.c:43453

```
// CASE expr IS WHEN choice => stmts; ... END CASE;
```

### ada83.c:43462

```
// Generate switch-like structure using branches
```

### ada83.c:43466

```
// Allocate labels for each alternative
```

### ada83.c:43471

```
// Generate branching logic
```

### ada83.c:43479

```
// Check each choice in this alternative
```

### ada83.c:43483

```
// OTHERS matches everything - jump to alternative
```

### ada83.c:43487

```
// Range check: low <= selector <= high
```

### ada83.c:43493

```
// Normalize range bounds to selector type; trust the produced value's
```

### ada83.c:43494

```
// own rep (the AST prediction can disagree for typed constants).
```

### ada83.c:43504

```
// Subtype range: WHEN T RANGE low..high => (RM 5.4)
```

### ada83.c:43507

```
// Low and high may have different emitted reps (e.g. a literal low at
```

### ada83.c:43508

```
// i32 and a typed-constant high at i8), so track each separately.
```

### ada83.c:43519

```
// Bare subtype name: WHEN SUBTYPE => use declared bounds
```

### ada83.c:43527

```
// Coerce each bound to the selector type for icmp.
```

### ada83.c:43539

```
// Bare subtype name as case choice (RM 5.4): range check
```

### ada83.c:43543

```
// Coerce to selector type if needed
```

### ada83.c:43557

```
// Single value check
```

### ada83.c:43562

```
// Normalize choice value to selector type (e.g. boolean
```

### ada83.c:43563

```
// expressions produce i1 but BOOLEAN selector is i8). Trust the
```

### ada83.c:43564

```
// produced value's rep over the AST prediction.
```

### ada83.c:43579

```
// Generate alternative bodies - expression is a block with statements
```

### ada83.c:43596

```
// FOR loop with iteration variable - iteration_scheme is NK_BINARY_OP TK_IN
```

### ada83.c:43599

```
// Not a FOR loop - fall back to simple loop
```

### ada83.c:43608

```
// Emit source location and FOR loop header comment
```

### ada83.c:43625

```
// Store exit label on loop symbol for named EXIT statements
```

### ada83.c:43630

```
// Allocate loop variable - use native Integer type, not i64
```

### ada83.c:43632

```
// The loop variable's storage must be the one every reference resolves to.
```

### ada83.c:43633

```
// A variable that lives in an enclosing frame (e.g. a generic instance body
```

### ada83.c:43634

```
// whose locals are laid out in the parent frame) is reached through its
```

### ada83.c:43635

```
// __frame alias, not a fresh local alloca — allocating one here would leave
```

### ada83.c:43636

```
// the loop's init/step writing a local the body never reads (it reads the
```

### ada83.c:43637

```
// frame slot). Only a genuinely local loop variable gets an alloca; all
```

### ada83.c:43638

```
// accesses below go through Emit_Symbol_Storage, which picks the right one.
```

### ada83.c:43646

```
// Get range bounds. Each branch produces typed values (low_v/high_v);
```

### ada83.c:43647

```
// a single coercion to loop_type happens after the chain so narrow
```

### ada83.c:43648

```
// bound types (e.g. i8 for CHARACTER/enum ranges) widen correctly.
```

### ada83.c:43658

```
// X'RANGE attribute - need to generate both 'FIRST and 'LAST
```

### ada83.c:43662

```
// RM 4.1: P'RANGE where P is access-to-array denotes the designated array
```

### ada83.c:43663

```
// (implicit dereference). Resolve through the access so the iteration uses
```

### ada83.c:43664

```
// the designated array's bounds — otherwise an access prefix matches
```

### ada83.c:43665

```
// neither the unconstrained nor the constrained branch below and falls to
```

### ada83.c:43666

```
// the degenerate single-expression case (a one-element range).
```

### ada83.c:43672

```
// Check if this is a fat-pointer array needing runtime bounds. A direct
```

### ada83.c:43673

```
// unconstrained array carries them in its own descriptor; an access to a
```

### ada83.c:43674

```
// fat array carries them in the access value the symbol holds — either way
```

### ada83.c:43675

```
// Emit_Load_Fat_Pointer reads the { data, bounds } from the symbol.
```

### ada83.c:43693

```
// Constrained array (direct or access-to-constrained) - static bounds
```

### ada83.c:43706

```
// Single expression used as range - evaluate as both low and high
```

### ada83.c:43711

```
// Subtype indication with constraint: SUBTYPE_NAME RANGE low..high                               
```

### ada83.c:43712

```
// Per Ada RM 3.6.1(4)/5.5(9): the bounds of the discrete_range                                   
```

### ada83.c:43713

```
// must belong to the subtype - CONSTRAINT_ERROR is raised otherwise.                            
```

### ada83.c:43714

```
// Apply_Range_Check on both low and high bounds.                                                
```

### ada83.c:43715

```
//                                                                                                
```

### ada83.c:43727

```
// RM 3.2.2(11): check range bounds against subtype ST.
```

### ada83.c:43728

```
// Null ranges (low > high) are always compatible.
```

### ada83.c:43729

```
// Apply_Range_Check on both bounds.
```

### ada83.c:43730

```
//
```

### ada83.c:43741

```
// low_v/high_v carry their real type (loop_type); pass it so the
```

### ada83.c:43742

```
// check doesn't assume the narrow subtype width and sext an i32.
```

### ada83.c:43748

```
// Range might be a name like T'RANGE
```

### ada83.c:43754

```
// No range constraint - use the subtype's type bounds
```

### ada83.c:43761

```
// Just a type name: FOR I IN TYPE_NAME LOOP - iterate over type's range
```

### ada83.c:43767

```
// Other expression - evaluate as low bound, assume scalar with same high
```

### ada83.c:43773

```
// Normalize bounds to the loop variable's working type. Branches above
```

### ada83.c:43774

```
// may produce narrower types (i8 for CHARACTER/enum); coerce widens them.
```

### ada83.c:43778

```
// Initialize loop variable
```

### ada83.c:43788

```
// Loop start - check condition
```

### ada83.c:43809

```
// Loop body
```

### ada83.c:43814

```
// Ada RM 5.5: loop parameter takes each value exactly once.                                     
```

### ada83.c:43815

```
// Must check if cur == final_value BEFORE incrementing to avoid                                  
```

### ada83.c:43816

```
// overflow when iterating up to TYPE'LAST or down to TYPE'FIRST.                                
```

### ada83.c:43817

```
//                                                                                                
```

### ada83.c:43850

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:43851

```
// §13.5 Exception Handling (body)                                                                       
```

### ada83.c:43852

```
//                                                                                                  
```

### ada83.c:43853

```
// The stack unwinder's memory is what makes exceptions possible.                                  
```

### ada83.c:43854

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:43858

```
// RAISE E; or RAISE; (reraise)
```

### ada83.c:43867

```
// Store exception identity and call __ada_raise
```

### ada83.c:43874

```
// Reraise current exception
```

### ada83.c:43883

```
// Emit location and block info comment
```

### ada83.c:43892

```
// Emit LLVM label for Ada label (enables GOTO targeting this block)
```

### ada83.c:43901

```
// block label
```

### ada83.c:43902

```
// New block started
```

### ada83.c:43905

```
// Block with optional declarations and exception handlers
```

### ada83.c:43908

```
// Per Ada RM: Exception handlers only cover the statement part,                                  
```

### ada83.c:43909

```
// NOT the declarative part. Exceptions in declarations propagate                                 
```

### ada83.c:43910

```
// to the enclosing block's handler.                                                             
```

### ada83.c:43911

```
//                                                                                                
```

### ada83.c:43915

```
// Allocate handler frame first (needed for stack allocation order)
```

### ada83.c:43919

```
// Generate declarations BEFORE setting up the exception handler.
```

### ada83.c:43920

```
// This ensures exceptions in declarative part propagate outward.
```

### ada83.c:43926

```
// Now setup exception handling for the statement part only
```

### ada83.c:43932

```
// Capture the master context (this block's own master, when it has one)
```

### ada83.c:43933

```
// so the handler can complete masters abandoned by a longjmp out of a
```

### ada83.c:43934

```
// nested statement (RM 9.4).
```

### ada83.c:43937

```
// Push exception handler
```

### ada83.c:43941

```
// Call setjmp on the jmp_buf field (field 1)
```

### ada83.c:43945

```
// Branch based on setjmp return
```

### ada83.c:43949

```
// conditional branch terminates block
```

### ada83.c:43951

```
// Normal execution path
```

### ada83.c:43955

```
// Save and set exception context
```

### ada83.c:43963

```
// Generate block statements (handler covers only this part)
```

### ada83.c:43965

```
// Activation point (RM 9.3): start tasks declared in this block now that
```

### ada83.c:43966

```
// the declarative part elaborated without raising.
```

### ada83.c:43970

```
// Pop handler on normal exit
```

### ada83.c:43977

```
// Exception handler entry: complete masters abandoned by the longjmp
```

### ada83.c:43978

```
// (RM 9.4) before any handler code runs.
```

### ada83.c:43984

```
// Get exception identity and dispatch to handlers
```

### ada83.c:43988

```
// End of block: master completion runs on BOTH the normal path and the
```

### ada83.c:43989

```
// handled-exception path (RM 9.4 — a block whose handler caught still
```

### ada83.c:43990

```
// waits for its dependent tasks before completing). Signal terminate
```

### ada83.c:43991

```
// alternatives (RM 9.7.1), join dependents, leave the runtime record.
```

### ada83.c:43996

```
// Restore exception context
```

### ada83.c:44001

```
// Simple block without exception handlers
```

### ada83.c:44009

```
// RM 9.3 activation point
```

### ada83.c:44011

```
// RM 9.7.1 + 9.4
```

### ada83.c:44015

```
// RM 9.5: bind the accept's formals to the caller's parameter block at the
```

### ada83.c:44016

```
// start of a rendezvous. One binder serves both the plain ACCEPT statement
```

### ada83.c:44017

```
// and an accept alternative of a selective wait — the block layout is the
```

### ada83.c:44018

```
// same ({ i64 slot per formal, then one trailing i64 per hidden 'CONSTRAINED
```

### ada83.c:44019

```
// flag (RM 3.7.4) }), and so are the binding rules: by-reference for records,
```

### ada83.c:44020

```
// fat arrays, and fat access values; copy-in for constrained arrays and
```

### ada83.c:44021

```
// scalars; frame-slot publication when a nested unit reads a formal uplevel.
```

### ada83.c:44022

```
// Must mirror Emit_Accept_Out_Param_Writeback's slot numbering exactly.
```

### ada83.c:44025

```
// Total formal count: the caller appended the hidden 'CONSTRAINED flags
```

### ada83.c:44026

```
// (RM 3.7.4) after this many parameter slots.
```

### ada83.c:44043

```
// STRING is TYPE_STRING, not TYPE_ARRAY — use the array-like
```

### ada83.c:44044

```
// predicate so an unconstrained STRING formal is bound as a fat
```

### ada83.c:44045

```
// pointer (the reader loads a {ptr,ptr} from its storage); the
```

### ada83.c:44046

```
// old kind==TYPE_ARRAY test dropped it to the scalar path, which
```

### ada83.c:44047

```
// stored the spilled fat's address as a bare i64 and then read a
```

### ada83.c:44048

```
// fat struct back from it (a null bounds pointer, then a crash on
```

### ada83.c:44049

```
// S'FIRST).
```

### ada83.c:44053

```
// Load source pointer from params block
```

### ada83.c:44057

```
// Array parameter: alloca correct size, memcpy from source.
```

### ada83.c:44058

```
// For static sizes, use compile-time constant.
```

### ada83.c:44059

```
// For dynamic sizes (bounds are BOUND_EXPR), compute at runtime.
```

### ada83.c:44060

```
//
```

### ada83.c:44064

```
// Unconstrained or dynamic-bounds array (fat): the slot holds
```

### ada83.c:44065

```
// the address of the caller's fat pointer { data, bounds };
```

### ada83.c:44066

```
// bind the formal to it by reference (the caller is blocked
```

### ada83.c:44067

```
// for the rendezvous, so its data and bounds are stable).
```

### ada83.c:44070

```
// A nested subprogram in the accept body reads this fat
```

### ada83.c:44071

```
// formal through its frame slot (a stable getelementptr the
```

### ada83.c:44072

```
// prologue emits before the rendezvous). Bind the formal to
```

### ada83.c:44073

```
// that slot and copy the caller's fat pointer { data, bounds }
```

### ada83.c:44074

```
// into it; element writes still reach the caller's storage
```

### ada83.c:44075

```
// through the shared data pointer. Otherwise bind the formal
```

### ada83.c:44076

```
// by reference to the caller's fat directly.
```

### ada83.c:44088

```
// Statically sized constrained array: the slot holds the data
```

### ada83.c:44089

```
// address; copy it into the formal's storage (a local buffer,
```

### ada83.c:44090

```
// or its frame slot when a nested subprogram reads it uplevel).
```

### ada83.c:44098

```
// Dynamic bounds: src is caller's fat ptr { data_ptr, bounds_ptr }.
```

### ada83.c:44099

```
// Create a proper local fat pointer so 'LENGTH etc. work.
```

### ada83.c:44107

```
// 1. Dereference caller's fat ptr to get data_ptr
```

### ada83.c:44111

```
// 2. Evaluate type bounds, compute data byte size
```

### ada83.c:44128

```
// 3. Alloca for local data copy, memcpy from data_ptr
```

### ada83.c:44133

```
// 4. Alloca for local bounds struct [2*ndims x i32]
```

### ada83.c:44138

```
// 5. Create fat pointer variable { data_ptr, bounds_ptr }
```

### ada83.c:44153

```
// By-reference record (RM 6.2): the slot holds the caller's
```

### ada83.c:44154

```
// record address; bind the formal directly to it so field and
```

### ada83.c:44155

```
// discriminant access dereference the caller's object (aliased
```

### ada83.c:44156

```
// for the rendezvous, so IN OUT / OUT updates reach it).
```

### ada83.c:44161

```
// A nested unit in the accept body reads this record uplevel.
```

### ada83.c:44162

```
// Bind it by reference (RM 6.2): publish the caller's record
```

### ada83.c:44163

```
// address in the formal's slot of the TASK'S OWN frame and
```

### ada83.c:44164

```
// mark it by-reference, so a nested reader's alias loads the
```

### ada83.c:44165

```
// pointer and dereferences the caller's live object — IN OUT
```

### ada83.c:44166

```
// updates and later reads stay consistent, not a bind-time
```

### ada83.c:44167

```
// snapshot.
```

### ada83.c:44180

```
// RM 3.7.4: a mutable OUT/IN OUT formal takes its 'CONSTRAINED
```

### ada83.c:44181

```
// from the caller's flag, read from the trailing slot the call
```

### ada83.c:44182

```
// appended, and spilled to the named slot the attribute reads.
```

### ada83.c:44199

```
// Fat access (ACCESS to an unconstrained array/string): the slot
```

### ada83.c:44200

```
// holds the address of the caller's spilled fat pointer {data,
```

### ada83.c:44201

```
// bounds}; bind the formal to it by reference so reads and an
```

### ada83.c:44202

```
// OUT/IN OUT write reach the caller's storage (RM 6.2), and the
```

### ada83.c:44203

```
// caller checks the returned designated bounds on copy-back.
```

### ada83.c:44217

```
// Scalar: store the value into its storage — the frame slot when
```

### ada83.c:44218

```
// a nested subprogram reads it uplevel, else a fresh local.
```

### ada83.c:44234

```
// RM 9.5: at the end of a rendezvous, the values of OUT and IN OUT formals
```

### ada83.c:44235

```
// are copied back to the caller. Scalars are written into their parameter-
```

### ada83.c:44236

```
// block slot (the caller reads the slot back after __ada_entry_call returns);
```

### ada83.c:44237

```
// statically sized arrays are copied straight into the caller's storage
```

### ada83.c:44238

```
// through the pointer the block carries. Must mirror the parameter-load
```

### ada83.c:44239

```
// loop's slot numbering exactly. Emitted before __ada_accept_complete on the
```

### ada83.c:44240

```
// NORMAL path only — after an exception the formals are not copied (RM 6.4).
```

### ada83.c:44253

```
// A fat access formal is bound by reference to the caller's spilled fat
```

### ada83.c:44254

```
// pointer, so an OUT/IN OUT write already reached the caller — writing
```

### ada83.c:44255

```
// the i64 slot (which holds the spill ADDRESS) would corrupt it.
```

### ada83.c:44266

```
// The block slot holds the caller's data pointer; copy the local
```

### ada83.c:44267

```
// back through it. Dynamically sized arrays keep their existing
```

### ada83.c:44268

```
// (no copy-back) limitation.
```

### ada83.c:44288

```
// A conditional (`select CALL; else S;`) or timed (`select CALL; or delay
```

### ada83.c:44289

```
// D; S;`) ENTRY CALL — RM 9.7.2/9.7.3 — masquerading as a select statement.
```

### ada83.c:44290

```
// Emits the non-blocking / deadline call and branches on whether it
```

### ada83.c:44291

```
// rendezvoused; returns false when the first alternative is not an entry
```

### ada83.c:44292

```
// call (a true selective wait, handled by the NK_SELECT case). Needs none
```

### ada83.c:44293

```
// of the selective wait's machinery: no advertisement, no bookkeeping
```

### ada83.c:44294

```
// allocas, no retry loop.
```

### ada83.c:44299

```
// The entry call may sit bare (NK_APPLY), wrapped in a call statement,
```

### ada83.c:44300

```
// or be a parameterless T.E (NK_SELECTED naming an entry).
```

### ada83.c:44316

```
// Timeout budget (0 = poll once = conditional). RM 9.7.3 defers the
```

### ada83.c:44317

```
// delay's evaluation to after the call's arguments, so hand the
```

### ada83.c:44318

```
// expression to the entry-call codegen rather than evaluating here.
```

### ada83.c:44336

```
// Accepted: run the statements following the entry call.
```

### ada83.c:44344

```
// Not accepted: the `else` part (conditional) or the delay's statements
```

### ada83.c:44345

```
// (timed).
```

### ada83.c:44357

```
// The accept statement of a selective-wait alternative: bare, or wrapped in
```

### ada83.c:44358

```
// a guarded NK_ASSOCIATION; NULL for delay/terminate/else-statement shapes.
```

### ada83.c:44372

```
// Procedure call - might be NK_APPLY, NK_IDENTIFIER (no args), or
```

### ada83.c:44373

```
// NK_SELECTED (for entry calls like Task.Entry without args)
```

### ada83.c:44381

```
// Emit procedure name comment if available
```

### ada83.c:44399

```
// Selected component - might be a parameterless entry call like T.E1
```

### ada83.c:44403

```
// Entry call without explicit arguments - if the entry has                                 
```

### ada83.c:44404

```
// parameters with defaults, evaluate them and pass a proper                                
```

### ada83.c:44405

```
// params block (RM 9.5: defaults evaluated at call site).                                 
```

### ada83.c:44406

```
//                                                                                          
```

### ada83.c:44430

```
// Simple entry (no family index here)
```

### ada83.c:44433

```
// Call runtime entry call function - widen entry index for RTS ABI.
```

### ada83.c:44434

```
// Conditional/timed entry call (RM 9.7.2/9.7.3) uses the
```

### ada83.c:44435

```
// non-blocking / deadline variant, like the parameterized path.
```

### ada83.c:44439

```
// Qualified procedure call like Pkg.Proc - generate actual call
```

### ada83.c:44445

```
// Emit the call instruction prefix: a function captures its
```

### ada83.c:44446

```
// result into a fresh temporary, a procedure returns void.
```

### ada83.c:44447

```
// The argument list (empty save for the static chain) is shared.
```

### ada83.c:44463

```
// Parameterless procedure/function call
```

### ada83.c:44465

```
// Keep for defaults
```

### ada83.c:44468

```
// A procedure-renames-entry peeled to an entry: route the bare call
```

### ada83.c:44469

```
// through the full apply machinery — the rename's bound task and
```

### ada83.c:44470

```
// family index (RM 8.5, hidden elaboration slots) and the rename's
```

### ada83.c:44471

```
// own parameter defaults (RM 6.4.2) all live in entry_path there
```

### ada83.c:44472

```
// (c85018a: `ENTA;` calling a defaulted entry FAMILY member).
```

### ada83.c:44482

```
// Check if calling a nested function (transitively inside another subprogram)
```

### ada83.c:44486

```
// Pre-compute default arguments BEFORE building the call                                 
```

### ada83.c:44487

```
// instruction, so complex expressions (record/array aggregates)                          
```

### ada83.c:44488

```
// don't interleave their IR with the call text.                                         
```

### ada83.c:44489

```
//                                                                                        
```

### ada83.c:44500

```
// Task default: by reference — pass the address of the
```

### ada83.c:44501

```
// default object's slot (Parameter_Passed_By_Reference),
```

### ada83.c:44502

```
// not the loaded TCB handle.
```

### ada83.c:44514

```
// Composite default: extract data ptr from fat ptr
```

### ada83.c:44515

```
// for statically-constrained array formals.
```

### ada83.c:44516

```
// Dynamic-bounded constrained arrays use fat ptrs
```

### ada83.c:44517

```
// in the ABI (Type_To_Rep), so pass as-is.
```

### ada83.c:44518

```
//
```

### ada83.c:44528

```
// String literals carry default bounds 1..N but                                
```

### ada83.c:44529

```
// must match the formal's index constraint (RM 4.3.2).                        
```

### ada83.c:44530

```
// Rebuild fat pointer with parameter type's bounds.                           
```

### ada83.c:44531

```
//                                                                              
```

### ada83.c:44549

```
// An aggregate default's per-dimension element count is
```

### ada83.c:44550

```
// checked against the parameter subtype at the root
```

### ada83.c:44551

```
// (Generate_Aggregate) when the default aggregate is
```

### ada83.c:44552

```
// generated above (RM 4.3.2(6) / 6.4.1).
```

### ada83.c:44555

```
// Record discriminant constraint check (RM 3.7.2)
```

### ada83.c:44565

```
// Load discriminant from aggregate
```

### ada83.c:44568

```
// Get constraint value
```

### ada83.c:44575

```
// Compare
```

### ada83.c:44581

```
// Record component constraint check (RM 3.3.2)
```

### ada83.c:44598

```
// Load component value from aggregate
```

### ada83.c:44604

```
// Access type default: rep depends on designated kind
```

### ada83.c:44605

```
// (LL_REP_FAT for access-to-unconstrained-array, else LL_REP_PTR).
```

### ada83.c:44611

```
// Use val's actual rep as src - default expr may
```

### ada83.c:44612

```
// return i8 (enum) and Emit_Convert(i32->i8).reg on an
```

### ada83.c:44613

```
// already-i8 value emits invalid `trunc i32 %v to i8`.
```

### ada83.c:44616

```
// Scalar constraint check (RM 6.4.1)
```

### ada83.c:44630

```
// Precompute static chain pointer before call (RM 8.3)
```

### ada83.c:44641

```
// Pass frame pointer to nested functions (RM 8.3 static chain)
```

### ada83.c:44647

```
// Emit pre-computed default arguments
```

### ada83.c:44676

```
// Named EXIT targets a specific loop; unnamed exits innermost
```

### ada83.c:44713

```
// DELAY expression - sleep for specified duration
```

### ada83.c:44716

```
// No code needed
```

### ada83.c:44727

```
// Expression should be in seconds, convert to microseconds
```

### ada83.c:44730

```
// If expression is fixed-point (e.g. DURATION), convert from
```

### ada83.c:44731

```
// scaled integer to seconds: sitofp then multiply by SMALL
```

### ada83.c:44739

```
// Convert to microseconds (assuming Duration in seconds)
```

### ada83.c:44746

```
// ACCEPT statement - rendezvous with caller (Ada 83 9.5)
```

### ada83.c:44747

```
// Runtime: wait for entry call, execute body, complete rendezvous
```

### ada83.c:44754

```
// Get entry index - combine entry_sym's base index with family offset.                    
```

### ada83.c:44755

```
// For entry families: entry_idx = base * 1000 + family_arg                                 
```

### ada83.c:44756

```
// For simple entries: entry_idx = base * 1000                                              
```

### ada83.c:44757

```
//                                                                                          
```

### ada83.c:44760

```
// A selective wait already evaluated and range-checked this family
```

### ada83.c:44761

```
// index once (RM 9.7.1); reload it rather than evaluating again.
```

### ada83.c:44769

```
// RM 9.5(6): an out-of-range family index raises CONSTRAINT_ERROR
```

### ada83.c:44770

```
// before the accept blocks, so the task's handler sees it.
```

### ada83.c:44775

```
// Wait for entry call - widen entry index for RTS ABI
```

### ada83.c:44779

```
// RM 9.10: __ada_accept_wait returns null when this task was aborted
```

### ada83.c:44780

```
// while blocked on the accept — abandon the accept and run the task's
```

### ada83.c:44781

```
// termination epilogue (no rest of the body, no handlers).
```

### ada83.c:44790

```
// Extract params pointer from rendezvous record.                                          
```

### ada83.c:44791

```
// RV layout: { ptr task, i64 entry_idx, ptr params, i8 complete, ptr next }                
```

### ada83.c:44792

```
// Params is at offset 2 (the third pointer-sized slot).                                   
```

### ada83.c:44793

```
//                                                                                          
```

### ada83.c:44800

```
// Execute the accept body under a handler that bridges any exception to
```

### ada83.c:44801

```
// the caller (RM 9.5(16)): record the exception id in the rendezvous
```

### ada83.c:44802

```
// record, complete the rendezvous (so the caller wakes and re-raises),
```

### ada83.c:44803

```
// then re-raise in the acceptor so it propagates here too.
```

### ada83.c:44814

```
// Publish this accept's completion context so a return within the body
```

### ada83.c:44815

```
// completes the accept instead of the task body (RM 5.8/9.5); restore
```

### ada83.c:44816

```
// afterward so a nested accept's context is popped back.
```

### ada83.c:44861

```
// SELECT statement - selective wait (Ada 83 9.7)                                             
```

### ada83.c:44862

```
// Forms: selective_wait, conditional_entry_call, timed_entry_call                            
```

### ada83.c:44863

```
// Runtime: check open alternatives, wait or execute else                                     
```

### ada83.c:44864

```
//                                                                                            
```

### ada83.c:44865

```
// A conditional/timed ENTRY CALL is its own statement shape.
```

### ada83.c:44874

```
// Every execution of a selective wait allocas bookkeeping (guard
```

### ada83.c:44875

```
// slots, family indices, delay budget, the open-entry list); scope
```

### ada83.c:44876

```
// it so a server loop does not grow its frame each iteration. All
```

### ada83.c:44877

```
// re-entrant paths rejoin at done_label, where the frame pointer is
```

### ada83.c:44878

```
// restored; exits that leave the frame entirely (EXIT, abort
```

### ada83.c:44879

```
// epilogue, exceptions) do not re-execute the select.
```

### ada83.c:44882

```
// Check for delay and terminate alternatives
```

### ada83.c:44894

```
// A selective wait re-sweeps its alternatives until one is
```

### ada83.c:44895

```
// selected when it has a terminate alternative (each sweep
```

### ada83.c:44896

```
// re-tries the consensus), a delay alternative (block until the
```

### ada83.c:44897

```
// deadline, waking early for arrivals — sleeping the whole
```

### ada83.c:44898

```
// duration in one shot would refuse rendezvous for its span,
```

### ada83.c:44899

```
// RM 9.7.1), or no else part (it then waits indefinitely). Each
```

### ada83.c:44900

```
// block-and-resweep round is event-driven: __ada_select_block
```

### ada83.c:44901

```
// parks until a broadcast or the budget's expiry.
```

### ada83.c:44904

```
// RM 9.7.1: guard conditions are evaluated ONCE, at the start of
```

### ada83.c:44905

```
// the selective wait — never re-evaluated while waiting (c97116a).
```

### ada83.c:44906

```
// Pre-evaluate every guard into an i8 slot; the polling loop loads.
```

### ada83.c:44932

```
// RM 9.7.1: the entry index of every OPEN accept alternative for an
```

### ada83.c:44933

```
// entry family is evaluated here — after all the guards and before any
```

### ada83.c:44934

```
// rendezvous is attempted — not lazily when that one alternative is
```

### ada83.c:44935

```
// finally selected (c97113a, c97115a: only the E1 caller rendezvouses,
```

### ada83.c:44936

```
// yet the other open families' indices must still be evaluated). Each
```

### ada83.c:44937

```
// open index is evaluated and range-checked once into an alloca that
```

### ada83.c:44938

```
// the accept generation reloads; a guarded index is evaluated only when
```

### ada83.c:44939

```
// its guard is open. A guarded accept alternative arrives wrapped in an
```

### ada83.c:44940

```
// NK_ASSOCIATION (guard, accept); an unguarded one is a bare NK_ACCEPT.
```

### ada83.c:44975

```
// Delay alternative deadline (RM 9.7.1): every delay expression is
```

### ada83.c:44976

```
// evaluated once, at the start of the selective wait; the budget is
```

### ada83.c:44977

```
// the MINIMUM over the OPEN delay alternatives (closed guards do
```

### ada83.c:44978

```
// not contribute). All delays closed leaves the sentinel: the wait
```

### ada83.c:44979

```
// then blocks indefinitely, like a select with no delay.
```

### ada83.c:44981

```
// delay whose trailing statements run
```

### ada83.c:45019

```
// RM 9.7.1: if ALL alternatives are closed and there is no else
```

### ada83.c:45020

```
// part, the selective wait raises PROGRAM_ERROR. Only possible when
```

### ada83.c:45021

```
// every alternative carries a guard; an unguarded alternative is
```

### ada83.c:45022

```
// always open.
```

### ada83.c:45031

```
// trailing statements
```

### ada83.c:45064

```
// Advertise the blocked wait (RM 9.7.2): publish the OPEN accept
```

### ada83.c:45065

```
// alternatives' encoded entry indices ({ count, idx... }, closed
```

### ada83.c:45066

```
// slots -1) at TCB+80, then -2 in waiting_entry. Guards and family
```

### ada83.c:45067

```
// indices were evaluated once above (RM 9.7.1), so the set is fixed
```

### ada83.c:45068

```
// for the whole wait and is published ONCE, before the retry loop.
```

### ada83.c:45069

```
// A conditional caller rendezvouses iff its exact entry is in the
```

### ada83.c:45070

```
// set; __ada_select_block and __ada_term_wait wake only for calls
```

### ada83.c:45071

```
// the sweep below can actually take. A select with an ELSE part is
```

### ada83.c:45072

```
// a single poll that never waits: it publishes nothing, so no
```

### ada83.c:45073

```
// conditional caller can park a call on it (c97201x). With a
```

### ada83.c:45074

```
// terminate alternative, also mark the task as willing to terminate
```

### ada83.c:45075

```
// (TCB+58) for the RM 9.7.1 consensus.
```

### ada83.c:45112

```
// Publish under the rendezvous lock: a caller that reads -2 under
```

### ada83.c:45113

```
// the same lock must also see the list pointer and its contents.
```

### ada83.c:45121

```
// A guarded terminate alternative counts toward the RM 9.7.1
```

### ada83.c:45122

```
// consensus only when its guard is TRUE (open); store the
```

### ada83.c:45123

```
// pre-evaluated guard value rather than a constant.
```

### ada83.c:45146

```
// selective wait retry
```

### ada83.c:45148

```
// Track if current iteration was a skipped delay
```

### ada83.c:45150

```
// Generate alternatives
```

### ada83.c:45158

```
// Guarded alternative: WHEN cond => stmt. The guard was
```

### ada83.c:45159

```
// evaluated once before the wait (RM 9.7.1); load it.
```

### ada83.c:45182

```
// Accept alternative
```

### ada83.c:45188

```
// Guard (RM 9.7.1): a false WHEN condition closes this
```

### ada83.c:45189

```
// alternative — skip it (do not accept its calls). The guard
```

### ada83.c:45190

```
// was evaluated once before the wait; load it.
```

### ada83.c:45208

```
// Get entry index - combine base index with family index.
```

### ada83.c:45209

```
// Formula: entry_idx = base * 1000 + family_arg
```

### ada83.c:45212

```
// Pre-evaluated once, above, with all the other open indices
```

### ada83.c:45213

```
// (RM 9.7.1); reload rather than re-evaluate.
```

### ada83.c:45221

```
// RM 9.5(6)/9.7.1: the family index of an open accept
```

### ada83.c:45222

```
// alternative is evaluated at the selective wait; out of
```

### ada83.c:45223

```
// range raises CONSTRAINT_ERROR here.
```

### ada83.c:45229

```
// Check if entry call is pending - widen for RTS ABI
```

### ada83.c:45238

```
// Load parameters from the caller's parameter block (the
```

### ada83.c:45239

```
// rendezvous record holds it at offset 16; the record itself
```

### ada83.c:45240

```
// starts with the target task pointer).
```

### ada83.c:45251

```
// Execute the rendezvous body (RM 9.5), complete the rendezvous
```

### ada83.c:45252

```
// (unblocking the caller), THEN run the alternative's trailing
```

### ada83.c:45253

```
// statements. Trailing statements that call back into the still-
```

### ada83.c:45254

```
// blocked caller's task would otherwise deadlock (a98002a: T's
```

### ada83.c:45255

```
// CHOICE.RETURN_CALL while CHOICE waits in T.WAIT).
```

### ada83.c:45273

```
// Delay alternative: handled by the budget check at the end
```

### ada83.c:45274

```
// of the polling chain (the duration was evaluated before the
```

### ada83.c:45275

```
// retry loop). Nothing to emit at this position; its trailing
```

### ada83.c:45276

```
// statements are emitted at delay_label below.
```

### ada83.c:45280

```
// Emit the next_label for branches that skip this alternative
```

### ada83.c:45283

```
// Terminate alternative (RM 9.7.1):                                                  
```

### ada83.c:45284

```
// Instead of immediately terminating, loop back                                      
```

### ada83.c:45285

```
// to re-check accept alternatives. The task will                                    
```

### ada83.c:45286

```
// be terminated when the master scope completes                                      
```

### ada83.c:45287

```
// and calls exit (), ending the process.                                            
```

### ada83.c:45288

```
//                                                                                    
```

### ada83.c:45289

```
// Terminate alternative (RM 9.7.1): if the master has completed,
```

### ada83.c:45290

```
// end this task via the normal task-body return — the wrapper then
```

### ada83.c:45291

```
// marks it completed, so the master's join returns and 'TERMINATED
```

### ada83.c:45292

```
// reads true. Otherwise sleep and re-poll the accept alternatives.
```

### ada83.c:45293

```
// A FALSE guard closes the alternative: no parking, no consensus.
```

### ada83.c:45307

```
// Park in __ada_term_wait (RM 9.7.1): sleeps on the consensus
```

### ada83.c:45308

```
// condvar; returns 1 when this task is selected for
```

### ada83.c:45309

```
// termination (master complete, whole sibling subtree
```

### ada83.c:45310

```
// quiescent), 0 when an entry call arrived — re-sweep the
```

### ada83.c:45311

```
// accept alternatives. (A selective wait exists only in a
```

### ada83.c:45312

```
// task body, RM 9.7.1 — the sweep above already relies on
```

### ada83.c:45313

```
// %__self_tcb unconditionally.)
```

### ada83.c:45322

```
// RM 9.4: a task selected for termination completes its own
```

### ada83.c:45323

```
// masters on the way out — publish completion first (a
```

### ada83.c:45324

```
// dependent blocked on one of THIS task's entries must get
```

### ada83.c:45325

```
// TASKING_ERROR, not deadlock the join), then run the same
```

### ada83.c:45326

```
// master ladder as a RETURN exit: raise the done flags
```

### ada83.c:45327

```
// (releasing inner terminate alternatives), join dependents,
```

### ada83.c:45328

```
// leave the records.
```

### ada83.c:45341

```
// term_wait wakes for serving only when the queue holds a
```

### ada83.c:45342

```
// call some OPEN alternative can take (closed-entry calls
```

### ada83.c:45343

```
// never wake it), so the re-sweep is guaranteed to progress
```

### ada83.c:45344

```
// — branch straight back, no pacing.
```

### ada83.c:45353

```
// For skipped delay alternatives, don't emit next_label since
```

### ada83.c:45354

```
// we've already branched to delay_label and this would be unreachable
```

### ada83.c:45358

```
// If this isn't the last alternative, fall through to next;
```

### ada83.c:45359

```
// otherwise go to delay or done
```

### ada83.c:45360

```
// Fall through to the next alternative's code; the chain end
```

### ada83.c:45361

```
// below (else part / delay budget / retry) handles the case
```

### ada83.c:45362

```
// where no alternative was selected.
```

### ada83.c:45366

```
// Nothing was selected this sweep.
```

### ada83.c:45368

```
// Immediate else (RM 9.7.1): one poll, then the else part.
```

### ada83.c:45372

```
// Delay alternative: block until a serviceable call, an abort, or
```

### ada83.c:45373

```
// the deadline; charge the blocked time against the budget.
```

### ada83.c:45398

```
// No else, no delay (and the terminate alternative, when present,
```

### ada83.c:45399

```
// branched back itself): block until a serviceable call or an
```

### ada83.c:45400

```
// abort arrives, then re-sweep (RM 9.7.1).
```

### ada83.c:45409

```
// Delay alternative body: the deadline expired without a rendezvous.
```

### ada83.c:45410

```
// Its trailing statements are the alternatives-list items between
```

### ada83.c:45411

```
// the (first) NK_DELAY and the next alternative.
```

### ada83.c:45427

```
// Leaving the selective wait: no longer accepting or terminating,
```

### ada83.c:45428

```
// and the open-entry list (frame storage of this select) retires.
```

### ada83.c:45445

```
// ABORT statement - abort named tasks (Ada 83 9.10)
```

### ada83.c:45451

```
// RM 9.10: a task aborting itself — directly, or by aborting an
```

### ada83.c:45452

```
// ANCESTOR, whose dependents (including this task) become abnormal
```

### ada83.c:45453

```
// with it — does not execute the statement that follows the abort.
```

### ada83.c:45454

```
// pthread_cancel of the running thread is deferred to the next
```

### ada83.c:45455

```
// cancellation point, so end the task body here when SELF became
```

### ada83.c:45456

```
// abnormal: read this task's own abort-pending flag, which
```

### ada83.c:45457

```
// __ada_abort_subtree just set if we were in the target's subtree
```

### ada83.c:45458

```
// (c94020a: T41 aborts its parent T4).
```

### ada83.c:45477

```
// Ada label - allocate LLVM label ID and emit label
```

### ada83.c:45485

```
// Need a branch to the label to terminate previous block (if not already)
```

### ada83.c:45490

```
// user label
```

### ada83.c:45491

```
// New block started
```

### ada83.c:45494

```
// Generate the labeled statement
```

### ada83.c:45501

```
// GOTO statement - use resolved target label and branch
```

### ada83.c:45513

```
// br is a terminator
```

### ada83.c:45522

```
// Strip quotes if present
```

### ada83.c:45532

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:45533

```
// §13.8 Declaration Code Generation                                                                
```

### ada83.c:45534

```
//                                                                                                  
```

### ada83.c:45535

```
// Names get bound to meanings, and those bindings are what we generate.                           
```

### ada83.c:45536

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:45538

```
// Check if external name is a builtin/runtime function (already defined)
```

### ada83.c:45546

```
// Custom text_io helpers
```

### ada83.c:45552

```
// Standard C library functions (declared in runtime preamble)
```

### ada83.c:45559

```
// Tasking/calendar runtime defined in the emitted runtime block
```

### ada83.c:45572

```
// Emit function signature for extern declaration
```

### ada83.c:45573

```
// One module-level `declare` per mangled name. Distinct Symbol instances can
```

### ada83.c:45574

```
// denote one entity — a package re-analysed for each compilation unit of a
```

### ada83.c:45575

```
// multi-unit source file — so per-symbol emitted flags cannot catch the
```

### ada83.c:45576

```
// duplicate, and LLVM rejects a repeated declare of one name (cd5003c).
```

### ada83.c:45577

```
// True on first registration; false when the name is already declared.
```

### ada83.c:45578

```
// Imported symbols keep their own external-name registry.
```

### ada83.c:45593

```
// Symbol_Mangle_Name (behind Symbol_Emitted_Name) hands out a rotating
```

### ada83.c:45594

```
// static buffer: copy the bytes, or the registry's entries mutate under it.
```

### ada83.c:45604

```
// Skip if already emitted
```

### ada83.c:45608

```
// Skip if this is a builtin function that we've already defined
```

### ada83.c:45614

```
// One declare per external name (RM generic copies share the C name).
```

### ada83.c:45627

```
// The declare must mirror the define's signature exactly: same return
```

### ada83.c:45628

```
// spelling (BIP functions are void) and same parameter list (ptr for
```

### ada83.c:45629

```
// OUT / IN OUT formals, hidden 'CONSTRAINED flags appended). Call sites
```

### ada83.c:45630

```
// already lower arguments this way, so a value-typed declare for a
```

### ada83.c:45631

```
// by-reference formal would make llvm-link reject the module pair.
```

### ada83.c:45638

```
// Defer an imported subprogram's `declare` to module scope. Generate_Declaration
```

### ada83.c:45639

```
// runs both at library level and — through generic instantiation — inside a
```

### ada83.c:45640

```
// function body, where a `declare` is not a legal instruction. Collecting the
```

### ada83.c:45641

```
// symbol and emitting it once at the flush point keeps every declaration at
```

### ada83.c:45642

```
// module top level regardless of where the spec was elaborated.
```

### ada83.c:45645

```
// Overflow: fall back to inline emit
```

### ada83.c:45668

```
// Activate every task created in the current declarative part since `from`, in
```

### ada83.c:45669

```
// declaration order (RM 9.3 — tasks declared in a declarative part are
```

### ada83.c:45670

```
// activated together at the enclosing master's `begin`). Emitted at the
```

### ada83.c:45671

```
// declaration->statement boundary; a runtime exception during the declarative
```

### ada83.c:45672

```
// part longjmps past this point, so the unactivated tasks are never threaded
```

### ada83.c:45673

```
// (RM 9.3: they become completed without activation). Clears back to `from`.
```

### ada83.c:45674

```
// The static-link argument for a task created in the current context: the
```

### ada83.c:45675

```
// current function's own frame when it has one, else the inherited parent
```

### ada83.c:45676

```
// frame when inside a nested subprogram or task body (the task type lives in
```

### ada83.c:45677

```
// an enclosing scope), else null at library level.
```

### ada83.c:45684

```
// The static-link argument for creating a task of `task_sym`'s type: the
```

### ada83.c:45685

```
// frame of the TYPE's defining scope, reached through the static chain
```

### ada83.c:45686

```
// exactly as a call to a subprogram declared beside the type would reach
```

### ada83.c:45687

```
// it. The task body reads its uplevel state through this frame — handing
```

### ada83.c:45688

```
// it the creating context's frame instead corrupts every uplevel access
```

### ada83.c:45689

```
// when the type is declared further out (c94001c case E: a task type from
```

### ada83.c:45690

```
// the main procedure created inside a doubly nested task body). May emit
```

### ada83.c:45691

```
// chain-walk instructions; returns a static buffer — consume within the
```

### ada83.c:45692

```
// next Emit calls.
```

### ada83.c:45705

```
// The master-record argument for __ada_task_create / __ada_task_start
```

### ada83.c:45706

```
// (RM 9.4): the innermost active master's record, or null at library level
```

### ada83.c:45707

```
// (the program-exit join is then the effective master). Static buffer —
```

### ada83.c:45708

```
// consume within the next Emit calls.
```

### ada83.c:45717

```
// Apply one task operation to every task embedded in `ty` at address `base`:
```

### ada83.c:45718

```
// a task object slot itself, record components, and (statically bounded)
```

### ada83.c:45719

```
// array elements, recursively. `failed_flag` is an alloca'd i8 the WAIT
```

### ada83.c:45720

```
// operation ORs its activation-failed bit into.
```

### ada83.c:45782

```
// Statically bounded arrays only; unrolled like Emit_Init_Record_Defaults.
```

### ada83.c:45792

```
// Apply one task operation to every element of a heap array whose element
```

### ada83.c:45793

```
// count is only known at run time — an unconstrained-array allocator with
```

### ada83.c:45794

```
// task components (RM 9.3). Emits a counted loop: for i in [0, count),
```

### ada83.c:45795

```
// apply `op` to the element at data + i*elem_size, recursing through
```

### ada83.c:45796

```
// Emit_Component_Task_Operation so the element may itself be a task, a
```

### ada83.c:45797

```
// record with task components, or a statically bounded array of them.
```

### ada83.c:45826

```
// RM 9.4: resolve the master a task created by an allocator depends on — the
```

### ada83.c:45827

```
// master that elaborated the ACCESS TYPE, found by its static scope id on the
```

### ada83.c:45828

```
// running thread's master chain (id 0 = library level → the program master,
```

### ada83.c:45829

```
// a null record). Writes "ptr %tN" or "ptr null" into `buffer`.
```

### ada83.c:45833

```
// A generic instance's formal access type is a VIEW of the instantiation
```

### ada83.c:45834

```
// actual; the master that elaborated the ACTUAL's declaration is the
```

### ada83.c:45835

```
// allocated task's master (RM 9.4 / RM 12.3 — c94011a), so peel views
```

### ada83.c:45836

```
// before reading the id.
```

### ada83.c:45852

```
// Apply one task operation to a pending-activation entry: a plain task
```

### ada83.c:45853

```
// object (TCB slot named by the symbol) or a composite containing tasks.
```

### ada83.c:45859

```
// Pending entries are activate/wait/join only — creation (which takes the
```

### ada83.c:45860

```
// master) happened at the declaration.
```

### ada83.c:45864

```
// Emit the activation-failure check: load the accumulated flag and raise
```

### ada83.c:45865

```
// TASKING_ERROR in the activator when any activation failed (RM 9.3(7)),
```

### ada83.c:45866

```
// after ALL activations have been waited for.
```

### ada83.c:45882

```
// RM 3.9 / AI-00149: accumulate (OR) an "unelaborated body" i1 over every
```

### ada83.c:45883

```
// task type reachable by value inside `ty` — the task itself, or task
```

### ada83.c:45884

```
// components of records and arrays, however nested. Returns the updated
```

### ada83.c:45885

```
// accumulator register (`acc` may be 0 = nothing accumulated yet).
```

### ada83.c:45905

```
// Raise PROGRAM_ERROR now if any task body reachable inside `ty` is not yet
```

### ada83.c:45906

```
// elaborated (allocator activation, where nothing is created before the
```

### ada83.c:45907

```
// check so a bare raise strands nothing).
```

### ada83.c:45915

```
// Activate the pending-activation entries in [from, to): spawn all threads
```

### ada83.c:45916

```
// first (the tasks of one declarative part activate in parallel), then wait
```

### ada83.c:45917

```
// for each activation to complete and raise TASKING_ERROR if any failed
```

### ada83.c:45918

```
// (RM 9.3). The entries stay recorded: they are also the enclosing master's
```

### ada83.c:45919

```
// dependents, joined at the master's exit by Emit_Join_Dependent_Tasks.
```

### ada83.c:45924

```
// RM 3.9 / AI-00149: the tasks of one declarative part activate together,
```

### ada83.c:45925

```
// so if ANY has a body not yet elaborated — including tasks nested inside
```

### ada83.c:45926

```
// record or array components (c93007a's OBJ : REC) — PROGRAM_ERROR is
```

### ada83.c:45927

```
// raised before the batch is activated and NONE of them runs (c39008c).
```

### ada83.c:45928

```
// The batch's tasks become COMPLETED (RM 9.3): they must not resurrect at
```

### ada83.c:45929

```
// an enclosing activation point, calls to them raise TASKING_ERROR, and
```

### ada83.c:45930

```
// the master's join passes over them.
```

### ada83.c:45962

```
// Activate every pending task since `from` (RM 9.3 — activated together at
```

### ada83.c:45963

```
// the enclosing master's `begin`). A runtime exception in the declarative
```

### ada83.c:45964

```
// part longjmps past this point, so those tasks are never threaded (they
```

### ada83.c:45965

```
// become completed without activation). Runtime activation is idempotent,
```

### ada83.c:45966

```
// so entries a nested package body already activated are passed over.
```

### ada83.c:45971

```
// RM 9.4: a master cannot leave until its dependent tasks have terminated —
```

### ada83.c:45972

```
// otherwise the frame the tasks read through their parent-frame pointer dies
```

### ada83.c:45973

```
// under them (observed as stack-reuse corruption in a98002a). Called at the
```

### ada83.c:45974

```
// master's normal exit, AFTER the master-done flag is raised (so dependents
```

### ada83.c:45975

```
// blocked at terminate alternatives can end). Clears the list back to `from`.
```

### ada83.c:45983

```
// Master-completion sequence for a RETURN path (RM 9.4): raise every active
```

### ada83.c:45984

```
// master's done flag (releasing terminate alternatives), join all dependents
```

### ada83.c:45985

```
// created since the outermost active master, and leave each runtime master
```

### ada83.c:45986

```
// record innermost-first. Compile-time state (pending list, master stack) is
```

### ada83.c:45987

```
// NOT popped here — the enclosing emission sites still own it for the
```

### ada83.c:45988

```
// normal-exit path; this only emits the runtime effect on the return path.
```

### ada83.c:46003

```
// The TCB designated by a task-valued prefix (RM 9.9 'CALLABLE/'TERMINATED).
```

### ada83.c:46004

```
// A task object's value IS the TCB pointer; an access-to-task value
```

### ada83.c:46005

```
// designates the allocator cell HOLDING the TCB pointer — one more load,
```

### ada83.c:46006

```
// null-guarded (a null access yields a null TCB, which the attribute
```

### ada83.c:46007

```
// runtimes read as not-callable / not-terminated rather than crashing).
```

### ada83.c:46029

```
// Record a task object awaiting activation at the enclosing master's `begin`
```

### ada83.c:46030

```
// (RM 9.3). The list grows without bound — a dropped entry would be a task
```

### ada83.c:46031

```
// that never runs.
```

### ada83.c:46044

```
// The innermost active master's runtime record temp, or 0 outside any master.
```

### ada83.c:46045

```
// The master stack is the single source of truth (RM 9.4).
```

### ada83.c:46052

```
// Master entry for a scope (RM 9.4): when the scope's declarations create
```

### ada83.c:46053

```
// task dependents, allocate the done flag, enter the runtime master record,
```

### ada83.c:46054

```
// and push it on the master stack; otherwise the scope is not a master and
```

### ada83.c:46055

```
// only the pending-activation watermark is captured. The returned scope is
```

### ada83.c:46056

```
// the ticket for the matching Emit_Master_Exit.
```

### ada83.c:46087

```
// Normal-exit master completion: raise the done flag, join dependents, leave
```

### ada83.c:46088

```
// the runtime master record. Skipped when the current block is already
```

### ada83.c:46089

```
// terminated (the unit ended in RETURN, which emitted the cleanup itself via
```

### ada83.c:46090

```
// Emit_Master_Cleanup_For_Return); compile-time state is always unwound.
```

### ada83.c:46104

```
// Allocate a master's done flag on the heap (stable for the lifetime of its
```

### ada83.c:46105

```
// dependents, which outlive the frame), initialised to 0. Returns its temp.
```

### ada83.c:46112

```
// Raise a master's done flag at the master's completion (RM 9.7.1 terminate
```

### ada83.c:46113

```
// consensus): dependent tasks parked at a terminate alternative then end.
```

### ada83.c:46114

```
// Goes through the runtime (lock + broadcast) — a parked dependent sleeps on
```

### ada83.c:46115

```
// the consensus condvar and is joined right after this; a plain store would
```

### ada83.c:46116

```
// deadlock the join.
```

### ada83.c:46122

```
// True when every discriminant named in `e` has its storage bound
```

### ada83.c:46123

```
// (disc_agg_temp) in the current context. A discriminant with no binding — as
```

### ada83.c:46124

```
// when checking a constrained subtype at its own declaration, where no object
```

### ada83.c:46125

```
// exists — cannot be read, so its expression must be skipped rather than lowered
```

### ada83.c:46126

```
// into an out-of-scope reference.
```

### ada83.c:46136

```
// Generate an LLVM SSA temp containing the constraint value for discriminant
```

### ada83.c:46137

```
// disc_index of record type_info. Prefers the pre-evaluated alloca (RM 3.7.1
```

### ada83.c:46138

```
// evaluate-once semantics) over the expression over the static compile-time
```

### ada83.c:46139

```
// value. Returns NO_VALUE when unavailable.
```

### ada83.c:46151

```
// Only evaluate when the constraint's discriminant references are bound.
```

### ada83.c:46164

```
// RM 3.7.2(3): When a record type with disc-dependent component constraints                        
```

### ada83.c:46165

```
// is explicitly constrained, the deferred checks on pre-evaluated non-disc                         
```

### ada83.c:46166

```
// expressions must be performed. For each component that has disc-dependent                       
```

### ada83.c:46167

```
// constraints, resolve discriminant values from the parent's now-known                             
```

### ada83.c:46168

```
// constraints and check pre-evaluated values against their disc subtypes.                         
```

### ada83.c:46169

```
//                                                                                                  
```

### ada83.c:46170

```
// RM 3.7.2: elaborate the discriminant constraint recorded on
```

### ada83.c:46171

```
// `constrained_type` - pre-evaluate what must be frozen, then range-check the
```

### ada83.c:46172

```
// constraint values against each discriminant's subtype. Shared by the type
```

### ada83.c:46173

```
// and subtype declaration arms of Generate_Declaration.
```

### ada83.c:46174

```
//
```

### ada83.c:46175

```
// If ANY constraint expression depends on a discriminant, the whole
```

### ada83.c:46176

```
// constraint is discriminant-dependent: the non-disc expressions are
```

### ada83.c:46177

```
// pre-evaluated once (RM 3.7.1) into frozen globals and every check is
```

### ada83.c:46178

```
// deferred until the constraint is applied explicitly (RM 3.7.2(3)).
```

### ada83.c:46179

```
//
```

### ada83.c:46180

```
// Otherwise each constraint value is checked against its discriminant's
```

### ada83.c:46181

```
// subtype now. `use_frozen_constraint_values` selects how that value is
```

### ada83.c:46182

```
// obtained:
```

### ada83.c:46183

```
//   true  - via Emit_Disc_Constraint_Value, which reads the frozen
```

### ada83.c:46184

```
//           evaluate-once value; a function constraint is NOT called again
```

### ada83.c:46185

```
//           (the type-declaration behavior).
```

### ada83.c:46186

```
//   false - by re-evaluating disc_constraint_exprs (falling back to the
```

### ada83.c:46187

```
//           static values); a side-effecting constraint function is called
```

### ada83.c:46188

```
//           again here, although RM 3.7.2 evaluates a discriminant
```

### ada83.c:46189

```
//           constraint exactly once (the subtype-declaration behavior,
```

### ada83.c:46190

```
//           preserved as-is).
```

### ada83.c:46206

```
// Pre-evaluate non-disc expressions and cache in frozen globals. This
```

### ada83.c:46207

```
// ensures they are evaluated once at elaboration (RM 3.7.1) but not
```

### ada83.c:46208

```
// checked until constraint application. Skip if already pre-evaluated
```

### ada83.c:46209

```
// (same constrained type referenced by multiple enclosing types).
```

### ada83.c:46212

```
// Already pre-evaluated
```

### ada83.c:46225

```
// Non-disc expression: evaluate once, store to a frozen global
```

### ada83.c:46235

```
// All checks deferred for disc-dependent constraints
```

### ada83.c:46245

```
// Read the frozen constraint value (preeval) rather than re-evaluating
```

### ada83.c:46246

```
// the constraint expression (RM 3.2.1) - a function constraint must
```

### ada83.c:46247

```
// not be called again here.
```

### ada83.c:46260

```
// RM 3.7.2(3): also check nested component disc constraints
```

### ada83.c:46269

```
// RM 3.7.3: Determine selected variant to skip absent components.                               
```

### ada83.c:46270

```
// Only check components in the fixed part or the selected variant.                              
```

### ada83.c:46271

```
// For dynamic disc values, emit runtime range checks.                                           
```

### ada83.c:46272

```
//                                                                                                
```

### ada83.c:46273

```
// -2 = unknown, -1+ = variant index
```

### ada83.c:46274

```
// LLVM temp for runtime disc value
```

### ada83.c:46277

```
// Check if disc expression is dynamic (disc_constraint_exprs[0] set)
```

### ada83.c:46282

```
// Static: use compile-time range matching
```

### ada83.c:46285

```
// default: no matching variant
```

### ada83.c:46296

```
// Dynamic: evaluate disc expression for runtime variant check.
```

### ada83.c:46297

```
// selected_variant stays -2 to trigger runtime branching below.
```

### ada83.c:46301

```
// Try resolving via disc_agg_temp first (allocator/aggregate path)
```

### ada83.c:46315

```
// The variant discriminant cannot be read here; without it the selected
```

### ada83.c:46316

```
// variant is unknown, so defer all of this record's checks to when an
```

### ada83.c:46317

```
// object of it is built.
```

### ada83.c:46323

```
// RM 3.7.3: a default-discriminant object supplies its governing discriminant
```

### ada83.c:46324

```
// at elaboration with no explicit constraint, so neither branch above resolves
```

### ada83.c:46325

```
// the variant. When the caller (an object or allocator elaboration) has read
```

### ada83.c:46326

```
// that value from the object, use it to guard each variant component's check
```

### ada83.c:46327

```
// at runtime — a component in an unselected arm is skipped, one in the
```

### ada83.c:46328

```
// selected arm is checked.
```

### ada83.c:46335

```
// Skip variant components not in the selected variant. When the selected
```

### ada83.c:46336

```
// variant is unknown at this point (no explicit disc constraint, no
```

### ada83.c:46337

```
// runtime disc value bound), variant components can't be checked yet —
```

### ada83.c:46338

```
// their constraints would raise spurious CE for values that will land in
```

### ada83.c:46339

```
// a different variant at object init (c37215f: X : CONS(D3 default 11),
```

### ada83.c:46340

```
// C1 is only in the -5..10 arm but the check evaluated its constraint
```

### ada83.c:46341

```
// against D3=11 anyway).
```

### ada83.c:46352

```
// Runtime variant guard: if disc value is dynamic and this component
```

### ada83.c:46353

```
// is in a variant, emit a conditional branch to skip it at runtime.
```

### ada83.c:46372

```
// Pre-evaluated non-disc expression: load the frozen value and check
```

### ada83.c:46377

```
// Disc-dependent: resolve from parent's constraints
```

### ada83.c:46401

```
// Recurse into component's own components
```

### ada83.c:46404

```
// Close runtime variant guard if opened
```

### ada83.c:46408

```
// Also check array components with disc-dependent index bounds.
```

### ada83.c:46409

```
// RM 3.6.1(7): when a constrained array's index bounds depend on a
```

### ada83.c:46410

```
// discriminant and that discriminant is now known, verify the bounds lie
```

### ada83.c:46411

```
// within the index subtype. Variant components not in the selected variant
```

### ada83.c:46412

```
// are skipped, as above.
```

### ada83.c:46420

```
// Runtime variant guard for array components
```

### ada83.c:46437

```
// Evaluate each bound, resolving disc references to parent
```

### ada83.c:46445

```
// A non-discriminant bound was evaluated once at type elaboration
```

### ada83.c:46446

```
// (RM 3.7.1). Load that frozen value rather than re-evaluating the
```

### ada83.c:46447

```
// expression, which would repeat its side effects.
```

### ada83.c:46462

```
// Only an explicit discriminant constraint yields a value here.
```

### ada83.c:46463

```
// An unconstrained parent (an object or allocator taking the
```

### ada83.c:46464

```
// defaults) has none — leave `resolved` false so the bound is
```

### ada83.c:46465

```
// read from the discriminant field bound below, not treated as
```

### ada83.c:46466

```
// a zero (which would make `2 .. D3` look like a null range and
```

### ada83.c:46467

```
// suppress the index check).
```

### ada83.c:46480

```
// Only check non-empty ranges (RM 3.6.1)
```

### ada83.c:46484

```
// Close runtime variant guard for array component
```

### ada83.c:46489

```
// Find discriminant symbols referenced in an expression tree.
```

### ada83.c:46490

```
// Used by nested default initialization to set disc_agg_temp.
```

### ada83.c:46502

```
// Recurse into children based on node kind
```

### ada83.c:46505

```
// cg->current_nesting_level is repurposed: 1 = has nested functions, use frame
```

### ada83.c:46548

```
// Allocate uninitialised backing store for an array with the given per-dimension
```

### ada83.c:46549

```
// bounds (value temps at Integer_Arith_Rep) and bind it to `sym` as a fat
```

### ada83.c:46550

```
// pointer. The byte size is (Π (hi-lo+1)) * element_size, clamped to zero for a
```

### ada83.c:46551

```
// null range so a negative count cannot turn into a huge unsigned alloca.
```

### ada83.c:46552

```
// `fat_bt` is the bound representation stored in the fat pointer.
```

### ada83.c:46558

```
// The element count and byte size are accumulated in i64 so the exact value
```

### ada83.c:46559

```
// survives even when it exceeds the index base type. A computation in the
```

### ada83.c:46560

```
// index width would wrap a 2**31-element array to a small — or zero — size,
```

### ada83.c:46561

```
// and the resulting under-sized alloca is then overrun by later component
```

### ada83.c:46562

```
// writes, silently corrupting neighbouring objects. RM 11.1: an object whose
```

### ada83.c:46563

```
// size is not representable cannot be allocated, so we raise STORAGE_ERROR
```

### ada83.c:46564

```
// instead of truncating.
```

### ada83.c:46565

```
// i64 product of the per-dimension lengths
```

### ada83.c:46569

```
// A null dimension (high < low) contributes length 0, not a wrapped count.
```

### ada83.c:46578

```
// RM 11.1: probe the actual remaining stack for this runtime-sized array.
```

### ada83.c:46580

```
// Independent structural guard: the trunc-to-index below must never wrap an
```

### ada83.c:46581

```
// over-2**31 byte count into a small — or zero — size. On a machine whose
```

### ada83.c:46582

```
// stack limit exceeds 2 GiB the probe alone would pass such a count through.
```

### ada83.c:46588

```
// RM 3.2.1(20): if any leaf is an access type it must default to NULL.
```

### ada83.c:46589

```
// The alloca gives us uninitialized stack — memset so the NULLs (and any
```

### ada83.c:46590

```
// records-of-access transitively) are observable at declaration.
```

### ada83.c:46597

```
// RM 3.6.1: an array object whose element type is a record carrying
```

### ada83.c:46598

```
// discriminant or component defaults default-initializes EACH element,
```

### ada83.c:46599

```
// exactly as a standalone record object does (Emit_Init_Record_Defaults).
```

### ada83.c:46600

```
// The alloca above is raw storage; loop over the element count applying the
```

### ada83.c:46601

```
// record defaults at each element's address so e.g. a mutable record's
```

### ada83.c:46602

```
// discriminant reads its default rather than uninitialized memory, and a
```

### ada83.c:46603

```
// nested record component's discriminant is bound from the enclosing one
```

### ada83.c:46604

```
// (cc3121a: REC2's B : REC1 (D) needs B.D set from D, which the shallow
```

### ada83.c:46605

```
// per-component pass does not reach).
```

### ada83.c:46642

```
// Clear cached bound temps (and a record's pre-evaluated disc constraints) on a
```

### ada83.c:46643

```
// type and its array index bounds, so a subsequent name in a multi-name decl
```

### ada83.c:46644

```
// re-evaluates its subtype from scratch (RM 3.2.1).
```

### ada83.c:46645

```
// Between the names of a multi-name object declaration (RM 3.2.1(11)) the
```

### ada83.c:46646

```
// per-type caches must be dropped so the next name evaluates its subtype
```

### ada83.c:46647

```
// indication and constraints afresh. A cached scalar bound, array-index bound,
```

### ada83.c:46648

```
// discriminant-preeval alloca, or array-element-type constraint from the
```

### ada83.c:46649

```
// previous name would otherwise silently reappear on the next name. Recurse
```

### ada83.c:46650

```
// through the type's structural children — element type of an array, designated
```

### ada83.c:46651

```
// type of an access — so a shared discriminated record hidden a level down
```

### ada83.c:46652

```
// (e.g. `A, B : LINK (F (1))` where LINK is `access CELL`) is dropped too.
```

### ada83.c:46670

```
// Discriminant value for slot `di`: a pre-cached aggregate value when present,
```

### ada83.c:46671

```
// otherwise the type's own constraint value (a zero result falls back to a
```

### ada83.c:46672

```
// literal 0 of the discriminant's representation).
```

### ada83.c:46681

```
// Drain the deferred library-scope elaboration actions (see
```

### ada83.c:46682

```
// pending_library_elaborations), in declaration order (RM 7.2): evaluate and
```

### ada83.c:46683

```
// store each dynamic object initializer, and re-enter Generate_Declaration
```

### ada83.c:46684

```
// for each deferred declaration so its elaboration code (subtype checks,
```

### ada83.c:46685

```
// address overlays) lands inside the elaboration function now open. Called
```

### ada83.c:46686

```
// from the package-body / spec-only elaboration function emitters.
```

### ada83.c:46687

```
// Queue one library-scope elaboration action for the owning unit's ___elab
```

### ada83.c:46688

```
// function. Order is preserved: actions drain in the order queued, which is
```

### ada83.c:46689

```
// declaration order (RM 7.2).
```

### ada83.c:46719

```
// Float value into a fixed-point object: scale to the stored mantissa.
```

### ada83.c:46736

```
// RM 3.6.1: elaborating a constrained array subtype indication checks that
```

### ada83.c:46737

```
// each index range is compatible with the corresponding index subtype. A
```

### ada83.c:46738

```
// NON-NULL range whose bounds fall outside the index subtype raises
```

### ada83.c:46739

```
// CONSTRAINT_ERROR; a null range (low > high) is always compatible, even with
```

### ada83.c:46740

```
// out-of-subtype bounds. E.g. STRING (INTEGER'FIRST .. INTEGER'FIRST + 3) is
```

### ada83.c:46741

```
// illegal (POSITIVE'FIRST = 1) while STRING (-1 .. INTEGER'FIRST) is a legal
```

### ada83.c:46742

```
// null string. Only static index-subtype bounds are checkable here; a
```

### ada83.c:46743

```
// non-discrete or open index subtype imposes nothing.
```

### ada83.c:46750

```
// For the index-subtype comparison, prefer this array's own base index
```

### ada83.c:46751

```
// type (from A's unconstrained declaration) over whatever the constraint
```

### ada83.c:46752

```
// expression left in `ix->index_type`. A constraint like
```

### ada83.c:46753

```
// `V : A (A_9_11'RANGE)` leaks A_9_11's index type into `ix->index_type`
```

### ada83.c:46754

```
// — either boundless or plain INTEGER (RM 3.6.1(2)) — and comparing
```

### ada83.c:46755

```
// against THAT instead of A's index subtype silently skips the check.
```

### ada83.c:46768

```
// A full INTEGER index subtype can never be violated by an i32 bound.
```

### ada83.c:46785

```
// RM 3.7.2: elaborating a constrained discriminated record subtype checks each
```

### ada83.c:46786

```
// discriminant-dependent component's index and discriminant constraints against
```

### ada83.c:46787

```
// the component's own subtype — a component ARR (0 .. D) whose index subtype
```

### ada83.c:46788

```
// starts at 1, or REC (D, F) whose discriminant subtype excludes F, raises
```

### ada83.c:46789

```
// CONSTRAINT_ERROR when the enclosing subtype is elaborated. The check itself
```

### ada83.c:46790

```
// lives in Emit_Nested_Disc_Checks, which sources the discriminant values from
```

### ada83.c:46791

```
// this subtype's own constraint (no object needed); this wrapper just gates it
```

### ada83.c:46792

```
// to the constrained case.
```

### ada83.c:46800

```
// RM 3.7.2: for a component whose discriminant constraint depends on a
```

### ada83.c:46801

```
// discriminant of the enclosing record (C1 : REC (D3, F1), F1 a function), the
```

### ada83.c:46802

```
// constraint expressions that do NOT depend on the discriminant are evaluated
```

### ada83.c:46803

```
// once at the enclosing type's elaboration and their compatibility is checked
```

### ada83.c:46804

```
// no later than the end of the declaration that completely declares the type —
```

### ada83.c:46805

```
// even for an access made through an incomplete-type constraint, where no
```

### ada83.c:46806

```
// object of the type triggers the check first (c37214a section B7). The
```

### ada83.c:46807

```
// discriminant-dependent parts still wait for the object. A mutable record
```

### ada83.c:46808

```
// (every discriminant defaulted) defers the whole check to object use instead
```

### ada83.c:46809

```
// (RM 3.7.2 case B, c37213b), so this fires only for an indefinite type — one
```

### ada83.c:46810

```
// where at least one discriminant lacks a default. Only fixed-part components
```

### ada83.c:46811

```
// are checked; a variant component's presence is not settled until the
```

### ada83.c:46812

```
// governing discriminant is bound at object creation.
```

### ada83.c:46813

```
// RM 3.7.1/3.7.2: check each frozen (evaluate-once) discriminant-constraint
```

### ada83.c:46814

```
// value recorded on `constrained_type` against its discriminant's subtype.
```

### ada83.c:46815

```
// The preeval table must exist; empty slots are skipped.
```

### ada83.c:46829

```
// RM 3.7.2: check the discriminant constraint of the record component subtype
```

### ada83.c:46830

```
// `constrained_type`, whose enclosing record `parent_record` sits at byte
```

### ada83.c:46831

```
// `prefix_offset` within `sym`'s storage.
```

### ada83.c:46832

```
//   - A constraint expression naming one of the parent's discriminants is
```

### ada83.c:46833

```
//     checked against the parent's stored discriminant value (the
```

### ada83.c:46834

```
//     discriminant has no standalone alloca).
```

### ada83.c:46835

```
//   - A frozen (evaluate-once, RM 3.7.1) constraint is checked via its
```

### ada83.c:46836

```
//     preeval global.
```

### ada83.c:46837

```
//   - A bare static constraint value is checked only when
```

### ada83.c:46838

```
//     `check_static_values`: components one nesting level deeper check it
```

### ada83.c:46839

```
//     (c37213k-style, 0 outside the discriminant's subtype), while direct
```

### ada83.c:46840

```
//     components leave it to the subtype's own elaboration.
```

### ada83.c:46865

```
// A nested discriminant given by a non-discriminant expression took its
```

### ada83.c:46866

```
// value once, at type elaboration (RM 3.7.1); that frozen value is checked
```

### ada83.c:46867

```
// against the nested discriminant's subtype.
```

### ada83.c:46872

```
// A static constraint value that neither depends on a discriminant nor was
```

### ada83.c:46873

```
// frozen (REC (D3, 0), the 0) is also checked against the nested
```

### ada83.c:46874

```
// discriminant's subtype - the disc-dependent expr and preeval passes above
```

### ada83.c:46875

```
// skip it because it has no expr and no frozen global.
```

### ada83.c:46914

```
// RM 3.8.1 / 3.7.2: a discriminant constraint imposed on an incomplete type
```

### ada83.c:46915

```
// (ACCESS CONS (11), CONS then completed later) has its discriminant-dependent
```

### ada83.c:46916

```
// component constraints checked no later than the full declaration that
```

### ada83.c:46917

```
// completes the type. Each such constrained view was registered on the
```

### ada83.c:46918

```
// incomplete base; now that `completed` carries the full layout, check every
```

### ada83.c:46919

```
// view's constraint against it. The view itself only holds the discriminant
```

### ada83.c:46920

```
// slice copied while the base was incomplete, so the check runs on a probe of
```

### ada83.c:46921

```
// the completed layout wearing the view's pinned discriminant constraint.
```

### ada83.c:46932

```
// RM 3.7.2: each pinned discriminant value must itself lie in the
```

### ada83.c:46933

```
// discriminant's subtype (ACC_INC (6) with A : SMALLER 1..5 raises here,
```

### ada83.c:46934

```
// deferred from the access declaration where the type was incomplete —
```

### ada83.c:46935

```
// c38107b).
```

### ada83.c:46948

```
// RM 3.7.2: every discriminant value pinned by a constrained access-to-record
```

### ada83.c:46949

```
// subtype's discriminant constraint (e.g. ACC(D => IDENT_INT(-1))) must fall
```

### ada83.c:46950

```
// within its discriminant's own subtype at the subtype/type elaboration point,
```

### ada83.c:46951

```
// before any object of the subtype exists. Traverses through access-to-access
```

### ada83.c:46952

```
// so ACCESS ACC(-1) checks at ACCA's elaboration; the caller decides whether
```

### ada83.c:46953

```
// to walk into array/record components (c37211d/e).
```

### ada83.c:46959

```
// Walk through access-to-access chains, but stop on a cycle — mutually
```

### ada83.c:46960

```
// recursive access types (X6 IS ACCESS X8; X8 IS ACCESS X6;, c38102a)
```

### ada83.c:46961

```
// must not send the compiler into an infinite descent.
```

### ada83.c:46987

```
// True when every bound of a discriminant-dependent array component is either a
```

### ada83.c:46988

```
// static integer or a direct discriminant reference — the shapes whose extent
```

### ada83.c:46989

```
// this file can fold from runtime discriminant values (STRING (1 .. D), and the
```

### ada83.c:46990

```
// like). A more general bound expression (D + 1, an enclosing variable) is
```

### ada83.c:46991

```
// declined so the record keeps its existing sizing rather than a wrong one.
```

### ada83.c:47017

```
// Emit an i64 temp giving the runtime byte size a discriminant-dependent array
```

### ada83.c:47018

```
// component occupies, given the enclosing subtype's discriminant values in
```

### ada83.c:47019

```
// disc_val[] (i64 temps). Assumes Disc_Component_Runtime_Sizable(comp) held.
```

### ada83.c:47049

```
// Runtime byte size of a nested record subtype `ct` whose size depends on its
```

### ada83.c:47050

```
// discriminants, given the enclosing record `rec` and its discriminant values
```

### ada83.c:47051

```
// `disc_val[]`. The nested record's own discriminants come from its constraint: a
```

### ada83.c:47052

```
// constraint naming an enclosing discriminant reads the matching disc_val; any
```

### ada83.c:47053

```
// other constraint expression is evaluated (a static value is materialized). The
```

### ada83.c:47054

```
// nested layout is then summed by the same on-the-fly accumulator — the recursion.
```

### ada83.c:47079

```
// The runtime byte footprint of component `ci` of `rec`, given the enclosing
```

### ada83.c:47080

```
// record's discriminant values in `disc_val[]` (i64 temps). The runtime twin of
```

### ada83.c:47081

```
// Component_Footprint_Static and the single per-component size used by every
```

### ada83.c:47082

```
// runtime layout path (descriptor, instance, aggregate): a capless dependent
```

### ada83.c:47083

```
// array folds from the discriminants; a nested dynamically-sized record recurses
```

### ada83.c:47084

```
// with its discriminants substituted; an array of such records is element count
```

### ada83.c:47085

```
// times the element's runtime size; a capped dependent array is reserved at its
```

### ada83.c:47086

```
// index-subtype maximum; everything else is its fixed size.
```

### ada83.c:47098

```
// Element count: fold the array bounds with a unit-size element so
```

### ada83.c:47099

```
// Emit_Runtime_Disc_Component_Size returns the count, then scale by esz.
```

### ada83.c:47112

```
// capped dependent → reserved at max
```

### ada83.c:47116

```
// The runtime byte offset of component `upto` from the record base — the sum of
```

### ada83.c:47117

```
// the footprints of components 0 .. upto-1 given `disc_val[]`. Passing
```

### ada83.c:47118

```
// `component_count` yields the record's total byte size. This is the GNAT-style
```

### ada83.c:47119

```
// on-the-fly layout: offsets are recomputed by summation rather than stored,
```

### ada83.c:47120

```
// so a component after a capless one gets a correct runtime offset regardless of
```

### ada83.c:47121

```
// how many precede it or where they sit.
```

### ada83.c:47133

```
// Load each of `rec`'s discriminants from the instance at `base` into
```

### ada83.c:47134

```
// disc_val[di] (i64 temps). Discriminants are the leading components at static
```

### ada83.c:47135

```
// offsets — before any capless component — so their offsets are always valid.
```

### ada83.c:47148

```
// A constrained discriminated subtype whose discriminant constraint is NON-static
```

### ada83.c:47149

```
// (RECTYPE (IDENT_INT (9))) is sized only at elaboration, when the constraint is
```

### ada83.c:47150

```
// evaluated. Give it a runtime record descriptor and compute @__rt_rec_N_size and
```

### ada83.c:47151

```
// a @__rt_rec_N_offK for EVERY component by folding the just-evaluated
```

### ada83.c:47152

```
// discriminant values into each component's footprint, then mark it dynamic
```

### ada83.c:47153

```
// (size 0). Handles any number of discriminant-dependent components at any
```

### ada83.c:47154

```
// position — the offset of a component after a capless one is a runtime value.
```

### ada83.c:47155

```
// Object allocation, component access, aggregates, and whole-record assignment
```

### ada83.c:47156

```
// route dynamic-size records through this descriptor, so this single elaboration
```

### ada83.c:47157

```
// step makes them all correct. No-op outside a function, if already elaborated,
```

### ada83.c:47158

```
// if the constraint is fully static (the static fold owns that), if there is a
```

### ada83.c:47159

```
// variant part, or if any component's footprint is not foldable.
```

### ada83.c:47170

```
// Fire for any record that needs runtime layout and is NOT fully static — a
```

### ada83.c:47171

```
// non-static discriminant, OR a component whose size is only known at run time
```

### ada83.c:47172

```
// (a nested record or array element constrained by an opaque value). A fully
```

### ada83.c:47173

```
// static subtype is owned by Fold_Static_Discriminant_Record_Layout.
```

### ada83.c:47202

```
// now runtime-sized: consumers read @__rt_rec_N_size
```

### ada83.c:47205

```
// Whether component type `ct`'s byte footprint depends on discriminant values: a
```

### ada83.c:47206

```
// capless dependent array, a nested dynamically-sized record, or an array whose
```

### ada83.c:47207

```
// element is such a record. These are the shapes that shift every later
```

### ada83.c:47208

```
// component to a runtime offset (RM 3.7.1).
```

### ada83.c:47216

```
// Whether `rec` has at least one component whose footprint depends on
```

### ada83.c:47217

```
// discriminant values and every component's footprint is foldable — the records
```

### ada83.c:47218

```
// for which offsets and size are computed on the fly (descriptor, instance,
```

### ada83.c:47219

```
// aggregate) rather than from static byte offsets. Records that fail are left
```

### ada83.c:47220

```
// with their base handling.
```

### ada83.c:47231

```
// Whether `rec` is a constrained subtype whose discriminants are all STATIC and
```

### ada83.c:47232

```
// whose every component footprint folds to a compile-time constant — the records
```

### ada83.c:47233

```
// the static fold owns (definite size, static offsets). A record that needs
```

### ada83.c:47234

```
// runtime layout but is NOT fully static (a non-static discriminant, or a nested
```

### ada83.c:47235

```
// component whose size is only known at run time, e.g. an IDENT_INT-constrained
```

### ada83.c:47236

```
// element) is instead given a runtime descriptor.
```

### ada83.c:47250

```
// Index of the first component whose offset is NOT statically known — the first
```

### ada83.c:47251

```
// component with a dynamic footprint. Every component before it sits at a static
```

### ada83.c:47252

```
// byte_offset; every component from it onward has a runtime offset that must be
```

### ada83.c:47253

```
// summed on the fly. Returns -1 when the record has a fully static layout.
```

### ada83.c:47262

```
// Byte size of a discriminated record INSTANCE stored at obj_addr whose layout
```

### ada83.c:47263

```
// depends on discriminants held in the instance itself (an unconstrained view: a
```

### ada83.c:47264

```
// mutable record or an OUT/IN OUT formal of the base type). Loads the governing
```

### ada83.c:47265

```
// discriminants from the object and sums every component's footprint. Returns an
```

### ada83.c:47266

```
// i64 temp, or 0 when the record does not need runtime layout.
```

### ada83.c:47275

```
// Byte size of a record aggregate of an unconstrained discriminated type whose
```

### ada83.c:47276

```
// layout is governed by discriminants SUPPLIED BY THE AGGREGATE (an aggregate
```

### ada83.c:47277

```
// names all discriminants, RM 4.3.1). Evaluates each discriminant association and
```

### ada83.c:47278

```
// sums every component's footprint. Returns an i64 temp, or 0 when the
```

### ada83.c:47279

```
// aggregate's type does not need runtime layout.
```

### ada83.c:47287

```
// Locate the aggregate association supplying discriminant di: a named choice
```

### ada83.c:47288

```
// matching the discriminant, else the di-th positional element.
```

### ada83.c:47319

```
// An object initializer that is an aggregate, seen through an optional
```

### ada83.c:47320

```
// qualification: `X : T := T'(...)` and `X : T := (...)` denote the same
```

### ada83.c:47321

```
// initializing aggregate. Returns the aggregate node, or NULL when the
```

### ada83.c:47322

```
// initializer is not (a qualification of) an aggregate. The qualification is a
```

### ada83.c:47323

```
// type assertion only — for initialization the aggregate still slides to the
```

### ada83.c:47324

```
// object's constraint (RM 5.2.1) — so callers treat both forms identically.
```

### ada83.c:47334

```
// Elaborate the task(s) a freshly declared object contains (RM 9.3). The
```

### ada83.c:47335

```
// TCB(s) are created now, into the object's OWN storage — a frame slot, a
```

### ada83.c:47336

```
// local alloca, or an instance-level global — all reached through the
```

### ada83.c:47337

```
// slot-aware Emit_Symbol_Addr, so this is correct wherever the object
```

### ada83.c:47338

```
// lives. A task declared inside a subprogram or a (possibly instantiated)
```

### ada83.c:47339

```
// package body defers its thread start to the enclosing master's `begin`
```

### ada83.c:47340

```
// via the pending-activation list; a library/package-level object with no
```

### ada83.c:47341

```
// enclosing function activates and waits for its tasks immediately, raising
```

### ada83.c:47342

```
// TASKING_ERROR on activation failure. A plain task object and a composite
```

### ada83.c:47343

```
// with task components are handled by the one recursive walk.
```

### ada83.c:47357

```
// Package elaboration: activate immediately and wait, like
```

### ada83.c:47358

```
// __ada_task_start does for a single package-level task.
```

### ada83.c:47369

```
// RM 7.4: the visible half of a deferred constant elaborates to nothing —
```

### ada83.c:47370

```
// the full declaration in the private part owns storage and initialization,
```

### ada83.c:47371

```
// including any default discriminant expressions (which must not be
```

### ada83.c:47372

```
// evaluated at the visible declaration, c48008a). Resolution redirected
```

### ada83.c:47373

```
// sym->declaration to the completion, so the superseded node identifies
```

### ada83.c:47374

```
// itself by no longer being its own symbol's declaration.
```

### ada83.c:47380

```
// A tree-copy package instance's package-level objects are per-instance
```

### ada83.c:47381

```
// GLOBALS even when the instantiation is nested inside a subprogram (storage
```

### ada83.c:47382

```
// class must agree with Symbol_Is_Global, which classifies them global for
```

### ada83.c:47383

```
// every reference path). Only the STORAGE differs from an ordinary object
```

### ada83.c:47384

```
// declaration: definitions go to module level through the accumulator, and
```

### ada83.c:47385

```
// every address names the global. The elaboration-point semantics — task
```

### ada83.c:47386

```
// creation (RM 9.3), discriminant constraints (RM 3.7.2, c38004b),
```

### ada83.c:47387

```
// component defaults (RM 3.2.1/3.9, c39006a), initialization — mirror the
```

### ada83.c:47388

```
// general path through the same shared emitters; anything storage-
```

### ada83.c:47389

```
// independent (the RM 4.8 allocator propagation, the RM 7.4 deferred-
```

### ada83.c:47390

```
// constant skip) is handled once in Generate_Object_Declaration before the
```

### ada83.c:47391

```
// paths fork, so the two can not drift apart.
```

### ada83.c:47418

```
// Initialized: composites copy into the global, scalars store.
```

### ada83.c:47455

```
// RM 8.5: an object renaming captures the ADDRESS of its target at the
```

### ada83.c:47456

```
// declaration's elaboration; subsequent uses of the rename read/write
```

### ada83.c:47457

```
// through that captured address. Emit `slot = alloca ptr; store <target
```

### ada83.c:47458

```
// addr>, slot` here; Generate_Composite_Address and Generate_Identifier
```

### ada83.c:47459

```
// consult sym->rename_address_slot on later reads. Only in a function
```

### ada83.c:47460

```
// context — a package-level rename's slot is a global with its own
```

### ada83.c:47461

```
// elaboration path, handled elsewhere.
```

### ada83.c:47472

```
// The denotation of an unconstrained (or dynamic-bounds) array is a fat
```

### ada83.c:47473

```
// pointer { data, bounds }, and every later read through the slot loads
```

### ada83.c:47474

```
// one from the captured address. Plain Generate_Lvalue would hand back
```

### ada83.c:47475

```
// the bare data pointer (a fat access's .ALL, a slice, …), and loading a
```

### ada83.c:47476

```
// fat from array data reads element bytes as a descriptor — garbage
```

### ada83.c:47477

```
// bounds, then a crash. Snapshot the descriptor into its own slot and
```

### ada83.c:47478

```
// capture that address: RM 8.5 fixes the object now, so a later change to
```

### ada83.c:47479

```
// the underlying access must not disturb the rename either.
```

### ada83.c:47494

```
// No storage of its own — the rename denotes the captured object.
```

### ada83.c:47497

```
// An object with an address clause (RM 13.5) owns no storage of its own:
```

### ada83.c:47498

```
// it is an overlay reached through its hidden address cell. The clause's
```

### ada83.c:47499

```
// elaboration point (codegen NK_REPRESENTATION_CLAUSE) emits the cell and
```

### ada83.c:47500

```
// performs the declared initialization through the overlay.
```

### ada83.c:47512

```
// The RM 3.6.1 index-subtype-compatibility check and the RM 3.7.2
```

### ada83.c:47513

```
// discriminant-dependent component check used to live here — once per
```

### ada83.c:47514

```
// declaration, right before the per-name loop. That evaluated the subtype's
```

### ada83.c:47515

```
// bound expressions before CACHE_BOUND_EXPR had populated their caches, so a
```

### ada83.c:47516

```
// side-effectful bound like `F (I)` ran once for the check and again for
```

### ada83.c:47517

```
// each name's own elaboration. RM 3.2.1(11) says every name in
```

### ada83.c:47518

```
// `A, B : T := E` gets its OWN subtype and initializer evaluation; two runs
```

### ada83.c:47519

```
// per name broke that. The checks are now emitted inside the per-name loop
```

### ada83.c:47520

```
// (below), immediately after CACHE_BOUND_EXPR fills the caches, so
```

### ada83.c:47521

```
// Emit_Single_Bound reuses those temps and the bound expression runs exactly
```

### ada83.c:47522

```
// once per name.
```

### ada83.c:47524

```
// RM 4.8 / 3.10: an initializing allocator builds its value at the
```

### ada83.c:47525

```
// declared access subtype's SHAPE (fat vs thin), checks the designated
```

### ada83.c:47526

```
// constraint, and attaches any allocated task to the master that
```

### ada83.c:47527

```
// elaborated the access type's declaration (RM 9.4, c94011a). Storage
```

### ada83.c:47528

```
// class plays no part — propagate once, ahead of the fork below, so no
```

### ada83.c:47529

```
// storage path can miss it. (Multi-name declarations share one declared
```

### ada83.c:47530

```
// type, so the first name's suffices.)
```

### ada83.c:47541

```
// Instance-global storage forks off here; semantics shared with the
```

### ada83.c:47542

```
// general path live in the emitters both call (and in the hoisted code
```

### ada83.c:47543

```
// above), never in copies.
```

### ada83.c:47555

```
// Elaborate one BOUND_EXPR bound into its cached temp/rep, exactly once
```

### ada83.c:47556

```
// (RM 3.3.1 / RM 3.6(5)). Dynamic subtype bounds are side-effectful
```

### ada83.c:47557

```
// expressions, so they must be evaluated and cached, never re-run. The
```

### ada83.c:47558

```
// argument is a Type_Bound lvalue (e.g. ty->low_bound).
```

### ada83.c:47567

```
// Emit source location for the object declaration
```

### ada83.c:47574

```
// RM 7.4
```

### ada83.c:47576

```
// RM 3.2.1: For multi-name declarations (S1, S2 : T := ...),                                   
```

### ada83.c:47577

```
// each name gets independent evaluation of the subtype indication                              
```

### ada83.c:47578

```
// and init expression. Clear cached temps before each subsequent                              
```

### ada83.c:47579

```
// name so bounds are re-evaluated from scratch.                                               
```

### ada83.c:47580

```
//                                                                                              
```

### ada83.c:47591

```
// Emit declaration info comment
```

### ada83.c:47598

```
// RM 3.7.1 / 3.3.1: a subtype constrained by a non-static discriminant
```

### ada83.c:47599

```
// (RECTYPE (IDENT_INT (9))) is sized when this declaration elaborates its
```

### ada83.c:47600

```
// subtype indication — fold the discriminant into the record's dependent
```

### ada83.c:47601

```
// components now so the allocation and every later use see the real size.
```

### ada83.c:47607

```
// Named numbers (constants without explicit type) don't need storage.                         
```

### ada83.c:47608

```
// They are compile-time values that get inlined when referenced.                              
```

### ada83.c:47609

```
// Per RM 3.2.2: Named numbers are not objects and have no storage.                            
```

### ada83.c:47610

```
//                                                                                              
```

### ada83.c:47611

```
// named numbers have no storage
```

### ada83.c:47614

```
// Check if this is an array type (constrained or unconstrained).                              
```

### ada83.c:47615

```
// is_any_array: true for any array-like type INCLUDING TYPE_STRING,                            
```

### ada83.c:47616

```
// used for aggregate/string initialization.                                                   
```

### ada83.c:47617

```
// is_constrained_array: true only for constrained, used for allocation                         
```

### ada83.c:47618

```
//                                                                                              
```

### ada83.c:47629

```
// Check if element is record or another constrained array
```

### ada83.c:47634

```
// Element with zero size means dynamic bounds - the outer
```

### ada83.c:47635

```
// array must use fat-pointer storage so runtime code can
```

### ada83.c:47636

```
// compute element size and lay out storage properly.
```

### ada83.c:47637

```
//
```

### ada83.c:47647

```
// Check if this is a record type
```

### ada83.c:47651

```
// Package-level variables are globals, local variables use alloca
```

### ada83.c:47652

```
// Skip if global was already emitted (e.g., from spec + body)
```

### ada83.c:47657

```
// Global variable at package level
```

### ada83.c:47660

```
// Constant with initializer - emit as constant
```

### ada83.c:47669

```
// String constant - emit fat pointer global
```

### ada83.c:47675

```
// Emit string data first: @SYMNAME.data = ...
```

### ada83.c:47679

```
// Emit escaped string contents
```

### ada83.c:47690

```
// Emit bounds global: @SYMNAME.bounds = { bt 1, bt N } where bt is
```

### ada83.c:47691

```
// the array's index bound rep (i32 for STRING), matching the
```

### ada83.c:47692

```
// Bounds_Type_For convention every fat-pointer consumer reads with.
```

### ada83.c:47700

```
// Emit fat pointer global: { ptr @X.data, ptr @X.bounds }
```

### ada83.c:47712

```
// Mark as emitted so extern declarations are suppressed
```

### ada83.c:47715

```
// Variable - emit as global, using static initializer when available.                       
```

### ada83.c:47716

```
// Per Ada RM 3.2.1: object declarations with static init expressions                         
```

### ada83.c:47717

```
// can be folded into the global initializer directly.                                       
```

### ada83.c:47718

```
//                                                                                            
```

### ada83.c:47729

```
// A fixed-point object stores the scaled mantissa, not the real.
```

### ada83.c:47740

```
// A non-static initializer cannot be folded into the global; queue it
```

### ada83.c:47741

```
// for the package body's elaboration function (RM 7.2). Composite
```

### ada83.c:47742

```
// shapes (arrays, records, fat pointers) keep their existing paths.
```

### ada83.c:47748

```
// Linkage: a unit compiled as the FILE'S OWN content emits the
```

### ada83.c:47749

```
// authoritative definition (strong); the same spec INLINED into a
```

### ada83.c:47750

```
// consumer (loaded unit) emits a discardable copy that merges with
```

### ada83.c:47751

```
// the authoritative one at link time. A strong define from a
```

### ada83.c:47752

```
// consumer would collide; a linkonce define from the owner gets
```

### ada83.c:47753

```
// DROPPED by llvm-link when nothing in its own module uses it.
```

### ada83.c:47793

```
// RM 7.4: Deferred constant and its completion share one symbol after                          
```

### ada83.c:47794

```
// Scope_Insert merge. The deferred decl (no init) emits storage first;                        
```

### ada83.c:47795

```
// the completion (has init) must reuse that storage, not re-emit it.                          
```

### ada83.c:47796

```
//                                                                                              
```

### ada83.c:47800

```
// Local variable allocation                                                                    
```

### ada83.c:47801

```
// Skip symbols that aren't actually in the current function's scope.                          
```

### ada83.c:47802

```
// This happens with package spec private part declarations: the AST                            
```

### ada83.c:47803

```
// nodes have orphaned symbol references (parent==NULL, frame_offset==0)                        
```

### ada83.c:47804

```
// while the proper symbols are already allocated in the frame under                            
```

### ada83.c:47805

```
// their package-qualified names. Emitting these would create duplicate                         
```

### ada83.c:47806

```
// or conflicting IR definitions.                                                              
```

### ada83.c:47807

```
//                                                                                              
```

### ada83.c:47826

```
// A dynamically-sized record's frame slot holds a pointer to storage
```

### ada83.c:47827

```
// allocated at runtime (its size is not known at compile time). Allocate
```

### ada83.c:47828

```
// the storage, publish the pointer in the frame slot for uplevel access,
```

### ada83.c:47829

```
// and let the local name denote the storage directly.
```

### ada83.c:47832

```
// RM 11.1 stack extent probe
```

### ada83.c:47846

```
// RM 11.1: this object's frame slot was capped to a placeholder because
```

### ada83.c:47847

```
// it cannot fit on the stack (Symbol_Assign_Frame_Slot); raise
```

### ada83.c:47848

```
// STORAGE_ERROR at its elaboration, reaching the enclosing scope.
```

### ada83.c:47853

```
// RM 3.2.1(20): access components default to NULL. A frame-allocated
```

### ada83.c:47854

```
// composite whose leaves include access types must start zeroed so
```

### ada83.c:47855

```
// those NULLs are observable — Emit_Init_Record_Defaults only stores
```

### ada83.c:47856

```
// components with explicit defaults, so an ACCESS-typed component
```

### ada83.c:47857

```
// without one would otherwise read as frame garbage.
```

### ada83.c:47867

```
// RM 3.7: a frame-allocated array whose element is a record with
```

### ada83.c:47868

```
// component or discriminant defaults still needs each element
```

### ada83.c:47869

```
// initialized — the alloca path below does this, so mirror it here.
```

### ada83.c:47870

```
// A dynamic-bounds array does NOT live inline in the frame: its slot is
```

### ada83.c:47871

```
// a fat pointer and its storage is bound later by the dynamic-bounds
```

### ada83.c:47872

```
// branch (Emit_Bind_Bounded_Array_Storage), which initializes each
```

### ada83.c:47873

```
// element itself. Running the inline init here would memset a statically
```

### ada83.c:47874

```
// guessed element count over the 16-byte fat-pointer slot and smash the
```

### ada83.c:47875

```
// frame (cc3121a: an ARRAY2 (IDENT_INT(6) .. 8) of a defaulted record).
```

### ada83.c:47893

```
// Constrained outer array whose element type is a dynamic-bound                                
```

### ada83.c:47894

```
// array/string: each element is stored as a fat pointer pair.                                 
```

### ada83.c:47895

```
// Allocate [N x { ptr, ptr }] so each slot holds one fat ptr.                                 
```

### ada83.c:47896

```
//                                                                                              
```

### ada83.c:47904

```
// Dynamic-bound arrays - or arrays whose element type has                                    
```

### ada83.c:47905

```
// dynamic size - need fat pointer storage ({ ptr, ptr }).                                   
```

### ada83.c:47906

```
// Runtime code computes actual sizes and allocates data.                                    
```

### ada83.c:47907

```
//                                                                                            
```

### ada83.c:47911

```
// Constrained array with static bounds: allocate [N x element_type]
```

### ada83.c:47914

```
// RM 11.1: no stack can hold this array. Emit a placeholder slot so the
```

### ada83.c:47915

```
// now-unreachable remainder still references valid storage, then raise
```

### ada83.c:47916

```
// STORAGE_ERROR at elaboration (propagates to the enclosing scope).
```

### ada83.c:47927

```
// Element is record or constrained array - use byte array
```

### ada83.c:47934

```
// RM 3.3.1/3.7: an array of records with component defaults (directly or
```

### ada83.c:47935

```
// nested) must initialize every element's defaults. Zero-init first so
```

### ada83.c:47936

```
// non-defaulted fields are predictable, then apply defaults per element.
```

### ada83.c:47937

```
// RM 3.2.1(20): also zero-init when a leaf is an access type so those
```

### ada83.c:47938

```
// slots default to NULL — the same memset covers both.
```

### ada83.c:47967

```
// Record with dynamic-sized components: load runtime size
```

### ada83.c:47970

```
// RM 11.1 stack extent probe
```

### ada83.c:47974

```
// Zero-init so memcmp-based equality sees clean padding
```

### ada83.c:47979

```
// Record type: allocate [N x i8] for the record size
```

### ada83.c:47982

```
// RM 11.1: too large for the stack — placeholder slot + raise.
```

### ada83.c:47991

```
// Zero-init all record types so that padding bytes are clean
```

### ada83.c:47992

```
// (for memcmp equality) and nested record component defaults
```

### ada83.c:47993

```
// start from a known state (RM 3.3.1, 4.5.2).
```

### ada83.c:47994

```
//
```

### ada83.c:47999

```
// Component defaults (RM 3.7) are applied once, by the
```

### ada83.c:48000

```
// obj_decl_init record path below (Emit_Apply_Component_Defaults);
```

### ada83.c:48001

```
// applying them here as well ran side-effectful defaults twice.
```

### ada83.c:48008

```
// RM 11.1: no stack can hold this object. Emit a placeholder slot so the
```

### ada83.c:48009

```
// now-unreachable remainder of the block still references valid storage,
```

### ada83.c:48010

```
// then raise STORAGE_ERROR at elaboration.
```

### ada83.c:48017

```
// For unconstrained array/string variables with explicit index
```

### ada83.c:48019

```
// constraints (e.g. S : STRING (1..N)), allocate data storage                                
```

### ada83.c:48020

```
// and initialize the fat pointer so it's valid before any use.                              
```

### ada83.c:48021

```
// Without this, the fat pointer is { null, null } and any                                    
```

### ada83.c:48022

```
// access to bounds causes a crash. (RM 3.6.1)                                                
```

### ada83.c:48023

```
//                                                                                            
```

### ada83.c:48034

```
// support up to 8 dimensions
```

### ada83.c:48040

```
// Coerce to bt from each bound's produced rep so the length
```

### ada83.c:48041

```
// arithmetic and bounds stores below see consistent widths.
```

### ada83.c:48052

```
// Compute total element count = product of all dimension lengths
```

### ada83.c:48066

```
// Multiply by element size
```

### ada83.c:48072

```
// Allocate data storage
```

### ada83.c:48075

```
// Build fat pointer with multi-dim bounds
```

### ada83.c:48088

```
// Create the object's task(s) and register/start their activation
```

### ada83.c:48089

```
// (RM 9.3): plain task object or any composite with task components.
```

### ada83.c:48093

```
// RM 3.3.1(5) + RM 3.6(5): Elaborate subtype indication by                                     
```

### ada83.c:48094

```
// evaluating all dynamic constraint expressions, even for                                      
```

### ada83.c:48095

```
// uninitialized objects. For arrays, the elaboration order is:                                
```

### ada83.c:48096

```
//   1. Index discrete ranges (array bounds)                                                    
```

### ada83.c:48097

```
//   2. Component subtype indication (element type constraints)                                 
```

### ada83.c:48098

```
// This ordering is critical because expressions may have side                                  
```

### ada83.c:48099

```
// effects (function calls setting globals read by later exprs).                               
```

### ada83.c:48100

```
//                                                                                              
```

### ada83.c:48101

```
// RM 3.7.1 (evaluate-once): a discriminated record subtype's constraint
```

### ada83.c:48102

```
// expressions must be evaluated at subtype elaboration, once, with the
```

### ada83.c:48103

```
// results reused by every later reader — the aggregate that fills the
```

### ada83.c:48104

```
// record, the discriminant-match check on an assignment or allocator,
```

### ada83.c:48105

```
// an access value flowing to a constrained access subtype, and so on.
```

### ada83.c:48106

```
// The record subtype carries a lazily-populated `disc_constraint_preeval`
```

### ada83.c:48107

```
// — a module-global id per discriminant holding the evaluated value — that
```

### ada83.c:48108

```
// Emit_Disc_Constraint_Value reads back. Populating it here is the single
```

### ada83.c:48109

```
// upstream point that keeps every downstream reader honest.
```

### ada83.c:48138

```
// RM 3.10: a constrained access subtype `A (E)` has its constraint on the
```

### ada83.c:48139

```
// DESIGNATED subtype, evaluated at object elaboration and shared by every
```

### ada83.c:48140

```
// allocator, dereference, and assignment through this access type. The
```

### ada83.c:48141

```
// designated may be a discriminated record — preeval its disc constraints
```

### ada83.c:48142

```
// — or a constrained array — cache its index bounds, so
```

### ada83.c:48143

```
// Emit_Alloc_Bound_Check_Dim reuses those values instead of re-running a
```

### ada83.c:48144

```
// side-effectful bound expression like `G` in `NEW ARR (1 .. G)`.
```

### ada83.c:48153

```
// RM 3.7.2: a constrained access subtype pins the designated record's
```

### ada83.c:48154

```
// discriminants; each must belong to its discriminant subtype, checked at
```

### ada83.c:48155

```
// the object's elaboration. The constraint was just frozen above, so the
```

### ada83.c:48156

```
// check reads the once-evaluated value — a side-effecting constraint
```

### ada83.c:48157

```
// (D1 | D2 => F) is not re-run.
```

### ada83.c:48161

```
// RM 3.7: an object of a directly discriminated record subtype elaborates
```

### ada83.c:48162

```
// its constraint expressions right here.
```

### ada83.c:48165

```
// Step 1: evaluate array index bounds (discrete ranges)
```

### ada83.c:48173

```
// Step 2: evaluate element subtype constraints.                                             
```

### ada83.c:48174

```
// For record elements with disc constraints: pre-evaluate once                               
```

### ada83.c:48175

```
// so aggregate disc checks reuse cached values (RM 3.7.1).                                  
```

### ada83.c:48176

```
// For scalar elements with dynamic range: cache bounds.                                     
```

### ada83.c:48177

```
//                                                                                            
```

### ada83.c:48186

```
// RM 3.6(5): For array-of-array, cache the element type's                                  
```

### ada83.c:48187

```
// index bounds so inner aggregates reuse them instead of                                   
```

### ada83.c:48188

```
// re-evaluating side-effectful expressions per outer element.                             
```

### ada83.c:48189

```
//                                                                                          
```

### ada83.c:48198

```
// RM 3.6.1(7): with every dynamic bound now cached, this check reuses
```

### ada83.c:48199

```
// the cached temps instead of re-running the expression, so a
```

### ada83.c:48200

```
// side-effectful bound like `F (I)` is visited exactly once per name.
```

### ada83.c:48201

```
// RM 3.7.2: same story for discriminant-dependent component constraints.
```

### ada83.c:48209

```
// RM 3.5(4): a subtype indication's range constraint must lie within the
```

### ada83.c:48210

```
// type mark's range, whether or not the object has an initializer. Evaluate
```

### ada83.c:48211

```
// it before any initialization to respect evaluation order. (A no-init
```

### ada83.c:48212

```
// object — A : SUBINT RANGE -11 .. 0 — must still raise CONSTRAINT_ERROR.)
```

### ada83.c:48215

```
// Initialize if provided
```

### ada83.c:48218

```
// String/character array initialization.                                                    
```

### ada83.c:48219

```
// NK_STRING always yields a fat pointer.                                                    
```

### ada83.c:48220

```
// Unconstrained array identifiers yield fat pointer values.                                 
```

### ada83.c:48221

```
// Constrained array identifiers yield plain ptr - use memcpy.                               
```

### ada83.c:48222

```
//                                                                                            
```

### ada83.c:48223

```
// CRITICAL: When the destination is unconstrained (STRING variable),                         
```

### ada83.c:48224

```
// the alloca holds a fat pointer descriptor { ptr, { bound, bound } }.                      
```

### ada83.c:48225

```
// We must NOT memcpy data into that descriptor. Instead:                                    
```

### ada83.c:48226

```
//   1. Allocate separate local data storage (dynamic alloca)                                 
```

### ada83.c:48227

```
//   2. Copy data from source to local storage                                                
```

### ada83.c:48228

```
//   3. Build a fat pointer { local_data, { low, high } }                                     
```

### ada83.c:48229

```
//   4. Store the fat pointer into the variable                                               
```

### ada83.c:48230

```
// This is "constrained by initialization" - GNAT does the same.                             
```

### ada83.c:48231

```
//                                                                                            
```

### ada83.c:48239

```
// Source produces a fat pointer value. A "constrained" source with
```

### ada83.c:48240

```
// dynamic bounds (e.g. S3A : STRING (I3 .. I3 + 2)) is fat-represented
```

### ada83.c:48241

```
// too, and its bounds are only known at run time — take this branch so
```

### ada83.c:48242

```
// they are read from the fat pointer, not from a static Type_Bound_Value
```

### ada83.c:48243

```
// (which would yield 0).
```

### ada83.c:48249

```
// Destination needs fat pointer storage (unconstrained or dynamic bounds).              
```

### ada83.c:48250

```
// Storage is { ptr, { bound, bound } }. We need separate                                
```

### ada83.c:48251

```
// data storage on the stack, then store the fat pointer.                                
```

### ada83.c:48252

```
//                                                                                        
```

### ada83.c:48260

```
// Allocate local data storage sized by source bounds
```

### ada83.c:48264

```
// Copy source data to local storage
```

### ada83.c:48269

```
// A constrained array object keeps its own subtype bounds; the
```

### ada83.c:48270

```
// initializer slides into them (RM 3.6, 5.2.1). An unconstrained
```

### ada83.c:48271

```
// object takes the initializer's bounds.
```

### ada83.c:48278

```
// Build fat pointer pointing to local data with the object's bounds
```

### ada83.c:48281

```
// Store fat pointer into variable
```

### ada83.c:48284

```
// Destination is constrained - just copy data bytes
```

### ada83.c:48289

```
// Source is a constrained character array - usually plain ptr.
```

### ada83.c:48290

```
// But may be a fat pointer if source has dynamic bounds.
```

### ada83.c:48296

```
// Source produced a fat pointer - extract data pointer
```

### ada83.c:48304

```
// Dest is unconstrained but source is constrained.                                    
```

### ada83.c:48305

```
// Build fat pointer from source's static bounds.                                      
```

### ada83.c:48306

```
// For multidimensional arrays, use product of all extents.                            
```

### ada83.c:48307

```
//                                                                                      
```

### ada83.c:48324

```
// Allocate local data storage
```

### ada83.c:48328

```
// Copy data
```

### ada83.c:48333

```
// Build fat pointer
```

### ada83.c:48337

```
// Store fat pointer into variable
```

### ada83.c:48340

```
// Both constrained - simple memcpy using target bounds.                                 
```

### ada83.c:48341

```
// For multidimensional arrays, compute total byte count                                  
```

### ada83.c:48342

```
// as product of all dimension extents * element size.                                   
```

### ada83.c:48343

```
//                                                                                        
```

### ada83.c:48365

```
// Fixed-point initialization from real literal:                                            
```

### ada83.c:48366

```
// Convert real value to scaled integer at compile time                                     
```

### ada83.c:48367

```
// Use Big_Real for precise scaling when available                                          
```

### ada83.c:48368

```
//                                                                                          
```

### ada83.c:48374

```
// Precise scaling: scaled = significand * 10^exponent / small
```

### ada83.c:48375

```
// For best precision, compute in arbitrary precision then round
```

### ada83.c:48383

```
// Use double for final division - Big_Real provides precise numerator.
```

### ada83.c:48384

```
// Use round() for correct rounding of negative values.
```

### ada83.c:48392

```
// RM 3.3.2: Scalar constraint check on initialization
```

### ada83.c:48398

```
// RM 3.2.2(5): Evaluate discriminant constraint expressions BEFORE                           
```

### ada83.c:48399

```
// the aggregate init. The subtype indication must be evaluated first                        
```

### ada83.c:48400

```
// so that side effects (function calls that set variables read by the                        
```

### ada83.c:48401

```
// aggregate) occur in the correct order. Cache the values so they                           
```

### ada83.c:48402

```
// aren't re-evaluated during the discriminant store.                                        
```

### ada83.c:48403

```
//                                                                                            
```

### ada83.c:48419

```
// Set up cg->disc_cache so Generate_Aggregate can use cached
```

### ada83.c:48420

```
// discriminant values instead of re-evaluating expressions.
```

### ada83.c:48427

```
// Record aggregate initialization - copy from aggregate to variable
```

### ada83.c:48430

```
// Clear disc cache after aggregate generation
```

### ada83.c:48433

```
// A dynamically-sized record takes its byte count from the type's rt
```

### ada83.c:48434

```
// descriptor; the static path uses the layout-derived record_size.
```

### ada83.c:48442

```
// If the type has discriminant constraints, store constraint values (RM 3.7.2)             
```

### ada83.c:48443

```
// This ensures discriminant fields are correctly set even after aggregate copy.           
```

### ada83.c:48444

```
// Use pre-cached expression results to avoid re-evaluation.                               
```

### ada83.c:48445

```
//                                                                                          
```

### ada83.c:48457

```
// Array aggregate initialization - copy from aggregate to variable.                         
```

### ada83.c:48458

```
// Works for both constrained and unconstrained arrays with aggregate initializers.          
```

### ada83.c:48459

```
// For arrays with dynamic bounds, the aggregate already returns a fat pointer.              
```

### ada83.c:48460

```
//                                                                                            
```

### ada83.c:48464

```
// A qualified aggregate of an unconstrained type (`ARR'(...)` where ARR
```

### ada83.c:48465

```
// is `array (INDEX range <>) of ...`) carries the aggregate's default
```

### ada83.c:48466

```
// INDEX'FIRST-based bounds, but initialization slides to the object's
```

### ada83.c:48467

```
// own constraint (RM 5.2.1). Re-seed the inner aggregate with the
```

### ada83.c:48468

```
// constrained destination type so its fat pointer carries the object's
```

### ada83.c:48469

```
// bounds — exactly as an unqualified aggregate does through context.
```

### ada83.c:48477

```
// Generate_Aggregate returns a fat ptr alloca when the aggregate
```

### ada83.c:48478

```
// type is unconstrained OR has dynamic bounds in ANY dimension.
```

### ada83.c:48479

```
// Detect this so we copy the fat ptr instead of re-wrapping.
```

### ada83.c:48480

```
//
```

### ada83.c:48486

```
// Aggregate returns SSA fat pointer { ptr, ptr }; store to destination.
```

### ada83.c:48492

```
// Destination needs a fat pointer { ptr, { bound, bound, ... } }.
```

### ada83.c:48493

```
// agg_ptr is the data pointer (static bounds), construct the fat pointer.
```

### ada83.c:48499

```
// Multi-dim: store all dimension bounds
```

### ada83.c:48519

```
// Static size known at compile time
```

### ada83.c:48523

```
// Dynamic size - compute from bounds at runtime
```

### ada83.c:48529

```
// Get bounds from aggregate type if available
```

### ada83.c:48534

```
// Bounds are static integers in the aggregate - use ALL dimensions
```

### ada83.c:48549

```
// Fallback: use element size as minimum
```

### ada83.c:48557

```
// Unconstrained non-character array init from expression (e.g. function call).            
```

### ada83.c:48558

```
// The source produces a fat pointer. We need to:                                          
```

### ada83.c:48559

```
// 1. Allocate local data storage from source bounds                                        
```

### ada83.c:48560

```
// 2. Copy data to local storage                                                            
```

### ada83.c:48561

```
// 3. Build new fat pointer with local data                                                 
```

### ada83.c:48562

```
// 4. Store fat pointer into variable                                                       
```

### ada83.c:48563

```
//                                                                                          
```

### ada83.c:48566

```
// Produced rep, not the declared form: a dynamic boolean-array op or
```

### ada83.c:48567

```
// other runtime fat producer can disagree with its declared "ptr" form.
```

### ada83.c:48571

```
// Source is fat pointer - unpack, alloca, copy, rebuild
```

### ada83.c:48591

```
// Widen byte_len to i64 for alloca and memcpy intrinsics
```

### ada83.c:48601

```
// Source is ptr (constrained) - wrap with bounds
```

### ada83.c:48612

```
// Fallback: store as fat pointer with unknown bounds
```

### ada83.c:48618

```
// Record initialized with non-aggregate expression (function call,                           
```

### ada83.c:48619

```
// qualified expression, type conversion, etc.). Generate_Expression                         
```

### ada83.c:48620

```
// returns a ptr to the result; memcpy into the variable. RM 3.3.1                           
```

### ada83.c:48621

```
//                                                                                            
```

### ada83.c:48624

```
// RM 3.2.2(5): Pre-evaluate discriminant constraint expressions                            
```

### ada83.c:48625

```
// BEFORE the init expression. The subtype indication must be                              
```

### ada83.c:48626

```
// evaluated first so that side effects (function calls that set                            
```

### ada83.c:48627

```
// variables read by the init) occur in the correct order.                                 
```

### ada83.c:48628

```
//                                                                                          
```

### ada83.c:48643

```
// RM 3.3.2: If target has discriminant constraints, verify that the                        
```

### ada83.c:48644

```
// initial value's discriminants match. Raise CONSTRAINT_ERROR on                          
```

### ada83.c:48645

```
// mismatch before the memcpy.                                                             
```

### ada83.c:48646

```
//                                                                                          
```

### ada83.c:48660

```
// Constrained array initialized with non-aggregate expression                                
```

### ada83.c:48661

```
// (function call, slice, type conversion, etc.). Similar to                                 
```

### ada83.c:48662

```
// record case - result is a ptr, memcpy into the variable.                                  
```

### ada83.c:48663

```
//                                                                                            
```

### ada83.c:48667

```
// If expression returned a fat pointer, extract data ptr
```

### ada83.c:48675

```
// RM 3.2.2(5): For scalar subtypes with dynamic bounds, the                                  
```

### ada83.c:48676

```
// subtype indication must be evaluated BEFORE the init expression.                          
```

### ada83.c:48677

```
// Pre-evaluate dynamic bounds and cache them so they are not                                 
```

### ada83.c:48678

```
// re-evaluated by constraint checks (which would cause side-                                 
```

### ada83.c:48679

```
// effectful expressions like function calls to be invoked                                    
```

### ada83.c:48680

```
// multiple times, breaking the c32001 family of ACATS tests).                               
```

### ada83.c:48681

```
//                                                                                            
```

### ada83.c:48691

```
// RM 3.2.2(5): For access types with constrained designated record,                        
```

### ada83.c:48692

```
// pre-evaluate discriminant constraint expressions before the init.                       
```

### ada83.c:48693

```
// This ensures side effects occur in the correct order and cached                          
```

### ada83.c:48694

```
// values are used by both aggregate generation and constraint checks.                     
```

### ada83.c:48695

```
//                                                                                          
```

### ada83.c:48713

```
// Set up cg->disc_cache for aggregate generation
```

### ada83.c:48725

```
// Clear disc cache after init generation
```

### ada83.c:48731

```
// The value's real LLVM type (what was actually emitted): e.g. a
```

### ada83.c:48732

```
// BOOLEAN array bound is loaded from the fat-pointer bounds struct as
```

### ada83.c:48733

```
// i32 though its Ada type is i8, so an Ada-type-derived width would
```

### ada83.c:48734

```
// understate it and skip the needed narrowing store.
```

### ada83.c:48737

```
// Float > fixed-point: scale by SMALL to the stored scaled-integer
```

### ada83.c:48738

```
// representation BEFORE the range check, so the check compares scaled
```

### ada83.c:48739

```
// int vs the subtype's scaled-int bounds rather than mis-widening a
```

### ada83.c:48740

```
// double. Fixed-point values are stored as `val / SMALL` (RM 3.5.9).
```

### ada83.c:48741

```
//
```

### ada83.c:48751

```
// RM 3.3.2: Scalar constraint check on initialization. The value is
```

### ada83.c:48752

```
// now at its stored representation (scaled int for fixed-point), so
```

### ada83.c:48753

```
// the check operates on matching types.
```

### ada83.c:48759

```
// RM 4.8(6): Access type constraint check. When assigning                                 
```

### ada83.c:48760

```
// an allocator result to a constrained access type variable,                               
```

### ada83.c:48761

```
// verify that the designated object's discriminants or bounds                              
```

### ada83.c:48762

```
// match the access subtype's constraints.                                                 
```

### ada83.c:48763

```
//                                                                                          
```

### ada83.c:48766

```
// Check discriminant constraints on designated record type
```

### ada83.c:48771

```
// RM 4.8(6): Extract data ptr from fat-pointer access,
```

### ada83.c:48772

```
// null-check, then verify discriminants.
```

### ada83.c:48787

```
// Check index constraints on designated array type
```

### ada83.c:48792

```
// Fat pointer - extract bounds and compare
```

### ada83.c:48799

```
// Expected bounds materialised at the bound type directly
```

### ada83.c:48800

```
// (Emit_Single_Bound handles enum/character/static bounds that
```

### ada83.c:48801

```
// Emit_Bound_Value left as an unmaterialised reg 0).
```

### ada83.c:48805

```
// Get actual bounds for this dimension from the fat pointer
```

### ada83.c:48809

```
// Check lo match
```

### ada83.c:48813

```
// Check hi match
```

### ada83.c:48820

```
// RM 4.8(6): Non-fat-pointer allocator - compare type-level                          
```

### ada83.c:48821

```
// bounds from the allocator's designated type against the                            
```

### ada83.c:48822

```
// constrained access subtype's designated type bounds.                              
```

### ada83.c:48823

```
// E.g. AC : ACCA (1..2) := NEW ARR (1..1) must raise CE.                            
```

### ada83.c:48824

```
//                                                                                    
```

### ada83.c:48835

```
// Null check first
```

### ada83.c:48850

```
// Skip check if any bound produced temp 0 (unresolved)
```

### ada83.c:48854

```
// Coerce to array bound type (may differ from i32)
```

### ada83.c:48860

```
// Check lo match
```

### ada83.c:48864

```
// Check hi match
```

### ada83.c:48874

```
// Convert if types differ, then store
```

### ada83.c:48880

```
// Clear cached bound temps so next object in a multi-declaration
```

### ada83.c:48881

```
// re-evaluates them independently (RM 3.2.2).
```

### ada83.c:48889

```
// RM 3.2.1: an access object with no initializer defaults to null.
```

### ada83.c:48906

```
// A fat-stored element (dynamic-bound array / string component) occupies
```

### ada83.c:48907

```
// a { ptr, ptr } slot, not its (zero) flat size — size the backing by it.
```

### ada83.c:48914

```
// Record without explicit initializer (RM 3.7):                                                
```

### ada83.c:48915

```
// 1. If constrained subtype, initialize discriminants from constraints                         
```

### ada83.c:48916

```
// 2. Apply component defaults for discriminants and regular components                         
```

### ada83.c:48917

```
//                                                                                              
```

### ada83.c:48920

```
// Initialize discriminant constraints if type is constrained (RM 3.7.2)
```

### ada83.c:48924

```
// Store + subtype-check the discriminants into the record (RM 3.7.2),
```

### ada83.c:48925

```
// then bind each to a value alloca so dependent component sizes resolve.
```

### ada83.c:48932

```
// The discriminant was just stored: bind its dependent-bounds
```

### ada83.c:48933

```
// alloca from the record's own slot. Re-evaluating the
```

### ada83.c:48934

```
// constraint expression here would repeat its side effects —
```

### ada83.c:48935

```
// each constraint is evaluated exactly once (RM 3.7.2).
```

### ada83.c:48942

```
// Generic instantiation: discriminant constraints from the AST                               
```

### ada83.c:48943

```
// (e.g., P2 : PRIV (T'VAL (F*100))) override defaults (RM 12.3).                            
```

### ada83.c:48944

```
// The formal type doesn't carry constraints, but the declaration                             
```

### ada83.c:48945

```
// AST has the constraint expressions.                                                       
```

### ada83.c:48946

```
//                                                                                            
```

### ada83.c:48954

```
// Unwrap NK_ASSOCIATION if present
```

### ada83.c:48963

```
// RM 3.7.3: Determine selected variant so we skip absent components
```

### ada83.c:48964

```
// during subcomponent init and default init.
```

### ada83.c:48965

```
// -2 = unknown
```

### ada83.c:48969

```
// Try static disc value first
```

### ada83.c:48997

```
// Dynamic: load the disc value that was just stored
```

### ada83.c:49009

```
// Pre-store discriminant defaults for mutable records (RM 3.3.1).
```

### ada83.c:49010

```
// When no explicit disc constraint exists, subcomponent disc init
```

### ada83.c:49011

```
// (below) loads parent disc values from record memory, so defaults
```

### ada83.c:49012

```
// must be written first to avoid reading uninitialized storage.
```

### ada83.c:49013

```
//
```

### ada83.c:49029

```
// RM 3.7.3: with the governing discriminant's default now stored, read
```

### ada83.c:49030

```
// it so the variant guards below construct only the selected arm's
```

### ada83.c:49031

```
// components. Without a constraint the static/dynamic paths above left
```

### ada83.c:49032

```
// the variant unknown, which would build (and wrongly check) a component
```

### ada83.c:49033

```
// that is absent for the default discriminant.
```

### ada83.c:49049

```
// Initialize discriminant constraints of record subcomponents (RM 3.7.2).
```

### ada83.c:49050

```
// E.g., for TYPE R2 (D2: POSITIVE) IS RECORD C : R1 (2); END RECORD;
```

### ada83.c:49051

```
// we must also initialize C.D1 = 2 at the appropriate offset.
```

### ada83.c:49052

```
//
```

### ada83.c:49057

```
// RM 3.7.3: Skip components in unselected variants
```

### ada83.c:49062

```
// Runtime variant guard for dynamic disc values
```

### ada83.c:49070

```
// A nested record component's discriminant constraint may be static
```

### ada83.c:49071

```
// (disc_constraint_values), a discriminant reference or a once-evaluated
```

### ada83.c:49072

```
// function (disc_constraint_exprs) — REC (D3, F1). The body below stores
```

### ada83.c:49073

```
// all three, so gate on the presence of ANY constraint, not just static
```

### ada83.c:49074

```
// values, otherwise REC (D3, F1)'s D1 (= D3) is never initialised.
```

### ada83.c:49083

```
// In an rt-laid-out record the component lives at a runtime offset;
```

### ada83.c:49084

```
// the static byte_offset would store the subcomponent discriminant in
```

### ada83.c:49085

```
// the wrong place.
```

### ada83.c:49105

```
// Check if the constraint expression references a discriminant                         
```

### ada83.c:49106

```
// of the parent record - if so, load from the record variable                          
```

### ada83.c:49107

```
// at the discriminant's offset rather than via Generate_Expression                     
```

### ada83.c:49108

```
// (the discriminant has no standalone alloca, RM 3.7.2).                              
```

### ada83.c:49109

```
//                                                                                      
```

### ada83.c:49120

```
// Use pre-evaluated value if available (RM 3.7.1:                                      
```

### ada83.c:49121

```
// non-disc expressions in disc-dependent constraints                                   
```

### ada83.c:49122

```
// are evaluated once at type elaboration).                                            
```

### ada83.c:49123

```
//                                                                                      
```

### ada83.c:49140

```
// RM 3.7.2: Check nested disc value against disc subtype
```

### ada83.c:49146

```
// Close runtime variant guard
```

### ada83.c:49150

```
// Apply component defaults (RM 3.7) - includes discriminant defaults
```

### ada83.c:49151

```
// for mutable records (all discriminants have defaults, no explicit constraint).
```

### ada83.c:49152

```
// Track disc_agg_temp for discriminant defaults so dependent component
```

### ada83.c:49153

```
// expressions (e.g., STRING(1..L)) can resolve disc values.
```

### ada83.c:49154

```
//
```

### ada83.c:49156

```
// The discriminant defaults of an unconstrained mutable record were
```

### ada83.c:49157

```
// already pre-stored above; tell the component-default pass to BIND them
```

### ada83.c:49158

```
// (read the record field) rather than re-evaluate the default — otherwise
```

### ada83.c:49159

```
// a function default like (D : INTEGER := F) is called twice.
```

### ada83.c:49168

```
// Apply defaults from nested record-typed components (RM 3.3.1).                            
```

### ada83.c:49169

```
// When a component has no explicit initializer but its type is a                             
```

### ada83.c:49170

```
// record with default expressions, apply those defaults recursively.                        
```

### ada83.c:49171

```
//                                                                                            
```

### ada83.c:49174

```
// Already handled above
```

### ada83.c:49179

```
// Check if this nested record type has any component defaults
```

### ada83.c:49189

```
// Set up disc_agg_temp for nested record discriminants so that                             
```

### ada83.c:49190

```
// discriminant-dependent default expressions can load disc values.                        
```

### ada83.c:49191

```
// Collect disc symbols from default expr ASTs, then provide values                         
```

### ada83.c:49192

```
// from either disc constraints (constrained) or defaults (unconstrained).                 
```

### ada83.c:49193

```
//                                                                                          
```

### ada83.c:49196

```
// Phase 1: Collect discriminant symbols referenced in default exprs
```

### ada83.c:49204

```
// Also check array bound expressions for disc refs
```

### ada83.c:49219

```
// Phase 2: For each disc of the nested record, generate its value
```

### ada83.c:49220

```
// and store both to memory and to disc_agg_temp.
```

### ada83.c:49226

```
// Constrained nested record: value from constraint.                                     
```

### ada83.c:49227

```
// RM 3.7.2: If the constraint expression references a                                    
```

### ada83.c:49228

```
// discriminant of the parent record (ty), load from                                      
```

### ada83.c:49229

```
// the parent variable at its offset - the discriminant                                   
```

### ada83.c:49230

```
// has no standalone alloca.                                                             
```

### ada83.c:49231

```
//                                                                                        
```

### ada83.c:49248

```
// Unconstrained nested record: value from default
```

### ada83.c:49254

```
// Store discriminant to memory
```

### ada83.c:49259

```
// Set disc_agg_temp for matching symbols found in defaults
```

### ada83.c:49271

```
// Apply non-discriminant component defaults
```

### ada83.c:49304

```
// Dynamic-size composite (disc-dependent array):
```

### ada83.c:49305

```
// extract data from fat-pointer aggregate and memcpy.
```

### ada83.c:49313

```
// The aggregate may produce the fat value directly (SSA) or a pointer
```

### ada83.c:49314

```
// to it; load only in the latter case.
```

### ada83.c:49345

```
// Clean up disc_agg_temp for nested record
```

### ada83.c:49350

```
// RM 3.3.2: After applying discriminant defaults, verify that                                
```

### ada83.c:49351

```
// discriminant-dependent component constraints are satisfiable.                             
```

### ada83.c:49352

```
// Load disc values directly from the record storage (disc symbols                            
```

### ada83.c:49353

```
// don't have standalone allocas).                                                           
```

### ada83.c:49354

```
//                                                                                            
```

### ada83.c:49359

```
// RM 3.7.3: Skip components in unselected variants
```

### ada83.c:49364

```
// Runtime variant guard for dynamic disc values
```

### ada83.c:49373

```
// Check array component with disc-dependent bounds.                                       
```

### ada83.c:49374

```
// RM 3.6.1: Null ranges (low > high) need not satisfy                                      
```

### ada83.c:49375

```
// index subtype constraints, so guard with a null check.                                  
```

### ada83.c:49376

```
//                                                                                          
```

### ada83.c:49380

```
// Check nested unconstrained discriminated record components.                             
```

### ada83.c:49381

```
// If a component is an unconstrained disc record type with                                 
```

### ada83.c:49382

```
// default disc values, those defaults may create invalid                                   
```

### ada83.c:49383

```
// constraints on the nested record's own subcomponents.                                   
```

### ada83.c:49384

```
// E.g., REC4 (D:=-1) has C:REC where REC (D:=-1) creates                                   
```

### ada83.c:49385

```
// invalid STRING (-1..3).                                                                 
```

### ada83.c:49386

```
//                                                                                          
```

### ada83.c:49391

```
// RM 3.7.3: the nested record's own governing discriminant selects
```

### ada83.c:49392

```
// which of ITS variant components are present. Read it from the
```

### ada83.c:49393

```
// nested record's storage so a component in an unselected arm is not
```

### ada83.c:49394

```
// checked (a nested CONS whose default D3 lands in the OTHERS arm must
```

### ada83.c:49395

```
// not have the -5..10 arm's C1 : REC (D3, ..) checked).
```

### ada83.c:49417

```
// Nested array with disc-dependent bounds (same guard as outer).
```

### ada83.c:49429

```
// Close runtime variant guard
```

### ada83.c:49433

```
// Clear disc_agg_temp so it doesn't leak to other contexts
```

### ada83.c:49439

```
// RM 3.2.1: drop every cache the loop built once the declaration is done.
```

### ada83.c:49440

```
// The cached bound temps and disc-preeval allocas are LOCAL to this
```

### ada83.c:49441

```
// elaboration — another elaboration (a different call, a different function)
```

### ada83.c:49442

```
// must evaluate the subtype indication fresh (RM 3.7.1). Clear_Cached_Bounds
```

### ada83.c:49443

```
// recurses into element and designated types, so a shared record whose disc
```

### ada83.c:49444

```
// preeval was populated deeper is also dropped.
```

### ada83.c:49459

```
// DECLARE block - check its declarations and nested statements
```

### ada83.c:49466

```
// Check all branches of IF
```

### ada83.c:49477

```
// An ACCEPT body is a sequence of statements like any other (RM 9.5) —
```

### ada83.c:49478

```
// a DECLARE block inside it can hold nested subprograms that reach this
```

### ada83.c:49479

```
// frame (c85005c: PROC1 declared inside ACCEPT START's block).
```

### ada83.c:49506

```
// One declaration list of the frame owner or of a package nested in it (to
```

### ada83.c:49507

```
// ANY depth — c41328a's P.Q hides its subprograms two package levels down):
```

### ada83.c:49508

```
// true when it declares anything that will take this frame's static chain.
```

### ada83.c:49523

```
// A SEPARATE package-body stub: the subunit's elaboration runs as a
```

### ada83.c:49524

```
// function over THIS frame (RM 10.2), so the frame must exist.
```

### ada83.c:49543

```
// Check declarations for subprogram/task bodies — directly here or inside
```

### ada83.c:49544

```
// nested packages at any depth. A nested package's subprograms take this
```

### ada83.c:49545

```
// frame's static chain (RM 9.1: task bodies count for the same reason).
```

### ada83.c:49557

```
// An attribute renaming carries a synthesized wrapper FUNCTION whose
```

### ada83.c:49558

```
// call sites pass this frame as the static chain (c85016a).
```

### ada83.c:49573

```
// Check statements for DECLARE blocks that might contain nested subprograms
```

### ada83.c:49577

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:49578

```
// Emit LLVM function header: define <ret> @<name>([ptr %__parent_frame,] params...) {              
```

### ada83.c:49579

```
// Shared by Generate_Subprogram_Body and Generate_Task_Body.                                      
```

### ada83.c:49580

```
//                                                                                                  
```

### ada83.c:49581

```
// For BIP functions (returning limited types), we prepend extra parameters:                        
```

### ada83.c:49582

```
//   i32 %__BIPalloc   - Allocation form selector                                                   
```

### ada83.c:49583

```
//   ptr %__BIPaccess  - Pointer to result destination                                              
```

### ada83.c:49584

```
// The function returns void since result is built into __BIPaccess.                               
```

### ada83.c:49585

```
//                                                                                                  
```

### ada83.c:49586

```
// Return-type spelling shared by define and declare: build-in-place
```

### ada83.c:49587

```
// functions return void (the result is built through __BIPaccess),
```

### ada83.c:49588

```
// procedures return void, functions return their type's value rep.
```

### ada83.c:49595

```
// Emit the parenthesized formal-parameter list of a subprogram's LLVM
```

### ada83.c:49596

```
// signature: static chain, build-in-place extra formals, regular formals
```

### ada83.c:49597

```
// (ptr for OUT / IN OUT per Param_Is_By_Reference, the type's value rep
```

### ada83.c:49598

```
// otherwise), then the hidden 'CONSTRAINED flags (RM 3.7.4) appended in
```

### ada83.c:49599

```
// parameter order so the regular %p<i> numbering is undisturbed.
```

### ada83.c:49600

```
// with_names distinguishes the define form (%p<i> names) from the bare
```

### ada83.c:49601

```
// declare form. ALI-loaded symbols may lack the formal's type; an IN
```

### ada83.c:49602

```
// formal then falls back to the integer arithmetic rep.
```

### ada83.c:49612

```
// BIP extra formals: __BIPalloc (allocation form) and __BIPaccess (dest
```

### ada83.c:49613

```
// ptr); __BIPmaster and __BIPchain follow when the result has task parts.
```

### ada83.c:49650

```
// New function: invalidate the producer-honesty shadow (temp ids restart).
```

### ada83.c:49657

```
// Each LLVM `define` uses fresh %0/%1/... SSA numbering. Reset the
```

### ada83.c:49658

```
// temp counter and per-function side tables so a stale entry from
```

### ada83.c:49661

```
// Initialize BIP state for code generator
```

### ada83.c:49665

```
// Find the Nth homograph body matching export name in a package body.
```

### ada83.c:49666

```
// Extracted from two identical 25-line blocks in Generate_Declaration.
```

### ada83.c:49667

```
// Process_Deferred_Bodies: Emit deferred nested subprogram/task/generic bodies.                   
```

### ada83.c:49668

```
// Called at end of enclosing function/task/generic instance after restoring context.              
```

### ada83.c:49669

```
// Processes all bodies deferred since saved_deferred_count.                                       
```

### ada83.c:49670

```
//                                                                                                  
```

### ada83.c:49671

```
// Elaborate a tree-copy instance's formal-object bindings (RM 12.3(80)):
```

### ada83.c:49672

```
// mode IN evaluates the actual once and stores its value (or copies its
```

### ada83.c:49673

```
// bytes, for flat composites) into the binding's per-instance global; mode
```

### ada83.c:49674

```
// IN OUT stores the actual variable's address into the binding's pointer
```

### ada83.c:49675

```
// cell. Emitted at the instantiation point, in the instantiation's own
```

### ada83.c:49676

```
// context.
```

### ada83.c:49677

```
// Define a per-instance global at module level (via the string-constant
```

### ada83.c:49678

```
// accumulator, so it is legal even when emission is inside a function):
```

### ada83.c:49679

```
// zero-initialized storage shaped by the symbol's type. Idempotent through
```

### ada83.c:49680

```
// extern_emitted.
```

### ada83.c:49709

```
// Emit (or defer, like any nested body) the synthesized wrapper bodies a
```

### ada83.c:49710

```
// tree-copy instance's formal-subprogram bindings carry.
```

### ada83.c:49726

```
// Emit one bound of a subtype-agreement comparison in the comparison
```

### ada83.c:49727

```
// domain. Discrete and float domains go through Emit_Type_Bound; the
```

### ada83.c:49728

```
// fixed-point domain compares scaled mantissas, so real-valued bounds
```

### ada83.c:49729

```
// (BOUND_FLOAT statically, float-typed expressions dynamically) divide
```

### ada83.c:49730

```
// by the type's small before the integer compare.
```

### ada83.c:49762

```
// Raise CONSTRAINT_ERROR at the instantiation point unless two single
```

### ada83.c:49763

```
// values agree. Static-equal values emit nothing; static-unequal emit an
```

### ada83.c:49764

```
// unconditional raise; dynamic values emit a runtime comparison in the
```

### ada83.c:49765

```
// domain's representation (fcmp for float values, icmp on scaled
```

### ada83.c:49766

```
// mantissas for fixed, icmp otherwise).
```

### ada83.c:49796

```
// Agreement over a low/high bound pair: both ends must match.
```

### ada83.c:49810

```
// A record subtype's discriminant constraint value as a Type_Bound: the
```

### ada83.c:49811

```
// resolved constraint expression when present, the folded value otherwise.
```

### ada83.c:49824

```
// Raise CONSTRAINT_ERROR at the instantiation point unless two subtypes
```

### ada83.c:49825

```
// have matching constraints (the RM 12.3.4(116-118) index/component and
```

### ada83.c:49826

```
// RM 12.3.2(104-105) discriminant agreement checks). Scalar pairs compare
```

### ada83.c:49827

```
// their own bounds. Array pairs compare per-dimension index constraints
```

### ada83.c:49828

```
// (an array subtype's constraint lives in Index_Info, not in the shared
```

### ada83.c:49829

```
// index_type) and recurse on the component subtypes, which may themselves
```

### ada83.c:49830

```
// be arrays.
```

### ada83.c:49890

```
// The instantiation-elaboration constraint checks of RM 12.3 (81): the
```

### ada83.c:49891

```
// subtype-agreement pairs captured at resolution emit here, in the
```

### ada83.c:49892

```
// instantiation's own context, before the bindings store.
```

### ada83.c:49904

```
// RM 12.3(80): every explicit generic actual is evaluated before any default
```

### ada83.c:49905

```
// expression. Walk the bindings twice in declaration order — explicit actuals
```

### ada83.c:49906

```
// (pass 0), then defaulted ones (pass 1) — so the side effects of the actual
```

### ada83.c:49907

```
// expressions and a default that references an earlier formal (cc3123b's
```

### ada83.c:49908

```
// GEN_INT2 := (F, GEN_INT1)) observe the required order.
```

### ada83.c:49930

```
// Composite IN binding: byte-array global, value copied in.
```

### ada83.c:49944

```
// RM 12.3.1: an array actual bound to a constrained-array formal must
```

### ada83.c:49945

```
// have matching per-dimension lengths (sliding does not apply). When
```

### ada83.c:49946

```
// both sides are statically bounded the mismatch is known at compile
```

### ada83.c:49947

```
// time; raise unconditionally then, as regular parameter passing does.
```

### ada83.c:49948

```
// A slice actual carries its OWN bounds (not those of its base
```

### ada83.c:49949

```
// subtype), so it is not statically checkable through the base type;
```

### ada83.c:49950

```
// let the fat-pointer path check at run time.
```

### ada83.c:49967

```
// Only decide statically when every bound is compile-time known;
```

### ada83.c:49968

```
// a dynamic bound goes to the fat-pointer path, which carries
```

### ada83.c:49969

```
// the actual's length at run time.
```

### ada83.c:49990

```
// Arrays copy through the expression's fat pointer — uniform for
```

### ada83.c:49991

```
// names, literals, slices, and aggregates.
```

### ada83.c:50003

```
// RM 12.3.1: a record actual whose formal has a constrained
```

### ada83.c:50004

```
// discriminant subtype must have matching discriminants, checked at
```

### ada83.c:50005

```
// instantiation. This mirrors the ordinary parameter-passing check.
```

### ada83.c:50037

```
// A statically constrained array actual bound to a formal whose type is
```

### ada83.c:50038

```
// unconstrained (e.g. a formal private type whose actual is an
```

### ada83.c:50039

```
// unconstrained array type) arrives as a plain data pointer. Wrap it
```

### ada83.c:50040

```
// into a fat pointer carrying the actual subtype's bounds so the
```

### ada83.c:50041

```
// descriptor store below is well-formed; the deep copy that follows
```

### ada83.c:50042

```
// then detaches it from the actual's storage.
```

### ada83.c:50056

```
// RM 12.3: an IN formal object is COPIED at instantiation. A dynamically
```

### ada83.c:50057

```
// bounded array actual is a fat pointer whose descriptor would otherwise
```

### ada83.c:50058

```
// alias the actual's own data buffer, so a later change to the actual
```

### ada83.c:50059

```
// would show through the formal (cc3120a's "PARAMETER SCREW_UP"). Deep-
```

### ada83.c:50060

```
// copy the data onto the secondary stack; static-size composites already
```

### ada83.c:50061

```
// copy through the byte-array-global branch above.
```

### ada83.c:50070

```
// RM 12.3.1 / 12.1.1(6): the actual for an IN parameter is checked
```

### ada83.c:50071

```
// against the formal's subtype constraint at instantiation, exactly as
```

### ada83.c:50072

```
// an assignment does. A range violation raises CONSTRAINT_ERROR at the
```

### ada83.c:50073

```
// instantiation, not at first use inside the instance.
```

### ada83.c:50079

```
// RM 12.3.1 / 3.7.2: an actual bound to a formal object of an ACCESS
```

### ada83.c:50080

```
// type must, when non-null, designate an object satisfying the
```

### ada83.c:50081

```
// formal's designated constraint — checked at the instantiation
```

### ada83.c:50082

```
// (cc3128a). A formal access type's actual-view carries the FORMAL's
```

### ada83.c:50083

```
// designated subtype (RM 12.3.5), so the binding's own type is the
```

### ada83.c:50084

```
// authority for both formal-typed and concrete marks.
```

### ada83.c:50089

```
// Value-level conversion, not rep-level: a real actual bound to a
```

### ada83.c:50090

```
// fixed-point formal stores the /SMALL-scaled mantissa (RM 3.5.9), which
```

### ada83.c:50091

```
// a raw double->i64 rep conversion would silently drop (cc1223a:
```

### ada83.c:50092

```
// D => FIXED'DELTA stored round(0.1) = 0 instead of mantissa 2).
```

### ada83.c:50104

```
// Queue a nested body for emission after the current function closes.
```

### ada83.c:50105

```
// Duplicate queuing is a no-op. The queue grows without bound: spilling
```

### ada83.c:50106

```
// into inline emission would nest a define inside the open function.
```

### ada83.c:50134

```
// A subprogram stub (PROCEDURE X IS SEPARATE;): the subunit's own
```

### ada83.c:50135

```
// compilation defines the body under the SAME stable mangled name
```

### ada83.c:50136

```
// (Symbol_Is_Subunit_Stub suppresses the per-compilation suffix), so
```

### ada83.c:50137

```
// this module declares it — through the SEPARATE-boundary flush, which
```

### ada83.c:50138

```
// emits the full call ABI including the static-link parameter.
```

### ada83.c:50144

```
// Skip if code already generated for this body (prevents duplicates)
```

### ada83.c:50151

```
// Mark this symbol as having been defined - prevents duplicate
```

### ada83.c:50152

```
// 'declare' statements for functions defined in the same file
```

### ada83.c:50157

```
// Check if this is a nested function (has enclosing function).                                  
```

### ada83.c:50158

```
// This handles nested packages: a subprogram inside a package inside                             
```

### ada83.c:50159

```
// a procedure needs static chain access to the outermost procedure's frame.                     
```

### ada83.c:50160

```
//                                                                                                
```

### ada83.c:50164

```
// Find nearest enclosing function/procedure (walks through packages)
```

### ada83.c:50170

```
// Emit comment showing function/procedure being generated
```

### ada83.c:50183

```
// Reset for new function
```

### ada83.c:50185

```
// A return inside THIS subprogram returns from it, never from an enclosing
```

### ada83.c:50186

```
// accept — clear the accept-completion context for the body's duration even
```

### ada83.c:50187

```
// if the body is generated while an outer accept's context is live.
```

### ada83.c:50191

```
// RM 11.1: runaway recursion must raise STORAGE_ERROR, not fault the
```

### ada83.c:50192

```
// process. Probe the remaining stack in every prologue — the request
```

### ada83.c:50193

```
// covers this frame plus a typical-frame floor, so exhaustion is caught
```

### ada83.c:50194

```
// while margin remains for the raise machinery (cb1010a/d, cb7001a: small
```

### ada83.c:50195

```
// recursive frames never trip the large-object probes).
```

### ada83.c:50203

```
// Check if this function has nested subprograms (in declarations or DECLARE blocks)
```

### ada83.c:50207

```
// If this function has nested subprograms, allocate a frame base.                               
```

### ada83.c:50208

```
// When also nested itself, reserve 8 extra bytes at the end to store                             
```

### ada83.c:50209

```
// the received __parent_frame (static chain pointer for RM 8.3).                                
```

### ada83.c:50210

```
//                                                                                                
```

### ada83.c:50216

```
// A large frame (many medium-sized locals; single oversized objects were
```

### ada83.c:50217

```
// already capped to placeholder slots) can still exceed the stack: probe
```

### ada83.c:50218

```
// before allocating. The raise here propagates out of the subprogram —
```

### ada83.c:50219

```
// its own handlers are not yet active at frame setup (RM 11.2).
```

### ada83.c:50223

```
// Store static chain: parent's frame at offset frame_size
```

### ada83.c:50230

```
// If nested, create aliases for accessing enclosing scope variables via
```

### ada83.c:50231

```
// the frame: Emit_Parent_Frame_Aliases walks every enclosing level
```

### ada83.c:50232

```
// through the static chain (the chain slot is the alloca above).
```

### ada83.c:50236

```
// Allocate and store parameters to local stack slots                                             
```

### ada83.c:50237

```
// For OUT/IN OUT: param is already a pointer, use directly                                       
```

### ada83.c:50238

```
// For IN: allocate local slot and copy value                                                     
```

### ada83.c:50239

```
//                                                                                                
```

### ada83.c:50246

```
// By-reference formals (OUT/IN OUT, and IN composites/tasks per
```

### ada83.c:50247

```
// Parameter_Passed_By_Reference): %p is a pointer to the caller's
```

### ada83.c:50248

```
// storage. Unconstrained arrays/strings use fat pointers {ptr, ptr}
```

### ada83.c:50249

```
// and are stored in an alloca like scalars.
```

### ada83.c:50252

```
// Spill the hidden 'CONSTRAINED flag (RM 3.7.4) into a named slot the
```

### ada83.c:50253

```
// attribute and assignment sites can find by symbol name.
```

### ada83.c:50263

```
// Publish the flag into the frame beside the parameter's slot so
```

### ada83.c:50264

```
// nested subprograms read it through the static chain (c64106d:
```

### ada83.c:50265

```
// REC3'CONSTRAINED inside a nested procedure).
```

### ada83.c:50274

```
// Create an alias so the parameter name points to caller's storage
```

### ada83.c:50279

```
// If nested subprograms can see this parameter, publish the pointer to
```

### ada83.c:50280

```
// the caller's storage in the frame so their uplevel access reaches the
```

### ada83.c:50281

```
// same object (a by-ref parameter's value lives at *%p, not in the frame).
```

### ada83.c:50288

```
// IN param with nested functions: allocate in frame
```

### ada83.c:50297

```
// IN param: allocate local and copy value
```

### ada83.c:50308

```
// Generate local declarations, passing has_nested flag via cg for frame allocation.
```

### ada83.c:50309

```
// Keep has_nested active for statements too (DECLARE blocks need it).
```

### ada83.c:50310

```
// Repurpose field temporarily
```

### ada83.c:50317

```
// Don't reset yet - statements may have DECLARE blocks with tasks needing frame
```

### ada83.c:50319

```
// Check if subprogram has exception handlers
```

### ada83.c:50322

```
// Setup exception handling using consolidated helper
```

### ada83.c:50327

```
// Normal execution path
```

### ada83.c:50329

```
// RM 9.3 activation point
```

### ada83.c:50331

```
// RM 9.7.1 + 9.4
```

### ada83.c:50335

```
// Exception handler entry
```

### ada83.c:50339

```
// Dispatch to exception handlers
```

### ada83.c:50343

```
// End label - normal return point
```

### ada83.c:50346

```
// Generate statements without exception handling
```

### ada83.c:50348

```
// RM 9.3 activation point
```

### ada83.c:50350

```
// RM 9.7.1 + 9.4
```

### ada83.c:50353

```
// Restore nesting level now that all code for this subprogram is generated
```

### ada83.c:50356

```
// Default return if block is not terminated
```

### ada83.c:50359

```
// BIP functions return void, but missing return is still PROGRAM_ERROR
```

### ada83.c:50363

```
// For BIP: raise error first, then ret void (unreachable)
```

### ada83.c:50368

```
// RM 6.4(11): raise PROGRAM_ERROR if function completes without RETURN
```

### ada83.c:50377

```
// Clean up BIP state
```

### ada83.c:50387

```
// Emit the task body function name for a task type symbol.                                        
```

### ada83.c:50388

```
// Resolves to the task TYPE's defining_symbol for consistency between                              
```

### ada83.c:50389

```
// task body definitions and task_start call sites.                                                
```

### ada83.c:50390

```
// The name format is: task_MANGLED_NAME                                                            
```

### ada83.c:50391

```
//                                                                                                  
```

### ada83.c:50395

```
// Non-generic: use the type's defining_symbol for a stable name that
```

### ada83.c:50396

```
// both the body (via node->symbol->type->defining_symbol) and call
```

### ada83.c:50397

```
// site (via ty->defining_symbol) resolve to the same symbol.
```

### ada83.c:50398

```
// Generic instances: a task symbol owned by the generic TEMPLATE is
```

### ada83.c:50399

```
// shared across instances, so its per-instance body is named with the
```

### ada83.c:50400

```
// instance id. A task type from OUTSIDE the generic (e.g. the actual
```

### ada83.c:50401

```
// behind a formal) keeps its one normal body name even when referenced
```

### ada83.c:50402

```
// while emitting an instance.
```

### ada83.c:50405

```
// A formal type's view of a task actual denotes the actual's one body
```

### ada83.c:50406

```
// (RM 12.3): peel the view before resolving, or an instance's task
```

### ada83.c:50407

```
// object references a body named after the FORMAL that nothing defines
```

### ada83.c:50408

```
// (c94010a). A derived task type reuses its parent's body (RM 9.1).
```

### ada83.c:50425

```
// Emit %__frame. aliases for every frame-resident variable of EVERY
```

### ada83.c:50426

```
// enclosing subprogram level (RM 8.3): the direct parent's variables read
```

### ada83.c:50427

```
// through %__parent_frame; deeper levels chase the static chain — each
```

### ada83.c:50428

```
// nested frame stores its parent's frame pointer at its own frame_size
```

### ada83.c:50429

```
// offset. Each level covers its own scope plus DECLARE-block frame_vars,
```

### ada83.c:50430

```
// deduplicated by unique_id; innermost level emits first, so the dedup
```

### ada83.c:50431

```
// realizes lexical shadowing. Shared by nested subprograms, task bodies,
```

### ada83.c:50432

```
// and package-body subunits — all run as functions over the parent's
```

### ada83.c:50433

```
// frame. No-op when there is no enclosing subprogram (library level:
```

### ada83.c:50434

```
// uplevel references are globals and must not alias through
```

### ada83.c:50435

```
// %__parent_frame).
```

### ada83.c:50444

```
// Lexical nesting cannot cycle; the walk ends when no subprogram
```

### ada83.c:50445

```
// encloses the level. No depth cap — a silent one cost the deepest
```

### ada83.c:50446

```
// ACATS subunit chains their root-frame aliases.
```

### ada83.c:50464

```
// module-level storage
```

### ada83.c:50465

```
// USE-alias: emit the original's slot
```

### ada83.c:50484

```
// A by-reference parameter's frame slot holds a pointer to the
```

### ada83.c:50485

```
// caller's storage, and a dynamically-sized record's slot holds a
```

### ada83.c:50486

```
// pointer to its separately-allocated storage; load it so the alias
```

### ada83.c:50487

```
// names that storage. Other entities live directly in the frame, so
```

### ada83.c:50488

```
// the slot IS their storage.
```

### ada83.c:50503

```
// The RM 3.7.4 hidden 'CONSTRAINED flag published beside the
```

### ada83.c:50504

```
// parameter's slot (c64106d: REC3'CONSTRAINED read uplevel).
```

### ada83.c:50516

```
// Chase the static chain to the next enclosing subprogram's frame.
```

### ada83.c:50532

```
// Skip stub bodies (TASK BODY X IS SEPARATE;) - the actual body
```

### ada83.c:50533

```
// will be provided by a separate subunit compilation
```

### ada83.c:50545

```
// Generate task entry point function - receives parent frame pointer                             
```

### ada83.c:50546

```
// for uplevel variable access (task bodies access enclosing scope).                             
```

### ada83.c:50547

```
// For task bodies inside generic instances, prefix with instance name                            
```

### ada83.c:50548

```
// to make the function name unique across multiple instantiations.                              
```

### ada83.c:50549

```
//                                                                                                
```

### ada83.c:50555

```
// Save and set context - task body is like a nested function
```

### ada83.c:50562

```
// Task bodies are nested in enclosing scope
```

### ada83.c:50563

```
// Access parent's vars
```

### ada83.c:50564

```
// Task entry points return ptr for pthread
```

### ada83.c:50566

```
// A LEAF task function allocates no %__frame_base: its locals are allocas,
```

### ada83.c:50567

```
// and enclosing-scope variables arrive through the %__parent_frame aliases
```

### ada83.c:50568

```
// below. (Deferred emission would otherwise leak the ENCLOSING function's
```

### ada83.c:50569

```
// has-nested flag into the task's declarations.) A task body WITH nested
```

### ada83.c:50570

```
// units is a static-chain level like any subprogram: its locals live in a
```

### ada83.c:50571

```
// per-instance frame so nested subprograms and tasks reach them through
```

### ada83.c:50572

```
// the chain (RM 9.1 — c9a006a: a task nested in task REGISTER's body
```

### ada83.c:50573

```
// aborting REGISTER's local task object), and the received %__parent_frame
```

### ada83.c:50574

```
// is stored at the frame's end as the chain link.
```

### ada83.c:50579

```
// Reset temp counter for new function
```

### ada83.c:50595

```
// Create frame aliases for accessing enclosing scope variables.
```

### ada83.c:50596

```
// Task bodies can reference variables from the enclosing scope (RM 9.1).
```

### ada83.c:50597

```
// The parent passed %__parent_frame pointing to the *enclosing subprogram's*
```

### ada83.c:50598

```
// frame, not the task's immediate DECLARE-block scope. So aliases must be
```

### ada83.c:50599

```
// built against the enclosing subprogram's scope; its `frame_vars` already
```

### ada83.c:50600

```
// aggregates any DECLARE-block variables that share the same function frame.
```

### ada83.c:50601

```
// Using `node->symbol->defining_scope` (the task's DECLARE-block scope)
```

### ada83.c:50602

```
// misses outer variables when the task lives inside a DECLARE block.
```

### ada83.c:50603

```
// Note: task bodies are emitted via Process_Deferred_Bodies *after* the
```

### ada83.c:50604

```
// enclosing function has returned, so cg->current_function has been popped
```

### ada83.c:50605

```
// — we recover the enclosing function from the task symbol's parent chain.
```

### ada83.c:50606

```
// Only a real enclosing SUBPROGRAM provides a frame. A task declared at
```

### ada83.c:50607

```
// library package level has no parent frame (it is started with a null
```

### ada83.c:50608

```
// static link); its uplevel references are globals and must not be
```

### ada83.c:50609

```
// aliased through %__parent_frame.
```

### ada83.c:50615

```
// Region 1 — the DECLARATIVE part (activation, RM 9.3). An exception raised
```

### ada83.c:50616

```
// while elaborating the declarations is an activation failure: it publishes
```

### ada83.c:50617

```
// state 2 for the waiting activator and completes the task silently; the
```

### ada83.c:50618

```
// task body's own WHEN handlers do not apply to it.
```

### ada83.c:50621

```
// Reset after br target label
```

### ada83.c:50625

```
// leave the activation guard
```

### ada83.c:50627

```
// RM 9.3: activation = elaboration of the declarative part. Publish
```

### ada83.c:50628

```
// completion under the rendezvous lock and wake the activator's
```

### ada83.c:50629

```
// __ada_activation_wait.
```

### ada83.c:50635

```
// RM 9.3 activation point
```

### ada83.c:50637

```
// Region 2 — the SEQUENCE OF STATEMENTS. Its exception guard is set up AFTER
```

### ada83.c:50638

```
// the declarations so the task's own local allocas dominate the handler path
```

### ada83.c:50639

```
// (RM 11.4.1). An exception here runs the WHEN handlers if any; an unmatched
```

### ada83.c:50640

```
// exception — or a task with no handlers at all — completes the task
```

### ada83.c:50641

```
// silently, because a task exception is never propagated to the activator.
```

### ada83.c:50644

```
// RM 11.5: an exception raised IN a task's own handler — most visibly an
```

### ada83.c:50645

```
// explicit RAISE re-raising the caught exception — must still complete the
```

### ada83.c:50646

```
// task silently, never propagate to the activator. The task's WHEN handlers
```

### ada83.c:50647

```
// therefore run UNDER an outer guard that swallows anything escaping them and
```

### ada83.c:50648

```
// falls through to completion. Without it, a re-raise (SLAVE's `RAISE` after
```

### ada83.c:50649

```
// trapping TASKING_ERROR in c95040d) escaped the task wrapper entirely.
```

### ada83.c:50656

```
// RM 9.10: expose the termination epilogue so a blocking entry call in the
```

### ada83.c:50657

```
// body can branch here when an abort was deferred until the rendezvous ended.
```

### ada83.c:50667

```
// leave the inner guard
```

### ada83.c:50670

```
// A matched handler that completes normally falls to handled_done; a handler
```

### ada83.c:50671

```
// that raises/re-raises lands in silent_exc (still pushed) and completes
```

### ada83.c:50672

```
// silently. An unmatched exception also falls through (RM 11.4.1).
```

### ada83.c:50679

```
// Normal completion or a WHEN handler that finished without raising: leave the
```

### ada83.c:50680

```
// outer silent guard and complete.
```

### ada83.c:50686

```
// A handler (or unmatched propagation) raised: swallow it and complete
```

### ada83.c:50687

```
// silently — a task exception is never propagated (RM 11.5).
```

### ada83.c:50691

```
// RM 9.4: the task is COMPLETED when its statements end or an exception is
```

### ada83.c:50692

```
// handled — callers get TASKING_ERROR from here on; TERMINATED (TCB+57,
```

### ada83.c:50693

```
// stored by the wrapper) follows the dependent waits in Emit_Master_Exit.
```

### ada83.c:50698

```
// RM 9.5(16): completed means callers get TASKING_ERROR — release the
```

### ada83.c:50699

```
// parked ones NOW, before the dependent joins below (a dependent calling
```

### ada83.c:50700

```
// back would otherwise deadlock the join, c95040c).
```

### ada83.c:50704

```
// RM 9.7.1 + 9.4
```

### ada83.c:50707

```
// Region-1 activation handler: a declarative-part exception publishes state
```

### ada83.c:50708

```
// 2 (still-unset slot) so the activator raises TASKING_ERROR, then the task
```

### ada83.c:50709

```
// ends silently.
```

### ada83.c:50723

```
// Restore context
```

### ada83.c:50732

```
// RM 3.3.1: Elaborate a constrained array type or subtype whose bounds are
```

### ada83.c:50733

```
// runtime expressions (BOUND_EXPR). Evaluates each dimension's bounds and the
```

### ada83.c:50734

```
// total byte size and stores them into the type's @__rt_type_<id>_lo/hi/size
```

### ada83.c:50735

```
// globals, for use by record layouts, allocas, and fat-pointer construction.
```

### ada83.c:50736

```
// No-op for static-bounds or non-array types.
```

### ada83.c:50737

```
//
```

### ada83.c:50758

```
// Evaluate or materialise each bound as i64
```

### ada83.c:50775

```
// dim_count = max(hi - lo + 1, 0)
```

### ada83.c:50791

```
// RM 3.2.1/3.5: evaluate a scalar subtype's non-static range constraint ONCE
```

### ada83.c:50792

```
// and freeze the bounds in its runtime descriptor (lo0/hi0). Every later
```

### ada83.c:50793

```
// constraint check reads the frozen value instead of re-evaluating the
```

### ada83.c:50794

```
// (possibly side-effecting) constraint expression.
```

### ada83.c:50797

```
// A scalar descriptor exists ONLY once elaborated (created here), so a
```

### ada83.c:50798

```
// non-elaborated scalar never carries an rt_global_id whose globals a
```

### ada83.c:50799

```
// constraint check could read as uninitialised zero.
```

### ada83.c:50827

```
// Cache the SSA reg on the Type_Bound too: subsequent readers
```

### ada83.c:50828

```
// (Emit_Bound_Value_Typed, Emit_Range_Constraint_Compat_Check) then
```

### ada83.c:50829

```
// reuse this frozen value instead of re-invoking a side-effectful
```

### ada83.c:50830

```
// bound expression (RM 3.2.1(11) — c37003a expects one call per
```

### ada83.c:50831

```
// component; c35003a case S1 needs a range check that must not
```

### ada83.c:50832

```
// spawn another call to the same bound function).
```

### ada83.c:50840

```
// RM 3.2.1: a discriminant constraint that is non-static but does NOT depend on
```

### ada83.c:50841

```
// a discriminant (e.g. REC3X (F), F a function) is evaluated exactly once at
```

### ada83.c:50842

```
// elaboration. Freeze each such expression into a slot so the constraint check
```

### ada83.c:50843

```
// reads the frozen value rather than re-evaluating it. Disc-DEPENDENT
```

### ada83.c:50844

```
// constraints (REC (L), L a discriminant) are left to the deferred path.
```

### ada83.c:50856

```
// non-disc-dependent runtime expr
```

### ada83.c:50866

```
// Freeze ONLY a non-disc-dependent expression. One that references an
```

### ada83.c:50867

```
// enclosing discriminant (REC1 (D2 => D)) cannot be evaluated at type
```

### ada83.c:50868

```
// elaboration — the discriminant is not bound until the object is
```

### ada83.c:50869

```
// initialized — so leave its slot empty for the deferred / object-init path.
```

### ada83.c:50879

```
// RM 3.6 / 3.7.1: freeze one record component's non-static constraint, in
```

### ada83.c:50880

```
// declaration order. Dispatches on the component subtype: a dynamic array, a
```

### ada83.c:50881

```
// dynamic scalar range, a discriminated record, or an access whose designated
```

### ada83.c:50882

```
// subtype is itself constrained. Returns true if it created an array runtime
```

### ada83.c:50883

```
// descriptor (the record then needs its own offset/size descriptor).
```

### ada83.c:50884

```
// A single bound that references an enclosing discriminant cannot be frozen at
```

### ada83.c:50885

```
// type elaboration — the discriminant is bound only when the object is
```

### ada83.c:50886

```
// initialized. Such a constraint is deferred to that point (the discriminant-
```

### ada83.c:50887

```
// dependent record machinery handles it), and it must NOT take the runtime
```

### ada83.c:50888

```
// type-descriptor path (which is for variable/function-bound components).
```

### ada83.c:50904

```
// RM 3.7.1: the non-discriminant expressions in a discriminant-dependent
```

### ada83.c:50905

```
// component's array index constraint (the INIT of ARR (INIT .. D)) are
```

### ada83.c:50906

```
// evaluated ONCE, when the record type is elaborated — not when each object is
```

### ada83.c:50907

```
// created, and not re-evaluated on each access. Freeze each into a global so a
```

### ada83.c:50908

```
// later change to the variable does not perturb the component's bounds.
```

### ada83.c:50934

```
// Disc-dependent layout, but its non-disc bounds still freeze now.
```

### ada83.c:50973

```
// Forward declaration - if imported, emit extern declaration. Deferred to
```

### ada83.c:50974

```
// module scope so a generic instantiation (expanded inside a function
```

### ada83.c:50975

```
// body) does not place a `declare` where an instruction is expected.
```

### ada83.c:50980

```
// RM 3.9: a forward spec inside a declarative part may be called before
```

### ada83.c:50981

```
// its body is reached. Give it an elaboration flag and reset it false at
```

### ada83.c:50982

```
// this elaboration point; the body sets it true, and a call checks it
```

### ada83.c:50983

```
// (access-before-elaboration). Skip imported subprograms (the body lives
```

### ada83.c:50984

```
// elsewhere and is always elaborated by the time it can be called).
```

### ada83.c:50988

```
// External linkage (not internal): a SEPARATE subunit's module reads
```

### ada83.c:50989

```
// this flag by the same stable name when it calls back into the parent.
```

### ada83.c:51002

```
// Nested package spec: emit object declarations for variables and                            
```

### ada83.c:51003

```
// constants declared in the visible and private parts.                                      
```

### ada83.c:51004

```
// Without this, variables from package specs without bodies                                  
```

### ada83.c:51005

```
// would never be allocated, causing undefined value errors.                                 
```

### ada83.c:51006

```
//                                                                                            
```

### ada83.c:51012

```
// Record which pending-activation entries this spec created
```

### ada83.c:51013

```
// (RM 9.3(3)): a body activates them at its begin; with no body they
```

### ada83.c:51014

```
// stay pending until the enclosing master's begin.
```

### ada83.c:51019

```
// RM 9.3: a LIBRARY package spec WITHOUT a body still activates the
```

### ada83.c:51020

```
// tasks its declarations create. Synthesize the <pkg>___elab a body
```

### ada83.c:51021

```
// would have provided — starting each single-task object — and join
```

### ada83.c:51022

```
// the elaboration order exactly like a body would (ca5004a: T : TSK
```

### ada83.c:51023

```
// in bodyless CA5004A1 is entry-called during a DEPENDENT body's
```

### ada83.c:51024

```
// elaboration, so it must be running by then).
```

### ada83.c:51054

```
// The unit's deferred library-scope elaborations (dynamic
```

### ada83.c:51055

```
// initializers, subtype checks, overlays) drain here when no
```

### ada83.c:51056

```
// body will host them — this define preempts the spec-only
```

### ada83.c:51057

```
// synthesis at the compilation-unit tail (elab_defined).
```

### ada83.c:51073

```
// TASK X IS ... — a single task; its function is its own.
```

### ada83.c:51099

```
// T : TSK — an object of a task TYPE (possibly one WITH'd
```

### ada83.c:51100

```
// from another unit); its function is the type's.
```

### ada83.c:51125

```
// RM 7.4: Deferred constant completion - copy the private part                             
```

### ada83.c:51126

```
// completion's value to the visible part deferred constant's                               
```

### ada83.c:51127

```
// storage so that references from outside the package see the                              
```

### ada83.c:51128

```
// correct initialized value. Match by base name (case-insensitive).                      
```

### ada83.c:51129

```
//                                                                                          
```

### ada83.c:51139

```
// Search visible_decls for a matching deferred constant
```

### ada83.c:51144

```
// not deferred
```

### ada83.c:51149

```
// same symbol
```

### ada83.c:51152

```
// Found matching deferred constant - emit memcpy
```

### ada83.c:51169

```
// Check for duplicate in deferred list
```

### ada83.c:51173

```
// RM 3.9: the body's declaration point is where it becomes elaborated.
```

### ada83.c:51174

```
// Set its flag true here so calls reached afterward pass the check; calls
```

### ada83.c:51175

```
// reached earlier (in preceding initializations) still saw false. A
```

### ada83.c:51176

```
// LIBRARY-level body is generated at module scope, where a store is not
```

### ada83.c:51177

```
// legal IR — its declaration point is its slot in the program's
```

### ada83.c:51178

```
// elaboration sequence, so defer the store to main's elaboration walk
```

### ada83.c:51179

```
// (c39005a).
```

### ada83.c:51194

```
// Defer nested subprogram bodies - emit after enclosing function
```

### ada83.c:51195

```
// Skip if already generated (prevents duplicates from re-processing)
```

### ada83.c:51205

```
// RM 3.9: a (generic) package body's declaration point is where it
```

### ada83.c:51206

```
// becomes elaborated; set its flag true so a later instantiation passes
```

### ada83.c:51207

```
// the access-before-elaboration check. Library-level bodies defer the
```

### ada83.c:51208

```
// store to main's elaboration walk — module scope admits no store.
```

### ada83.c:51223

```
// A package-body stub (PACKAGE BODY X IS SEPARATE;): the subunit's
```

### ada83.c:51224

```
// module defines @<x>___elab(ptr) under the same stable name; this
```

### ada83.c:51225

```
// module declares it and calls it at the stub's elaboration point
```

### ada83.c:51226

```
// with the enclosing frame (RM 10.2: the subunit's body elaborates
```

### ada83.c:51227

```
// where the stub stands). A GENERIC's stub sets only the elaboration
```

### ada83.c:51228

```
// flag above: elaborating a generic body establishes it for
```

### ada83.c:51229

```
// instantiation and runs nothing (RM 12.2) — the body's code is
```

### ada83.c:51230

```
// cloned into each instance, so no ___elab exists to call.
```

### ada83.c:51236

```
// The declaration is deferred to the post-generation flush: in a
```

### ada83.c:51237

```
// single-file program the subunit's define lands in THIS module,
```

### ada83.c:51238

```
// and a textual declare alongside it is a redefinition error.
```

### ada83.c:51262

```
// Use symbol from semantic analysis
```

### ada83.c:51264

```
// A GENERIC package body emits no code of its own — the body's
```

### ada83.c:51265

```
// declarations and statements are cloned into each instance
```

### ada83.c:51266

```
// (RM 12.2). This precedes the subunit path below: a generic
```

### ada83.c:51267

```
// subunit body must not become a runnable ___elab that executes
```

### ada83.c:51268

```
// the template's statements against unbound formals.
```

### ada83.c:51272

```
// A SUBUNIT package body whose parent chain nests in a subprogram:
```

### ada83.c:51273

```
// all its state lives in the PARENT's frame (offsets agree — both
```

### ada83.c:51274

```
// compilations resolve the same parent source), so the whole body
```

### ada83.c:51275

```
// emits as one elaboration function over that frame. %__frame_base
```

### ada83.c:51276

```
// aliases %__parent_frame and the ordinary frame-mode declaration
```

### ada83.c:51277

```
// machinery lays the package's variables into their shared slots.
```

### ada83.c:51278

```
// Only the subunit ITSELF opens the function: a package body nested
```

### ada83.c:51279

```
// inside it elaborates inline in the already-open ___elab
```

### ada83.c:51280

```
// (c83f01d1's PACKAGE BODY P) — a fresh define mid-function is
```

### ada83.c:51281

```
// malformed IR.
```

### ada83.c:51307

```
// The SPEC's declarations elaborated at their own declaration
```

### ada83.c:51308

```
// point inside the parent's module (RM 7.2); only the body's
```

### ada83.c:51309

```
// declarative part and statements belong to this function.
```

### ada83.c:51322

```
// Nested subprogram bodies deferred during the elaboration
```

### ada83.c:51323

```
// function emit now, at module level — the subunit's whole
```

### ada83.c:51324

```
// payload (ca1002's P) lives here.
```

### ada83.c:51329

```
// For library-level package bodies (separate compilation), emit the                        
```

### ada83.c:51330

```
// associated spec's visible/private declarations as globals. The spec                     
```

### ada83.c:51331

```
// is loaded from a separate .ads file and never visited by codegen,                        
```

### ada83.c:51332

```
// so constants like LEGAL_FILE_NAME must be emitted here.                                 
```

### ada83.c:51333

```
// Skip this for nested packages - their specs are already in the                           
```

### ada83.c:51334

```
// enclosing scope's declaration list and have already been emitted.                       
```

### ada83.c:51335

```
//                                                                                          
```

### ada83.c:51341

```
// Ownership of the spec's globals (linkage): when the spec
```

### ada83.c:51342

```
// REQUIRES a body, consumers reference them externally and
```

### ada83.c:51343

```
// this body's module is the one authoritative definition
```

### ada83.c:51344

```
// (strong). When the body is optional, the spec's own
```

### ada83.c:51345

```
// compilation (and every consumer that inlined it) already
```

### ada83.c:51346

```
// defines them — this re-emission is a discardable copy.
```

### ada83.c:51357

```
// RM 9.3(3): a nested package body's `begin` activates the tasks of
```

### ada83.c:51358

```
// its own declarative part AND the tasks its SPEC declared (a
```

### ada83.c:51359

```
// bodyless package's spec tasks instead wait for the enclosing
```

### ada83.c:51360

```
// master's begin). Entries stay recorded for the enclosing join.
```

### ada83.c:51361

```
// Activation runs INSIDE the guarded elaboration below — the body's
```

### ada83.c:51362

```
// handlers apply to activation failures (RM 9.3/11.4, c93007a).
```

### ada83.c:51363

```
// A generic instance's symbol points its `declaration` at the
```

### ada83.c:51364

```
// instantiation node; the recorded activation range lives on the
```

### ada83.c:51365

```
// resolved SPEC CLONE (cc1207b: the instance body's begin must
```

### ada83.c:51366

```
// activate the instance's spec task before calling its entry).
```

### ada83.c:51376

```
// Check if the package spec OR BODY has any single task declarations
```

### ada83.c:51377

```
// that need starting at elaboration (RM 9.2: tasks are activated at
```

### ada83.c:51378

```
// the end of the declarative region containing the task declaration).
```

### ada83.c:51379

```
//
```

### ada83.c:51400

```
// Generate initialization/elaboration function if the package has                            
```

### ada83.c:51401

```
// init statements OR package-level tasks that need starting.                                
```

### ada83.c:51402

```
// For nested packages (inside a function), emit statements inline                            
```

### ada83.c:51403

```
// in the enclosing function - Ada RM 7.2: elaboration occurs at the                          
```

### ada83.c:51404

```
// point of the package body in the enclosing declarative region.                            
```

### ada83.c:51405

```
// For library-level packages, create a separate __elab function.                            
```

### ada83.c:51406

```
//                                                                                            
```

### ada83.c:51407

```
// Nested package: emit the begin — task activation, then the
```

### ada83.c:51408

```
// initialization statements — inline, under the body's handlers
```

### ada83.c:51409

```
// (RM 11.4). Runs even without statements: activation alone still
```

### ada83.c:51410

```
// belongs to the body's begin.
```

### ada83.c:51417

```
// Library-level package: emit the elaboration function that runs the
```

### ada83.c:51418

```
// deferred library-level initializers, starts package-level tasks,
```

### ada83.c:51419

```
// and runs any initialization statements. Emitted even when empty:
```

### ada83.c:51420

```
// a dependent compiled against the spec alone calls @<pkg>___elab
```

### ada83.c:51421

```
// as an external reference, so the body's compilation must always
```

### ada83.c:51422

```
// define it (RM 10.5).
```

### ada83.c:51428

```
// A subunit package body runs its elaboration as a function over
```

### ada83.c:51429

```
// the parent's frame, exactly like a task body: uniform
```

### ada83.c:51430

```
// @<name>___elab(ptr) ABI (callers pass the frame, or null at
```

### ada83.c:51431

```
// library level), with %__frame. aliases for the parent's
```

### ada83.c:51432

```
// frame-resident variables.
```

### ada83.c:51456

```
// The frame owner is the parent itself when the parent IS a
```

### ada83.c:51457

```
// subprogram (stub directly inside a procedure body); otherwise
```

### ada83.c:51458

```
// the nearest subprogram enclosing the parent package.
```

### ada83.c:51465

```
// A tree-copy instance's formal-object bindings elaborate first:
```

### ada83.c:51466

```
// the declarative part's initializers may read them (RM 12.3(80)).
```

### ada83.c:51470

```
// RM 7.2: elaborate the declarative part first — store each deferred
```

### ada83.c:51471

```
// library-level initializer (declaration order), before the package's
```

### ada83.c:51472

```
// tasks activate and before its initialization statements run.
```

### ada83.c:51475

```
// Start package-level tasks declared in the spec or the body.
```

### ada83.c:51488

```
// Find the task object variable symbol
```

### ada83.c:51504

```
// Start the task and store handle in the global
```

### ada83.c:51515

```
// Run initialization statements under the body's exception handlers
```

### ada83.c:51516

```
// (RM 11.4): a library package body's BEGIN..EXCEPTION..END must catch
```

### ada83.c:51517

```
// an exception raised while elaborating, just as a nested body does.
```

### ada83.c:51529

```
// Nested subprograms declared in the body's declarative part or its
```

### ada83.c:51530

```
// statement blocks (c39006f2's CHECK) deferred while the elaboration
```

### ada83.c:51531

```
// function was open; emit them now, at module level.
```

### ada83.c:51534

```
// Track this elaboration function for calling from main.
```

### ada83.c:51535

```
// Also register with the §15 elaboration graph for
```

### ada83.c:51536

```
// proper dependency-ordered elaboration.
```

### ada83.c:51537

```
//
```

### ada83.c:51541

```
// Register unit in elaboration graph (§15)
```

### ada83.c:51542

```
// is_body
```

### ada83.c:51545

```
// has_elab_code
```

### ada83.c:51552

```
// Generate code for generic instantiation. Per Ada RM 12.3: "The
```

### ada83.c:51553

```
// elaboration of a generic instantiation declares an instance"
```

### ada83.c:51559

```
// RM 3.9: instantiating a generic whose body has not yet elaborated
```

### ada83.c:51560

```
// raises PROGRAM_ERROR.
```

### ada83.c:51563

```
// A package instantiation that textually preceded the generic
```

### ada83.c:51564

```
// body (or whose body is a SEPARATE subunit) completes now that
```

### ada83.c:51565

```
// the body exists (RM 12.2).
```

### ada83.c:51571

```
// Tree-copy package instance: bindings elaborate at the
```

### ada83.c:51572

```
// instantiation point, then the resolved spec and body clones emit
```

### ada83.c:51573

```
// through the ordinary package paths — globals, subprogram bodies,
```

### ada83.c:51574

```
// and the body's statement sequence exactly as if written here
```

### ada83.c:51575

```
// (RM 12.3, 7.2).
```

### ada83.c:51583

```
// Bodyless library instance: the bindings (and the spec's
```

### ada83.c:51584

```
// pending initializers, drained below) get their own ___elab.
```

### ada83.c:51585

```
// With a body, the package-body emitter's ___elab elaborates
```

### ada83.c:51586

```
// the bindings first instead.
```

### ada83.c:51610

```
// A library-level instantiation is a library unit: its
```

### ada83.c:51611

```
// binding elaboration joins the §15 graph like any body.
```

### ada83.c:51623

```
// A subprogram instantiation that textually preceded the generic
```

### ada83.c:51624

```
// body resolves its clone now that the body exists (RM 12.2: the
```

### ada83.c:51625

```
// body serves every instantiation).
```

### ada83.c:51630

```
// Tree-copy subprogram instance: its resolved clone emits as an
```

### ada83.c:51631

```
// ordinary subprogram body — deferred when nested, exactly like a
```

### ada83.c:51632

```
// directly written nested body. No instance context is needed.
```

### ada83.c:51639

```
// Elaborate the formal-object bindings at the instantiation
```

### ada83.c:51640

```
// point (RM 12.3(80)). At library level the initialization code
```

### ada83.c:51641

```
// needs a function to live in: give the instance an ___elab and
```

### ada83.c:51642

```
// register it with the elaboration list.
```

### ada83.c:51668

```
// A library-level instantiation is a library unit: its
```

### ada83.c:51669

```
// binding elaboration joins the §15 graph like any body.
```

### ada83.c:51684

```
// Find the variable symbol in the type symbol's defining scope.                               
```

### ada83.c:51685

```
// The type and variable were both added to the same scope during                               
```

### ada83.c:51686

```
// semantic analysis. Use defining_scope instead of sm->current_scope                           
```

### ada83.c:51687

```
// since current_scope may have changed during code generation.                                
```

### ada83.c:51688

```
//                                                                                              
```

### ada83.c:51691

```
// Generic declarations don't generate code - only instances do.
```

### ada83.c:51692

```
// RM 3.9: instantiating a generic whose body is not yet elaborated
```

### ada83.c:51693

```
// raises PROGRAM_ERROR. Give the generic an elaboration flag, reset it
```

### ada83.c:51694

```
// false here; its body sets it true, and an instantiation checks it.
```

### ada83.c:51695

```
// Only a generic that actually has a body can be instantiated too early;
```

### ada83.c:51696

```
// a bodyless generic package has nothing to elaborate and is always safe.
```

### ada83.c:51712

```
// Task type/object specification - record entry points
```

### ada83.c:51716

```
// RM 3.9 / AI-00149: a task type can be used to create and ACTIVATE a
```

### ada83.c:51717

```
// task before its body is elaborated (NEW TT before TASK BODY TT), which
```

### ada83.c:51718

```
// must raise PROGRAM_ERROR. Give the type an elaboration flag reset here;
```

### ada83.c:51719

```
// the body sets it true and each activation checks it.
```

### ada83.c:51733

```
// RM 3.1/9.1: elaborating a task specification elaborates its entry
```

### ada83.c:51734

```
// declarations in textual order, including the discrete range of any
```

### ada83.c:51735

```
// entry family index subtype — whose bound expressions may have side
```

### ada83.c:51736

```
// effects that must occur at the spec's elaboration point, in order
```

### ada83.c:51737

```
// (c91006a). The per-call family-index check re-derives these bounds for
```

### ada83.c:51738

```
// its range test; here we evaluate them purely for that elaboration
```

### ada83.c:51739

```
// effect.
```

### ada83.c:51754

```
// RM 3.5(4): the bounds of a non-null index range must each belong to
```

### ada83.c:51755

```
// the index subtype; otherwise CONSTRAINT_ERROR is raised here, at the
```

### ada83.c:51756

```
// spec's elaboration, so no task of this declarative part is ever
```

### ada83.c:51757

```
// activated (c91007a). A null range (low > high) is unconstrained.
```

### ada83.c:51780

```
// For single tasks (not task types), allocate task control block storage
```

### ada83.c:51781

```
// and start the task body in a separate thread
```

### ada83.c:51801

```
// Package-level tasks need global storage, not alloca.                                  
```

### ada83.c:51802

```
// alloca is only valid inside a function. For package-level                             
```

### ada83.c:51803

```
// tasks, emit a global variable here and defer the task_start                            
```

### ada83.c:51804

```
// to the package body elaboration function (RM 9.2).                                    
```

### ada83.c:51805

```
// Emit global variable for the task control block (once only)                            
```

### ada83.c:51806

```
//                                                                                        
```

### ada83.c:51814

```
// Task start is deferred to package body elaboration
```

### ada83.c:51815

```
// Allocate task object - use frame if nested, else stack
```

### ada83.c:51817

```
// A package-level task object of a generic instance is module
```

### ada83.c:51818

```
// storage — every function of the instance names @<sym>, never a
```

### ada83.c:51819

```
// frame slot of whichever function elaborated the instantiation.
```

### ada83.c:51839

```
// Create the task control block now, but DEFER thread start to the
```

### ada83.c:51840

```
// master's activation point (RM 9.3). Record the object so the
```

### ada83.c:51841

```
// enclosing declarative part activates it at `begin`.
```

### ada83.c:51846

```
// Pass the static link for uplevel access (null at library level)
```

### ada83.c:51847

```
// and the enclosing master record (RM 9.4).
```

### ada83.c:51851

```
// Store the TCB pointer in the task object.
```

### ada83.c:51869

```
// RM 3.9: the body's declaration point is where the task type becomes
```

### ada83.c:51870

```
// elaborated. Set its flag true so an activation reached afterward
```

### ada83.c:51871

```
// passes; one reached earlier (NEW TT above) still saw false.
```

### ada83.c:51879

```
// Defer task body generation when inside another function
```

### ada83.c:51887

```
// RM §3.3.1: Type elaboration. For types with static bounds,                                  
```

### ada83.c:51888

```
// no code is needed. For constrained array types whose bounds are                             
```

### ada83.c:51889

```
// runtime expressions (BOUND_EXPR), evaluate bounds at elaboration                             
```

### ada83.c:51890

```
// time and store in globals for use by record layouts, alloca, etc.                           
```

### ada83.c:51891

```
// For record types with dynamic-sized components, compute and store                            
```

### ada83.c:51892

```
// cumulative byte offsets and total size in globals.                                          
```

### ada83.c:51893

```
//                                                                                              
```

### ada83.c:51899

```
// ── Array type with expression bounds ──────────────────────────────────────────────────────
```

### ada83.c:51903

```
// RM 3.5(4)/3.6.1: scalar subtype indications elaborated as part of
```

### ada83.c:51904

```
// this type declaration — array index constraints, an access type's
```

### ada83.c:51905

```
// designated subtype, a derived scalar type's own constraint (checked
```

### ada83.c:51906

```
// against the PARENT's range; a derived type is its own base) — have
```

### ada83.c:51907

```
// their ranges checked against the type mark here.
```

### ada83.c:51912

```
// An index constraint's bounds live in the dimension's
```

### ada83.c:51913

```
// Index_Info; the mark is the dimension's index type.
```

### ada83.c:51926

```
// RM 3.6.1: a constrained access-to-array subtype's index constraint
```

### ada83.c:51927

```
// (TYPE T IS NEW ACC_ARR (0 .. 11)) must lie within the designated
```

### ada83.c:51928

```
// array's index subtype, just like a direct array subtype.
```

### ada83.c:51939

```
// A derived type's range constraint lives on the parent subtype
```

### ada83.c:51940

```
// indication's (possibly anonymous) constrained subtype; check
```

### ada83.c:51941

```
// that subtype against ITS type mark. The derived type's own
```

### ada83.c:51942

```
// bounds are copies of the parent's, so the direct comparison
```

### ada83.c:51943

```
// is the no-constraint case's no-op.
```

### ada83.c:51949

```
// RM 3.7.2: elaborating a constrained discriminated record subtype as
```

### ada83.c:51950

```
// this type checks its discriminant-dependent component constraints —
```

### ada83.c:51951

```
// whether the record is the type itself (a derived type NEW CONS (1)),
```

### ada83.c:51952

```
// the element subtype of an array (ARRAY (..) OF CONS (1)), a component
```

### ada83.c:51953

```
// subtype of another record (C : CONS (1)), or an access type's
```

### ada83.c:51954

```
// designated subtype (ACCESS CONS (1)).
```

### ada83.c:51959

```
// RM 3.6.1(9): a constrained_array_definition elaborates its
```

### ada83.c:51960

```
// component subtype indication at type-declaration time, so an
```

### ada83.c:51961

```
// ARRAY OF ACC (V) with V out of range raises here (c37211e ARR
```

### ada83.c:51962

```
// case). Only fires for access-to-record element types — the
```

### ada83.c:51963

```
// helper returns early otherwise — so it does not add side
```

### ada83.c:51964

```
// effects for value-carrying record/array element types (which
```

### ada83.c:51965

```
// elaborate their constraints per object; c37010a).
```

### ada83.c:51978

```
// ── Record type with dynamic-sized components ──────────────────────────────────────────────
```

### ada83.c:51982

```
// Detection only (creates array descriptor globals, emits no code, so
```

### ada83.c:51983

```
// it is safe at package level): does any component need an array
```

### ada83.c:51984

```
// runtime descriptor, making the record itself dynamically sized?
```

### ada83.c:51993

```
// RM 3.6 / 3.8: the components' non-static constraints are evaluated
```

### ada83.c:51994

```
// once each, in declaration order. Freeze every kind (dynamic array,
```

### ada83.c:51995

```
// scalar range, discriminant, access designated) in that single
```

### ada83.c:51996

```
// ordered pass (emits code, so function context only).
```

### ada83.c:52001

```
// The fixed parts of a disc-dependent component constraint are now
```

### ada83.c:52002

```
// frozen; check them against their discriminant subtypes at this
```

### ada83.c:52003

```
// full-declaration point for an indefinite type (RM 3.7.2, c37214a).
```

### ada83.c:52006

```
// If this declaration completes an incomplete type, flush the
```

### ada83.c:52007

```
// discriminant-dependent component checks deferred from constrained
```

### ada83.c:52008

```
// views built against it while incomplete (RM 3.8.1, c37216a).
```

### ada83.c:52011

```
// RM 3.5(4): a scalar component's ANONYMOUS subtype indication
```

### ada83.c:52012

```
// (`T RANGE L..H` with no name) must have its range fall inside T
```

### ada83.c:52013

```
// when the record elaborates. Skip when the component type carries
```

### ada83.c:52014

```
// a defining symbol — that's a named TYPE / SUBTYPE that has its
```

### ada83.c:52015

```
// own elaboration path, and comparing its bounds against its base
```

### ada83.c:52016

```
// would misfire on user integer types like SYSTEM.ADDRESS whose
```

### ada83.c:52017

```
// range legitimately extends past INTEGER's.
```

### ada83.c:52045

```
// The component array type's bounds were already elaborated in
```

### ada83.c:52046

```
// the ordered freezing pass above; just read the frozen size.
```

### ada83.c:52059

```
// RM 3.7.2: During type elaboration, check discriminant constraint                           
```

### ada83.c:52060

```
// values for any constrained record types referenced by this type                            
```

### ada83.c:52061

```
// (array element types, access designated types, derived types,                              
```

### ada83.c:52062

```
// record components).                                                                       
```

### ada83.c:52063

```
//                                                                                            
```

### ada83.c:52087

```
// An access component's constraint lives on its designated subtype
```

### ada83.c:52088

```
// (AC1 : ACR (F)); freeze that per component.
```

### ada83.c:52096

```
// (A non-disc-dependent dynamic discriminant constraint is frozen in
```

### ada83.c:52097

```
// the ordered per-component pass above; the checks read it.)
```

### ada83.c:52104

```
// Subtype elaboration constraint check (RM 3.3.2(7)):                                          
```

### ada83.c:52105

```
// For "subtype S is T range L..H", check that L and H are                                      
```

### ada83.c:52106

```
// within T's range. Raise CONSTRAINT_ERROR if not.                                            
```

### ada83.c:52107

```
//                                                                                              
```

### ada83.c:52113

```
// A library-scope subtype declaration elaborates inside the owning
```

### ada83.c:52114

```
// unit's ___elab function — module scope admits no instructions, and
```

### ada83.c:52115

```
// dropping the checks would skip required RM 3.3.2 elaboration
```

### ada83.c:52116

```
// semantics.
```

### ada83.c:52123

```
// RM 3.3.1: a constrained array subtype with runtime bounds (e.g.
```

### ada83.c:52124

```
// ARRAY_TYPE (1 .. IDENT_INT (5))) elaborates its bounds here so its
```

### ada83.c:52125

```
// @__rt_type globals are populated before any use.
```

### ada83.c:52128

```
// RM 3.3.2 / 3.5(4): a range constraint with dynamic bounds is
```

### ada83.c:52129

```
// checked against the type mark's range when the SUBTYPE elaborates
```

### ada83.c:52130

```
// (a non-null range with a bound outside the type mark raises
```

### ada83.c:52131

```
// CONSTRAINT_ERROR here, not at first use).
```

### ada83.c:52134

```
// RM 3.5/3.6.1: an array subtype's index constraint (SUBTYPE S IS
```

### ada83.c:52135

```
// WEEK_ARRAY (MID_WEEK RANGE MON..WED)) checks each discrete range against
```

### ada83.c:52136

```
// its index subtype and, when written `S RANGE L..H`, against S — exactly
```

### ada83.c:52137

```
// as a constrained array TYPE declaration does.
```

### ada83.c:52150

```
// RM 3.7.2: a constrained discriminated record subtype (SUBTYPE S IS
```

### ada83.c:52151

```
// CONS (1)) checks its discriminant-dependent component constraints when
```

### ada83.c:52152

```
// the subtype elaborates. Access-to-record subtypes (SUBTYPE SUBACC IS
```

### ada83.c:52153

```
// ACC (VAL)) get their disc-constraint range check here too (c37211e).
```

### ada83.c:52159

```
// RM 3.5.7: a floating point subtype's DIGITS must not exceed the base
```

### ada83.c:52160

```
// type's DIGITS; an incompatible accuracy constraint raises
```

### ada83.c:52161

```
// CONSTRAINT_ERROR at elaboration.
```

### ada83.c:52168

```
// RM 3.5.9: a fixed point subtype's DELTA must not be finer (smaller)
```

### ada83.c:52169

```
// than the base type's DELTA; an incompatible accuracy raises
```

### ada83.c:52170

```
// CONSTRAINT_ERROR at elaboration.
```

### ada83.c:52177

```
// RM 3.3.2: Only check when an explicit range constraint is given.
```

### ada83.c:52178

```
// "SUBTYPE S IS T" (no constraint) need not be checked.
```

### ada83.c:52179

```
//
```

### ada83.c:52180

```
// A range to check is present for a bare RANGE constraint or for a real
```

### ada83.c:52181

```
// accuracy constraint (DIGITS/DELTA) that also carries a RANGE.
```

### ada83.c:52199

```
// Only check scalar subtypes with both bounds known
```

### ada83.c:52209

```
// Check if subtype range is a non-null range
```

### ada83.c:52220

```
// Only non-null ranges need checking (RM 3.3.2(8))
```

### ada83.c:52223

```
// Static violation: always raise
```

### ada83.c:52231

```
// RM 3.7.2: For record subtypes with discriminant constraints,                               
```

### ada83.c:52232

```
// check that each constraint value lies within the discriminant's                            
```

### ada83.c:52233

```
// subtype range. Raise CONSTRAINT_ERROR if not.                                             
```

### ada83.c:52234

```
//                                                                                            
```

### ada83.c:52243

```
// RM 8.5: elaborating a renaming declaration evaluates the renamed
```

### ada83.c:52244

```
// name. For an entry rename that means the task prefix and any family
```

### ada83.c:52245

```
// index bind NOW — their variables may change later without affecting
```

### ada83.c:52246

```
// what the rename denotes. Store both into the rename's hidden locals;
```

### ada83.c:52247

```
// entry calls through the rename read them back.
```

### ada83.c:52251

```
// An attribute renaming carries a synthesized wrapper body
```

### ada83.c:52252

```
// (RM 8.5(3), c85016a) — emit it like an instance wrapper.
```

### ada83.c:52271

```
// A slot owned by the current function's frame gets its local name
```

### ada83.c:52272

```
// bound here; a slot in an enclosing frame (a rename inside a task
```

### ada83.c:52273

```
// body or nested subprogram) is reached through the %__frame. alias
```

### ada83.c:52274

```
// the function header already emitted — Emit_Symbol_Storage picks.
```

### ada83.c:52316

```
// Address clause elaboration (RM 13.5): emit the hidden cell's storage,
```

### ada83.c:52317

```
// store the evaluated address into it, then perform the overlaid
```

### ada83.c:52318

```
// object's declared initialization through the overlay (the assignment
```

### ada83.c:52319

```
// redirects via the object's renamed_object dereference).
```

### ada83.c:52321

```
// A library-scope representation clause elaborates inside the owning
```

### ada83.c:52322

```
// unit's ___elab function: a STORAGE_SIZE expression evaluates and
```

### ada83.c:52323

```
// freezes, an RM 13.5 overlay evaluates and stores its target address.
```

### ada83.c:52324

```
// Module scope admits no instructions.
```

### ada83.c:52336

```
// A 'STORAGE_SIZE specification elaborates here (RM 13.2): a non-static
```

### ada83.c:52337

```
// expression is evaluated once and frozen for the attribute to read
```

### ada83.c:52338

```
// back, and an ACCESS type's collection additionally gets its budget
```

### ada83.c:52339

```
// global — the remaining storage units every allocator debits.
```

### ada83.c:52409

```
// Other declaration kinds that need no codegen
```

### ada83.c:52428

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:52429

```
// §13.6.2 Implicit Equality Function Generation                                                      
```

### ada83.c:52430

```
//                                                                                                  
```

### ada83.c:52431

```
// Generate equality functions for composite types at freeze points.                               
```

### ada83.c:52432

```
// Per RM 4.5.2, equality is predefined for all non-limited types.                                 
```

### ada83.c:52433

```
// The RM specifies the semantics and the compiler provides the implementation.                    
```

### ada83.c:52434

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:52440

```
// Emit function definition with linkonce_odr for linker deduplication
```

### ada83.c:52446

```
// Save and reset temp counter for this function
```

### ada83.c:52448

```
// Start after %0 and %1
```

### ada83.c:52451

```
// Empty record - always equal
```

### ada83.c:52456

```
// Per-component record equality is a single algorithm. Materialize the
```

### ada83.c:52457

```
// two pointer parameters as temps and delegate to Generate_Record_Equality
```

### ada83.c:52458

```
// (the recursive source of truth) instead of carrying a second copy of the
```

### ada83.c:52459

```
// component-type dispatch here.
```

### ada83.c:52469

```
// Constrained array with static bounds - use memcmp
```

### ada83.c:52471

```
// A memcmp is unsound when the element is a record with a variant part or
```

### ada83.c:52472

```
// a discriminant-dependent component (RM 4.5.2): delegate to the inline
```

### ada83.c:52473

```
// array equality, which compares such elements one at a time respecting
```

### ada83.c:52474

```
// the active discriminant. Materialize the pointer parameters as temps so
```

### ada83.c:52475

```
// the shared routine can index from them.
```

### ada83.c:52492

```
// Fat-pointer array (unconstrained or dynamic-bound constrained).
```

### ada83.c:52493

```
// Materialize the two fat parameters as ordinary registers and delegate
```

### ada83.c:52494

```
// to Generate_Array_Equality — the recursive source of truth, which
```

### ada83.c:52495

```
// compares every dimension's length and handles elements that need
```

### ada83.c:52496

```
// componentwise comparison (RM 4.5.2).
```

### ada83.c:52509

```
// Unknown composite type - conservatively return false (not equal).
```

### ada83.c:52510

```
// This is safer than the previous "always equal" default.
```

### ada83.c:52517

```
// Restore temp counter
```

### ada83.c:52521

```
// Generate equality functions for all frozen composite types
```

### ada83.c:52527

```
// Generate global constants for exception identities
```

### ada83.c:52530

```
// Generate globals for all registered exceptions (from declarations).                           
```

### ada83.c:52531

```
// Dedup by mangled name to avoid redefinition when the same exception                            
```

### ada83.c:52532

```
// is declared in multiple WITH'd packages.                                                      
```

### ada83.c:52533

```
//                                                                                                
```

### ada83.c:52541

```
// Get mangled name to check for duplicates
```

### ada83.c:52566

```
// Also emit globals for all referenced exception names that weren't                              
```

### ada83.c:52567

```
// already emitted above (e.g., instance-prefixed exceptions from                                 
```

### ada83.c:52568

```
// generic instantiations like SEQ_IO.NAME_ERROR).                                               
```

### ada83.c:52569

```
// Build set of already-emitted names for dedup                                                   
```

### ada83.c:52570

```
//                                                                                                
```

### ada83.c:52589

```
// Also include standard exception names
```

### ada83.c:52598

```
// Check against emitted declaration names
```

### ada83.c:52603

```
// Check against standard exceptions
```

### ada83.c:52610

```
// Also check against previously emitted exc_refs (dedup within the list)
```

### ada83.c:52623

```
// Generate extern declarations for all loaded package specs
```

### ada83.c:52629

```
// Iterate through WITH'd packages
```

### ada83.c:52638

```
// Skip extern declarations for packages whose bodies will be code-generated.
```

### ada83.c:52639

```
// Those symbols will be defined, not external.
```

### ada83.c:52642

```
// For ALI-loaded packages without declaration, use exported[] array
```

### ada83.c:52661

```
// Done with this package
```

### ada83.c:52667

```
// Emit extern for each subprogram and object in the package
```

### ada83.c:52679

```
// Emit extern for object declarations (constants/variables)
```

### ada83.c:52697

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:52698

```
// §13.11 Compilation Unit Code Generation                                                           
```

### ada83.c:52699

```
//                                                                                                  
```

### ada83.c:52700

```
// A compilation unit is the quantum of separate compilation.                                      
```

### ada83.c:52701

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:52703

```
// Digit-scan loop of a literal exponent: digits with embedded single
```

### ada83.c:52704

```
// underscores accumulate (an exponent is always written in base ten) into
```

### ada83.c:52705

```
// <label_prefix>_v, branching to apply_label at end of input or to %bad on
```

### ada83.c:52706

```
// a malformed exponent. Entry is from <label_prefix>_fc_plus with the scan
```

### ada83.c:52707

```
// start index in start_value. underscore_after_value and
```

### ada83.c:52708

```
// underscore_clear_value name the two underscore-legality scratch values,
```

### ada83.c:52709

```
// which the two instantiations spell differently.
```

### ada83.c:52730

```
// Must have seen a digit and not end on an underscore (no trailing '_').
```

### ada83.c:52747

```
// Underscore allowed only immediately after a digit (no leading, trailing,
```

### ada83.c:52748

```
// or consecutive underscores in the exponent).
```

### ada83.c:52774

```
// Power loop applying a scanned exponent: <label_prefix>p_acc becomes
```

### ada83.c:52775

```
// multiplier ** exponent by repeated multiplication, entered from
```

### ada83.c:52776

```
// apply_label with the exponent in exponent_value, exiting to sign_label.
```

### ada83.c:52799

```
// Final negate-and-return of a parsed magnitude: apply the sign captured in
```

### ada83.c:52800

```
// %is_neg to `value` and return it, naming the temporaries after `stem`.
```

### ada83.c:52811

```
// Idempotent; guarantees the inherited
```

### ada83.c:52812

```
// context is present for extern emission.
```

### ada83.c:52814

```
// A subunit grafted into its generic parent's template emits only
```

### ada83.c:52815

```
// through instantiations — nothing stands alone here.
```

### ada83.c:52818

```
// Generate LLVM module header (only once per file)
```

### ada83.c:52824

```
// External C library and LLVM intrinsic declarations
```

### ada83.c:52846

```
// Stack scoping for statements that alloca per execution (entry-call
```

### ada83.c:52847

```
// parameter blocks, selective-wait bookkeeping): a loop around them must
```

### ada83.c:52848

```
// not grow the frame each iteration.
```

### ada83.c:52869

```
// C file I/O (for TEXT_IO pragma Import)
```

### ada83.c:52885

```
// LLVM overflow intrinsics for GNAT-style arithmetic overflow checks (RM 4.5).
```

### ada83.c:52886

```
// Signed add/sub/mul with overflow detection at each standard width.
```

### ada83.c:52904

```
// Integer'VALUE - parse string to integer.
```

### ada83.c:52905

```
// Bound type derived from STRING's index type via type system.
```

### ada83.c:52909

```
// Cache the IR spellings of the two representations used pervasively below.
```

### ada83.c:52910

```
// LLVM_Rep_To_String yields a fixed string for a fixed representation, so
```

### ada83.c:52911

```
// hoisting these once collapses the long, repeated emit arguments.
```

### ada83.c:52915

```
// Integer'VALUE helper - Ada RM 3.5.5: full parsing with based literals.                        
```

### ada83.c:52916

```
// Handles: [spaces] [sign] decimal_literal | based_literal [spaces]                              
```

### ada83.c:52917

```
// decimal_literal ::= digit {[_] digit} [exponent]                                               
```

### ada83.c:52918

```
// based_literal   ::= base # hex_digit {[_] hex_digit} # [exponent]                              
```

### ada83.c:52919

```
// exponent        ::= E [+] digit {[_] digit}                                                    
```

### ada83.c:52920

```
// Raises CONSTRAINT_ERROR on any malformed input.                                               
```

### ada83.c:52921

```
//                                                                                                
```

### ada83.c:52929

```
// Copy to NUL-terminated C buffer for sscanf-like processing in C helper
```

### ada83.c:52937

```
// The actual parser is a C-callable function emitted as LLVM IR.                                
```

### ada83.c:52938

```
// It handles spaces, sign, decimal, based literals, exponent, underscores,                       
```

### ada83.c:52939

```
// and raises CONSTRAINT_ERROR on any validation failure.                                        
```

### ada83.c:52940

```
//                                                                                                
```

### ada83.c:52944

```
// Phase 1: skip leading spaces (only ASCII 32; HT/other > error)
```

### ada83.c:52959

```
// Phase 2: find end (skip trailing spaces; HT/other at end > error)
```

### ada83.c:52965

```
// Use `slt` (strict): st_i == sl_i is a valid single-character string
```

### ada83.c:52966

```
// (the char was already verified non-space in skip_lead). Only error if
```

### ada83.c:52967

```
// we ran past the leading position.
```

### ada83.c:52979

```
// Now sl_i..st_i is the trimmed content (inclusive)
```

### ada83.c:52981

```
// exclusive end
```

### ada83.c:52983

```
// Phase 3: check for sign
```

### ada83.c:52992

```
// Phase 4: Parse digit sequence, looking for '#' or ':' (based literal delimiter)
```

### ada83.c:53004

```
// Check for '#' or ':' > based literal
```

### ada83.c:53010

```
// Check for E/e > exponent
```

### ada83.c:53017

```
// Check for underscore
```

### ada83.c:53023

```
// Consecutive underscores or leading underscore > error
```

### ada83.c:53029

```
// Parse decimal digit
```

### ada83.c:53046

```
// Based literal: d1_val is the base, parse digits in that base until '#'/':'
```

### ada83.c:53048

```
// copy
```

### ada83.c:53050

```
// Validate base: 2..16
```

### ada83.c:53056

```
// remember delimiter
```

### ada83.c:53064

```
// missing closing delimiter
```

### ada83.c:53069

```
// Check for closing delimiter
```

### ada83.c:53073

```
// Check underscore
```

### ada83.c:53083

```
// Parse hex digit (0-9, A-F, a-f)
```

### ada83.c:53098

```
// 'A'-10 = 55
```

### ada83.c:53106

```
// 'a'-10 = 87
```

### ada83.c:53112

```
// Check digit < base
```

### ada83.c:53124

```
// After closing '#'/':' - check for optional exponent
```

### ada83.c:53135

```
// trailing junk
```

### ada83.c:53137

```
// Exponent for based literal: exp_start_b, base is still %%base. The syntax
```

### ada83.c:53138

```
// is the same as a decimal exponent (RM 2.4.2): an optional '+' (a negative
```

### ada83.c:53139

```
// exponent is illegal for an integer literal), then digits with embedded
```

### ada83.c:53140

```
// single underscores -- no leading, trailing, or consecutive underscores.
```

### ada83.c:53158

```
// Apply exponent for based literal: result = b_val * base^exp
```

### ada83.c:53168

```
// Decimal exponent path
```

### ada83.c:53171

```
// Check for trailing underscore before E
```

### ada83.c:53176

```
// Check first char after E: must be digit or '+'; '-' > error
```

### ada83.c:53189

```
// First char after E (or E+) must be a digit, not underscore or other
```

### ada83.c:53198

```
// Apply decimal exponent: result = mantissa * 10^exp
```

### ada83.c:53206

```
// Simple decimal result (no exponent, no base) - apply sign
```

### ada83.c:53209

```
// Check for trailing underscore
```

### ada83.c:53214

```
// Error: raise CONSTRAINT_ERROR
```

### ada83.c:53221

```
// Float'VALUE (RM 3.5.10). The fat-pointer data is a slice and is NOT
```

### ada83.c:53222

```
// NUL-terminated, and Ada literals admit '_' separators that C's strtod does
```

### ada83.c:53223

```
// not, so copy the characters into a fresh NUL-terminated buffer with the
```

### ada83.c:53224

```
// underscores removed before parsing. Validity is enforced by requiring
```

### ada83.c:53225

```
// strtod to consume the whole (blank-trimmed) buffer; otherwise the value
```

### ada83.c:53226

```
// has the wrong syntax and Constraint_Error is raised (which Text_IO turns
```

### ada83.c:53227

```
// into Data_Error). Based real literals are not handled here.
```

### ada83.c:53237

```
// Copy loop, dropping '_'.
```

### ada83.c:53264

```
// Reject if strtod consumed nothing.
```

### ada83.c:53267

```
// Allow only trailing blanks after the number.
```

### ada83.c:53289

```
// Both power helpers run at 64-bit width so every integer type's base
```

### ada83.c:53290

```
// values fit; the call site narrows the result back with a range check.
```

### ada83.c:53292

```
// Integer power function - signed, overflow-checked (RM 4.5.6).                                 
```

### ada83.c:53293

```
// Uses binary exponentiation (O(log n)) with overflow checking.                                 
```

### ada83.c:53294

```
// Raises Constraint_Error on negative exponent or overflow.                                     
```

### ada83.c:53295

```
//                                                                                                
```

### ada83.c:53300

```
// Check for negative exponent
```

### ada83.c:53309

```
// exp == 0 -> return 1
```

### ada83.c:53315

```
// Binary exponentiation loop: result *= b when e is odd, b *= b, e /= 2
```

### ada83.c:53321

```
// Check if e is odd: e & 1
```

### ada83.c:53326

```
// Multiply result by base (overflow checked)
```

### ada83.c:53333

```
// Square the base for next iteration
```

### ada83.c:53337

```
// Halve exponent
```

### ada83.c:53340

```
// Check if done (e == 0 after shift)
```

### ada83.c:53344

```
// Actually compute b*b (only if we need pow_t for next iteration)
```

### ada83.c:53362

```
// Modular power function - unsigned, wrapping (RM 3.5.4).
```

### ada83.c:53363

```
// Uses binary exponentiation (O(log n)), no overflow check as modular wraps.
```

### ada83.c:53372

```
// Binary exponentiation loop
```

### ada83.c:53396

```
// String trim helpers for Enum'VALUE (Ada RM 3.5): count leading/trailing
```

### ada83.c:53397

```
// spaces so the comparison uses the trimmed content only.
```

### ada83.c:53399

```
// Always emitted - linkonce_odr deduplicates, and codegen sets
```

### ada83.c:53400

```
// needs_trim_helpers too late (after header emission).
```

### ada83.c:53403

```
// count_leading_spaces(ptr data, iN len) -> iN
```

### ada83.c:53423

```
// count_trailing_spaces(ptr data, iN len) -> iN
```

### ada83.c:53442

```
// trailing count = len - 1 - i (when i is last non-space index) or len (all spaces)
```

### ada83.c:53450

```
// Runtime globals
```

### ada83.c:53456

```
// Per-thread current-exception id (pthread TLS, like the handler stack):
```

### ada83.c:53457

```
// a task raising concurrently must not clobber another thread's id between
```

### ada83.c:53458

```
// its longjmp and the handler's dispatch.
```

### ada83.c:53462

```
// Registry of every started task, for master/program-exit join (RM 9.4).
```

### ada83.c:53463

```
// Intrusive list of { ptr tcb, ptr next } nodes; head-inserted at task start.
```

### ada83.c:53465

```
// Set once the environment task has completed its statements and begun the
```

### ada83.c:53466

```
// join (RM 9.4). A task blocked at a `select ... or terminate` alternative
```

### ada83.c:53467

```
// ends itself when this is set (termination consensus, RM 9.7.1).
```

### ada83.c:53469

```
// pthread TLS key for the per-thread exception handler stack; created by
```

### ada83.c:53470

```
// __ada_tls_init at the top of main, before any thread exists.
```

### ada83.c:53472

```
// pthread TLS key for the current master record (RM 9.4 dependent tracking):
```

### ada83.c:53473

```
// { ptr dependents_head, ptr prev_master }. Per-thread, so each task carries
```

### ada83.c:53474

```
// its own master nesting. Tasks created at runtime (e.g. record/array
```

### ada83.c:53475

```
// components) register here and are joined at the master's exit.
```

### ada83.c:53478

```
// Terminate-consensus condition variable, paired with @__rv_mutex. 48 B of
```

### ada83.c:53479

```
// zeros == PTHREAD_COND_INITIALIZER on glibc (same trick as @__rv_mutex).
```

### ada83.c:53480

```
// Broadcast, never signal: one transition (a task terminating, a master
```

### ada83.c:53481

```
// completing) can satisfy several distinct waiters at once.
```

### ada83.c:53483

```
// Global mutex serialising all entry-queue operations (RM 9.5). Zero-init is
```

### ada83.c:53484

```
// PTHREAD_MUTEX_INITIALIZER on glibc; 40 bytes covers pthread_mutex_t.
```

### ada83.c:53486

```
// Format string for unhandled-exception diagnostic. Exceptions are
```

### ada83.c:53487

```
// identified at runtime by the *address* of their `@__exc.<name>` global,
```

### ada83.c:53488

```
// and we store the printable upper-case name as the *contents* of that
```

### ada83.c:53489

```
// global. The unhandled handler inttoptr's the exception id and prints
```

### ada83.c:53490

```
// it as a C string, so the message reads e.g.
```

### ada83.c:53491

```
//     Unhandled exception: CONSTRAINT_ERROR
```

### ada83.c:53492

```
// instead of the bare integer address. Address-based comparisons in
```

### ada83.c:53493

```
// handler dispatch (Generate_Exception_Dispatch) are unaffected.
```

### ada83.c:53496

```
// Standard exceptions (RM 11.1). Symbol names are lowercase to match the
```

### ada83.c:53497

```
// mangled call-site `@__exc.<name>`; the *contents* of each global are
```

### ada83.c:53498

```
// the upper-case display name (null-terminated) used by the unhandled
```

### ada83.c:53499

```
// diagnostic above.
```

### ada83.c:53507

```
// Allocator backend (RM 4.8, 13.2): malloc with a cumulative budget — the
```

### ada83.c:53508

```
// implementation-defined default collection size. Linux overcommit means
```

### ada83.c:53509

```
// a raw malloc essentially never fails, so a heap-exhaustion program
```

### ada83.c:53510

```
// (cb1010b allocates in a loop until STORAGE_ERROR) would run until the
```

### ada83.c:53511

```
// OOM killer; the budget bounds it. Frees are not credited back —
```

### ada83.c:53512

```
// adequate for the default-collection model.
```

### ada83.c:53533

```
// Stack-extent probe (RM 11.1): raise STORAGE_ERROR when an automatic
```

### ada83.c:53534

```
// object of %size bytes cannot fit in the current thread's remaining
```

### ada83.c:53535

```
// stack. The anchor global is thread-local so each task thread measures
```

### ada83.c:53536

```
// its own stack, lazily snapshotting the stack pointer on the thread's
```

### ada83.c:53537

```
// first probe (≈ its stack top; anything consumed before that first
```

### ada83.c:53538

```
// probe is absorbed by the safety margin). The limit comes from
```

### ada83.c:53539

```
// getrlimit(RLIMIT_STACK), cached after the first query; an unlimited
```

### ada83.c:53540

```
// or unqueryable stack falls back to a large or conservative default
```

### ada83.c:53541

```
// respectively. A 256 KiB margin is reserved for the runtime's own
```

### ada83.c:53542

```
// needs (raise machinery, handler frames) below the probed object.
```

### ada83.c:53590

```
// Secondary stack initialization
```

### ada83.c:53600

```
// Secondary stack mark
```

### ada83.c:53606

```
// Secondary stack release
```

### ada83.c:53612

```
// Secondary stack allocate
```

### ada83.c:53644

```
// Exception handling: push handler                                                               
```

### ada83.c:53645

```
// Handler frame structure: { ptr prev, [200 x i8] jmp_buf }                                      
```

### ada83.c:53646

```
// Field 0 = link to previous handler                                                             
```

### ada83.c:53647

```
// Field 1 = jmp_buf for setjmp/longjmp                                                           
```

### ada83.c:53648

```
//                                                                                                
```

### ada83.c:53650

```
// The handler stack must be PER-THREAD: every task pushes/pops handlers, and
```

### ada83.c:53651

```
// a process-global stack head lets one task pop or longjmp into another's
```

### ada83.c:53652

```
// frames (observed: a98002a corrupting the chain). pthread TLS via
```

### ada83.c:53653

```
// pthread_get/setspecific works under lli (plain libc calls), unlike IR-level
```

### ada83.c:53654

```
// thread_local. All keys are created by __ada_tls_init, main's first act.
```

### ada83.c:53664

```
// Exception handling: pop handler
```

### ada83.c:53676

```
// The single creation point for every per-thread key, called from main
```

### ada83.c:53677

```
// before any task thread exists; every other runtime function just loads
```

### ada83.c:53678

```
// the key globals.
```

### ada83.c:53687

```
// ── Rendezvous wait/wake primitives — §18-style host dispatch ─────────
```

### ada83.c:53688

```
// Chosen when the COMPILER is built, exactly as the SIMD tiers are: the
```

### ada83.c:53689

```
// emitted runtime targets the build host. On x86-64 and AArch64 a
```

### ada83.c:53690

```
// completion wait runs a bounded spin — PAUSE (x86) or YIELD (ARM)
```

### ada83.c:53691

```
// between probes, covering the short accept-body window with no
```

### ada83.c:53692

```
// syscall — then parks directly on the completion word itself: a
```

### ada83.c:53693

```
// private futex on Linux, WaitOnAddress on Windows (kernelbase /
```

### ada83.c:53694

```
// Synchronization.lib, Windows 8+; the surrounding runtime still rides
```

### ada83.c:53695

```
// winpthreads there). The completer wakes exactly that waiter. Every
```

### ada83.c:53696

```
// other host takes the portable pthreads path over
```

### ada83.c:53697

```
// @__rv_mutex/@__term_cond.
```

### ada83.c:53698

```
//
```

### ada83.c:53699

```
// Conventions: __ada_wait_word REQUIRES @__rv_mutex NOT held (the
```

### ada83.c:53700

```
// portable version blocks on it); __ada_wake_word runs UNDER the lock
```

### ada83.c:53701

```
// and only issues the direct wake — the existing @__term_cond broadcast
```

### ada83.c:53702

```
// at each completion site remains the portable wake channel.
```

### ada83.c:53725

```
// FUTEX_WAIT_PRIVATE = 128, FUTEX_WAKE_PRIVATE = 129. The wait re-checks
```

### ada83.c:53726

```
// the word before every park (futex itself re-checks atomically in the
```

### ada83.c:53727

```
// kernel), so a wake between probe and park is never lost.
```

### ada83.c:53759

```
// WaitOnAddress re-checks the word against *CompareAddress atomically in
```

### ada83.c:53760

```
// the kernel before sleeping, so a wake between probe and park is never
```

### ada83.c:53761

```
// lost — the same guarantee futex gives. INFINITE = -1.
```

### ada83.c:53792

```
// The object file carries its own link dependency (the COFF .drectve
```

### ada83.c:53793

```
// mechanism behind #pragma comment(lib, ...)): lld-link and MSVC link
```

### ada83.c:53794

```
// pull Synchronization.lib automatically. GNU ld ignores the directive
```

### ada83.c:53795

```
// — MinGW builds get the library from the Makefile's RUNTIME_LIBS.
```

### ada83.c:53815

```
// Portable wake rides the completion sites' @__term_cond broadcast.
```

### ada83.c:53821

```
// RM 9.10 / RM 9.5: a dying task releases every caller still parked on
```

### ada83.c:53822

```
// one of its rendezvous — queued calls never accepted, and calls taken
```

### ada83.c:53823

```
// into a rendezvous that will now never complete — with TASKING_ERROR
```

### ada83.c:53824

```
// bridged into the record. Runs UNDER @__rv_mutex; the caller's own
```

### ada83.c:53825

```
// broadcast (or wake) follows at the call site.
```

### ada83.c:53864

```
// A fresh rendezvous record ({ task, entry_idx, params, completion i32,
```

### ada83.c:53865

```
// next, bridged-exception }, 48 bytes) — malloc'd here, freed by the
```

### ada83.c:53866

```
// CALLER once complete.
```

### ada83.c:53883

```
// Fill a caller-provided timespec with now + us (nanosecond carry into
```

### ada83.c:53884

```
// seconds) for pthread_cond_timedwait.
```

### ada83.c:53904

```
// Bounded spin on racy peeks of the queue head and the abort flag —
```

### ada83.c:53905

```
// PAUSE/YIELD between probes — before an acceptor takes the lock. The
```

### ada83.c:53906

```
// locked path re-checks properly; the abort peek keeps a spin from
```

### ada83.c:53907

```
// delaying RM 9.10.
```

### ada83.c:53931

```
// Exception handling: raise
```

### ada83.c:53945

```
// The exception id is the i64 address of `@__exc.<name>`, whose contents
```

### ada83.c:53946

```
// are the printable name (null-terminated). inttoptr back to a string ptr
```

### ada83.c:53947

```
// so printf("%s") gets the readable form.
```

### ada83.c:53954

```
// Exception handling: reraise
```

### ada83.c:53961

```
// Exception handling: get current exception (per-thread)
```

### ada83.c:53969

```
// Integer power function
```

### ada83.c:53995

```
// Delay statement support (RM 9.6). nanosleep, not usleep: the full i64
```

### ada83.c:53996

```
// microsecond budget survives (usleep's i32 wrapped a negative delay to
```

### ada83.c:53997

```
// ~71 minutes and truncated long ones), a non-positive delay returns at
```

### ada83.c:53998

```
// once, and the EINTR retry loop guarantees the delay lasts AT LEAST the
```

### ada83.c:53999

```
// requested time. nanosleep is a pthread cancellation point, which abort
```

### ada83.c:54000

```
// relies on.
```

### ada83.c:54022

```
// CALENDAR.CLOCK backend (RT_TIME): current wall-clock time as a DURATION
```

### ada83.c:54023

```
// value (seconds since the Unix epoch, matching CALENDAR's EPOCH=2440588).
```

### ada83.c:54024

```
// DURATION'SMALL is 1e-5, so the i64 representation is seconds*100000 (units
```

### ada83.c:54025

```
// of 10 microseconds); a timespec contributes tv_sec*100000 + tv_nsec/10000.
```

### ada83.c:54039

```
// Task abort: signal task to terminate (simplified: sets abort-pending flag)
```

### ada83.c:54040

```
// Release any entry call this task is blocked on as the CALLER: if its queued
```

### ada83.c:54041

```
// rendezvous record is still in the target's queue (not yet accepted), unlink
```

### ada83.c:54042

```
// it (E'COUNT drops immediately, RM 9.10) and mark it complete so the caller's
```

### ada83.c:54043

```
// wait returns at once. A call already IN a rendezvous is left in place — the
```

### ada83.c:54044

```
// acceptor completes it normally. Caller holds @__rv_mutex.
```

### ada83.c:54062

```
// Abort a task (RM 9.10): make it (and its dependents) abnormal. This ONLY
```

### ada83.c:54063

```
// sets the abort-pending flag, releases any queued entry call, and wakes
```

### ada83.c:54064

```
// everyone — it never sets TERMINATED, never cancels a thread, and never
```

### ada83.c:54065

```
// decrements the awake count. Termination is cooperative: the task notices
```

### ada83.c:54066

```
// abort_pending at its next abort-completion point (the return from a blocking
```

### ada83.c:54067

```
// entry call / accept / delay, checked by codegen) and runs its normal
```

### ada83.c:54068

```
// termination epilogue, which joins its own dependents. A master aborted while
```

### ada83.c:54069

```
// a dependent is in a rendezvous therefore stays un-terminated automatically —
```

### ada83.c:54070

```
// it is blocked joining that dependent until the dependent's rendezvous ends.
```

### ada83.c:54071

```
// Aborting an already-terminated task is a no-op.
```

### ada83.c:54094

```
// ═══════════════════════════════════════════════════════════════════════
```

### ada83.c:54095

```
// TASKING RUNTIME — protocol invariants
```

### ada83.c:54096

```
//
```

### ada83.c:54097

```
// Lock discipline. @__rv_mutex guards ALL cross-task mutable state: entry
```

### ada83.c:54098

```
//   queues, serving lists, waiting_entry / open_entries advertisements,
```

### ada83.c:54099

```
//   master awake counts, passive flags, and every completion publication.
```

### ada83.c:54100

```
//   @__ada_wait_word is called with the lock NOT held; @__ada_wake_word
```

### ada83.c:54101

```
//   runs UNDER it. Nothing blocks while holding the lock except the
```

### ada83.c:54102

```
//   condvar waits, which release it atomically.
```

### ada83.c:54103

```
//
```

### ada83.c:54104

```
// Wake channels. Two, by event kind:
```

### ada83.c:54105

```
//   - The rendezvous COMPLETION word (rv+24, i32 0=pending 1=complete,
```

### ada83.c:54106

```
//     release-published): its waiter is woken directly — bounded spin,
```

### ada83.c:54107

```
//     then futex (Linux) or WaitOnAddress (Windows) on x86-64/AArch64;
```

### ada83.c:54108

```
//     @__term_cond broadcast on other hosts.
```

### ada83.c:54109

```
//   - Every other state change — queue arrival, activation, master flag,
```

### ada83.c:54110

```
//     consensus, abort — broadcasts @__term_cond; waiters re-check their
```

### ada83.c:54111

```
//     predicate under the lock, so spurious wakes are harmless and lost
```

### ada83.c:54112

```
//     wakes impossible.
```

### ada83.c:54113

```
//
```

### ada83.c:54114

```
// Completion is total. A caller parked on rv+24 is released by exactly
```

### ada83.c:54115

```
//   one of: the acceptor (accept_complete), an abort of the CALLER
```

### ada83.c:54116

```
//   (abort_release_call), or the TARGET's completion in any form —
```

### ada83.c:54117

```
//   body end, terminate alternative, abort epilogue, failed activation,
```

### ada83.c:54118

```
//   exception unwind — each of which runs @__ada_release_callers over
```

### ada83.c:54119

```
//   the target's queue AND serving list, bridging TASKING_ERROR into
```

### ada83.c:54120

```
//   the record (RM 9.5(16)/9.10). No caller ever polls for a dead task.
```

### ada83.c:54121

```
//
```

### ada83.c:54122

```
// Ownership. Rendezvous records are malloc'd and freed by the CALLER
```

### ada83.c:54123

```
//   after completion. Master records and their list nodes are freed by
```

### ada83.c:54124

```
//   @__ada_master_leave after the joins (dependents can no longer reach
```

### ada83.c:54125

```
//   them: awake decrements precede join returns, and the abort walker
```

### ada83.c:54126

```
//   reaches records only through the owned-masters list, unlinked by
```

### ada83.c:54127

```
//   then). TCBs are immortal by design — 'TERMINATED and 'CALLABLE
```

### ada83.c:54128

```
//   remain legal after termination for as long as the program runs.
```

### ada83.c:54129

```
// ═══════════════════════════════════════════════════════════════════════
```

### ada83.c:54131

```
// Task wrapper: calls the task body, then publishes completion and
```

### ada83.c:54132

```
// termination. (The authoritative TCB layout is documented at
```

### ada83.c:54133

```
// __ada_task_create below.)
```

### ada83.c:54136

```
// Publish this thread's own TCB in TLS so runtime routines executing on the
```

### ada83.c:54137

```
// task's thread (master enter in any subprogram it calls, abort, consensus)
```

### ada83.c:54138

```
// can identify the owning task dynamically.
```

### ada83.c:54145

```
// Publish termination under the consensus lock (RM 9.7.1): leave the
```

### ada83.c:54146

```
// enclosing master's awake count — unless this task already left it by
```

### ada83.c:54147

```
// going passive (it died via a terminate alternative).
```

### ada83.c:54169

```
// Task start: allocate TCB, spawn wrapper thread. Returns TCB ptr.
```

### ada83.c:54170

```
// Create a task control block WITHOUT starting its thread (RM 9.3: a declared
```

### ada83.c:54171

```
// task is created during elaboration but activated later, at the enclosing
```

### ada83.c:54172

```
// master's `begin`). The thread handle (TCB+16) is left null; an unactivated
```

### ada83.c:54173

```
// task therefore has a null handle and is skipped by the join.
```

### ada83.c:54174

```
// TCB layout (96 bytes, calloc-zeroed):
```

### ada83.c:54175

```
//   0  func           8  parent_frame   16 thread_handle  24 completed (i8)
```

### ada83.c:54176

```
//   32 master_done_ptr 40 entry_queue (FIFO RV list head)
```

### ada83.c:54177

```
//   48 waiting_entry (i64: entry index this task is blocked at an accept for,
```

### ada83.c:54178

```
//      -2 = blocked in a selective wait (open_entries below then says which),
```

### ada83.c:54179

```
//      or -1 = not accepting; for RM 9.7.2 conditional-call readiness)
```

### ada83.c:54180

```
//   56 activation_state (i8: 0 = created/activating, 1 = activated — its
```

### ada83.c:54181

```
//      declarative part elaborated, 2 = activation failed — the declarative
```

### ada83.c:54182

```
//      part raised; the activator gets TASKING_ERROR, RM 9.3(7))
```

### ada83.c:54183

```
//   57 terminated (i8): set after the task's dependents are awaited
```

### ada83.c:54184

```
//      ('TERMINATED); the completed byte at 24 flips earlier, at the end
```

### ada83.c:54185

```
//      of the body statements (RM 9.4: completion precedes termination)
```

### ada83.c:54186

```
//   58 at_terminate (i8): in a selective wait with an open terminate
```

### ada83.c:54187

```
//      alternative — counts toward the RM 9.7.1 terminate consensus
```

### ada83.c:54188

```
//   59 passive (i8): parked at a terminate alternative AND its whole
```

### ada83.c:54189

```
//      dependent subtree is quiescent — no longer counted in its enclosing
```

### ada83.c:54190

```
//      master's awake count (RM 9.7.1 consensus, maintained under
```

### ada83.c:54191

```
//      @__rv_mutex)
```

### ada83.c:54192

```
//   64 owned_masters (head of the intrusive { master_rec, next } list of
```

### ada83.c:54193

```
//      masters this task has entered and not yet left — lets OTHER threads
```

### ada83.c:54194

```
//      walk this task's subtree for transitive consensus and abort)
```

### ada83.c:54195

```
//   80 open_entries (ptr to { i64 count, i64 idx[count] }: the OPEN
```

### ada83.c:54196

```
//      accept alternatives' encoded entry indices for the current
```

### ada83.c:54197

```
//      selective wait, closed slots holding -1. Frame-local storage of
```

### ada83.c:54198

```
//      the select; null and meaningless unless waiting_entry is -2.)
```

### ada83.c:54199

```
//   88 serving_rv (head of the rendezvous records this task has taken
```

### ada83.c:54200

```
//      into rendezvous and not yet completed, linked through rv+32 —
```

### ada83.c:54201

```
//      free after the dequeue. A dying task completes each with
```

### ada83.c:54202

```
//      TASKING_ERROR so its callers never park forever, RM 9.10.)
```

### ada83.c:54203

```
// Offset 32 holds the enclosing MASTER RECORD ({ deps, prev, done_flag,
```

### ada83.c:54204

```
//   owner_tcb, awake_count }), not the bare flag.
```

### ada83.c:54205

```
// Per-task queue → per-entry FIFO without cross-task interference (RM 9.5).
```

### ada83.c:54214

```
// The master this task depends on (RM 9.4), passed EXPLICITLY: declared
```

### ada83.c:54215

```
// objects pass their bracketing scope's record; allocators pass the record
```

### ada83.c:54216

```
// of the master that elaborated the access type; null = library level
```

### ada83.c:54217

```
// (the program-exit join is the master).
```

### ada83.c:54220

```
// completed (24), thread_handle (16) and queue (40) are zero from calloc.
```

### ada83.c:54221

```
// Register in the global task list so the program-exit join reaps any task a
```

### ada83.c:54222

```
// master didn't (joins are idempotent via the nulled handle).
```

### ada83.c:54229

```
// Register as the master's dependent so it is joined at that master's
```

### ada83.c:54230

```
// exit, not just at program end.
```

### ada83.c:54245

```
// Activate a created task: spawn its wrapper thread (RM 9.3). Idempotent —
```

### ada83.c:54246

```
// a second call (e.g. the enclosing master's begin re-walking entries a
```

### ada83.c:54247

```
// nested package body already activated) is a no-op, keyed on the
```

### ada83.c:54248

```
// activation state. A null TCB (activation skipped) is also a no-op.
```

### ada83.c:54249

```
// A batch activation that raised PROGRAM_ERROR (RM 9.3 / RM 3.9) leaves
```

### ada83.c:54250

```
// its created-but-never-threaded tasks COMPLETED: entry calls raise
```

### ada83.c:54251

```
// TASKING_ERROR, 'CALLABLE is false, later activation points skip them,
```

### ada83.c:54252

```
// and joins pass over the null thread handle. Activation state 1 keeps
```

### ada83.c:54253

```
// any straggling activation_wait from reporting a second failure — the
```

### ada83.c:54254

```
// batch's PROGRAM_ERROR already did.
```

### ada83.c:54279

```
// A task completed WITHOUT ever being activated (its batch's activation
```

### ada83.c:54280

```
// raised PROGRAM_ERROR, RM 9.3) stays completed: an enclosing activation
```

### ada83.c:54281

```
// point must not resurrect it.
```

### ada83.c:54288

```
// Count the dependent as awake BEFORE the thread exists (RM 9.7.1 consensus)
```

### ada83.c:54289

```
// — the wrapper's terminating decrement must never precede the increment.
```

### ada83.c:54308

```
// No thread could be created: the task never activates. Mark it completed
```

### ada83.c:54309

```
// and activation-failed so waiters get TASKING_ERROR instead of hanging,
```

### ada83.c:54310

```
// take back its awake count, and wake any activation_wait.
```

### ada83.c:54321

```
// Wait until a task finishes activating — its declarative part has been
```

### ada83.c:54322

```
// elaborated (RM 9.3: the activator continues only after all activated
```

### ada83.c:54323

```
// tasks complete their activation). Returns the activation state: 1 =
```

### ada83.c:54324

```
// activated, 2 = the declarative part raised (caller raises TASKING_ERROR).
```

### ada83.c:54325

```
// A null TCB returns 1 (nothing to wait for).
```

### ada83.c:54348

```
// Lazy pthread-TLS key for the executing task's own TCB (null on the
```

### ada83.c:54349

```
// environment thread). Set by the task wrapper at thread start; read by
```

### ada83.c:54350

```
// master enter (record ownership), abort, and the terminate consensus.
```

### ada83.c:54351

```
// The executing task's TCB, or null on the environment thread.
```

### ada83.c:54359

```
// Enter a master (RM 9.4): push a fresh master record onto this thread's
```

### ada83.c:54360

```
// master stack. Record layout (48 bytes, calloc-zeroed):
```

### ada83.c:54361

```
//   0 dependents_head  8 prev (TLS stack link)  16 done_flag
```

### ada83.c:54362

```
//   24 owner_tcb (the task executing this master; null = environment)
```

### ada83.c:54363

```
//   32 awake_count (i64: dependents activated and neither terminated nor
```

### ada83.c:54364

```
//      passive — RM 9.7.1 consensus; guarded by @__rv_mutex)
```

### ada83.c:54365

```
//   40 scope_id (i64: the master's static scope id — __ada_master_find
```

### ada83.c:54366

```
//      resolves an access type's elaborating master through it)
```

### ada83.c:54367

```
// The record is also linked into the owner's owned-masters list (TCB+64)
```

### ada83.c:54368

```
// so other threads can walk the owner's subtree. Returns the record for
```

### ada83.c:54369

```
// the matching leave.
```

### ada83.c:54400

```
// Unlink a left master record from its owner's owned-masters list
```

### ada83.c:54401

```
// (enter/leave are LIFO per thread, so it is the head — but walk anyway:
```

### ada83.c:54402

```
// the exception unwind can leave records out of order with respect to
```

### ada83.c:54403

```
// masters entered by subprograms the unwound frames had called).
```

### ada83.c:54433

```
// ── RM 9.7.1 terminate consensus: counter maintenance ──────────────────
```

### ada83.c:54434

```
// awake_count (master rec +32) counts dependents that were activated and
```

### ada83.c:54435

```
// are neither terminated nor passive. A task is PASSIVE (TCB+59) when it
```

### ada83.c:54436

```
// is parked at an open terminate alternative AND every master it owns has
```

### ada83.c:54437

```
// awake_count == 0 — counters carry transitivity: a running grandchild
```

### ada83.c:54438

```
// keeps its master's count above zero, which keeps its owner non-passive,
```

### ada83.c:54439

```
// which keeps the grandparent's count above zero. ALL transitions run
```

### ada83.c:54440

```
// under @__rv_mutex (the entry-queue lock — enqueue and consensus must
```

### ada83.c:54441

```
// serialize) and broadcast @__term_cond.
```

### ada83.c:54443

```
// RM 9.10: any task depending on an aborted task becomes abnormal too.
```

### ada83.c:54444

```
// Walk the target's owned masters' dependent lists recursively (caller
```

### ada83.c:54445

```
// holds @__rv_mutex): mark each not-yet-completed dependent completed +
```

### ada83.c:54446

```
// terminated, leave its master's awake count, cancel its thread when it
```

### ada83.c:54447

```
// is NOT parked at a terminate alternative (a parked task may sit in
```

### ada83.c:54448

```
// pthread_cond_wait on @__rv_mutex — a cancel there would kill it holding
```

### ada83.c:54449

```
// the lock; the completed flag + the caller's broadcast make it die
```

### ada83.c:54450

```
// cleanly through __ada_term_wait instead).
```

### ada83.c:54451

```
// RM 9.10: a task's dependents become abnormal when it is aborted. Set each
```

### ada83.c:54452

```
// non-terminated dependent's abort-pending flag, release any queued entry call
```

### ada83.c:54453

```
// it is blocked on, and recurse into ITS dependents. Purely cooperative — no
```

### ada83.c:54454

```
// cancel, no terminated/completed, no awake-count change: each dependent runs
```

### ada83.c:54455

```
// its own termination epilogue at its next abort-completion point. Caller holds
```

### ada83.c:54456

```
// @__rv_mutex. Skip a dependent already terminated or already abnormal (its
```

### ada83.c:54457

```
// subtree was walked on the earlier abort), which also bounds the recursion.
```

### ada83.c:54501

```
// Raise a master's done flag under the consensus lock and wake parked
```

### ada83.c:54502

```
// dependents. Every flag raise goes through here: a parked dependent
```

### ada83.c:54503

```
// sleeps in @__ada_term_wait, and a plain store would be followed by a
```

### ada83.c:54504

```
// pthread_join of that very dependent — deadlock without the broadcast.
```

### ada83.c:54516

```
// Decrement a master's awake count; caller holds @__rv_mutex. When the
```

### ada83.c:54517

```
// count reaches zero and the owner is parked at a terminate alternative,
```

### ada83.c:54518

```
// the owner may now be passive — cascade up (the Make_Passive walk).
```

### ada83.c:54540

```
// Make a task passive if it qualifies (parked at terminate, queue empty,
```

### ada83.c:54541

```
// every owned master quiescent); caller holds @__rv_mutex. Going passive
```

### ada83.c:54542

```
// removes it from its enclosing master's awake count — recursing the
```

### ada83.c:54543

```
// cascade upward through __ada_awake_dec.
```

### ada83.c:54556

```
// RM 9.7.1: not selectable while an entry call is queued.
```

### ada83.c:54561

```
// Every owned master must be quiescent (awake == 0).
```

### ada83.c:54587

```
// Inverse cascade: a passive task becomes awake again (an entry call
```

### ada83.c:54588

```
// arrived); caller holds @__rv_mutex. Re-increment its enclosing master
```

### ada83.c:54589

```
// and un-passify owners whose passivity depended on this subtree.
```

### ada83.c:54618

```
// Park at an open terminate alternative until the consensus decides
```

### ada83.c:54619

```
// (RM 9.7.1): returns 1 = selected for termination (caller runs its
```

### ada83.c:54620

```
// master ladder and returns from the body), 0 = an entry call arrived
```

### ada83.c:54621

```
// (caller re-sweeps its accept alternatives). The whole decision runs
```

### ada83.c:54622

```
// under @__rv_mutex in a Mesa-monitor loop — no polling, immune to lost
```

### ada83.c:54623

```
// wakeups: every state change broadcasts @__term_cond under the same
```

### ada83.c:54624

```
// lock.
```

### ada83.c:54630

```
// Aborted while parked: die immediately — and INVARIANT: a completed task
```

### ada83.c:54631

```
// never reaches the cond_wait below, so __ada_task_abort may only
```

### ada83.c:54632

```
// pthread_cancel targets that are NOT parked here (a cancel delivered
```

### ada83.c:54633

```
// inside cond_wait would kill the thread holding @__rv_mutex).
```

### ada83.c:54639

```
// An arrived entry call that some OPEN alternative can serve re-awakens
```

### ada83.c:54640

```
// the task. A call for a CLOSED entry must not: the sweep would take
```

### ada83.c:54641

```
// nothing and park again, and this check would fire again — a spin.
```

### ada83.c:54647

```
// Global backstop (program exit) terminates unconditionally.
```

### ada83.c:54665

```
// Master complete: the group dies when every dependent is terminated or
```

### ada83.c:54666

```
// passive (awake == 0) and this task itself made it to passive.
```

### ada83.c:54689

```
// The current master record of this thread (null outside any master).
```

### ada83.c:54697

```
// Find the nearest active master with the given static scope id on this
```

### ada83.c:54698

```
// thread's master chain (RM 9.4: an allocator's task depends on the master
```

### ada83.c:54699

```
// that elaborated the access type definition — under recursion the nearest
```

### ada83.c:54700

```
// elaboration instance is the live one). Null when none: the access type
```

### ada83.c:54701

```
// was elaborated at library level, whose master is the program.
```

### ada83.c:54725

```
// Exception unwind across masters (RM 9.4): pop and complete every master
```

### ada83.c:54726

```
// entered after `saved` (the TLS master at the catching frame's entry).
```

### ada83.c:54727

```
// For each: raise its done flag (releasing terminate alternatives), mark
```

### ada83.c:54728

```
// never-activated dependents completed (RM 9.3: created-but-not-activated
```

### ada83.c:54729

```
// tasks become terminated when their declarative part's exception
```

### ada83.c:54730

```
// propagates), then join the activated dependents before the handler runs.
```

### ada83.c:54749

```
// Pass 1: complete never-activated dependents so blocked callers get
```

### ada83.c:54750

```
// TASKING_ERROR instead of deadlocking the joins below. Under
```

### ada83.c:54751

```
// @__rv_mutex — the marking and the caller release mutate shared state.
```

### ada83.c:54777

```
// Pass 2: join the activated dependents (idempotent; null handles skip).
```

### ada83.c:54801

```
// Leave a master (RM 9.4): join every dependent task registered while this
```

### ada83.c:54802

```
// master was current, then pop the master stack. Dependents registered by
```

### ada83.c:54803

```
// __ada_task_create as { tcb, next } nodes.
```

### ada83.c:54806

```
// Re-publish the master's completion under the consensus lock and wake
```

### ada83.c:54807

```
// parked dependents (RM 9.7.1): the codegen stored the done flag without
```

### ada83.c:54808

```
// the lock; the locked store + broadcast is what a parked dependent's
```

### ada83.c:54809

```
// predicate re-check synchronizes with. The joins below are phase 2 —
```

### ada83.c:54810

```
// they return as the dependents die.
```

### ada83.c:54841

```
// Joined, disowned, popped: nothing can reach the record now (every
```

### ada83.c:54842

```
// dependent decremented awake before its join returned; the abort
```

### ada83.c:54843

```
// walker reaches records only through the owned-masters list).
```

### ada83.c:54848

```
// Join one task (RM 9.4): wait for its thread, then null the handle so a
```

### ada83.c:54849

```
// later join (e.g. the program-exit __ada_join_all) skips it. No-op for a
```

### ada83.c:54850

```
// null TCB or a never-activated / already-joined task.
```

### ada83.c:54865

```
// Create and immediately activate (RM 9.3: tasks created by an allocator are
```

### ada83.c:54866

```
// activated as part of evaluating the allocator, and the activator waits for
```

### ada83.c:54867

```
// the activation to complete). Used for `new T` and package-level tasks.
```

### ada83.c:54868

```
// An activation failure raises TASKING_ERROR in the activator (RM 9.3(7)).
```

### ada83.c:54885

```
// Join every registered task: wait for all task threads to terminate before
```

### ada83.c:54886

```
// the program exits (RM 9.4 — the environment task is master of all). Walks
```

### ada83.c:54887

```
// the registry, joining each thread by its handle at TCB offset 16.
```

### ada83.c:54903

```
// Skip tasks that were created but never activated (null handle): RM 9.3 —
```

### ada83.c:54904

```
// an exception in the declarative part completes them without activation.
```

### ada83.c:54920

```
// Remove a specific rendezvous record from a task's queue (locks itself).
```

### ada83.c:54921

```
// Used to cancel a conditional/timed entry call that was not accepted.
```

### ada83.c:54922

```
// Returns 1 if the record was found and removed, 0 if it was no longer
```

### ada83.c:54923

```
// queued — the acceptor already took it, so the rendezvous is COMMITTED
```

### ada83.c:54924

```
// and the caller must wait for completion instead of cancelling
```

### ada83.c:54925

```
// (RM 9.7.2/9.7.3: an accepted call cannot be cancelled).
```

### ada83.c:54926

```
// Lock-free unlink: remove %rv from %self's queue, returns 1 if found. The
```

### ada83.c:54927

```
// caller MUST hold @__rv_mutex. Shared by the locked wrapper below and by
```

### ada83.c:54928

```
// callers that already hold the lock (the abort's synchronous unlink path).
```

### ada83.c:54964

```
// Locked wrapper for callers that do NOT already hold @__rv_mutex (the
```

### ada83.c:54965

```
// timed/conditional entry-call cancellation path).
```

### ada83.c:54974

```
// Conditional / timed entry call (RM 9.7.2/9.7.3). Enqueues a rendezvous and
```

### ada83.c:54975

```
// waits up to timeout_us microseconds for it to be accepted. Returns 1 if the
```

### ada83.c:54976

```
// rendezvous completed, 0 if it timed out (the record is then removed from the
```

### ada83.c:54977

```
// queue). Raises TASKING_ERROR if the target is/becomes completed or abnormal,
```

### ada83.c:54978

```
// and re-raises a bridged accept-body exception. timeout_us = 0 polls once
```

### ada83.c:54979

```
// (conditional-call "immediately possible").
```

### ada83.c:54993

```
// The dead-check, the readiness check, and the enqueue form ONE critical
```

### ada83.c:54994

```
// section: the readiness the caller observes must still hold when its
```

### ada83.c:54995

```
// call lands in the queue, or a target that just left its wait keeps the
```

### ada83.c:54996

```
// stale call parked — observably, via E'COUNT — until the budget expires.
```

### ada83.c:54999

```
// RM 9.5(16)/9.10: a completed / terminated / abnormal target can never
```

### ada83.c:55000

```
// rendezvous → TASKING_ERROR (an aborted target is abnormal, not completed).
```

### ada83.c:55009

```
// Conditional call (timeout 0, RM 9.7.2): rendezvous only if the target is
```

### ada83.c:55010

```
// BLOCKED at an accept for this entry, or blocked in a selective wait,
```

### ada83.c:55011

```
// RIGHT NOW (a selective wait with an else part never blocks and never
```

### ada83.c:55012

```
// advertises). If not, take the else part immediately (return 0) without
```

### ada83.c:55013

```
// enqueuing. Timed calls (timeout>0) skip this and wait up to the deadline.
```

### ada83.c:55022

```
// Blocked selective wait: ready iff THIS entry is among its published
```

### ada83.c:55023

```
// open alternatives (exact index match, families included).
```

### ada83.c:55057

```
// Re-awaken a target parked at a terminate alternative (RM 9.7.1): the
```

### ada83.c:55058

```
// queued call makes terminate unselectable; the broadcast wakes its
```

### ada83.c:55059

```
// __ada_term_wait to serve the call. Same lock as the enqueue — the
```

### ada83.c:55060

```
// re-awaken and the consensus serialize.
```

### ada83.c:55063

```
// Wait for the completion word — the ONLY thing a caller ever waits for
```

### ada83.c:55064

```
// (completion is total: acceptor, caller-abort, or target death all
```

### ada83.c:55065

```
// publish it). A conditional call (timeout 0) verified the target
```

### ada83.c:55066

```
// BLOCKED with this entry OPEN under this same lock hold, so its
```

### ada83.c:55067

```
// rendezvous is committed: wait unbounded. A timed call waits to its
```

### ada83.c:55068

```
// absolute deadline, then cancels iff the acceptor has not yet dequeued
```

### ada83.c:55069

```
// the record; once dequeued the rendezvous can no longer be cancelled
```

### ada83.c:55070

```
// (RM 9.7.2/9.7.3) and the wait becomes unbounded too.
```

### ada83.c:55097

```
// Acceptor holds the record: committed — wait for completion.
```

### ada83.c:55118

```
// A task is "dead" for the purpose of an entry call (RM 9.5(16)): it has
```

### ada83.c:55119

```
// completed its body (24), fully terminated (57), or is abnormal (61). A call
```

### ada83.c:55120

```
// to such a task can never rendezvous. Caller holds @__rv_mutex.
```

### ada83.c:55135

```
// An entry call that is about to raise TASKING_ERROR because its target is
```

### ada83.c:55136

```
// ABNORMAL (abort-pending) first waits for the target to actually terminate
```

### ada83.c:55137

```
// (RM 9.4: an abnormal task completes at its next abort completion point and
```

### ada83.c:55138

```
// its teardown then publishes TERMINATED). This makes the RM 9.9 observable
```

### ada83.c:55139

```
// ordering deterministic: after the caller sees TASKING_ERROR, T'TERMINATED
```

### ada83.c:55140

```
// is already true (c9a006a). Two exclusions keep it deadlock-free:
```

### ada83.c:55141

```
//   - a target that merely COMPLETED normally raises immediately — its
```

### ada83.c:55142

```
//     termination can be delayed indefinitely by still-running dependents;
```

### ada83.c:55143

```
//   - a caller that is itself abort-pending raises immediately — it may be a
```

### ada83.c:55144

```
//     dependent the dying master is joining, and must unwind first.
```

### ada83.c:55145

```
// Caller holds @__rv_mutex; the task wrapper broadcasts @__term_cond after
```

### ada83.c:55146

```
// setting TERMINATED under the same mutex, so the wait cannot miss the wake.
```

### ada83.c:55175

```
// T'CALLABLE (RM 9.9): callable iff not completed, terminated, or abnormal.
```

### ada83.c:55188

```
// Entry call: caller side of rendezvous. Appends a rendezvous record to the
```

### ada83.c:55189

```
// TARGET task's own FIFO queue under the global mutex (RM 9.5: each entry has
```

### ada83.c:55190

```
// its own queue; a per-task list scanned by entry index gives per-entry FIFO),
```

### ada83.c:55191

```
// then waits for the acceptor to mark it complete. RV layout (40 bytes):
```

### ada83.c:55192

```
//   0 task, 8 entry_idx, 16 params, 24 complete (i8), 32 next.
```

### ada83.c:55193

```
// RV layout (48 bytes): 0 task, 8 entry_idx, 16 params, 24 complete (i8),
```

### ada83.c:55194

```
// 32 next, 40 exception_id (i64; 0 = none). The exception slot bridges an
```

### ada83.c:55195

```
// accept-body exception back to the caller (RM 9.5(16)).
```

### ada83.c:55196

```
// Entry call, caller side. Enqueues a rendezvous record on the target's FIFO
```

### ada83.c:55197

```
// queue and blocks on the global condvar until the acceptor marks it complete.
```

### ada83.c:55198

```
// No polling: __ada_accept_complete and __ada_task_abort both broadcast. The
```

### ada83.c:55199

```
// caller records the live rv in its own TCB+72 so an abort can unlink it (see
```

### ada83.c:55200

```
// __ada_abort_release_call); the post-call abort check emitted by codegen runs
```

### ada83.c:55201

```
// the termination epilogue if this task was aborted during the rendezvous.
```

### ada83.c:55202

```
// RV layout (48 B): 0 task, 8 entry_idx, 16 params, 24 complete(i8),
```

### ada83.c:55203

```
// 32 next, 40 exception_id(i64; 0 = none).
```

### ada83.c:55220

```
// RM 9.5(16): calling an entry of a target that is completed / terminated /
```

### ada83.c:55221

```
// abnormal can never rendezvous → TASKING_ERROR.
```

### ada83.c:55252

```
// Wake a target parked at a terminate alternative (RM 9.7.1) so it serves this
```

### ada83.c:55253

```
// call, and any acceptor blocked in accept_wait.
```

### ada83.c:55256

```
// Wait OUTSIDE the lock on the record's own completion word. Every path
```

### ada83.c:55257

```
// that can end this rendezvous completes the word: the acceptor
```

### ada83.c:55258

```
// (accept_complete), an abort of the CALLER (abort_release_call), and
```

### ada83.c:55259

```
// the TARGET dying in any way (release_callers bridges TASKING_ERROR
```

### ada83.c:55260

```
// into the record) — so no dead-polling is needed here at all.
```

### ada83.c:55273

```
// Rendezvous complete (by the acceptor, an abort, or the target's death).
```

### ada83.c:55274

```
// Clear pending_rv under the lock (an aborter dereferences it there),
```

### ada83.c:55275

```
// take the bridged exception id, unlock, free, re-raise if any.
```

### ada83.c:55295

```
// Scan a task's queue for the first (FIFO) rendezvous matching entry_idx,
```

### ada83.c:55296

```
// unlink and return it, or null. Caller holds @__rv_mutex. Factored as a
```

### ada83.c:55297

```
// helper used by accept_wait/accept_try/entry_count.
```

### ada83.c:55329

```
// Taken into rendezvous: push onto self's serving list (rv+32 is free
```

### ada83.c:55330

```
// once dequeued) so a death before accept_complete still releases the
```

### ada83.c:55331

```
// caller (RM 9.10).
```

### ada83.c:55343

```
// Accept wait: acceptor blocks until a matching entry call is queued on its
```

### ada83.c:55344

```
// own task. self_tcb is the accepting task's TCB (passed by codegen).
```

### ada83.c:55347

```
// Publish that this task is now blocked accepting entry_idx (RM 9.7.2
```

### ada83.c:55348

```
// conditional-call readiness); cleared once a call is taken.
```

### ada83.c:55358

```
// RM 9.10: if the acceptor itself is aborted while blocked here, abandon the
```

### ada83.c:55359

```
// accept and return null — codegen branches to the task's termination epilogue.
```

### ada83.c:55378

```
// Accept try: one non-blocking check (for SELECT alternatives).
```

### ada83.c:55387

```
// Is `entry_idx` among the OPEN alternatives of self's current blocked
```

### ada83.c:55388

```
// selective wait? The select published { i64 count, i64 idx[count] } at
```

### ada83.c:55389

```
// TCB+80 (closed alternatives hold -1, which matches no real index).
```

### ada83.c:55390

```
// Null list (not in a blocked select) matches nothing. Caller holds
```

### ada83.c:55391

```
// @__rv_mutex.
```

### ada83.c:55419

```
// Does the queue hold a call some OPEN alternative can serve? Exact
```

### ada83.c:55420

```
// matching against the published open-index list — an over-approximation
```

### ada83.c:55421

```
// here would wake the select's sweep, which takes nothing, and spin.
```

### ada83.c:55422

```
// Caller holds @__rv_mutex.
```

### ada83.c:55448

```
// Block a selective wait until something it could serve MIGHT have
```

### ada83.c:55449

```
// happened: a call for an open alternative arrives, an abort lands, or
```

### ada83.c:55450

```
// (timeout_us > 0) the deadline passes. Returns 1 iff the deadline
```

### ada83.c:55451

```
// passed. Wakes spuriously rather than misses: every producer — entry
```

### ada83.c:55452

```
// calls, aborts, completions, the terminate consensus — broadcasts
```

### ada83.c:55453

```
// @__term_cond under @__rv_mutex, and the queue/abort re-check here runs
```

### ada83.c:55454

```
// under the same lock, so a wakeup can never be lost. The caller
```

### ada83.c:55455

```
// re-sweeps its alternatives after every return.
```

### ada83.c:55487

```
// Entry'COUNT (RM 9.9): number of calls queued on entry_idx of task self.
```

### ada83.c:55515

```
// Accept complete: mark the rendezvous done and wake the blocked caller. The
```

### ada83.c:55516

```
// store is under @__rv_mutex + a broadcast so the caller's pthread_cond_wait
```

### ada83.c:55517

```
// observes it (and the flag is visible without a data race).
```

### ada83.c:55521

```
// Off the server's serving list first (the death path must not complete
```

### ada83.c:55522

```
// it twice), then publish completion and wake the caller directly.
```

### ada83.c:55550

```
// Finalization support
```

### ada83.c:55584

```
// TEXT_IO inline implementation (Ada 83 RM §14)
```

### ada83.c:55590

```
// Wrapper functions for pragma Import (C, ..., "__ada_stdin") etc. in text_io.adb.
```

### ada83.c:55591

```
// The three standard streams share one body shape; only the C global differs.
```

### ada83.c:55601

```
// Note: fputc, fgetc declared in main preamble; add remaining here
```

### ada83.c:55606

```
// Format string depends on INTEGER width: %d for i32, %lld for i64
```

### ada83.c:55614

```
// NEW_LINE: output line terminator
```

### ada83.c:55621

```
// PUT_CHAR: output single character
```

### ada83.c:55629

```
// PUT: output string (ptr + bounds in native STRING bound type).
```

### ada83.c:55630

```
// Bound type derived from STRING's index type via type system.
```

### ada83.c:55654

```
// PUT_LINE: output string followed by newline
```

### ada83.c:55663

```
// PUT_INTEGER: output integer with optional width
```

### ada83.c:55670

```
// PUT_FLOAT: output float value
```

### ada83.c:55677

```
// GET_CHAR: read single character
```

### ada83.c:55685

```
// GET_LINE: read line into buffer, return fat pointer.
```

### ada83.c:55686

```
// Bounds use STRING bound type derived from type system.
```

### ada83.c:55710

```
// adjlen is i64 from strlen; trunc directly to bound type
```

### ada83.c:55721

```
// 'IMAGE runtime: Integer'IMAGE (x) returns string representation
```

### ada83.c:55731

```
// Integer'IMAGE - convert integer to string fat pointer.
```

### ada83.c:55732

```
// Bounds use STRING bound type derived from type system.
```

### ada83.c:55740

```
// snprintf returns i32
```

### ada83.c:55747

```
// Character'IMAGE - single char to string (3 chars: 'x')
```

### ada83.c:55762

```
// Float'IMAGE - convert float to string
```

### ada83.c:55767

```
// snprintf returns i32
```

### ada83.c:55775

```
// Generate exception identity globals
```

### ada83.c:55778

```
// Generate implicit operators for frozen composite types
```

### ada83.c:55782

```
// End of header emission block
```

### ada83.c:55784

```
// Generate extern declarations for WITH'd packages
```

### ada83.c:55787

```
// RM 10.2/10.5: feed the elaboration graph this unit's context — WITH
```

### ada83.c:55788

```
// edges and Elaborate/Elaborate_All pragma edges.
```

### ada83.c:55791

```
// Generate declarations
```

### ada83.c:55796

```
// A spec-only library package with dynamic constant initializers (e.g. an
```

### ada83.c:55797

```
// address constant read from a function) has no body to host them. Emit its
```

### ada83.c:55798

```
// elaboration function here so those RM 7.2 initializations run, and register
```

### ada83.c:55799

```
// it for @main's elaboration sequence — otherwise the constants keep their
```

### ada83.c:55800

```
// zero placeholder and any FOR x USE AT <constant> overlay reads a null
```

### ada83.c:55801

```
// address.
```

### ada83.c:55805

```
// Only when no BODY of this package is emitted by THIS compilation:
```

### ada83.c:55806

```
// that body's own ___elab hosts the same initializers, and defining
```

### ada83.c:55807

```
// both is an IR-level duplicate (c83025c). A body merely known to the
```

### ada83.c:55808

```
// library does not count — it may never be linked (spprt13's spec-only
```

### ada83.c:55809

```
// address constants).
```

### ada83.c:55839

```
// Emit imported-subprogram externs deferred out of generic instantiations
```

### ada83.c:55842

```
// Emit buffered string constants at module level
```

### ada83.c:55847

```
// Reset buffer for next compilation unit
```

### ada83.c:55850

```
// Generate main function if this is a main program (library-level procedure).                   
```

### ada83.c:55851

```
// Emit @main() for the LAST parameterless library-level procedure in the file.                  
```

### ada83.c:55852

```
// We track main_candidate and emit at end of Compile_File instead.                              
```

### ada83.c:55853

```
//                                                                                                
```

### ada83.c:55858

```
// Check if this is a library-level procedure (no parameters) and NOT a
```

### ada83.c:55859

```
// SEPARATE subunit: is_separate marks only the STUB — a subunit's own
```

### ada83.c:55860

```
// compilation unit is recognized by its SEPARATE (parent) clause. A
```

### ada83.c:55861

```
// generic subprogram's subunit body pairs with the SYMBOL_GENERIC and
```

### ada83.c:55862

```
// is no main program either (ca2009d).
```

### ada83.c:55873

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:55874

```
// §13.10 BUILD-IN-PLACE - Limited Type Function Returns                                            
```

### ada83.c:55875

```
//                                                                                                  
```

### ada83.c:55876

```
// Ada limited types cannot be copied (RM 7.5). Functions returning limited                         
```

### ada83.c:55877

```
// types must construct the result directly in caller-provided space-the                            
```

### ada83.c:55878

```
// "Build-in-Place" (BIP) protocol. This eliminates intermediate temporaries.                      
```

### ada83.c:55879

```
//                                                                                                  
```

### ada83.c:55880

```
// The protocol passes extra hidden parameters to BIP functions:                                    
```

### ada83.c:55881

```
//   __BIPalloc  - Allocation form selector (caller space, heap, pool, etc.)                        
```

### ada83.c:55882

```
//   __BIPaccess - Pointer to destination where result is constructed                               
```

### ada83.c:55883

```
//   __BIPfinal  - Finalization collection (for controlled components)                              
```

### ada83.c:55884

```
//   __BIPmaster - Task master ID (for task components)                                             
```

### ada83.c:55885

```
//   __BIPchain  - Activation chain (for task components)                                           
```

### ada83.c:55886

```
//                                                                                                  
```

### ada83.c:55887

```
// Reference: Ada RM 7.5 (Limited Types), RM 6.5 (Return Statements)                                
```

### ada83.c:55888

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:55890

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:55891

```
// §13.10.1 Algebraic Types - Sum types for BIP protocol                                            
```

### ada83.c:55892

```
//                                                                                                  
```

### ada83.c:55893

```
// BIP_Alloc_Form determines where the function result is allocated:                                
```

### ada83.c:55894

```
//   - CALLER: Caller provides stack/object space (most common)                                     
```

### ada83.c:55895

```
//   - SECONDARY_STACK: Use secondary stack for dynamic-sized returns                               
```

### ada83.c:55896

```
//   - GLOBAL_HEAP: Allocate on heap (from 'new' expression)                                        
```

### ada83.c:55897

```
//   - USER_POOL: Use user-defined storage pool                                                     
```

### ada83.c:55898

```
//                                                                                                  
```

### ada83.c:55899

```
// BIP_Formal_Kind identifies which extra formal parameter is being accessed.                      
```

### ada83.c:55900

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:55902

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:55903

```
// §13.10.2 Type Predicates - Pure functions for BIP decisions                                      
```

### ada83.c:55904

```
//                                                                                                  
```

### ada83.c:55905

```
// These predicates determine whether a type requires BIP handling. Per Ada RM 7.5, limited types
```

### ada83.c:55906

```
// include:                                                           
```

### ada83.c:55907

```
//   - Task types (always limited)                                                                  
```

### ada83.c:55908

```
//   - Types with "limited" in their declaration                                                    
```

### ada83.c:55909

```
//   - Private types declared "limited private"                                                     
```

### ada83.c:55910

```
//   - Composite types with limited components                                                      
```

### ada83.c:55911

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:55913

```
// Check if record type has any limited components (recursive)
```

### ada83.c:55920

```
// Task component makes the record limited
```

### ada83.c:55923

```
// Limited private component makes the record limited
```

### ada83.c:55926

```
// Recursively check nested records
```

### ada83.c:55933

```
// Master predicate: Is this type limited? (RM 7.5)
```

### ada83.c:55937

```
// Build-in-place exists because some VALUES cannot be copied (tasks,
```

### ada83.c:55938

```
// limited composites). A limited type whose full view is scalar-
```

### ada83.c:55939

```
// represented (e.g. a private file handle completed as an integer)
```

### ada83.c:55940

```
// carries the SEMANTIC no-copy rule, enforced at resolution — its
```

### ada83.c:55941

```
// representation copies fine, so plain return applies.
```

### ada83.c:55944

```
// Limited by its flag (task types set it at creation, RM 9.2; also explicit
```

### ada83.c:55945

```
// `limited record`/`limited private` declarations).
```

### ada83.c:55948

```
// Limited private types
```

### ada83.c:55951

```
// Records with limited components
```

### ada83.c:55955

```
// Arrays of limited element type
```

### ada83.c:55962

```
// Does this function return a type requiring BIP?
```

### ada83.c:55968

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:55969

```
// §13.10.3 Extra Formal Parameters - Hidden BIP parameters                                         
```

### ada83.c:55970

```
//                                                                                                  
```

### ada83.c:55971

```
// BIP functions receive extra hidden parameters prepended to their formals:                        
```

### ada83.c:55972

```
//   __BIPalloc  : i32     (BIP_Alloc_Form enum value)                                              
```

### ada83.c:55973

```
//   __BIPaccess : ptr     (pointer to result destination)                                          
```

### ada83.c:55974

```
//   __BIPmaster : i32     (task master ID, if tasks)                                               
```

### ada83.c:55975

```
//   __BIPchain  : ptr     (activation chain, if tasks)                                             
```

### ada83.c:55976

```
//                                                                                                  
```

### ada83.c:55977

```
// These are added during code generation, not during semantic analysis,                            
```

### ada83.c:55978

```
// so the Symbol structure remains unchanged.                                                      
```

### ada83.c:55979

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:55981

```
// BIP extra formal names (matched by code generator)
```

### ada83.c:55988

```
// Count of BIP extra formals for a given function
```

### ada83.c:55991

```
// alloc_form + object_access always present
```

### ada83.c:55993

```
// task_master + activation_chain
```

### ada83.c:55995

```
// Future: finalization collection for controlled types
```

### ada83.c:55999

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:56000

```
// §13.10.4 Call-Site Transformation - Expanding BIP function calls                                 
```

### ada83.c:56001

```
//                                                                                                  
```

### ada83.c:56002

```
// When calling a BIP function, the caller must:                                                    
```

### ada83.c:56003

```
//   1. Determine allocation form (usually CALLER for declarations)                                 
```

### ada83.c:56004

```
//   2. Allocate destination space if CALLER                                                        
```

### ada83.c:56005

```
//   3. Pass extra BIP actuals before regular arguments                                             
```

### ada83.c:56006

```
//                                                                                                  
```

### ada83.c:56007

```
// Transform: X : Limited_Type := F(args);                                                          
```

### ada83.c:56008

```
// Into:      space = alloca(sizeof (Limited_Type))                                                 
```

### ada83.c:56009

```
//            F(__BIPalloc => CALLER, __BIPaccess => space, args)                                   
```

### ada83.c:56010

```
//            X = *space  (or X IS space if we alias)                                               
```

### ada83.c:56011

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:56013

```
// Determine allocation form from call context
```

### ada83.c:56018

```
// Propagate caller's
```

### ada83.c:56020

```
// Temp needed
```

### ada83.c:56023

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:56024

```
// §13.10.5 Return Statement Expansion - Building result in place                                   
```

### ada83.c:56025

```
//                                                                                                  
```

### ada83.c:56026

```
// In a BIP function, return statements build directly into __BIPaccess:                            
```

### ada83.c:56027

```
//                                                                                                  
```

### ada83.c:56028

```
//   return (Field1 => V1, Field2 => V2);                                                           
```

### ada83.c:56029

```
//                                                                                                  
```

### ada83.c:56030

```
// Becomes (for CALLER allocation):                                                                 
```

### ada83.c:56031

```
//   __BIPaccess->Field1 = V1;                                                                      
```

### ada83.c:56032

```
//   __BIPaccess->Field2 = V2;                                                                      
```

### ada83.c:56033

```
//   return;                                                                                        
```

### ada83.c:56034

```
//                                                                                                  
```

### ada83.c:56035

```
// For HEAP allocation, we allocate first then build:                                               
```

### ada83.c:56036

```
//   tmp = malloc (sizeof (T));                                                                     
```

### ada83.c:56037

```
//   tmp->Field1 = V1;                                                                              
```

### ada83.c:56038

```
//   tmp->Field2 = V2;                                                                              
```

### ada83.c:56039

```
//   *__BIPaccess = tmp;  // Return allocated pointer                                               
```

### ada83.c:56040

```
//   return;                                                                                        
```

### ada83.c:56041

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:56043

```
// Initialize BIP state for a new function
```

### ada83.c:56053

```
// Check if we're in a BIP function
```

### ada83.c:56058

```
// End BIP state for function
```

### ada83.c:56063

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:56064

```
// §14. LIBRARY MANAGEMENT - GNAT-Compatible Library Information                                    
```

### ada83.c:56065

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:56068

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:56069

```
// §14.1 Unit_Info - Compilation unit metadata collector                                            
```

### ada83.c:56070

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:56071

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:56072

```
// §14.2 CRC32 - Fast checksum for source identity                                                  
```

### ada83.c:56073

```
//                                                                                                  
```

### ada83.c:56074

```
// Standard CRC-32/ISO-HDLC polynomial: 0xEDB88320 (bit-reversed 0x04C11DB7)                        
```

### ada83.c:56075

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:56097

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:56098

```
// §14.3 Unit_Name_To_File - GNAT naming convention                                                 
```

### ada83.c:56099

```
//                                                                                                  
```

### ada83.c:56100

```
// Maps Ada unit names to file names:                                                               
```

### ada83.c:56101

```
//   Package_Name      > package_name.ads                                                           
```

### ada83.c:56102

```
//   Package_Name%b    > package_name.adb                                                           
```

### ada83.c:56103

```
//   Parent.Child      > parent-child.ads                                                           
```

### ada83.c:56104

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:56112

```
// Dots become hyphens
```

### ada83.c:56114

```
// Lowercase
```

### ada83.c:56120

```
// Append extension
```

### ada83.c:56127

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:56128

```
// §14.4 ALI_Collect - Gather unit info from parsed AST                                             
```

### ada83.c:56129

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:56137

```
// Each WITH clause may have multiple names
```

### ada83.c:56149

```
// TODO: detect LIMITED WITH
```

### ada83.c:56153

```
// Derive file names from unit name
```

### ada83.c:56170

```
// Helper: extract unit name from a subprogram spec/body
```

### ada83.c:56176

```
// Body has spec nested inside
```

### ada83.c:56184

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:56185

```
// §14.4.2 ALI_Collect_Exports - Gather exported symbols from package spec                          
```

### ada83.c:56186

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:56188

```
// Grow the export table; a silent cap would silently lose symbols on
```

### ada83.c:56189

```
// ALI fast-load.
```

### ada83.c:56304

```
// Exception identity
```

### ada83.c:56314

```
// Flatten a (possibly dotted) name AST into "A.B.C" text for unit naming.
```

### ada83.c:56348

```
// Extract unit name based on declaration kind
```

### ada83.c:56372

```
// Only reachable as a SEPARATE subunit (a task body is otherwise
```

### ada83.c:56373

```
// nested inside its package or subprogram body).
```

### ada83.c:56395

```
// A subunit's library name is PARENT.CHILD (RM 10.2: subunit names
```

### ada83.c:56396

```
// live in the parent's namespace); record the kind so the catalog can
```

### ada83.c:56397

```
// distinguish it from an ordinary body.
```

### ada83.c:56412

```
// The resolved unit's scope publishes the frame layout (F/FS lines).
```

### ada83.c:56415

```
// Compute source checksum
```

### ada83.c:56418

```
// Record the ACTUAL source path as given to the compiler (the old
```

### ada83.c:56419

```
// name-derived guess was a lie for any file whose name does not match
```

### ada83.c:56420

```
// its unit). Catalog loading uses a bare basename relative to the
```

### ada83.c:56421

```
// library directory and any other form relative to the compilation's
```

### ada83.c:56422

```
// working directory.
```

### ada83.c:56433

```
// Unit categorization pragmas (RM 10.5), as resolved on the symbol.
```

### ada83.c:56436

```
// Pure units have no elaboration
```

### ada83.c:56438

```
// RM 7.1: does this spec require a body? Subprogram declarations
```

### ada83.c:56439

```
// always do; package (and generic package) specs per their contents.
```

### ada83.c:56451

```
// RM 10.2: record the body's stubs so the binder can demand their
```

### ada83.c:56452

```
// subunits — and ignore subunits a replacement body no longer stubs.
```

### ada83.c:56489

```
// Collect WITH dependencies — per UNIT: a spec's W lines must not
```

### ada83.c:56490

```
// inherit the body's context (obsolescence is judged per unit).
```

### ada83.c:56493

```
// Collect exported symbols from package specs
```

### ada83.c:56499

```
// LLVM type signature derived from the type system via Symbol_Manager.
```

### ada83.c:56500

```
// Looks up the Ada type name in the symbol table and uses Type_To_Rep +
```

### ada83.c:56501

```
// LLVM_Rep_To_String to get the correct LLVM representation as a string slice.
```

### ada83.c:56502

```
//                                                                                                  
```

### ada83.c:56505

```
// Look up in the symbol table for proper type-system derivation
```

### ada83.c:56512

```
// Error: type not found in symbol table
```

### ada83.c:56519

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:56520

```
// §14.5 ALI_Write - Emit .ali file in GNAT format                                                  
```

### ada83.c:56521

```
//                                                                                                  
```

### ada83.c:56522

```
// Per lib-writ.ads, the minimum valid ALI file needs:                                              
```

### ada83.c:56523

```
//   V line (version) - MUST be first                                                               
```

### ada83.c:56524

```
//   P line (parameters) - MUST be present                                                          
```

### ada83.c:56525

```
//   At least one U line (unit)                                                                     
```

### ada83.c:56526

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:56531

```
// V line: Version - must be first per GNAT spec
```

### ada83.c:56534

```
// P line: Parameters/flags - ZX = zero-cost exceptions
```

### ada83.c:56537

```
// Blank line before restrictions
```

### ada83.c:56540

```
// R line: Restrictions (minimal)
```

### ada83.c:56543

```
// U lines: Unit entries
```

### ada83.c:56547

```
// U unit-name source-name version [flags]
```

### ada83.c:56554

```
// Flags
```

### ada83.c:56560

```
// PK/SU discriminate the unit KIND (package vs subprogram), for either
```

### ada83.c:56561

```
// spec or body — GNAT ALI semantics. A spec lookup may accept a
```

### ada83.c:56562

```
// subprogram's body entry (the body is its own declaration, RM 10.1)
```

### ada83.c:56563

```
// but never a package's (a package body declares nothing, RM 7.1).
```

### ada83.c:56568

```
// ST lines: the body's stub names (RM 10.2), for the binder.
```

### ada83.c:56573

```
// F/FS lines: the unit's frame layout, the cross-compilation ground
```

### ada83.c:56574

```
// truth for subunits that share this frame. One F line per
```

### ada83.c:56575

```
// frame-resident variable (emitted name + byte offset), then the
```

### ada83.c:56576

```
// frame's alloca size (the static-chain slot sits at that offset).
```

### ada83.c:56579

```
// Dedup by emitted name, LAST occurrence wins: resolution may hold
```

### ada83.c:56580

```
// duplicate symbol objects for one variable (spec re-installation),
```

### ada83.c:56581

```
// and declaration emission binds to the latest — the published
```

### ada83.c:56582

```
// offset must be the one the module actually stores at.
```

### ada83.c:56639

```
// W lines: WITH dependencies for this unit
```

### ada83.c:56647

```
// Assume spec dependency
```

### ada83.c:56659

```
// D lines: Source dependencies
```

### ada83.c:56664

```
// Self-dependency
```

### ada83.c:56679

```
// X lines: Exported symbols (extended format for Ada83 separate compilation)                     
```

### ada83.c:56680

```
//                                                                                                
```

### ada83.c:56681

```
// Format: X kind name:line llvm_type @mangled [ada_type] [(params)]                              
```

### ada83.c:56682

```
//                                                                                                
```

### ada83.c:56683

```
//   kind: T=type, S=subtype, V=variable, C=constant, P=procedure, F=function, E=exception        
```

### ada83.c:56684

```
//   llvm_type: LLVM IR type signature (i64, double, void, ptr, etc.)                             
```

### ada83.c:56685

```
//   @mangled: LLVM symbol name for linking                                                       
```

### ada83.c:56686

```
//   ada_type: Ada type name for typed symbols                                                    
```

### ada83.c:56687

```
//   (params): Parameter count for subprograms                                                    
```

### ada83.c:56688

```
//                                                                                                
```

### ada83.c:56689

```
// This provides everything needed to compile against the package without source:                 
```

### ada83.c:56690

```
//   - Type checking via ada_type                                                                 
```

### ada83.c:56691

```
//   - Code generation via llvm_type and @mangled                                                 
```

### ada83.c:56692

```
//   - Linking via @mangled symbol references                                                     
```

### ada83.c:56693

```
//                                                                                                
```

### ada83.c:56699

```
// X kind name:line
```

### ada83.c:56705

```
// llvm_type
```

### ada83.c:56712

```
// @mangled
```

### ada83.c:56717

```
// ada_type (for typed symbols)
```

### ada83.c:56722

```
// (params) for subprograms
```

### ada83.c:56731

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:56732

```
// §14.6 Library Catalog - The program library's unit -> file map (RM 10.4)
```

### ada83.c:56733

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:56766

```
// RM 10.3: a LATER compilation of the same unit replaces the earlier
```

### ada83.c:56767

```
// one. Directory scan order is arbitrary; the ALI's mtime decides.
```

### ada83.c:56768

```
// Replacement resets the per-registration facts (frame layout, WITH
```

### ada83.c:56769

```
// deps, stubs) — the new ALI's lines repopulate them.
```

### ada83.c:56807

```
// RM 10.3 consistency for a WITH'd unit at main-program compile time:
```

### ada83.c:56808

```
// a body compiled before the unit's current specification is obsolete,
```

### ada83.c:56809

```
// and so is a subunit compiled before its parent's current body.
```

### ada83.c:56815

```
// A fileless body entry is Mark_Body_Loaded's in-process load marker,
```

### ada83.c:56816

```
// not a compiled unit — no obsolescence to judge.
```

### ada83.c:56819

```
// An obsolete OPTIONAL body is simply no longer part of the program
```

### ada83.c:56820

```
// (the loader drops it); only a REQUIRED body's obsolescence forbids
```

### ada83.c:56821

```
// execution. Subunit staleness is judged against the current body's
```

### ada83.c:56822

```
// stubs (ST lines) — a replacement body may have dropped the stub.
```

### ada83.c:56850

```
// The binder (RM 10.3/10.5, GNAT's gnatbind): verify the catalog closure
```

### ada83.c:56851

```
// of a main program AFTER all compilations. A unit whose WITH'd
```

### ada83.c:56852

```
// specification was recompiled later than the unit itself is obsolete,
```

### ada83.c:56853

```
// and an obsolete unit anywhere in the closure forbids execution.
```

### ada83.c:56854

```
// Returns the number of consistency errors found.
```

### ada83.c:56865

```
// Spec/body and subunit recompilation order for this unit.
```

### ada83.c:56870

```
// Walk every catalog entry belonging to the unit: its spec, its
```

### ada83.c:56871

```
// body, and its subunits (whose names extend "UNIT.").
```

### ada83.c:56877

```
// The body's stubs (ST lines) demand current subunits (RM 10.2).
```

### ada83.c:56903

```
// Subunits join the closure walk through their own deps.
```

### ada83.c:56916

```
// The dependency's specification governs obsolescence; a
```

### ada83.c:56917

```
// subprogram compiled body-only carries its implicit spec in
```

### ada83.c:56918

```
// the body entry, so recompiling that body recompiles the spec.
```

### ada83.c:56924

```
// An obsolete OPTIONAL body was dropped from the program by the
```

### ada83.c:56925

```
// dependent's compilation — only a required body (or a spec)
```

### ada83.c:56926

```
// going stale is a bind error.
```

### ada83.c:56943

```
// Enqueue the dependency once.
```

### ada83.c:56964

```
// Scan a directory's .ali files and register every U line's unit. The
```

### ada83.c:56965

```
// catalog stores source paths joined to the directory so a later
```

### ada83.c:56966

```
// compilation from any working directory still finds the file. A stale or
```

### ada83.c:56967

```
// malformed ALI contributes nothing (loudly, not silently — the name
```

### ada83.c:56968

```
// lookup falls back to convention and the obsolescence pass re-checks).
```

### ada83.c:57033

```
// W <unit-name>%s [<source-file> <ali-file>] — record the WITH
```

### ada83.c:57034

```
// dependency for the binder's closure walk.
```

### ada83.c:57052

```
// U <unit-name>%s|%b <source-file> <checksum> [flags...]
```

### ada83.c:57085

```
// Absolute or CWD-relative path: use as recorded.
```

### ada83.c:57095

```
// A rejected (stale) registration keeps the newer ALI's facts:
```

### ada83.c:57096

```
// this older file's F/W/ST lines must not attach to it.
```

### ada83.c:57114

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57115

```
// §14.7 Generate_ALI_File - Entry point for ALI generation
```

### ada83.c:57116

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57122

```
// Build ALI path from output path (replace .ll with .ali)
```

### ada83.c:57136

```
// Collect information from all compilation units
```

### ada83.c:57142

```
// Write ALI file
```

### ada83.c:57148

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57149

```
// §14.8 ALI_Reader - Parse .ali files for dependency management                                    
```

### ada83.c:57150

```
//                                                                                                  
```

### ada83.c:57151

```
// We read ALI files to:                                                                            
```

### ada83.c:57152

```
//   1. Skip recompilation of unchanged units (checksum match)                                      
```

### ada83.c:57153

```
//   2. Load exported symbols from precompiled packages                                             
```

### ada83.c:57154

```
//   3. Track dependencies for elaboration ordering                                                 
```

### ada83.c:57155

```
//   4. Find generic templates for instantiation                                                    
```

### ada83.c:57156

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57157

```
// Global ALI cache                                                                                 
```

### ada83.c:57158

```
//                                                                                                  
```

### ada83.c:57162

```
// Skip whitespace
```

### ada83.c:57168

```
// Read until whitespace or newline, return end pointer
```

### ada83.c:57179

```
// Parse a hex value
```

### ada83.c:57192

```
// Read and parse an ALI file, returning cache entry or NULL
```

### ada83.c:57195

```
// Check if already cached
```

### ada83.c:57202

```
// Read ALI file
```

### ada83.c:57206

```
// Cache full: refuse the CACHE slot, not the load — the caller falls
```

### ada83.c:57207

```
// back to parsing the source, which is correct, only slower.
```

### ada83.c:57220

```
// Version line: V "version" - reject if compiler build differs.                               
```

### ada83.c:57221

```
// This ensures stale ALI files from an older compiler rebuild                                  
```

### ada83.c:57222

```
// are not reused; the unit will be reparsed from source.                                      
```

### ada83.c:57223

```
//                                                                                              
```

### ada83.c:57236

```
// ALI was produced by a different compiler build - stale
```

### ada83.c:57239

```
// release the cache slot
```

### ada83.c:57246

```
// Parameters line: P flags
```

### ada83.c:57251

```
// Unit line: U name source checksum [flags]
```

### ada83.c:57252

```
// Skip 'U'
```

### ada83.c:57254

```
// Unit name (with %s/%b suffix)
```

### ada83.c:57262

```
// Source file
```

### ada83.c:57266

```
// Checksum
```

### ada83.c:57270

```
// Parse flags
```

### ada83.c:57280

```
// With line: W/Y/Z name [source ali] [flags]
```

### ada83.c:57283

```
// Strip %s/%b suffix
```

### ada83.c:57292

```
// Dependency line: D source timestamp checksum - informational
```

### ada83.c:57297

```
// Export line: X kind name:line llvm_type @mangled [ada_type] [(params)]                     
```

### ada83.c:57298

```
//                                                                                            
```

### ada83.c:57299

```
// Example: X F Get_Value:9 i64 @test_exports__get_value Counter                              
```

### ada83.c:57300

```
//                                                                                            
```

### ada83.c:57313

```
// Skip 'X'
```

### ada83.c:57315

```
// Kind (single char: T/S/V/C/P/F/E)
```

### ada83.c:57319

```
// Name:line
```

### ada83.c:57331

```
// llvm_type
```

### ada83.c:57338

```
// @mangled
```

### ada83.c:57341

```
// Skip '@'
```

### ada83.c:57346

```
// Remaining tokens: ada_type and/or (params)
```

### ada83.c:57351

```
// (params)
```

### ada83.c:57356

```
// ada_type
```

### ada83.c:57371

```
// Check if an ALI file is up-to-date with its source
```

### ada83.c:57376

```
// Read source and compute checksum
```

### ada83.c:57385

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57386

```
// §15. ELABORATION MODEL - Standard-Style Dependency Graph Algorithm                               
```

### ada83.c:57387

```
//                                                                                                  
```

### ada83.c:57388

```
// Implements the full Standard elaboration ordering algorithm as described                         
```

### ada83.c:57389

```
// in bindo-elaborators.adb. This determines the safe order in which library                        
```

### ada83.c:57390

```
// units must be elaborated at program startup (Ada RM 10.2).                                      
```

### ada83.c:57391

```
//                                                                                                  
```

### ada83.c:57392

```
// The algorithm proceeds in phases:                                                                
```

### ada83.c:57393

```
//   1. BUILD GRAPH: Create vertices for units, edges for dependencies                              
```

### ada83.c:57394

```
//   2. FIND COMPONENTS: Tarjan's SCC for cyclic dependency handling                                
```

### ada83.c:57395

```
//   3. ELABORATE: Topological sort with priority ordering                                          
```

### ada83.c:57396

```
//   4. VALIDATE: Verify all constraints satisfied                                                  
```

### ada83.c:57397

```
//                                                                                                  
```

### ada83.c:57398

```
// Key insight from GNAT: Edges are classified as "strong" (must-satisfy) or                        
```

### ada83.c:57399

```
// "weak" (can-ignore-for-dynamic-model). This allows breaking cycles when                          
```

### ada83.c:57400

```
// compiled with -gnatE (dynamic elaboration checking).                                            
```

### ada83.c:57401

```
//                                                                                                  
```

### ada83.c:57402

```
// Style: Haskell-like C99 with algebraic data types (tagged unions),                               
```

### ada83.c:57403

```
// pure functions where possible, and composition over mutation.                                   
```

### ada83.c:57404

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57406

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57407

```
// §15.1 Algebraic Types - Sum types via tagged unions                                              
```

### ada83.c:57408

```
//                                                                                                  
```

### ada83.c:57409

```
// Following the Haskell pattern: data Kind = A | B | C                                             
```

### ada83.c:57410

```
// In C99: enum for tag, union for payload, struct wrapper.                                        
```

### ada83.c:57411

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57413

```
// Is this edge kind inherently strong?
```

### ada83.c:57418

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57419

```
// §15.4 Graph Structure - Vertices + Edges + Components                                            
```

### ada83.c:57420

```
//                                                                                                  
```

### ada83.c:57421

```
// Uses arena allocation for vertices/edges, dynamic arrays for order.                             
```

### ada83.c:57422

```
// Maximum capacities chosen to handle large Ada programs.                                         
```

### ada83.c:57423

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57425

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57426

```
// §15.5 Graph Construction - Pure creation functions                                               
```

### ada83.c:57427

```
//                                                                                                  
```

### ada83.c:57428

```
// Functions return new graph/vertex/edge without side effects.                                    
```

### ada83.c:57429

```
// Following functional style: prefer immutable creation over mutation.                            
```

### ada83.c:57430

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57432

```
// Initialize an empty graph
```

### ada83.c:57438

```
// Add a vertex, returning its ID (or 0 on failure)
```

### ada83.c:57442

```
// IDs are 1-based
```

### ada83.c:57462

```
// Find vertex by name, returning ID (or 0 if not found)
```

### ada83.c:57475

```
// Get vertex by ID (1-based), returns NULL if invalid
```

### ada83.c:57483

```
// Add an edge from pred_id to succ_id, returning edge ID (or 0 on failure)
```

### ada83.c:57488

```
// No self-loops
```

### ada83.c:57490

```
// Check for duplicate edge
```

### ada83.c:57496

```
// Already exists
```

### ada83.c:57511

```
// Thread into predecessor's outgoing list
```

### ada83.c:57517

```
// Thread into successor's incoming list
```

### ada83.c:57523

```
// Update pending counts
```

### ada83.c:57530

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57531

```
// §15.6 Tarjan's SCC Algorithm - Find strongly connected components                                
```

### ada83.c:57532

```
//                                                                                                  
```

### ada83.c:57533

```
// Standard O(V+E) algorithm for finding SCCs. Each SCC becomes a component                         
```

### ada83.c:57534

```
// that must be elaborated together (handles circular dependencies).                               
```

### ada83.c:57535

```
//                                                                                                  
```

### ada83.c:57536

```
// Invariant: After completion, every vertex has a non-zero component_id.                          
```

### ada83.c:57537

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57546

```
// Push onto stack
```

### ada83.c:57550

```
// Visit all successors
```

### ada83.c:57555

```
// Successor not yet visited
```

### ada83.c:57561

```
// Successor is on stack, part of current SCC
```

### ada83.c:57569

```
// If v is a root node, pop SCC from stack
```

### ada83.c:57586

```
// Reset Tarjan state
```

### ada83.c:57595

```
// Run Tarjan's algorithm
```

### ada83.c:57602

```
// Compute component-level predecessor counts
```

### ada83.c:57614

```
// Check for Elaborate_All edge in cycle (fatal)
```

### ada83.c:57623

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57624

```
// §15.7 Vertex Predicates - Pure functions for elaboration decisions                               
```

### ada83.c:57625

```
//                                                                                                  
```

### ada83.c:57626

```
// These predicates determine vertex eligibility and priority.                                     
```

### ada83.c:57627

```
// All are pure (no side effects) for functional composition.                                      
```

### ada83.c:57628

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57630

```
// Can this vertex be elaborated now? (all strong predecessors done)
```

### ada83.c:57635

```
// Can this vertex be weakly elaborated? (only weak predecessors remain)
```

### ada83.c:57641

```
// Does this spec vertex have an elaborable body?
```

### ada83.c:57644

```
// reserved for future use
```

### ada83.c:57647

```
// pragma Elaborate_Body forces it
```

### ada83.c:57651

```
// Compare two vertices for elaboration priority.                                                  
```

### ada83.c:57652

```
// Returns PREC_HIGHER if a should elaborate before b.                                             
```

### ada83.c:57653

```
// Per GNAT bindo-elaborators.adb Is_Better_Elaborable_Vertex.                                     
```

### ada83.c:57654

```
//                                                                                                  
```

### ada83.c:57658

```
// reserved for future use
```

### ada83.c:57661

```
// 1. Prefer spec with Elaborate_Body before its paired body
```

### ada83.c:57665

```
// 2. Prefer predefined units (Ada.*, System.*, Interfaces.*)
```

### ada83.c:57669

```
// 3. Prefer internal units (GNAT.*)
```

### ada83.c:57673

```
// 4. Prefer preelaborated units
```

### ada83.c:57677

```
// 5. Prefer pure units
```

### ada83.c:57681

```
// 6. Lexicographical tiebreaker for determinism
```

### ada83.c:57692

```
// Compare for weak elaboration: prefer fewer weak predecessors
```

### ada83.c:57702

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57703

```
// §15.8 Vertex Set Operations - Functional set manipulation                                        
```

### ada83.c:57704

```
//                                                                                                  
```

### ada83.c:57705

```
// Uses bitmap representation for O(1) membership testing.                                         
```

### ada83.c:57706

```
// Pure functions that return new sets rather than mutating.                                       
```

### ada83.c:57707

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57728

```
// Brian Kernighan's trick
```

### ada83.c:57733

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57734

```
// §15.9 Best Vertex Selection - Find optimal elaboration candidate                                 
```

### ada83.c:57735

```
//                                                                                                  
```

### ada83.c:57736

```
// Scans a vertex set to find the best candidate using a comparator.                               
```

### ada83.c:57737

```
// Pure function with no side effects.                                                             
```

### ada83.c:57738

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57762

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57763

```
// §15.10 Elaboration Core - The main elaboration algorithm                                         
```

### ada83.c:57764

```
//                                                                                                  
```

### ada83.c:57765

```
// Implements the Standard elaboration loop:                                                        
```

### ada83.c:57766

```
//   1. Create elaborable/waiting vertex sets                                                       
```

### ada83.c:57767

```
//   2. Repeatedly find best elaborable vertex                                                      
```

### ada83.c:57768

```
//   3. Elaborate it and update successor counts                                                    
```

### ada83.c:57769

```
//   4. Handle weak elaboration for cycles                                                          
```

### ada83.c:57770

```
//                                                                                                  
```

### ada83.c:57771

```
// Per bindo-elaborators.adb Elaborate_Library_Graph.                                              
```

### ada83.c:57772

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57774

```
// Update successor when predecessor is elaborated
```

### ada83.c:57785

```
// Decrement appropriate predecessor count
```

### ada83.c:57791

```
// Update component counts if cross-component edge
```

### ada83.c:57800

```
// Move successor from waiting to elaborable if now ready
```

### ada83.c:57805

```
// Also update complement (spec/body pair)
```

### ada83.c:57815

```
// Elaborate a single vertex
```

### ada83.c:57822

```
// Mark as elaborated
```

### ada83.c:57827

```
// Add to elaboration order
```

### ada83.c:57831

```
// Update all successors
```

### ada83.c:57838

```
// If this is a spec with Elaborate_Body, immediately elaborate the body
```

### ada83.c:57844

```
// If this spec has an elaborable body, elaborate it too
```

### ada83.c:57850

```
// Main elaboration algorithm
```

### ada83.c:57853

```
// Initialize vertex sets
```

### ada83.c:57866

```
// Main elaboration loop
```

### ada83.c:57867

```
// Find best elaborable vertex
```

### ada83.c:57873

```
// If no strongly elaborable vertex, try weak elaboration
```

### ada83.c:57880

```
// If still nothing, we have an unresolvable cycle
```

### ada83.c:57891

```
// Pair spec and body vertices after all vertices are added
```

### ada83.c:57897

```
// Find matching body
```

### ada83.c:57905

```
// Found the pair
```

### ada83.c:57909

```
// Add spec-before-body edge
```

### ada83.c:57916

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57917

```
// §15.12 Elaboration Order API - Public interface                                                  
```

### ada83.c:57918

```
//                                                                                                  
```

### ada83.c:57919

```
// These functions are called from the main driver (§19) and code generator                         
```

### ada83.c:57920

```
// (§13) to determine and use the elaboration order.                                               
```

### ada83.c:57921

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:57923

```
// Global elaboration graph (initialized during compilation)
```

### ada83.c:57927

```
// Initialize the global elaboration graph
```

### ada83.c:57935

```
// Add a unit to the elaboration graph
```

### ada83.c:57955

```
// Find-or-add a vertex without touching the elaboration flags Register_Unit
```

### ada83.c:57956

```
// owns (a context reference must not clobber a unit's own registration).
```

### ada83.c:57963

```
// RM 10.5 invocation edge: a call executed during CALLER_UNIT's library
```

### ada83.c:57964

```
// elaboration needs CALLEE's enclosing library unit elaborated first —
```

### ada83.c:57965

```
// a WEAK preference (EDGE_INVOCATION), so it guides the order without
```

### ada83.c:57966

```
// manufacturing unsatisfiable cycles.
```

### ada83.c:57973

```
// intra-unit call
```

### ada83.c:57976

```
// categorized units need no invocation ordering
```

### ada83.c:57985

```
// RM 10.2 / 10.5: a compilation unit's context clause becomes elaboration
```

### ada83.c:57986

```
// edges. Every WITH'd unit's SPEC elaborates before this unit (EDGE_WITH);
```

### ada83.c:57987

```
// pragma Elaborate (X) orders X's BODY before this unit (EDGE_ELABORATE);
```

### ada83.c:57988

```
// pragma Elaborate_All (X) does the same with transitive strength
```

### ada83.c:57989

```
// (EDGE_ELABORATE_ALL). Vertices are find-or-add — the unit's own code
```

### ada83.c:57990

```
// generation completes the flags when its body registers.
```

### ada83.c:58006

```
// A library instantiation elaborates its bindings as one unit;
```

### ada83.c:58007

```
// it joins the graph under the instance name (UNIT_BODY: the
```

### ada83.c:58008

```
// bindings ARE its body-level elaboration).
```

### ada83.c:58013

```
// subprogram units order through main, not the graph
```

### ada83.c:58019

```
// WITH clauses: the named unit's spec elaborates first.
```

### ada83.c:58032

```
// Context pragmas Elaborate / Elaborate_All: the named unit's BODY
```

### ada83.c:58033

```
// elaborates before this unit.
```

### ada83.c:58056

```
// Compute the elaboration order (call after all units registered)
```

### ada83.c:58060

```
// Pair specs with their bodies
```

### ada83.c:58063

```
// Find strongly connected components
```

### ada83.c:58066

```
// Compute elaboration order
```

### ada83.c:58070

```
// Get the computed elaboration order
```

### ada83.c:58084

```
// Pure units never need elaboration calls
```

### ada83.c:58087

```
// Preelaborate units without explicit elab code don't need calls
```

### ada83.c:58090

```
// A body's ___elab is called at the body's position in the order.
```

### ada83.c:58093

```
// A bodyless spec with a SYNTHESIZED ___elab (dynamic initializers,
```

### ada83.c:58094

```
// deferred subtype checks, address overlays) is called at the spec's
```

### ada83.c:58095

```
// position — leaving it to the tail sweep would run it after its
```

### ada83.c:58096

```
// dependents, reading uninitialized state (cd5003a: PKG2's overlay
```

### ada83.c:58097

```
// address reads SPPRT13's dynamic constants).
```

### ada83.c:58102

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:58103

```
// §16. GENERIC INSTANTIATION - Tree-copy cloners
```

### ada83.c:58104

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:58106

```
// Clone a node list, structurally (Clone_Subtree on each element).
```

### ada83.c:58120

```
// Re-resolution owns every semantic decoration: the clone is ordinary
```

### ada83.c:58121

```
// unresolved syntax. Source locations stay with the template so
```

### ada83.c:58122

```
// diagnostics point at the generic's text.
```

### ada83.c:58187

```
// A reference to the remapped entity may live in the TYPE decoration alone:
```

### ada83.c:58188

```
// an attribute such as T'BASE carries the formal placeholder's Type_Info as
```

### ada83.c:58189

```
// node->type with no symbol reference at all. Swing those too, or an
```

### ada83.c:58190

```
// instance evaluates the template placeholder — unset bounds — instead of
```

### ada83.c:58191

```
// the actual's view (cc1220a: T'BASE'FIRST as a formal-object default).
```

### ada83.c:58226

```
// Read "<base>.<ext>" if it exists, else NULL.
```

### ada83.c:58233

```
// Find a package source file in include paths
```

### ada83.c:58234

```
// Build the lowercased "<include_path[index]>/<name>" filename into `out`.
```

### ada83.c:58244

```
// Search every include path for `name` with the primary extension, falling
```

### ada83.c:58245

```
// back to ".ada" (which serves both specs and bodies).
```

### ada83.c:58262

```
// Read a catalog entry's file; NULL when the entry is absent, fileless
```

### ada83.c:58263

```
// (registered in-process without a source), or unreadable.
```

### ada83.c:58273

```
// The program library catalog answers first (it knows units whose file
```

### ada83.c:58274

```
// names match nothing); the GNAT-style naming convention is the fallback.
```

### ada83.c:58280

```
// A library SUBPROGRAM has only a body entry, and that body IS its
```

### ada83.c:58281

```
// declaration (RM 10.1); the catalog's pick (the NEWEST compilation,
```

### ada83.c:58282

```
// RM 10.3 replacement) must outrank a name-derived file that happens
```

### ada83.c:58283

```
// to exist with an older version. A PACKAGE body declares nothing
```

### ada83.c:58284

```
// (RM 7.1) — serving it as the spec would lose every spec symbol.
```

### ada83.c:58293

```
// Check if a precompiled .ll file exists for a package in include paths
```

### ada83.c:58305

```
// Find a package body source file: catalog first, then include paths.
```

### ada83.c:58311

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:58312

```
// §17. FILE LOADING - Include Path and Unit Loading                                                
```

### ada83.c:58313

```
//                                                                                                  
```

### ada83.c:58314

```
// Resolves Ada WITH clauses by searching include paths for source files,                           
```

### ada83.c:58315

```
// loading package specs/bodies on demand, and tracking which units have                            
```

### ada83.c:58316

```
// already been loaded to avoid duplicate processing.                                              
```

### ada83.c:58317

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:58324

```
// Set by `-g` on the command line. Propagated into `cg` at Code_Generator_Init
```

### ada83.c:58325

```
// time so every emitted IR line gets a trailing `; @ FUNC:LINE` comment.
```

### ada83.c:58328

```
// Set by `--clone-check` on the command line. Swaps every parsed compilation
```

### ada83.c:58329

```
// unit for its structural deep clone before resolution; output must be
```

### ada83.c:58330

```
// identical to a normal compile, so any difference convicts a missing or
```

### ada83.c:58331

```
// wrong edge in SYNTAX_TREE_SHAPE.
```

### ada83.c:58334

```
// --native: finish in-process, IR -> host executable via the runtime-loaded
```

### ada83.c:58335

```
// LLVM-C backend (§20).
```

### ada83.c:58338

```
// Track loaded package bodies for code generation (arena-grown, no cap)
```

### ada83.c:58356

```
// "Already loaded into this compilation" is the catalog's `loaded` flag —
```

### ada83.c:58357

```
// one tracking structure, not two (the old Loaded_Body_Names array died
```

### ada83.c:58358

```
// into the catalog).
```

### ada83.c:58393

```
// Swap with last and decrement count
```

### ada83.c:58399

```
// Find ALI file for a unit name in include paths
```

### ada83.c:58404

```
// Convert unit name to file name (lowercase, dots to hyphens)
```

### ada83.c:58414

```
// Try each include path
```

### ada83.c:58424

```
// Load symbols from an ALI file into the symbol manager
```

### ada83.c:58429

```
// Recursively load dependencies first
```

### ada83.c:58438

```
// For specs, create package symbol and exports
```

### ada83.c:58442

```
// Check if package already exists
```

### ada83.c:58446

```
// Create package symbol
```

### ada83.c:58452

```
// Allocate exported array for qualified access
```

### ada83.c:58458

```
// Push package scope to add exports
```

### ada83.c:58461

```
// Create symbols from exports                                                                    
```

### ada83.c:58462

```
//                                                                                                
```

### ada83.c:58463

```
// The mangled_name from ALI becomes external_name on Symbol,                                     
```

### ada83.c:58464

```
// enabling direct LLVM references without re-mangling.                                          
```

### ada83.c:58465

```
//                                                                                                
```

### ada83.c:58475

```
// Type: create type symbol with size derived from exported LLVM type.                       
```

### ada83.c:58476

```
// Type_New defaults to 4 bytes (i32) which is wrong for smaller                              
```

### ada83.c:58477

```
// types like 3-value enumerations (i8). Use the LLVM type                                   
```

### ada83.c:58478

```
// signature from the ALI export to set the correct size so that                              
```

### ada83.c:58479

```
// cross-compilation-unit type widths are consistent.                                        
```

### ada83.c:58480

```
//                                                                                            
```

### ada83.c:58492

```
// Subtype: link to base type
```

### ada83.c:58505

```
// Variable: external reference
```

### ada83.c:58518

```
// Constant: external reference
```

### ada83.c:58531

```
// Procedure: external subprogram declaration
```

### ada83.c:58540

```
// Function: external subprogram declaration
```

### ada83.c:58555

```
// Exception: external exception identity
```

### ada83.c:58564

```
// Set parent for proper scoping
```

### ada83.c:58567

```
// Also add to package exported list for qualified access; the
```

### ada83.c:58568

```
// array was allocated for exactly entry->export_count symbols.
```

### ada83.c:58577

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:58578

```
// §17.1 ALI-Based Loading                                                                          
```

### ada83.c:58579

```
//                                                                                                  
```

### ada83.c:58580

```
// Try_Load_From_ALI is called by Load_Package_Spec to attempt fast loading                         
```

### ada83.c:58581

```
// from a pre-existing ALI file, bypassing full source parsing when the                             
```

### ada83.c:58582

```
// source checksum matches.                                                                        
```

### ada83.c:58583

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:58585

```
// Try_Load_From_ALI - Called by Load_Package_Spec to attempt ALI-based loading                     
```

### ada83.c:58586

```
//                                                                                                  
```

### ada83.c:58587

```
// This is the entry point for ALI-based separate compilation:                                      
```

### ada83.c:58588

```
//   1. Look for ALI file in include paths                                                          
```

### ada83.c:58589

```
//   2. Verify checksum against source                                                              
```

### ada83.c:58590

```
//   3. Load symbols directly from ALI X lines                                                      
```

### ada83.c:58591

```
//                                                                                                  
```

### ada83.c:58592

```
// Returns true if successful (caller should skip parsing).                                        
```

### ada83.c:58593

```
//                                                                                                  
```

### ada83.c:58627

```
// RM 7.1: a package specification requires a body when it declares
```

### ada83.c:58628

```
// something only a body can complete — a subprogram, a task unit, a
```

### ada83.c:58629

```
// generic unit, or an incomplete type with no full declaration in the
```

### ada83.c:58630

```
// same specification.
```

### ada83.c:58651

```
// Incomplete type: a full declaration later in the spec
```

### ada83.c:58652

```
// completes it; otherwise the body must (RM 3.8.1).
```

### ada83.c:58698

```
// A subunit among the CURRENT compilation's units satisfies a stub just as
```

### ada83.c:58699

```
// a library-compiled one does (RM 10.2: subunits may share the compilation
```

### ada83.c:58700

```
// with their parent — c94008d keeps SEPARATE (SHARED_C94008D) TASK BODY
```

### ada83.c:58701

```
// SHARE in the same file as the generic and the main).
```

### ada83.c:58728

```
// RM 10.2 stub completeness of a LOADED unit: every body stub in a
```

### ada83.c:58729

```
// WITH'd dependency must have a compiled subunit in the library by the
```

### ada83.c:58730

```
// time a main program is compiled. The compilation that contains the
```

### ada83.c:58731

```
// stub itself is exempt — its subunit may legitimately follow later.
```

### ada83.c:58772

```
// RM 10.3/10.5: compiling a main program is this compiler's bind point —
```

### ada83.c:58773

```
// the library it closes over must be complete and consistent. Checks the
```

### ada83.c:58774

```
// main's direct WITHs for obsolete bodies/subunits and uncompiled generic
```

### ada83.c:58775

```
// bodies, and every loaded dependency body for unsatisfied stubs.
```

### ada83.c:58794

```
// A generic SUBPROGRAM always requires a body; a generic
```

### ada83.c:58795

```
// PACKAGE only when its spec does (RM 7.1, RM 12.2).
```

### ada83.c:58806

```
// Stubs inside a generic's body need compiled subunits just as
```

### ada83.c:58807

```
// non-generic bodies do (RM 10.2).
```

### ada83.c:58854

```
// Build source path from unit name
```

### ada83.c:58865

```
// Find full source path in include paths
```

### ada83.c:58875

```
// Check if ALI is current
```

### ada83.c:58877

```
// Stale - need to recompile
```

### ada83.c:58880

```
// Read ALI and check for exports
```

### ada83.c:58884

```
// Load symbols from ALI
```

### ada83.c:58912

```
// Context pragmas resolve through the ordinary pragma path — that is how
```

### ada83.c:58913

```
// configuration pragmas (RESTRICTIONS, PROFILE) take effect for the unit.
```

### ada83.c:58914

```
// ELABORATE/ELABORATE_ALL fall through it untouched; the elaboration
```

### ada83.c:58915

```
// graph walk reads them separately.
```

### ada83.c:58921

```
// Load and resolve a package specification
```

### ada83.c:58925

```
// Predefined library unit (resolved from the implementation's rts
```

### ada83.c:58926

```
// directory): the runtime itself provides its body. Captured before
```

### ada83.c:58927

```
// any recursive lookup overwrites the flag.
```

### ada83.c:58930

```
// Check if already loaded — a GENERIC library unit counts (reloading one
```

### ada83.c:58931

```
// from the catalog would shadow the in-file symbol whose template AST
```

### ada83.c:58932

```
// carries later graftings, e.g. a subunit completing its stub, c94008d).
```

### ada83.c:58936

```
// Already loaded
```

### ada83.c:58939

```
// Check for circular dependency (package currently being loaded)
```

### ada83.c:58941

```
// Break cycle - package will be available when outer load completes
```

### ada83.c:58944

```
// Try to load from cached ALI file first.
```

### ada83.c:58945

```
// If successful, we skip parsing entirely.
```

### ada83.c:58950

```
// Mark package as loading to detect cycles
```

### ada83.c:58953

```
// Parse the package (ALI not available or stale)
```

### ada83.c:58954

```
// ".ads"
```

### ada83.c:58957

```
// A source file may hold several compilation units (a library subprogram's
```

### ada83.c:58958

```
// spec sitting beside an independent one, ACATS fragments). Parse them in
```

### ada83.c:58959

```
// turn and keep the one whose defined name is the unit we were asked for —
```

### ada83.c:58960

```
// never assume it is the first. If none matches by name (a unit kind we do
```

### ada83.c:58961

```
// not key on), fall back to the first, preserving the single-unit behaviour.
```

### ada83.c:58981

```
// Recursively load WITH'd packages
```

### ada83.c:58984

```
// The spec's context (WITH clauses, Elaborate pragmas) feeds the
```

### ada83.c:58985

```
// elaboration graph here — a loaded spec's compilation unit never
```

### ada83.c:58986

```
// passes through code generation, only its body does.
```

### ada83.c:58989

```
// Resolve the package declarations
```

### ada83.c:58995

```
// Create package symbol
```

### ada83.c:59004

```
// Resolve the spec's USE clauses (RM 8.4): its declarations may
```

### ada83.c:59005

```
// name entities of WITH'd packages directly.
```

### ada83.c:59008

```
// Push package scope
```

### ada83.c:59011

```
// Resolve visible declarations (RM 3.4: subprograms declared here are
```

### ada83.c:59012

```
// derivable of the first kind).
```

### ada83.c:59018

```
// Populate package exports for qualified access (e.g., SYSTEM.MAX_INT)
```

### ada83.c:59021

```
// Resolve private declarations
```

### ada83.c:59026

```
// SYSTEM.ADDRESS override (RM 13.7):                                                         
```

### ada83.c:59027

```
// The SYSTEM package declares ADDRESS as NEW INTEGER (32-bit),                               
```

### ada83.c:59028

```
// but on 64-bit targets, addresses require 64 bits. Override                                
```

### ada83.c:59029

```
// the parsed type to match the compiler's internal type_address.                            
```

### ada83.c:59030

```
//                                                                                            
```

### ada83.c:59050

```
// Library-level subprogram (e.g., WITH CHECK_FILE where CHECK_FILE
```

### ada83.c:59051

```
// is `PROCEDURE CHECK_FILE (...) IS ... END;`). Register a
```

### ada83.c:59052

```
// SYMBOL_PROCEDURE / SYMBOL_FUNCTION so callers can resolve it.
```

### ada83.c:59053

```
// Resolve USE clauses from the loaded unit's context so types from
```

### ada83.c:59054

```
// WITH'd packages (e.g. TEXT_IO.FILE_TYPE) are visible when
```

### ada83.c:59055

```
// resolving the procedure's parameter types.
```

### ada83.c:59057

```
// Delegate to Resolve_Declaration which fully handles the body
```

### ada83.c:59058

```
// (param resolution, body declarations, statements). This sets
```

### ada83.c:59059

```
// unit->symbol as a side effect of NK_PROCEDURE_BODY handling.
```

### ada83.c:59062

```
// If the WITH'd unit is a body, queue it for codegen so the
```

### ada83.c:59063

```
// emitted .ll defines @check_file (etc.) rather than just
```

### ada83.c:59064

```
// declaring it extern. Skip mere spec compilation units.
```

### ada83.c:59073

```
// Generic unit: create SYMBOL_GENERIC with the inner spec
```

### ada83.c:59083

```
// Create generic symbol
```

### ada83.c:59090

```
// Store formals list for later instantiation
```

### ada83.c:59097

```
// Resolve the generic package spec's declarations so type/exception                        
```

### ada83.c:59098

```
// names are available when the body is parsed. Push scope, install                         
```

### ada83.c:59099

```
// generic formals, then resolve visible/private declarations.                             
```

### ada83.c:59100

```
//                                                                                          
```

### ada83.c:59104

```
// Install generic formal parameters (shared installer, RM 12.1)
```

### ada83.c:59107

```
// Resolve visible declarations
```

### ada83.c:59110

```
// Populate package exports for qualified access
```

### ada83.c:59113

```
// Resolve private declarations
```

### ada83.c:59121

```
// Done loading this package
```

### ada83.c:59124

```
// Some files (ACATS .ada convention) contain both spec and body
```

### ada83.c:59125

```
// in a single file. Keep parsing additional compilation units from
```

### ada83.c:59126

```
// the SAME parser to pick up the body when it follows the spec.
```

### ada83.c:59127

```
// This handles generic procedures/functions like LENGTH_CHECK where
```

### ada83.c:59128

```
// the body must attach to the SYMBOL_GENERIC created above so
```

### ada83.c:59129

```
// instantiation has a template body to clone.
```

### ada83.c:59145

```
// Process the body cu's WITH/USE clauses so types referenced
```

### ada83.c:59146

```
// by the body (e.g. UNCHECKED_CONVERSION) are visible.
```

### ada83.c:59150

```
// Resolve the body in the generic scope so identifiers bind to
```

### ada83.c:59151

```
// symbols. Mirrors the package-body branch below.
```

### ada83.c:59156

```
// Install spec's parameters
```

### ada83.c:59170

```
// Other subprogram units sharing the file (ACATS multi-unit
```

### ada83.c:59171

```
// convention) are separate compilations — never resolve or queue
```

### ada83.c:59172

```
// them while loading this unit.
```

### ada83.c:59178

```
// Spec and body in ONE file (ACATS convention): the body found
```

### ada83.c:59179

```
// here IS this unit's body — hand it to the body-loading section
```

### ada83.c:59180

```
// below instead of re-reading the file (which would re-find the
```

### ada83.c:59181

```
// spec and wrongly classify the package as bodyless).
```

### ada83.c:59187

```
// Resolve through the NK_PACKAGE_BODY case: its generic-completion
```

### ada83.c:59188

```
// path installs the formals, resolves the spec and the body
```

### ada83.c:59189

```
// (declarations, statement part, handlers), and records the body
```

### ada83.c:59190

```
// on the generic symbol. Assigning generic_body without resolving
```

### ada83.c:59191

```
// left every instantiation emitting from an unresolved AST.
```

### ada83.c:59199

```
// Also try to load the package body if available.
```

### ada83.c:59200

```
// Skip if a precompiled .ll file exists (package provided externally).
```

### ada83.c:59202

```
// Body already loaded
```

### ada83.c:59206

```
// A precompiled unit still constrains elaboration order: its ALI's
```

### ada83.c:59207

```
// recorded WITH set enters the graph even though no source loads
```

### ada83.c:59208

```
// (RM 10.5). Pure units record no elaboration dependency.
```

### ada83.c:59222

```
// Precompiled version will be linked in
```

### ada83.c:59224

```
// RM 10.3: an OPTIONAL body that became obsolete (one of its WITH'd
```

### ada83.c:59225

```
// specs was recompiled after it) is no longer part of the program —
```

### ada83.c:59226

```
// load the spec alone, even when the body shares the spec's file. A
```

### ada83.c:59227

```
// required body's obsolescence is reported by the completeness sweep.
```

### ada83.c:59257

```
// Parse the body - arena-allocate filename so AST Source_Locations stay valid
```

### ada83.c:59266

```
// Spec-only package — no body file, or the path lookup's .ada fallback
```

### ada83.c:59267

```
// handed back the spec itself: queue the SPEC for code generation so its
```

### ada83.c:59268

```
// package-level state is DEFINED in this module. The extern path would
```

### ada83.c:59269

```
// otherwise leave the globals undefined forever.
```

### ada83.c:59280

```
// RM 7.1/10.5: the spec requires a body the library has not
```

### ada83.c:59281

```
// compiled. Defining its elaboration here would mask that —
```

### ada83.c:59282

```
// record it instead: @main calls the elaboration as an external
```

### ada83.c:59283

```
// reference, satisfied by a later compilation of the body or
```

### ada83.c:59284

```
// failing execution when none exists.
```

### ada83.c:59296

```
// Library subprogram declaration without a compiled body: anchor
```

### ada83.c:59297

```
// it from @main so execution requires a later compilation to
```

### ada83.c:59298

```
// provide the body (RM 10.5).
```

### ada83.c:59308

```
// Recursively load WITH'd packages from body
```

### ada83.c:59311

```
// The body's own context also carries elaboration edges.
```

### ada83.c:59314

```
// Resolve the body's USE clauses so the WITH'd packages' contents
```

### ada83.c:59315

```
// are directly visible while the body resolves (RM 8.4).
```

### ada83.c:59318

```
// Look up the package symbol
```

### ada83.c:59323

```
// Link body to spec
```

### ada83.c:59327

```
// For generic packages, store the body for later instantiation
```

### ada83.c:59332

```
// Resolve the body within package scope
```

### ada83.c:59335

```
// Install visible and private declarations from package spec
```

### ada83.c:59336

```
// into the body's scope (RM 7.1, 7.2)
```

### ada83.c:59338

```
// For generics, install formals first, then look at the unit
```

### ada83.c:59367

```
// Install visible declarations
```

### ada83.c:59373

```
// Install private declarations
```

### ada83.c:59380

```
// Formals and spec entities are in scope: resolve IS-name
```

### ada83.c:59381

```
// formal-subprogram defaults (RM 12.1.3).
```

### ada83.c:59390

```
// Freeze types and resolve the statement part and handlers,
```

### ada83.c:59391

```
// exactly like the NK_PACKAGE_BODY resolution case: a generic
```

### ada83.c:59392

```
// body's statement sequence executes at every instantiation
```

### ada83.c:59393

```
// (RM 12.3), so its statements must be resolved here or the
```

### ada83.c:59394

```
// instantiation emitter has nothing legal to emit.
```

### ada83.c:59407

```
// Store for code generation
```

### ada83.c:59415

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59416

```
// §18. VECTOR PATHS - SIMD-Accelerated Scanning on x86-64 and ARM64                                
```

### ada83.c:59417

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59420

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59421

```
// §18.1 Runtime CPU Feature Detection                                                              
```

### ada83.c:59422

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59424

```
// Runtime CPU feature detection for x86-64. AVX-512 code paths are only compiled when the
```

### ada83.c:59425

```
// toolchain is invoked with -mavx512bw; without that flag we fall back to AVX2 or scalar.
```

### ada83.c:59428

```
// -1 = unchecked, 0 = absent, 1 = present
```

### ada83.c:59431

```
// Permanently disabled: not compiled with AVX-512
```

### ada83.c:59435

```
// Already detected, nothing to do
```

### ada83.c:59438

```
// CPUID leaf 07H, sub-leaf 0: structured extended feature flags. AVX2 is EBX bit 5.
```

### ada83.c:59450

```
// AVX-512F is EBX bit 16, AVX-512BW is EBX bit 30 - both are required.
```

### ada83.c:59456

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59457

```
// §18.2 SIMD-Accelerated Scanning Functions                                                        
```

### ada83.c:59458

```
//                                                                                                  
```

### ada83.c:59459

```
// Four architecture paths: x86-64 (AVX-512/AVX2/SSE4.2), ARM64 (NEON), generic scalar fallback.   
```

### ada83.c:59460

```
// Each function scans for interesting bytes (whitespace boundaries, end-of-identifier, etc.)       
```

### ada83.c:59461

```
// without character-by-character loops.                                                           
```

### ada83.c:59462

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59466

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59467

```
// x86-64 AVX-512/AVX2 SIMD Lexer Acceleration                                                      
```

### ada83.c:59468

```
//                                                                                                  
```

### ada83.c:59469

```
//   AVX-512BW - 64-byte vector processing with k-mask registers                                    
```

### ada83.c:59470

```
//   AVX2      - 32-byte processing with optimised instruction scheduling                           
```

### ada83.c:59471

```
//   BMI2      - TZCNT for fast "find first non-matching byte" within a mask                        
```

### ada83.c:59472

```
//                                                                                                  
```

### ada83.c:59473

```
// Scanning functions:                                                                              
```

### ada83.c:59474

```
//   Simd_Skip_Whitespace  - skip space (0x20) and C0 controls (0x09–0x0D)                          
```

### ada83.c:59475

```
//   Simd_Find_Char_X86    - generic single-character search (newline, quotes)                      
```

### ada83.c:59476

```
//   Simd_Scan_Identifier  - match the [A-Za-z0-9_] character class                                 
```

### ada83.c:59477

```
//   Simd_Scan_Digits      - match [0-9_] for numeric literals                                      
```

### ada83.c:59478

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59480

```
// BMI2 TZCNT (trailing zero count) - returns the index of the lowest set bit.
```

### ada83.c:59492

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59493

```
// AVX-512 whitespace skip - 64 bytes at a time using k-mask registers. Matches space (0x20)       
```

### ada83.c:59494

```
// and the C0 control range 0x09–0x0D (tab, LF, VT, FF, CR). Only compiled with -mavx512bw.       
```

### ada83.c:59495

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59502

```
// Prefetch next cache line
```

### ada83.c:59503

```
// Load 64 bytes
```

### ada83.c:59504

```
// Broadcast space char
```

### ada83.c:59505

```
// k1 = (c == ' ')
```

### ada83.c:59506

```
// Broadcast 0x08
```

### ada83.c:59507

```
// Broadcast 0x0E
```

### ada83.c:59508

```
// k2 = (c > 0x08)
```

### ada83.c:59509

```
// k3 = (0x0E > c)
```

### ada83.c:59510

```
// k2 = in range [0x09,0x0D]
```

### ada83.c:59511

```
// k0 = whitespace mask
```

### ada83.c:59512

```
// Extract to GPR
```

### ada83.c:59519

```
// If any non-whitespace byte was found, return its position.
```

### ada83.c:59529

```
// __AVX512BW__
```

### ada83.c:59531

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59532

```
// AVX2 Whitespace Skip with 2x Unrolling                                                           
```

### ada83.c:59533

```
//                                                                                                  
```

### ada83.c:59534

```
// Processes 64 bytes (two 32-byte YMM loads) per iteration for better throughput on long runs of   
```

### ada83.c:59535

```
// whitespace. Falls back to a single 32-byte pass for the remaining tail before returning to the  
```

### ada83.c:59536

```
// scalar loop in the dispatcher.                                                                  
```

### ada83.c:59537

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59541

```
// 2x unrolled: process 64 bytes per iteration
```

### ada83.c:59547

```
// Load two 32-byte chunks in parallel
```

### ada83.c:59551

```
// Broadcast constants once, reuse for both chunks
```

### ada83.c:59559

```
// First chunk: space match, range check 0x09..0x0D
```

### ada83.c:59564

```
// Second chunk: same comparisons on the next 32 bytes
```

### ada83.c:59569

```
// Combine results: whitespace = space OR (> 0x08 AND < 0x0E)
```

### ada83.c:59575

```
// Extract per-byte masks
```

### ada83.c:59590

```
// Handle remaining 32-byte chunk
```

### ada83.c:59619

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59620

```
// Whitespace Skip Dispatcher                                                                       
```

### ada83.c:59621

```
//                                                                                                  
```

### ada83.c:59622

```
// Selects the best available SIMD path at runtime based on CPU features detected by                
```

### ada83.c:59623

```
// Simd_Detect_Features, then falls through to a scalar tail for the last few bytes.               
```

### ada83.c:59624

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59637

```
// Scalar tail for remaining bytes
```

### ada83.c:59646

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59647

```
// Generic Single-Character Search                                                                  
```

### ada83.c:59648

```
//                                                                                                  
```

### ada83.c:59649

```
// Searches for the first occurrence of a specific byte (newline, quote, etc.) in the given range. 
```

### ada83.c:59650

```
// The first 16 bytes are checked with a scalar fast path that covers most short comments and       
```

### ada83.c:59651

```
// string literals; beyond that the function dispatches to AVX-512 or AVX2 SIMD loops. Used as     
```

### ada83.c:59652

```
// the building block for Simd_Find_Newline, Simd_Find_Quote, and Simd_Find_Double_Quote.          
```

### ada83.c:59653

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59657

```
// Fast path: scalar check for first 16 bytes (covers most short comments/strings)
```

### ada83.c:59662

```
// Short remaining buffer - finish with scalar
```

### ada83.c:59668

```
// Fast path didn't find it, continue with SIMD
```

### ada83.c:59672

```
// AVX-512: 64 bytes at a time
```

### ada83.c:59694

```
// AVX2: 32 bytes at a time
```

### ada83.c:59712

```
// Scalar tail
```

### ada83.c:59717

```
// Convenience wrappers for common character searches
```

### ada83.c:59728

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59729

```
// Identifier Character Class Scanner                                                               
```

### ada83.c:59730

```
//                                                                                                  
```

### ada83.c:59731

```
// Uses a fast unrolled table lookup for the first 8 characters, which covers the vast majority of  
```

### ada83.c:59732

```
// Ada identifiers. Longer identifiers continue with a scalar table-driven loop - the branch       
```

### ada83.c:59733

```
// predictor handles this well since identifiers rarely exceed 8 characters.                       
```

### ada83.c:59734

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59738

```
// Fast path: unrolled table lookup for first 8 chars (covers most identifiers)
```

### ada83.c:59748

```
// Long identifier (> 8 chars) - continue with table lookup
```

### ada83.c:59754

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59755

```
// Digit Scanner for Numeric Literals                                                               
```

### ada83.c:59756

```
//                                                                                                  
```

### ada83.c:59757

```
// Matches the character class [0-9_] - decimal digits with optional underscores, which is the Ada  
```

### ada83.c:59758

```
// numeric literal syntax (LRM §2.4). Returns a pointer to the first character that is neither a   
```

### ada83.c:59759

```
// digit nor an underscore.                                                                        
```

### ada83.c:59760

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59766

```
// AVX-512 path
```

### ada83.c:59775

```
// byte > '0'-1
```

### ada83.c:59776

```
// '9'+1 > byte
```

### ada83.c:59777

```
// digit range
```

### ada83.c:59778

```
// underscore
```

### ada83.c:59779

```
// digit OR underscore
```

### ada83.c:59797

```
// AVX2 path
```

### ada83.c:59826

```
// Scalar tail
```

### ada83.c:59832

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59833

```
// ARM64 NEON Implementation (raw inline assembly)                                                  
```

### ada83.c:59834

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:59840

```
// Load 16 bytes
```

### ada83.c:59842

```
// Check for space (0x20)
```

### ada83.c:59846

```
// Check range 0x09..0x0D: byte > 0x08 and byte < 0x0E
```

### ada83.c:59851

```
// in range
```

### ada83.c:59853

```
// Combine: whitespace = space OR in_range, then invert for non-whitespace
```

### ada83.c:59867

```
// Scalar tail
```

### ada83.c:59901

```
// Check a-z
```

### ada83.c:59908

```
// Check A-Z
```

### ada83.c:59915

```
// Check 0-9
```

### ada83.c:59922

```
// Check underscore
```

### ada83.c:59926

```
// Combine: valid = lower | upper | digit | underscore, then invert
```

### ada83.c:59996

```
// Check 0-9
```

### ada83.c:60003

```
// Check underscore
```

### ada83.c:60007

```
// Combine and invert
```

### ada83.c:60025

```
// Generic Scalar Implementation (Portable Fallback)                                                
```

### ada83.c:60026

```
//                                                                                                  
```

### ada83.c:60027

```
// Reference implementation for platforms without SIMD support. All SIMD paths must produce        
```

### ada83.c:60028

```
// identical results to these scalar functions for all possible inputs.                            
```

### ada83.c:60029

```
//                                                                                                  
```

### ada83.c:60064

```
// SIMD architecture selection
```

### ada83.c:60066

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:60067

```
// §18.3 SIMD-Accelerated Decimal Parsing                                                           
```

### ada83.c:60068

```
//                                                                                                  
```

### ada83.c:60069

```
// Instead of processing one digit at a time (multiply by 10, add digit), the SIMD path batches     
```

### ada83.c:60070

```
// eight digits at once (multiply by 10**8, add the 8-digit value).                                
```

### ada83.c:60071

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:60075

```
// Parse exactly 8 ASCII digit characters ('0'–'9') into a 32-bit integer in the range
```

### ada83.c:60076

```
// [0 .. 99_999_999] using AVX2 multiply-add instructions (see §6.1 header for the algorithm).
```

### ada83.c:60081

```
// Load 8 bytes into low portion of xmm0
```

### ada83.c:60084

```
// Broadcast '0' and subtract to get digit values
```

### ada83.c:60089

```
// Multiply-add bytes to words: weights [10,1,10,1,10,1,10,1]
```

### ada83.c:60093

```
// Multiply-add words to dwords: weights [100,1,100,1]
```

### ada83.c:60097

```
// Extract two dwords and combine: dw0 * 10000 + dw1
```

### ada83.c:60105

```
// byte weights [10,1,10,1,10,1,10,1]  (0x0A = 10)
```

### ada83.c:60106

```
// word weights [100,1,100,1]
```

### ada83.c:60112

```
// Parse up to 16 ASCII digits from the buffer [cursor .. limit) using AVX2. Validates that all    
```

### ada83.c:60113

```
// bytes are ASCII digits first; returns the number of digits actually consumed and stores the      
```

### ada83.c:60114

```
// parsed value in *out.                                                                           
```

### ada83.c:60115

```
//                                                                                                  
```

### ada83.c:60119

```
// Fewer than 8 digits available - fall back to scalar.
```

### ada83.c:60131

```
// Validate that the first eight bytes are all ASCII digits using SIMD comparison.
```

### ada83.c:60150

```
// Not all eight are digits - fall back to scalar.
```

### ada83.c:60163

```
// If 16 digits are available, validate the second group of eight.
```

### ada83.c:60188

```
// Only the first eight digits were valid.
```

### ada83.c:60192

```
// SIMD_X86_64
```

### ada83.c:60194

```
// Convert a decimal digit string (with optional leading sign) to a Big_Integer. Uses the AVX2
```

### ada83.c:60195

```
// SIMD path when available to process eight or sixteen digits at a time.
```

### ada83.c:60201

```
// Skip leading zeros.
```

### ada83.c:60210

```
// Locate the end of the digit run.
```

### ada83.c:60222

```
// When eight or more digits remain, try the SIMD batch path.
```

### ada83.c:60246

```
// Scalar fallback: one digit at a time.
```

### ada83.c:60257

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:60258

```
// §19. DRIVER                                                                                      
```

### ada83.c:60259

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:60262

```
// The library identity of a compilation unit (RM 10.1): its unit name plus
```

### ada83.c:60263

```
// whether it stands as the unit's SPEC, its BODY, or both — a subprogram
```

### ada83.c:60264

```
// body or a library instantiation serves as its own spec. Subunits replace
```

### ada83.c:60265

```
// by dotted name through the catalog, not through this in-set scan.
```

### ada83.c:60280

```
// A body never REPLACES a declaration of its name — it completes one
```

### ada83.c:60281

```
// (a generic subprogram's body after the generic declaration,
```

### ada83.c:60282

```
// c23006e). It stands as its own spec only when no declaration
```

### ada83.c:60283

```
// precedes it, which the replacement scan needs no flag for: body
```

### ada83.c:60284

```
// replaces body, and a later spec obsoletes it.
```

### ada83.c:60310

```
// Reset loaded bodies for this compilation, then prime the program
```

### ada83.c:60311

```
// library catalog (RM 10.4) from the output directory's ALI files —
```

### ada83.c:60312

```
// units compiled earlier into the same library become findable by name
```

### ada83.c:60313

```
// even when their file names match nothing (ACATS fragments, subunits).
```

### ada83.c:60337

```
// Parse all compilation units in the file
```

### ada83.c:60350

```
// Cloner self-check: resolution and emission proceed on structural deep
```

### ada83.c:60351

```
// clones of the parsed units. A NULL instance makes the clone substitution-
```

### ada83.c:60352

```
// free, so the output must match a normal compile exactly.
```

### ada83.c:60357

```
// RM 10.1: within one compile set a later compilation unit REPLACES an
```

### ada83.c:60358

```
// earlier one of the same identity, and a later spec makes an earlier
```

### ada83.c:60359

```
// body of the unit obsolete. Replaced units vanish from the compilation —
```

### ada83.c:60360

```
// never resolved, never emitted (ca5004b: PACKAGE CA5004B0 recompiled
```

### ada83.c:60361

```
// with a new spec drops both the old spec and its now-obsolete body).
```

### ada83.c:60375

```
// same category recompiled
```

### ada83.c:60377

```
// new spec obsoletes the body
```

### ada83.c:60384

```
// Semantic analysis for all units
```

### ada83.c:60390

```
// RM 10.3/10.5: a main-program compilation is the bind point — sweep
```

### ada83.c:60391

```
// the library closure for obsolete bodies, obsolete subunits, missing
```

### ada83.c:60392

```
// generic bodies, and unsatisfied stubs in loaded dependencies.
```

### ada83.c:60408

```
// A subunit compilation (SEPARATE (parent) ...) emits ONLY the
```

### ada83.c:60409

```
// subunit's own code under its stable stub name: the parent chain and
```

### ada83.c:60410

```
// every WITH-loaded unit are already defined in the modules that
```

### ada83.c:60411

```
// compiled them, and re-emitting them here would collide at link time.
```

### ada83.c:60412

```
// A file can also hold the parent AND its subunits together (single-file
```

### ada83.c:60413

```
// programs): then everything is in-module and this is an ordinary
```

### ada83.c:60414

```
// whole-program compile. Subunit mode applies only when some subunit's
```

### ada83.c:60415

```
// parent is NOT compiled by this invocation.
```

### ada83.c:60416

```
//
```

### ada83.c:60417

```
// The parent is located purely by symbol identity; it may itself be a
```

### ada83.c:60418

```
// subunit (a nested subunit's immediate parent is another subunit stub),
```

### ada83.c:60419

```
// so do not require the in-file parent to be a non-separate library unit.
```

### ada83.c:60420

```
// In a genuine separate compile the parent symbol is simply absent from
```

### ada83.c:60421

```
// this file, so parent_in_file stays false and subunit mode still applies.
```

### ada83.c:60443

```
// Code generation
```

### ada83.c:60455

```
// Output to stdout if no -o specified
```

### ada83.c:60459

```
// WITH'd library subprograms without compiled bodies still need a
```

### ada83.c:60460

```
// declaration with the correct ABI for their @main presence anchors.
```

### ada83.c:60472

```
// Generate code for loaded package bodies (e.g., TEXT_IO) — except in
```

### ada83.c:60473

```
// a subunit compilation, where the loaded parent chain is context only.
```

### ada83.c:60482

```
// Declare package-stub elaboration functions another module defines —
```

### ada83.c:60483

```
// skipped when the subunit's define landed in this module.
```

### ada83.c:60492

```
// Declare callees living across a SEPARATE boundary: noted at their
```

### ada83.c:60493

```
// call sites, still without a define after all generation. The
```

### ada83.c:60494

```
// declaration mirrors the call ABI — static-link pointer first for
```

### ada83.c:60495

```
// nested callees, by-reference composites as ptr.
```

### ada83.c:60502

```
// Package-level data defined in another module (subunit package
```

### ada83.c:60503

```
// state): external global at the type's representation.
```

### ada83.c:60528

```
// Note: Derived type operations (RM 3.4) don't need wrapper functions.                          
```

### ada83.c:60529

```
// Derived types have identical representation to parent types in Ada 83,                         
```

### ada83.c:60530

```
// so calls to derived operations are emitted directly to the parent's                            
```

### ada83.c:60531

```
// implementation (GNAT-style optimization). See call_target handling                             
```

### ada83.c:60532

```
// in Generate_Apply ().reg.                                                                         
```

### ada83.c:60533

```
//                                                                                                
```

### ada83.c:60535

```
// Emit address marker globals for 'ADDRESS attribute on packages/generics
```

### ada83.c:60543

```
// ═══════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:60544

```
// Emit @main() with Standard-style elaboration order (§15)                                       
```

### ada83.c:60545

```
//                                                                                                
```

### ada83.c:60546

```
// Per Ada RM 10.2, library units must be elaborated in a safe order                              
```

### ada83.c:60547

```
// before the main subprogram executes. The elaboration model (§15)                               
```

### ada83.c:60548

```
// computes this order using dependency analysis and topological sort.                           
```

### ada83.c:60549

```
//                                                                                                
```

### ada83.c:60550

```
// The algorithm respects:                                                                        
```

### ada83.c:60551

```
//   - WITH clause dependencies                                                                   
```

### ada83.c:60552

```
//   - pragma Elaborate / Elaborate_All                                                           
```

### ada83.c:60553

```
//   - Spec-before-body ordering                                                                  
```

### ada83.c:60554

```
//   - Preelaborate / Pure unit optimizations                                                     
```

### ada83.c:60555

```
// ═══════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:60556

```
// Compute elaboration order using the dependency graph algorithm                                 
```

### ada83.c:60557

```
//                                                                                                
```

### ada83.c:60562

```
// RM 10.5: if no consistent elaboration order exists, the program
```

### ada83.c:60563

```
// is illegal — emitting an arbitrary order would silently run a
```

### ada83.c:60564

```
// program the language forbids.
```

### ada83.c:60574

```
// RM 10.5: WITH'd packages whose required body the library has not
```

### ada83.c:60575

```
// compiled — elaboration resolves against an external definition (a
```

### ada83.c:60576

```
// later compilation) or execution is rejected.
```

### ada83.c:60583

```
// Sink for the RM 10.5 presence anchors below — private per module,
```

### ada83.c:60584

```
// so multi-module links never collide on it.
```

### ada83.c:60588

```
// Create every per-thread pthread key while the program is still
```

### ada83.c:60589

```
// single-threaded; everything after simply loads the key globals.
```

### ada83.c:60590

```
// (Lazy per-key creation raced: two task threads at the program's
```

### ada83.c:60591

```
// first raise each ran pthread_key_create, the loser's key orphaning
```

### ada83.c:60592

```
// the winner's thread-specific values — a raise then read exception
```

### ada83.c:60593

```
// id 0 and fell into WHEN OTHERS, c95022b under load.)
```

### ada83.c:60596

```
// Call elaboration functions in computed dependency order.
```

### ada83.c:60597

```
// Prefer the graph-computed order; fall back to source order.
```

### ada83.c:60603

```
// A library-level body's RM 3.9 elaboration flag sets at ITS slot in
```

### ada83.c:60604

```
// this sequence (deferred from module-scope generation, where a store
```

### ada83.c:60605

```
// is not legal IR). NULL emits every store not yet emitted — the
```

### ada83.c:60606

```
// closing sweep for units the order walk did not cover.
```

### ada83.c:60633

```
// No silent drops: any registered elaboration function the graph
```

### ada83.c:60634

```
// did not cover (a unit kind the graph does not model yet) still
```

### ada83.c:60635

```
// runs, in source order, after the graph-ordered units.
```

### ada83.c:60643

```
// Fallback to source order (old behavior)
```

### ada83.c:60651

```
// Library-body elaboration flags the order walk did not place.
```

### ada83.c:60659

```
// RM 10.5 presence anchors: a WITH'd library subprogram without a
```

### ada83.c:60660

```
// compiled body must fail the program before it runs, even when
```

### ada83.c:60661

```
// nothing calls it. The ptrtoint forces symbol resolution when
```

### ada83.c:60662

```
// @main materializes; a later compilation linking the definition
```

### ada83.c:60663

```
// satisfies it. (Missing SUBUNITS are rejected earlier — by the
```

### ada83.c:60664

```
// completeness sweep at compile time and the binder's ST checks.)
```

### ada83.c:60666

```
// A bare ptrtoint is dead code the JIT never resolves; the
```

### ada83.c:60667

```
// volatile store makes each anchor live.
```

### ada83.c:60681

```
// RM 9.4: the environment task waits for all dependent tasks to terminate
```

### ada83.c:60682

```
// before the program completes. Without this, main exits while task threads
```

### ada83.c:60683

```
// are still live, crashing during C runtime teardown.
```

### ada83.c:60690

```
// Emit tracked exception globals that weren't defined in the header.                            
```

### ada83.c:60691

```
// This handles instance-prefixed exceptions from generic instantiations                          
```

### ada83.c:60692

```
// (e.g., @__exc.seq_io__status_error_s0 from SEQ_IO.STATUS_ERROR).                              
```

### ada83.c:60693

```
//                                                                                                
```

### ada83.c:60698

```
// Check if already defined by Generate_Exception_Globals (in header).
```

### ada83.c:60699

```
// Compare against each declared exception's mangled name.
```

### ada83.c:60715

```
// Also check standard exceptions
```

### ada83.c:60734

```
// Generate GNAT-compatible .ali file for dependency tracking
```

### ada83.c:60740

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:60741

```
// Derive output .ll path from input path by replacing extension.                                  
```

### ada83.c:60742

```
// Writes into caller-supplied buffer.                                                             
```

### ada83.c:60743

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:60751

```
// Only replace if the dot is after the last slash (i.e., part of filename)
```

### ada83.c:60757

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:60758

```
// Parallel compilation - fork-based worker called from a pthread.                                 
```

### ada83.c:60759

```
//                                                                                                  
```

### ada83.c:60760

```
// Each thread forks a child process that compiles one file. fork() gives                          
```

### ada83.c:60761

```
// complete isolation of all global state (arena, error count, loaded                               
```

### ada83.c:60762

```
// packages, etc.) without refactoring Compile_File.                                               
```

### ada83.c:60763

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:60775

```
// Child - compile and exit. Cross-compilation globals inherited from
```

### ada83.c:60776

```
// the parent reset first: a worker must see the same state a fresh
```

### ada83.c:60777

```
// single-file invocation would (stale ALI cache entries and a
```

### ada83.c:60778

```
// pre-populated elaboration graph would otherwise leak in).
```

### ada83.c:60815

```
// Bind mode (RM 10.3/10.5): `--bind <libdir> <main-unit>` verifies the
```

### ada83.c:60816

```
// post-compilation consistency of the main program's library closure —
```

### ada83.c:60817

```
// the analogue of GNAT's gnatbind, run by the build after all compiles.
```

### ada83.c:60831

```
// NULL means derive from input name
```

### ada83.c:60833

```
// Parse command-line arguments
```

### ada83.c:60870

```
// Native mode: exactly one Ada source plus any number of pre-compiled
```

### ada83.c:60871

```
// .ll modules to link in-process.
```

### ada83.c:60883

```
// more than one Ada source
```

### ada83.c:60895

```
// ── Auto-discover rts path from executable location ────────────────────────────────────────────
```

### ada83.c:60903

```
// Fallback: use argv[0]
```

### ada83.c:60923

```
// ── Auto-discover input file's directory as include path ───────────────────────────────────────
```

### ada83.c:60938

```
// Add current directory to include paths by default
```

### ada83.c:60942

```
// ── Compile ────────────────────────────────────────────────────────────────────────────────────
```

### ada83.c:60943

```
// Single file - existing sequential behaviour
```

### ada83.c:60964

```
// RM 10.1: the order of a multi-file compilation matters when a later
```

### ada83.c:60965

```
// file depends on (WITHs, SEPARATEs from, or recompiles a unit of) an
```

### ada83.c:60966

```
// earlier one — the earlier file's library entries must exist before
```

### ada83.c:60967

```
// the later file compiles. Decide by a parse-only pre-scan: collect
```

### ada83.c:60968

```
// each file's defined unit names and referenced unit names, then check
```

### ada83.c:60969

```
// every later file against every earlier file's definitions. Dependent
```

### ada83.c:60970

```
// sets compile sequentially (each child primes its catalog from the
```

### ada83.c:60971

```
// ALIs the previous children wrote); independent sets keep the
```

### ada83.c:60972

```
// fork-per-file parallelism.
```

### ada83.c:60975

```
// A file overflowing the scan tables forces the conservative
```

### ada83.c:60976

```
// (sequential) order rather than silently dropping a dependency.
```

### ada83.c:61024

```
// A subunit references its parent chain's root unit.
```

### ada83.c:61072

```
// recompilation replaces (RM 10.3)
```

### ada83.c:61078

```
// Multiple files - parallel compilation using pthreads + fork.
```

### ada83.c:61079

```
// Order-sensitive builds run one file at a time, in command-line order.
```

### ada83.c:61091

```
// derive from input
```

### ada83.c:61109

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:61110

```
// §20. NATIVE BACKEND - In-Process IR -> Executable via Runtime-Loaded LLVM-C
```

### ada83.c:61111

```
// ═════════════════════════════════════════════════════════════════════════════════════════════════
```

### ada83.c:61127

```
// The slice of LLVM-C this backend drives. All of these have been ABI-stable
```

### ada83.c:61128

```
// across every LLVM major release this probe list covers; they are declared
```

### ada83.c:61129

```
// here precisely so that NO LLVM header (and no build-time LLVM at all) is
```

### ada83.c:61130

```
// needed to build the compiler.
```

### ada83.c:61131

```
// LLVMBool: zero is success
```

### ada83.c:61164

```
// LLVMErrorRef; null OK
```

### ada83.c:61165

```
// consumes the error
```

### ada83.c:61170

```
// consumes source
```

### ada83.c:61173

```
// The emitted runtime targets the build host (§18 doctrine), so the backend
```

### ada83.c:61174

```
// initializes exactly the host's target family.
```

### ada83.c:61181

```
// Optional embedded libLLVM (Windows single-file distribution): the build
```

### ada83.c:61182

```
// may link LLVM-C.dll into the executable as a binary blob (`make
```

### ada83.c:61183

```
// embed-llvm`, which objcopys tools/win64/LLVM-C.dll); these weak symbols
```

### ada83.c:61184

```
// are non-null exactly when it did. First use extracts the blob — once,
```

### ada83.c:61185

```
// size-tagged, atomically via rename — into %LOCALAPPDATA%\ada83 and
```

### ada83.c:61186

```
// loads it from there. Extraction is the supported way to run a DLL
```

### ada83.c:61187

```
// carried inside an executable: manually mapping it from memory would
```

### ada83.c:61188

```
// bypass the Windows loader (TLS, dependency resolution, unwind tables).
```

### ada83.c:61207

```
// already extracted by an earlier run
```

### ada83.c:61218

```
// Losing an extraction race to a concurrent run is fine iff the
```

### ada83.c:61219

```
// winner's copy is in place.
```

### ada83.c:61227

```
// Probe order: explicit override, the unversioned dev symlink, then recent
```

### ada83.c:61228

```
// major versions in both sonaming styles (libLLVM.so.N and Debian's
```

### ada83.c:61229

```
// libLLVM-N.so), then the platform dylib/DLL names. On Windows, LoadLibrary
```

### ada83.c:61230

```
// searches the executable's own directory first — shipping LLVM-C.dll next
```

### ada83.c:61231

```
// to ada83.exe is the complete install story.
```

### ada83.c:61315

```
// Link one object file into an executable with the first system C compiler
```

### ada83.c:61316

```
// found — the only pipeline step with no stable LLVM C API (lld), and the
```

### ada83.c:61317

```
// one tool every development machine already has.
```

### ada83.c:61332

```
// Exit 127 = shell could not find the tool: try the next probe.
```

### ada83.c:61333

```
// Any other failure is a real link error from a real linker.
```

### ada83.c:61367

```
// Parse_Ir consumes the buffer regardless of outcome.
```

### ada83.c:61373

```
// Additional IR modules (separately compiled units, e.g. a library's
```

### ada83.c:61374

```
// .ll) — the in-process equivalent of llvm-link. Link_Modules consumes
```

### ada83.c:61375

```
// the source module on success and failure alike.
```

### ada83.c:61401

```
// 3 = aggressive, 2 = PIC, 0 = default code model.
```

### ada83.c:61420

```
// 1 = object file.
```

## Makefile

### Makefile:1

```
# Ada83 compiler. `make` is the whole story:
```

### Makefile:2

```
#   - builds ./ada83 (one C file; LLVM is never a build-time dependency)
```

### Makefile:3

```
#   - on Linux, best-effort provisions the runtime-loaded libLLVM that
```

### Makefile:4

```
#     `--native` uses, probing the distribution's package manager
```

### Makefile:5

```
#   - on Windows (MSYS2), decompresses the vendored libLLVM under
```

### Makefile:6

```
#     tools/win64/ and embeds it into ada83.exe, making the executable a
```

### Makefile:7

```
#     fully standalone single-file toolchain
```

### Makefile:24

```
# The vendored DLL is upstream MSYS2's libLLVM, unmodified, recompressed
```

### Makefile:25

```
# alone so the repository carries only the one file --native requires
```

### Makefile:26

```
# (provenance and checksum: tools/win64/README.md). zstd is always present
```

### Makefile:27

```
# under MSYS2 — pacman itself depends on it. objcopy turns the DLL into a
```

### Makefile:28

```
# linkable blob; §20's weak symbols pick it up and extract it to
```

### Makefile:29

```
# %LOCALAPPDATA% on first --native use.
```

### Makefile:36

```
# Provision libLLVM for --native (runtime-dlopen'd; never a build
```

### Makefile:37

```
# dependency). Failure is non-fatal — --native prints an actionable hint.
```

